- 2026-07-04: Environment hardening slice `#3580` shipped on the `#3214` lane: the Studio-host real-sample round-trip family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `ScopedDefaultLocaleCatalogEnvironment` wrappers in `test_studio_host_real_sample_round_trip.cpp`, `test_studio_host_real_sample_object_round_trip.cpp`, `test_studio_host_real_sample_object_memo_round_trip.cpp`, `test_studio_host_real_sample_section_round_trip.cpp`, and `test_studio_host_real_sample_grouping_round_trip.cpp`. The refactor preserves the existing `COPPERFIN_VFP9_REPORTS_ROOT` override plus mounted-sample probing behavior, removes another Windows-risky duplicated locale-wrapper family without product-code changes, and keeps the real-sample JSON and round-trip contracts unchanged. Focused builds/tests passed for all five affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3579` shipped on the `#3214` lane: the remaining selected page Studio-host family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_selected_page_footer_objects_stable.cpp`, `test_studio_host_selected_page_footer_sections_record.cpp`, `test_studio_host_selected_page_footer_sections_stable.cpp`, `test_studio_host_selected_page_header_objects_orphaned.cpp`, and `test_studio_host_selected_page_header_objects_stable.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the selected-page JSON contracts unchanged. Focused builds/tests passed for all five affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3578` shipped on the `#3214` lane: the Studio-host selected group header/footer family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_selected_group_footer_objects_stable.cpp`, `test_studio_host_selected_group_footer_sections_record.cpp`, `test_studio_host_selected_group_footer_sections_stable.cpp`, `test_studio_host_selected_group_header_objects_stable.cpp`, `test_studio_host_selected_group_header_sections_record.cpp`, and `test_studio_host_selected_group_header_sections_stable.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the selected-group JSON contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3577` shipped on the `#3214` lane: the Studio-host selected column header/footer family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_selected_column_footer_objects_stable.cpp`, `test_studio_host_selected_column_footer_sections_record.cpp`, `test_studio_host_selected_column_footer_sections_stable.cpp`, `test_studio_host_selected_column_header_objects_stable.cpp`, `test_studio_host_selected_column_header_sections_record.cpp`, and `test_studio_host_selected_column_header_sections_stable.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the selected-column JSON contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3576` shipped on the `#3214` lane: the Studio-host deleted object batch family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_deleted_object_duplicate_batch.cpp`, `test_studio_host_deleted_object_duplicate_batch_errors.cpp`, `test_studio_host_deleted_object_rename_batch.cpp`, `test_studio_host_deleted_object_rename_batch_errors.cpp`, `test_studio_host_deleted_object_reorder_batch.cpp`, and `test_studio_host_deleted_object_reorder_batch_errors.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the deleted object batch JSON contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables, with the regex also covering the nearby deleted-object section batch variants.
- 2026-07-04: Environment hardening slice `#3575` shipped on the `#3214` lane: the Studio-host selected object/section family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_selected_report_objects.cpp`, `test_studio_host_selected_report_sections.cpp`, `test_studio_host_selected_label_objects.cpp`, `test_studio_host_selected_label_sections.cpp`, and `test_studio_host_selected_objects_stable.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the selected object/section JSON contracts unchanged. Focused builds/tests passed for all five affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3574` shipped on the `#3214` lane: the Studio-host object batch family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_object_duplicate_batch.cpp`, `test_studio_host_object_duplicate_batch_errors.cpp`, `test_studio_host_object_rename_batch.cpp`, `test_studio_host_object_rename_batch_errors.cpp`, `test_studio_host_object_reorder_batch.cpp`, and `test_studio_host_object_reorder_batch_errors.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the object batch JSON contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3573` shipped on the `#3214` lane: the Studio-host deleted-state family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_object_deleted_states.cpp`, `test_studio_host_object_deleted_states_errors.cpp`, `test_studio_host_mixed_deleted_states.cpp`, `test_studio_host_mixed_deleted_states_errors.cpp`, `test_studio_host_subtree_deleted_state.cpp`, and `test_studio_host_subtree_deleted_state_errors.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the deleted-state JSON contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3572` shipped on the `#3214` lane: the Studio-host section deleted-state and section delete/restore selection family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_settings_section_deleted_states.cpp`, `test_studio_host_settings_section_deleted_states_errors.cpp`, `test_studio_host_section_delete_record_selection.cpp`, `test_studio_host_section_delete_stable_selection.cpp`, `test_studio_host_section_restore_record_selection.cpp`, `test_studio_host_section_restore_stable_selection.cpp`, and `test_studio_host_section_deleted_object_counts.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the section deleted-state and selection JSON contracts unchanged. Focused builds/tests passed for all seven affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3571` shipped on the `#3214` lane: the Studio-host settings restore/delete/memo family now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_settings_restore_record.cpp`, `test_studio_host_settings_restore_stable.cpp`, `test_studio_host_settings_delete_record.cpp`, `test_studio_host_settings_delete_stable.cpp`, `test_studio_host_settings_memo_record.cpp`, and `test_studio_host_settings_memo_stable.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, removes another Windows-risky duplicated environment-helper family without product-code changes, and keeps the settings JSON and round-trip contracts unchanged. Focused builds/tests passed for all six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3570` shipped on the `#3214` lane: the Studio-host selected-settings pair now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `COPPERFIN_LOCALE` / `COPPERFIN_LOCALE_DIR` wrapper code in `test_studio_host_selected_report_settings.cpp` and `test_studio_host_selected_label_settings.cpp`. The refactor preserves the existing `en-US` plus cwd-anchored `resources/locales` behavior, makes another Windows-risky duplicated environment-helper family disappear without product-code changes, and keeps the selected report/label settings JSON contract unchanged. Focused builds/tests passed for both affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3569` shipped on the `#3214` lane: the Studio-host margin/orientation settings cluster now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `_dupenv_s` / `std::getenv` / `setenv` wrappers across `test_studio_host_bottom_margin_stable_settings.cpp`, `test_studio_host_page_margin_stable_settings.cpp`, `test_studio_host_page_margin_record_settings.cpp`, `test_studio_host_orientation_stable_settings.cpp`, `test_studio_host_orientation_record_settings.cpp`, and `test_studio_host_side_margin_record_settings.cpp`. The refactor preserves the existing `COPPERFIN_LOCALE = en-US` and cwd-anchored `resources/locales` behavior while removing another Windows-risky duplicated environment-helper family without product-code changes. Focused builds/tests passed for the six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3568` shipped on the `#3214` lane: the Studio-host record-settings cluster now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `_dupenv_s` / `std::getenv` / `setenv` wrappers across `test_studio_host_paper_size_record_settings.cpp`, `test_studio_host_report_sort_settings_record.cpp`, `test_studio_host_column_setup_record_settings.cpp`, `test_studio_host_vertical_grid_record_settings.cpp`, `test_studio_host_horizontal_grid_record_settings.cpp`, and `test_studio_host_bottom_margin_record_settings.cpp`. The refactor preserves the existing `COPPERFIN_LOCALE = en-US` and cwd-anchored `resources/locales` behavior while removing another Windows-risky duplicated environment-helper family without product-code changes. Focused builds/tests passed for the six affected Studio-host executables.
- 2026-07-04: Environment hardening slice `#3567` shipped on the `#3214` lane: the Studio-host stable-settings cluster now routes locale-catalog environment setup through the shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `_dupenv_s` / `std::getenv` / `setenv` wrappers across `test_studio_host_paper_size_stable_settings.cpp`, `test_studio_host_selected_settings_stable.cpp`, `test_studio_host_vertical_grid_stable_settings.cpp`, `test_studio_host_horizontal_grid_stable_settings.cpp`, `test_studio_host_report_sort_settings_stable.cpp`, and `test_studio_host_column_setup_stable_settings.cpp`. The refactor preserves the existing `COPPERFIN_LOCALE = en-US` and cwd-anchored `resources/locales` behavior while removing another Windows-risky duplicated environment-helper family without product-code changes. Focused builds/tests passed for the six affected Studio-host executables.
- 2026-07-04: Runtime parity slice `#3566` shipped on the `#3217` lane with representative native collection default-item member-chain parity: function-style default-item calls such as `oForm.Controls(1)` and `oForm.grdLedger.Columns(1)` now preserve chained property access on the selected item, and nested collection paths like `oForm.Controls(2).Columns(1).CurrentControl` now continue through both the selected object and the subsequent collection item. This closes a nearby common-in-the-wild gap left after the shipped bracket-access slice, while preserving ordinary array-call syntax and ordinary function-call behavior. Focused `test_prg_engine_runtime_surface_functions` coverage now proves `Controls(1).BaseClass`, `Columns(1).CurrentControl`, nested `Controls(2).Columns(1).CurrentControl`, `aNames(2)`, and `DoubleIt(3)` without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3565` shipped on the `#3217` lane with representative native collection subscript parity: the PRG expression parser now routes bracket access through ordinary arrays when a declared array exists and otherwise resolves native collection `Item(...)` selection on runtime object collections, then continues through chained member reads on the selected item. This closes a common-in-the-wild gap evidenced by extracted VFP/FFC sources such as `VFPSource/builders/wbgrid.prg`, `VFPSource/Wizards/wzform/wizform.prg`, and `FFC/_controls.vc2`, which use patterns like `Columns[m.ColIndex].CurrentControl`, `Controls[m.i].BaseClass`, and `Pages[m.j]` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves bracketed native `Controls`, `Objects`, and `Columns` access plus nested `oForm.Controls[2].Columns[1].CurrentControl` chaining while preserving ordinary array bracket behavior and the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3564` shipped on the `#3217` lane with representative native `Column.Bound` parity: same-session native `Column` objects now seed built-in `Bound = .T.` and `ControlSource = ""` state by default, preserve declarative derived overrides such as `Bound = .F.` and `ControlSource = 'orders.header'`, keep ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection coherent, and coordinate contained control `ControlSource` behavior with the owning column. Bound columns now block direct contained-control `ControlSource` writes, unbound columns permit those direct child writes, and later column-level `ControlSource` changes override contained child control sources coherently, matching the mounted VFP9 `Bound Property` contract. This is directly evidenced by mounted VFP9 help for the `Bound Property`, which documents runtime availability, default `.T.`, blocked contained-control writes while bound, and column-level override semantics, plus extracted FFC source usage in `FFC/_app.vc2`, which reads `loColumn.Bound` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, initial bound child propagation, blocked child writes while bound, allowed child writes while unbound, later column override propagation, derived override retention, and protected built-in behavior without regressing the shipped `Column.CurrentControl`, `Column.DynamicCurrentControl`, `Grid.ColumnCount`, or general `ControlSource` lanes.
- 2026-07-04: Runtime parity slice `#3563` shipped on the `#3217` lane with representative native `Grid.ColumnCount` parity: same-session native `Grid` objects now seed a built-in `ColumnCount = -1` state by default, preserve declarative derived overrides such as `ColumnCount = 1`, keep ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection coherent, and materialize or remove owned native `Column` children coherently when `ColumnCount` is set to a non-negative value so common `Column1` / `EVAL('.COLUMN'+...)` flows have real runtime children behind them. The native child-collection lane was hardened alongside this slice so built-in read-only `Objects`, `Controls`, and new `Columns` collection properties can no longer be shadowed, erased, or mutated through ordinary property writes or `ADDPROPERTY()` / `REMOVEPROPERTY()`. This is directly evidenced by mounted VFP9 help for the `ColumnCount Property (Visual FoxPro)`, which documents Grid runtime writability, default `-1`, and explicit positive-count behavior, plus extracted VFP source usage in `VFPSource/builders/wbgrid.prg` and `VFPSource/Wizards/wzform/wizform.prg`, which set and read `Grid.ColumnCount` in ordinary builder and wizard code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `-1`, direct and `SETPEM()` mutation, coherent `Columns.Count` reflection, materialized `Column1` / `Column2` / `Column3` children, preserved derived overrides, and protected built-in behavior without regressing the shipped combo/list `ColumnCount` lane or the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3562` shipped on the `#3217` lane with representative native `Column.ColumnOrder` parity: same-session native `Column` objects now seed a built-in `ColumnOrder = 1` state for standalone columns, grid-owned columns default to the next display position under the owning grid, and explicit `ColumnOrder` writes now reorder sibling native columns coherently through ordinary assignment plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection without changing machine-readable runtime identifiers. This is directly evidenced by mounted VFP9 help for the `ColumnOrder Property`, which documents run-time availability, sibling renumbering when one column moves, and the fact that `ColumnOrder` values need not remain sequential, plus extracted VFP/FFC source usage in `VFPSource/builders/wbgrid.prg`, `FFC/_app.vc2`, and `FFC/_table.vc2`, which read and assign `ColumnOrder` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves standalone defaults, direct and `SETPEM()` mutation, grid-sibling reordering in both directions, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3561` shipped on the `#3217` lane with representative native `Grid.AllowAddNew` parity: same-session native `Grid` objects now seed built-in `AllowAddNew = .F.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `AllowAddNew = .T.`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `AllowAddNew Property`, which documents run-time writability and default `False (.F.)`. While landing this slice, the direct native write and `SETALL()` eligibility paths were also hardened so shipped read-only native `Grid.LeftColumn` and `Grid.SplitBar` surfaces can no longer be mutated through ordinary dotted assignment or `SETALL()`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation for `AllowAddNew`, preserved read-only behavior for `SplitBar`, contained-form reads and writes, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3560` shipped on the `#3217` lane with representative native `Grid.SplitBar` parity: same-session native `Grid` objects now seed built-in `SplitBar = .T.` state by default, preserve class-definition seeded derived values such as `SplitBar = .F.`, expose coherent ordinary reads plus `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and block ordinary assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` attempts against that built-in read-only surface. This is directly evidenced by mounted VFP9 help for the `SplitBar Property`, which documents default `True (.T.)` and read-only behavior at run time, plus extracted FFC source usage in `FFC/_ws3client.vc2`, which sets `SplitBar = .F.` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and contained-object reads, blocked mutation, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3559` shipped on the `#3217` lane with representative native `Grid.DeleteMark` parity: same-session native `Grid` objects now seed built-in `DeleteMark = .T.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `DeleteMark = .F.`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `DeleteMark Property`, which documents run-time writability and default `True (.T.)`, plus extracted VFP/FFC source usage in `VFPSource/builders/wbgrid.prg` and `FFC/_ws3client.vc2`, which set `DeleteMark = .F.` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, contained-form reads and writes, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3558` shipped on the `#3217` lane with representative native Grid display-property parity for `AllowCellSelection`, `GridLines`, `Highlight`, `HighlightRow`, and `RecordMark`: same-session native `Grid` objects now seed documented defaults of `AllowCellSelection = .T.`, `GridLines = 3`, `Highlight = .T.`, `HighlightRow = .T.`, and `RecordMark = .T.`, keep those members coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `AllowCellSelection = .F.` and `GridLines = 2`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surfaces. This is directly evidenced by mounted VFP9 help for each property's runtime mutability and defaults, plus extracted VFP/FFC source usage in `VFPSource/builders/wbgrid.prg`, `FFC/_ws3client.vc2`, and `FFC/_dataquery.vc2`, which seed these properties in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves defaults, direct and `SETPEM()` mutation, contained-form reads and writes, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3557` shipped on the `#3217` lane with representative native `Grid.LeftColumn` parity: same-session native `Grid` objects now seed a built-in `LeftColumn = 1` state, preserve class-definition seeded derived values such as `LeftColumn = 3`, expose coherent ordinary reads plus `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and block ordinary assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` attempts against that built-in read-only surface. This uses `1` as a representative default by inference from mounted VFP9 help semantics, which document `LeftColumn` as the left-most visible column number and the property as read-only at runtime without an explicit standalone default-value table. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and contained-object reads, blocked mutation, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3556` shipped on the `#3217` lane with representative native `Column.DynamicCurrentControl` parity: same-session native `Column` objects now seed built-in `DynamicCurrentControl = "Text1"` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `DynamicCurrentControl = 'spnHours'`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This uses `Text1` as a representative default by inference from mounted VFP9 help, which documents the default control as a `TextBox` named `Text1` for the property without spelling out a separate default-value table. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, contained-column reads through a grid-owned child object, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3555` shipped on the `#3217` lane with representative native `Column.CurrentControl` parity: same-session native `Column` objects now seed built-in `CurrentControl = "Text1"` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `CurrentControl = 'spnHours'`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `CurrentControl Property`, which documents run-time writability, mounted Grid property help that documents `Text1` as the default current control, plus extracted VFP/FFC source usage in `toolboxui.prg`, `dataexplorercolumn.prg`, `builders/wbgrid.prg`, and FFC `_table.vc2` / `_app.vc2`, which read or assign `CurrentControl` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, contained-column reads through a grid-owned child object, derived override retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3554` shipped on the `#3217` lane with representative native `Grid.RecordSource` and `Grid.RecordSourceType` parity: same-session native `Grid` objects now seed built-in `RecordSource = ""` and `RecordSourceType = 1` state by default, keep both members coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `RecordSource = 'orders_cursor'` and `RecordSourceType = 2`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surfaces. This is directly evidenced by mounted VFP9 help for the `RecordSource Property`, which documents run-time writability and the common `SPACE(0)` preservation pattern, the `RecordSourceType Property`, which documents default `1` (Alias) and run-time writability, plus mounted FFC/VFP source usage in `_dataquery.vc2`, `_ws3client.vc2`, `_ws3utils.vc2`, and `builders/wbgrid.prg`, which assign grid record sources in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves defaults, direct and `SETPEM()` mutation, child `THISFORM` access, derived override retention, and protected built-in behavior for both surfaces without regressing the stack-frugal runtime path.
- 2026-07-04: Environment hardening slice `#3553` shipped on the `#3214` lane: the Studio-host selected-section report/label test cluster now routes locale-catalog environment setup through a shared `tests/test_locale_catalog_environment_support.h` helper instead of repeating local `_dupenv_s` / `std::getenv` / `setenv` wrappers across `test_studio_host_selected_summary_sections_record.cpp`, `test_studio_host_selected_summary_sections_stable.cpp`, `test_studio_host_selected_page_header_sections_record.cpp`, and `test_studio_host_selected_page_header_sections_stable.cpp`. The new helper preserves the existing `COPPERFIN_LOCALE = en-US` default plus cwd-anchored `resources/locales` discovery, keeps command/test behavior unchanged, and removes one more Windows-risky environment-wrapper family without product-code changes. Focused build/test validation passed for the four affected Studio-host executables.
- 2026-07-04: Runtime parity slice `#3552` shipped on the `#3217` lane with representative native `DisplayValue` parity for `ComboBox` and `ListBox`: same-session native `ComboBox` and `ListBox` objects now seed a deterministic built-in `DisplayValue = ""` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `DisplayValue = 'DerivedText'`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `DisplayValue Property`, which applies the property to `ComboBox` and `ListBox`, documents runtime availability, and describes both character-string and numeric values. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, mixed string/numeric direct and `SETPEM()` mutation, child `THISFORM` access across combo and list objects, derived override retention, and protected built-in behavior without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3551` shipped on the `#3217` lane with representative native `ListIndex` parity for `ComboBox` and `ListBox`: same-session native `ComboBox` and `ListBox` objects now seed built-in `ListIndex = 0` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `ListIndex = 4`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `ListIndex Property (Visual FoxPro)`, which applies the property to `ComboBox` and `ListBox`, documents runtime read/write behavior, and explicitly documents default `0` as “no items selected.” Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, child `THISFORM` access across combo and list objects, derived override retention, and protected built-in behavior without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3550` shipped on the `#3217` lane with representative native `ComboBox` list-shaping property parity for `BoundColumn`, `ColumnCount`, and `ColumnWidths`: same-session native `ComboBox` objects now seed built-in `BoundColumn = 1`, `ColumnCount = 0`, and `ColumnWidths = ""` state by default, keep those members coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `BoundColumn = 2`, `ColumnCount = 2`, and `ColumnWidths = "90,0"`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surfaces. This is directly evidenced by mounted VFP9 help for the `BoundColumn Property`, which explicitly documents default `1`, the `ColumnCount Property (Visual FoxPro)`, which documents `0` as the combo/list single-column mode, the `ColumnWidths Property`, which documents the width-string contract, plus mounted VFP9 sample `samples/solution/reports/datepick.prg`, which declares real combo boxes with `BoundColumn = 2`, `ColumnCount = 2`, `ColumnWidths = "72,0"`, `RowSourceType = 1`, and `RowSource = ...`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default state, direct and `SETPEM()` mutation, child `THISFORM` access, derived override retention, and protected built-in behavior for all three surfaces without regressing the existing stack-frugal runtime path.
- 2026-07-04: Environment hardening slice `#3544` shipped on the `#3214` lane: the Studio-host report/label selection batch now routes locale-catalog environment setup through the shared Windows-safe helper in `tests/test_environment_support.h` instead of repeating local `_dupenv_s` / `std::getenv` / `setenv` wrappers across `test_studio_host_real_sample_round_trip.cpp`, `test_studio_host_selected_report_objects.cpp`, `test_studio_host_selected_report_settings.cpp`, `test_studio_host_selected_label_objects.cpp`, and `test_studio_host_selected_label_settings.cpp`. Each file keeps its existing `ScopedDefaultLocaleCatalogEnvironment` behavior and command/test surface, but the duplicated per-file environment plumbing is gone, reducing one more Windows-risky test cluster without changing product behavior. Focused build/test validation passed for the five affected Studio-host executables.
- 2026-07-04: Runtime parity slice `#3543` shipped on the `#3217` lane with representative native `ComboBox.ReadOnly` parity plus the documented `Style = 2` guard: same-session native `ComboBox` objects now seed built-in `ReadOnly = .F.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `ReadOnly = .T.` when the control remains `Style = 0`, and reject transitions to `ReadOnly = .T.` when `Style = 2` (drop-down list). The runtime now also normalizes the representative invariant when a `ComboBox` is materialized or switched into `Style = 2`, so invalid `Style = 2` / `ReadOnly = .T.` combinations collapse back to `.F.` instead of persisting as impossible native state. This is directly evidenced by mounted VFP9 help for the `ReadOnly Property`, which applies the property to `ComboBox`, documents default `False (.F.)`, and explicitly states that `ReadOnly` cannot be set to true when `Style = 2`, plus mounted VFP9 sample `samples/solution/reports/datepick.prg`, which declares real `ComboBox` controls with `Style = 2`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, allowed `Style = 0` mutation, blocked `Style = 2` promotion, invariant normalization on `Style = 2`, child `THISFORM` access, derived `ReadOnly = .T.` retention for editable combos, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3542` shipped on the `#3217` lane with representative native `ComboBox.Style` property parity: same-session native `ComboBox` objects now seed built-in `Style = 0` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `Style = 2`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `Style Property`, which documents `ComboBox` values `0` (default drop-down combo) and `2` (drop-down list), plus mounted VFP9 sample `samples/solution/reports/datepick.prg`, which declares real `ComboBox` controls with `Style = 2`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `0` state, direct and `SETPEM()` mutation, child `THISFORM` access, derived `2` retention, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Environment hardening slice `#3541` shipped on the `#3214` lane: the PRG-engine test cluster now routes scoped environment set/restore behavior through the shared Windows-safe helper in `tests/test_environment_support.h` instead of repeating ad hoc `std::getenv`-based wrappers across `test_prg_engine_control_flow_support.h`, `test_prg_engine_data_io_support.h`, `test_prg_engine_seek_index.cpp`, `test_prg_engine_sql_cursors.cpp`, `test_prg_engine_table_structure.cpp`, `test_prg_engine_index_seek_optimization.cpp`, `test_prg_engine_runtime_surface_functions.cpp`, and `test_xasset_methods.cpp`. The control-flow and data-io shared helper translation units now delegate their `set_env_value(...)` entrypoints to the shared helper as well, keeping existing call sites stable while removing duplicated Windows-risky env access patterns from that test family. Focused build/test validation over the affected PRG-engine and xAsset targets passed with no product-behavior changes.
- 2026-07-04: Runtime parity slice `#3540` shipped on the `#3217` lane with expanded native `ReadOnly` control parity for `CheckBox` and `Spinner`: same-session native `CheckBox` and `Spinner` objects now seed built-in `ReadOnly = .F.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `ReadOnly = .T.`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `ReadOnly Property`, which explicitly applies to `CheckBox` and `Spinner` and documents default `False (.F.)`. While landing this slice, the native built-in activation list was also hardened so plain `CREATEOBJECT('Column')` now routes through the native runtime path instead of the OLE fallback, making the shipped `#3539` `Column.ReadOnly` parity real under fresh validation rather than only through derived-class coverage. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, derived `.T.` retention, and protected built-in behavior for both native surfaces without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3539` shipped on the `#3217` lane with expanded native `ReadOnly` control parity for `Grid` and `Column`: same-session native `Grid` and `Column` objects now seed built-in `ReadOnly = .F.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `ReadOnly = .T.`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `ReadOnly Property`, which explicitly applies to `Column` and `Grid` and documents default `False (.F.)`, plus mounted FFC sources including `_dataquery.vc2` and `_ws3client.vc2`, which set `Grid.ReadOnly = .T.` and `Column.ReadOnly = .T.` in ordinary product code. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, derived `.T.` retention, and protected built-in behavior for both native surfaces without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3538` shipped on the `#3217` lane with representative `_SCREEN.WindowState` / `_VFP.WindowState` root-alias parity: the native PRG runtime now keeps a deterministic application-level `WindowState = 0` state alongside the existing representative caption surface, routes both root aliases through that shared state for reads and writes, and keeps both directions coherent when code assigns through either alias. This is directly evidenced by mounted VFP9 help for `WindowState Property (Visual FoxPro)`, which applies the property to `Form | _SCREEN` and documents values `0` (Normal), `1` (Minimized), and `2` (Maximized), including explicit `_SCREEN.WindowState` guidance. Focused `test_prg_engine_runtime_surface_functions` coverage now proves deterministic defaults plus `_SCREEN`-to-`_VFP` and `_VFP`-to-`_SCREEN` alias propagation without regressing the existing stack-frugal runtime path or the shipped native `Form.WindowState` surface.
- 2026-07-04: Runtime parity slice `#3537` shipped on the `#3217` lane with representative native `Form.WindowState` property parity: same-session native `Form` objects now seed built-in `WindowState = 0` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `WindowState = 2`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for `WindowState Property (Visual FoxPro)`, which documents runtime values `0` (Normal), `1` (Minimized), and `2` (Maximized), plus mounted FFC/VFP source usage in `FFC-master.zip` and `VFPSource.zip` including `_ui.vc2`, `_ws3client.vc2`, `_ws3utils.vc2`, and ReportBuilder/ReportPreview `frxcommon.prg`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `0` state, direct and `SETPEM()` mutation, derived `2` retention, child `THISFORM.WindowState` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3536` shipped on the `#3217` lane with representative native `Form.Desktop` property parity: same-session native `Form` objects now seed built-in `Desktop = .F.` state by default, keep that member visible through ordinary reads plus `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `Desktop = .T.`, and reject runtime mutation through direct assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` because VFP9 documents `Form.Desktop` as runtime read-only. This is directly evidenced by mounted VFP9 help for the `Desktop Property`, which documents default `False (.F.)` and run-time read-only behavior, plus mounted FFC/VFP source usage in `FFC-master.zip` and `VFPSource.zip` including `_ws3utils.vc2`, `_ws3client.vc2`, `_reportlistener.vc2`, and ReportBuilder/ReportPreview `frxcommon.prg`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, runtime read-only enforcement, derived `.T.` retention, child `THISFORM.Desktop` reads, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3535` shipped on the `#3217` lane with representative native text-entry `ReadOnly` property parity: same-session native `TextBox` and `EditBox` objects now seed built-in `ReadOnly = .F.` state by default, keep that member coherent through ordinary reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded child values such as `ReadOnly = .T.`, and reject `ADDPROPERTY()` / `REMOVEPROPERTY()` attempts to shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 help for the `ReadOnly Property`, which documents default `False (.F.)` and run-time writability for control surfaces, plus mounted sample code in `program files/microsoft visual foxpro 9/samples/servers/foxisapi/foxis/t1.prg`, which sets `readonly = .t.` on a `TextBox`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, child `THISFORM` access across `TextBox` and `EditBox`, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3534` shipped on the `#3217` lane with representative native `Form.ScrollBars` property parity: same-session native `Form` objects now seed built-in `ScrollBars = 0` state by default, keep that member visible through ordinary reads plus `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, preserve class-definition seeded derived values such as `ScrollBars = 3`, and reject runtime mutation through direct assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` because VFP9 documents `Form.ScrollBars` as runtime read-only. This is directly evidenced by shipped VFP9 help for the `ScrollBars Property`, plus mounted VFP source usage in `VFPSource.zip` including `browser/browser.prg` and `Wizards/wzform/wizform.prg`. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `0` state, runtime read-only enforcement, derived `3` retention, child `THISFORM.ScrollBars` reads, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3533` shipped on the `#3217` lane with representative native `Form.TitleBar` property parity: same-session native `Form` objects now seed writable built-in `TitleBar = 1` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by shipped VFP9 help for the `TitleBar Property`, which documents form values `0` (hidden) and `1` (default, displayed), plus mounted sample assets including `samples/solution/toledo/irregular.prg`, which sets `TitleBar = 0`, and matching VFP source references in `VFPSource.zip`. This slice is intentionally limited to representative property parity in the headless PRG runtime and does not claim full native window-chrome side effects. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `1` state, direct and `SETPEM()` mutation, child `THISFORM.TitleBar` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3532` shipped on the `#3217` lane with representative native `Form.BorderStyle` property parity: same-session native `Form` objects now seed writable built-in `BorderStyle = 3` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by shipped VFP9 help for the `BorderStyle Property`, which documents form values `0` (No border), `1` (Fixed Single), `2` (Fixed Dialog), and `3` (Default, Sizable), plus mounted sample assets including `samples/solution/reports/datepick.prg` and `samples/solution/toledo/irregular.prg`, which set `BorderStyle = 2` and `BorderStyle = 0` respectively. This slice is intentionally limited to representative property parity in the headless PRG runtime and does not claim full native window-chrome rendering behavior. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `3` state, direct and `SETPEM()` mutation, child `THISFORM.BorderStyle` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3531` shipped on the `#3217` lane with representative native `Form.WindowType` property parity: same-session native `Form` objects now seed writable built-in `WindowType = 0` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by shipped VFP9 help for the `WindowType Property`, which documents form values `0` (modeless) and `1` (modal), plus mounted sample and source assets including `samples/solution/reports/datepick.prg` and `VFPSource.zip` wizard code that set `WindowType = 1`. This slice is intentionally limited to representative property parity in the headless PRG runtime and does not claim full modal event-loop or UI-blocking behavior. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `0` state, direct and `SETPEM()` mutation, child `THISFORM.WindowType` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: E3 slice `#3530` shipped on the `#24` lane with real VFP9 report grouping round-trip coverage: the shared Studio-host regression suite now exercises mounted `invoice.frx`/`.frt` sample assets at a stable real grouping-backed section selection, proving supported memo-backed group-header `EXPR` edits persist through the shared visual-asset editor, refresh the stable `document.reportLayout.groupings` plus section grouping-context JSON surfaces after reopen, and drive the expected paired primary-plus-sidecar write path instead of silently mutating only one file. Focused `test_studio_host_real_sample_grouping_round_trip` coverage now proves representative real grouping-expression edits against a mounted VFP9-era report asset instead of only synthetic fixtures or manual probes.
- 2026-07-04: E3 slice `#3529` shipped on the `#24` lane with real VFP9 report/label section round-trip coverage: the shared Studio-host regression suite now exercises mounted `invoice.frx`/`.frt` and `cust.lbx`/`.lbt` sample assets at stable section selections, proving supported direct-field section edits persist through the shared visual-asset editor, preserve the section-selection JSON lane through `selectedReportSelectionKind` plus the `reportLayout.sections` record identity/title/height contract, and change only the primary `FRX/LBX` bytes while leaving `FRT/LBT` sidecars untouched for that narrow section-geometry lane. Focused `test_studio_host_real_sample_section_round_trip` coverage now proves representative real `Detail` band height edits on both report and label samples instead of only synthetic fixtures.
- 2026-07-04: E3 slice `#3528` shipped on the `#24` lane with real VFP9 report/label memo-backed object round-trip coverage: the shared Studio-host regression suite now exercises mounted `invoice.frx`/`.frt` and `cust.lbx`/`.lbt` sample assets at stable placed-object record selections, proving supported memo-backed `EXPR` edits persist through the shared visual-asset editor, refresh the selected-object JSON title as expected, preserve unaffected direct fields such as `WIDTH`, and drive the expected paired primary-plus-sidecar write path instead of silently mutating only one file. Focused `test_studio_host_real_sample_object_memo_round_trip` coverage now proves representative real report and label expression-object edits against mounted VFP9-era assets instead of only synthetic fixtures.
- 2026-07-04: E3 slice `#3527` shipped on the `#24` lane with real VFP9 report/label placed-object round-trip coverage: the shared Studio-host regression suite now exercises mounted `invoice.frx`/`.frt` and `cust.lbx`/`.lbt` sample assets at stable placed-object record selections, proving supported direct-field object edits persist through the shared visual-asset editor, preserve the stable host JSON object-selection contract after reopen, and keep memo-backed expression text untouched while changing only the primary `FRX/LBX` bytes. Focused `test_studio_host_real_sample_object_round_trip` coverage now proves representative `WIDTH` edits on a real report expression object and a real label expression object, with byte-stable `FRT/LBT` sidecars throughout that narrow object-edit lane.
- 2026-07-04: E3 slice `#3526` shipped on the `#24` lane with real VFP9 report/label root-settings round-trip coverage: the Studio-host regression suite now exercises mounted `invoice.frx`/`.frt` and `cust.lbx`/`.lbt` sample assets by copying them into writable temp space, proving no-op `--json` reads leave both primary and sidecar bytes unchanged, supported root-setting edits such as `GRIDV` persist through the shared visual-asset editor, and those supported edits change only the primary `FRX/LBX` bytes while preserving the sidecar `FRT/LBT` payload verbatim. Focused `test_studio_host_real_sample_round_trip` coverage also reopens the edited assets and proves the stable host JSON settings-selection contract (`selectedReportSettingsAvailable`, `selectedReportSelectionKind`, page-setup fields, and persisted `gridVertical`) against real VFP9-era files instead of only synthetic fixtures.
- 2026-07-04: Runtime parity slice `#3525` shipped on the `#3217` lane with representative native `Form.AutoCenter` property parity: same-session native `Form` objects now seed writable `AutoCenter = .F.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 assets including `scctext.prg`, `vfpxtab.prg`, `samples/solution/reports/datepick.prg`, and `samples/solution/toledo/irregular.prg`, which all enable centering, plus shipped VFP9 help documenting `False (.F.)` as the default AutoCenter state. This slice is intentionally limited to representative property parity in the headless PRG runtime and does not claim full desktop/window-placement centering behavior. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, child `THISFORM.AutoCenter` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3524` shipped on the `#3217` lane with representative native `Form.MaxButton` property parity: same-session native `Form` objects now seed writable `MaxButton = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 assets including `scctext.prg`, `vfpxtab.prg`, and `samples/solution/reports/datepick.prg`, which explicitly disable the maximize button, plus shipped VFP9 help documenting `True (.T.)` as the default MaxButton state. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, child `THISFORM.MaxButton` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3523` shipped on the `#3217` lane with representative native `Form.MinButton` property parity: same-session native `Form` objects now seed writable `MinButton = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 assets including `scctext.prg`, `vfpxtab.prg`, and `samples/solution/reports/datepick.prg`, which explicitly disable the minimize button, plus shipped VFP9 help documenting `True (.T.)` as the default MinButton state. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, child `THISFORM.MinButton` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3522` shipped on the `#3217` lane with representative native `Form.Closable` property parity: same-session native `Form` objects now seed writable `Closable = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 assets including `vfpxtab.prg`, `scctext.prg`, `samples/solution/toledo/irregular.prg`, and `samples/solution/reports/datepick.prg`, which all explicitly disable closing, plus shipped VFP9 help documenting `True (.T.)` as the default Closable state. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, child `THISFORM.Closable` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3521` shipped on the `#3217` lane with representative native `Form.ControlBox` property parity: same-session native `Form` objects now seed writable `ControlBox = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 assets including `vfpxtab.prg`, `scctext.prg`, and `samples/solution/forms/graphics/fdproc.prg`, all of which explicitly disable the form control box. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, child `THISFORM.ControlBox` access, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3520` shipped on the `#3217` lane with representative native `Form.ShowWindow` property parity: same-session native `Form` objects now seed writable `ShowWindow = 0` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by mounted VFP9 sample/menu code that branches on `oFormRef.ShowWindow # 2` and sets `ShowWindow = 2` for top-level forms. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `0` state, direct and `SETPEM()` mutation including the representative `2` top-level value, child `THISFORM.ShowWindow` access on the existing owner-chain lane, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3519` shipped on the `#3217` lane with representative root-application caption alias parity: the native PRG runtime now treats `_SCREEN.Caption` and `_VFP.Caption` as a shared deterministic representative caption surface instead of letting those reads fall through the generic dotted-object fault path. Direct dotted assignment through either alias now updates the same shared caption state, so mounted VFP9 FFC patterns such as `MESSAGEBOX(..., _screen.Caption)` can read a stable title source without requiring a fake `_SCREEN` runtime object model. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default reads from both aliases plus bidirectional `_SCREEN`/`_VFP` write aliasing without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3518` shipped on the `#3217` lane with representative native `Form.AlwaysOnTop` property parity: same-session native `Form` objects now seed writable `AlwaysOnTop = .F.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. This is directly evidenced by the mounted VFP9 `vfpxtab.prg` source tree, which uses `AlwaysOnTop` on a form definition. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, child `THISFORM.AlwaysOnTop` access on the existing owner-chain lane, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3517` shipped on the `#3217` lane with representative native visual geometry member parity: same-session native visual runtime objects now seed writable built-in `Left`, `Top`, `Width`, and `Height` state for the native PRG lane, so ordinary reads/writes plus `GETPEM()`, `SETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on representative forms and native child controls while the existing built-in `Move()` fallback continues updating that same geometry state. `ADDPROPERTY()` and `REMOVEPROPERTY()` can no longer shadow or erase the representative geometry surface, and mounted VFP9 sample-style geometry code such as `THIS.Width = ...` now stays on a protected native member lane instead of an incidental raw-property path. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default geometry state, direct and `SETPEM()` mutation, reflection coherence, protected built-in behavior, and preserved `Move()` fallback updates without regressing the stack-frugal runtime path.
- 2026-07-04: E2 slice `#3516` shipped with a Studio-host contract fix for memo-backed visual-property reorder batches: `copperfin_studio_host --visual-property-reorder-batch` now reports the requested batch item count through `affectedObjectCount` on successful SCX/VCX property reorder commands instead of leaking the lower-level real-mutation count from the shared editor core. This restores the shipped `#1444` host JSON contract for successful batch reorders while deliberately leaving the editor-core `reorder_visual_object_properties(...)` mutation-count semantics unchanged, so shared host JSON/text output again matches the expected batch-facing surface without regressing the underlying visual-asset editor behavior or undo availability.
- 2026-07-04: Runtime parity slice `#3514` shipped on the `#3217` lane with representative native `Name` property parity for containment traversal: same-session native runtime objects now seed deterministic built-in `Name` state for representative roots plus child objects created through class-body `ADD OBJECT` materialization and runtime `AddObject()`, so mounted VFP9 sample-style reads such as `oChild.Parent.Name` stay on native state instead of missing-property fallthrough. This slice keeps `Name` protected from `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` to avoid pretending full runtime rename/rebind semantics exist before parent-member rebinding does, while preserving existing broad `AMEMBERS()` member-list contracts. Focused `test_prg_engine_runtime_surface_functions` coverage now proves parent-chain `Name` reads, child-name preservation for declarative and runtime-added controls, `GETPEM()` / `PEMSTATUS()` coherence, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3513` shipped on the `#3217` lane with representative native string-control `Value` property parity: same-session native `TextBox` and `ComboBox` runtime objects now seed writable builtin `Value` state as an empty string by default, so ordinary property reads/writes plus `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` stay coherent on both plain controls and representative child controls. `ADDPROPERTY()` and `REMOVEPROPERTY()` can no longer shadow or erase that builtin surface, and mounted VFP9 sample-style reads such as `THISFORM.txt1.Value` and `THISFORM.cboFromMonth.Value` now stay on deterministic native state instead of falling through to synthetic `ole:<...>.value` strings. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default empty-string state, direct and `SETPEM()` mutation, reflection coherence, and protected builtin behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3512` shipped on the `#3217` lane with representative native visual `Visible` property parity: same-session native visual runtime objects now seed writable `Visible = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain visual objects and derived child controls, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. `OleControl` hosts keep their existing direct-contained-object `Visible` reflection route instead of being shadowed by a host-side synthetic property, so the shipped `.Object`/contained-object parity remains intact. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, reflection coherence, protected built-in behavior, and preserved `OleControl.Visible` routing without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime pipeline slice `#3509` shipped on the `#110` lane: `copperfin_runtime_host` now enforces the `app.cfmanifest` version contract instead of treating every manifest as an unvalidated key/value bag. Runtime-host loads now require `manifest_version`, accept the shipped compatibility/runtime package versions `1` and `2`, and reject missing, malformed, or unsupported versions with localized diagnostics while preserving the existing machine-readable `status: error` surface and the established exit code `4` for manifest-contract failures. Focused `test_runtime_host_debug_output` coverage now proves supported `manifest_version=2` packages still run, omitted versions fail fast, and unsupported versions localize correctly without falling back to raw English prose.
- 2026-07-04: Environment hardening slice `#3508` shipped on the `#3214` lane: the remaining direct product-code environment reads in `src/vfp/dbf_table.cpp`, `src/runtime/prg_engine_free_functions.inl`, `src/runtime/prg_engine_variables.inl`, and `src/runtime/prg_engine_runtime_surface_functions.cpp` now route through `copperfin::platform::read_environment_variable(...)` instead of ad hoc `std::getenv` / `_dupenv_s` branches. Runtime behavior is unchanged, but Windows CRT handling is now consistent across the targeted VFP/runtime hot paths, including DBF staged-write fault injection hooks, runtime `GETENV()` reads, HOME/WINDIR font-root discovery, and locale/code-page fallback probing.
- 2026-07-04: E3 slice `#3506` shipped with asset-family read-only inference in the shared Studio document model: `open_document()` no longer trusts only the launch contract for `document.read_only`, and sidecar-backed designer assets now report `readOnly: true` whenever the main asset file or an existing sidecar is not writable while still honoring explicit `--read-only` launches. This closes a real mounted-VFP9 regression where wizard-era `FRX/FRT` assets on a read-only filesystem opened through `copperfin_studio_host` as falsely editable; direct probing now shows mounted `bandedh.frx` surfaces `readOnly: true`, writable copied `SCX/SCT` assets remain editable, and the same copied assets flip back to `readOnly: true` once write bits are removed.
- 2026-07-04: Runtime parity slice `#3505` shipped with representative FormSet owner-chain parity on the `#3217` lane: native method frames now carry distinct owner-form and owner-formset references, so controls under a form inside a `FormSet` now see `THISFORM` as the live form, `THISFORMSET` as the owning formset, `_SCREEN.ActiveForm` tracks the live form after focus activity, and representative close flows such as `RELEASE THISFORMSET` can tear down the formset through that corrected owner chain. The shipped fallback for older non-formset native `Custom` owner hierarchies remains intact, so existing `THISFORM` / `THISFORMSET` coverage outside real FormSet graphs does not regress. Focused `test_prg_engine_runtime_surface_functions` coverage now proves distinct `THISFORM` vs `THISFORMSET` reads inside a FormSet child control plus `_SCREEN.ActiveForm.Parent` and `RELEASE THISFORMSET` behavior on the representative mounted-sample lane.
- 2026-07-04: Runtime parity slice `#3504` shipped with representative native `Release` override plus `NODEFAULT` parity on the `#3217` lane: the parser/runtime now recognize `NODEFAULT` where needed for native method default-action suppression, same-session native `Release()` overrides now run before the built-in release path, and when that override requests `NODEFAULT` the built-in release/destroy path is suppressed so the object stays live with the override's return value preserved. Focused `test_prg_engine_runtime_surface_functions` coverage now proves override-first `Release()` behavior, builtin release fallthrough when `NODEFAULT` is absent, and builtin release suppression when `NODEFAULT` is present, without regressing the stack-frugal runtime path or ordinary shipped `Release()` behavior.
- 2026-07-04: Runtime parity slice `#3503` shipped with representative native `RELEASE THISFORM` / `RELEASE THISFORMSET` parity on the `#3217` lane: explicit same-session owner-alias `RELEASE` statements inside native PRG methods now route into the existing native object release path instead of only erasing a local alias as a memory binding, so common-in-the-wild form/formset close patterns such as `RELEASE THISFORMSET` can tear down the owner form plus owned child graph while the current method still returns deterministically through the stack-frugal frame machine. Focused `test_prg_engine_runtime_surface_functions` coverage now proves both `RELEASE THISFORM` and `RELEASE THISFORMSET` from child methods release the owner form hierarchy and let the script complete cleanly without regressing ordinary `RELEASE` memory-variable behavior.
- 2026-07-04: Runtime parity slice `#3502` shipped with representative `_VFP.ActiveForm` / `_SCREEN.ActiveForm` parity on the `#3217` lane: the same-session native runtime now tracks a representative active form from built-in `Show()` / `SetFocus()` activity, exact `_VFP.ActiveForm` and `_SCREEN.ActiveForm` reads return the live native form reference, nested property chains such as `_VFP.ActiveForm.Caption` and `_VFP.ActiveForm.oToolbar.cmdRed.Enabled` resolve through the existing native object/property machinery, and common-in-the-wild method calls such as `_SCREEN.ActiveForm.Hide()` now route into that active form instead of falling through the missing-root path. Focused `test_prg_engine_runtime_surface_functions` coverage now proves direct alias reads, nested child-property reads, `ActiveControl` coherence, and alias-driven `Hide()` behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3501` shipped with representative native `Enabled` property parity on the `#3217` lane: same-session native visual runtime objects now seed writable `Enabled = .T.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain visual objects and derived child controls, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.T.` state, direct and `SETPEM()` mutation, reflection coherence, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3500` shipped with representative native `Form.LockScreen` property parity on the `#3217` lane: same-session native `Form` objects now seed writable `LockScreen = .F.` state by default, so ordinary property reads, direct assignment, `SETPEM()`, `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` stay coherent on both plain `CREATEOBJECT('Form')` and derived same-session form objects, while `ADDPROPERTY()` and `REMOVEPROPERTY()` cannot shadow or erase the built-in surface. Focused `test_prg_engine_runtime_surface_functions` coverage now proves default `.F.` state, direct and `SETPEM()` mutation, child `THISFORM.LockScreen` access on the existing owner-chain lane, and protected built-in behavior without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3499` shipped with representative native `ControlCount` property parity on the `#3217` lane: child-owning native runtime objects that already surface the hidden read-only `Controls` collection now also expose synchronized read-only `ControlCount` values through ordinary property reads plus `GETPEM()`, `PEMSTATUS(..., 1|5)`, and `AMEMBERS(..., 1|3)`, while `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` cannot mutate or shadow that built-in count surface. Focused `test_prg_engine_runtime_surface_functions` coverage now proves direct `ControlCount` reads match `Controls.Count`, the value updates after `RemoveObject()`, and the existing stack-frugal runtime path remains intact.
- 2026-07-04: Runtime parity slice `#3495` shipped with representative `FOR EACH ... IN <expr> FOXOBJECT` tolerance on the `#3217` lane: the parser now strips the documented trailing `FOXOBJECT` qualifier from `FOR EACH` collection expressions instead of treating it as part of the expression text, so real-source patterns such as the local ReportBuilder `FOR EACH loMember IN THIS.Objects FOXOBJECT` form can run through the existing iterative `FOR EACH` path when the underlying collection surface is available. Focused `test_prg_engine_control_flow` coverage now proves both direct-variable and dotted-member native collection expressions still iterate correctly with the `FOXOBJECT` qualifier, without regressing the existing array, scalar-expression, or native-collection `FOR EACH` baselines.
- 2026-07-04: Runtime parity slice `#3493` shipped with representative native `Collection` `FOR EACH` iteration support on the `#3217` lane: `FOR EACH <item> IN <expr>` now snapshots same-session native `Collection` objects by their contained items in insertion order instead of treating the collection object itself as one scalar element, so both direct variables and dotted member-path collection expressions iterate like real VFP code while empty collections skip the loop body cleanly. Focused `test_prg_engine_control_flow` coverage now proves direct and member-path native collection iteration plus the empty-collection skip case without regressing existing array or scalar `FOR EACH` behavior.
- 2026-07-04: Runtime parity slice `#3492` shipped with representative native `Collection` default `Item()` invocation support on the `#3217` lane: same-session native PRG collection objects can now treat `oItems(1)` and dotted member-path forms such as `oHost.oItems('second')` as default `Item(...)` calls instead of falling through the ordinary function path, while ordinary array indexing and unrelated function calls keep their existing behavior. Focused `test_prg_engine_runtime_surface_functions` coverage now proves bare collection indexing by numeric position and key across both direct and dotted-object paths without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3491` shipped with representative built-in native `Release()` no-parens and reflection parity on the `#3217` lane: same-session native PRG objects that already support the built-in release path now also treat bare dotted statements such as `oChild.Release` the same way as explicit no-arg calls, and `PEMSTATUS(..., 1|5)`, `GETPEM()`, and `AMEMBERS(..., 2|3)` now surface `Release` alongside the other representative shipped native built-ins only on the runtime objects where native release actually exists. Focused regressions now prove bare no-parens release dispatch plus reflection/member-enumeration coherence without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3490` shipped with representative built-in native `Move(nLeft[, nTop[, nWidth[, nHeight]]])` support on the `#3217` lane: same-session native visual PRG objects now honor the documented required-left plus trailing-optional geometry update contract without host-stack recursion, same-PRG `Move` overrides still win over the built-in baseline, and `PEMSTATUS(..., 1|5)`, `GETPEM()`, and `AMEMBERS(..., 2|3)` now surface `Move` only on the native visual objects where that built-in actually exists. Focused regressions now prove `Move(-5000)`-style left-only updates, four-argument geometry updates, override precedence, and reflection coherence without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3489` shipped with reflective visibility for the representative built-in native methods already supported on the `#3217` lane: `PEMSTATUS(..., 1)`, `GETPEM()`, and `AMEMBERS(..., 2|3)` now surface shipped native `Refresh`, `Show`, `Hide`, `SetFocus`, and `ResetToDefault` behavior on the same-session visual/class objects where those built-ins actually exist, while `PEMSTATUS(..., 5)` continues reporting methods as non-read-only just like ordinary method reflection. Focused regressions now prove reflective method discovery for those built-ins without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3488` shipped with representative built-in native `ResetToDefault(cProperty)` support on the `#3217` lane: same-session native PRG objects now capture an internal snapshot of their initial class-defined and seeded runtime properties during materialization, and the built-in fallback can restore those captured defaults deterministically while recording `prg.object.resettodefault` activity instead of leaving the call unsupported. Same-PRG `ResetToDefault` overrides still win over the built-in baseline, and focused regressions now prove inherited default restoration plus override precedence without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3487` shipped with representative built-in native `SetFocus()` support on the `#3217` lane: focusable same-session visual PRG objects now treat bare expression statements such as `THISFORM.txtName.SetFocus` the same way as explicit no-arg method calls when the leaf resolves to a representative built-in focus method, same-PRG `SetFocus` overrides still win over the built-in baseline, and the built-in fallback now updates owner-form `ActiveControl` metadata deterministically while recording `prg.object.setfocus` activity instead of collapsing into the property-read path. Focused regressions now prove both bare no-parens built-in dispatch and override precedence on representative native form/control hierarchies without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3486` shipped with representative built-in native `Show()` / `Hide()` support on the `#3217` lane: native visual PRG objects now treat bare expression statements such as `THISFORM.Hide` and `oForm.cmdSave.Show` the same way as explicit no-arg method calls when the leaf resolves to a representative built-in visual method, same-PRG `Show` overrides still win over the built-in baseline, and the built-in fallback now toggles `Visible` deterministically while recording `prg.object.show` / `prg.object.hide` activity instead of collapsing into the property-read path. Focused regressions now prove both bare no-parens built-in dispatch and override precedence on representative native form/control hierarchies without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3485` shipped with first-pass bare dotted native no-arg method-statement invocation plus representative built-in `Refresh()` support on the `#3217` lane: bare expression statements such as `THISFORM.Refresh` and `oContainer.child.Refresh` now execute as native method calls when the leaf resolves to an invokable native method, same-PRG `Refresh` overrides still win over any built-in baseline, and native PRG objects without an override now get a deterministic built-in `Refresh()` no-op that records runtime activity instead of collapsing into a property-read path. Focused regressions now prove both bare no-parens override dispatch and built-in fallback behavior on representative native form/control hierarchies without regressing the stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3484` shipped with first-pass native `SetAll()` support over contained object graphs on the `#3217` lane: same-session native PRG/container objects can now execute `SetAll(cProperty, value [, cClass])` through the existing iterative frame machine, walking contained descendants without host-stack recursion, honoring the optional class-name filter against the native object class name, and skipping objects that do not actually expose the requested property instead of inventing new members. Focused regressions now prove recursive descendant updates, class-filter targeting, and no-invented-property behavior on representative native form/container hierarchies.
- 2026-07-04: E3 slice `#3215` closed as evidence closure: focused `test_vfp_assets` coverage now proves the current `LBX` label-family route already resolves its memo sidecar through `.lbt` and preserves the existing DBF-family missing-sidecar validation contract, confirming the previously suspected `AssetFamily::label` drift in `asset_inspector.cpp` is no longer present in the current tree without requiring production code changes.
- 2026-07-04: Runtime parity slice `#3483` shipped with representative direct contained-object shadowing/removal hardening on the native OLE host lane: host-side `ADDPROPERTY()` and `REMOVEPROPERTY()` on native `OleControl` objects now reject representative direct contained-object member names when the container itself does not own them, so direct host/property reads, `GETPEM()`, `SETPEM()`, and `AMEMBERS()` continue resolving to the synthesized contained Automation surface instead of being shadowed or erased across `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3482` shipped with representative direct contained-object `AMEMBERS()` enumeration on the native OLE host lane: synthesized native `OleControl` Automation surfaces now seed a minimal representative member inventory for common direct contained-object introspection, and `AMEMBERS()` on native `OleControl` hosts now merges that nested inventory when the container itself does not own the member, so both explicit `.Object` enumeration and direct host enumeration expose representative direct contained-object properties/methods such as `Left`, `Visible`, and `Compose` across `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3481` shipped with representative direct contained-object `SETPEM()` updates on the native OLE host lane: `SETPEM()` on native `OleControl` hosts now falls through to the synthesized contained Automation object when the container itself does not own the updated member, so direct host/property updates stay coherent with both ordinary dotted assignment and the explicit `.Object` path across `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3480` shipped with representative direct contained-object reflection on the native OLE host lane: `GETPEM()` and `PEMSTATUS(..., 1|5)` on native `OleControl` hosts now fall through to the synthesized contained Automation object when the container itself does not own the reflected member, so direct host/property reflection matches the explicit `.Object` path across both `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the existing stack-frugal runtime path.
- 2026-07-04: Runtime parity slice `#3479` closed as evidence closure: focused regressions now prove the shipped native OleControl lane already preserves representative explicit `.Object` reference assignment flows without additional runtime changes, including direct host reads and nested `.Object` reads that both retain the transferred runtime object reference across `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects while preserving the existing stack-frugal runtime path.
- 2026-07-03: Runtime parity slice `#3478` shipped with representative OleControl conflict-sensitive `.Object` handling on the native OLE host lane: native `OleControl` hosts now preserve a host-owned `Application` conflict surface that resolves to a synthetic Visual FoxPro application object, while the contained Automation object keeps its own separate `Application` reference under `.Object`. Focused regressions now prove direct `Application.Name` / `Application.Quit()` stay distinct from `Object.Application.Name` / `Object.Application.Quit()` across both `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects, while non-conflicting direct contained-object routing from `#3477` remains intact.
- 2026-07-03: Runtime parity slice `#3477` shipped with representative direct contained-object member routing on the native OLE host lane: when a native `OleControl` host does not resolve an ordinary property or method on the container itself, direct reads, writes, and method calls now fall through to the synthesized contained Automation object instead of inventing unrelated host-only state. Focused regressions now prove direct host access such as `oHost.Left`, `oHost.Compose()`, and `oDoc.Visible` stays coherent with the explicit `.Object` path across both `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the shipped `Object`, `DoVerb()`, `ObjectVerbs`, activation-policy, or timeout-policy surfaces.
- 2026-07-03: Runtime parity slice `#3476` shipped with representative OleControl verb-list inspection on the native OLE host lane: native `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects now expose a deterministic read-only `ObjectVerbsCount` inspection property plus zero-based `ObjectVerbs(index)` inspection calls that return the representative supported named activation verbs `edit` and `open`. Focused regressions now prove ordinary `ObjectVerbsCount` reads, `GETPEM()` parity, read-only `PEMSTATUS()`, blocked `SETPem()` / `REMOVEPROPERTY()` mutation for both inspection surfaces, zero-based verb-list inspection, and unchanged named-verb `DoVerb()` activation behavior after inspection across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3475` shipped with representative OleControl named-verb activation hardening on the native OLE host lane: native `DoVerb()` calls on `AddObject(..., 'OleControl', cProgId)` ActiveX hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects now canonicalize the documented common activation names `edit` and `open` onto the same representative activation verbs as `-1` and `-2`, even when callers pass mixed-case or padded string values. Focused regressions now prove named-verb activation success, canonicalized activation intent/event tracking, and unchanged stack-frugal runtime behavior across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3474` shipped with representative OleControl timeout-policy hardening on the native OLE host lane: native `AddObject(..., 'OleControl', cProgId)` ActiveX hosts now seed writable `OLERequestPendingTimeout`, `OLEServerBusyTimeout`, and `OLEServerBusyRaiseError` surfaces, while same-PRG `DEFINE CLASS ... AS OleControl` objects preserve class-defined timeout-policy values on that same runtime lane. Focused regressions now prove ordinary reads, `GETPEM()` parity, writable `PEMSTATUS()`, successful `SETPem()` updates, preserved post-update reads for all three timeout-policy members, and unchanged `DoVerb()` activation behavior after programmatic timeout-policy changes across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3473` shipped with representative OleControl activation-policy hardening on the native OLE host lane: native `AddObject(..., 'OleControl', cProgId)` ActiveX hosts now seed the documented default `AutoActivate = 2` and `AutoVerbMenu = .T.` surfaces, while same-PRG `DEFINE CLASS ... AS OleControl` objects preserve class-defined activation-policy values on the same runtime lane. Focused regressions now prove ordinary reads, `GETPEM()` parity, writable `PEMSTATUS()`, successful `SETPem()` updates, preserved post-update reads for both properties, and unchanged `DoVerb()` activation behavior after programmatic policy changes across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3472` shipped with representative OleControl activation/automation hardening on the native OLE host lane: native `OleControl` hosts now synthesize a stable read-only `Object` surface that resolves to a nested Automation-style object reference derived from `OLEClass`, so dotted member access like `oHost.Object.Left` and `oHost.Object.Compose()` now routes through the existing generic OLE property/method model instead of collapsing onto the container, and built-in `DoVerb()` activation calls now succeed deterministically on both `AddObject(..., 'OleControl', cProgId)` hosts and same-PRG `DEFINE CLASS ... AS OleControl` objects without regressing the shipped `hWnd`, `OLEClass`, `DocumentFile`, or `OLETypeAllowed` surfaces. Focused regressions now prove ordinary reads, `GETPEM()` parity, read-only `PEMSTATUS()`, blocked `SETPEM()` / `REMOVEPROPERTY()` mutation for `Object`, nested Automation-style property/method access, representative default/edit/open `DoVerb()` success, activation intent tracking on the nested object state, and preserved stack-frugal runtime behavior across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3471` shipped with representative OLE container content/configuration hardening on the native OleControl lane: native `AddObject(..., 'OleControl', cProgId)` ActiveX host objects now preserve intentional read-only `DocumentFile` and `OLETypeAllowed` inspection surfaces alongside the shipped `OLEClass` / `hWnd` behavior, with representative ActiveX hosts defaulting to empty `DocumentFile` plus `OLETypeAllowed = -2`, while same-PRG native `DEFINE CLASS ... AS OleControl` objects preserve class-defined `DocumentFile` / `OLETypeAllowed` values as creation-time-only surfaces. Focused regressions now prove ordinary reads, `GETPEM()` parity, read-only `PEMSTATUS()`, blocked `SETPEM()` / `REMOVEPROPERTY()` mutation, and preserved stack-frugal runtime behavior across both ActiveX-host and class-defined OLE container paths.
- 2026-07-03: Runtime parity slice `#3470` shipped with representative OleControl ActiveX provenance hardening on the Windows-message lane: native `AddObject(..., 'OleControl', cProgId)` host objects now preserve the hosted ActiveX class identifier through the `OLEClass` property, `GETPEM()` / `PEMSTATUS()` now report that provenance coherently, and the runtime now treats `OLEClass` as a creation-time surface that remains read-only on existing OleControl hosts without regressing the shipped `hWnd`, `SYS(2326)` / `SYS(2327)`, or Win Msg `BINDEVENT()` behavior. Focused regressions now prove representative ActiveX host creation, ordinary `OLEClass` reads, `GETPEM()` parity, read-only `PEMSTATUS()`, blocked `SETPEM()` / `REMOVEPROPERTY()` mutation, and preserved handle/event behavior on the existing stack-frugal runtime lane.
- 2026-07-03: Runtime parity slice `#3469` shipped with representative ActiveX/OleControl `hWnd` coverage on the Windows-message lane: built-in `OleControl` runtime hosts now materialize on the native fallback path, representative `OleControl` objects now expose deterministic read-only `hWnd` metadata alongside `GETPEM()` / `PEMSTATUS()` and `SYS(2326)` / `SYS(2327)` round-trips, and the common `Form.AddObject(..., 'OleControl', cProgId)` host path now participates in focused Win Msg `BINDEVENT()` dispatch without regressing the intentional unsupported-`hWnd` fault path for native controls that do not own a window handle. Focused regressions now prove representative ActiveX host creation, read-only handle inspection, WHANDLE round-trips, and Windows-message dispatch through the existing stack-frugal event/runtime lane.
- 2026-07-03: Runtime parity slice `#3468` shipped with focused native visual-control `hWnd` hardening on the Windows-message lane: declarative built-in VFP child controls now instantiate as native runtime objects instead of falling through the generic OLE placeholder path, indented class-body `ADD OBJECT` / `OBJECT ... ENDOBJECT` declarations now parse into that runtime object graph, and representative native controls without their own window handles now fail direct `.hWnd` reads intentionally while `GETPEM()`, `PEMSTATUS()`, and `SETPEM()` continue reporting coherent unsupported-member behavior. Focused regressions now prove `CommandButton`, `TextBox`, and `Container` children round-trip through the native object graph, reject unsupported direct `hWnd` reads with catchable missing-member diagnostics, and preserve the existing stack-frugal event/runtime lane.
- 2026-07-03: Runtime parity slice `#3467` shipped with first-pass handle-access coverage for the new Windows-message event lane: ordinary `_SCREEN.hWnd` and `_VFP.hWnd` reads now resolve intentionally through the runtime, native `Form` and `Toolbar` objects now surface read-only `hWnd` metadata through ordinary property reads plus `GETPEM()` / `PEMSTATUS()`, and `SYS(2326)` / `SYS(2327)` now provide a deterministic WHANDLE-to-`hWnd` bridge for those representative runtime windows and objects without destabilizing the existing stack-frugal event path. Focused regressions now prove round-trip translation, read-only behavior, native form/toolbar property access, and Windows-message `BINDEVENT()` dispatch through both `_SCREEN.hWnd` and native form `hWnd` values while preserving the existing runtime object inventory and prior event-lane coverage.
- 2026-07-03: Runtime parity slice `#3466` shipped with first-pass Windows-message event binding support on the native event lane: `BINDEVENT(hWnd | 0, nMessage, oHandler, cDelegate)`, `AEVENTS(array, 1)`, and `UNBINDEVENTS(hWnd[, nMessage])` now preserve representative Win Msg bindings in the runtime session; `AEVENTS(array, 0)` now reports coherent current-message metadata during delegate dispatch with system event type `0`; and `PrgRuntimeSession` now exposes a focused host-dispatch seam for representative window-message delivery through the existing stack-frugal frame machine while the runtime is paused in `READ EVENTS`. Focused regressions now prove exact-hWnd and wildcard-hWnd dispatch, binding inspection, targeted unbinding, integer handler return propagation, and event-loop restoration/exit behavior without regressing the shipped native object/property event lanes.
- 2026-07-03: Runtime parity slice `#3465` closed as evidence closure: focused regressions now prove the shipped native property-event lane from `#3462` already covers representative handler-object delegates whose implementations resolve through an external PRG base without additional runtime changes, including dotted property reads/writes, `GETPEM()` / `SETPEM()` participation, inherited handler-method dispatch, coherent `AEVENTS(array, 0)` current-event metadata during property-triggered handlers, preserved external handler class-library provenance, and the existing stack-frugal event-dispatch path.
- 2026-07-03: Runtime parity slice `#3464` closed as evidence closure: focused regressions now prove the shipped native property-event lane from `#3462` already covers representative same-session native object-method delegates without additional runtime changes, including dotted property reads/writes, `GETPEM()` / `SETPEM()` participation, handler-object method dispatch, and coherent `AEVENTS(array, 0)` current-event metadata during property-triggered handlers on the existing stack-frugal event-dispatch path.
- 2026-07-03: Runtime parity slice `#3463` closed as evidence closure: focused external-base regressions now prove the shipped native property-event lane from `#3462` already covers representative external PRG base `ACCESS` / `ASSIGN` behavior without additional runtime changes, including dotted property reads/writes, `GETPEM()` / `SETPEM()` participation, and coherent `AEVENTS(array, 0)` current-event metadata during property-triggered handlers while preserving the external base class-library provenance and the existing stack-frugal event-dispatch path.
- 2026-07-03: Runtime parity slice `#3462` shipped with first-pass native property-event dispatch on the same-session PRG event lane: representative `BINDEVENT()` bindings on native property names now observe dotted property reads/writes plus `GETPEM()` / `SETPEM()` activity through the existing stack-frugal event-dispatch path, including accessor-backed members, raw-property fallback, before/after flags, and `AEVENTS(array, 0)` current-event metadata during property-triggered handlers. Focused regressions now prove same-PRG property access/assignment dispatch without regressing existing native property semantics, unknown/readonly `SETPEM()` failure behavior, or COM paths.
- 2026-07-03: Runtime parity slice `#3461` shipped with first-pass `AEVENTS(array, 0)` current-event metadata on the same-session native PRG event lane: native delegate handlers can now inspect a deterministic three-element array containing the active source object, normalized event name, and representative trigger type (`1` for `RAISEEVENT()`, `2` for direct method-call dispatch) while a bound handler is running; the runtime preserves nested-safe current-event context only within delegate execution and leaves existing or missing target arrays untouched when `AEVENTS(array, 0)` is called outside an active event handler. Focused regressions now prove both trigger types plus the no-clobber rule without regressing the stack-frugal native runtime path.
- 2026-07-03: Runtime parity slice `#3460` shipped with first-pass `AEVENTS()` support over the same-session native PRG event-binding lane: `AEVENTS(array, oObject)` now enumerates representative object-bound native event rows in a deterministic five-column runtime-array shape over the `#3459` binding table, preserves object references for source/handler rows, keeps top-level routine delegate rows machine-readable with an empty counterpart-object cell, and leaves existing or missing target arrays untouched when the matching row count is zero; focused regressions now prove source/handler enumeration plus targeted `UNBINDEVENTS()` coherence without regressing the stack-frugal native runtime path.
- 2026-07-03: Runtime parity slice `#3459` shipped with first-pass same-session native PRG event binding support: the expression/runtime layer now supports `BINDEVENT()`, `RAISEEVENT()`, and `UNBINDEVENTS()` over native PRG objects in the current session, including native-method and top-level routine delegates, before/after dispatch flags, deterministic unbinding, runtime event telemetry, released-object binding cleanup, and focused regressions that prove explicit raises, direct method-call trigger behavior, and stack-frugal handler execution without regressing existing native-object or COM behavior.
- 2026-07-03: Runtime parity slice `#3458` shipped as evidence closure: focused control-flow regressions now prove representative `THROW oException` chaining already matches the documented VFP outer-exception shape, with a new outer `Exception` object, `User Thrown Error.` / `2071` compatibility surfaces, and `UserValue` preserving an object reference to the original inner exception while `AERROR()`, `ERROR()`, `MESSAGE()`, and `LINENO()` continue reporting the outer throw site without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3457` shipped with focused `TRY/CATCH/FINALLY` outward-propagation hardening plus regressions: unmatched inner faults that run through `FINALLY` now re-propagate outward after cleanup instead of being swallowed; outer `CATCH` and `ON ERROR` handlers now still see the original fault message, line number, statement text, and error code after inner `FINALLY` completes; and the implementation preserves the existing stack-frugal frame machine and `CATCH WHEN` / `THROW` parity while keeping `FINALLY` cleanup execution intact before escalation.
- 2026-07-03: Runtime parity slice `#3456` shipped with focused `CATCH WHEN` filtering plus regressions: the PRG parser now splits `CATCH` clauses into the optional `TO <var>` binding and optional top-level `WHEN <expr>` predicate; `TRY` state now tracks every catch clause in lexical order instead of only the first one; runtime fault dispatch now evaluates selective catch predicates against the native caught `Exception` object, resets non-matching `TO <var>` bindings back to empty (`.NULL.`-like) state before later clauses run, and falls through to later `CATCH` clauses or outer handlers when no inner clause matches, without regressing stack-frugal execution or the shipped `THROW`/`CATCH TO` parity.
- 2026-07-03: Runtime parity slice `#3455` shipped with documented `THROW` parity hardening plus focused regressions: handled `THROW <expr>` now surfaces the representative VFP user-thrown code/message path (`2071`, localized `User Thrown Error.`) while preserving `UserValue` through the synthetic native `Exception` object, `AERROR()`, `ERROR()`, and `MESSAGE()`; bare `THROW` now rethrows the active exception context instead of manufacturing an empty generic fault, including same-object propagation for `CATCH TO` rethrows and the documented default user-thrown path when no active exception exists; and the runtime catalogs now carry the new user-thrown diagnostic key across `en-US`, `es-419`, `pt-BR`, and `qps-ploc`.
- 2026-07-03: Runtime parity slice `#3454` shipped with first-pass `THROW` command parity plus focused regressions: the PRG parser, runtime dispatcher, and AST/IR manifest now recognize `THROW`; handled `TRY/CATCH` flows now preserve representative thrown `Exception.UserValue` metadata through the native synthetic `Exception` object, `AERROR()`, `ERROR()`, `MESSAGE()`, `LINENO()`, and `PROGRAM()` surfaces; and the generic runtime fault snapshot now retains thrown-user-value compatibility payloads long enough for catch-path materialization without regressing stack-frugal execution or localized diagnostics.
- 2026-07-03: Runtime parity slice `#3453` shipped with a targeted `TRY/CATCH` runtime fix plus focused regressions: `CATCH TO <var>` now binds a synthetic native `Exception` object instead of a plain string, with representative `Exception` identity and metadata surfaces (`Message`, `ErrorNo`, `LineNo`, `Procedure`, `Details`, `LineContents`, `StackLevel`, `UserValue`) sourced from the existing runtime error snapshot while preserving `AERROR()`, `ERROR()`, `MESSAGE()`, `LINENO()`, `PROGRAM()`, stack-frugal execution, and localized diagnostics; shared `TYPE()`/`VARTYPE()` classification now also recognizes runtime object-handle references as objects (`O`) instead of character values.
- 2026-07-03: Runtime parity slice `#3452` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Exception ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3451` shipped with a targeted runtime fix plus focused regressions: same-PRG native `DEFINE CLASS ... AS Collection ... ENDDEFINE` classes now instantiate through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, stable COM behavior, and representative built-in `Collection` member behavior (`Add`, `Count`, `Item`, `Remove`) instead of falling through the generic OLE placeholder path.
- 2026-07-03: Runtime parity slice `#3450` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS CursorAdapter ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3449` closed as an evidence-audit follow-on: existing focused runtime-surface coverage already proved representative same-PRG native `DEFINE CLASS ... AS Session OLEPUBLIC ... ENDDEFINE` classes instantiate through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior via the shipped `#3417` regression, so no additional runtime or test changes were needed.
- 2026-07-03: Runtime parity slice `#3448` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS ProjectHook ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3447` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS StatusBar ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3446` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS ToolBar ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3445` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Separator ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3444` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Relation ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3443` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS PageFooter ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3442` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS PageHeader ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3441` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS OleControl ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3440` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS CommandGroup ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3439` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS OptionGroup ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3438` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Line ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3437` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Shape ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3436` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Timer ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3435` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Image ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3434` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS EditBox ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3433` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Spinner ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3432` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS OptionButton ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3431` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS CheckBox ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3430` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS ListBox ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3429` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS ComboBox ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3428` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Header ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3427` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Column ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3426` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Grid ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3425` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS PageFrame ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3424` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Page ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3423` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Container ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3422` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Label ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3421` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS TextBox ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3420` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS CommandButton ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3419` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS FormSet ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3418` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Form ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3417` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Session OLEPUBLIC ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3416` shipped as evidence closure: focused runtime-surface regressions now prove representative same-PRG native `DEFINE CLASS ... AS Session ... ENDDEFINE` classes can be instantiated through `CREATEOBJECT()` and `NEWOBJECT()` with ordinary property initialization, method invocation, `BaseClass`/`Class` identity surfaces, native class-hierarchy storage, empty same-PRG `ClassLibrary` semantics, and stable COM behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3413` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children whose child class resolves only from the derived startup PRG keep dotted child reads, dotted child writes, dotted parent round-trips, and child method calls resolving against the live child object and owner chain across representative `CREATEOBJECT()` and `NEWOBJECT()` flows while preserving the already-shipped same-PRG source identity, caption overrides, empty `ClassLibrary` semantics, owner dispatch, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3412` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent runtime `AddObject()` children whose child class resolves only from the derived startup PRG keep dotted child reads, dotted child writes, dotted parent round-trips, and child method calls resolving against the live child object and owner chain across representative `CREATEOBJECT()` and `NEWOBJECT()` flows while preserving the already-shipped same-PRG source identity, empty `ClassLibrary` semantics, owner dispatch, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3411` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent declarative `ADD OBJECT` children whose child class resolves only from the derived startup PRG keep dotted child reads, dotted child writes, dotted parent round-trips, and child method calls resolving against the live child object and owner chain across representative `CREATEOBJECT()` and `NEWOBJECT()` flows while preserving the already-shipped same-PRG source identity, empty `ClassLibrary` semantics, owner dispatch, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3410` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent declarative `ADD OBJECT` children whose child class resolves only from the derived startup PRG reject representative `NEWOBJECT()` leaf `Class`, `BaseClass`, `ParentClass`, and empty `ClassLibrary` mutation attempts across `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct assignment while preserving the already-shipped activation, provenance, owner dispatch, same-PRG class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3409` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent runtime `AddObject()` children whose child class resolves only from the derived startup PRG reject representative `NEWOBJECT()` leaf `Class`, `BaseClass`, `ParentClass`, and empty `ClassLibrary` mutation attempts across `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct assignment while preserving the already-shipped activation, provenance, owner dispatch, same-PRG class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3408` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children whose child class resolves only from the derived startup PRG reject representative `NEWOBJECT()` leaf `Class`, `BaseClass`, `ParentClass`, and empty `ClassLibrary` mutation attempts across `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct assignment while preserving activation, provenance, caption overrides, owner dispatch, same-PRG class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3407` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent declarative `ADD OBJECT` children whose child class resolves only from the derived startup PRG keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing activation, provenance, owner dispatch, same-PRG empty-`ClassLibrary` semantics, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3406` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent runtime `AddObject()` children whose child class resolves only from the derived startup PRG keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing activation, provenance, owner dispatch, same-PRG empty-`ClassLibrary` semantics, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3405` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children whose child class resolves only from the derived startup PRG keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing activation, provenance, caption overrides, owner dispatch, same-PRG empty-`ClassLibrary` semantics, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3404` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent declarative `ADD OBJECT` children whose child class is defined in the derived startup PRG and preserves a non-empty immediate external `ClassLibrary` path keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing owner dispatch, source identity, immediate external `ClassLibrary` provenance, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3403` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent runtime `AddObject()` children whose child class is defined in the derived startup PRG and preserves a non-empty immediate external `ClassLibrary` path keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing owner dispatch, source identity, immediate external `ClassLibrary` provenance, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3402` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children whose child class is defined in the derived startup PRG and preserves a non-empty immediate external `ClassLibrary` path keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing owner dispatch, source identity, immediate external `ClassLibrary` provenance, class-hierarchy storage, and COM/plain-object behavior, without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3401` shipped: focused runtime-surface regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children whose child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep representative `NEWOBJECT()` leaf ordinary reads, `GETPEM()`, `PEMSTATUS()`, `AMEMBERS()`, and `ACLASS()` surfaces coherent alongside existing owner dispatch, source identity, immediate external `ClassLibrary` provenance, and COM/plain-object behavior. While landing that evidence closure, the expression parser was also hardened so `ACLASS(...)` remains a function call even after populating `aClass` arrays, preserving common-in-the-wild VFP reflection flows under the existing stack-frugal runtime frame machine.
- 2026-07-03: Runtime parity slice `#3400` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-base runtime `AddObject()` children whose added child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep `Class`, `BaseClass`, `ParentClass`, immediate external `ClassLibrary`, source identity, `GETPEM()` / `PEMSTATUS()`, `AMEMBERS()`, `ACLASS()`, and stored-object provenance surfaces coherent across representative `NEWOBJECT()` leaf graphs as well as existing `CREATEOBJECT()` flows, while preserving COM/plain-object behavior and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3399` shipped as evidence closure: focused runtime-surface regressions now prove inherited external-base runtime `AddObject()` children whose added child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path reject `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment shadowing across representative `NEWOBJECT()` leaf graphs as well as existing `CREATEOBJECT()` flows, while preserving source identity, immediate external `ClassLibrary` provenance, reflection/member surfaces, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3398` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep dotted child reads, dotted child writes, dotted parent round-trips, and child method calls resolving against the live child object and owner chain across representative `CREATEOBJECT()` and `NEWOBJECT()` flows while preserving source identity, immediate external `ClassLibrary` provenance, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3397` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep `Class`, `BaseClass`, `ParentClass`, and immediate external `ClassLibrary` metadata coherent across `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment attempts while preserving source identity, reflection/member surfaces, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3396` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, immediate external `ClassLibrary` provenance, reflection/member surfaces, and runtime hierarchy behavior across representative `CREATEOBJECT()` and `NEWOBJECT()` flows while preserving owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3395` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep dotted child reads, dotted child writes, dotted parent round-trips, and child method calls resolving against the live child object and owner chain while preserving source identity, immediate external `ClassLibrary`, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3394` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep `Class`, `BaseClass`, `ParentClass`, and immediate external `ClassLibrary` metadata coherent across `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment attempts while preserving owner dispatch, reflection/member surfaces, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3393` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native declarative `ADD OBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, immediate external `ClassLibrary` provenance, reflection/member surfaces, and runtime hierarchy behavior while preserving owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3392` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `OBJECT ... ENDOBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving source identity, immediate external `ClassLibrary` provenance, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3391` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `OBJECT ... ENDOBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep `Class`, `BaseClass`, `ParentClass`, and immediate external `ClassLibrary` metadata coherent after `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment attempts while preserving owner dispatch, source identity, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3390` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `OBJECT ... ENDOBJECT` children whose nested child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, immediate external `ClassLibrary` provenance, reflection/member surfaces, and runtime hierarchy behavior while preserving owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3389` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native runtime `AddObject()` children whose added child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving child-definition source identity, immediate external `ClassLibrary` provenance, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3388` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base runtime `AddObject()` children whose added child class preserves both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object across `CREATEOBJECT` and `NEWOBJECT` variants while preserving child-definition source identity, immediate external `ClassLibrary` provenance, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3387` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children whose child class is defined in the inherited external parent library while preserving both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object across `CREATEOBJECT` and `NEWOBJECT` variants while preserving child-definition source identity, immediate external `ClassLibrary` provenance, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3386` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children whose child class is defined in the inherited external parent library while preserving both deeper ancestry and a non-empty immediate external `ClassLibrary` path reject direct-assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` shadowing of `Class`, `BaseClass`, `ParentClass`, and immediate external `ClassLibrary` metadata while preserving child-definition source identity, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3385` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children whose child class is defined in the inherited external parent library while preserving both deeper ancestry and a non-empty immediate external `ClassLibrary` path keep coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, immediate external `ClassLibrary` provenance, and `ACLASS()` hierarchy across ordinary reads, reflection, contained-owner dispatch, and leaf inheritance flows while preserving COM/plain-object behavior and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3384` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` whose derived child class itself inherits from a deeper external base reject direct-assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` shadowing of `Class`, `BaseClass`, `ParentClass`, and derived empty-`ClassLibrary` semantics while preserving child-definition source identity, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3383` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` whose derived child class itself inherits from a deeper external base preserve coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, `ACLASS()` hierarchy, and derived empty-`ClassLibrary` semantics across ordinary reads, reflection, contained-owner dispatch, and leaf inheritance flows while preserving COM/plain-object behavior and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3382` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` reject direct-assignment, `SETPem()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` shadowing of `Class`, `BaseClass`, `ParentClass`, and immediate external `ClassLibrary` metadata while preserving child-definition source identity, owner dispatch, COM/plain-object behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3381` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` preserve coherent `Class`, `BaseClass`, `ParentClass`, child-definition source identity, and immediate external `ClassLibrary` provenance across ordinary reads, reflection, `ACLASS()`, contained-owner dispatch, and leaf inheritance flows while preserving COM/plain-object behavior and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3380` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object when the child class itself directly inherits from an external base, while preserving child definition source, immediate external `ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3379` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base nested `OBJECT ... ENDOBJECT` children sourced through `OBJECT cmdSave AS SaveButton OF buttons.prg` keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object even when the derived child class is defined in that explicit external PRG library while inheriting from a deeper external base, while preserving child definition source, empty derived `ClassLibrary` semantics, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3378` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base declarative `ADD OBJECT ... OF buttons.prg` children whose derived class is defined in that explicit external PRG library while inheriting from a deeper external base keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving child definition source, empty derived `ClassLibrary` semantics, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3377` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base declarative `ADD OBJECT ... OF buttons.prg` children whose derived class is sourced from an explicit external PRG library keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving child definition source, immediate external `ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3376` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base runtime `AddObject()` children whose derived class is sourced from an explicit external PRG library and whose parent class comes from a deeper external library keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving child definition source, deeper ancestry coherence, empty derived `ClassLibrary` semantics, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3375` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-base runtime `AddObject()` children sourced from an explicit external PRG library keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving child definition source, immediate external `ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal activation path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3374` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children declared only in the derived startup PRG keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving object-block caption overrides, external-base `BaseClass`/`ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3373` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent declarative `ADD OBJECT` children declared only in the derived startup PRG keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving external-base `BaseClass`/`ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3372` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent runtime `AddObject()` children declared only in the derived startup PRG keep representative dotted reads, dotted writes, dotted parent round-trips, and dotted child method calls resolving against the live child object while preserving external-base `BaseClass`/`ClassLibrary` provenance, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3371` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent runtime `AddObject()` children declared only in the derived startup PRG keep external-base `Class`, `BaseClass`, `ParentClass`, and `ClassLibrary` metadata read-only and resistant to `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment shadowing while preserving source identity, child usability, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3370` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent runtime `AddObject()` children declared only in the derived startup PRG preserve coherent external-base provenance after fallback materialization, keeping `Class`, `BaseClass`, `ParentClass`, `ClassLibrary`, `ACLASS`, source identity, parent/owner wiring, child usability, COM behavior, and the existing stack-frugal fallback path intact without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3369` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent declarative `ADD OBJECT` children declared only in the derived startup PRG keep external-base `Class`, `BaseClass`, `ParentClass`, and `ClassLibrary` metadata read-only and resistant to `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment shadowing while preserving source identity, child usability, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3368` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent declarative `ADD OBJECT` children declared only in the derived startup PRG preserve coherent external-base provenance after fallback materialization, keeping `Class`, `BaseClass`, `ParentClass`, `ClassLibrary`, `ACLASS`, source identity, parent/owner wiring, child usability, COM behavior, and the existing stack-frugal fallback path intact without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3367` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children declared only in the derived startup PRG keep external-base `Class`, `BaseClass`, `ParentClass`, and `ClassLibrary` metadata read-only and resistant to `SETPem()`, `ADDPROPERTY()`, `REMOVEPROPERTY()`, and direct-assignment shadowing while preserving source identity, child usability, owner dispatch, COM behavior, and the existing stack-frugal fallback path without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3366` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children can preserve coherent external-base provenance when the child class exists only in the derived startup PRG, keeping `Class`, `BaseClass`, `ParentClass`, `ClassLibrary`, `ACLASS`, source identity, parent/owner wiring, object-block property overrides, COM behavior, and the existing stack-frugal fallback path intact without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3365` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external-parent nested `OBJECT ... ENDOBJECT` children can instantiate when the child class exists only in the derived startup PRG, preserving object-block property overrides, child usability, parent/owner wiring, COM behavior, and the same stack-frugal runtime path already used by the shipped declarative and runtime `AddObject()` fallbacks.
- 2026-07-03: Runtime parity slice `#3364` shipped: inherited external-parent declarative child materialization now falls back from the inherited class body's defining PRG to the live derived owner's source PRG when no explicit child class library is provided and the requested declarative child class is not present in the inherited library, so representative `ChildForm AS ParentForm OF widgetlib.prg` graphs can instantiate declarative child classes declared only in the derived startup PRG while preserving shipped runtime `AddObject()` fallback behavior, child usability, COM behavior, and the stack-frugal iterative runtime path.
- 2026-07-03: Runtime parity slice `#3363` shipped: inherited external-parent native `AddObject()` activation now falls back from the inherited method's defining PRG to the live derived owner's source PRG when no explicit class library is provided and the requested child class is not present in the inherited library, so representative `ChildForm AS ParentForm OF widgetlib.prg` graphs can materialize child classes declared only in the derived startup PRG while preserving shipped external-library resolution, child usability, COM behavior, and the stack-frugal iterative runtime path.
- 2026-07-03: Runtime parity slice `#3362` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `THISFORM` and `THISFORMSET` resolve the live derived owner across representative inherited external-parent native class graphs whose child behavior is sourced through an external base control library, preserving inherited external-base child-to-owner method dispatch, live owner-reference mutation, derived-owner identity over inherited base placeholders, and existing stack-frugal PRG runtime behavior without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3361` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `THISFORM` and `THISFORMSET` resolve the live derived owner across representative inherited external-parent native class graphs, preserving child-to-owner method dispatch, live owner-reference mutation, derived-owner identity over inherited base placeholders, and existing stack-frugal PRG runtime behavior without additional runtime changes.
- 2026-07-03: Runtime parity slice `#3360` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited object-block external-base native subtrees whose derived owner is instantiated from an external parent class library, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, non-empty immediate external `ClassLibrary` provenance with coherent external-library source identity, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3359` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative object-block external-base native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, non-empty immediate external `ClassLibrary` provenance with coherent local source identity, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3358` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited object-block external-child native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, external-child source and empty derived `ClassLibrary` coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3357` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative object-block external-child native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, external-child source and empty derived `ClassLibrary` coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3356` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited object-block same-PRG native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, same-PRG inherited source/class identity coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3355` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative object-block same-PRG native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, surviving sibling object state, same-PRG object-block source/class identity coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3354` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited declarative external-base native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, non-empty immediate external `ClassLibrary` provenance with coherent local source identity, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3353` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative declarative external-base native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, non-empty immediate external `ClassLibrary` provenance with coherent local source identity, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3352` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited declarative external-child native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, the shipped split between library-backed source provenance and empty derived `ClassLibrary` semantics, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3351` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative declarative external-child native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, the shipped split between library-backed source provenance and empty derived `ClassLibrary` semantics, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3350` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited declarative same-PRG native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, same-PRG declarative ancestry coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3349` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative declarative same-PRG native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, same-PRG declarative ancestry coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3348` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited runtime-created same-PRG native subtrees, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, same-PRG ancestry coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3347` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative runtime-created same-PRG native subtrees, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, same-PRG ancestry coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3346` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited runtime-created external-child native subtrees whose detached child is sourced through an explicit PRG library but carries empty derived `ClassLibrary` semantics, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, empty-derived ancestry/provenance coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3345` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative inherited runtime-created external-base native subtrees whose detached child is defined in an external parent library and preserves non-empty immediate `ClassLibrary` provenance, preserving derived-owner slot removal, detached-child `Parent` clearing, live detached descendant state, external-base ancestry/provenance coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3344` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative runtime-created external-base native subtrees whose detached child is defined in the active PRG but preserves non-empty immediate `ClassLibrary` provenance from an external base, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, external-base ancestry/provenance coherence, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3343` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `RemoveObject()` detaches representative runtime-created external-child native subtrees whose detached child is sourced through an explicit PRG library but carries empty derived `ClassLibrary` semantics, preserving owner-slot removal, detached-child `Parent` clearing, live detached descendant state, empty-derived ancestry/provenance semantics, and existing COM behavior under the shipped stack-frugal containment path.
- 2026-07-03: Runtime parity slice `#3342` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove standalone `Release()` on representative native object subtrees whose direct object is sourced externally but carries empty derived `ClassLibrary` semantics recursively tears down descendant and object state, preserving descendant-before-object destroy ordering, released-subtree invalidation, empty-derived external-child ancestry semantics, release-event dispatch, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3341` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove standalone `Release()` on representative native object subtrees whose direct object itself inherits externally (non-empty immediate `ClassLibrary` provenance) recursively tears down descendant and object state, preserving descendant-before-object destroy ordering, released-subtree invalidation, external-base object ancestry semantics, release-event dispatch, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3340` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative object-block child subtrees whose leaf child class itself inherits externally (non-empty immediate `ClassLibrary` provenance) recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, external-base child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3339` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative object-block child subtrees whose leaf child class itself inherits externally (non-empty immediate `ClassLibrary` provenance) detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, released-subtree invalidation, external-base child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3338` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative declarative child subtrees whose leaf child class itself inherits externally (non-empty immediate `ClassLibrary` provenance) recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, external-base child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3337` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited same-PRG contained native child subtrees activated through runtime `AddObject(...)` paths recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, same-PRG child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3336` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited external contained native child subtrees activated through runtime `AddObject(...)` paths with external PRG-backed child classes recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, external child ancestry semantics, non-empty immediate `ClassLibrary` provenance, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3335` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited external contained native child subtrees declared through declarative `ADD OBJECT` lines whose child class is sourced through `OF <file>.prg` recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, external child ancestry semantics, empty derived `ClassLibrary` semantics where the immediate parent class is same-program, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3334` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited same-PRG contained native child subtrees declared through declarative `ADD OBJECT` lines recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, same-PRG child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3333` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited same-PRG contained native child subtrees declared through nested `OBJECT ... ENDOBJECT` blocks recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, same-PRG child ancestry semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3332` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove owner `Release()` on representative same-PRG and inherited-external contained native child subtrees declared through nested `OBJECT ... ENDOBJECT` blocks whose child class is sourced through `OF <file>.prg` recursively tears down descendant, child, and owner state, preserving descendant-before-child-before-owner destroy ordering, owner and child visibility during destroy, released ownership-chain invalidation, external child ancestry/hierarchy semantics, empty derived `ClassLibrary` semantics where the immediate parent class is same-program, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3331` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative same-PRG and inherited-external contained native child subtrees declared through nested `OBJECT ... ENDOBJECT` blocks whose child class is sourced through `OF <file>.prg` detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, surviving sibling state, released-subtree invalidation, external child ancestry/hierarchy semantics, empty derived `ClassLibrary` semantics where the immediate parent class is same-program, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3330` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative same-PRG and inherited-external contained native child subtrees declared through nested `OBJECT ... ENDOBJECT` blocks detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, surviving sibling state, released-subtree invalidation, object-block child materialization semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3329` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative declarative same-PRG and inherited same-PRG contained native child subtrees detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, surviving sibling state, released-subtree invalidation, empty same-PRG `ClassLibrary` semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3328` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative declarative external-base and inherited-declarative contained native child subtrees detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, surviving sibling state, released-subtree invalidation, declarative external-child source semantics, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3327` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative external-base and inherited-external contained native child subtrees detaches the released subtree while the owner and surviving sibling remain alive, preserving descendant-before-child destroy ordering, owner visibility during child and descendant destroy, surviving sibling state, released-subtree invalidation, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3326` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on representative external-base and inherited-external contained native children detaches the released child while the owner and surviving sibling remain alive, preserving external `ClassLibrary` provenance before release, owner visibility during child destroy, surviving sibling state, released-child invalidation, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3325` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove direct `Release()` on a same-PRG contained native child detaches the released child subtree while leaving the owner and surviving siblings alive, preserving child/descendant destroy ordering, owner visibility during child destroy, surviving sibling state, released-child invalidation, and existing COM behavior under the shipped stack-frugal lifecycle path.
- 2026-07-03: Runtime parity slice `#3324` shipped: native PRG-defined objects now treat `Release()` as a first-pass lifecycle operation instead of a generic OLE-shaped member call, dispatching native `Destroy` methods through the existing stack-frugal iterative frame machine, recursively releasing contained native children before owner teardown, detaching released objects from parent ownership chains, and invalidating released runtime handles so `GETPEM()` / `PEMSTATUS()` no longer surface stale state after release. Focused runtime-surface and control-flow regressions prove representative standalone and contained-owner release flows preserve expected destroy ordering, parent visibility during child destroy, released-object invalidation, and existing COM behavior.
- 2026-07-03: Runtime parity slice `#3323` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper runtime-created child objects whose leaf class itself inherits externally already reject ordinary direct-assignment shadowing for `Class`, `BaseClass`, `ParentClass`, and the non-empty immediate `ClassLibrary` provenance across both local-form and inherited-external `AddObject()` topologies, while preserving the defining child-class source path, immediate external `ClassLibrary` path, and deeper stored class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity/provenance coverage for both empty-leaf-`ClassLibrary` and non-empty-immediate-`ClassLibrary` topologies, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3322` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper runtime-created child objects whose leaf class itself inherits externally already reject `REMOVEPROPERTY()` erasure for `Class`, `BaseClass`, `ParentClass`, and the non-empty immediate `ClassLibrary` provenance across both local-form and inherited-external `AddObject()` topologies, while preserving the defining child-class source path, immediate external `ClassLibrary` path, and deeper stored class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity/provenance coverage for both empty-leaf-`ClassLibrary` and non-empty-immediate-`ClassLibrary` topologies, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3321` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper runtime-created child objects whose leaf class itself inherits externally already reject `ADDPROPERTY()` shadow creation for `Class`, `BaseClass`, `ParentClass`, and the non-empty immediate `ClassLibrary` provenance across both local-form and inherited-external `AddObject()` topologies, while preserving the defining child-class source path, immediate external `ClassLibrary` path, and deeper stored class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity/provenance coverage for both empty-leaf-`ClassLibrary` and non-empty-immediate-`ClassLibrary` topologies, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3320` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper runtime-created child objects whose leaf class itself inherits externally already keep `Class`, `BaseClass`, `ParentClass`, and the non-empty immediate `ClassLibrary` provenance read-only to `SETPEM()` across both local-form and inherited-external `AddObject()` topologies, while preserving the defining child-class source path, immediate external `ClassLibrary` path, and deeper stored class hierarchy without materializing writable shadow properties. The shipped deeper runtime-created child identity/provenance coverage for both empty-leaf-`ClassLibrary` and non-empty-immediate-`ClassLibrary` topologies, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3319` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper runtime-created child objects whose leaf class itself inherits externally already surface the expected non-empty immediate `ClassLibrary` provenance, immediate `BaseClass` / `ParentClass` identity, and deeper stored class hierarchy across both local-form and inherited-external `AddObject()` topologies, while preserving the defining child-class source path and avoiding conflicting stored properties. The shipped deeper runtime-created child identity/provenance coverage for the mixed-chain empty-`ClassLibrary` topology, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3318` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper or mixed external runtime-created child objects already reject ordinary direct-assignment shadowing for `Class`, `BaseClass`, `ParentClass`, and the leaf child's empty `ClassLibrary` semantics across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, immediate `BaseClass` / `ParentClass` identity, empty derived-child `ClassLibrary`, and deeper runtime-object class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity reads/reflection, deeper runtime-created child `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` guards, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3317` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper or mixed external runtime-created child objects already reject `REMOVEPROPERTY()` erasure for `Class`, `BaseClass`, `ParentClass`, and the leaf child's empty `ClassLibrary` semantics across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, immediate `BaseClass` / `ParentClass` identity, empty derived-child `ClassLibrary`, and deeper runtime-object class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity reads/reflection, deeper runtime-created child `SETPEM()` / `ADDPROPERTY()` guards, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3316` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper or mixed external runtime-created child objects already reject `ADDPROPERTY()` shadow creation for `Class`, `BaseClass`, `ParentClass`, and the leaf child's empty `ClassLibrary` semantics across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, immediate `BaseClass` / `ParentClass` identity, empty derived-child `ClassLibrary`, and deeper runtime-object class hierarchy without materializing conflicting stored properties. The shipped deeper runtime-created child identity reads/reflection, deeper runtime-created child `SETPEM()` guards, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3315` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper or mixed external runtime-created child objects already keep `Class`, `BaseClass`, `ParentClass`, and the leaf child's empty `ClassLibrary` semantics read-only to `SETPEM()` across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, immediate `BaseClass` / `ParentClass` identity, empty derived-child `ClassLibrary`, and deeper runtime-object class hierarchy without materializing writable shadow properties. The shipped deeper runtime-created child identity reads/reflection, contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3314` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove deeper or mixed external runtime-created child objects already surface coherent identity metadata through ordinary dotted reads, `GETPEM()` / `PEMSTATUS()`, and `AMEMBERS(..., 1|3)` across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, empty derived-child `ClassLibrary`, immediate `BaseClass` / `ParentClass` identity, and deeper runtime-object class hierarchy. The shipped contained-child `ACLASS()` reflection, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3313` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove runtime-created child objects loaded through deeper or mixed external PRG inheritance chains already reflect coherent inheritance rows through `ACLASS()` across both local-form and inherited-external `AddObject()` topologies, while preserving the expected child definition source path, empty-vs-external `ClassLibrary` semantics, and deeper runtime-object class hierarchy. The shipped contained-child identity reflection, ordinary child identity reads, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3312` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove runtime-created child objects with external class bases already reflect coherent inheritance chains through `ACLASS()` across both local-form and inherited-external `AddObject()` topologies, while preserving the expected external PRG provenance and child runtime-object class hierarchy. The shipped contained-child identity reflection, ordinary child identity reads, declarative and runtime-created external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3311` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove runtime-created native child objects with external class bases already surface coherent `ClassLibrary` provenance across identity surfaces and representative guarded-mutation cases, including both local-form and inherited-external `AddObject()` topologies. The shipped contained-child identity reflection, ordinary child identity reads, contained-child `ACLASS()` reflection, declarative external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3310` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove externally inherited contained child `ClassLibrary` provenance already stays coherent through deeper child inheritance chains, including representative guarded-mutation cases, across both same-PRG and external-form topologies. The shipped contained-child identity reflection, ordinary child identity reads, contained-child `ACLASS()` reflection, external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3309` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove the existing identity mutation guards already keep a non-empty contained-child `ClassLibrary` value intact across `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` / ordinary direct assignment when the child class itself inherits externally, without materializing conflicting shadow properties. The shipped contained-child identity reflection, ordinary child identity reads, contained-child `ACLASS()` reflection, external child-class `ClassLibrary` provenance reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3308` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained child classes that inherit externally already surface coherent `ClassLibrary` provenance across `GETPEM()` / `PEMSTATUS()` / ordinary reads / `AMEMBERS(..., 1|3)` and preserve the expected child runtime-object metadata across both same-PRG and external-form topologies. The shipped contained-child identity reflection, child identity mutation protections, contained-child `ACLASS()` reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3307` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already keep deeper inherited class chains coherent through `ACLASS()` across same-PRG and external-base child definitions, reusing the same stored native class-lineage path that already covered direct native objects and simpler child classes. The shipped contained-child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child identity mutation protections, contained-child `ACLASS()` reflection, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3306` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already reflect coherent inheritance chains through `ACLASS()` across same-PRG and external-base child definitions, reusing the same stored native class-hierarchy path that already covered direct native objects. The shipped contained-child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child identity mutation protections, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3305` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already reject ordinary direct assignment to native identity metadata across same-PRG and external-base child definitions where applicable, reusing the same shared dispatcher identity guard that already covered direct native objects and child `Parent`. The shipped child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child identity `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` protections, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3304` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already reject `REMOVEPROPERTY()` on native identity metadata across same-PRG and external-base child definitions where applicable, reusing the same shared native identity-name guard that already covered direct native objects. The shipped child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child identity `SETPEM()` and `ADDPROPERTY()` protections, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3303` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already reject `ADDPROPERTY()` shadow creation for native identity metadata across same-PRG and external-base child definitions where applicable, reusing the same shared native identity-name guard that already covered direct native objects. The shipped child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child identity `SETPEM()` protections, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3302` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already keep native identity metadata read-only to `SETPEM()` across same-PRG and external-base child definitions where applicable, reusing the same shared native identity protection behavior that already covered direct native objects. The shipped child identity reflection through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)`, ordinary child identity reads, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3301` shipped: contained native child objects now surface coherent native identity metadata through ordinary dotted reads across same-PRG and external-base child definitions, because the ordinary native member-read path now returns empty for non-applicable built-in identity members instead of falling through to generic placeholder dispatch. Focused runtime-surface and control-flow regressions prove child `Class`, `BaseClass`, and `ParentClass` align with the already-shipped `GETPEM()` / `PEMSTATUS()` / `AMEMBERS(..., 1|3)` identity surfaces while same-PRG and non-derived child `ClassLibrary` stays empty where appropriate, and existing child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3300` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already enumerate coherent native identity metadata through `AMEMBERS(..., 1|3)` across same-PRG and external-base child definitions where applicable, reusing the same shared identity-member enumeration path that already backed the shipped direct-object native metadata surface. The shipped contained-child identity reflection through `GETPEM()` / `PEMSTATUS()`, child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3299` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already surface coherent native identity metadata through `GETPEM()` / `PEMSTATUS()` across same-PRG and external-base child definitions where applicable, reusing the same stored native class identity fields that already backed the shipped direct-object reflection surface. The shipped child `Parent` reflection/enumeration/write protections, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3298` shipped: contained native child objects can no longer be shadowed through ordinary direct assignment to `Parent` across same-PRG and external-base child definitions, because the native dotted-assignment path now treats the stored child-parent object reference as protected native ownership metadata alongside the shipped runtime-surface helper guards. Focused runtime-surface and control-flow regressions prove ordinary `Parent` assignment attempts preserve coherent `GETPEM()` / `PEMSTATUS()` reflection, `AMEMBERS(..., 1|3)` enumeration, ordinary `Parent` reads, `PARENT` locals, child ownership state, COM/plain-object reflection, and the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3297` shipped: contained native child objects now keep their built-in `Parent` reference protected from `REMOVEPROPERTY(..., 'Parent')` across same-PRG and external-base child definitions, because the runtime-surface removal path now treats the stored child-parent object reference as protected native ownership metadata alongside the shipped `SETPEM()` and `ADDPROPERTY()` guards. Focused runtime-surface and control-flow regressions prove rejected `REMOVEPROPERTY()` attempts preserve coherent `GETPEM()` / `PEMSTATUS()` reflection, `AMEMBERS(..., 1|3)` enumeration, ordinary `Parent` reads, `PARENT` locals, child ownership state, COM/plain-object reflection, and the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3296` shipped: contained native child objects can no longer be shadowed through `ADDPROPERTY(..., 'Parent', ...)` across same-PRG and external-base child definitions, because the runtime-surface property-creation path now recognizes the stored child-parent object reference as protected native ownership metadata alongside the shipped `SETPEM()` guard. Focused runtime-surface and control-flow regressions prove rejected `ADDPROPERTY()` attempts preserve coherent `GETPEM()` / `PEMSTATUS()` reflection, `AMEMBERS(..., 1|3)` enumeration, ordinary `Parent` reads, `PARENT` locals, child ownership state, COM/plain-object reflection, and the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3295` shipped: contained native child objects now keep their built-in `Parent` reference read-only to `SETPEM()` across same-PRG and external-base child definitions, because the runtime-surface write path and `PEMSTATUS(..., 5)` now both recognize the stored child-parent object reference as protected native ownership metadata. Focused runtime-surface and control-flow regressions prove failed `SETPEM(..., 'Parent', ...)` attempts preserve coherent `GETPEM()` / `PEMSTATUS()` reflection, `AMEMBERS(..., 1|3)` enumeration, ordinary `Parent` reads, `PARENT` locals, child ownership state, COM/plain-object reflection, and the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3294` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already enumerate coherent `Parent` entries through `AMEMBERS(..., 1|3)` across same-PRG and external-base child definitions, because the stored child-parent object reference already lives in the native property bag alongside the shipped identity metadata overlay. Existing ordinary `Parent` reads, `PARENT` locals, shipped `GETPEM()` / `PEMSTATUS()` child-parent reflection, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3293` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove contained native child objects already surface coherent `Parent` references through `GETPEM()` / `PEMSTATUS()` across same-PRG and external-base child definitions, reusing the stored parent object reference already required for ordinary `Parent` reads and `PARENT` locals. Existing identity metadata behavior, inherited child behavior, COM/plain-object reflection, and the stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3292` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `ParentClass` already stays protected from ordinary direct-assignment shadowing on same-PRG and direct external-base objects, because the shared reserved native identity-member guard now covers that path too. The shipped `GETPEM()` / `PEMSTATUS()` reflection, ordinary identity reads, `AMEMBERS(..., 1|3)` enumeration, neighboring native identity protections, COM/plain-object behavior, and the stack-frugal frame machine all remain stable.
- 2026-07-03: Runtime parity slice `#3291` shipped: native `ParentClass` can no longer be shadowed through `ADDPROPERTY()` on same-PRG and direct external-base objects, because the reserved native identity-member guard now treats `ParentClass` the same way it already treated `Class`, `BaseClass`, and `ClassLibrary`. Focused runtime-surface and control-flow regressions prove rejected shadow creation leaves runtime identity intact and does not materialize conflicting stored properties, while the shipped reflection, ordinary reads, `AMEMBERS(..., 1|3)` enumeration, COM/plain-object behavior, and stack-frugal frame machine remain stable.
- 2026-07-03: Runtime parity slice `#3290` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `ParentClass` already stays effectively protected from `REMOVEPROPERTY()` on same-PRG and direct external-base objects, without erasing runtime identity or materializing a conflicting shadow property. The shipped `GETPEM()` / `PEMSTATUS()` reflection, ordinary identity reads, `AMEMBERS(..., 1|3)` enumeration, neighboring native identity protections, COM/plain-object behavior, and the stack-frugal frame machine all remain stable.
- 2026-07-03: Runtime parity slice `#3289` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `ParentClass` already stays effectively read-only to `SETPEM()` on same-PRG and direct external-base objects, without mutating runtime identity or materializing a writable shadow property. The shipped `GETPEM()` / `PEMSTATUS()` reflection, ordinary identity reads, `AMEMBERS(..., 1|3)` enumeration, neighboring native identity protections, COM/plain-object behavior, and the stack-frugal frame machine all remain stable.
- 2026-07-03: Runtime parity slice `#3288` shipped: native `ParentClass` now appears coherently in `AMEMBERS(..., 1|3)` for same-PRG and direct external-base PRG objects where applicable, so member enumeration no longer lags behind the shipped `GETPEM()` / `PEMSTATUS()` reflection and ordinary `o.ParentClass` reads. Focused runtime-surface and control-flow regressions prove the added identity entry preserves expected alphabetical enumeration alongside existing native metadata and accessor-backed property surfaces without disturbing COM/plain-object behavior or the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3287` shipped: same-PRG and direct external-base native PRG objects now surface `ParentClass` coherently through ordinary `o.Member` property reads where applicable, because the native ordinary-read identity helper now widens beyond `Class` / `BaseClass` / `ClassLibrary` to include the already-shipped `ParentClass` reflection metadata. Focused runtime-surface and control-flow regressions prove ordinary `o.ParentClass` reads now line up with `GETPEM()` / `PEMSTATUS()` without disturbing current `AMEMBERS()` shape, existing identity protections, COM/plain-object behavior, or the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3286` shipped: native `ParentClass` now reflects coherently through `GETPEM()` / `PEMSTATUS()` for same-PRG and direct external-base PRG objects, using the existing immediate-parent class provenance already preserved on native runtime objects while keeping this slice reflection-only. Focused runtime-surface regressions prove `ParentClass` is exposed as read-only metadata without disturbing the shipped `Class` / `BaseClass` / `ClassLibrary` ordinary reads and reflection, `AMEMBERS()` shape, identity write protections, COM/plain-object behavior, or the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3285` shipped: same-PRG and direct external-base native PRG objects now surface `Class` / `BaseClass` / `ClassLibrary` identity metadata coherently through ordinary `o.Member` property reads where applicable, because the native ordinary-read path now consults the shared identity-metadata surface before falling back to raw stored properties. Focused runtime-surface and control-flow regressions prove ordinary reads now line up with the previously-shipped `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` / `ACLASS()` metadata plus `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` / direct-assignment protections without disturbing inherited behavior, COM/plain-object reflection, or the stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3284` shipped: ordinary direct native member assignment can no longer shadow `Class` / `BaseClass` / `ClassLibrary` identity metadata, because the native assignment path now skips writes to those reserved identity member names after accessor dispatch and before raw-property fallback. Focused runtime-surface and control-flow regressions prove same-PRG and direct external-base native objects keep their identity values intact without materializing conflicting shadow properties, while existing `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` metadata, `SETPEM()` / `ADDPROPERTY()` / `REMOVEPROPERTY()` protections, `ACLASS()` chains, and COM/plain-object reflection remain stable.
- 2026-07-03: Runtime parity slice `#3283` shipped: native identity metadata names (`Class`, `BaseClass`, `ClassLibrary`) can no longer be shadowed through `ADDPROPERTY()` on PRG-defined objects, because the runtime-surface `ADDPROPERTY()` path now rejects those reserved native identity member names for native objects before writing into the ordinary property bag. Focused runtime-surface and control-flow regressions prove same-PRG and direct external-base native objects keep their identity values intact and do not materialize conflicting shadow properties, while existing `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` metadata, `SETPEM()` / `REMOVEPROPERTY()` protections, `ACLASS()` chains, and COM/plain-object reflection remain stable.
- 2026-07-03: Runtime parity slice `#3282` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `Class` / `BaseClass` / `ClassLibrary` identity metadata already stays effectively protected from `REMOVEPROPERTY()` on same-PRG and direct external-base native objects, without erasing runtime identity or materializing conflicting shadow properties. The existing direct metadata reads, reflected member enumeration, `SETPEM()` read-only behavior, `ACLASS()` chain, and COM/plain-object reflection all remain stable.
- 2026-07-03: Runtime parity slice `#3281` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove native `Class` / `BaseClass` / `ClassLibrary` identity metadata already stays effectively read-only to `SETPEM()` on same-PRG and direct external-base native objects, without materializing writable shadow properties or disturbing the previously-shipped direct metadata reads, reflected member enumeration, `ACLASS()` chain, or COM/plain-object reflection.
- 2026-07-03: Runtime parity slice `#3280` shipped: native `Class` / `BaseClass` / `ClassLibrary` identity metadata now appears coherently in `AMEMBERS(..., 1|3)` for PRG-defined objects where applicable, so native object reflection no longer splits those members across direct `GETPEM()` / `PEMSTATUS()` probes only. Focused runtime-surface and control-flow regressions now prove same-PRG and direct external-base native objects enumerate those metadata members alongside ordinary stored and accessor-backed properties without disturbing the previously-shipped direct metadata reads, `ACLASS()` chain, or COM/plain-object reflection.
- 2026-07-03: Runtime parity slice `#3279` shipped: native PRG-defined objects now surface direct `Class` identity through `GETPEM()` / `PEMSTATUS()` as read-only metadata, reusing the native object provenance already preserved by the activation path without widening COM/plain-object behavior. Focused runtime-surface and control-flow regressions now prove same-PRG and external-base native instances expose coherent direct class identity while keeping the previously-shipped `ACLASS()` chain plus `BaseClass` / `ClassLibrary` metadata stable.
- 2026-07-03: Runtime parity slice `#3278` shipped: native PRG-defined objects now surface immediate `BaseClass` identity through `GETPEM()` / `PEMSTATUS()`, and direct external-base native objects also surface resolved `ClassLibrary` provenance through that same reflection path as read-only metadata. Focused runtime-surface and control-flow regressions now prove same-PRG and external-base instances expose coherent native identity metadata without disturbing the previously-shipped `ACLASS()` chain, inherited method/property behavior, or COM/plain-object reflection.
- 2026-07-03: Runtime parity slice `#3277` shipped: native `ACLASS()` now returns the real inheritance chain for PRG-defined objects instead of the previous hard-coded `[class, OBJECT]` pair, including same-PRG and external-PRG-base class hierarchies through the existing stack-frugal native activation path while preserving COM/plain-object reflection behavior. Focused runtime-surface and control-flow regressions now prove native instances surface derived-to-base class identity coherently through `ACLASS()` and preserve that hierarchy in runtime object metadata without disturbing inherited methods, properties, or COM reflection.
- 2026-07-03: Runtime parity slice `#3276` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited methods loaded from external PRG base libraries already remain visible through `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()` on derived instances, while follow-on inherited and derived method invocation plus COM/plain-object reflection behavior stay stable under the existing stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3275` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove child methods inherited from external PRG base libraries already resolve `PARENT`, `THISFORM`, and `THISFORMSET` correctly, including follow-on parent method calls and parent state updates through those ownership locals.
- 2026-07-03: Runtime parity slice `#3274` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `SETPEM()` already routes correctly through inherited external PRG base assigners, preserves read-only behavior for inherited access-only properties, and keeps follow-on ordinary reads plus `GETPEM()` reflection in sync with assigner-updated backing state while leaving COM `SETPEM()` behavior unchanged.
- 2026-07-03: Runtime parity slice `#3273` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove inherited external PRG base `*_ACCESS` / `*_ASSIGN` methods already dispatch correctly through ordinary property reads/writes on derived classes, and `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection already surfaces those inherited accessor-backed properties coherently enough to preserve the full reflected member shape plus inherited accessor-method invoke events.
- 2026-07-03: Runtime parity slice `#3272` shipped as evidence closure: focused runtime-surface and control-flow regressions now prove `DODEFAULT()` already dispatches correctly across external PRG base methods, preserving `THIS`, by-reference `Init` flow, follow-on child logic, and base-invoke event emission when derived classes override methods or `Init` locally and base-call into parent implementations loaded from another `.prg` library.
- 2026-07-03: Runtime parity slice `#3271` shipped: bare helper-method calls made from inherited external PRG base methods now resolve against the defining method PRG instead of always the derived object's source PRG, so helper methods that live only in the external base library continue to beat same-named top-level routines without requiring explicit `THIS.` syntax. Focused runtime-surface and control-flow regressions prove derived classes can inherit external-base `Init` and ordinary methods that call helper methods from the defining library, by-reference flow and follow-on state updates remain stable, and existing same-PRG helper-method resolution plus COM behavior remain stable.
- 2026-07-03: Runtime parity slice `#3270` shipped: inherited native methods loaded from external PRG base classes now resolve same-PRG `THIS.AddObject('<name>', '<class>')` child classes against the defining method PRG instead of always the derived object's source PRG, while explicit third-argument `.prg` child libraries continue to win and keep their trailing constructor-argument behavior. Focused runtime-surface and control-flow regressions prove derived classes can inherit external-base `Init` methods that materialize child classes from the defining library without repeating `'<file>.prg'`, child `PARENT` methods and follow-on parent reads remain stable, and existing same-PRG `THIS.AddObject()` plus COM behavior remain stable.
- 2026-07-03: Runtime parity slice `#3269` shipped: inherited declarative child definitions now resolve same-PRG child classes and relative child-library paths against the defining PRG of each lineage class instead of always the derived object's PRG source. Focused runtime-surface and control-flow regressions prove derived classes can inherit declarative child definitions from external `.prg` base classes without repeating `OF <file>.prg`, inherited child property overrides and parent `Init` visibility remain stable, child `PARENT` methods still work, and existing same-PRG declarative children plus COM behavior remain stable.
- 2026-07-03: Runtime parity slice `#3268` shipped: native `THIS.AddObject('<name>', '<class>', '<file>.prg')` now treats a narrow third `.prg` argument as an external native class-library source instead of passing it into child `Init`, and routes that runtime child activation through the existing external PRG class-library path while preserving parent linkage and trailing constructor arguments. Focused runtime-surface and control-flow regressions prove external PRG child classes materialize before follow-on parent logic reads them, child `PARENT` methods still work, child `Init` still receives trailing constructor arguments, and existing same-PRG `THIS.AddObject()` plus COM behavior remain stable.
- 2026-07-03: Runtime parity slice `#3267` shipped: native `DEFINE CLASS <name> AS <base> OF <file>.prg` now preserves the external PRG base source distinctly from the base class identifier, and native class activation/method lookup can follow inheritance across loaded PRG files for defaults, methods, and inherited `Init` behavior. Focused parser, runtime-surface, and control-flow regressions prove derived classes can inherit parent defaults and methods from external `.prg` class libraries through both `CREATEOBJECT()` and bare `NEWOBJECT()`, while same-PRG inheritance, explicit external `NEWOBJECT('<class>', '<file>.prg')`, declarative child activation, and COM/plain-object behavior remain stable.
- 2026-07-03: Runtime parity slice `#3266` shipped: native `DEFINE CLASS` declarative child definitions now support narrow `OF <file>.prg` external class sources for both one-line `ADD OBJECT` forms and multi-line `OBJECT ... ENDOBJECT` blocks, and native class activation resolves those external PRG child libraries through the existing class-library path before parent `Init` runs. Focused parser, runtime-surface, and control-flow regressions prove the external source path stays distinct from child class names and child property clauses, both `CREATEOBJECT()` and bare `NEWOBJECT()` see those external children during parent `Init`, owner `PARENT` reads plus child property overrides still work, and COM/plain-object behavior remains stable.
- 2026-07-03: Backlog and agent-guidance cleanup shipped: previously untracked license-manifest schema/provenance, code-page runtime parity stub, and headless runtime surface stub gaps are now tracked under live GitHub parent/child issues (`#3242`-`#3254`), and `.github/agents/copperfin-vfp-runtime.agent.md` now follows the current live-issue guidance hierarchy instead of directing runtime agents to deprecated `remaining-work.md` queues or closed Phase A issue chains.
- 2026-07-03: Runtime parity slice `#3265` shipped: native `DEFINE CLASS` bodies now support narrow multi-line `OBJECT <name> AS <class>` ... `ENDOBJECT` child blocks, and native class activation applies those block-scoped child property assignments after child materialization and before parent `Init` runs. Focused parser, runtime-surface, and control-flow regressions prove the child blocks stay distinct from ordinary class property assignments and one-line declarative `ADD OBJECT` forms, both `CREATEOBJECT()` and bare `NEWOBJECT()` see those overridden child properties during parent `Init`, owner `PARENT` reads still work, and COM/plain-object behavior remains stable.
- 2026-07-03: Runtime parity slice `#3264` shipped: native `DEFINE CLASS` bodies now support narrow declarative `ADD OBJECT <name> AS <class> WITH <property>=<expr>[, ...]` child clauses, and native class activation applies those child property overrides after child materialization and before parent `Init` runs. Focused parser, runtime-surface, and control-flow regressions prove the `WITH` property clauses are captured distinctly from both ordinary class property assignments and bare `ADD OBJECT` declarations, both `CREATEOBJECT()` and bare `NEWOBJECT()` see the overridden child properties during parent `Init`, owner `PARENT` reads still work, and COM/plain-object behavior remains stable.
- 2026-07-03: Runtime parity slice `#3263` shipped: native `DEFINE CLASS` bodies now support narrow declarative `ADD OBJECT <name> AS <class>` child definitions, and native class activation materializes those contained child objects automatically before parent `Init` runs while preserving the existing stack-frugal runtime `THIS.AddObject()` path. Focused parser, runtime-surface, and control-flow regressions prove declarative child lines are captured distinctly from ordinary property assignments, both `CREATEOBJECT()` and bare `NEWOBJECT()` see those children during parent `Init`, owner `PARENT` reads still work, and COM/plain-object behavior remains stable.
- 2026-07-03: Package-contract slice `#3244` shipped: durable `app.cfmanifest` and `app.cfdebug` outputs no longer persist raw local `license_source_path` provenance while preserving the remaining display/audit-only `license_*` metadata and the schema version `2` contract introduced in `#3243`. Focused runtime-pipeline coverage now proves non-sensitive license metadata still survives package/debug materialization while local machine license paths stay out of the emitted artifacts.
- 2026-07-03: Package-contract slice `#3243` shipped: the public runtime package and debug manifest builders now explicitly version the license-extended `app.cfmanifest` / `app.cfdebug` contracts at schema version `2` instead of silently emitting `license_*` fields under the old version `1` headers. Focused runtime-pipeline materialization coverage proves both manifests preserve the current `license_*` fields while advertising the newer contract version, leaving `#3244` to remove the still-persisted local license path provenance as a separate follow-up.
- 2026-07-03: Runtime parity slice `#3262` shipped: native child-object method frames now also bind `THISFORMSET` through the same owner chain used for `THISFORM`, exposing the owning top-level native object as a stable local reference for exact `THISFORMSET`, `THISFORMSET.<property>`, and `THISFORMSET.<method>()` usage without introducing a separate formset runtime model. Focused runtime-surface, parser-class, and control-flow regressions prove child-method owning-formset reads, child-triggered parent method calls through `THISFORMSET`, preserved owner state updates, and runtime stability all remain under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3261` shipped: same-PRG native containment now supports narrow `THIS.RemoveObject('<name>')` detachment, removing the contained child reference from the parent, clearing the detached child object's persisted `PARENT` reference, and making `GETPEM()` / `PEMSTATUS()` on the removed member disappear consistently while leaving any separately held child object reference otherwise intact. Focused runtime-surface, parser-class, and control-flow regressions prove child detachment, removed-member disappearance, preserved detached-child state, and COM stability all remain under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3260` shipped: ordinary dotted native assignment now walks contained child-object references segment by segment before applying the final write, including `Parent` hops, so statements like `oForm.cmdSave.Caption = 'OK'` and `oForm.cmdSave.Parent.Caption = 'Done'` update the actual contained target object instead of flattening the full dotted suffix onto the root object. Focused runtime-surface, parser-class, and control-flow regressions prove direct dotted child writes, dotted parent-chain writes, stable follow-on reads/method calls, and COM stability all remain under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3259` shipped: ordinary dotted native reads and method calls now walk contained child-object references segment by segment, including `Parent` hops, so expressions like `oForm.cmdSave.Parent.Caption` and calls like `oForm.cmdSave.OwnerCaption()` resolve against the contained target object instead of treating the full dotted suffix as one flat leaf on the root object. Focused runtime-surface, parser-class, and control-flow regressions prove direct dotted child-property reads, dotted child-method calls, dotted parent-chain reads, preserved owning-form state updates, and COM stability all remain under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3258` shipped: native child-object method frames now resolve `THISFORM` through the current parent chain, exposing the owning top-level native object as a stable local reference for exact `THISFORM`, `THISFORM.<property>`, and `THISFORM.<method>()` usage while preserving the existing `PARENT` and `DODEFAULT()` bindings. Focused runtime-surface, parser-class, and control-flow regressions prove child-method owning-form reads, child-triggered parent method calls, preserved parent-form state updates, and runtime stability all remain under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3257` shipped: same-PRG native classes can now materialize child native objects through narrow `THIS.AddObject('<name>', '<class>')` activation, persist the created child reference on the parent, and expose the owning object as both a child `PARENT` property and a `parent` local in child method frames. Focused runtime-surface, parser-class, and control-flow regressions prove contained child activation, parent-property readback, child-method `PARENT.<member>` resolution, and existing COM dictionary behavior all remain stable under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3256` shipped: `NEWOBJECT('<class>', '<file>.prg')` now treats explicit `.prg` library targets as native PRG class sources instead of bare constructor arguments, resolves relative `.prg` targets through the current runtime default-directory rules, and reuses the existing iterative native-class activation path so external `DEFINE CLASS` libraries gain the same `Init`, inheritance, and by-reference constructor behavior as same-PRG activation without disturbing COM/server-targeted `NEWOBJECT()` flows. Focused runtime-surface, parser-class, and control-flow regressions prove external PRG class activation, constructor/by-reference `Init` flow, preserved resolved source provenance, and existing COM dictionary plus `app.vcx@server` metadata behavior all remain stable under the same stack-frugal frame machine.
- 2026-07-03: Runtime parity slice `#3241` shipped: `SETPem()` now routes same-PRG native accessor-backed property writes through matching `*_ASSIGN` methods before any raw-property fallback, so dynamic property updates observe the same effective behavior as ordinary `o.Property = value` syntax while preserving existing COM/plain-object `SETPem()` behavior. Focused runtime-surface, parser-class, and control-flow regressions prove assigner-backed writes update backing state visible through ordinary reads plus `GETPEM()`, access-only properties stay read-only to `SETPem()`, and existing COM dictionary behavior remains stable under the same stack-frugal iterative PRG frame machine.
- 2026-07-03: Runtime parity slice `#3240` shipped: same-PRG native accessor-backed properties now reflect coherently through `GETPEM()`, `PEMSTATUS()`, and `AMEMBERS()`, so runtime/tooling inspection can see virtual properties exposed through `*_ACCESS` / `*_ASSIGN` methods without changing stable machine-readable COM/plain-object behavior. Focused runtime-surface, parser-class, and control-flow regressions prove accessor-backed reads, readonly detection for access-only properties, property-name reflection, inherited native-class stability, and existing COM dictionary behavior all remain stable under the same stack-frugal iterative PRG frame machine.
- 2026-07-03: Runtime parity slice `#3239` shipped: same-PRG native classes can now virtualize ordinary member reads and writes through inherited or local `*_ACCESS` / `*_ASSIGN` methods while preserving raw-property fallback when no accessor exists. Focused runtime-surface, parser-class, and control-flow regressions prove accessor-backed `o.Property` reads/writes, `THIS.Property` reads inside methods, inherited accessor dispatch, raw-property fallback, and existing inheritance/`DODEFAULT()`/bare-helper/plain-object/COM activation all remain stable under the same stack-frugal iterative PRG frame machine.
- 2026-07-03: Runtime parity slice `#3238` shipped: bare same-PRG helper-method calls from inside native class methods now resolve against the current instance before unrelated top-level routines, so helper calls from `Init` and ordinary methods preserve `THIS`, inheritance, and by-reference behavior without requiring explicit `THIS.` syntax. Focused runtime-surface, parser-class, and control-flow regressions prove helper calls beat same-named top-level routines, helper calls from `Init` preserve by-reference write-back, and existing inheritance/`DODEFAULT()`/plain-object/COM activation all remain stable under the same stack-frugal iterative PRG frame machine.
- 2026-07-03: Runtime parity slice `#3226` shipped: same-PRG native overriding methods can now invoke inherited base implementations through expression-level `DODEFAULT()` dispatch that reuses the existing iterative PRG frame machine, including inherited `Init` flows and by-reference constructor/method arguments. Focused runtime-surface, parser-class, and control-flow regressions prove child/base method composition, child `Init` plus base `Init` chaining, by-reference write-back through `DODEFAULT(@arg)`, and existing inherited/plain-object/COM activation all remain stable without reintroducing recursive host-stack growth.
- 2026-07-03: Runtime parity slice `#3225` shipped: same-PRG native `DEFINE CLASS ... AS BaseClass` activation now applies inherited default properties, inherited methods, and inherited `Init` lookup across multilevel same-PRG class chains while letting child defaults and child method overrides win predictably. Focused runtime-surface, parser-class, and control-flow regressions prove inherited method dispatch, inherited `Init` execution, child-overrides-over-parent precedence, and existing plain-object/COM activation all remain stable under the same stack-frugal iterative PRG frame machine.
- 2026-07-03: Runtime parity slice `#3224` shipped: same-PRG native object member calls and automatic native `Init` execution now preserve `@` by-reference updates through the existing PRG call-binding model, and the expression parser now skips inter-argument whitespace before recognizing by-reference arguments so calls like `CREATEOBJECT('Widget', @nSeed)` no longer materialize a phantom empty argument ahead of the caller variable. Focused runtime-surface and control-flow regressions prove constructor-style `Init` write-back, ordinary native method by-reference updates, and existing COM object activation all remain stable while the iterative stack-frugal PRG frame machine stays intact.
- 2026-07-03: Runtime parity slice `#3223` shipped: bare same-PRG native `NEWOBJECT()` now forwards trailing constructor arguments into the automatic native `Init` call when the activation still looks like an in-program class construction rather than an explicit classlib/COM target, bringing constructor-parameter parity with `CREATEOBJECT()` while preserving library/server-targeted COM `NEWOBJECT()` activation and the iterative PRG runtime model under focused runtime-surface and control-flow regressions.
- 2026-07-03: Runtime parity slice `#3222` shipped: same-PRG native `CREATEOBJECT()` now forwards trailing constructor arguments into the automatic native `Init` call through the normal PRG call-argument binding path, so constructor-style class initialization can combine explicit creation arguments with pre-materialized `THIS` state while bare native `NEWOBJECT()`, plain `CREATEOBJECT('Empty')`, and COM object activation remain stable under focused runtime-surface and control-flow regressions.
- 2026-07-03: Runtime parity slice `#3221` shipped: same-PRG native objects created through `CREATEOBJECT()` and bare `NEWOBJECT()` now automatically run a discovered `Init` method immediately after default property materialization, preserving `THIS` binding and the iterative PRG frame machine so constructor-style class initialization can read/write instance state during creation while ordinary plain-object and COM object creation behavior remains stable under focused runtime-surface and control-flow regressions.
- 2026-07-03: Runtime parity slice `#3220` shipped: same-PRG native objects created through `CREATEOBJECT()` / bare `NEWOBJECT()` now dispatch simple member calls into parsed class methods on the iterative PRG frame machine, bind `THIS` as the current instance reference inside those method frames, and persist `THIS.<property>` reads/writes back to the instantiated runtime object while leaving ordinary OLE/member invocation behavior stable under the shared runtime-surface and control-flow regressions.
- 2026-07-03: Runtime parity slice `#3219` shipped: native PRG `CREATEOBJECT()` and bare `NEWOBJECT()` now consult same-PRG `DEFINE CLASS` metadata captured in `#3218`, materialize runtime objects with default class property assignments plus method-name reflection, and reuse the existing call-depth guardrail to bound recursive native-class instantiation so common VFP class creation starts working without reintroducing unbounded host-stack growth; ordinary non-class `CREATEOBJECT('Empty')` and OLE `NEWOBJECT('Scripting.Dictionary', 'vbscript.dll')` flows remain covered and unchanged.
- 2026-07-03: Runtime parity slice `#3218` shipped: the native PRG parser/model now recognizes `DEFINE CLASS` / `ENDDEFINE` blocks, preserves declared class names and base-class text, captures class-level default property statements plus contained `PROCEDURE` / `FUNCTION` bodies as class-owned methods, and keeps ordinary top-level routine parsing stable for mixed PRGs, establishing the next native VFP class-runtime foundation without changing the stack-frugal iterative PRG execution path.
- 2026-07-02: Runtime parity slice `#3216` shipped: expression-level user-defined `FUNCTION` and `PROCEDURE` calls now execute through the native PRG runtime instead of falling back to string literals, so common Fox/VFP patterns such as `x = MyFunc(...)`, `IF SomePredicate(...)`, nested user-routine return expressions, and `@` by-reference argument updates now work while still preserving Copperfin's stack-frugal heap-backed frame execution model instead of reintroducing recursive host-stack growth.
- 2026-07-02: Cross-cutting hardening shipped for `#3211`, `#3212`, `#3213`, and `#3215`: Windows builds now reserve a 16 MiB stack for `copperfin_studio_host` through the target linker configuration that matched the verified Windows workaround for the `--json` stack overflow, `copperfin_build_host` now accepts and documents the normalized `--license-status` flag while preserving legacy positional compatibility, localization catalog JSON parsing now decodes surrogate-pair Unicode escapes correctly and rejects malformed surrogate sequences instead of emitting broken UTF-8, label assets now participate explicitly in the asset-family companion-index switch instead of falling through the warning-only default path, and the core licensing/localization/build/runtime/security tests now share a Windows-safe environment helper instead of repeating ad hoc `getenv` access patterns in the touched suites.
- 2026-07-02: E3/#3209 shared asset-editor deleted duplicate preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` deleted duplicate command smokes now capture source live preview bounds before opening the editor, compare section continuity from the section model instead of brittle list text, and fail if deleted preview metadata drifts after delete, duplicate, or reload or if restored live preview plus remaining deleted preview metadata drift after restore, so the shared asset-editor deleted-row duplicate workflow can no longer accept preview-metadata regressions hidden behind superficially correct deleted/live identity alone.
- 2026-07-02: E3/#3208 shared asset-editor deleted rename preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` deleted rename command smokes now reuse captured source live preview bounds, compare section continuity from the section model instead of brittle list text, and fail if deleted preview metadata drifts after delete, rename, undo, or reload or if restored live preview metadata drifts after restore, so the shared asset-editor deleted-row rename workflow can no longer accept preview-metadata regressions hidden behind superficially correct deleted/live identity alone.
- 2026-07-02: E3/#3207 shared asset-editor live duplicate preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` live duplicate command smokes now capture the source live preview-bounds geometry before opening the editor, compare section continuity from the section model instead of brittle list text, and fail if `PREVIEWBOUNDS` drift after duplicate or on reload, so the shared asset-editor live-row duplicate workflow can no longer accept preview-metadata regressions hidden behind superficially correct duplicated identity and section counts alone.
- 2026-07-02: E3/#3206 shared asset-editor live rename preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` live rename command smokes now capture the source live preview-bounds geometry before opening the editor, compare section continuity from the section model instead of brittle list text, and fail if `PREVIEWBOUNDS` drift after rename or after undo/reload, so the shared asset-editor live-row rename workflow can no longer accept preview-metadata regressions hidden behind superficially correct object identity alone.
- 2026-07-02: E3/#3205 shared asset-editor live reorder preview-metadata fidelity shipped: the shared real `STYLE2V.FRX` live reorder command smoke now captures the source live preview-bounds geometry before opening the editor, asserts grouped-section continuity from the section model instead of brittle decorated list text, and fails if `PREVIEWBOUNDS` drift after reorder or on reload, so the shared asset-editor live-row reorder workflow can no longer accept preview-metadata regressions hidden behind superficially correct object ordering and identity alone.
- 2026-07-02: E3/#3204 shared asset-editor deleted reorder preview-metadata fidelity shipped: the shared real `bandedm.FRX` deleted reorder command smoke now captures the source live preview-bounds geometry before opening the editor, asserts the deleted host snapshot keeps live `PREVIEWBOUNDS` stable after delete and reorder operations, and fails if `DELETEDPREVIEWBOUNDS` drift out of sync with reordered deleted-object geometry after delete, reorder, or reload, so the shared asset-editor deleted-row workflow can no longer accept preview-metadata regressions hidden behind superficially correct design-surface ordering alone.
- 2026-07-02: E3/#3203 shared real host-backed settings preview-metadata fidelity shipped: the shared real host-backed settings and missing-settings smokes now support explicit preview-bounds assertions, and representative `by_author.FRX` plus `STYLELBL.LBX` settings-root cases now fail if `PREVIEWBOUNDS` drift across settings property update, settings insertion, reload, or undo, so settings-root coverage can no longer accept host-JSON preview regressions hidden behind correct settings metadata, selection continuity, and persisted property values alone.
- 2026-07-02: E3/#3202 shared real host-backed section preview-metadata fidelity shipped: the shared real host-backed section smoke now supports explicit preview-bounds assertions, and representative `by_author.FRX` plus `STYLELBL.LBX` section cases now fail if `PREVIEWBOUNDS` drift across section property update, reload, or undo for both geometry-changing and non-geometry edits, so section-edit coverage can no longer accept host-JSON preview regressions hidden behind correct section identity and persisted property values alone.
- 2026-07-02: E3/#3201 shared real host-backed live batch preview-metadata fidelity shipped: the shared real host-backed batch smoke now supports explicit preview-bounds assertions, and representative `by_author.FRX` and `STYLELBL.LBX` live-batch cases now fail if `PREVIEWBOUNDS` drift across batch update, reload, or undo for both geometry-changing and non-geometry edits, so live batch coverage can no longer accept host-JSON preview regressions hidden behind correct object identity and batch-updated property values alone.
- 2026-07-02: E3/#3200 shared real host-backed live property preview-metadata fidelity shipped: the shared real host-backed property smoke now supports explicit preview-bounds assertions, and representative `by_author.FRX`, `STYLELBL.LBX`, and `STYLE3V.FRX` live-property cases now fail if `PREVIEWBOUNDS` drift across update, reload, or undo for both geometry-changing and non-geometry edits, so live property coverage can no longer accept host-JSON preview regressions hidden behind correct object identity and property values alone.
- 2026-07-02: E3/#3199 shared real host-backed live placement preview-metadata fidelity shipped: the shared real `STYLE3V.FRX` host-backed live placement smoke now captures the source preview-bounds geometry and fails if `PREVIEWBOUNDS` drift while an unplaced object is moved into a live section, after reload, or after undo, so live placement coverage can no longer accept host-JSON preview regressions hidden behind correct VPOS, section continuity, and unplaced-object counts alone.
- 2026-07-02: E3/#3198 shared real host-backed live reorder preview-metadata fidelity shipped: the shared real `STYLE2V.FRX` host-backed live reorder smoke now captures the source preview-bounds geometry and fails if `PREVIEWBOUNDS` drift during reorder or reload, so live reorder coverage can no longer accept host-JSON preview regressions hidden behind correct UNIQUEID, section-count, and record-order behavior alone.
- 2026-07-02: E3/#3197 shared real host-backed live identity preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` host-backed live duplicate and live rename smokes now capture the source preview-bounds geometry and fail if `PREVIEWBOUNDS` drift during duplicate, rename, reload, or undo, so live identity coverage can no longer accept host-JSON preview regressions hidden behind correct UNIQUEID and section-count behavior alone.
- 2026-07-02: E3/#3196 shared real host-backed deleted reorder preview-metadata fidelity shipped: the shared real `bandedm.FRX` host-backed deleted reorder smoke now captures the source live preview-bounds geometry, asserts that live `PREVIEWBOUNDS` stay unchanged when only deleted rows are reordered, and fails if `DELETEDPREVIEWBOUNDS` drift out of sync with the reordered deleted layout-object geometry after reorder or reload, so deleted reorder coverage can no longer accept host-JSON preview drift hidden behind correct deleted-row identity and ordering alone.
- 2026-07-02: E3/#3195 shared real host-backed deleted-identity preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` host-backed deleted rename and deleted duplicate smokes now capture the source live preview-bounds geometry, assert that deleted preview bounds stay aligned with current deleted layout-object geometry through rename/duplicate/reload, and fail if restore leaves the live `PREVIEWBOUNDS` or remaining `DELETEDPREVIEWBOUNDS` inconsistent with the real host snapshot contract, so deleted identity coverage can no longer accept host-JSON preview drift hidden behind correct UNIQUEID/count behavior alone.
- 2026-07-02: E3/#3194 shared real host-backed deleted-property preview-metadata fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` host-backed deleted-property and deleted-batch smokes now capture the original live preview-bounds geometry, assert that deleted snapshots keep `DELETEDPREVIEWBOUNDS` aligned with the current deleted layout-object geometry through update/reload/undo, and fail if restore leaves stale deleted preview metadata or loses the original live `PREVIEWBOUNDS`, so deleted-object property coverage can no longer accept host-JSON preview drift that is inconsistent with either the deleted object state or the restored live asset state.
- 2026-07-02: E3/#3193 shared real host-backed delete-restore preview-bounds fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` host-backed delete/restore smokes now capture the source preview-bounds geometry, assert that both the immediate restore result and the reload from disk preserve live `PREVIEWBOUNDS`, and fail if stale `DELETEDPREVIEWBOUNDS` remain after restore, so host-JSON delete/restore coverage can no longer accept a superficially successful object restore while deleted preview metadata still leaks through the shared native snapshot contract.
- 2026-07-02: E3/#3192 shared real delete-restore preview-bounds fidelity shipped: the shared real `by_author.FRX` and `STYLELBL.LBX` delete/restore smokes now assert that a restored asset keeps live `PREVIEWBOUNDS`, clears stale `DELETEDPREVIEWBOUNDS`, and reselects the shared settings scope to verify the property grid surfaces only the live preview metadata after restore, so report/label delete-restore coverage can no longer accept a superficially successful object restore while stale deleted-preview state still leaks through the shared designer selection model.
- 2026-07-02: E3/#3191 shared real project debugger restart warning gating shipped: the shared real `addlabel.pjx` debugger UI smoke now clicks the embedded `Restart` command after the first session loads, asserts a fresh restarted debug session with preserved runtime/debug manifest paths plus zero inherited build warnings, and fails if restart drops the staged `wzcommon/registry.vcx` dependency or leaves stale command history in place, so the editor-shell debugger restart path can no longer hide packaging regressions behind a successful first pause state.
- 2026-07-02: E3/#3190 shared real project editor build-button warning gating shipped: the shared asset editor now exposes a narrow dialog-suppression hook for project-workflow smoke execution without changing normal product behavior, and the real `addlabel.pjx` project editor smoke now clicks `Build Copperfin Project`, asserts preserved output/manifest/debug-manifest/launcher paths plus zero inherited build warnings, and fails if the surfaced runtime manifest contains warning lines or loses the staged `wzcommon/registry.vcx` dependency, so the editor-shell build path can no longer hide packaging regressions behind a superficially successful completion dialog.
- 2026-07-02: E3/#3189 shared real project editor run-button warning gating shipped: the shared asset editor now retains the latest non-debug `CopperfinProjectExecutionResult`, and the real `addlabel.pjx` project editor smoke now clicks `Run Copperfin Project`, asserts preserved output/manifest/debug-manifest/launcher paths plus zero inherited build warnings, and fails if the surfaced runtime manifest contains warning lines or loses the staged `wzcommon/registry.vcx` dependency, so the editor-shell run path can no longer hide packaging regressions behind a superficially successful launcher start.
- 2026-07-02: E3/#3188 shared real project run warning gating shipped: the shared real `addlabel.pjx` run smoke now executes `CopperfinProjectWorkflow.ExecuteAsync(..., Run)`, asserts preserved output/manifest/debug-manifest/launcher paths plus zero inherited build warnings, and fails if the surfaced runtime manifest contains warning lines or loses the staged `wzcommon/registry.vcx` dependency, so the managed run path can no longer hide packaging regressions behind a superficially successful launcher start.
- 2026-07-02: E3/#3187 shared real project debugger UI warning gating shipped: the shared real `addlabel.pjx` debugger UI smoke now inspects the embedded editor control's active `CopperfinRuntimeDebugSession`, asserts a preserved runtime-manifest path plus zero inherited build warnings, and fails if the surfaced manifest contains warning lines or loses the staged `wzcommon/registry.vcx` dependency, so the UI-facing debugger path can no longer hide packaging regressions behind a successful pause summary alone.
- 2026-07-02: E3/#3186 shared real project debug replay warning gating shipped: `CopperfinRuntimeDebugSession` now preserves runtime-manifest path plus managed build warning count/details from `CopperfinRuntimeDebugClient.StartSessionAsync`, and the shared real `addlabel.pjx` replay smoke now asserts a warning-free inherited build, zero manifest warning lines, and the staged `wzcommon/registry.vcx` dependency so replay coverage can no longer hide packaging regressions behind a successful pause state.
- 2026-07-02: E3/#3185 shared real project build smoke warning gating shipped: `CopperfinProjectWorkflow` now exposes runtime-manifest path plus structured warning count/details from build-host output, and the shared real `addlabel.pjx` build smoke now asserts a materialized manifest, zero workflow warnings, zero manifest warning lines, and the staged `wzcommon/registry.vcx` dependency so future missing-asset regressions fail the managed workflow coverage instead of hiding behind a superficially successful build.
- 2026-07-02: E3/#3184 real addlabel shared-class lookup fidelity shipped: the runtime package pipeline now falls back on a case-insensitive tail-match search under the extracted project source root when a VFP parent-relative asset path misses after normal normalization, which resolves the real `addlabel.pjx` `..\wzcommon\registry.vcx` dependency from the VFPSource sample layout, stages it safely as `content/wzcommon/registry.vcx`, and removes the last build warning for that sample without changing project-workspace metadata or machine-readable manifest path contracts.
- 2026-07-02: E3/#3183 safe VFP relative-path runtime packaging shipped: the runtime package pipeline now interprets VFP-style backslash-separated project asset paths when resolving source files, sanitizes package-relative asset paths so parent traversal cannot escape `content/`, and adds focused staging coverage for `..\wzcommon\registry.vcx`-style entries while preserving existing project-workspace metadata and host JSON contracts.
- 2026-07-02: E3/#3182 shared real project debugger smoke activation shipped: the stale non-Windows guard is gone from the shared WinForms real-project debugger smoke, and focused UI-facing probe coverage now proves the actual asset-editor debug surface can open a real `addlabel.pjx` project on Linux, invoke the shared managed replay flow, and render a debug summary with pause reason, call stack, and runtime events without changing machine-readable build/debug contracts.
- 2026-07-02: E3/#3181 security-enabled shared project debug replay shipped: the shared managed runtime debug client now rewrites only its replay manifest copy when a security-enabled project build inherited the default `build-engineer` role, replays through the runtime-capable `runtime-operator` role without changing packaged manifest contracts, and treats runtime-host debug pause states as successful sessions even when the host exits nonzero for an actual paused-on-error runtime condition, while focused shared smoke coverage now proves real `addlabel.pjx` replay can surface a pause reason, call stack, and runtime events on Linux.
- 2026-07-02: E3/#3180 security-enabled non-Windows project build workflow shipped: the shared managed project workflow now defaults security-enabled build-host invocations to the native `build-engineer` role when no explicit `COPPERFIN_SECURITY_ROLE` is configured and stops over-requiring `--emit-dotnet-launcher` off Windows, while a focused shared smoke now proves a real `tasklist.PJX` build can materialize output, launcher, and debug-manifest artifacts on Linux without changing Windows launcher-emission behavior or machine-readable build/debug contracts.
- 2026-07-02: E3/#3179 zip-backed VFP9 sample discovery shipped: the shared designer smoke real-asset helpers now accept `COPPERFIN_VFP9_ZIP` plus common local sample-zip names, extract a VFP9 sample archive into the same temp-root scheme used for `VFPSource.zip`, and resolve `solution.pjx`, `invoice.frx`, `cust.lbx`, and related sample assets through the existing VFP9 helper path when a full local install root is unavailable, while focused synthetic-zip coverage also proves current `VFPSource` discovery still resolves unchanged.
- 2026-07-02: E3/#3178 VFPSource real layout-action seam closure shipped: focused net472/Mono probe coverage now reruns the shared real-asset align-left/top, resize-to-anchor width/height, distribute-horizontal/vertical, and snap-vertical/horizontal/to-grid editor smokes directly against `DATAEXPLORERQUICKREPORT.FRX`, `STYLE3V.FRX`, and `STYLELBL.LBX` under the same resolved Studio/build/runtime host toolchain used by the main suite, proving those shared report/label layout-action round-trips already preserve live selection continuity, on-disk write-back, and undo without changing invariant host contracts or localized machine-readable fields.
- 2026-07-01: E3/#3177 report layout-action Windows smoke activation shipped: the shared designer smoke family for report align-left/top, match-size/width/height, distribute-horizontal/vertical, and snap-vertical/horizontal/to-grid commands no longer self-skip on Windows solely due to stale POSIX guards, and focused net472/Mono probe coverage now proves those smokes still pass on Linux while the Windows override path remains `.cmd` batch-wrapper ready without changing invariant host command contracts.
- 2026-07-01: E3/#3176 label layout-action Windows smoke activation shipped: the shared designer smoke family for label align-left/top, match-size/width/height, distribute-horizontal/vertical, and snap-vertical/horizontal/to-grid commands no longer self-skip on Windows solely due to stale POSIX guards, and focused net472/Mono probe coverage now proves those smokes still pass on Linux while the Windows override path remains `.cmd` batch-wrapper ready without changing invariant host command contracts.
- 2026-07-01: E3/#3175 label-object Windows smoke activation shipped: the shared designer smoke family for live label-object host updates, placement transitions into the unplaced tray, deleted-object summary refresh, live/deleted undo refresh, and duplicate/rename/delete/restore/reorder-back object commands no longer self-skip on Windows solely due to stale POSIX guards, and focused net472/Mono probe coverage now proves those smokes still pass on Linux while the Windows override path remains `.cmd` batch-wrapper ready without changing invariant host command contracts.
- 2026-07-01: E3/#3174 report-object Windows smoke activation shipped: the shared designer smoke family for report-side unplaced placement transitions, deleted-object summary refresh, live/deleted undo refresh, and duplicate/rename/delete/restore/reorder-front object commands no longer self-skips on Windows solely due to stale POSIX guards, and focused net472/Mono probe coverage now proves those smokes still pass on Linux while the Windows override path remains `.cmd` batch-wrapper ready without changing invariant host command contracts.
- 2026-07-01: E3/#3173 deleted report-settings missing-property Windows smoke activation shipped: the shared designer smoke family for deleted report-settings missing-property host updates now reuses the cross-platform fake Studio host wrapper instead of hard-coded POSIX `.sh` paths and stale Windows skips, and focused net472/Mono probe coverage now proves the deleted ORIENTATION/PAPERSIZE/GRID/TAG/printer/column-setup materialization smokes still pass on Linux while the Windows override path emits the expected `.cmd` batch-wrapper fake host without changing invariant `--set-property` contracts.
- 2026-07-01: #2348/#3172 language-service IntelliSense metadata localization shipped: `FoxProIntelliSenseCatalog` now routes built-in Quick Info, completion metadata, signature-help documentation, and source-derived project symbol prose through the shared localization catalogs instead of hard-coded English strings, caches locale-specific project metadata without changing token names or signature content, and `Copperfin.LanguageServiceTests` now proves a Spanish language-service path localizes alias and `MESSAGEBOX` help text while preserving invariant completion kinds and signature content.
- 2026-07-01: #2348/#3171 VSIX project/debug insight summary localization shipped: the shared asset editor now routes the remaining user-facing inline line formats in the workspace, debugger, task-list, code-reference, data-explorer, object-browser, toolbox, builders, coverage, and database summary panes through the existing localization catalogs instead of hard-coded English string assembly, while preserving invariant machine-readable contracts, asset-family identifiers, and other non-localized tokens.
- 2026-07-01: E3/#3170 fresh-run VFPSource visual startup-path smoke coverage shipped: the fresh-run `VFPSource.zip` startup-path smoke now directly opens representative `books.scx`, `envmgr.vcx`, and `handler_context.mnx` assets from the newly materialized extraction root through the shared asset editor, verifies the adjacent `SCT`, `VCT`, and `MNT` sidecars remain present, and proves form/class-library/menu startup flows surface family-specific guidance plus visible design content without changing machine-readable contracts or localized UI text.
- 2026-07-01: E3/#3169 fresh-run VFPSource label startup-path smoke coverage shipped: the fresh-run `VFPSource.zip` startup-path smoke now also resolves `STYLELBL.LBX` from the newly materialized extraction root, verifies the adjacent `STYLELBL.LBT` sidecar is preserved, and proves the shared asset editor can open that real label asset directly from the fresh extracted root without changing machine-readable contracts or localized UI text.
- 2026-07-01: E3/#3168 fresh-run VFPSource startup-path smoke coverage shipped: shared designer smoke coverage now forces a fresh `VFPSource.zip` extraction-root materialization pass before exercising real project/program/form/report startup paths, proving zip-backed `tasklist.PJX`, `main.prg`, `books.scx`, and `by_author.FRX` open through the shared project editor, program editor, and standalone Studio startup flows without changing machine-readable contracts or localized UI text.
- 2026-07-01: E3/#3167 fresh-run VFPSource zip real-asset root discovery shipped: shared designer real-asset discovery now materializes the `VFPSource.zip` extraction root on demand during root enumeration instead of only after earlier asset-specific extraction side effects, so broader FRX/LBX startup-path and asset-discovery flows can see zip-backed VFPSource reports and labels on a fresh run without changing machine-readable contracts or localized UI text.
- 2026-07-01: E3/#3166 shared Windows fake Studio host batch-update smoke activation shipped: the shared asset-editor batch-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant report-object batch host updates preserve section/object selection continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3165 shared Windows fake Studio host label drag summary-refresh smoke activation shipped: the shared label asset-editor drag summary-refresh smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant label-object batch host updates refresh shell preview-bounds text while preserving label identity, object selection, and containing-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3164 shared Windows fake Studio host report drag summary-refresh smoke activation shipped: the shared asset-editor report drag summary-refresh smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant report-object batch host updates refresh shell preview-bounds text while preserving report identity, object selection, and containing-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3163 shared Windows fake Studio host unplaced-report-object summary-refresh smoke activation shipped: the shared asset-editor unplaced-report-object summary-refresh smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant unplaced report-object `HPOS` updates on record `9` refresh shell preview-bounds text while preserving object-rooted selection and unplaced-scope continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3162 shared Windows fake Studio host unplaced-label-object summary-refresh smoke activation shipped: the shared asset-editor unplaced-label-object summary-refresh smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant unplaced label-object `HPOS` updates on record `9` refresh shell preview-bounds text while preserving label identity, object-rooted selection, and unplaced-scope continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3161 shared Windows fake Studio host unplaced-label-object smoke activation shipped: the shared asset-editor unplaced-label-object host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant unplaced label-object `HPOS` updates on record `9` preserve label identity, object-rooted selection, and unplaced-scope continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3160 shared Windows fake Studio host unplaced-report-object smoke activation shipped: the shared asset-editor unplaced-report-object host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant unplaced report-object `HPOS` updates on record `9` preserve object-rooted selection plus unplaced-scope continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3159 shared Windows fake Studio host deleted-label-object smoke activation shipped: the shared asset-editor deleted-label-object host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant deleted label-object `HPOS` updates on record `13` preserve deleted-object selection plus deleted-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3158 shared Windows fake Studio host deleted-report-object smoke activation shipped: the shared asset-editor deleted-report-object host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant deleted report-object `HPOS` updates on record `13` preserve deleted-object selection plus deleted-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3157 shared Windows fake Studio host report-object smoke activation shipped: the shared asset-editor report-object host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant report-object `HPOS` updates on record `6` preserve object-rooted selection plus containing-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3156 shared Windows fake Studio host deleted-label-section smoke activation shipped: the shared asset-editor deleted-label-section host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves deleted label section `TOP` UI edits serialize back through invariant host `VPOS` on record `51` while preserving deleted-section continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3155 shared Windows fake Studio host label-section smoke activation shipped: the shared asset-editor label-section host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves label section `TOP` UI edits serialize back through invariant host `VPOS` on record `42` while preserving section-rooted continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3154 shared Windows fake Studio host deleted-report-section smoke activation shipped: the shared asset-editor deleted-report-section host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves deleted section `TOP` UI edits serialize back through invariant host `VPOS` on record `51` while preserving deleted-section refresh continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3153 shared Windows fake Studio host deleted-settings smoke activation shipped: the shared asset-editor deleted-report-settings host-update smoke no longer self-skips on Windows solely because the fake Studio host used to be POSIX-only, now reuses the shared platform-specific `.sh`/`.cmd` fake-host helper, and still proves invariant deleted-settings host arguments (`COLSPACING` on record `0`) plus deleted settings-rooted refresh continuity without changing host JSON keys, localized UI text, or other machine-readable contracts.
- 2026-07-01: E3/#3152 shared Windows fake Studio host report-smoke activation shipped: the shared designer fake Studio host helper now emits platform-specific `.sh` or `.cmd` wrappers, and the shared asset-editor report-section plus report-settings host-update smokes no longer self-skip on Windows solely because the fake host was POSIX-only; focused validation still proves the Linux fake-host path passes, the section `TOP` UI field still serializes back to invariant host `VPOS`, the settings path still emits invariant `COLWIDTH`, and Windows batch wrapper generation keeps machine-readable host arguments, JSON contracts, and localized UI text unchanged.
- 2026-07-01: E3/#3151 shared Windows fake Studio host launcher support shipped: `CopperfinStudioHostBridge` now centralizes Studio host `ProcessStartInfo` creation, preserves invariant host arguments for direct executables, and wraps Windows `.cmd`/`.bat` `COPPERFIN_STUDIO_HOST_PATH` values through `COMSPEC` so shared Studio snapshot and launch paths can use script-backed fake hosts on Windows without changing machine-readable Studio host arguments, JSON contracts, or localized UI text.
- 2026-07-01: E3/#3150 shared real missing column-spacing settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `COLSPACING` settings can be added through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, EXPR-backed provenance at `record 0`, `field 6`, `sourceLineIndex 3`, memo blocks `142/17`, and undo continuity back to the original no-column-setup state without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3149 shared real missing column-width settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `COLWIDTH` settings can be added through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, EXPR-backed provenance at `record 0`, `field 6`, `sourceLineIndex 3`, memo blocks `142/17`, and undo continuity back to the original no-column-setup state without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3148 shared real missing column-count settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `COLS` settings can be added through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, EXPR-backed provenance at `record 0`, `field 6`, `sourceLineIndex 3`, memo blocks `142/17`, and undo continuity back to the original no-column-setup state without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3147 shared real color settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `COLOR` settings round-trip through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, memo-backed provenance transitions (`8 -> 142 -> 143` report, `8 -> 17 -> 18` label), and undo continuity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3146 shared real paper-size settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `PAPERSIZE` settings round-trip through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, memo-backed provenance transitions (`8 -> 142 -> 143` report, `8 -> 17 -> 18` label), and undo continuity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3145 shared real grid-horizontal settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root `GRIDH` settings round-trip through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, raw snapshot fidelity, on-disk FRX/LBX updates, and undo continuity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3144 shared real missing printer-device settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root settings can add missing memo-backed `DEVICE` through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3143 shared real missing output-path settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root settings can add missing memo-backed `OUTPUT` through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3142 shared real missing TrueType-option settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root settings can add missing memo-backed `TTOPTION` through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3141 shared real missing Y-resolution settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root settings can add missing memo-backed `YRESOLUTION` through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3140 shared real missing print-quality settings round-trip coverage shipped: the shared designer real-asset settings smokes now prove fallback report and label root settings can add missing memo-backed `PRINTQUALITY` through both the host-backed and shared asset-editor paths, preserving selected-settings metadata, on-disk FRX/LBX updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3139 shared real report page-header height-contained-object stability evidence shipped: the shared designer real-asset section smoke now proves live `STYLE3V.FRX` `Page Header` `HEIGHT` edits preserve all three contained page-header objects' absolute and section-relative geometry across edit and undo while still updating the section height on disk, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3138 shared real report page-header contained-object top reflow evidence shipped: the shared designer real-asset section smoke now proves live `STYLE3V.FRX` `Page Header` `VPOS` edits move all three contained page-header objects by the section delta while preserving section-relative geometry and undo fidelity, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3137 shared real report page-header section-top round-trip fix shipped: report/label section property-grid selections now keep `TOP` as the shared UI field while serializing section-top edits back to native FRX/LBX `VPOS`, and the shared designer real-asset section smoke now covers live `STYLE3V.FRX` `Page Header` `VPOS` edits, proving populated report page-header sections preserve section-rooted continuity, on-disk FRX top updates, and undo fidelity without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3136 shared real report page-header section-height round-trip evidence shipped: the shared designer real-asset section smoke now covers live `STYLE3V.FRX` `Page Header` `HEIGHT` edits, proving populated report page-header sections preserve section-rooted continuity, on-disk FRX height updates, undo fidelity, section counts, and in-section object membership without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3135 shared real report page-footer section-height round-trip evidence shipped: the shared designer real-asset section smoke now covers live `STYLE3V.FRX` `Page Footer` `HEIGHT` edits, proving blank report page-footer sections preserve section-rooted continuity, on-disk FRX height updates, undo fidelity, section counts, and zero-object membership without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3134 shared real label column-header section-height round-trip evidence shipped: the shared designer real-asset section smoke now covers live `STYLELBL.LBX` `Column Header` `HEIGHT` edits, proving blank label header sections preserve section-rooted continuity, on-disk LBX height updates, undo fidelity, section counts, and zero-object membership without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3133 shared real label column-footer section-height round-trip evidence shipped: the shared designer real-asset section smoke now covers live `STYLELBL.LBX` `Column Footer` `HEIGHT` edits, proving blank label footer sections preserve section-rooted continuity, on-disk LBX height updates, undo fidelity, section counts, and zero-object membership without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3132 shared real label page-footer section-height round-trip evidence shipped: the shared designer real-asset section smoke now covers live `STYLELBL.LBX` `Page Footer` `HEIGHT` edits, proving blank label footer sections preserve section-rooted continuity, on-disk LBX height updates, undo fidelity, section counts, and zero-object membership without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3131 shared deleted label page-footer metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `STYLELBL.LBX` `Page Footer` selection, proving the property grid preserves deleted label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3130 shared real label page-footer metadata selection evidence shipped: the shared designer real-asset smoke now covers live `STYLELBL.LBX` `Page Footer` section selection, proving the property grid surfaces stable label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3129 shared deleted label column-footer metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `STYLELBL.LBX` `Column Footer` selection, proving the property grid preserves deleted label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3128 shared real label column-footer metadata selection evidence shipped: the shared designer real-asset smoke now covers live `STYLELBL.LBX` `Column Footer` section selection, proving the property grid surfaces stable label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3127 shared deleted label column-header metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `STYLELBL.LBX` `Column Header` selection, proving the property grid preserves deleted label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3126 shared real label column-header metadata selection evidence shipped: the shared designer real-asset smoke now covers live `STYLELBL.LBX` `Column Header` section selection, proving the property grid surfaces stable label band state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3125 shared deleted real page-footer report section metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `STYLE3V.FRX` `Page Footer` selection, proving the property grid preserves deleted page-footer section state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3124 shared real page-footer report section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `STYLE3V.FRX` `Page Footer` section selection, proving the property grid surfaces stable page-footer section state/band/geometry metadata while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3123 shared deleted nested foxref group-footer metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `foxrefresultsa4.frx` nested `Group Footer - RefID` selection, proving the property grid preserves nested footer grouping depth/index metadata, grouped-expression provenance, and live grouping-partner metadata with zero visible section objects while omitting fabricated direct `EXPRESSIONFIELD` and `EXPRESSIONMEMO` metadata, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3122 shared deleted outer foxref group-footer metadata selection evidence shipped: the shared designer real-asset deleted-section smoke now covers deleted `foxrefresultsa4.frx` outer `Group Footer - SetID` selection, proving the property grid preserves top-level footer grouping depth/index metadata, grouped-expression provenance, and live grouping-partner metadata with zero visible section objects while omitting fabricated direct `EXPRESSIONFIELD` and `EXPRESSIONMEMO` metadata, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3121 shared outer real group-footer metadata selection evidence shipped: the shared designer real-asset smoke now covers live `foxrefresultsa4.frx` outer `Group Footer - SetID` section selection, proving the property grid surfaces top-level footer grouping depth/index metadata, retained grouped-expression provenance, live grouping-partner metadata, and invariant zero-object geometry without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3120 shared nested real group-footer metadata selection evidence shipped: the shared designer real-asset smoke now covers live `foxrefresultsa4.frx` nested `Group Footer - RefID` section selection, proving the property grid surfaces stable nested footer grouping depth/index metadata, retained grouped-expression provenance, live grouping-partner metadata, and invariant zero-object geometry without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3119 shared deleted nested foxref group-header metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `foxrefresultsa4.frx` nested `Group Header - RefID` section selection, proving the property grid preserves nested grouping depth/index metadata, direct and grouped expression provenance, and live grouping-partner metadata with zero visible section objects, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3118 shared deleted populated foxref group-header metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `foxrefresultsa4.frx` outer `Group Header - SetID` section selection, proving the property grid preserves direct and grouped expression provenance plus live grouping-partner metadata while shrinking visible section membership to the two undeleted child objects, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3117 shared nested real group-header metadata selection evidence shipped: the shared designer real-asset smoke now covers live `foxrefresultsa4.frx` nested `Group Header - RefID` section selection, proving the property grid surfaces stable nested grouping depth/index metadata, direct and grouped expression provenance, live grouping-partner metadata, and invariant geometry fields without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3116 shared deleted populated real-asset title report section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `STYLE1H.FRX` title-section selection, proving the property grid surfaces stable deleted section metadata for a populated `Title` band, clears visible section object membership and `OBJECTCOUNT` after deletion, and omits fabricated direct-expression and grouping-only properties without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3115 shared populated real-asset group-header report section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `bandedm.FRX` group-header section selection, proving the property grid surfaces stable live section metadata for a populated `Group Header` band, preserves the expected eight-object membership, and retains direct plus grouped expression provenance and live partner metadata without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3114 shared deleted populated real-asset group-header report section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `bandedm.FRX` group-header section selection, proving the property grid surfaces stable deleted section metadata for a populated `Group Header` band, clears visible section object membership and `OBJECTCOUNT` after deletion, and preserves direct plus grouped expression provenance and live partner metadata without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3113 shared deleted populated real-asset detail report section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `DATAEXPLORERQUICKREPORT.FRX` detail-section selection, proving the property grid surfaces stable deleted section metadata for a populated `Detail` band while preserving the existing twelve-object section membership and omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3112 shared populated real-asset detail report section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `DATAEXPLORERQUICKREPORT.FRX` detail-section selection, proving the property grid surfaces stable live section metadata for a populated `Detail` band with the expected twelve-object membership and geometry while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3111 shared deleted real-asset page-header report section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `STYLE3V.FRX` page-header section selection, proving the property grid surfaces stable deleted section metadata for a formerly populated `Page Header` band, clears section object-list membership and `OBJECTCOUNT` after deletion, and omits fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3110 shared real-asset page-header report section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `STYLE3V.FRX` page-header section selection, proving the property grid surfaces stable live section metadata for a populated `Page Header` band with the expected object count and geometry while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: Durable UI-direction guidance was promoted into `docs/17-modern-designer-direction.md`: Copperfin host shells are now explicitly required to follow Microsoft Windows, Apple macOS, and Linux/VS Code-class platform conventions, the standalone IDE is expected to own dockable `Command` and `Terminal` panes, the VSIX `Command` surface is expected to live as a bottom-region tabbed tool window by default, and future standalone extensibility remains a curated host-owned compatibility problem rather than general VSIX parity.
- 2026-07-01: E3/#3109 shared real-asset report detail section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `by_author.FRX` detail-section selection, proving the property grid surfaces stable live section metadata for the plain detail band while omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3108 shared real-asset report title section metadata selection evidence shipped: the shared designer real-asset smoke now covers live `by_author.FRX` title-section selection, proving the property grid surfaces stable live section metadata while preserving the expected populated object list for the contained title object and omitting fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3107 shared deleted real-asset report title section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `by_author.FRX` title-section selection, proving the property grid surfaces stable deleted section metadata, clears contained-object list membership for the deleted title band, and omits fabricated direct-expression and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3106 shared deleted real-asset label detail section metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `STYLELBL.LBX` detail-section selection, proving the property grid surfaces stable deleted section metadata while omitting fabricated direct-expression and grouping-only properties, and the shared deleted-section smoke helper now allows the expected nonzero object-list count for deleted sections that still contain visible objects, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3105 shared real-asset label detail section metadata selection evidence shipped: the shared designer real-asset smoke now covers `STYLELBL.LBX` detail-section selection, proving the property grid surfaces stable section state/band/geometry/object-count metadata while omitting fabricated direct-expression, provenance, and grouping-only properties, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3104 shared deleted real-asset group-header metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `by_author.FRX` group-header selection, proving the property grid preserves direct `EXPR` metadata, grouped `GROUPINGEXPRESSION` provenance, and live partner-state metadata after section deletion without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3103 shared deleted real-asset group-footer metadata selection evidence shipped: the shared designer real-asset smoke now covers deleted `by_author.FRX` group-footer selection, proving the property grid keeps blank direct `EXPR` editing separate from grouped provenance after section deletion by preserving `GROUPINGEXPRESSION`/field/memo metadata and live partner state while omitting fabricated direct `EXPRESSIONFIELD`/`EXPRESSIONMEMO` metadata, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3102 shared real-asset group-footer metadata selection evidence shipped: the shared designer real-asset section-metadata smoke now covers `by_author.FRX` group-footer selection, proving the property grid keeps blank direct `EXPR` editable while exposing grouping-expression provenance from the paired header and omitting fabricated direct `EXPRESSIONFIELD`/`EXPRESSIONMEMO` metadata, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3101 stable-selected group header/footer section evidence shipped: focused native report/label stable `UNIQUEID` section-selection regressions now prove live and deleted grouped FRX/LBX group-header and group-footer selections preserve the same section, preview-bounds, and sibling metadata already covered by record selection, without changing host JSON keys or other machine-readable contracts.
- 2026-07-01: E3/#3100 stable-selected nested grouping partner-metadata evidence shipped: focused native report/label stable `UNIQUEID` section-selection regressions now prove live and deleted nested grouped FRX/LBX selections preserve grouping context, grouping-expression provenance, partner-section linkage, and deleted-partner state across the shared host JSON contract without changing machine-readable keys or selectors.
- 2026-07-01: E3/#3099 TAG sort-setting unsupported `EXPR` memo preservation evidence shipped: focused native report/label stable-selection and record-selection `TAG` sort-setting regressions now prove live and deleted root-row `TAG` update/clear flows preserve raw unsupported `EXPR` comment and blank lines while parsed `settings`, `deletedSettings`, and `selectedReportSettings` continue to omit comment rows and keep the original `sourceLineIndex` gaps around supported memo-backed settings, without changing host JSON keys or other machine-readable contracts.
- 2026-07-01: E3/#3098 stable settings `EXPR` memo replacement unsupported-line parsing evidence shipped: focused native report/label stable settings memo regressions now prove direct `EXPR` replacement preserves raw unsupported comment and blank lines while parsed `settings`, `deletedSettings`, and `selectedReportSettings` omit comment rows and keep the original `sourceLineIndex` gaps around them, and `EXPR` clear continues to remove the memo-derived settings cleanly while preserving the direct-field carrier and machine-readable contracts.
- 2026-07-01: E3/#3097 record-selected settings `EXPR` memo replacement unsupported-line parsing evidence shipped: focused native report/label record-selected settings memo regressions now prove direct `EXPR` replacement preserves raw unsupported comment and blank lines while parsed `settings`, `deletedSettings`, and `selectedReportSettings` omit comment rows and keep the original `sourceLineIndex` gaps around them, and `EXPR` clear continues to remove the memo-derived settings cleanly while preserving the direct-field carrier and machine-readable contracts.
- 2026-07-01: E3/#3096 record-selected FRX/LBX settings delete/restore unsupported `EXPR` preservation evidence shipped: focused native report/label record-selected settings lifecycle regressions now prove live-to-deleted and deleted-to-live settings row transitions preserve raw unsupported `EXPR` comment and blank lines while keeping parsed `settings`, `deletedSettings`, and `selectedReportSettings` provenance aligned to the surviving key/value rows and their original `sourceLineIndex` gaps, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3095 stable FRX/LBX settings delete/restore unsupported `EXPR` preservation evidence shipped: focused native report/label stable-settings lifecycle regressions now prove live-to-deleted and deleted-to-live `UNIQUEID`-selected settings row transitions preserve raw unsupported `EXPR` comment and blank lines while keeping parsed `settings`, `deletedSettings`, and `selectedReportSettings` provenance aligned to the surviving key/value rows and their original `sourceLineIndex` gaps, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3094 stable FRX/LBX unsupported `EXPR`-line preservation evidence shipped: focused native report/label stable-settings regressions now prove live and deleted `UNIQUEID`-selected `DRIVER` add/remove edits preserve raw unsupported `EXPR` comment and blank lines in the memo while keeping parsed `selectedReportSettings` and `deletedSettings` provenance aligned to the surviving key/value rows and their original `sourceLineIndex` gaps, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3093 deleted FRX/LBX unsupported `EXPR`-line preservation evidence shipped: focused native report/label root-settings regressions now prove live and deleted `DRIVER` add/remove edits preserve raw unsupported `EXPR` comment and blank lines in the memo while keeping parsed `settings`/`deletedSettings`/`selectedReportSettings` provenance aligned to the surviving key/value rows and their original `sourceLineIndex` gaps, without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3092 shared deleted-settings missing gridh continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `GRIDH` field when that deleted FRX/LBX root horizontal-grid setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3091 shared deleted-settings missing gridv continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `GRIDV` field when that deleted FRX/LBX root vertical-grid setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3090 shared deleted-settings missing papersize continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `PAPERSIZE` field when that deleted FRX/LBX root page-size setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3089 shared deleted-settings missing rightmargin continuity evidence shipped: the shared VSIX/standalone `CopperfinDesignerSelection` settings descriptor map now materializes blank editable `RIGHTMARGIN` when that deleted FRX/LBX root right-margin setting is absent, and focused deleted-settings host smokes prove the asset-editor sends the invariant `--set-property` host update while preserving deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3088 shared deleted-settings missing leftmargin continuity evidence shipped: the shared VSIX/standalone `CopperfinDesignerSelection` settings descriptor map now materializes blank editable `LEFTMARGIN` when that deleted FRX/LBX root left-margin setting is absent, and focused deleted-settings host smokes prove the asset-editor sends the invariant `--set-property` host update while preserving deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3087 shared deleted-settings missing paperwidth continuity evidence shipped: the shared VSIX/standalone `CopperfinDesignerSelection` settings descriptor map now materializes blank editable `PAPERWIDTH` when that deleted FRX/LBX root page-width setting is absent, and focused deleted-settings host smokes prove the asset-editor sends the invariant `--set-property` host update while preserving deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3086 shared deleted-settings missing paperlength continuity evidence shipped: the shared VSIX/standalone `CopperfinDesignerSelection` settings descriptor map now materializes blank editable `PAPERLENGTH` when that deleted FRX/LBX root page-length setting is absent, and focused deleted-settings host smokes prove the asset-editor sends the invariant `--set-property` host update while preserving deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3085 shared deleted-settings missing orientation continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `ORIENTATION` field when that deleted FRX/LBX root orientation setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3084 shared deleted-settings missing cols continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COLS` field when that deleted FRX/LBX root column-count setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3083 shared deleted-settings missing colwidth continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COLWIDTH` field when that deleted FRX/LBX root column-width setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3082 shared deleted-settings missing colspacing continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COLSPACING` field when that deleted FRX/LBX root column-spacing setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3081 shared deleted-settings missing color continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COLOR` field when that deleted FRX/LBX root color setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3080 shared deleted-settings missing copies continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COPIES` field when that deleted FRX/LBX root copies setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3079 shared deleted-settings missing collate continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `COLLATE` field when that deleted FRX/LBX root collation setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3078 shared deleted-settings missing ascii continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `ASCII` field when that deleted FRX/LBX root ASCII-mode setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3077 shared deleted-settings missing ttoption continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `TTOPTION` field when that deleted FRX/LBX root TrueType option setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3076 shared deleted-settings missing y-resolution continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `YRESOLUTION` field when that deleted FRX/LBX root y-resolution setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3075 shared deleted-settings missing print-quality continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `PRINTQUALITY` field when that deleted FRX/LBX root print-quality setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3074 shared deleted-settings missing printer-output continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `OUTPUT` field when that deleted FRX/LBX root printer-output setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3073 shared deleted-settings missing printer-device continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `DEVICE` field when that deleted FRX/LBX root printer-device setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3072 shared deleted-settings missing printer-driver continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes a blank editable `DRIVER` field when that deleted FRX/LBX root printer setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3071 shared deleted-settings missing EXPR-backed default-source continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes blank editable `DEFAULTSOURCE` when that deleted FRX/LBX root setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3070 shared deleted-settings missing sort-setting continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes blank editable `TAG` when that deleted FRX/LBX root sort-setting is absent, sends the invariant `--set-property` host update, and preserves deleted settings-rooted selection while surfacing derived sort metadata (`SORTEXPRESSION`, field index, memo provenance) after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3069 shared deleted-settings missing top/bottom margin add continuity evidence shipped: focused shared VSIX/standalone deleted-settings host smokes now prove the asset-editor property grid materializes blank editable `TOPMARGIN` and `BOTMARGIN` fields when those deleted FRX/LBX root settings are absent, sends invariant `--set-property` updates through the existing host contract, and preserves deleted settings-rooted selection plus deleted preview/document/sort continuity after the host-backed refresh without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3068 shared missing direct-field top/bottom report-margin parity shipped: the shared VSIX/standalone report-settings selection model now materializes absent FRX/LBX root `TOPMARGIN` and `BOTMARGIN` as editable numeric settings when the underlying asset lacks current values, using the existing host-backed direct-field update path without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts; focused host-backed and shared real-asset smokes now prove top/bottom margin add, reload, and undo continuity on real `by_author.FRX` and `STYLELBL.LBX` samples, including stable selected-settings provenance after the fields are created.
- 2026-07-01: E3/#3067 shared missing direct-field `TAG` root-setting parity shipped: the shared VSIX/standalone report-settings selection model now materializes absent FRX/LBX root `TAG` as an editable setting when the underlying asset lacks a current value, using the existing host-backed direct-field update path without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts; focused host-backed and shared real-asset smokes now prove `TAG` add, reload, and undo continuity on real `by_author.FRX` and `STYLELBL.LBX` samples, including stable selected-settings provenance after the field is created.
- 2026-07-01: E3/#3066 shared missing EXPR-backed root-setting parity shipped: the shared VSIX/standalone report-settings selection model now materializes the remaining add-safe absent FRX/LBX root settings that the current host path can create through the shared `EXPR` update seam, including numeric fields such as `DEFAULTSOURCE`, `ASCII`, `COLLATE`, `COPIES`, `PRINTQUALITY`, `YRESOLUTION`, `TTOPTION`, `COLS`, `COLWIDTH`, `COLSPACING`, `GRIDV`, `GRIDH`, `ORIENTATION`, and `PAPERSIZE`, while keeping optional numeric fields blank in the property grid until the user edits them instead of fabricating `0`; focused shared real-asset smokes now prove `DEFAULTSOURCE` can be added, reloaded, and undone through the shared settings-rooted editor on real `by_author.FRX` and `STYLELBL.LBX` samples, and focused host-backed real-asset smokes prove the same add/remove lane through the stable `reportLayout.settings` contract without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3065 FRX/LBX root-settings unsupported EXPR-line preservation shipped: the shared native report/label root-settings write path now preserves non-edited unsupported `EXPR` rows that survive the current memo parsing layer, including unknown setting rows and comment-style rows after parser canonicalization, when a supported FRX/LBX root setting is added, updated, or removed through the existing host-backed property-edit path instead of collapsing the memo into a supported-only list; focused native regression coverage now proves `DRIVER` add/remove updates keep canonicalized unsupported line ordering and supported setting `sourceLineIndex` provenance stable for both reports and labels without changing host JSON keys, invariant property names, enum values, or other machine-readable contracts.
- 2026-07-01: E3/#3064 real memo-backed printer-setting add/remove continuity shipped: the shared VSIX/standalone report-settings selection model now exposes blank editable `DRIVER`, `DEVICE`, and `OUTPUT` fields even when those known string-backed root settings are absent from the live FRX/LBX `EXPR` memo, so the shared property grid can add them through the existing host-backed update path without changing invariant host JSON keys, property names, enum values, or other machine-readable contracts; focused native regression coverage now locks memo-backed string-setting insertion ordering plus command-undo removal, and shared host-backed plus VSIX/standalone real-asset `by_author.FRX` and `STYLELBL.LBX` DRIVER smokes now prove update, reload, and undo continuity against the stable `reportLayout.settings` contract.
- 2026-07-01: E3/#3063 memo-backed real FRX/LBX page-setup setting writes shipped: the shared native report/label visual-property update path now reads and writes root `EXPR` memo settings such as `ORIENTATION` and `PAPERSIZE` from real FRX/LBX settings records instead of limiting writes to direct DBF fields and SCX/VCX `PROPERTIES` blobs, while preserving invariant host JSON keys, property names, enum values, and other machine-readable contracts; focused native regression coverage now locks memo-backed root-setting provenance, and shared host-backed plus VSIX/standalone real-asset `by_author.FRX` and `STYLELBL.LBX` ORIENTATION smokes now prove update, reload, and undo continuity against the stable `reportLayout.settings` contract without requiring raw snapshot object properties for memo-backed settings.
- 2026-07-01: E3/#3062 grouped real FRX key-expression continuity evidence shipped: shared host-backed and VSIX/standalone real-asset section `EXPR` smokes now prove grouped `bandedm.FRX` `Group Header` expression edits preserve the paired `Group Footer` sibling row, grouped metadata, explorer title, and contained-object geometry across update, reload, and undo; the real section smoke helpers now distinguish update-phase versus undo-phase grouped metadata expectations so real grouped memo-block provenance stays covered without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3061 grouped real FRX section-height continuity evidence shipped: shared host-backed and VSIX/standalone real-asset section `HEIGHT` smokes now prove grouped multi-object `bandedm.FRX` `Group Header` height edits preserve all eight contained object geometries plus the untouched `Group Footer` sibling row and grouping linkage metadata across update, reload, and undo, without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3060 non-top real FRX/LBX sibling-row continuity evidence shipped: shared host-backed and VSIX/standalone real-asset section `HEIGHT` and grouped `EXPR` smokes now prove non-top section edits preserve untouched sibling rows for `STYLELBL.LBX` and `by_author.FRX` across update, reload, and undo, including grouped-header metadata continuity for the real report sample, without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3059 real FRX/LBX sibling-row continuity evidence shipped: shared host-backed and VSIX/standalone real-asset section `VPOS` smokes now prove targeted section edits preserve untouched sibling section rows for `STYLELBL.LBX` and grouped `bandedm.FRX` across update, reload, and undo, including grouped-footer metadata continuity for the real report sample, without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3058 real multi-object report section-top continuity shipped: the shared native report-layout grouping pass now pairs report group header/footer sections by structural record order instead of mutable section top, so moving a real grouped section such as `bandedm.FRX` `Group Header` below its footer preserves grouping index and partner linkage instead of reclassifying the section; focused native regression coverage now locks that moved-header pairing seam, shared real-asset section-top smokes now validate grouped `bandedm.FRX` section edits across all eight contained objects plus grouping metadata, and the companion `bandedm.FRX` deleted-reorder real-asset smokes now expect the host's real post-reorder section-row renumbering (`5 -> 6`) rather than treating that stable machine-readable behavior as a failure, without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3057 real report/label section-top continuity shipped: the shared FRX/LBX section-top write path now detects band-record `VPOS` edits, reparses the full table before batching the update, and shifts contained placed-object `VPOS` values in the same command so real section-top edits preserve section-relative geometry instead of leaving contained objects behind when the initial host selection snapshot did not include the whole asset; the shared native report-layout section assignment rule now prefers the deepest overlapping section for both host snapshots and write-back targeting, the shared VSIX/standalone undo path now explicitly rebinds non-object explorer selections after command undo, and focused native plus real-asset designer regressions now cover `by_author.FRX`, `STYLELBL.LBX`, and the real `bandedm.FRX` deleted-reorder overlap seam without changing host JSON field names, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3056 shared real label axis-specific snap continuity coverage shipped: focused designer smokes now drive the shared VSIX/standalone asset editor through real `STYLELBL.LBX` horizontal-only and vertical-only snap commands, then reload and undo those edits to prove the existing Studio host `--snap-object --snap-mode horizontal|vertical` contracts preserve label selection continuity, invariant host JSON keys, invariant property names, and round-trip-safe raw/layout geometry updates without localizing any machine-readable fields.
- 2026-07-01: E3/#3055 shared report/label vertical-snap command parity shipped: the shared VSIX/standalone asset editor now exposes localized vertical-snap chrome for live FRX/LBX objects and routes it through the existing Studio host `--snap-object --snap-mode vertical` contract with invariant `--record`, repeated `--snap-target-unique-id` selectors, and shared `GRIDH`/`GRIDV` report settings; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `STYLE3V.FRX` round trip with undo, without changing host JSON keys, invariant property names, or existing snap-object command contracts.
- 2026-07-01: E3/#3054 shared report/label horizontal-snap command parity shipped: the shared VSIX/standalone asset editor now exposes localized horizontal-snap chrome for live FRX/LBX objects and routes it through the existing Studio host `--snap-object --snap-mode horizontal` contract with invariant `--record`, repeated `--snap-target-unique-id` selectors, and shared `GRIDH`/`GRIDV` report settings; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `STYLE3V.FRX` round trip with undo, without changing host JSON keys, invariant property names, or existing snap-object command contracts.
- 2026-07-01: E3/#3053 shared report/label vertical-distribution command parity shipped: the shared VSIX/standalone asset editor now exposes localized vertical-distribution chrome for live multi-selected FRX/LBX objects and routes it through the existing Studio host `--distribute-object --distribution-mode vertical` contract with invariant `--record` plus repeated `--distribute-target-unique-id` selectors; focused designer smokes now cover invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `STYLE3V.FRX` vertical-distribution round trip with undo, without changing host JSON keys or existing distribution contracts.
- 2026-07-01: E3/#3052 shared report/label top-alignment command parity shipped: the shared VSIX/standalone asset editor now exposes localized top-alignment chrome for live multi-selected FRX/LBX objects and routes it through the existing Studio host `--align-object --alignment-mode top` contract with invariant `--record`, `--anchor-unique-id`, and repeated `--align-target-unique-id` selectors; focused designer smokes now cover invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `DATAEXPLORERQUICKREPORT.FRX` top-only round trip, and the VFPSource ZIP extraction helper now refreshes writable cached fixtures then seals them read-only while writable smoke copies explicitly clear that bit so real-asset runs cannot inherit mutated source fixtures.
- 2026-07-01: E3/#3049 shared report/label resize-to-anchor height command parity shipped: the shared VSIX/standalone asset editor now routes multi-selected live FRX/LBX object resize-to-anchor through the existing Studio host `--resize-object --resize-mode height` contract with invariant `--record`, `--anchor-unique-id`, and repeated `--resize-target-unique-id` selectors, exposes localized match-height command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves anchor selection continuity across both host-backed resize and command-undo refreshes so the anchor stays focused while resized geometry is immediately reflected in the shared property grid; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `DATAEXPLORERQUICKREPORT.FRX` height-only round trip without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3048 shared report/label resize-to-anchor width command parity shipped: the shared VSIX/standalone asset editor now routes multi-selected live FRX/LBX object resize-to-anchor through the existing Studio host `--resize-object --resize-mode width` contract with invariant `--record`, `--anchor-unique-id`, and repeated `--resize-target-unique-id` selectors, exposes localized match-width command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves anchor selection continuity across both host-backed resize and command-undo refreshes so the anchor stays focused while resized geometry is immediately reflected in the shared property grid; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `DATAEXPLORERQUICKREPORT.FRX` width-only round trip without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3047 shared report/label resize-to-anchor size command parity shipped: the shared VSIX/standalone asset editor now routes multi-selected live FRX/LBX object resize-to-anchor through the existing Studio host `--resize-object --resize-mode size` contract with invariant `--record`, `--anchor-unique-id`, and repeated `--resize-target-unique-id` selectors, exposes localized match-size command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves anchor selection continuity across both host-backed resize and command-undo refreshes so the anchor stays focused while resized geometry is immediately reflected in the shared property grid; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `DATAEXPLORERQUICKREPORT.FRX` width/height round trip without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3046 shared report/label snap-to-grid command parity shipped: the shared VSIX/standalone asset editor now routes live FRX/LBX object snap-to-grid through the existing Studio host `--snap-object --snap-mode both` contract with invariant `--record`, repeated `--snap-target-unique-id`, and grid dimensions sourced from the existing shared `GRIDH`/`GRIDV` report settings, exposes localized snap-to-grid command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves selection continuity across both host-backed snap and command-undo refreshes so the selected object stays active while snapped geometry is immediately reflected in the shared property grid; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and real `STYLE3V.FRX` + `STYLELBL.LBX` round trips without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3045 shared report/label horizontal distribution command parity shipped: the shared VSIX/standalone asset editor now routes multi-selected live FRX/LBX object distribution through the existing Studio host `--distribute-object --distribution-mode horizontal` contract with invariant `--record` plus repeated `--distribute-target-unique-id` selectors, exposes localized horizontal-distribution command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves focused multi-selection continuity across both host-backed distribute and command-undo refreshes so the focused object stays selected while the distributed selection set remains available for further shared commands; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `STYLE3V.FRX` round trip without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3044 shared report/label left-alignment command parity shipped: the shared VSIX/standalone asset editor now routes multi-selected live FRX/LBX object alignment through the existing Studio host `--align-object --alignment-mode left` contract with invariant `--record`, `--anchor-unique-id`, and repeated `--align-target-unique-id` selectors, exposes localized align-left command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserves multi-selection continuity across both host-backed align and command-undo refreshes so the anchor stays focused while the aligned selection set remains available for further shared commands; focused designer smokes now prove invariant host invocation shape plus shared continuity on synthetic report/label shells and a real `DATAEXPLORERQUICKREPORT.FRX` round trip without changing host JSON keys, invariant property names, or existing layout-action command contracts.
- 2026-07-01: E3/#3043 shared report/label post-nudge property-grid geometry refresh shipped: the shared VSIX/standalone `CopperfinDesignerSelection` integer normalization now truncates FRX/LBX decimal geometry values such as `HPOS` and `VPOS` into the same integer display coordinates used by the shared editor, so live report/label selections immediately show updated nudge geometry without a manual reload, while focused real-asset designer smokes now prove visible pre-nudge, post-nudge, and post-undo `HPOS`/`VPOS` continuity on representative `by_author.FRX` and `STYLELBL.LBX` samples without changing host JSON keys, invariant property names, or existing nudge command contracts.
- 2026-07-01: E3/#3042 shared real-asset report/label nudge continuity coverage shipped: the shared VSIX/standalone bridge and snapshot client now expose the existing host-backed report/label nudge command with stable machine-readable arguments, shared undo for report/label selections now preserves the selected record when the host supports it, and focused real-asset designer smokes now drive representative `by_author.FRX` and `STYLELBL.LBX` rows through live nudge, reload, and undo continuity without changing host JSON keys, invariant property names, or existing command contracts.
- 2026-07-01: E3/#3041 real deleted horizontal batch visual property continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative `by_author.FRX` and `STYLELBL.LBX` rows through delete, one-command deleted-object `HPOS` + `WIDTH` updates, reloads, undos, and restore, extending real batch-edit continuity across the deleted-object horizontal geometry lane without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3040 real deleted font-option batch visual property continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative `by_author.FRX` and `STYLELBL.LBX` rows through delete, one-command deleted-object `FONTSTYLE` + `FONTSIZE` updates, reloads, undos, and restore, extending real batch-edit continuity further into the deleted-object lane without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3039 real deleted batch visual property continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative `by_author.FRX` and `STYLELBL.LBX` rows through delete, one-command deleted-object `VPOS` + `HEIGHT` updates, reloads, undos, and restore, extending real batch-edit continuity into the deleted-object lane without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3038 real font-option batch visual property continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative live `by_author.FRX` and `STYLELBL.LBX` rows through one-command `FONTSTYLE` + `FONTSIZE` updates, reloads, and undos, extending real batch-edit continuity beyond the earlier geometry pairs without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3037 real vertical batch visual property continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative live `by_author.FRX` and `STYLELBL.LBX` rows through one-command `VPOS` + `HEIGHT` updates, reloads, and undos, extending real batch-edit continuity beyond the earlier horizontal `HPOS` + `WIDTH` pair without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3036 remaining host-backed FRX/LBX live visual property continuity coverage shipped: focused native host-backed smokes now drive representative live `by_author.FRX` and `STYLELBL.LBX` rows through `VPOS`, `WIDTH`, and `HEIGHT` update, reload, and undo flows, closing the remaining real live visual-property seam in the direct host lane without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3035 remaining real FRX/LBX live visual property continuity coverage shipped: focused shared VSIX/standalone designer smokes now drive representative live `by_author.FRX` and `STYLELBL.LBX` rows through `VPOS`, `WIDTH`, and `HEIGHT` edit, reload, and undo flows, closing the remaining direct shared property-grid visual-property seam for live placed report/label objects without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3034 remaining real deleted FRX/LBX visual property continuity coverage shipped: focused shared VSIX/standalone designer smokes now drive representative deleted `by_author.FRX` and `STYLELBL.LBX` rows through `VPOS`, `WIDTH`, `HEIGHT`, and `FONTFACE` edit, reload, undo, and restore flows, completing real deleted-object coverage for the shared visual-property surface already proven synthetically without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3033 real deleted FRX/LBX font option continuity coverage shipped: focused shared VSIX/standalone designer smokes now drive representative deleted `by_author.FRX` and `STYLELBL.LBX` rows through `FONTSTYLE` and `FONTSIZE` edit, reload, undo, and restore flows, while the deleted-object real-asset assertion lane now treats layout-only geometry checks as optional so font option round trips prove deleted section/object continuity and on-disk raw snapshot fidelity without inventing new report-layout fields or changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3032 real FRX/LBX font style and size continuity coverage shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative real `by_author.FRX` and `STYLELBL.LBX` rows through `FONTSTYLE` and `FONTSIZE` edit, reload, and undo flows, proving real report/label font option continuity against writable on-disk assets without changing host JSON keys, invariant property names, or other machine-readable contracts.
- 2026-07-01: E3/#3031 first-class PRG shared-editor guidance and real PRG open coverage shipped: the shared VSIX/standalone editor now treats `.prg` assets as localized visual programs instead of generic Copperfin assets, routes program-specific guidance through the managed `en-US`, `es-419`, `pt-BR`, and `qps-ploc` catalogs, exposes `.prg` in the shared open-file filter, and adds focused designer smokes that prove localized program guidance plus a real `VFPSource/tasklist/main.prg` open path without changing snapshot kind/family identities, host JSON keys, or other machine-readable contracts.
- 2026-07-01: E3/#3030 real column-setup continuity after EXPR settings round trip shipped: focused shared designer smoke coverage now starts from writable copies of the real `by_author.FRX` and `STYLELBL.LBX` assets, drives host-backed root `EXPR` updates to create `COLS`, `COLWIDTH`, and `COLSPACING`, reloads the mutated assets through the shared VSIX/standalone property-grid path, and proves shared column-setup continuity plus undo restoration to the original no-column-setup state without relying on an upstream corpus sample that already contains column settings; no host JSON keys, invariant property names, or machine-readable setting names changed.
- 2026-07-01: E3/#3029 deleted-preview settings continuity after real delete round trip shipped: focused shared designer smoke coverage now starts from writable copies of the real `by_author.FRX` and `STYLELBL.LBX` assets, drives real host-backed deletes to create deleted preview metadata, reloads the mutated assets through the shared VSIX/standalone property-grid path, and proves `DELETEDPREVIEWBOUNDS` continuity without relying on an upstream corpus sample that already contains deleted preview settings; no host JSON keys, invariant property names, or machine-readable setting names changed.
- 2026-07-01: E3/#3028 real label document-title continuity evidence shipped: focused shared designer smoke coverage now explicitly requires a real LBX candidate and proves the shared VSIX/standalone settings selection surfaces `DOCUMENTTITLE` from the real `STYLELBL.LBX` sample instead of passing on an earlier report asset; no host JSON keys, invariant property names, or machine-readable setting names changed.
- 2026-07-01: E3/#3027 real label preview-bounds settings continuity evidence shipped: focused shared designer smoke coverage now drives the real `STYLELBL.LBX` settings scope through the shared VSIX/standalone property-grid model and proves existing `PREVIEWBOUNDS` metadata continuity while explicitly requiring a real LBX candidate instead of passing on an earlier report asset; no host JSON keys, invariant property names, or machine-readable setting names changed.
- 2026-07-01: E3/#3026 real label settings continuity evidence shipped: focused shared designer smoke coverage now drives the real `STYLELBL.LBX` settings scope through the shared VSIX/standalone property-grid model and proves invariant `ORIENTATION`, `PAPERSIZE`, `COLOR`, `GRIDV`, and `GRIDH` continuity while explicitly requiring a real LBX candidate instead of passing on an earlier report asset; no host JSON keys, invariant property names, or machine-readable setting names changed.
- 2026-07-01: E3/#3025 real report auxiliary print settings continuity shipped: the shared VSIX/standalone report-settings property-grid path now treats invariant `ASCII`, `COLLATE`, and `COPIES` as localized numeric settings in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused shared designer smokes now prove live and deleted settings-root continuity plus host-refresh continuity for those auxiliary print values without changing update targets, and a focused real-asset smoke now proves the shared selection model surfaces those settings from the real `foxrefresultsa4.frx` sample without changing host JSON keys or machine-readable setting names.
- 2026-07-01: E3/#3024 real report color settings continuity shipped: the shared VSIX/standalone report-settings property-grid path now treats invariant `COLOR` as a localized numeric setting in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused shared designer smokes now prove live and deleted settings-root continuity plus host-refresh continuity for that color-mode value without changing update targets, and a focused real-asset smoke now proves the shared selection model surfaces `COLOR` from the real `by_author.FRX` sample without changing host JSON keys or machine-readable setting names.
- 2026-07-01: E3/#3023 real report print-profile settings continuity shipped: the shared VSIX/standalone report-settings property-grid path now treats invariant `DEFAULTSOURCE`, `PRINTQUALITY`, `YRESOLUTION`, and `TTOPTION` as localized numeric settings in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused shared designer smokes now prove live and deleted settings-root continuity plus host-refresh continuity for those print-profile values without changing update targets, and a focused real-asset smoke now proves the shared selection model surfaces those settings from the real `STYLE1H.FRX` sample without changing host JSON keys or machine-readable setting names.
- 2026-07-01: E3/#3022 real report printer-identity settings continuity shipped: the shared VSIX/standalone report-settings property-grid path now routes invariant `DRIVER`, `DEVICE`, and `OUTPUT` setting names through localized display labels in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused shared designer smokes now prove live and deleted settings-root continuity plus host-refresh continuity for those string settings without changing update targets, and a focused real-asset smoke now proves the shared selection model surfaces printer-identity values from the real `STYLE1H.FRX` sample without changing host JSON keys or machine-readable setting names.
- 2026-07-01: E3/#3021 real report paper-dimension settings continuity shipped: the shared VSIX/standalone report-settings property-grid path now treats `PAPERLENGTH` and `PAPERWIDTH` as first-class numeric settings with localized display labels in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused designer smokes now prove live and deleted synthetic settings continuity plus host-refresh continuity for those invariant property names, and a focused real-asset smoke now proves the shared selection model surfaces `PAPERLENGTH` and `PAPERWIDTH` from the real `DATAEXPLORERQUICKREPORT.FRX` sample without changing host JSON keys or machine-readable setting names.
- 2026-07-01: E3/#3020 shared column-setup settings continuity shipped: the focused VSIX/standalone designer smokes now prove live and deleted FRX/LBX root-settings selections surface `COLS`, `COLWIDTH`, and `COLSPACING`, localize their display labels through `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and preserve settings-rooted shared host-refresh continuity after column-setup edits without changing invariant property names, host JSON keys, or record/field provenance contracts.
- 2026-07-01: E3/#3019 real settings-root write-back continuity shipped: focused Studio-host and shared VSIX/standalone designer smokes now drive representative real `by_author.FRX` and `STYLELBL.LBX` settings scopes through live `GRIDV` edit, reload, and undo flows, while the managed Studio snapshot model now retains the existing `selectedReportSettingsAvailable`, `selectedReportSelectionKind`, and `selectedReportSettings` host JSON fields so real settings-root refresh continuity can be asserted directly without changing machine-readable field names or report/label identity contracts.
- 2026-07-01: E3/#3018 real page-setup settings continuity shipped: the focused shared designer smokes now prove representative real FRX and LBX settings scopes surface live `ORIENTATION`, `PAPERSIZE`, `GRIDV`, and `GRIDH` values from the resolved VFP asset corpus through the existing VSIX/standalone property-grid model, preserving invariant property names, host JSON keys, and root-settings provenance contracts without adding new machine-readable fields.
- 2026-07-01: E3/#3017 shared side-margin settings parity shipped: the focused VSIX/standalone designer smokes now prove live and deleted FRX/LBX root-settings selections surface `LEFTMARGIN` and `RIGHTMARGIN`, localize their display labels through `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and round-trip side-margin edits through the existing shared property-grid and Studio-host refresh paths without changing invariant property names, host JSON keys, or record/field provenance contracts.
- 2026-07-01: E3/#3016 left/right page-margin record-mutation evidence shipped: the focused Studio-host record-settings regression lane now proves live and deleted FRX/LBX record-selected `LEFTMARGIN` and `RIGHTMARGIN` update/clear flows persist on disk, refresh through the existing `settings`/`deletedSettings`/`selectedReportSettings` JSON paths, preserve preview and section/object metadata continuity, and do not add new machine-readable left/right summary keys beyond the existing shared report-layout contract.
- 2026-07-01: E3/#3015 left/right page-margin stable-mutation evidence shipped: the focused Studio-host page-setup regression suite now proves live and deleted FRX/LBX stable-selected `LEFTMARGIN` and `RIGHTMARGIN` update/clear flows persist on disk, refresh through the existing selected-settings JSON path, preserve selected-settings provenance/order, and do not add new machine-readable left/right summary keys beyond the existing shared report-layout contract.
- 2026-07-01: UI-direction guidance was reaffirmed as durable product direction in `agent-handoff.md`: future host-shell work should keep the shared designer behavior while making each shell native to its platform, with the VSIX following modern Visual Studio 202x conventions including a bottom-tabbed Command window, the standalone IDE owning dockable Command and terminal panes, and backlog tracking continuing under `#2996`, `#2997`, and `#2998` instead of duplicate UI-direction issues.
- 2026-07-01: E3/#3014 shared report/label left/right page-margin settings exposure shipped: the shared FRX/LBX report-layout parser now carries nonblank direct `LEFTMARGIN` and `RIGHTMARGIN` root settings through the existing settings snapshot contract for live and deleted roots, preserving invariant machine-readable property names and selected-settings host JSON while reusing the existing localized property-grid labels already present in the shared designer selection model; focused native and Studio-host regressions now prove live/deleted provenance and selected-settings parity without introducing new host JSON keys.
- 2026-07-01: E3/#3013 broadened remaining real-asset settings candidate discovery shipped: the shared designer-smoke harness now resolves sort-metadata, document-title, preview-bounds, and deleted-preview-bounds settings continuity candidates from the broader FRX/LBX corpus exposed by configured VFP roots and extracted real-asset archives instead of a narrow preferred-file list, proving live sort/document-title/preview continuity against the available corpus while the deleted-preview path still emits an explicit skip because no current real asset exposes the required settings-plus-deleted-preview combination; host JSON and machine-readable contracts were unchanged.
- 2026-07-01: E3/#3011 shared report/label shell-summary deleted-count exposure shipped: the shared VSIX/standalone FRX/LBX shell summary now reports deleted section, deleted settings, and deleted object counts alongside the existing live section, grouping, settings, and unplaced-object totals while preserving host JSON and machine-readable contracts; focused designer smokes now prove `en-US`, `es-419`, `pt-BR`, and `qps-ploc` routing for the expanded live/deleted summary text.
- 2026-07-01: E3/#3010 real-asset report/label settings preview-metadata smoke coverage shipped: the shared FRX/LBX settings-root selection now has real-asset designer-smoke proof that preview-bounds metadata survives from live FRX snapshots into the shared property grid with invariant `PREVIEWBOUNDS` values, while deleted-preview-bounds coverage emits an explicit skip when no current real asset exposes the required settings-plus-deleted-preview combination and no host or machine-readable contracts changed.
- 2026-07-01: E3/#3009 shared report/label document-title settings exposure shipped: the shared VSIX/standalone FRX/LBX root-settings property-grid selection now surfaces localized read-only document-title metadata from the existing managed report-layout model while preserving stable host JSON keys, invariant editable setting update targets, and shared behavior between Visual Studio and standalone Studio; focused designer smokes now prove synthetic and host-backed settings continuity plus `en-US`, `es-419`, `pt-BR`, and `qps-ploc` routing for the new visible document-title metadata.
- 2026-07-01: E3/#3008 shared report/label sort-metadata settings exposure shipped: the shared VSIX/standalone FRX/LBX root-settings property-grid selection now surfaces localized read-only sort metadata from the existing `TAG` root setting, including active sort expression plus provenance field/memo details, while preserving invariant editable `TAG` update targets, stable host JSON keys, and shared behavior between Visual Studio and standalone Studio; focused designer smokes now prove synthetic and host-backed settings continuity plus `en-US`, `es-419`, `pt-BR`, and `qps-ploc` routing for the new visible sort metadata.
- 2026-06-30: E3/#3007 shared report/label grouping explorer selection shipped: the shared VSIX/standalone FRX/LBX explorer now exposes root grouping definitions from the existing managed report-layout model, and the shared property-grid path now surfaces localized read-only grouping index, nesting depth, expression provenance, and header/footer linkage metadata without changing host JSON keys or machine-readable update targets; focused designer smokes now prove synthetic and real grouped-asset continuity plus `en-US`, `es-419`, `pt-BR`, and `qps-ploc` routing for the new visible grouping rows and property labels.
- 2026-06-30: E3/#3006 shared report/label preview-metadata settings exposure shipped: the shared VSIX/standalone FRX/LBX root-settings property-grid selection now surfaces localized live and deleted preview bounds from the existing managed report-layout model while preserving invariant update targets, stable host JSON keys, and shared behavior between Visual Studio and standalone Studio; focused designer smokes now prove settings-scope explorer selection, host-backed refresh continuity, and `en-US`, `es-419`, `pt-BR`, and `qps-ploc` localization for the new visible preview metadata.
- 2026-06-30: E3/#3005 shared deleted report/label settings scope shipped: the shared VSIX/standalone FRX/LBX managed report-layout model now retains `deletedSettings`, the shared explorer now exposes a localized deleted-settings scope alongside live settings and sections, the shared property-grid model now surfaces deleted settings with stable record identity, localized deleted/live state, and invariant update targets, and focused designer smokes now prove deleted-settings explorer selection, localization, and host-backed root-setting updates without changing host JSON keys or machine-readable property names.
- 2026-06-30: E3/#3004 shared report/label settings scope shipped: the shared VSIX/standalone FRX/LBX explorer now exposes a localized settings scope alongside sections and unplaced objects, the shared property-grid model now surfaces editable root settings with invariant update targets and localized display labels for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, managed snapshot settings now retain root-setting record/field/memo provenance, and focused designer smokes now prove settings-scope explorer selection, localization, and host-backed root-setting updates without changing host JSON keys or machine-readable property names.
- 2026-06-30: UI-direction guidance tightened in `agent-handoff.md`: future shell work should treat the current shared WinForms chrome as temporary, keep the VSIX aligned to current Visual Studio 202x conventions with the Command window as a bottom-tabbed tool window, give the standalone IDE a modern dockable shell with dockable Command and terminal panes, and follow Microsoft Windows, Apple macOS, and Linux/VS Code-class platform UI conventions per host.
- 2026-06-30: E3/#3003 shared report-section expression provenance exposure shipped: the shared VSIX/standalone section property-grid path now surfaces localized raw section-expression field-index and memo-block metadata separately from grouping-expression provenance for report/label section selections, and focused designer smokes now prove both synthetic localized selections and a real grouped `by_author.FRX` `Group Header` selection expose the same raw section-expression provenance already emitted by the host contract without changing host JSON keys, editable update targets, or localized contract names.
- 2026-06-30: E3/#3002 grouped FRX section-expression parity shipped: the shared managed report-section snapshot model now preserves raw section `expression` metadata alongside grouping metadata, grouped report/label section property-grid selections now bind editable `EXPR` to the section-rooted expression while exposing grouping expression metadata through a separate read-only row, and focused host-backed plus shared VSIX/standalone designer smokes now drive the real grouped `by_author.FRX` `Group Footer` section through `EXPR` edit, reload, and undo flows to prove section-expression continuity stays aligned with on-disk host behavior without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#3001 real grouped FRX section continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive the real grouped `by_author.FRX` `Group Footer` section through section-rooted `HEIGHT` edits, reloads, and undo flows, proving grouping-role, grouping-expression, and partner-section metadata stay stable alongside section identity and on-disk round trips without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#3000 real live FRX/LBX rename continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative live `by_author.FRX` and `STYLELBL.LBX` rows through real rename, reload, and command-undo flows, proving stable section/object continuity and on-disk `UNIQUEID` round trips for supported live report/label renames without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2999 real live FRX reorder continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive a representative live `STYLE2V.FRX` row through real reorder and reload flows, proving a live report object can be promoted to the front while its `UNIQUEID` stays stable, the containing `Title` section survives record renumbering, companion rows keep identity, and the shared editor preserves selection continuity without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: Backlog UI direction captured: future shell work is now explicitly tracked under `#2996` (VSIX native Visual Studio 2022 shell standards), `#2997` (modern dockable standalone IDE shell with dockable Command and terminal panes plus platform-native Windows/macOS/Linux conventions), and `#2998` (future curated standalone extension support while explicitly excluding the Copperfin VSIX package), and `agent-handoff.md` now records that the current shared WinForms shell chrome is provisional rather than the target product UX.
- 2026-06-30: E3/#2995 real deleted FRX reorder continuity shipped: the shared VSIX/standalone report/label asset editor now keeps deleted-row reorder commands available for selected deleted report/label objects, rebinds shared explorer/property-grid/design-surface continuity by stable `UNIQUEID` after host-backed reorders that renumber records or deleted sections, and focused host-backed plus shared real-asset designer smokes now drive representative deleted `bandedm.FRX` rows through reorder and reload flows to prove deleted-row order, deleted-section continuity, and on-disk identity persistence without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2994 real live LBX duplicate continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive a representative live `STYLELBL.LBX` row through real duplicate and reload flows, proving the duplicated label row gets a distinct live record plus on-disk `UNIQUEID`, preserves `Detail` section continuity, and keeps shared selection continuity aligned with the reloaded snapshot without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2993 real deleted FRX/LBX duplicate continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative deleted rows from VFPSource `by_author.FRX` and `STYLELBL.LBX` through real duplicate, reload, and restore flows, proving deleted-source identity, duplicated deleted-row identity, section deleted/live counts, and on-disk `UNIQUEID` round trips stay stable while a restored duplicate returns live and the original row remains deleted, without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2992 real deleted FRX/LBX rename continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative deleted rows from VFPSource `by_author.FRX` and `STYLELBL.LBX` through real rename, reload, command-undo, and restore flows, proving deleted-object identity, containing-section continuity, and on-disk `UNIQUEID` round trips stay stable for supported deleted-row renames without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2991 real deleted FRX/LBX property-edit continuity shipped: focused host-backed and shared VSIX/standalone designer smokes now drive representative deleted rows from VFPSource `by_author.FRX` and `STYLELBL.LBX` through real `HPOS` edits, reloads, command undo, and restore flows, proving deleted-object identity, containing-section continuity, deleted layout geometry, and on-disk round trips stay stable for supported direct-field edits without changing machine-readable host JSON keys, selectors, or localized contract names.
- 2026-06-30: E3/#2990 real deleted FRX/LBX section-member continuity shipped: the shared managed report/label snapshot model now preserves `reportLayout.deletedObjects`, and the common VSIX/standalone explorer plus design surface now keep deleted objects that still belong to a live or deleted section visible, selectable, restorable, and visually marked instead of dropping them after a real FRX/LBX delete; focused real-asset designer smokes now prove host-backed and shared-editor delete/restore continuity against representative VFPSource `by_author.FRX` and `STYLELBL.LBX` assets without changing machine-readable host JSON keys or selectors.
- 2026-06-30: E3/#2989 report/label batch undo fidelity shipped: shared native visual-object batch edits now collapse successful FRX/LBX multi-property update history into a single undo journal command while preserving the existing per-property/per-object undo semantics for SCX/VCX and other non-report asset families, backward-compatible undo journal reads still handle older single-entry files, and focused native, Studio-host, plus shared designer real-asset smoke coverage now proves representative FRX/LBX batch edits restore all touched properties in one undo without changing machine-readable host JSON keys, selectors, or undo-label fields.
- 2026-06-30: E3/#2988 shared FRX/LBX batch update host refresh shipped: the VSIX/standalone Studio snapshot client now accepts either a full returned snapshot or the existing `visualObjectUpdateBatch` result envelope for multi-property report/label edits, falling back to an immediate reload when the host only reports batch mutation status, and focused designer smoke coverage now proves representative real VFPSource `by_author.FRX` and `STYLELBL.LBX` object batch updates (`HPOS` + `WIDTH`) persist on disk, reload into the shared designer model, and unwind through the existing command-undo chain without changing machine-readable host JSON keys or selectors.
- 2026-06-30: E3/#2987 real FRX/LBX section property continuity shipped: the shared designer smoke harness now drives representative VFPSource section rows from `by_author.FRX` and `STYLELBL.LBX` through host-backed `HEIGHT` edits, reloads, and undo flows, while new section-aware real-asset assertions prove report/label identity, section counts, section geometry, section object counts, and raw snapshot property values stay aligned across both direct Studio-host round trips and the shared VSIX/standalone property-grid path without changing host JSON field names or other machine-readable contracts.
- 2026-06-30: E3/#2986 real FRX duplicate reopen continuity shipped: Studio document opens now load full DBF previews for report/label assets instead of truncating FRX/LBX table previews to eight rows, so a fresh read-only reopen preserves real layout rows such as duplicated `by_author.FRX` objects and the shared VSIX/standalone designer model now sees the same persisted report/label row set that host-backed mutation commands already returned; focused report/label open-document coverage now locks the full-preview contract, and the shared designer smoke harness now proves a real `by_author.FRX` duplicate survives reload in both the host-backed and shared editor paths while the real `STYLE3V.FRX` placement smokes assert the asset's actual unplaced-object counts under the full preview.
- 2026-06-30: E3/#2985 real unplaced FRX placement-transition continuity shipped: the shared designer smoke harness now drives a real VFPSource unplaced `STYLE3V.FRX` row into the real `Page Header` section with a host-backed `VPOS` edit, proves reload and undo restore the original unplaced state, and keeps the shared explorer/property-grid/design-surface continuity aligned with the real placement transition without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2984 real unplaced FRX continuity shipped: the shared designer smoke harness now drives a real VFPSource unplaced `STYLE3V.FRX` row through host-backed `FONTFACE` edit, reload, and undo flows, while the real-asset report snapshot assertions now distinguish placed vs unplaced report-layout ownership so the suite proves unplaced-object continuity without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2983 VFPSource real-asset memo-backed write-back continuity shipped: memo-sidecar resolution in the shared DBF/designer/Studio-host paths now case-folds existing companion files so uppercase `FRT/LBT/SCT/...` sidecars remain writable on case-sensitive filesystems, focused native `test_visual_asset_editor` coverage now proves memo-backed font edits still persist and undo through renamed uppercase sidecars, and the shared designer smoke harness now drives real VFPSource `FONTFACE` edits plus undo/reload continuity against representative `by_author.FRX` and `STYLELBL.LBX` assets while matching the assets' actual selected titles and baseline font values.
- 2026-06-30: E3/#2982 shared asset-editor real FRX/LBX property-grid write-back continuity shipped: the shared designer smoke harness now opens writable copies of representative VFPSource `by_author.FRX` and `STYLELBL.LBX` assets through the real `CopperfinAssetEditorControl`, selects the real section/object rows, applies host-backed `HPOS` edits through the shared property-grid path, proves the shared shell keeps section/object continuity plus undo availability coherent after refresh, and reloads the edited and undone files from disk to prove the actual FRX/LBX bytes were written back and restored while report/label identity, section counts, and placed-object titles remain stable.
- 2026-06-30: E3/#2981 VFPSource real-asset write-back round-trip smoke coverage shipped: the shared designer smoke harness now stages writable copies of representative VFPSource `by_author.FRX` and `STYLELBL.LBX` asset directories, drives real Studio host `HPOS` edits against those copied assets, reloads the mutated files to prove persisted FRX/LBX write-back, then undoes the change and reloads again to prove the original on-disk values return while report/label identity, section counts, and placed-object titles remain stable.
- 2026-06-30: E3/#2980 VFPSource-backed real-asset smoke fallback coverage shipped: the shared designer smoke harness now resolves optional FRX/LBX/PJX/SCX/VCX/MNX evidence from `COPPERFIN_VFPSOURCE_ROOT`, `COPPERFIN_VFPSOURCE_ZIP`, or `~/Downloads/VFPSource.zip`, extracts the containing archive directories so sidecars and neighboring project assets stay available, and auto-resolves local Studio/build/runtime host binaries from the repo so real-asset editor, project-workspace, and standalone tab smokes can run on non-Windows machines without a full VFP9 install; focused smoke expectations now accept the actual VFPSource project-group titles surfaced by `addlabel.pjx`, while the real-project debugger smoke stays explicitly Windows-only until runtime build execution support is validated outside the Windows path.
- 2026-06-30: E3/#2979 shared report/label placement-transition continuity coverage shipped: focused designer smokes now prove host-backed FRX/LBX `VPOS` edits can move the selected object across the section-versus-unplaced boundary in the shared VSIX/standalone asset editor, refreshing shell summary details and preserving explorer/property-grid/design-surface continuity when a report object moves from unplaced into a section and when a label object moves from a section into the unplaced tray, without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2978 shared asset-editor delete/restore lifecycle quadrant coverage shipped: focused designer smokes now prove the remaining shared FRX/LBX object lifecycle quadrants by exercising report `--restore-object` and label `--delete-object` host-backed flows, refreshing shell summary details from the returned snapshot, and preserving explorer/property-grid/design-surface continuity plus live/deleted command-surface flips without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2977 shared asset-editor object identity rename parity shipped: the shared VSIX/standalone FRX/LBX asset editor now routes report/label object id regeneration through the existing Studio host `--rename-object` contract with invariant `--record`, stable optional `--unique-id`, and replacement `--new-unique-id` selectors, exposes localized rename-status chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and focused designer smokes now prove live report plus deleted label rename flows refresh shell summary details and preserve explorer/property-grid/design-surface continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2976 shared asset-editor object reorder front/back command parity shipped: the shared VSIX/standalone FRX/LBX asset editor now routes report/label object bring-to-front and send-to-back actions through the existing Studio host `--reorder-object` contract with invariant `--record`, stable `--unique-id`, and `--placement front|back` selectors, exposes localized reorder command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and focused designer smokes now prove report front plus label back reorder flows refresh shell summary details, reorder visible rows, and preserve explorer/property-grid/design-surface continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2975 shared asset-editor duplicate object command parity shipped: the shared VSIX/standalone FRX/LBX asset editor now routes report/label object duplication through the existing Studio host `--duplicate-object` contract with invariant `--record`, stable `--unique-id`, and replacement `--new-unique-id` selectors, exposes localized duplicate command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and focused designer smokes now prove report and label duplicate flows refresh shell summary details and move explorer/property-grid/design-surface continuity onto the duplicated object without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2974 shared asset-editor delete/restore object command parity shipped: the shared VSIX/standalone FRX/LBX asset editor now routes report/label object lifecycle actions through the existing Studio host `--delete-object` and `--restore-object` contracts with invariant `--record` and optional `--unique-id` selectors, exposes localized delete/restore command chrome for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, and focused designer smokes now prove report delete plus label restore refresh shell summary details and preserve explorer/property-grid/design-surface continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2973 FRX/LBX sibling-row edit isolation evidence shipped: focused native visual-asset regressions now prove supported report/label direct-field and memo-backed edits on one selected row in a multi-record asset preserve sibling live rows, sibling deleted rows, and deleted flags through both the edit and undo chain, tightening safe round-trip evidence for richer FRX/LBX layouts without changing machine-readable asset contracts.
- 2026-06-30: E3/#2972 shared label surface selection and drag parity shipped: focused shared designer smokes now prove direct LBX surface interactions outside the host-refresh path keep live, deleted, and unplaced scope selection aligned across the explorer, property grid, and surface highlight state, and emit invariant `HPOS`/`VPOS` drag updates while preserving containing-section continuity through the shared VSIX/standalone design-surface model.
- 2026-06-30: E3/#2971 label drag shell-summary refresh parity shipped: focused shared designer smokes now prove dragging a live LBX object through the shared VSIX/standalone asset editor still routes one invariant `--visual-object-update-batch` host call, consumes the returned label snapshot, refreshes visible label shell preview-bounds details, and preserves label identity plus object and containing-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2970 report drag shell-summary refresh parity shipped: focused shared designer smokes now prove dragging a live FRX object through the shared VSIX/standalone asset editor still routes one invariant `--visual-object-update-batch` host call, consumes the returned snapshot, refreshes visible report shell preview-bounds details, and preserves report identity plus object and containing-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2969 FRX/LBX inspection no-op binary round-trip evidence shipped: focused native visual-asset regressions now prove representative inspection-only report/label editor operations such as property query and property listing leave `.frx/.frt` and `.lbx/.lbt` table and memo bytes untouched and do not create undo history, tightening explicit no-op binary round-trip evidence without changing asset contracts.
- 2026-06-30: E3/#2968 batch property reorder no-op/undo parity shipped: the shared visual-asset property reorder path now treats already-satisfied reorder requests as successful no-ops with zero affected objects, batch reorder responses now sum only real mutations, and focused native regressions now prove undo history aligns exactly with the count of real memo-property reorders so the full `test_visual_asset_editor` suite passes again.
- 2026-06-30: E3/#2967 FRX/LBX unsupported-metadata preservation evidence shipped: focused native visual-asset regressions now prove supported report/label edits such as `HPOS` and `EXPR` preserve unrelated unsupported direct and memo fields in the same `.frx/.frt` and `.lbx/.lbt` records through both the edit and undo chain, tightening safe round-trip evidence without changing machine-readable asset contracts.
- 2026-06-30: E3/#2966 FRX/LBX no-op property-write byte preservation shipped: focused native visual-asset regressions now prove unchanged report/label direct-field and memo-backed property edits short-circuit as no-ops without rewriting `.frx/.frt` or `.lbx/.lbt` bytes and without creating undo history, and the shared direct-field update path now uses field-aware no-op detection so report/label safe round-trip continuity matches existing SCX-style unchanged-write behavior.
- 2026-06-30: E3/#2965 deleted label undo-backed shell summary refresh parity shipped: focused shared designer smokes now prove a real deleted LBX selection with command undo available invokes the existing Studio host undo contract `--undo-mode command`, consumes the returned label snapshot, refreshes the visible shell details with deleted preview-bounds text from `reportLayout.deletedPreviewBounds*`, and preserves label identity plus deleted object and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2964 deleted report undo-backed shell summary refresh parity shipped: focused shared designer smokes now prove a real deleted FRX selection with command undo available invokes the existing Studio host undo contract `--undo-mode command`, consumes the returned snapshot, refreshes the visible shell details with deleted preview-bounds text from `reportLayout.deletedPreviewBounds*`, and preserves report identity plus deleted object and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2963 deleted label shell summary refresh after host-backed updates shipped: focused shared designer smokes now prove a real deleted LBX object-rooted property-grid edit in the shared VSIX/standalone asset editor consumes the returned snapshot, refreshes the visible shell details with deleted preview-bounds text from `reportLayout.deletedPreviewBounds*`, and preserves label identity plus deleted object and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2962 deleted report shell summary refresh after host-backed updates shipped: focused shared designer smokes now prove a real deleted FRX object-rooted property-grid edit in the shared VSIX/standalone asset editor consumes the returned snapshot, refreshes the visible shell details with deleted preview-bounds text from `reportLayout.deletedPreviewBounds*`, and preserves report identity plus deleted object and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2961 deleted report object property-grid host update parity shipped: focused shared designer smokes now prove a real deleted FRX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed snapshot as a report asset, and preserves report identity plus deleted object-rooted and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2960 deleted report section property-grid host update parity shipped: focused shared designer smokes now prove a real deleted FRX section-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant section field names such as `TOP`, consumes the refreshed snapshot as a report asset, and preserves report identity plus deleted section-rooted and deleted-scope continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: Localization/#2959 shared non-report designer property-label localization shipped: the shared VSIX/standalone `CopperfinDesignerSelection` path now routes remaining form/class-library, menu, project, and generic property-grid display labels through the existing localization catalogs instead of hard-coded English text, reusing existing invariant field names and update targets; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for those visible labels while preserving machine-readable property names such as `PROMPT`.
- 2026-06-30: E3/#2958 undo-backed label shell summary refresh parity shipped: focused shared designer smokes now prove a real unplaced LBX selection with command undo available invokes the existing Studio host undo contract `--undo-mode command`, consumes the returned label snapshot, refreshes the visible report/label shell summary with preview-bounds and unplaced-object context, and preserves label identity plus unplaced-object selection continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2957 label shell summary refresh after unplaced host-backed updates shipped: focused shared designer smokes now prove a real unplaced LBX object-rooted property-grid edit invokes the existing Studio host `--set-property` contract, consumes the returned label snapshot, refreshes the visible report/label shell summary with preview-bounds and unplaced-object context, and preserves label identity plus unplaced-object selection continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2956 undo-backed report shell summary refresh parity shipped: focused shared designer smokes now prove a real unplaced FRX selection with command undo available invokes the existing Studio host undo contract `--undo-mode command`, consumes the returned snapshot, refreshes the visible report shell summary with updated preview-bounds and unplaced-object context, and preserves unplaced-object selection continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2955 report/label shell summary refresh after host-backed updates shipped: the shared VSIX/standalone asset editor now refreshes its report/label details summary from the returned snapshot after successful host-backed property and undo updates, so preview-bounds and layout-summary context no longer stays stale when unplaced-object edits move the effective layout bounds; focused shared designer smokes now prove a real unplaced FRX object edit updates the visible shell summary with the returned preview-bounds and unplaced-object context without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2954 unplaced label object property-grid host update parity shipped: focused shared designer smokes now prove a real unplaced LBX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed label snapshot, and preserves label identity plus unplaced-object selection and unplaced-scope continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2953 unplaced report object property-grid host update parity shipped: focused shared designer smokes now prove a real unplaced FRX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed snapshot, and preserves unplaced-object selection plus unplaced-scope continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2952 deleted label section property-grid host update parity shipped: focused shared designer smokes now prove a real deleted LBX section-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant section field names such as `TOP`, consumes the refreshed snapshot as a label asset, and preserves label identity plus deleted section-rooted and deleted-scope continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2951 deleted label object property-grid host update parity shipped: focused shared designer smokes now prove a real deleted LBX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed snapshot as a label asset, and preserves label identity plus deleted object-rooted and deleted-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2950 shared label object property-grid host update parity shipped: focused shared designer smokes now prove a real LBX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed snapshot as a label asset, and preserves label identity plus object-rooted and containing-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2949 shared label section property-grid host update parity shipped: focused shared designer smokes now prove a real LBX section-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant section field names such as `TOP`, consumes the refreshed snapshot as a label asset, and preserves label identity plus section-rooted continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2948 shared report object property-grid host update parity shipped: focused shared designer smokes now prove a real FRX/LBX object-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant object field names such as `HPOS`, consumes the refreshed snapshot, and preserves both object-rooted selection and containing-section continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2947 shared report section property-grid host update parity shipped: focused shared designer smokes now prove a real FRX/LBX section-rooted property-grid edit in the shared VSIX/standalone asset editor invokes the existing Studio host `--set-property` contract with invariant section field names such as `TOP`, consumes the refreshed snapshot, and preserves section-rooted explorer/property-grid/surface continuity without changing host or snapshot machine-readable contracts.
- 2026-06-30: E3/#2946 shared report-drag batch host update shipped: the shared VSIX/standalone FRX/LBX asset-editor drag path now routes report and label object coordinate changes through one existing Studio host `--visual-object-update-batch` call instead of two separate single-property updates, preserving invariant `HPOS`/`VPOS` field names plus selection continuity without changing host/snapshot contract keys; focused shared designer smokes now prove one managed host invocation carries both coordinate updates and keeps section/object scope stable after refresh.
- 2026-06-30: E3/#2945 shared report-surface object drag parity evidence shipped: focused shared designer smokes now prove dragging live, deleted, and unplaced FRX/LBX objects on the shared WinForms surface emits the expected stable record identity plus invariant `HPOS`/`VPOS` coordinate updates while preserving the containing live/deleted section or unplaced-object scope highlight, without changing host or snapshot contracts.
- 2026-06-30: E3/#2944 shared report object layout/font edit-parity evidence shipped: focused shared designer smokes now prove live, deleted, and shared-label FRX/LBX object property-grid selections serialize `HPOS`, `VPOS`, `WIDTH`, `HEIGHT`, `FONTFACE`, `FONTSTYLE`, and `FONTSIZE` edits through the same invariant machine-readable update targets as `EXPR`, without changing host or snapshot contracts.
- 2026-06-30: E3/#2943 shared report object state-metadata exposure shipped: the shared FRX/LBX object property-grid path now exposes localized read-only live/deleted object state plus stable record identity for report and label object selections in both the VSIX and standalone Studio while preserving invariant editable update targets and host/snapshot contracts; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible object-state label, deleted/live state values, deleted-object edit parity, and deleted-object surface selection metadata.
- 2026-06-30: E3/#2942 deleted report section property-grid edit parity evidence shipped: focused shared designer smokes now prove deleted FRX/LBX section property-grid selections serialize `TOP`, `HEIGHT`, and `EXPR` edits through the same machine-readable shared update targets as live sections whenever deleted section grouping metadata is present, while preserving deleted-section record identity and deleted-state semantics without changing host or snapshot contracts.
- 2026-06-30: E3/#2941 shared report section object-count property exposure shipped: the shared FRX/LBX section property-grid path now exposes localized read-only section object-count metadata for live and deleted report and label sections in both the VSIX and standalone Studio while preserving invariant raw section object membership in managed section snapshots and host contracts; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible object-count label and that the managed section snapshot keeps the same machine-readable object membership counts.
- 2026-06-30: E3/#2940 shared report section-record property exposure shipped: the shared FRX/LBX section property-grid path now exposes localized read-only section record metadata for live and deleted report and label sections in both the VSIX and standalone Studio while preserving invariant raw section record indexes in managed section snapshots and host contracts; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible record label and that the managed section snapshot keeps the same machine-readable record indexes.
- 2026-06-30: E3/#2939 shared report grouping-expression provenance exposure shipped: the shared FRX/LBX section property-grid path now exposes read-only grouping expression field-index and memo-block metadata for grouped report and label sections in both the VSIX and standalone Studio while preserving invariant raw `groupingExpressionFieldIndex` and `groupingExpressionMemoBlockNumber` snapshot/host contract values; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the new provenance labels and that the managed section snapshot keeps the same machine-readable provenance fields.
- 2026-06-30: E3/#2938 shared report section-state property exposure shipped: the shared FRX/LBX section property-grid path now exposes localized live/deleted section state text for report and label sections in both the VSIX and standalone Studio while preserving invariant raw section deleted-state values in managed section snapshots and host contracts; focused designer smokes now prove live and deleted state routing for `es-419`, `pt-BR`, and `qps-ploc` and that the managed section snapshot keeps the same machine-readable deleted flag.
- 2026-06-30: E3/#2937 shared report grouping-index property exposure shipped: the shared FRX/LBX section property-grid path now exposes read-only grouping index and localized grouping nesting-depth metadata for grouped report and label sections in both the VSIX and standalone Studio while preserving invariant raw `groupingIndex` and `groupingNestingDepth` snapshot/host contract values; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the new visible grouping metadata and that the managed section snapshot keeps the same machine-readable grouping fields.
- 2026-06-30: E3/#2936 shared report grouping-partner property exposure shipped: the shared FRX/LBX section property-grid path now exposes read-only grouping partner section id, partner record, and localized partner state metadata for grouped report and label sections in both the VSIX and standalone Studio while preserving invariant raw `groupPartnerSectionId`, `groupPartnerRecordIndex`, and `groupPartnerDeleted` snapshot/host contract values; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the new visible partner metadata and that the managed section snapshot keeps the same machine-readable partner fields.
- 2026-06-30: E3/#2935 shared report grouping-role property-value localization shipped: the shared FRX/LBX section property-grid path now renders localized visible grouping-role values such as header and footer for report and label sections in both the VSIX and standalone Studio while preserving invariant raw `groupRole` snapshot/host contract values; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible grouping-role text and that the managed section snapshot keeps the same machine-readable grouping role.
- 2026-06-30: E3/#2934 shared detail-header band-kind localization shipped: the shared FRX/LBX design surface and section property-grid path now localize `detail_header` band-kind text for report and label sections in both the VSIX and standalone Studio instead of falling back to raw underscore-separated text, while preserving invariant raw `bandKind` values in host JSON and managed section snapshot contracts; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for visible detail-header text on both shared surfaces.
- 2026-06-30: E3/#2933 shared report section band-kind property-value localization shipped: the shared FRX/LBX section property-grid path now renders localized visible band-kind values for known report/label sections such as detail, page header, group footer, and summary for both the VSIX and standalone Studio while preserving invariant raw `bandKind` values in host JSON and managed section snapshot contracts; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible property-grid value and that the managed section snapshot keeps the same machine-readable band kind.
- 2026-06-30: E3/#2932 shared report object-kind display localization shipped: the shared FRX/LBX object list now renders localized visible report/label object-kind text such as field, label, line, rectangle, group, picture, variable, and fallback object names for both the VSIX and standalone Studio while preserving invariant raw `subtitle`/`objectKind` snapshot and host JSON contract values; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the shared visible type text and that the snapshot subtitle remains machine-readable.
- 2026-06-30: E3/#2931 shared report object fallback-title localization shipped: the shared FRX/LBX object list and design surface now route untitled report/label object fallback titles through the localization catalog instead of hard-coded English `Record {n}` text, while preserving the same underlying record indexes and host/snapshot contracts for both the VSIX and standalone Studio; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the visible fallback titles in the shared editor/surface path.
- 2026-06-30: E3/#2930 shared report band-kind display localization shipped: the shared FRX/LBX WinForms design surface now renders localized visible band-kind text for known report/label sections such as page headers, details, group headers/footers, and summaries while preserving invariant raw `bandKind` values in host JSON and managed snapshot contracts for both the VSIX and standalone Studio; focused designer smokes now prove `en-US`, `es-419`, `pt-BR`, and `qps-ploc` routing for the visible band-kind text on the shared surface.
- 2026-06-30: E3/#2929 shared report property-status localization shipped: the shared FRX/LBX editor now resolves property apply/update status text from the active localized report/label object or section selection instead of leaking raw target ids like `WIDTH` or `EXPR` when a localized selection is present, while preserving invariant update targets and host contracts for both the VSIX and standalone Studio; focused designer smokes now prove Spanish, Portuguese, and pseudo-localized report object/section status text and that the shared update path still serializes the same machine-readable property names.
- 2026-06-30: E3/#2928 shared report object property-label localization shipped: the shared FRX/LBX object property-grid path now routes report/label object display labels through the shared localization catalog for both the VSIX and standalone Studio while preserving machine-readable property target names like `OBJTYPE`, `OBJCODE`, `EXPR`, `HPOS`, and `FONTSIZE`; focused designer smokes now prove `es-419`, `pt-BR`, and `qps-ploc` routing for the localized object labels and that the editable update path still serializes the same invariant target names.
- 2026-06-30: E3/#2927 shared report object containing-scope highlight shipped: the shared WinForms FRX/LBX surface now keeps a selected report/label object's containing live section, deleted section, or unplaced-object tray highlighted instead of clearing section-scope context, while preserving object-rooted selection for both the VSIX and standalone Studio without changing host JSON or managed contract names; focused designer smokes now prove live, deleted, and unplaced object clicks preserve the expected containing-scope highlight state on the shared surface.
- 2026-06-30: E3/#2926 shared report object-click scope alignment shipped: the shared WinForms FRX/LBX editor now re-scopes the explorer to an object's containing live section, deleted section, or unplaced-object tray before syncing object-list and property-grid selection when the shared design surface selects a report/label object, keeping VSIX and standalone Studio behavior aligned without changing host JSON or managed contract names; focused designer smokes now prove live, deleted, and unplaced object clicks route through the shared surface into the expected section scope and object selection state.
- 2026-06-30: E3/#2925 shared report-surface scope selection shipped: the shared WinForms FRX/LBX design surface now supports direct selection of live sections, deleted sections, and the unplaced-object tray, driving the existing explorer/object-list/property-grid section scope for both the VSIX and standalone Studio while keeping host JSON and managed contract names unchanged; focused designer smokes now prove live/deleted section clicks and unplaced-tray clicks route through the shared surface into the expected editor selection state.
- 2026-06-30: E3/#2924 shared deleted-section design-surface pickup shipped: the shared WinForms FRX/LBX design surface now loads `reportLayout.deletedSections` alongside live sections, renders deleted bands with distinct deleted styling, and labels deleted section headers through the existing localized deleted-section catalog path for both the VSIX and standalone Studio without changing host JSON or managed contract names; focused designer smokes now prove live+deleted section loading, deleted visual flags, deleted header-title routing, and visible deleted-section rendering in the shared surface path.
- 2026-06-30: E3/#2923 shared report preview-bounds pickup shipped: the managed FRX/LBX snapshot model now carries live and deleted preview-bounds metadata, and the shared WinForms shell appends localized preview-bounds summaries to report/label details for both the VSIX and standalone Studio without changing host JSON or managed contract names; focused designer smokes now prove live/deleted preview-bounds text flows through the shared model and catalog routing for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`.
- 2026-06-30: E3/#2922 shared deleted-section explorer pickup shipped: the managed FRX/LBX snapshot model now carries `reportLayout.deletedSections`, and the shared WinForms explorer surfaces those deleted section rows with localized deleted markers, deleted-section object filtering, and section-rooted property-grid selection for both the VSIX and standalone Studio without changing host JSON or managed contract names; focused designer smokes now prove deleted section row exposure, deleted-section object scoping, and `en-US`/`es-419`/`pt-BR`/`qps-ploc` routing for the new explorer text.
- 2026-06-30: E3/#2921 shared report section expression edit parity shipped: the shared WinForms FRX/LBX section property-grid selection now treats section expression metadata as editable `EXPR` state instead of read-only context whenever the managed section snapshot carries expression provenance, routing edits through the existing property-update pathway for both the VSIX and standalone Studio without changing host JSON or managed snapshot contract keys; focused designer smokes now prove section-rooted expression update serialization alongside the existing `TOP`/`HEIGHT` section-edit path.
- 2026-06-30: E3/#2920 shared report refresh-selection continuity shipped: the shared WinForms FRX/LBX shell now preserves section-scoped explorer selection across section-list rebuilds during shared refresh paths and only reselects the design-surface/object row when the prior selection was actually object-rooted, keeping section-rooted property-grid editing intact through update/undo refreshes for both the VSIX and standalone Studio without changing host JSON or managed snapshot contract keys; focused designer smokes now prove section-rooted refresh continuity and that object-rooted reselection still survives the same refresh path.
- 2026-06-30: E3/#2919 shared report section property-grid selection parity shipped: the shared WinForms FRX/LBX shell now turns a selected section row into a section-rooted property-grid selection with localized section labels and editable `TOP`/`HEIGHT` fields routed through the existing property-update pathway for both the VSIX and standalone Studio, without changing host JSON or managed snapshot contract keys; focused designer smokes now prove section-rooted selection, update serialization for `TOP`/`HEIGHT`, and `en-US`/`es-419`/`pt-BR`/`qps-ploc` routing for the new section field labels.
- 2026-06-30: E3/#2918 shared report explorer section-scoping parity shipped: the shared WinForms FRX/LBX explorer now filters object rows to the selected section instead of always showing every snapshot object, and it adds a localized unplaced-objects row that scopes the object list to `reportLayout.unplacedObjects` for both the VSIX and standalone Studio without changing host JSON or managed snapshot contract keys; focused designer smokes now prove section-scoped filtering, unplaced-object filtering, and `en-US`/`es-419`/`pt-BR`/`qps-ploc` routing for the new explorer row.
- 2026-06-30: E3/#2917 shared report design-surface unplaced/deleted context parity shipped: the shared WinForms report/label design surface now renders `reportLayout.unplacedObjects` in a localized unplaced-objects tray and surfaces per-section deleted-object counts in localized section headers for both the VSIX and standalone Studio without changing host JSON or managed snapshot contracts; focused designer smokes now prove placed+unplaced object loading, deleted-object count surfacing, and `en-US`/`es-419`/`pt-BR`/`qps-ploc` title routing through the shared catalog.
- 2026-06-30: E3/#2916 shared report-shell grouping/layout summary parity shipped: the shared WinForms report/label shell now routes file metadata through the existing localization catalog, appends a localized layout summary with section/grouping/settings/unplaced-object counts after FRX/LBX snapshots load, and surfaces grouped section expressions in explorer titles for both the VSIX and standalone Studio without changing host JSON or designer-model contracts; focused designer smokes now cover Spanish/Portuguese layout-summary text, grouped-section explorer titles, and the English real-asset shell-summary surface when Windows VFP sample assets are present.
- 2026-06-30: E3/#2915 deleted report visual-property copy shard extraction shipped: the focused `test_studio_host_deleted_report_visual_property_copies` executable now compiles its FRX/LBX deleted visual-property copy and copy-batch stable-selection smoke from the new `tests/test_studio_host_json_deleted_report_visual_property_copies.cpp` translation unit instead of recompiling the unrelated clear/move/rejection coverage still owned by `tests/test_studio_host_json_report_visual_properties.cpp`; the adjacent deleted report visual-property clears, moves, and rejections executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2914 detail-header/footer object lifecycle broad-host shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_objects_dhf_lifecycle.cpp`, with the detail-header/footer object delete/restore, duplicate, rename, and reorder stable-selection smoke now owned by the focused `test_studio_host_detail_header_footer_object_lifecycle` executable; the adjacent focused object geometry, expressions, and layout-actions executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2913 detail-header/footer section lifecycle broad-host shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_sections_dhf_lifecycle.cpp`, with the detail-header/footer section delete/restore, duplicate, rename, and reorder stable-selection smoke now owned by the focused `test_studio_host_detail_header_footer_section_lifecycle` executable; the adjacent focused section geometry, expressions, and preview-bounds executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2912 detail-header/footer object broad-host shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_objects_dhf_expressions_and_exposure.cpp`, with the detail-header/footer object containment plus stable/deleted expression smoke now owned by the focused `test_studio_host_detail_header_footer_object_expressions` executable and the shared object fixtures moved into `test_studio_host_json_objects_dhf_shared_fixtures.cpp`; the adjacent focused geometry and layout-actions executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2911 detail-header/footer section broad-host shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_sections_dhf_expressions_and_exposure.cpp`, with the remaining non-expression detail-header/footer section kind and stable height/top live/deleted smoke now owned by the focused `test_studio_host_detail_header_footer_section_geometry` executable and the shared section fixtures moved into `test_studio_host_json_sections_dhf_shared_fixtures.cpp`; the adjacent focused expression and preview-bounds executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2910 report/label TAG record-mutation evidence shipped: the new focused `test_studio_host_report_sort_settings_record` host regression now proves FRX/LBX record-selected live/deleted root `TAG` sort settings update and clear through the shared property-edit path, and that a fresh host reopen preserves the expected `reportLayout.settings`, `deletedSettings`, and `selectedReportSettings` counts/provenance without changing host JSON keys or production behavior.
- 2026-06-30: E3/#2909 report/label TAG stable-mutation evidence shipped: the new focused `test_studio_host_report_sort_settings_stable` host regression now proves FRX/LBX stable-selected live/deleted root `TAG` sort settings update and clear through the shared property-edit path, and that a fresh host reopen preserves the expected `reportLayout.settings`, `deletedSettings`, and `selectedReportSettings` counts/provenance without changing host JSON keys or production behavior.
- 2026-06-30: E3/#2908 report/label TAG sort-metadata exposure shipped: `build_report_layout(...)` now carries nonblank FRX/LBX root `TAG` fields through the existing report/label settings snapshot contract for live and deleted roots, so `reportLayout.settings`, `deletedSettings`, and selected-settings host JSON now expose sort-setting provenance without changing machine-readable keys; focused host regressions cover FRX/LBX selected-settings parity and the new native `test_report_layout_sort_settings` target locks down live, deleted, and blank-`TAG` behavior.
- 2026-06-30: E3/#2895 redundant detail-header/footer object layout-actions broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_objects_dhf_layout_actions.cpp`, leaving the detail-header/footer object align/resize/snap/nudge/distribute coverage owned by the focused `test_studio_host_detail_header_footer_object_layout_actions` executable; the broad suite plus the focused DHF layout-actions executable remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2894 redundant report direct-setting validation broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_settings_direct_field_validation.cpp`, leaving the blank/mixed-invalid/trimmed/fractional/oversized/dot-leading report direct-setting validation coverage owned by the focused `test_studio_host_report_direct_setting_fields` executable; the broad suite plus the focused direct-setting executable remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2893 redundant report geometry-dimensions broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_geometry_dimensions.cpp`, leaving the report geometry-default and detail-header/footer object-geometry coverage owned by focused executables including `test_studio_host_report_geometry_defaults` and `test_studio_host_detail_header_footer_object_geometry`; the broad suite plus those focused geometry executables remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2892 redundant report settings-memo broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_settings_memo_parsing.cpp`, leaving the unresolved/mixed/invalid/fractional/blank/malformed/CR-only/mixed-case report/label settings-memo coverage owned by the focused `test_studio_host_report_settings_memo_parsing` executable; the broad suite plus the focused settings-memo executable remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2891 redundant report unresolved-memo broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_report_memo_and_placeholders.cpp`, leaving the root unresolved report/label memo placeholder coverage owned by the focused `test_studio_host_report_unresolved_memo_placeholders` executable while the broad suite still keeps `COPPERFIN_REPORT_UNRESOLVED_MEMO_SKIP_HOST_SMOKE` to exclude the adjacent unresolved section/object host-smoke families that remain outside the broad target; the broad suite plus the focused unresolved-memo executable remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2890 redundant report layout-classification broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_report_layout_classifications.cpp`, dropping the obsolete report/label layout-classification host-smoke shard and its broad-target skip macros now that the focused `test_studio_host_report_layout_classifications` executable fully owns the invalid/fractional/trimmed/missing-schema/unknown-band and invalid direct page-setup coverage; the broad suite plus the focused classification executable remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2889 redundant deleted report visual-property broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_report_visual_properties.cpp`, dropping the obsolete deleted report/label visual-property clear/copy/move/rejection host-smoke shard and its broad-target skip macros now that the focused `test_studio_host_deleted_report_visual_property_*` executables fully own that coverage; the broad suite plus all focused deleted report visual-property tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2888 redundant report visual-batches broad-suite shard retirement shipped: the broad `test_studio_host_json` target no longer builds or runs `tests/test_studio_host_json_objects_visual_batches.cpp`, with the obsolete report/label subtree-duplicate and visual-object update/rename/duplicate/reorder batch host-smoke entry points, declarations, and target-local skip macros removed now that the focused subtree/batch executables fully own that coverage; the broad suite plus all focused subtree-duplicate, update-batch, rename-batch, duplicate-batch, and reorder-batch tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2887 redundant report reorder-batch broad-suite retirement shipped: the broad `test_studio_host_json` target now skips the live/deleted report visual-object reorder-batch host-smoke family in `tests/test_studio_host_json_objects_visual_batches.cpp`, leaving that success and missing-target rollback/error coverage owned by the focused reorder-batch executables while the broad suite and all focused reorder-batch tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2886 redundant report duplicate-batch broad-suite retirement shipped: the broad `test_studio_host_json` target now skips the live/deleted report visual-object duplicate-batch host-smoke family in `tests/test_studio_host_json_objects_visual_batches.cpp`, leaving that success and collision/rollback coverage owned by the focused duplicate-batch executables while the broad suite and all focused duplicate-batch tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2885 redundant report rename-batch broad-suite retirement shipped: the broad `test_studio_host_json` target now skips the live/deleted report visual-object rename-batch host-smoke family in `tests/test_studio_host_json_objects_visual_batches.cpp`, leaving that success and collision/rollback coverage owned by the focused rename-batch executables while the broad suite and all focused rename-batch tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2884 redundant report update-batch broad-suite retirement shipped: the broad `test_studio_host_json` target now skips the live/deleted report visual-object update-batch host-smoke family in `tests/test_studio_host_json_objects_visual_batches.cpp`, leaving that success and rollback/error coverage owned by the focused update-batch executables while the broad suite and all focused update-batch tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2883 redundant report subtree-duplicate broad-suite retirement shipped: the broad `test_studio_host_json` target now skips the report/label subtree-duplicate host-smoke family in `tests/test_studio_host_json_objects_visual_batches.cpp`, leaving that live/deleted success, missing-selector, and collision coverage owned by the existing focused subtree-duplicate executables while the broad suite and all focused subtree-duplicate tests remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2882 report layout-classification helper gating shipped: `tests/test_studio_host_json_report_layout_classifications.cpp` now gates its synthetic classification fixture builders and preview helpers behind an explicit target definition so the broad `test_studio_host_json` target stops compiling that unused helper section while the focused `test_studio_host_report_layout_classifications` executable keeps the full coverage green, without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2881 report preview-helper target gating shipped: `tests/test_studio_host_json_geometry_preview_bounds.cpp` now gates the unresolved/missing preview-bounds helper family behind explicit target definitions so the broad `test_studio_host_json` target and the focused width/height/detail preview-bounds shards stop compiling unused memo/schema helper code, while the focused unresolved-memo, schema-fallback, and report-layout-diagnostics executables still opt into the shared helper family and all affected focused and broad host suites remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2880 remaining report diagnostics helper gating shipped: `tests/test_studio_host_json_objects_diagnostics_and_misc.cpp` now gates the last two shared helper families by target so the broad `test_studio_host_json` target and the focused `test_studio_host_report_deleted_states` shard stop compiling the unused extended-object-kind helper, `test_studio_host_report_layout_diagnostics` stops compiling the unused layout-subtree helper, and the deleted-states shard now also skips the unrelated layout-diagnostics test body while all focused report and broad host suites remain green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2879 orphaned report object fixture-builder pruning shipped: `tests/test_studio_host_json_objects_diagnostics_and_misc.cpp` no longer carries the dead FRX/LBX summary, group-header, group-footer, title, page-footer, column-header, and column-footer object fixture builders that were left behind after the host-test refactors, leaving those stable/deleted object fixtures owned only by their focused selected-object executables while the report layout-diagnostics, report deleted-states, selected-object, and broad `test_studio_host_json` suites stay green without any host JSON or production-behavior changes.
- 2026-06-30: E3/#2878 report object fallback source extraction shipped: the new focused `test_studio_host_json_report_object_fallbacks` translation unit now carries the FRX/LBX missing-object `OBJCODE`, unresolved deleted-object memo, unresolved unplaced-object memo, missing-object `EXPR`, and missing-object title host JSON coverage that previously compiled through `test_studio_host_json_objects_diagnostics_and_misc.cpp`, keeping the existing `test_studio_host_report_schema_fallbacks` and `test_studio_host_report_unresolved_memo_placeholders` executables green while the broad `test_studio_host_json` target plus the unrelated report deleted-state/layout-diagnostics shards now skip those fallback helper builders after the recent build-time refactors; the adjacent `#1737` unresolved-section memo fixture also restores stable `UNIQUEID` values so the focused memo-placeholder regression continues to prove the current shared host JSON contract.
- 2026-06-30: E3/#2877 report layout diagnostics extraction shipped: the new focused `test_studio_host_report_layout_diagnostics` target now carries the FRX/LBX report layout provenance and extended object-kind host JSON coverage that previously lived in `test_studio_host_json_objects_diagnostics_and_misc.cpp`, preserving current preview accounting, setting/object/section provenance, object-kind summaries, selected-object exposure, and report/label parity while shrinking that broad report diagnostics shard after the recent build-time refactors.
- 2026-06-30: E3/#2876 report deleted-state extraction shipped: the new focused `test_studio_host_report_deleted_states` target now carries the FRX/LBX stable-selection settings+section deleted-states, object deleted-states, subtree deleted-state, and mixed deleted-states host JSON coverage that previously lived in `test_studio_host_json_objects_diagnostics_and_misc.cpp`, preserving current preview accounting, deleted/live counts, selected-object/section/settings nullability, rollback behavior, stable-selector routing, subtree semantics, and report/label parity while shrinking that broad report diagnostics shard after the recent build-time refactors.
- 2026-06-30: E3/#2875 deleted report visual-property move extraction shipped: the new focused `test_studio_host_deleted_report_visual_property_moves` target now carries the FRX/LBX deleted visual-property move and move-batch host JSON coverage that previously lived in `test_studio_host_json_report_visual_properties.cpp`, preserving current undo metadata, deleted/live object counts, preview accounting, containing-section metadata, geometry-move behavior, missing-selector failures, rollback semantics, and report/label parity while shrinking that report/label host shard after the recent build-time refactors.
- 2026-06-30: E3/#2874 deleted report visual-property copy extraction shipped: the new focused `test_studio_host_deleted_report_visual_property_copies` target now carries the FRX/LBX deleted visual-property copy and copy-batch host JSON coverage that previously lived in `test_studio_host_json_report_visual_properties.cpp`, preserving current undo metadata, deleted/live object counts, preview accounting, containing-section metadata, geometry-copy behavior, missing-selector failures, rollback semantics, and report/label parity while shrinking that report/label host shard after the recent build-time refactors.
- 2026-06-30: E3/#2873 deleted report visual-property clear extraction shipped: the new focused `test_studio_host_deleted_report_visual_property_clears` target now carries the FRX/LBX deleted visual-property clear and clear-batch host JSON coverage that previously lived in `test_studio_host_json_report_visual_properties.cpp`, preserving current undo metadata, deleted/live object counts, preview accounting, containing-section metadata, geometry-clear behavior, missing-selector failures, and report/label parity while shrinking that report/label host shard after the recent build-time refactors.
- 2026-06-30: E3/#2872 deleted report visual-property rejection extraction shipped: the new focused `test_studio_host_deleted_report_visual_property_rejections` target now carries the FRX/LBX deleted visual-property rename, rename-batch, reorder, and reorder-batch rejection host JSON coverage that previously lived in `test_studio_host_json_report_visual_properties.cpp`, preserving current direct-field rejection semantics, missing-selector failures, deleted/live object counts, preview accounting, containing-section metadata, and report/label parity while shrinking that report/label host shard after the recent build-time refactors.
- 2026-06-30: E3/#2871 detail-header/footer preview-bounds extraction shipped: the new focused `test_studio_host_detail_header_footer_section_preview_bounds` target now carries the FRX/LBX detail-header/footer section preview-bounds stable-update, deleted-section preview-bounds stable-update, and section delete/restore preview-bounds host JSON coverage that previously lived in `test_studio_host_json_geometry_preview_bounds.cpp`, preserving current preview accounting, deleted/live section metadata, object containment inside deleted/restored sections, stable-selection routing, and report/label parity while shrinking the broad preview-bounds shard after the recent build-time refactors.
- 2026-06-30: E3/#2870 report height/top preview-bounds extraction shipped: the new focused `test_studio_host_report_height_top_preview_bounds` target now carries the FRX/LBX report-layout object height and top preview-bounds update/clear host JSON coverage that previously lived in `test_studio_host_json_geometry_preview_bounds.cpp`, preserving current preview accounting, selected-object containing-section and unplaced-object metadata, field persistence/clear semantics, and report/label parity while shrinking the broad preview-bounds shard after the recent build-time refactors.
- 2026-06-30: E3/#2869 report width/left preview-bounds extraction shipped: the new focused `test_studio_host_report_width_left_preview_bounds` target now carries the FRX/LBX report-layout object width and left preview-bounds update/clear host JSON coverage that previously lived in `test_studio_host_json_geometry_preview_bounds.cpp`, preserving current preview accounting, selected-object containing-section metadata, field persistence/clear semantics, and report/label parity while shrinking the broad preview-bounds shard after the recent build-time refactors.
- 2026-06-30: E3/#2868 report geometry defaulting extraction shipped: the new focused `test_studio_host_report_geometry_defaults` target now carries the FRX/LBX negative-dimension clamping, fractional-geometry truncation, and missing-geometry defaulting host JSON coverage that previously lived in `test_studio_host_json_geometry_dimensions.cpp`, preserving current preview accounting, field-provenance behavior, and report/label parity while shrinking the broad geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2861 report schema-fallback evidence shipped: the new focused `test_studio_host_report_schema_fallbacks` host regression now proves FRX/LBX missing section `OBJCODE`, missing section `EXPR`, missing section geometry, missing object `OBJCODE`, missing object `EXPR`, and missing object title fallback flows preserve stable `UNIQUEID`-backed section ids, deleted placed-object containing-section metadata, zero-geometry preview accounting, and report/label parity without changing production behavior; the extracted schema-fallback calls are retired from the broad `test_studio_host_json` driver, and stale null-section/id-synthesis expectations were corrected to the current shared host JSON contract.
- 2026-06-30: E3/#2867 edited object delete-geometry extraction shipped: the new focused `test_studio_host_edited_object_delete_geometry` target now carries the FRX/LBX record-selected edited placed-object and edited unplaced-object delete host JSON coverage that previously lived in `test_studio_host_json_geometry_dimensions.cpp`, preserving current preview accounting, deleted-object geometry metadata, containing-section behavior, and report/label parity while shrinking the broad `test_studio_host_json` geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2866 edited deleted object restore-geometry extraction shipped: the new focused `test_studio_host_deleted_object_restore_geometry` target now carries the FRX/LBX record-selected and stable-selected edited deleted object restore host JSON coverage that previously lived in `test_studio_host_json_geometry_dimensions.cpp`, preserving current restored geometry fields, preview accounting, containing-section metadata, and report/label parity while shrinking the broad `test_studio_host_json` geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2865 deleted object height/top geometry extraction shipped: the new focused `test_studio_host_deleted_object_height_top_geometry` target now carries the deleted FRX/LBX object height/top update and clear host JSON coverage that previously lived in `test_studio_host_json_geometry_dimensions.cpp`, preserving the current deleted-object containing-section and geometry contract while shrinking the broad `test_studio_host_json` geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2864 deleted object width/left geometry extraction shipped: the new focused `test_studio_host_deleted_object_width_left_geometry` target now carries the deleted FRX/LBX object width/left update and clear host JSON coverage that previously lived in `test_studio_host_json_geometry_dimensions.cpp`, preserving the current deleted-object containing-section and geometry contract while shrinking the broad `test_studio_host_json` geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2860 unresolved report memo placeholder evidence shipped: the new focused `test_studio_host_report_unresolved_memo_placeholders` host regression now proves FRX/LBX root memo placeholder suppression, section memo placeholder suppression, deleted placed-object memo placeholder suppression, unplaced object memo placeholder suppression, and geometry memo defaulting preserve memo-leak suppression, preview-bounds fallback behavior, selected section/object metadata, and report/label parity without changing production behavior; the extracted unresolved memo/defaulting calls are retired from the broad `test_studio_host_json` driver, and the deleted placed-object unresolved-memo contract now explicitly expects containing detail-section metadata instead of `null`.
- 2026-06-30: E3/#2859 report settings diagnostics evidence shipped: the new focused `test_studio_host_report_settings_diagnostics` host regression now proves FRX/LBX record-selected settings delete routing, missing root `OBJCODE` schema handling, duplicate setting precedence, invalid-first duplicate precedence, and missing root `EXPR` schema handling preserve selected-settings provenance, empty preview-bounds behavior, duplicate-order semantics, and report/label parity without changing production behavior; the extracted settings diagnostics calls are retired from the broad `test_studio_host_json` driver and the duplicate precedence cases are covered again in focused regression.
- 2026-06-30: E3/#2858 report settings memo parsing evidence shipped: the new focused `test_studio_host_report_settings_memo_parsing` host regression now proves FRX/LBX unresolved direct-setting memo placeholders, mixed direct-setting memo placeholders, invalid memo values, fractional memo values, blank memo values, malformed memo lines, CR-only memo lines, and mixed-case memo names preserve selected-settings provenance, memo source-line metadata, empty preview-bounds behavior, and report/label parity without changing production behavior; the extracted settings memo parsing calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2857 report direct-setting field evidence shipped: the new focused `test_studio_host_report_direct_setting_fields` host regression now proves FRX/LBX invalid direct column-setup, invalid direct margin/grid, blank, mixed-invalid, trimmed, fractional, oversized, and dot-leading direct-setting field flows preserve selected-settings provenance, page/column-setup availability flags, empty preview-bounds behavior, and report/label parity without changing production behavior; the extracted direct-setting field calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2856 report-layout classification evidence shipped: the new focused `test_studio_host_report_layout_classifications` host regression now proves FRX/LBX invalid, dot-leading, negative, unsupported, fractional, trimmed, missing-schema, unknown-band, and invalid direct page-setup classification flows preserve current preview accounting, raw-settings exposure, and `UNIQUEID`-backed unknown-band section ids when `bandKind` falls back to `other`, without changing production behavior; the extracted report-layout classification calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2855 detail-header/footer object layout-action evidence shipped: the new focused `test_studio_host_detail_header_footer_object_layout_actions` host regression now proves FRX/LBX detail-header/detail-footer object align, resize, snap, nudge, horizontal distribute, and vertical distribute flows across live and deleted selections preserve stable-selection routing, containing-section metadata, preview bounds, and deleted-state behavior without changing production behavior; the extracted detail-header/detail-footer object layout-action calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2854 detail-header/footer object-geometry evidence shipped: the new focused `test_studio_host_detail_header_footer_object_geometry` host regression now proves FRX/LBX detail-header/detail-footer object geometry update, clear, deleted-object mutation, and deleted-object preview/accounting flows preserve stable-selection routing, containing-section metadata, preview bounds, and deleted-state behavior without changing production behavior; the extracted detail-header/detail-footer object-geometry calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2853 detail-header/footer section-expression evidence shipped: the new focused `test_studio_host_detail_header_footer_section_expressions` host regression now proves FRX/LBX detail-header/detail-footer section expression update, clear, stable-selection exposure, deleted-section mutation, and deleted-section exposure flows preserve section selection metadata, preview accounting, memo-field provenance, and no-partial-mutation behavior without changing production behavior; the extracted detail-header/detail-footer section-expression calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2852 remaining object-lifecycle evidence shipped: the new focused `test_studio_host_remaining_object_lifecycle` host regression now proves SCX host JSON reparent, reorder, group, delete-mark, and ungroup flows preserve stable-selector routing, parser/runtime failure payloads, and no-partial-mutation behavior without changing production behavior; the extracted remaining object-lifecycle calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2851 object action lifecycle evidence shipped: the new focused `test_studio_host_object_action_lifecycle` host regression now proves SCX host JSON subtree duplicate, object delete, object restore, object duplicate, and object rename flows preserve stable-selector routing, replacement-identity validation, parser/runtime failure payloads, and no-partial-mutation behavior without changing production behavior; the extracted object-action lifecycle calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2850 stable-selector mutation lifecycle evidence shipped: the new focused `test_studio_host_stable_selector_mutation` host regression now proves SCX host JSON stable-selector property set, property rename, deleted-states batch, and subtree deleted-state flows preserve record/object-name/`UNIQUEID` routing, rollback-on-failure behavior, parser/runtime failure payloads, and no-partial-mutation guarantees without changing production behavior; the extracted stable-selector mutation calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2849 toolbox-create lifecycle evidence shipped: the new focused `test_studio_host_toolbox_create_lifecycle` host regression now proves SCX host JSON `toolbox-create`, `selection-toolbox-create`, `toolbox-create-batch`, and `selection-toolbox-create-batch` flows preserve form/report/label context routing, created-object summaries, caller field persistence, parser/runtime failure payloads, and no-partial-mutation behavior without changing production behavior; the extracted toolbox-create lifecycle calls are retired from the broad `test_studio_host_json` driver.
- 2026-06-30: E3/#2848 live-edited geometry-action evidence shipped: the new focused `test_studio_host_geometry_live_edit_actions` host regression now proves FRX/LBX live-edited nudge, align, resize, snap, restore, distribute, reorder, duplicate, and rename flows preserve preview accounting, selected-object metadata, record/stable selector routing, and unplaced-versus-section context without changing production behavior; `tests/test_studio_host_json_geometry_live_edit_actions.cpp` is retired from the broad host target.
- 2026-06-30: E3/#2847 stable-selector layout-action evidence shipped: the new focused `test_studio_host_layout_actions` host regression now proves SCX stable-selector align, resize, distribute, snap, nudge, and dynamic-alignment flows preserve current success behavior, selector validation, ambiguity handling, and no-mutation-on-failure guarantees without changing production behavior; `tests/test_studio_host_json_layout_actions.cpp` is retired from the broad host target.
- 2026-06-30: E3/#2846 remaining grouping-expression exposure evidence shipped: the new focused `test_studio_host_report_grouping_remaining_exposure` host regression now proves FRX/LBX record-selected group-header exposure plus stable-selected live/deleted group-footer and deleted group-header exposure preserve current `UNIQUEID`-backed section ids, live/deleted preview accounting, grouping expression metadata, and host JSON field names without changing production behavior; `tests/test_studio_host_json_report_grouping_expressions.cpp` is retired from the broad host target.
- 2026-06-29: E3/#2845 deleted grouping-mutation evidence shipped: the new focused `test_studio_host_report_grouping_deleted_mutation` host regression now proves FRX/LBX record-selected deleted group-header/group-footer expression update and clear flows preserve the current synthesized deleted-section ids when `UNIQUEID` is absent, deleted/live preview accounting, memo-field persistence, and host JSON field names without changing production behavior; the deleted mutation shard is now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2844 deleted grouping-exposure evidence shipped: the new focused `test_studio_host_report_grouping_deleted_exposure` host regression now proves FRX/LBX record-selected deleted group-header/group-footer exposure preserves the current synthesized deleted-section ids when `UNIQUEID` is absent, deleted/live preview accounting, grouping expression metadata, and host JSON field names without changing production behavior; the deleted exposure shard is now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2843 record-selected grouping-mutation evidence shipped: the new focused `test_studio_host_report_grouping_record_mutation` host regression now proves FRX/LBX record-selected live group-header/group-footer expression update and clear flows preserve the current synthesized section ids when `UNIQUEID` is absent, preview accounting, memo-field persistence, and host JSON field names without changing production behavior; the record-selected non-deleted mutation shard is now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2842 stable grouping-mutation evidence shipped: the new focused `test_studio_host_report_grouping_stable_mutation` host regression now proves FRX/LBX stable-selected live and deleted group-header/group-footer expression update and clear flows preserve the current stable-`UNIQUEID` section ids, deleted/live preview accounting, memo-field persistence, and host JSON field names without changing production behavior; the stable non-nested mutation shard is now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2841 nested grouping-mutation evidence shipped: the new focused `test_studio_host_report_grouping_nested_mutation` host regression now proves FRX/LBX record-selected and stable-selected nested group-section expression update/clear flows, including deleted nested footer mutations, preserve the current stable-`UNIQUEID` nested section ids, deleted/live preview accounting, memo-field persistence, and host JSON field names without changing production behavior; the top nested mutation shard is now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2840 grouping-exposure evidence shipped: the new focused `test_studio_host_report_grouping_exposure` host regression now proves FRX/LBX stable selected group-header exposure, stable nested grouping ordering exposure, grouping layout summaries, nested mixed-state grouping summaries, and resolved blank group-footer grouping-expression metadata preserve the current stable-`UNIQUEID` section/grouping ids and host JSON field names without changing production behavior; the top-of-file read-only grouping smokes are now out of `tests/test_studio_host_json_report_grouping_expressions.cpp`.
- 2026-06-29: E3/#2839 deep section/settings selector evidence shipped: the new focused `test_studio_host_report_selection_deep_section_settings` host regression now proves FRX/LBX deep stable section/settings selection and deep ambiguous stable section/settings no-selection behavior preserve current selection-kind routing, deep record metadata, and live/deleted summary payloads without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2838 deep selector-resolution evidence shipped: the new focused `test_studio_host_report_selection_deep` host regression now proves FRX/LBX deep record-selected object/section/settings routing, deep stable object selection, deep ambiguous stable object no-selection behavior, and deep live/deleted ambiguous object/section/settings no-selection behavior preserve current selection-kind routing, deep record metadata, and live/deleted summary payloads without changing host JSON field names or production behavior; the old `test_studio_host_json_report_selection_resolution.cpp` shard is retired from the broad host target.
- 2026-06-29: E3/#2837 padded selector-resolution evidence shipped: the new focused `test_studio_host_report_selection_padded` host regression now proves FRX/LBX trimmed padded stable selectors, record-selector precedence over conflicting stable selectors, and out-of-range record selectors preserve current selection-kind routing, null fallback payloads, requested launch record metadata, and stable object/section/settings summaries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2836 no-selection stable-selector evidence shipped: the new focused `test_studio_host_report_selection_none` host regression now proves FRX/LBX missing, blank, missing-`UNIQUEID`, ambiguous, and live/deleted-ambiguous stable selectors preserve explicit `"selectedReportSelectionKind": "none"` routing, null object/section/settings payloads, and current live/deleted count summaries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2835 selected label-section evidence shipped: the new focused `test_studio_host_selected_label_sections` host regression now proves LBX selected live label section inspection preserves selected-section metadata, preview/deleted-preview accounting, and null object/settings payloads for direct record-selected section queries, while the deleted label-section leg now proves the verified record-selected delete path preserves selected deleted-section metadata and deleted preview accounting without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2834 selected label-object evidence shipped: the new focused `test_studio_host_selected_label_objects` host regression now proves LBX selected live, page-header, deleted, unplaced, and deleted-unplaced label object selection preserves selected-object metadata, containing-section routing, preview/deleted-preview accounting, and null section/settings payloads for record-selected object queries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2833 selected label-settings evidence shipped: the new focused `test_studio_host_selected_label_settings` host regression now proves LBX selected live and deleted label settings selection preserves selected-settings metadata, live/deleted preview accounting, memo/direct setting provenance, and null section/object/object-section payloads for record-selected settings queries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2832 selected report-settings evidence shipped: the new focused `test_studio_host_selected_report_settings` host regression now proves FRX selected live and deleted report settings selection preserves selected-settings metadata, current live/deleted preview accounting, memo/direct setting provenance, and null section/object/object-section payloads for record-selected settings and object queries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2831 selected report-object evidence shipped: the new focused `test_studio_host_selected_report_objects` host regression now proves FRX selected live, page-header, deleted, unplaced, and deleted-unplaced report object selection preserves selected-object metadata, containing-section routing, preview/deleted-preview accounting, and null section/settings payloads for record-selected object and section queries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2830 selected report-section evidence shipped: the new focused `test_studio_host_selected_report_sections` host regression now proves FRX selected live and deleted report section selection preserves selected-section metadata, current live/deleted preview accounting, deleted detail section object membership, and null object/settings selection payloads for record-selected section and object queries without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2817 record-selected report/label group-footer selected-sections evidence shipped: the new focused `test_studio_host_selected_group_footer_sections_record` host regression now proves FRX/LBX record-selected live and deleted group-footer section selection preserves selected-section metadata, seeded group-footer section ids, grouping expression fields, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2816 record-selected report/label group-header selected-sections evidence shipped: the new focused `test_studio_host_selected_group_header_sections_record` host regression now proves FRX/LBX record-selected live and deleted group-header section selection preserves selected-section metadata, seeded group-header section ids, grouping expression fields, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2815 record-selected report/label page-header selected-sections evidence shipped: the new focused `test_studio_host_selected_page_header_sections_record` host regression now proves FRX/LBX record-selected live and deleted page-header section selection preserves selected-section metadata, seeded page-header section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2814 record-selected report/label page-footer selected-sections evidence shipped: the new focused `test_studio_host_selected_page_footer_sections_record` host regression now proves FRX/LBX record-selected live and deleted page-footer section selection preserves selected-section metadata, seeded page-footer section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2813 record-selected report/label column-footer selected-sections evidence shipped: the new focused `test_studio_host_selected_column_footer_sections_record` host regression now proves FRX/LBX record-selected live and deleted column-footer section selection preserves selected-section metadata, seeded column-footer section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2812 record-selected report/label column-header selected-sections evidence shipped: the new focused `test_studio_host_selected_column_header_sections_record` host regression now proves FRX/LBX record-selected live and deleted column-header section selection preserves selected-section metadata, seeded column-header section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2811 record-selected report/label title selected-sections evidence shipped: the new focused `test_studio_host_selected_title_sections_record` host regression now proves FRX/LBX record-selected live and deleted title section selection preserves selected-section metadata, seeded title section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2810 stable-selected report/label column-footer selected-sections evidence shipped: the new focused `test_studio_host_selected_column_footer_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted column-footer section selection preserves selected-section metadata, seeded column-footer section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2809 stable-selected report/label column-header selected-sections evidence shipped: the new focused `test_studio_host_selected_column_header_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted column-header section selection preserves selected-section metadata, seeded column-header section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2808 stable-selected report/label page-footer selected-sections evidence shipped: the new focused `test_studio_host_selected_page_footer_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted page-footer section selection preserves selected-section metadata, seeded page-footer section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2807 stable-selected report/label title selected-sections evidence shipped: the new focused `test_studio_host_selected_title_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted title section selection preserves selected-section metadata, seeded title section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2806 stable-selected report/label summary selected-sections evidence shipped: the new focused `test_studio_host_selected_summary_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted summary section selection preserves selected-section metadata, seeded summary section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2805 record-selected report/label summary selected-sections evidence shipped: the new focused `test_studio_host_selected_summary_sections_record` host regression now proves FRX/LBX record-selected live and deleted summary section selection preserves selected-section metadata, seeded summary section ids, preview bounds, sibling section exposure, and null object/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2804 record-selected report/label summary selected-objects evidence shipped: the new focused `test_studio_host_selected_summary_objects_record` host regression now proves FRX/LBX record-selected live and deleted summary object selection preserves selected-object metadata, summary containing-section routing, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2803 stable-selected report/label summary selected-objects evidence shipped: the new focused `test_studio_host_selected_summary_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted summary object selection preserves selected-object metadata, summary containing-section routing, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2802 stable-selected report/label selected-objects evidence shipped: the new focused `test_studio_host_selected_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted object selection preserves selected-object metadata, containing-section routing, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2801 stable-selected report/label page-header selected-sections evidence shipped: the new focused `test_studio_host_selected_page_header_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted page-header section selection preserves selected-section metadata, section and object counts, preview bounds, and null settings/object selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2800 stable-selected report/label selected-sections evidence shipped: the new focused `test_studio_host_selected_sections_stable` host regression now proves FRX/LBX stable-selected live and deleted section selection preserves selected-section availability, preview bounds, section counts, and null settings/object selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2799 stable-selected report/label selected-settings evidence shipped: the new focused `test_studio_host_selected_settings_stable` host regression now proves FRX/LBX stable-selected live and deleted settings selection preserves selected-settings availability, settings provenance, preview bounds, and null section/object selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2798 record-selected report/label settings restore evidence shipped: the new focused `test_studio_host_settings_restore_record` host regression now proves FRX/LBX record-selected settings restore success preserves live setting counts, selected-settings metadata, full preview bounds, and surrounding section/object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2797 record-selected report/label settings delete evidence shipped: the new focused `test_studio_host_settings_delete_record` host regression now proves FRX/LBX record-selected settings delete success preserves deleted-setting counts, selected deleted-settings availability, full preview bounds, and surrounding section/object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2796 stable-selected report/label settings restore evidence shipped: the new focused `test_studio_host_settings_restore_stable` host regression now proves FRX/LBX stable-selected settings restore success preserves live setting counts, selected-settings metadata, full preview bounds, and surrounding section/object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2795 stable-selected report/label settings delete evidence shipped: the new focused `test_studio_host_settings_delete_stable` host regression now proves FRX/LBX stable-selected settings delete success preserves deleted-setting counts, selected deleted-settings metadata, full preview bounds, and surrounding section/object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2794 stable-selected report/label section restore evidence shipped: the new focused `test_studio_host_section_restore_stable_selection` host regression now proves FRX/LBX stable-selected section restore success preserves the current section-restore payload, section counts, live preview state, selected-section metadata, and restored section object membership for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2793 record-selected report/label section restore evidence shipped: the new focused `test_studio_host_section_restore_record_selection` host regression now proves FRX/LBX record-selected section restore success preserves the current section-restore payload, section counts, live preview state, selected-section metadata, and restored section object membership for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2792 stable-selected report/label section delete evidence shipped: the new focused `test_studio_host_section_delete_stable_selection` host regression now proves FRX/LBX stable-selected section delete success preserves the current deleted-section lifecycle payload, section counts, deleted preview bounds, selected deleted-section metadata, and stable deleted-section object membership for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2791 record-selected report/label section delete evidence shipped: the new focused `test_studio_host_section_delete_record_selection` host regression now proves FRX/LBX record-selected section delete success preserves the current deleted-section lifecycle payload, section counts, deleted preview bounds, selected deleted-section metadata, and deleted-section object membership for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2790 report/label stable-selected column-setup settings success evidence shipped: the new focused `test_studio_host_column_setup_stable_settings` host regression now proves FRX/LBX stable-selected `EXPR` column-setup memo update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, direct-field carrier behavior after clear, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2789 report/label record-selected column-setup settings success evidence shipped: the new focused `test_studio_host_column_setup_record_settings` host regression now proves FRX/LBX record-selected `EXPR` column-setup memo update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, direct-field carrier behavior after clear, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2788 report/label stable-selected column-spacing setting success evidence shipped: the new focused `test_studio_host_column_spacing_stable_settings` host regression now proves FRX/LBX stable-selected `COLSPACING` direct-field update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2787 report/label record-selected column-spacing setting success evidence shipped: the new focused `test_studio_host_column_spacing_record_settings` host regression now proves FRX/LBX record-selected `COLSPACING` direct-field update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2786 report/label record-selected column-width setting success evidence shipped: the new focused `test_studio_host_column_width_record_settings` host regression now proves FRX/LBX record-selected `COLWIDTH` direct-field update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2785 report/label record-selected column-count setting success evidence shipped: the new focused `test_studio_host_column_count_record_settings` host regression now proves FRX/LBX record-selected `COLS` direct-field update and clear success for live and deleted settings rows preserve the current column-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2784 report/label record-selected settings memo success evidence shipped: the new focused `test_studio_host_settings_memo_record` host regression now proves FRX/LBX record-selected `EXPR` settings-memo update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, direct-field carrier behavior after clear, and full live/deleted preview and section/object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2783 report/label record-selected paper-size setting success evidence shipped: the new focused `test_studio_host_paper_size_record_settings` host regression now proves FRX/LBX record-selected `PAPERSIZE` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2782 report/label record-selected orientation setting success evidence shipped: the new focused `test_studio_host_orientation_record_settings` host regression now proves FRX/LBX record-selected `ORIENTATION` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2781 report/label record-selected horizontal-grid setting success evidence shipped: the new focused `test_studio_host_horizontal_grid_record_settings` host regression now proves FRX/LBX record-selected `GRIDH` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2780 report/label record-selected vertical-grid setting success evidence shipped: the new focused `test_studio_host_vertical_grid_record_settings` host regression now proves FRX/LBX record-selected `GRIDV` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2779 report/label record-selected bottom-margin setting success evidence shipped: the new focused `test_studio_host_bottom_margin_record_settings` host regression now proves FRX/LBX record-selected `BOTMARGIN` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2778 report/label record-selected page-margin setting success evidence shipped: the new focused `test_studio_host_page_margin_record_settings` host regression now proves FRX/LBX record-selected `TOPMARGIN` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and full live/deleted preview bounds for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2777 report/label stable page-margin setting success evidence shipped: the new focused `test_studio_host_page_margin_stable_settings` host regression now proves FRX/LBX stable-selected `TOPMARGIN` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and full live/deleted preview bounds for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2776 report/label stable settings memo success evidence shipped: the new focused `test_studio_host_settings_memo_stable` host regression now proves FRX/LBX stable-selected `EXPR` settings-memo update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared direct-field carrier behavior, and full live/deleted preview bounds for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2775 report/label stable paper-size setting success evidence shipped: the new focused `test_studio_host_paper_size_stable_settings` host regression now proves FRX/LBX stable-selected `PAPERSIZE` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2774 report/label stable orientation setting success evidence shipped: the new focused `test_studio_host_orientation_stable_settings` host regression now proves FRX/LBX stable-selected `ORIENTATION` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2773 report/label stable horizontal-grid setting success evidence shipped: the new focused `test_studio_host_horizontal_grid_stable_settings` host regression now proves FRX/LBX stable-selected `GRIDH` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2772 report/label stable vertical-grid setting success evidence shipped: the new focused `test_studio_host_vertical_grid_stable_settings` host regression now proves FRX/LBX stable-selected `GRIDV` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2771 report/label stable bottom-margin setting success evidence shipped: the new focused `test_studio_host_bottom_margin_stable_settings` host regression now proves FRX/LBX stable-selected `BOTMARGIN` direct-field update and clear success for live and deleted settings rows preserve the current page-setup/settings counts, selected-settings provenance, cleared-field behavior, and non-fabricated preview state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2770 report/label mixed deleted-states success evidence shipped: the new focused `test_studio_host_mixed_deleted_states` host regression now proves FRX/LBX mixed settings+section+object `--deleted-states` delete and restore success preserve the current preview bounds, live/deleted settings, section, and object counts, deleted-metadata routing, and non-fabricated selection state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2769 report/label settings+section deleted-states success evidence shipped: the new focused `test_studio_host_settings_section_deleted_states` host regression now proves FRX/LBX `--deleted-states` settings+section delete and restore success preserve the current preview bounds, live/deleted settings and section counts, deleted-metadata routing, and non-fabricated selection state for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2768 report/label object deleted-states success evidence shipped: the new focused `test_studio_host_object_deleted_states` host regression now proves FRX/LBX `--deleted-states` object delete and restore success preserve the current preview bounds, live/deleted object counts, section-aware live object metadata, and unrelated deleted-row metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2767 deleted report/label reorder-batch success evidence shipped: the new focused `test_studio_host_deleted_object_reorder_batch` host regression now proves deleted FRX/LBX `--visual-object-reorder-batch` success preserves the current batch result payload, moves deleted rows without changing deleted-state membership, and keeps the refreshed reopen selection, containing-section, and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2766 report/label reorder-batch success evidence shipped: the new focused `test_studio_host_object_reorder_batch` host regression now proves stable FRX/LBX `--visual-object-reorder-batch` success preserves the current batch result payload, applies ordered stable-selector moves without changing identities, and keeps the refreshed reopen selection and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2765 deleted report/label duplicate-batch success evidence shipped: the new focused `test_studio_host_deleted_object_duplicate_batch` host regression now proves deleted FRX/LBX `--visual-object-duplicate-batch` success preserves the current batch result payload, appends deleted duplicates without changing the original rows or deleted-state membership, and keeps the refreshed reopen selection, containing-section, and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2764 report/label duplicate-batch success evidence shipped: the new focused `test_studio_host_object_duplicate_batch` host regression now proves stable FRX/LBX `--visual-object-duplicate-batch` success preserves the current batch result payload, appends selected duplicates without changing original rows, and keeps the refreshed reopen selection and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2763 deleted report/label rename-batch success evidence shipped: the new focused `test_studio_host_deleted_object_rename_batch` host regression now proves deleted FRX/LBX `--visual-object-rename-batch` success preserves the current batch result payload, replaces deleted selected identities without changing order, counts, or deleted-state membership, and keeps the refreshed reopen selection, containing-section, and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2762 report/label rename-batch success evidence shipped: the new focused `test_studio_host_object_rename_batch` host regression now proves stable FRX/LBX `--visual-object-rename-batch` success preserves the current batch result payload, replaces selected identities without changing object order or counts, and keeps the refreshed reopen selection and preview metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2761 deleted report/label update-batch rollback evidence shipped: the new focused `test_studio_host_deleted_update_batch_errors` host regression now proves deleted FRX/LBX `--visual-object-update-batch` rollback on a missing stable selector preserves the current null-batch JSON error payload, omits stale success-only fields, restores earlier property values, and preserves deleted-state membership for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2760 report/label update-batch rollback evidence shipped: the new focused `test_studio_host_update_batch_errors` host regression now proves stable FRX/LBX `--visual-object-update-batch` rollback on a missing stable selector preserves the current null-batch JSON error payload, omits stale success-only fields, and restores earlier direct and memo-backed property values for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2759 report/label update-batch success evidence shipped: the new focused `test_studio_host_update_batch` host regression now proves stable FRX/LBX `--visual-object-update-batch` success preserves the current batch result payload, persisted direct and memo-backed property updates, live/deleted preview bounds, and refreshed selected-object metadata for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2758 deleted report/label update-batch success evidence shipped: the new focused `test_studio_host_deleted_update_batch` host regression now proves deleted FRX/LBX `--visual-object-update-batch` success preserves the current batch result payload, persisted property updates, deleted preview bounds, deleted-row section metadata, and stable identities for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2757 report/label subtree deleted-state success evidence shipped: the stable FRX/LBX `--subtree-deleted-state` delete assertions in `test_studio_host_json` now track the current shared report-layout behavior instead of stale null containing sections, so deleted selected rows preserve their live detail-band id, deleted-object section counts, and selected record metadata; and the new focused `test_studio_host_subtree_deleted_state` host regression now proves report/label subtree delete and restore success preserve the current preview bounds, section-aware selection payloads, object counts, and stable identities for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2756 report/label subtree duplicate success evidence shipped: the new focused `test_studio_host_subtree_duplicate` host regression now proves stable FRX/LBX `--visual-object-duplicate-subtree` success preserves the current copied-row result payload, section-aware reopen metadata, preview bounds, live object counts, and stable row ordering for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2755 deleted report/label subtree duplicate success evidence shipped: the stable deleted FRX/LBX `--visual-object-duplicate-subtree` reopen regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted copied rows reopened after stable-identity subtree duplicate preserve their live detail-band id, selected record index, per-section object counts, and deleted-preview/report selection metadata; and the new focused `test_studio_host_deleted_subtree_duplicate` host regression now proves deleted report/label subtree duplicate success keeps that section-aware payload for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2754 deleted report/label subtree duplicate collision evidence shipped: the new focused `test_studio_host_deleted_subtree_duplicate_collision_errors` host regression now proves deleted report/label subtree duplicate requests reject replacement collisions without mutating layout rows while preserving the current replacement-identity collision payload, deleted root state, object count, and stable row ordering for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2753 deleted report/label subtree duplicate missing-selector evidence shipped: the stable deleted FRX/LBX `--visual-object-duplicate-subtree` regressions now pin `en-US` host locale state before asserting English selector prose, and the new focused `test_studio_host_deleted_subtree_duplicate_errors` host regression now proves deleted report/label subtree duplicate requests reject missing stable selectors without mutating layout rows while preserving the current selector-error payload for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2752 report/label subtree duplicate collision evidence shipped: the new focused `test_studio_host_subtree_duplicate_collision_errors` host regression now proves report/label subtree duplicate requests reject replacement collisions without mutating layout rows while preserving the current replacement-identity collision payload, object count, and stable row ordering for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2751 report/label subtree duplicate missing-selector evidence shipped: the stable FRX/LBX `--visual-object-duplicate-subtree` regressions now pin `en-US` host locale state before asserting English selector prose, and the new focused `test_studio_host_subtree_duplicate_errors` host regression now proves report/label subtree duplicate requests reject missing stable selectors without mutating layout rows while preserving the current selector-error payload for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2750 report/label subtree deleted-state missing-selector evidence shipped: the stable FRX/LBX `--subtree-deleted-state` regressions now pin `en-US` host locale state before asserting English selector prose, and the new focused `test_studio_host_subtree_deleted_state_errors` host regression now proves report/label subtree deleted-state requests reject missing stable selectors without mutating layout rows while preserving the current selector-error payload for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2749 report/label settings+section deleted-states rollback evidence shipped: the new focused `test_studio_host_settings_section_deleted_states_errors` host regression now proves FRX/LBX settings+section `--deleted-states` batches roll back earlier settings and section deletions when a later stable selector is missing while preserving the current invariant error-status payload and unrelated pre-deleted rows for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2748 report/label mixed deleted-states rollback evidence shipped: the new focused `test_studio_host_mixed_deleted_states_errors` host regression now proves FRX/LBX mixed `--deleted-states` batches roll back earlier settings, section, and object deletions when a later stable selector is missing while preserving the current invariant error-status payload, unrelated pre-deleted rows, and asset object counts for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2747 report/label object deleted-states rollback evidence shipped: the new focused `test_studio_host_object_deleted_states_errors` host regression now proves FRX/LBX `--deleted-states` object batches roll back earlier object deletions when a later stable selector is missing while preserving the current invariant error-status payload and asset object counts for both reports and labels, without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2746 report/label object-rename-batch rollback payload evidence shipped: the stable FRX/LBX `--visual-object-rename-batch` rollback regressions now pin `en-US` host locale state before asserting English collision prose, the broad `#1843` rollback assertions in `test_studio_host_json` now track the current identity-value collision payload deterministically instead of drifting against leaked locale state, and the new focused `test_studio_host_object_rename_batch_errors` host regression now proves report/label rename-batch rollback failures keep earlier identity mutations rolled back while reporting the current `visualObjectRenameBatch: null` / identity-value collision JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2745 report/label object-duplicate-batch rollback payload evidence shipped: the stable FRX/LBX `--visual-object-duplicate-batch` rollback regressions now pin `en-US` host locale state before asserting English collision prose, the broad `#1844` rollback assertions in `test_studio_host_json` now track the current replacement-identity collision payload deterministically instead of drifting against leaked locale state, and the new focused `test_studio_host_object_duplicate_batch_errors` host regression now proves report/label duplicate-batch rollback failures keep earlier duplicate mutations rolled back while reporting the current `visualObjectDuplicateBatch: null` / replacement-identity collision JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2744 report/label object-reorder-batch rollback payload evidence shipped: the stable FRX/LBX `--visual-object-reorder-batch` rollback regressions now pin `en-US` host locale state before asserting English missing-target prose, the broad `#1845` rollback assertions in `test_studio_host_json` now track the current missing-target payload deterministically instead of drifting against leaked locale state, and the new focused `test_studio_host_object_reorder_batch_errors` host regression now proves report/label reorder-batch rollback failures keep earlier reorder mutations rolled back while reporting the current `visualObjectReorderBatch: null` / missing-target JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2743 deleted object-rename-batch rollback payload evidence shipped: the stable deleted FRX/LBX `--visual-object-rename-batch` rollback regressions now pin `en-US` host locale state before asserting English identity-collision prose, the broad `#1862` rollback assertions in `test_studio_host_json` now track the current identity-value collision payload deterministically instead of drifting against leaked locale state, and the new focused `test_studio_host_deleted_object_rename_batch_errors` host regression now proves deleted placed rename-batch rollback failures keep earlier rename mutations rolled back while reporting the current `visualObjectRenameBatch: null` / identity-value collision JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2742 deleted object-duplicate-batch rollback payload evidence shipped: the stable deleted FRX/LBX `--visual-object-duplicate-batch` rollback regressions now pin `en-US` host locale state before asserting English collision prose, the broad `#1860` rollback assertions in `test_studio_host_json` now track the current replacement-identity collision payload deterministically instead of drifting against leaked locale state, and the new focused `test_studio_host_deleted_object_duplicate_batch_errors` host regression now proves deleted placed duplicate-batch rollback failures keep earlier duplicate mutations rolled back while reporting the current `visualObjectDuplicateBatch: null` / replacement-identity collision JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2741 deleted object-reorder-batch rollback payload evidence shipped: the stable deleted FRX/LBX `--visual-object-reorder-batch` rollback regressions now pin `en-US` host locale state before asserting English error prose, the broad `#1861` rollback assertions in `test_studio_host_json` now match the current missing-target payload shape instead of drifting against leaked locale state, and the new focused `test_studio_host_deleted_object_reorder_batch_errors` host regression now proves deleted placed reorder-batch rollback failures keep earlier mutations rolled back while reporting the current `visualObjectReorderBatch: null` / missing-target JSON error for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2740 deleted object-reorder-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-object-reorder-batch` reopen regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted moved rows reopened after stable-identity reorder-batch preserve their live detail-band id, selected record index, and per-section deleted-object counts while selecting the moved identity; and the new focused `test_studio_host_deleted_object_reorder_batch_sections` host regression now proves deleted placed reorder-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2739 deleted object-duplicate-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-object-duplicate-batch` reopen regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted copied rows reopened after stable-identity duplicate-batch preserve their live detail-band id, selected record index, and per-section deleted-object counts while selecting the copied identity; and the new focused `test_studio_host_deleted_object_duplicate_batch_sections` host regression now proves deleted placed duplicate-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2738 deleted object-rename-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-object-rename-batch` reopen regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after stable-identity rename-batch preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts while selecting the renamed identity; and the new focused `test_studio_host_deleted_object_rename_batch_sections` host regression now proves deleted placed rename-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2737 deleted property-batch-rejection section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-rename-batch` and `--visual-property-reorder-batch` rejection regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after direct-field batch rejection preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_batch_rejection_sections` host regression now proves deleted placed rename-batch/reorder-batch rejection selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2736 deleted property-rejection section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-rename` and `--visual-property-reorder` rejection regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after direct-field rejection preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_rejection_sections` host regression now proves deleted placed rename/reorder rejection selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2735 deleted property-move-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-move-batch` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a property move-batch preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_move_batch_sections` host regression now proves deleted placed move-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2734 deleted property-move section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-move` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a property move preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_move_sections` host regression now proves deleted placed move selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2733 deleted property-copy-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-copy-batch` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a property copy-batch preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_copy_batch_sections` host regression now proves deleted placed copy-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2732 deleted property-copy section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-copy` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a property copy preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_copy_sections` host regression now proves deleted placed copy selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2731 deleted property-clear-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-clear-batch` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a clear-batch preserve their live detail-band id, record index, section-relative geometry, current section-local ordering, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_clear_batch_sections` host regression now proves deleted placed clear-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2730 deleted property-clear section-context evidence shipped: the stable deleted FRX/LBX `--visual-property-clear` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after a property clear preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_property_clear_sections` host regression now proves deleted placed clear selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2729 deleted update-batch section-context evidence shipped: the stable deleted FRX/LBX `--visual-object-update-batch` regressions in `test_studio_host_json` now assert the current shared report-layout behavior instead of stale null containing sections, so deleted placed rows reopened after batch property edits preserve their live detail-band id, record index, section-relative geometry, and per-section deleted-object counts; and the new focused `test_studio_host_deleted_update_batch_sections` host regression now proves deleted placed update-batch selections keep that section-aware context for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2728 deleted band-object section-context evidence shipped: the stable-selected deleted FRX/LBX placed-object regressions for group header, group footer, title, page footer, column header, and column footer now assert the current shared report-layout behavior instead of stale null containing sections, so those fixtures preserve live containing-section ids, record indexes, section-relative geometry, and per-section deleted-object counts when the selected object is deleted but still placed inside a live band; and the new focused `test_studio_host_deleted_band_object_sections` host regression now proves that section-aware deleted-object context across all six band families without changing host JSON field names or production behavior.
- 2026-06-28: E3/#2727 report/label band-identity fidelity shipped: the shared report-layout snapshot now preserves FRX/LBX band `UNIQUEID` values as section ids, including field provenance for live and deleted sections, instead of always synthesizing `detail_header_N` / `detail_footer_N` identifiers; the existing Studio host JSON regressions for detail header/footer report/label flows now assert those stable band ids where the fixtures already provide them; and a new focused `test_studio_host_report_section_ids` host regression proves selected sections plus selected object containing-section metadata round-trip the shared GUID-backed band ids without changing fallback behavior for older assets that lack `UNIQUEID`.
- 2026-06-28: Localization/#2726 generated C# transpilation exception parity shipped: `src/runtime/runtime_pipeline.cpp` now emits locale-aware transpiled C# helpers that resolve unsupported-statement and xAsset manual-port `NotSupportedException` text from embedded `Runtime.Package.Transpilation.Error.*` catalog-backed values for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, honoring `--locale` and `COPPERFIN_LOCALE` at runtime without per-language transpiled artifacts; focused runtime-pipeline coverage now proves the generated PRG and xAsset C# output preserves statement-text and method-identity placeholders while routing the exception prose through localization instead of raw English literals.
- 2026-06-28: Localization/#2725 generated launcher runtime-package parity shipped: `src/runtime/runtime_pipeline.cpp` now emits a locale-aware .NET launcher that resolves launcher failure text from embedded `Runtime.Package.Launcher.Error.*` catalog-backed values for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, honoring `--locale` and `COPPERFIN_LOCALE` before the runtime host starts; focused runtime-pipeline coverage now proves the generated launcher preserves manifest/debug forwarding, package exit codes, and working-directory behavior while routing missing-host, missing-manifest, and host-start failures through localized lookup instead of raw English control-flow literals.
- 2026-06-28: Localization/#2724 Studio host usage-label fallback cleanup shipped: `apps/copperfin_studio_host/main.cpp` no longer carries raw English fallback labels for the catalog-backed object-assignment usage lines, so pseudo-localized and non-English usage output now depends directly on the shipped `StudioHost.LaunchParse.ObjectAssignment.*` catalogs in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`; direct qps-ploc host verification now proves the selected-back-color, OLE drop-mode, and WhatsThis help ID usage labels route through localization without changing CLI switches, invariant placeholders, or other machine-readable host contracts.
- 2026-06-28: Localization/#2723 ALTER TABLE field-declaration validation parity shipped: the remaining `ALTER TABLE ADD COLUMN` / `ALTER TABLE ALTER COLUMN` unsupported-field-declaration failures in `src/runtime/prg_engine_dispatch.inl` now route through shared `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`; focused localization coverage now proves placeholder-preserving catalog parity for the new `ALTER TABLE` validation key while keeping invariant command tokens; and focused PRG table-structure coverage now proves localized `ALTER TABLE` invalid-declaration failures without changing parser behavior, command tokens, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2722 shared PRG dispatch array/object-target helper parity shipped: the remaining shared `COPY TO ARRAY`, `SCATTER TO`, `SCATTER NAME`, and `GATHER NAME` array/object-target helper failures in `src/runtime/prg_engine_dispatch.inl` now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`; focused localization coverage now proves placeholder-preserving catalog parity for the new helper keys while keeping invariant command tokens like `COPY TO ARRAY` and `SCATTER NAME`; and focused PRG control-flow coverage now proves localized array-name-required, invalid-array-name, and invalid-object-target failures without changing parser behavior, command tokens, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2721 residual PRG aggregate precondition parity shipped: the remaining aggregate-command selected/target work-area, `TO ARRAY` target-validation, missing-expression, and aggregate-target count-mismatch failures in `src/runtime/prg_engine_aggregate.inl` now route through shared `Runtime.Prg.Aggregate.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`; focused localization coverage now proves placeholder-preserving catalog parity for the new aggregate keys while keeping invariant command tokens like `SUM`, `TO`, and `ARRAY`; and focused PRG control-flow coverage now proves localized aggregate precondition failures without changing aggregate behavior, pause reasons, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2720 PRG runtime guardrail overflow parity shipped: the remaining call-depth, executed-statement, and loop-iteration overflow faults in `src/runtime/prg_engine_session.inl` now route through shared `Runtime.Prg.Core.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`; focused localization coverage now proves placeholder-preserving catalog parity for the new guardrail keys; and focused PRG control-flow coverage now proves pseudo-localized guardrail overflow faults plus explicit loop-iteration enforcement without changing pause reasons, numeric limit values, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2719 residual PRG OLE invocation/property-read runtime parity shipped: the remaining OLE method-invocation and property-read fallback faults in `src/runtime/prg_engine.cpp` now route through shared `Runtime.Prg.Core.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`; focused localization coverage now proves placeholder-preserving catalog parity for the new OLE invocation/read keys; and focused PRG control-flow coverage now proves pseudo-localized missing-object and missing-member OLE invocation/read failures without changing AERROR metadata, automation bridge identifiers, object state handling, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2718 PRG object-helper fallback runtime parity shipped: the remaining object-helper OLE property-assignment, generic object-target assignment, and `SCATTER NAME` object-creation fallback failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, focused localization coverage now proves placeholder-preserving catalog parity for the last shared fallback messages, focused PRG control-flow coverage now proves pseudo-localized OLE property-assignment failures without changing AERROR metadata, object-target semantics, or other machine-readable runtime contracts, and `src/runtime/prg_engine_dispatch.inl` no longer contains direct hard-coded `last_error_message` string literals.
- 2026-06-28: Localization/#2717 residual PRG command-precondition and cancellation runtime parity shipped: the remaining direct hard-coded `TEXT`, `TRY`, `REPLACE`, `UPDATE`, `INSERT INTO`, `UNLOCK RECORD`, and `SLEEP cancelled` dispatch failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG control-flow coverage now proves pseudo-localized command failures and spawned-task sleep cancellation without changing command syntax, cancellation semantics, event categories, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2716 top-level PRG runtime fault wrapper parity shipped: the remaining dispatch-layer out-of-memory, filesystem/system fault, generic exception, and unknown-exception wrappers now route through the existing `Runtime.Prg.Core.Error.*` catalog entries instead of hard-coded English, and focused PRG control-flow coverage now proves runtime-fault wrappers honor the active locale without changing exception detail propagation, event categories, pause reasons, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2715 PRG DECLARE DLL runtime error parity shipped: the remaining direct hard-coded `DECLARE DLL` missing-function/path, DLL-load, function-not-found, and non-Windows guard failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG runtime coverage now proves pseudo-localized DECLARE failures without changing PRG command syntax, DLL/function identifiers, downstream OS error payloads, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2714 PRG SET FILTER, DIMENSION/DECLARE, and SLEEP runtime error parity shipped: the remaining direct hard-coded `SET FILTER`, `DIMENSION/DECLARE`, and `SLEEP` precondition failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO coverage now prove pseudo-localized command-precondition failures without changing PRG command syntax, filter semantics, array-declaration behavior, sleep behavior, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2713 PRG CREATE CURSOR/TABLE and ALTER TABLE runtime error parity shipped: the remaining direct hard-coded `CREATE CURSOR`, `CREATE TABLE`, and `ALTER TABLE` precondition failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus table-structure coverage now prove pseudo-localized DDL failures without changing PRG command syntax, field-declaration parsing, alter-action semantics, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2712 PRG SCATTER/GATHER runtime error parity shipped: the remaining direct hard-coded `SCATTER` and `GATHER` no-work-area, no-current-record, `FIELDS`-mismatch, and missing-object-variable failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO coverage now prove pseudo-localized `SCATTER`/`GATHER` failures without changing PRG command syntax, field-filter behavior, object-variable semantics, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2711 PRG APPEND FROM TYPE wrapper runtime error parity shipped: the remaining repeated hard-coded `APPEND FROM TYPE {type}: {errorMessage}` wrapper failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO coverage now prove pseudo-localized typed wrapper failures without changing PRG command syntax, type tokens, typed import behavior, DBF write behavior, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2710 PRG APPEND FROM TYPE source-open and FIELDS-clause runtime error parity shipped: the repeated hard-coded `APPEND FROM TYPE {type}` open-source and no-matching-fields failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO/SQL-result-cursor coverage now prove pseudo-localized typed import failures without changing PRG command syntax, type tokens, field-filter behavior, DBF/SQL result cursor semantics, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2709 PRG APPEND FROM shared runtime error parity shipped: the remaining hard-coded shared `APPEND FROM` runtime failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO/SQL-result-cursor coverage now prove pseudo-localized no-work-area, unsupported-source-type, no-matching-fields, and downstream wrapper failures without changing PRG command syntax, field-filter behavior, DBF/SQL result cursor semantics, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2708 PRG APPEND FROM ARRAY runtime error parity shipped: the remaining hard-coded `APPEND FROM ARRAY` runtime failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO coverage now prove pseudo-localized `APPEND FROM ARRAY` failures without changing PRG command syntax, array semantics, field-filter behavior, DBF write behavior, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2707 PRG COPY TO runtime error parity shipped: the remaining hard-coded `COPY TO` / `COPY TO ARRAY` runtime failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG data-IO coverage now prove pseudo-localized `COPY TO` failures without changing PRG command syntax, output-type tokens, schema selection rules, OS error propagation, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2706 PRG file-operation runtime error parity shipped: the remaining hard-coded `ERASE`, `COPY FILE`, and `RENAME` runtime failures now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus PRG control-flow coverage now prove pseudo-localized file-operation failures without changing PRG command syntax, OS error propagation, path handling, or other machine-readable runtime contracts.
- 2026-06-28: Localization/#2705 PRG SAVE/RESTORE runtime error parity shipped: the remaining hard-coded `SAVE TO` / `RESTORE FROM` memvar command errors now route through `Runtime.Prg.Dispatch.Error.*` catalog entries with shipped parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization plus runtime data-IO coverage now prove pseudo-localized runtime failures without changing PRG syntax, file-extension behavior, command semantics, or other machine-readable runtime contracts.
- 2026-06-27: E3/#2691 report/label layout font-option edit fidelity shipped: focused Studio host JSON regressions now prove FRX/LBX layout-object `FONTSIZE` updates and `MODE` clears round-trip through record-selected, stable-selected, and deleted record-selected flows; the deleted layout-object font regressions now also align with current placed deleted-object containing-section semantics; and label identity, machine-readable report-layout JSON field names, band/object identifiers, and other designer contracts remain unchanged.
- 2026-06-27: E3/#2690 live report-object deleted-section fidelity shipped: the shared report-layout snapshot now preserves containing deleted-section identity, relative geometry, and section-local object order/count metadata for live FRX/LBX objects whose geometry belongs to deleted bands; deleted sections now retain corresponding live object membership instead of presenting empty object arrays; `copperfin_studio_host --json` resolves `selectedReportObjectSection` for those selections while keeping truly unplaced objects sectionless; and focused report-layout plus direct host validation now prove live objects inside deleted sections stay placed with deleted-preview context instead of degrading into unplaced orphans.
- 2026-06-27: E3/#2689 deleted report-object containing-section fidelity shipped: the shared report-layout snapshot now assigns containing-section identity, relative geometry, and section-local deleted-object order/count metadata to deleted placed FRX/LBX objects when their geometry belongs to a live or deleted band; `copperfin_studio_host --json` now resolves `selectedReportObjectSection` for those deleted placed-object selections without fabricating containing sections for deleted unplaced objects; and focused report-layout plus direct host validation now proves both live-section and deleted-section deleted-object selections retain stable section-aware context.
- 2026-06-27: E3/#2688 report/label per-section deleted-object counts shipped: `StudioReportSectionSnapshot` now carries additive per-section deleted placed-object counts, `copperfin_studio_host --json` exposes the same `deletedObjectCount` field on live/deleted sections plus selected containing sections, and focused report-layout plus direct host validation now proves live and deleted FRX/LBX sections count deleted placed objects within their own geometry while unplaced deleted-object selections keep containing-section JSON absent.
- 2026-06-27: E3/#2687 resolved report/label grouping-expression JSON shipped: `StudioReportSectionSnapshot` now carries the resolved grouping expression plus its field-index and memo-block provenance for group-header/group-footer bands, `copperfin_studio_host --json` exposes the same additive fields on `sections`, `deletedSections`, and `selectedReportSection`, and focused report-layout plus host validation now proves blank-footer FRX/LBX sections inherit stable grouping-expression metadata from their paired headers while non-group selected object sections keep that metadata absent.
- 2026-06-27: E3/#2686 report/label section grouping-context JSON shipped: `StudioReportSectionSnapshot` now carries explicit FRX/LBX grouping context for group-header/group-footer bands, including grouping index, nesting depth, header/footer role, and partner band identity/deleted-state; `copperfin_studio_host --json` exposes the same additive fields on `sections`, `deletedSections`, and `selectedReportSection`; and focused report-layout plus host validation now proves live and deleted nested group sections retain correct context while non-group bands keep grouping context absent.
- 2026-06-27: E3/#2685 report/label grouping summary JSON shipped: `StudioReportLayoutSnapshot` now derives explicit FRX/LBX grouping summaries from live and deleted group-header/group-footer bands, `copperfin_studio_host --json` exposes `groupingCount` plus ordered `groupings` metadata without changing existing machine-readable field names, and focused report-layout plus host validation now proves nested live/deleted pairing, expression provenance, stable header/footer identities, and label identity.
- 2026-06-27: E3/#2684 stable-selected nested report/label group-section EXPR edit fidelity shipped: focused Studio host JSON regressions now prove FRX/LBX `--unique-id` selection can update and clear live inner nested group-header and deleted inner nested group-footer `EXPR` fields, persist memo-backed changes, refresh selected live/deleted nested section metadata, preserve unaffected outer/inner sibling expressions plus preview/deleted-preview bounds, and keep label identity without changing report-layout JSON field names, band-kind tokens, selection contracts, or other machine-readable designer metadata.
- 2026-06-26: E3/#2683 stable-selected report/label group-section EXPR edit fidelity shipped: focused Studio host JSON regressions now prove FRX/LBX `--unique-id` selection can update and clear live and deleted group-header/group-footer `EXPR` fields, persist memo-backed changes, refresh selected live/deleted section metadata, preserve sibling section ordering and preview bounds, and keep label identity without changing report-layout JSON field names, band-kind tokens, selection contracts, or other machine-readable designer metadata.
- 2026-06-26: E3/#2682 nested report/label group-section EXPR edit fidelity shipped: focused Studio host JSON regressions now prove FRX/LBX record-selected nested live inner group-header and deleted inner group-footer `EXPR` updates and clears persist memo changes, refresh selected live/deleted section metadata, preserve nested sibling ordering and unaffected outer/inner group expressions, retain live/deleted preview bounds, and keep label identity without changing report-layout JSON field names, band-kind tokens, selection contracts, or other machine-readable designer metadata.
- 2026-06-26: E3/#2681 record-selected nested report/label group-section coverage shipped: focused Studio host JSON regressions now prove FRX/LBX `--json --record <section-record>` preserves nested live and deleted group-header/group-footer sibling ordering, selected deleted/live section metadata, preview and deleted-preview bounds, distinct inner/outer group expressions, and label identity without changing report-layout JSON field names, band-kind tokens, selection contracts, or other machine-readable designer metadata.
- 2026-06-26: E3/#2680 nested report/label group ordering fidelity shipped: focused `test_report_layout` and Studio host JSON regressions now prove FRX/LBX layouts preserve nested group-header/group-footer ordering, distinct group expressions, selected inner-group section metadata, full preview bounds, and label identity without changing report-layout JSON field names, band-kind tokens, selection contracts, or other machine-readable designer metadata.
- 2026-06-26: Localization/#2628 studio-host object-command label parity shipped: the remaining `StudioHost.LaunchParse.ObjectCommand.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused studio-host coverage now verifies translated and pseudo-localized object-command labels without changing CLI flag names, placeholder option tokens, launch-parse machine behavior, JSON contract fields, or other machine-readable studio-host contracts.
- 2026-06-26: Localization/#2627 toolbox dispatch request-error parity shipped: the remaining `Studio.ToolboxDispatch.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized request errors without changing toolbox item ids, selection-context tokens, command tokens, palette contracts, item-metadata contracts, or other machine-readable toolbox-dispatch request semantics.
- 2026-06-26: Localization/#2626 toolbox creation dispatch-error parity shipped: the remaining `Studio.ToolboxCreation.Dispatch.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused toolbox-creation coverage now verifies translated and pseudo-localized dispatch errors without changing toolbox item ids, admitted create-operation semantics, planned object-name rules, descriptor field contracts, asset paths, or other machine-readable toolbox-creation dispatch contracts.
- 2026-06-26: Localization/#2625 editor-action-dispatch request-error parity shipped: the remaining `Studio.EditorActionDispatch.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized request errors without changing action ids, selection-context tokens, command tokens, execution flags, or other machine-readable editor-action-dispatch contracts.
- 2026-06-26: Localization/#2624 toolbox creation request-error parity shipped: the remaining `Studio.ToolboxCreation.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused toolbox-creation coverage now verifies translated and pseudo-localized request errors without changing toolbox item ids, designer-context filtering, admitted-dispatch semantics, object-identity handling, asset paths, or other machine-readable toolbox-creation contracts.
- 2026-06-26: Localization/#2623 toolbox category label parity shipped: the remaining `Studio.Toolbox.Category.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized toolbox category labels without changing toolbox item ids, VFP/base-class metadata, default name prefixes, report-context filtering, or other machine-readable toolbox palette contracts.
- 2026-06-26: Localization/#2622 project-workspace build-target label parity shipped: the remaining `Studio.ProjectWorkspace.BuildTarget.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused project-workspace coverage now verifies translated and pseudo-localized build-target labels without changing `output_kind` machine values, project-item ids, field provenance, memo-block provenance, or other machine-readable build-plan contracts.
- 2026-06-26: Localization/#2621 editor-action-dispatch catalog-entry label parity shipped: the remaining `Studio.EditorActionDispatch.CatalogEntry.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized catalog-entry errors without changing action ids, selection-context tokens, command tokens, execution flags, or other machine-readable editor-action-dispatch contracts.
- 2026-06-26: Localization/#2620 builder-dispatch request-label parity shipped: the remaining `Studio.BuilderDispatch.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused builder-registry coverage now verifies translated and pseudo-localized request errors without changing builder ids, context tokens, command tokens, launch entry points, execution flags, or other machine-readable builder-dispatch contracts.
- 2026-06-26: Localization/#2619 toolbox batch-dispatch label parity shipped: the remaining `Studio.ToolboxCreation.BatchDispatch.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused toolbox-creation coverage now verifies translated and pseudo-localized batch-dispatch errors without changing toolbox item ids, asset paths, planned object-name generation, descriptor field contracts, or other machine-readable toolbox-creation dispatch behavior.
- 2026-06-26: Localization/#2618 toolbox-dispatch catalog-entry label parity shipped: the remaining `Studio.ToolboxDispatch.CatalogEntry.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized catalog-entry errors without changing toolbox item ids, selection-context tokens, command tokens, execution flags, or other machine-readable toolbox-dispatch contracts.
- 2026-06-26: Localization/#2617 designer-dispatch execution-label parity shipped: the remaining `Studio.DesignerDispatch.Execution.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused designer-context coverage now verifies translated and pseudo-localized execution errors without changing selection-context tokens, action ids, builder ids, toolbox item ids, execution flags, or other machine-readable designer-dispatch contracts.
- 2026-06-26: Localization/#2616 designer-dispatch catalog-entry label parity shipped: the remaining `Studio.DesignerDispatch.CatalogEntry.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused designer-context coverage now verifies translated and pseudo-localized catalog-entry errors without changing selection-context tokens, action ids, builder ids, toolbox item ids, execution flags, or other machine-readable designer-dispatch contracts.
- 2026-06-26: Localization/#2615 builder-dispatch catalog-entry label parity shipped: the remaining `Studio.BuilderDispatch.CatalogEntry.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused builder-registry coverage now verifies translated and pseudo-localized catalog-entry errors without changing builder ids, context tokens, command tokens, execution flags, or other machine-readable builder-dispatch contracts.
- 2026-06-26: Localization/#2614 editor-action-dispatch execution-label catalog parity shipped: the remaining `Studio.EditorActionDispatch.Execution.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized execution errors without changing action ids, selection-context tokens, command tokens, execution flags, or other machine-readable editor-action-dispatch contracts.
- 2026-06-26: Localization/#2613 toolbox-dispatch execution-label catalog parity shipped: the remaining `Studio.ToolboxDispatch.Execution.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused context-editor-actions coverage now verifies translated and pseudo-localized execution errors without changing toolbox item ids, selection-context tokens, command tokens, execution flags, or other machine-readable toolbox-dispatch contracts.
- 2026-06-26: Localization/#2612 builder-dispatch execution-label catalog parity shipped: the remaining `Studio.BuilderDispatch.Execution.Error.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused builder-registry coverage now verifies translated and pseudo-localized execution errors without changing builder ids, context tokens, command tokens, execution flags, or other machine-readable builder-dispatch contracts.
- 2026-06-26: Localization/#2611 project-workspace label catalog parity shipped: the remaining `Studio.ProjectWorkspace.Group.*` and `Studio.ProjectWorkspace.ItemType.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused project-workspace coverage now verifies translated and pseudo-localized `group_title`/`type_title` values without changing invariant `group_id` values, type codes, record indexes, provenance fields, or other machine-readable workspace contracts.
- 2026-06-26: Localization/#2610 report-layout section-label catalog parity shipped: the remaining `Studio.ReportLayout.Section.*` labels now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused report-layout coverage now verifies translated and pseudo-localized section titles without changing invariant `band_kind` values, section ordering, provenance fields, or other machine-readable report-layout contracts.
- 2026-06-26: Localization/#2609 PRG TOTAL-diagnostic catalog parity shipped: the remaining `Runtime.Prg.Total.Error.*` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog coverage now verifies translated prose, placeholder preservation, and pseudo-localized resolution without changing `TOTAL` command behavior, selected-work-area handling, alias/target resolution, runtime identifiers, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2608 PRG cursor-diagnostic catalog parity shipped: the remaining `Runtime.Prg.Cursor.Error.*` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog coverage now verifies translated prose, placeholder preservation, and pseudo-localized resolution without changing `USE`/`SEEK` behavior, alias/work-area resolution, runtime identifiers, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2607 PRG session-diagnostic catalog parity shipped: the remaining `Runtime.Prg.CommandUndo.Error.*`, `Runtime.Prg.CriticalSection.Error.*`, and `Runtime.Prg.Transaction.Error.*` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog coverage now verifies translated prose, placeholder preservation, and pseudo-localized resolution without changing `UNDO`, `CRITICAL`, transaction-journal behavior, runtime identifiers, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2606 runtime package-error catalog parity shipped: the remaining `Runtime.Package.Error.*` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, the shared runtime-pipeline helper now routes package diagnostics through the runtime locale helper instead of a one-time static catalog snapshot, and focused runtime-pipeline coverage now verifies locale-specific catalog presence, placeholder preservation, pseudo-localization, and in-process locale switching without changing manifest/debug-manifest field names, output kinds, file paths, security-profile ids, runtime-host names, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2605 PRG static-analysis catalog parity shipped: the remaining `Runtime.PrgStaticAnalysis.Diagnostic.PRG0001.Message`, `PRG1001.Message`, and `PRG1002.Message` strings now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, the shared PRG static-analysis helper now routes human-facing diagnostics through the runtime locale helper instead of a one-time static catalog snapshot, and focused control-flow analyzer coverage now verifies locale-specific catalog presence, pseudo-localization, and in-process locale switching without changing diagnostic codes, severities, file paths, line numbers, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2604 xAsset executable-model catalog parity shipped: the remaining `Runtime.XAsset.Error.TablePreviewMissing` and `Runtime.XAsset.Error.UnsupportedExecutableFamily` strings now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, the shared xAsset executable-model helper now routes human-facing diagnostics through the runtime locale helper instead of a one-time static catalog snapshot, and focused xAsset coverage now verifies locale-specific catalog presence, pseudo-localization, and in-process locale switching without changing asset kinds, action ids, routine names, source-field ordinals, memo-block provenance, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2603 index-seek optimizer catalog parity shipped: the remaining `Runtime.IndexSeek.*` match-reason, pattern-reason, plan-decision, and field-label strings now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, the shared index-seek optimizer now routes its human-facing prose through the runtime locale helper instead of a one-time static catalog snapshot, and focused optimizer coverage now verifies locale-specific catalog presence, placeholder preservation, pseudo-localization, and in-process locale switching without changing optimizer enums, selected order names, execution strategies, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2602 VFP asset/native diagnostic catalog parity shipped: the remaining `Vfp.AssetInspector.*`, `Vfp.CdxHeader.*`, `Vfp.DbfHeader.*`, `Vfp.DbfTable.*`, and `Vfp.IndexProbe.*` user-facing strings now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused VFP/DBF coverage now verifies locale-specific catalog presence, placeholder preservation, and pseudo-localized resolution without changing DBF/CDX/index format identifiers, parser behavior, asset metadata, or other machine-readable/runtime contracts.
- 2026-06-26: Localization/#2601 security diagnostics catalog parity shipped: the existing non-profile `Security.*` audit, external-process-policy, process-hardening, secret-provider, and SHA-256 diagnostic keys now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, focused security coverage now verifies localized diagnostics without changing placeholders or machine-readable/runtime contracts, and the shared `security_text(...)` helper now refreshes when the effective locale or locale-root changes so repeated in-process security lookups honor the selected locale.
- 2026-06-26: Localization/#2600 native security profile catalog parity shipped: the existing `Security.Profile.*` mode/policy, permission, role, identity-provider, feature, and hardening-profile keys now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused platform-model coverage now verifies localized native-security-profile prose without changing permission ids, role ids, identity-provider kinds, feature ids, audit event ids, or other machine-readable profile contracts.
- 2026-06-26: Localization/#2599 extensibility profile catalog parity shipped: the existing `Platform.Extensibility.*` AI-feature, language, .NET parity, .NET interop decision, and guardrail keys now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused platform-model coverage now verifies localized extensibility-profile and policy-gateway prose without changing language ids, AI feature ids, parity ids, reason tags, decision enums, execution-path values, or other machine-readable profile contracts.
- 2026-06-26: Localization/#2598 database federation profile catalog parity shipped: the existing `Platform.Database.*` connector, query-path, and guardrail keys now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused platform-model coverage now verifies localized database profile prose without changing connector ids, connector family values, query-path ids, policy booleans, or other machine-readable profile contracts.
- 2026-06-26: Localization/#2597 report output and asset diagnostic parity shipped: the shared `Runtime.Prg.ReportOutput.Error.PathRequired`, `Runtime.Prg.ReportOutput.Error.OpenFailed`, `Runtime.Prg.ReportOutput.Error.WriteFailed`, and `Runtime.Prg.ReportAsset.Error.ResolveFailed` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, focused shipped-catalog plus PRG control-flow coverage now verifies localized report output and missing-asset failures without changing report/label preview semantics, machine-readable runtime contracts, or normal `REPORT/LABEL ... TO FILE <path>` behavior, and bare `TO FILE` now correctly resolves to the existing localized path-required failure instead of falling through to an output-open error on the current directory.
- 2026-06-26: Localization/#2596 SQL runtime diagnostic parity shipped: the shared `Runtime.Prg.Sql.Error.HandleNotFound` and `Runtime.Prg.Sql.Error.SqlExecRequiresCommandOrPreparedStatement` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog plus PRG control-flow coverage now verifies localized SQL runtime failures without changing `SQLEXEC`/`SQLDISCONNECT` behavior, AERROR payload shape, handle values, or other machine-readable runtime contracts.
- 2026-06-26: Localization/#2595 aggregate command diagnostic parity shipped: the shared `Runtime.Prg.Aggregate.Error.CalculateRequiresAssignments`, `Runtime.Prg.Aggregate.Error.CalculateRequiresAggregateExpression`, and `Runtime.Prg.Aggregate.Error.CountToSingleTarget` diagnostics now have shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog plus PRG control-flow coverage now verifies localized aggregate command failures without changing `CALCULATE`/`COUNT TO` runtime behavior, command semantics, or other machine-readable runtime contracts.
- 2026-06-26: Localization/#2594 query-translator select/from rejection diagnostic parity shipped: the shared `Platform.QueryTranslator.Error.SelectFromOnly` diagnostic now has shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused query-translator, shipped-catalog, and runtime-host coverage now verifies localized deterministic translator rejection payloads, including planner-fallback composition, without changing translator behavior, `runtime.mode: federation-query-plan`, exit codes, backend ids, or other machine-readable contract fields.
- 2026-06-26: Localization/#2593 federation AI planner fallback diagnostic parity shipped: the shared `Platform.FederationExecution.Error.AiPlannerNotImplemented` wrapper now has shipped-catalog parity in `es-419`, `pt-BR`, and `qps-ploc`, and focused shipped-catalog plus runtime-host coverage now verifies localized federation planner-fallback prose without changing `runtime.mode: federation-query-plan`, exit codes, permission gating, unresolved machine-readable payload keys, or other runtime-host contract fields.
- 2026-06-26: Localization/#2592 runtime host security-denial audit-detail parity shipped: the remaining raw-English runtime-host denial/audit detail wrappers for `project.open`, `runtime.admin`, and manifest-hash verification denials now route through existing localized runtime-host prose, focused shipped-catalog plus runtime-host coverage verifies localized console and immutable-audit detail text without changing exit codes, `status: error`, `policy.denied`, permission ids, role ids, or the audit-chain file format.
- 2026-06-26: Localization/#2591 runtime host quit-confirm prompt parity shipped: the interactive `RuntimeHost.Prompt.QuitConfirm` prompt now routes through the runtime-host catalogs in `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, focused shipped-catalog and runtime-host coverage verifies localized quit-confirm prose without changing quit callback semantics or success exit behavior, and the prompt now preserves the live `[y/N]` confirmation tokens across locales, including pseudo-localization.
- 2026-06-26: Localization/#2590 shared runtime watch error parity shipped: the remaining shared `Runtime.Prg.Watch.Error.*` keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused runtime-host plus shipped-catalog coverage now verifies localized paused-watch failures without changing `debug.watch.ok`, `debug.watch.expression`, `debug.reason`, breakpoint behavior, or other machine-readable runtime-host fields.
- 2026-06-26: Localization/#2589 shared runtime pause/session message parity shipped: the remaining shared `Runtime.Prg.Session.*` pause/session messages now exist in `es-419`, `pt-BR`, and `qps-ploc`, `PrgRuntimeSession` now routes pause/session prose through shared runtime catalogs, and the shared runtime localization loader now refreshes when the effective locale or locale-root changes so runtime-host debug flows honor localized session messages without changing `debug.reason`, `status` fields, `READ EVENTS`, file paths, asset paths, or other machine-readable contract data.
- 2026-06-26: Localization/#2588 runtime host manifest verification error catalog parity shipped: the remaining `RuntimeHost.Error.*` manifest verification keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused runtime-host plus shipped-catalog coverage now verifies localized missing-`runtime_host_sha256` and missing-extension-payload failures without changing exit codes, `status: error`, filenames, manifest fields, or other machine-readable contract data.
- 2026-06-26: Localization/#2587 runtime host bridge error catalog parity shipped: the remaining `RuntimeHost.Bridge.Error.*` keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused runtime-host coverage now verifies localized bridge request-artifact failures in `es-419`, `pt-BR`, and `qps-ploc` without changing `runtime.mode: bridge-invocation`, media-type/schema tokens, request/response paths, exit codes, or machine-readable status fields.
- 2026-06-26: Localization/#2586 runtime host debug error catalog parity shipped: the remaining `RuntimeHost.Debug.Error.*` keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused runtime-host coverage now verifies localized invalid-breakpoint, unknown-breakpoint, and watch-paused debug failures without changing breakpoint grammar, file paths, line numbers, debug command tokens, prefixes, exit codes, or machine-readable status fields.
- 2026-06-26: Localization/#2585 runtime host top-level catalog parity shipped: the remaining top-level `RuntimeHost.Usage.*`, `RuntimeHost.Error.*`, and `RuntimeHost.Launch.Note.*` keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused runtime-host coverage now verifies localized usage, top-level command errors, federation validation, and compatibility-note surfaces without changing CLI tokens, placeholders, backend ids, permissions, roles, or machine-readable status fields.
- 2026-06-26: Localization/#2584 shared core catalog parity shipped: the remaining top-level `Command.*`, `Diagnostic.*`, `Error.*`, and `Help.*` keys now exist in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization coverage now verifies real shipped catalogs preserve placeholders, parser tokens, locale identifiers, and pseudo-localization behavior without falling back to raw English for the covered shared-core surfaces.
- 2026-06-26: Localization/#2583 Studio host toolbox parse catalog parity shipped: `StudioHost.ToolboxParse.Error.*` now has catalog entries in `es-419`, `pt-BR`, and `qps-ploc`, and focused Studio host locale checks now verify representative toolbox parse failures no longer fall back to raw English outside `en-US` while preserving CLI option names, selection-context tokens, toolbox-context tokens, `name=value`, `true`/`false`, JSON field names, exit codes, and other machine-readable contracts unchanged.
- 2026-06-26: Localization/#2582 Studio host toolbox-palette parse catalog parity shipped: `StudioHost.ToolboxPaletteParse.Error.*` now has catalog entries in `es-419`, `pt-BR`, and `qps-ploc`, and focused Studio host locale checks now verify representative toolbox-palette parse failures no longer fall back to raw English outside `en-US` while preserving CLI option names, selection-context tokens, toolbox-context tokens, JSON field names, exit codes, and other machine-readable contracts unchanged.
- 2026-06-26: Localization/#2581 Studio host designer parse catalog parity shipped: `StudioHost.DesignerParse.Error.*` now has catalog entries in `es-419`, `pt-BR`, and `qps-ploc`, and focused Studio host locale checks now verify representative designer parse failures no longer fall back to raw English outside `en-US` while preserving CLI option names, selection-context tokens, `true`/`false`, JSON field names, exit codes, and other machine-readable contracts unchanged.
- 2026-06-26: Localization/#2580 Studio host editor-action parse catalog parity shipped: `StudioHost.EditorActionParse.Error.*` now has catalog entries in `es-419`, `pt-BR`, and `qps-ploc`, and focused Studio host locale checks now verify representative editor-action parse failures no longer fall back to raw English outside `en-US` while preserving CLI option names, action ids, selection-context tokens, `true`/`false`, JSON field names, exit codes, and other machine-readable contracts unchanged.
- 2026-06-26: Localization/#2579 inspect-host usage catalog parity shipped: `Inspect.Usage` now has catalog entries in `es-419`, `pt-BR`, and `qps-ploc`, and focused localization coverage now verifies real inspect-host usage text no longer falls back to raw English outside `en-US` while preserving `copperfin_inspect`, `--locale`, placeholder names, exit codes, and machine-readable output fields unchanged.
- 2026-06-26: Localization/#2578 Studio host builder parse error catalog parity shipped: `es-419` and `pt-BR` now cover the remaining common `StudioHost.BuilderParse.Error.*` helper-driven diagnostics for missing values, missing builder/selection context, missing builder id/launch command, unknown builder/selection context tokens, non-negative record validation, boolean validation, and unknown option handling, so representative Studio host builder parse failures no longer fall back to raw English outside `en-US` while CLI option names, builder ids, context tokens, JSON field names, exit codes, and other machine-readable contracts remain unchanged.
- 2026-06-26: Localization/#2577 Studio host builder context-conflict localization shipped: `es-419` and `pt-BR` now localize `StudioHost.BuilderParse.Error.ContextConflict` plus the related builder request-name labels for launch-plan, invocation-admission, dispatch, and execute, so ambiguous builder-context diagnostics no longer fall back to raw English outside `en-US` while CLI option names, builder ids, selection/builder context tokens, JSON field names, exit codes, and other machine-readable contracts remain unchanged.
- 2026-06-26: Localization/#2576 Studio host usage-wrapper localization shipped: `copperfin_studio_host` now has `es-419`, `pt-BR`, and `qps-ploc` catalog parity for `StudioHost.Usage.Primary`, `StudioHost.Usage.Alternate`, `StudioHost.Usage.ObjectEntry`, and `StudioHost.Usage.SelectionContextTokens`, so usage/output wrapper prose no longer falls back to raw English outside `en-US` while CLI switches, option names, placeholder values, selection-context tokens, JSON field names, parser tokens, and other machine-readable contracts remain unchanged.
- 2026-06-26: Localization/#2575 Studio host data-source object-usage localization shipped: the remaining hard-coded `copperfin_studio_host` usage labels for record-source-type, column-order, highlight-style, child-order, fill-color, list-item-id, tab-orientation, display-orientation, help-context-id, record-source, form-set-class, default-file-path, and initial-selected-alias object assignments now route through `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `en-US` title normalization to preserve existing English usage text and `es-419`, `pt-BR`, and `qps-ploc` catalog coverage added while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2574 Studio host row/grid object-usage localization shipped: the remaining hard-coded `copperfin_studio_host` usage labels for row-height, lock-columns, lock-columns-left, grid-line-width, grid-lines, highlight-row-line-width, and partition object assignments now route through existing `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for those labels while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2573 Studio host numeric/grid object-usage localization shipped: the remaining hard-coded `copperfin_studio_host` usage labels for button-count, curvature, draw-mode/style/width, fill-style, scale-mode, buffer-mode, buffer-mode-override, data-session, grid-line-color, and header-height object assignments now route through existing `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for those labels while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2572 Studio host dynamic/font object-usage localization shipped: the remaining hard-coded `copperfin_studio_host` usage labels for dynamic-input-mask, dynamic-line-height, dynamic-alignment, font, and max-position/max-width object assignments now route through existing `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for those labels while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2571 Studio host window/chrome object-usage localization shipped: the remaining hard-coded `copperfin_studio_host` usage labels for window/chrome object assignments now route through existing `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for desktop, bind-controls, auto-verb-menu, sizing, border, window-state, mouse-pointer, and picture-display labels while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2570 Studio host color/display object-usage localization shipped: the remaining hard-coded Studio host usage labels for the display/color object-assignment block now route through existing `StudioHost.LaunchParse.ObjectAssignment.*Title` localization keys, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for those labels while preserving CLI switches, option values, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2569 Studio host object-usage label localization shipped: the remaining hard-coded Studio host usage labels for OLE drop-mode, OLE drop-effects, OLE drop text-insertion, WhatsThis help ID, WhatsThis help, and WhatsThis button now route through existing object-assignment localization keys in `copperfin_studio_host`, with `es-419`, `pt-BR`, and `qps-ploc` catalog parity added for those labels while preserving CLI switches, usage templates, JSON field names, parser tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2568 Studio host builder request-label localization shipped: builder parse context-conflict diagnostics in `copperfin_studio_host` now route the human-facing request labels for launch-plan, invocation-admission, dispatch, and execute through localization catalog keys for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, while preserving exit codes, `status:` lines, CLI switches, JSON field names, builder ids, selection/context tokens, and other machine-readable host contracts.
- 2026-06-26: Localization/#2567 Studio host prefix localization shipped: `copperfin_studio_host` now routes its remaining human-facing text-mode `error:` and `warning:` prefixes through the native Studio localization catalogs for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, loads localization early enough for process-hardening fallback warnings, and preserves Studio host exit codes, `status:` lines, CLI switches, JSON field names, builder/designer/runtime machine-readable contracts, and invariant selection/context tokens across locales.
- 2026-06-26: Localization/#2566 runtime host prefix localization shipped: `copperfin_runtime_host` now routes its remaining human-facing `error:` and `warning:` prefixes through the runtime-host localization catalogs for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, loads localization early enough for process-hardening fallback warnings, preserves runtime/debug/bridge machine-readable contracts, and seeds `COPPERFIN_LOCALE_DIR` from the resolved executable-path catalog root so downstream shared localized diagnostics still resolve correctly when the host runs from another working directory.
- 2026-06-26: Localization/#2565 inspect host prefix localization shipped: the remaining human-facing `error:` and `warning:` prefixes in `copperfin_inspect` now route through the inspect/localization catalog for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, while preserving inspect exit codes, `status:` lines, asset-inspection field names, and other machine-readable output.
- 2026-06-26: Localization/#2563 build host prefix localization shipped: the remaining human-facing `error:` and `warning:` prefixes in `copperfin_build_host` now route through the build-host localization catalog for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, while preserving exit codes, `status:` lines, CLI/build/package contracts, and the already-localized underlying message bodies.
- 2026-06-26: Localization/#2561 runtime package-plan warning localization shipped: shared runtime package/build-plan warnings for unavailable project workspace, unknown requested security-role fallback, missing project assets, unresolved startup/debug startup sources, unavailable native security or .NET output profiles, missing library export discovery, and missing FXP PRG discovery now route through the portable runtime localization catalog for `en-US`, `es-419`, `pt-BR`, and `qps-ploc` while preserving package-plan behavior, manifest/debug-manifest keys, paths, launcher/output tokens, and other machine-readable contracts.
- 2026-06-26: Localization/#2560 Studio host execution fallback error localization shipped: builder, editor-action, toolbox, and aggregate designer child-execution fallback errors now route through the native Studio localization catalog for `en-US`, `es-419`, `pt-BR`, and `qps-ploc`, while preserving exit codes, observed child-process status, JSON field names, command tokens, and default machine-readable host contracts.
- 2026-06-26: Localization/#2559 shared .NET `qps-ploc` pseudo-localization support shipped: `CopperfinLocalization` now recognizes `qps-ploc`, reuses the English key set for parity checks, pseudo-localizes shared VSIX/Studio catalog-backed strings while preserving placeholder tokens and formatted runtime values, and extends focused managed coverage with language-service and WinForms smoke tests so hard-coded UI in the shared .NET designer stack stands out without changing machine-readable keys, enum values, JSON/contracts, or default `en-US` behavior.
- 2026-06-26: Localization/#2558 VSIX summary prose localization shipped: the shared .NET `CopperfinLocalization` catalogs now cover remaining Visual Studio summary headings, labels, empty-state prose, reusable indented summary lines, the report/layout design-surface no-layout empty state, localized excluded suffixes surfaced by project insights, and VSIX dialog titles for the localized project/workflow and Studio launch flows; `CopperfinAssetEditorControl`, `CopperfinDesignSurfaceControl`, `CopperfinProjectCommands`, `OpenInCopperfinStudioCommand`, `CopperfinProjectWorkflow`, `CopperfinRuntimeDebugClient`, `CopperfinStudioSnapshotClient`, and `CopperfinProjectInsightClient` now route those user-facing strings through catalog keys while preserving debugger commands, host/runtime field names, JSON/contracts, and default `en-US` behavior; focused validation ran `git diff --check` plus `dotnet run --project vsix/Copperfin.LanguageServiceTests/Copperfin.LanguageServiceTests.csproj -c Release --no-restore`.
- 2026-06-26: Localization/#2557 table-maintenance dispatch work-area error localization shipped: `REPLACE`, `UPDATE`, `APPEND BLANK`, `DELETE`, `DELETE FROM`, `RECALL`, `INSERT INTO`, `PACK`, `ZAP`, and `UNLOCK` work-area precondition errors now route through the portable C++ localization catalog with a named `{command}` placeholder while preserving command names, cursor targeting semantics, parser/runtime state, event contracts, and default en-US output.
- 2026-06-26: Localization/#2556 dispatch target-work-area error localization shipped: `GO`, `SKIP`, `BROWSE`, `SET ORDER`, and `SELECT` target work-area precondition errors now route through the portable C++ localization catalog with named `{command}` placeholders while preserving command names and parser/runtime state.
- 2026-06-26: Localization/#2555 dispatch work-area command localization shipped: `SEEK`, `LOCATE`, and `SCAN` target work-area precondition errors now route through the portable C++ localization catalog with a named `{command}` placeholder while preserving VFP command names, cursor selection semantics, event categories, parser/dispatch behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2554 DO/CALL target resolution error localization shipped: `DO` and `CALL` target resolution errors now route through the portable C++ localization catalog with named `{command}` and `{target}` placeholders while preserving VFP command names, target paths/names, parser behavior, event categories, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2553 async task dispatch error localization shipped: `YIELD`, `SPAWN`, and `AWAIT` async task command validation errors now route through the portable C++ localization catalog with named `{command}`, `{target}`, and `{handle}` placeholders while preserving VFP command names, task handles, targets, event categories, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2552 xAsset bootstrap materialization error localization shipped: xAsset bootstrap materialization failures now route through the portable C++ localization catalog with a named `{path}` placeholder while preserving asset paths, generated bootstrap names, xAsset identifiers, event categories, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2551 runtime cancellation and fault message localization shipped: async task cancellation, runtime resource faults, generic exception faults, and unknown exception faults now route through the portable C++ localization catalog with named `{detail}` placeholders while preserving event categories, debug pause reasons, exception detail values, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2550 native DLL proc-address error localization shipped: missing native DLL proc-address errors now route through the portable C++ localization catalog with a named `{functionName}` placeholder while preserving declared function names, DLL paths, interop event categories, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2549 .NET CLR interop error localization shipped: CLR creation/runtime discovery/host/default-domain, AppDomain dispatch, assembly load/dispatch, type/method lookup, and method invocation errors now route through the portable C++ localization catalog with named HRESULT/path/type/method placeholders while preserving COM/CLR API names, HRESULT values, assembly paths, type and method names, interop event categories, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2548 FOXTOOLS API registration error localization shipped: FOXTOOLS load and registered API handle precondition errors now route through the portable C++ localization catalog with a named `{handle}` placeholder while preserving the `FOXTOOLS` product token, API handle values, interop event categories, function names, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2547 record table-maintenance cursor error localization shipped: `APPEND BLANK`, `PACK`, and `ZAP` local table-backed cursor precondition errors now route through the portable C++ localization catalog with a named `{command}` placeholder while preserving VFP command names, parser behavior, cursor state, event categories, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2546 record constraint and `INSERT INTO` error localization shipped: NOT NULL field validation and selected `INSERT INTO` target-field/count validation errors now route through the portable C++ localization catalog with named `{constraint}`, `{fieldName}`, and `{command}` placeholders while preserving VFP command names, field names, parser behavior, cursor state, event categories, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2545 runtime surface cursor XML warning localization shipped: `CURSORTOXML()` and `XMLTOCURSOR()` cursor snapshot/load, target cursor, output write, input/alias, XML parse, and materialization warnings now route through the portable C++ localization catalog with named `{function}` and `{capability}` placeholders while preserving VFP function names, cursor callback capability tokens, runtime event categories, parser behavior, path handling, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2544 runtime surface object/member warning localization shipped: `AMEMBERS()`, `ACLASS()`, `PEMSTATUS()`, `ADDPROPERTY()`, `GETPEM()`, `SETPEM()`, and `REMOVEPROPERTY()` stub/fallback warnings now route through the portable C++ localization catalog with named `{function}` and `{capability}` placeholders while preserving VFP function names, callback capability tokens, runtime event categories, parser behavior, machine-readable state, and default en-US output.
- 2026-06-26: Localization/#2543 bitwise runtime range error localization shipped: bit-position range error prose now routes through the portable C++ localization catalog with named `{minimum}` and `{maximum}` placeholders while preserving numeric bounds, function behavior, parser tokens, event categories, runtime state fields, and default en-US output.
- 2026-06-26: Localization/#2542 record mutation precondition localization shipped: selected cursor/record precondition errors, lock-retry cancellation, and remote SQL cursor field-not-found prose now route through the portable C++ localization catalog with named `{command}` and `{fieldName}` placeholders while preserving command names, field names, parser behavior, event categories, runtime state fields, and default en-US output.
- 2026-06-26: Localization/#2541 expression evaluator runtime error localization shipped: integer division-by-zero, general division-by-zero, and missing function-argument error prose now routes through the portable C++ localization catalog while preserving parser tokens, source function names, diagnostic identities, runtime state fields, event categories, and default en-US output.
- 2026-06-26: Localization/#2540 numeric runtime domain error localization shipped: `LOG()`, `LOG10()`, `ASIN()`, and `ACOS()` domain error prose now routes through the portable C++ localization catalog with named `{function}` and `{value}` placeholders while preserving function names, numeric argument values, parser behavior, runtime result semantics, event contracts, and default en-US output.
- 2026-06-26: Localization/#2539 build-host catalog parity shipped: build-host usage, parse diagnostics, and process-hardening warning labels now have portable C++ catalog entries across `en-US`, `es-419`, `pt-BR`, and `qps-ploc` while preserving command names, CLI options, configuration tokens, machine-readable status fields, output-field labels, placeholder values, and default en-US output.
- 2026-06-25: Localization/#2538 local `USE` target resolution error localization shipped: missing table target error prose now routes through the portable C++ localization catalog with a named `{path}` placeholder while preserving FoxPro/VFP `USE`, `ALIAS`, `IN`, and `USE AGAIN` syntax, table path and `.dbf` extension resolution, DBF parsing, work-area allocation, runtime error control flow, file paths, and default en-US output.
- 2026-06-25: Localization/#2537 report asset resolution error localization shipped: missing report/label asset error prose now routes through the portable C++ localization catalog with a named `{path}` placeholder while preserving FoxPro/VFP `REPORT FORM`/`LABEL FORM` and `PREVIEW` syntax, asset path resolution, event-loop behavior, runtime error control flow, file paths, and default en-US output.
- 2026-06-25: Localization/#2536 report output runtime error localization shipped: missing `TO FILE` output path, output-open failure, and output-write failure prose now route through the portable C++ localization catalog with named `{path}` placeholders while preserving FoxPro/VFP `REPORT FORM`/`LABEL FORM`, `PREVIEW`, and `TO FILE` syntax, report/label render behavior, output artifacts, event contracts, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2535 transaction journal runtime error localization shipped: transaction journal initialization, state persistence, backup creation, backup-journal persistence, and replay failure error prose now route through the portable C++ localization catalog with a named `{path}` placeholder while preserving FoxPro/VFP transaction syntax, nesting, data-session behavior, journal replay, cursor refresh, event contracts, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2534 critical-section runtime error localization shipped: blocking-policy, descending acquire-order, unknown-section, LIFO-exit, and missing-mutex error prose now routes through the portable C++ localization catalog with named operation/section placeholders while preserving FoxPro/VFP `ENTER CRITICAL`/`EXIT CRITICAL` syntax, normalized section policy, event details, task/lock behavior, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2533 command undo runtime error localization shipped: command undo journal initialization, backup creation, persistence, replay, and empty `UNDO` stack errors now route through the portable C++ localization catalog with a named `{path}` placeholder while preserving FoxPro/VFP `UNDO`/`UNDO ALL` syntax, command undo stack behavior, journal replay behavior, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2532 SQL runtime error localization shipped: missing SQL handle messages and empty `SQLEXEC` command/prepared-statement errors now route through the portable C++ localization catalog with a named `{handle}` placeholder while preserving SQL function names, handle/session semantics, AERROR details, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2531 runtime cursor `USE` and `SEEK` error localization shipped: duplicate alias/table, `USE IN` target lookup, missing order, and `SEEK` prerequisite messages now route through the portable C++ localization catalog with named placeholders while preserving FoxPro/VFP cursor/search syntax, cursor state, order/search behavior, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2530 runtime watch-evaluation message localization shipped: empty-expression, unpaused-frame, out-of-memory, and fallback watch messages now route through the portable C++ localization catalog while preserving watch expression parsing, evaluated values, exception-specific expression errors, `RuntimeWatchResult` fields, and default en-US output.
- 2026-06-25: Localization/#2529 aggregate `CALCULATE` and `COUNT TO` error localization shipped: missing-assignment, malformed aggregate-expression, and multi-target `COUNT TO` error prose now route through the portable C++ localization catalog while preserving FoxPro/VFP command syntax, aggregate semantics, variable targeting, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2528 runtime `TOTAL` command error localization shipped: parser and execution error prose now routes through a reusable `cf_xbase_runtime` localization helper with a named `{fieldName}` placeholder while preserving FoxPro/VFP `TOTAL` syntax, work-area targeting, field names, cursor behavior, runtime error control flow, and default en-US output.
- 2026-06-25: Localization/#2527 PRG static-analysis diagnostic message localization shipped: PRG0001, PRG1001, and PRG1002 message prose now routes through the portable C++ localization catalog while preserving diagnostic codes, severity values, file/line locations, parser behavior, FoxPro/VFP syntax tokens, and default en-US output.
- 2026-06-25: Localization/#2526 runtime Rushmore/index-seek rationale localization shipped: index-expression pattern reasons, candidate match reasons, and plan decision rationale text now route through the portable C++ localization catalog with named field/order placeholders while preserving optimizer enums, field/order identifiers, runtime planning behavior, event/detail contracts, and default en-US output.
- 2026-06-25: Localization/#2525 Studio launch z-order assignment label localization shipped: always-on-top and always-on-bottom diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2524 Studio launch marker/sizing assignment label localization shipped: allow-cell-selection, hide-selection, delete-mark, record-mark, split-bar, highlight-row, panel-link, allow-header-sizing, allow-row-sizing, resizable, and add-line-feeds diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2523 Studio launch max/auto assignment label localization shipped: max-width, max-left, max-top, auto-center, auto-size, auto-release, continuous-scroll, dockable, clip-controls, sparse, and lock-screen diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2522 Studio launch font assignment label localization shipped: font-name, font-size, font-bold, font-italic, font-underline, font-strikethru, font-outline, and font-shadow diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2521 Studio launch dynamic assignment label localization shipped: dynamic-input-mask, dynamic-line-height, dynamic-alignment, dynamic-current-control, dynamic-font-name, dynamic-font-size, dynamic-font-bold, dynamic-font-italic, dynamic-font-underline, dynamic-font-strikethru, dynamic-font-outline, and dynamic-font-shadow diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, expression text, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2520 Studio launch visual state assignment label localization shipped: special-effect, scroll-bars, window-state, show-window, title-bar, mouse-pointer, picture-margin, picture-position, picture-spacing, and picture-selection-display diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2519 Studio launch bounds/style assignment label localization shipped: min-height, min-width, max-height, movable, half-height-caption, MDI-form, back-style, border-style, border-width, and border-color diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2518 Studio launch window flag assignment label localization shipped: closable, control-box, allow-output, bind-controls, auto-verb-menu, desktop, key-preview, mac-desktop, max-button, and min-button diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2517 Studio launch general color assignment label localization shipped: highlight-back-color, highlight-fore-color, back-color, fore-color, disabled-back-color, disabled-fore-color, dynamic-back-color, and dynamic-fore-color diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2516 Studio launch item color assignment label localization shipped: selected-back-color, selected-fore-color, selected-item-back-color, selected-item-fore-color, disabled-item-back-color, disabled-item-fore-color, item-back-color, and item-fore-color diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2515 Studio launch list scalar assignment label localization shipped: row-source-type, bound-column, column-count, style, list-index, left-column, display-value, and list scalar value diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2514 Studio launch deleted-state request label localization shipped: deleted-states, deleted-state target, subtree deleted-state, target selector, and root selector diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, deleted-state request semantics, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2513 Studio launch row-source/list assignment label localization shipped: row-source, column-widths, column-lines, integral-height, incremental-search, and multi-select diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2512 Studio launch text-binding assignment label localization shipped: record source, tooltip text, status-bar text, link-master, control-source, current-control, and input-mask diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2511 Studio launch partition/list assignment label localization shipped: partition, record-source-type, column-order, highlight-style, child-order, fill-color, and list-item-id diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2510 Studio launch grid assignment label localization shipped: grid-line-color, header-height, row-height, lock-columns, lock-columns-left, grid-line-width, grid-lines, and highlight-row-line-width diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2509 Studio launch drawing and buffer assignment label localization shipped: button-count, curvature, draw-mode, draw-style, draw-width, fill-style, scale-mode, buffer-mode, buffer-mode-override, and data-session diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2508 Studio launch OLE and drag assignment label localization shipped: OLE drag-picture, mouse-icon, drag-icon, drag-mode, OLE drag-mode, OLE drop-mode, OLE drop-effects, and OLE drop text-insertion diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2507 Studio launch core state/media assignment label localization shipped: tab-order, starting tab index, tab-stop, visibility, enabled, read-only, locked, caption, picture, down-picture, and disabled-picture diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2506 Studio launch form metadata assignment label localization shipped: form-set-class, default-file-path, initial-selected-alias, tab/display orientation, help-context-id, WhatsThis help, WhatsThis help ID, and WhatsThis button diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, assignment flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2505 Studio launch reorder and format diagnostic label localization shipped: the remaining small `reorder` object-command label and `format` object-assignment labels now route through the portable C++ localization catalog while preserving CLI option names, command flags, placement/format values, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2504 Studio launch property-command diagnostic label localization shipped: update, clear, and rename property-command diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, property command flags, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2503 Studio launch layout action diagnostic label localization shipped: align/alignment, resize, distribution, snap, and nudge diagnostic labels now route through the portable C++ localization catalog while preserving CLI option names, layout action command tokens, mode values, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2502 Studio launch object-command option-list localization shipped: `--rename-object` and `--reparent-object` required-option list grammar and affected object-command labels now route through the portable C++ localization catalog while preserving CLI option names, object command tokens, JSON/status contracts, parser behavior, mutation safety, and default en-US output.
- 2026-06-25: Localization/#2501 Studio launch selection-context allowed-value list grammar localization shipped: the `--selection-context` allowed-values list grammar now routes through the portable C++ localization catalog while preserving selection-context tokens, CLI option names, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2500 visual asset group-container error localization shipped: the unavailable-created-group-container error now routes through the portable C++ localization catalog while preserving visual object ids, object names, unique ids, parent names, field names, grouping/container creation/rollback behavior, table and memo write behavior, and default en-US output.
- 2026-06-25: Localization/#2499 Studio document fallback record title localization shipped: synthetic fallback document object titles now route through the portable C++ localization catalog with a named `{recordIndex}` placeholder while preserving source record indexes, object identifiers, object kind/identity fields, field names, field indexes, memo provenance, geometry metadata, containment behavior, and default en-US output.
- 2026-06-25: Localization/#2498 report layout fallback record title localization shipped: synthetic fallback report/label object titles now route through the portable C++ localization catalog with a named `{recordIndex}` placeholder while preserving record indexes, object kind values, band kind values, field names, field indexes, memo provenance, geometry, containment behavior, and default en-US output.
- 2026-06-25: Localization/#2497 Studio project fallback record title localization shipped: synthetic fallback project item titles now route through the portable C++ localization catalog with a named `{recordIndex}` placeholder while preserving record indexes, project item type codes, group ids, paths, field names, memo provenance, field indexes, record selection behavior, build-plan behavior, and default en-US output.
- 2026-06-25: Localization/#2496 Studio project item type and group label localization shipped: project workspace item type titles and group titles now route through the portable C++ localization catalog while preserving project item type codes, group ids, file extensions, paths, field names, memo provenance, field indexes, record selection behavior, build-plan behavior, and default en-US output.
- 2026-06-25: Localization/#2495 Studio project build target label localization shipped: project workspace build-plan target labels now route through the portable C++ localization catalog while preserving `output_kind` machine values, project item type codes, paths, field names, memo provenance, field indexes, record selection behavior, and default en-US output.
- 2026-06-25: Localization/#2494 DBF header version description localization shipped: DBF-family header version/type prose now routes through the portable C++ localization catalog while preserving DBF version bytes, parse behavior, diagnostic keys, asset-inspection contracts, FoxPro/VFP semantics, and default en-US output.
- 2026-06-25: Localization/#2493 extensibility interop decision reason localization shipped: .NET interop policy decision reason prose now routes through the portable C++ localization catalog while preserving `DotNetInteropDecision` enum values, execution path tokens, capability ids, allowlist/denylist ids, latency budgets, policy behavior, and default en-US output.
- 2026-06-25: Localization/#2492 extensibility profile display text localization shipped: language integration titles/mode/boundary/story prose, AI tooling titles/descriptions/boundary prose, .NET output primary story, .NET parity titles/rationales, and extensibility guardrails now route through the portable C++ localization catalog while preserving language ids, AI feature ids, parity ids, reason tags, allowlist/denylist ids, verification references, enum values, booleans, policy behavior, and default en-US output.
- 2026-06-25: Localization/#2491 database federation profile display text localization shipped: connector display titles, access-mode labels, schema-shape labels, translation stories, query path titles/source-target/complexity labels and strategy prose, and database guardrails now route through the portable C++ localization catalog while preserving connector ids/family values, query path ids, booleans, API behavior, and default en-US output.
- 2026-06-25: Localization/#2490 native security profile display text localization shipped: security profile mode/policy summaries, permission/role/provider/feature titles and descriptions, and hardening profile summaries now route through the portable C++ localization catalog while preserving permission ids, role ids, provider ids/kinds, feature ids, audit event ids, manifest counts/fields, authorization behavior, and default en-US output.
- 2026-06-25: Localization/#2489 report layout section title localization shipped: report/label section display titles for title, page/column/group/detail bands, summary, detail header/footer, and fallback bands now route through the portable C++ localization catalog while preserving `band_kind` values, raw FRX/LBX `OBJCODE` semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2488 Studio launch residual core parse diagnostics localization shipped: line and column unsigned-integer validation, undo-mode allowed-value validation, unknown/extra argument, missing asset path, property/object required-option, and group-only selector diagnostics now route through the portable C++ localization catalog while preserving CLI option names, command names, argument text, allowed mode tokens, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2487 Studio launch extended form metadata diagnostics localization shipped: form-set-class, default-file-path, initial-selected-alias, tab-orientation, display-orientation, help-context-id, whats-this-help-id, whats-this-help, and whats-this-button parser missing-value, integer, non-negative, and logical-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, form metadata/help assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2486 Studio launch core selector and undo missing-value diagnostics localization shipped: object-name, unique-id, line, column, undo-mode, and undo-label missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, object selector/source-location/undo assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2485 Studio launch marker and z-order target selector diagnostics localization shipped: delete-mark, record-mark, split-bar, highlight-row, panel-link, allow-header-sizing, allow-row-sizing, resizable, add-line-feeds, always-on-top, and always-on-bottom target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, marker/sizing/z-order assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2484 Studio launch max and selection target selector diagnostics localization shipped: max-width, max-left, max-top, auto-size, auto-release, continuous-scroll, dockable, clip-controls, sparse, lock-screen, allow-cell-selection, and hide-selection target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, max/auto/selection assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2483 Studio launch font target selector diagnostics localization shipped: font-name, font-size, font-bold, font-italic, font-underline, font-strikethru, font-outline, and font-shadow target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, font assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2482 Studio launch dynamic target selector diagnostics localization shipped: dynamic-input-mask, dynamic-line-height, dynamic-alignment, dynamic-current-control, dynamic-font-name, dynamic-font-size, dynamic-font-bold, dynamic-font-italic, dynamic-font-underline, dynamic-font-strikethru, dynamic-font-outline, and dynamic-font-shadow target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, dynamic expression/font assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2481 Studio launch window and picture target selector diagnostics localization shipped: special-effect, scroll-bars, window-state, show-window, title-bar, mouse-pointer, picture-margin, picture-position, picture-spacing, and picture-selection-display target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, window/display and picture-layout assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2480 Studio launch dimension and border target selector diagnostics localization shipped: min-height, min-width, max-height, movable, half-height-caption, MDI-form, back-style, border-style, border-width, and border-color target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, dimension/border assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2479 Studio launch form boolean target selector diagnostics localization shipped: closable, control-box, allow-output, auto-center, auto-verb-menu, bind-controls, desktop, key-preview, mac-desktop, max-button, and min-button target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, boolean assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2478 Studio launch color target selector value diagnostics localization shipped: selected/back/fore item color, disabled item color, item color, highlight color, back/fore color, disabled back/fore color, and dynamic back/fore color target selector missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, color/dynamic-color assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2477 Studio launch deleted-state value diagnostics localization shipped: deleted-state-target-object-name, deleted-state-target-unique-id, deleted-state, and subtree-deleted missing-value, true/false, and dependency diagnostics now route through the portable C++ localization catalog while preserving CLI option names, true/false tokens, deleted-state assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2476 Studio launch row-source-type and list index target selector value diagnostics localization shipped: row-source-type-target-object-name, row-source-type-target-unique-id, bound-column-target-object-name, bound-column-target-unique-id, column-count-target-object-name, column-count-target-unique-id, style-target-object-name, style-target-unique-id, list-index-target-object-name, list-index-target-unique-id, left-column-target-object-name, left-column-target-unique-id, display-value-target-object-name, and display-value-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, row-source-type/list selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2475 Studio launch row source list target selector value diagnostics localization shipped: row-source-target-object-name, row-source-target-unique-id, column-widths-target-object-name, column-widths-target-unique-id, column-lines-target-object-name, column-lines-target-unique-id, integral-height-target-object-name, integral-height-target-unique-id, incremental-search-target-object-name, incremental-search-target-unique-id, multi-select-target-object-name, and multi-select-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, row-source/list selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2474 Studio launch record-source text target selector value diagnostics localization shipped: record-source-target-object-name, record-source-target-unique-id, tooltip-text-target-object-name, tooltip-text-target-unique-id, status-bar-text-target-object-name, status-bar-text-target-unique-id, link-master-target-object-name, link-master-target-unique-id, control-source-target-object-name, control-source-target-unique-id, current-control-target-object-name, current-control-target-unique-id, input-mask-target-object-name, input-mask-target-unique-id, format-target-object-name, and format-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, record-source/text binding selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2473 Studio launch partition and list target selector value diagnostics localization shipped: partition-target-object-name, partition-target-unique-id, record-source-type-target-object-name, record-source-type-target-unique-id, column-order-target-object-name, column-order-target-unique-id, highlight-style-target-object-name, highlight-style-target-unique-id, child-order-target-object-name, child-order-target-unique-id, fill-color-target-object-name, fill-color-target-unique-id, list-item-id-target-object-name, and list-item-id-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, partition/list selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2472 Studio launch grid target selector value diagnostics localization shipped: grid-line-color-target-object-name, grid-line-color-target-unique-id, header-height-target-object-name, header-height-target-unique-id, row-height-target-object-name, row-height-target-unique-id, lock-columns-target-object-name, lock-columns-target-unique-id, lock-columns-left-target-object-name, lock-columns-left-target-unique-id, grid-line-width-target-object-name, grid-line-width-target-unique-id, grid-lines-target-object-name, grid-lines-target-unique-id, highlight-row-line-width-target-object-name, and highlight-row-line-width-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, grid selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2471 Studio launch drawing and buffer target selector value diagnostics localization shipped: draw-width-target-object-name, draw-width-target-unique-id, fill-style-target-object-name, fill-style-target-unique-id, scale-mode-target-object-name, scale-mode-target-unique-id, buffer-mode-target-object-name, buffer-mode-target-unique-id, buffer-mode-override-target-object-name, buffer-mode-override-target-unique-id, data-session-target-object-name, and data-session-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, drawing/buffer selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2470 Studio launch OLE drop and drawing target selector value diagnostics localization shipped: ole-drop-effects-target-object-name, ole-drop-effects-target-unique-id, ole-drop-text-insertion-target-object-name, ole-drop-text-insertion-target-unique-id, button-count-target-object-name, button-count-target-unique-id, curvature-target-object-name, curvature-target-unique-id, draw-mode-target-object-name, draw-mode-target-unique-id, draw-style-target-object-name, and draw-style-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, OLE drop/drawing selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2469 Studio launch mouse and drag target selector value diagnostics localization shipped: mouse-icon-target-object-name, mouse-icon-target-unique-id, drag-icon-target-object-name, drag-icon-target-unique-id, drag-mode-target-object-name, drag-mode-target-unique-id, ole-drag-mode-target-object-name, ole-drag-mode-target-unique-id, ole-drop-mode-target-object-name, and ole-drop-mode-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, mouse/drag selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2468 Studio launch media target selector value diagnostics localization shipped: caption-target-object-name, caption-target-unique-id, picture-target-object-name, picture-target-unique-id, down-picture-target-object-name, down-picture-target-unique-id, disabled-picture-target-object-name, disabled-picture-target-unique-id, ole-drag-picture-target-object-name, and ole-drag-picture-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, caption/media selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2467 Studio launch state target selector value diagnostics localization shipped: tab-order-target-object-name, tab-order-target-unique-id, tab-stop-target-object-name, tab-stop-target-unique-id, visibility-target-object-name, visibility-target-unique-id, enabled-target-object-name, enabled-target-unique-id, read-only-target-object-name, read-only-target-unique-id, locked-target-object-name, and locked-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, tab/state selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2466 Studio launch layout target selector value diagnostics localization shipped: anchor-object-name, anchor-unique-id, align-target-object-name, align-target-unique-id, resize-target-object-name, resize-target-unique-id, distribute-target-object-name, distribute-target-unique-id, snap-target-object-name, snap-target-unique-id, nudge-target-object-name, and nudge-target-unique-id missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, layout selector assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2465 Studio launch color value diagnostics localization shipped: selected-back-color, selected-fore-color, selected-item-back-color, selected-item-fore-color, disabled-item-back-color, disabled-item-fore-color, item-back-color, item-fore-color, highlight-back-color, highlight-fore-color, back-color, fore-color, disabled-back-color, and disabled-fore-color missing and integer validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, color assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2464 Studio launch marker/sizing/z-order value diagnostics localization shipped: delete-mark, record-mark, split-bar, highlight-row, panel-link, allow-header-sizing, allow-row-sizing, resizable, add-line-feeds, always-on-top, and always-on-bottom missing and true/false validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, marker/sizing/z-order assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2463 Studio launch max/auto selection value diagnostics localization shipped: max-width, max-left, max-top, auto-size, auto-release, continuous-scroll, dockable, clip-controls, sparse, lock-screen, allow-cell-selection, and hide-selection missing, integer, not-negative, and true/false validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, max/auto/selection assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2462 Studio launch font value diagnostics localization shipped: font-name, font-size, font-bold, font-italic, font-underline, font-strikethru, font-outline, and font-shadow missing, numeric, not-negative, and true/false validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, font assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2461 Studio launch dynamic expression value diagnostics localization shipped: dynamic-input-mask, dynamic-line-height, dynamic-alignment, dynamic-current-control, dynamic-font-name, dynamic-font-size, dynamic-font-bold, dynamic-font-italic, dynamic-font-underline, dynamic-font-strikethru, dynamic-font-outline, and dynamic-font-shadow missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, dynamic expression assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2460 Studio launch form appearance value diagnostics localization shipped: special-effect, scroll-bars, window-state, show-window, title-bar, mouse-pointer, picture-margin, picture-position, picture-spacing, and picture-selection-display missing, integer, and not-negative validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, numeric parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2459 Studio launch bounds/border value diagnostics localization shipped: min-height, min-width, max-height, movable, half-height-caption, MDI-form, back-style, border-style, border-width, and border-color missing, integer, not-negative, and true/false validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, numeric/boolean parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2458 Studio launch window flag value diagnostics localization shipped: closable, control-box, allow-output, auto-center, auto-verb-menu, bind-controls, desktop, key-preview, mac-desktop, max-button, and min-button missing and true/false validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, boolean parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2457 Studio launch display/dynamic color value diagnostics localization shipped: display-value, dynamic-back-color, and dynamic-fore-color missing-value diagnostics now route through the portable C++ localization catalog while preserving CLI option names, string assignment semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2456 Studio launch record/list value diagnostics localization shipped: partition, record-source-type, column-order, highlight-style, child-order, fill-color, and list-item-id missing, integer, and non-negative validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, record/list numeric parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2455 Studio launch grid value diagnostics localization shipped: grid-line-color, header-height, row-height, lock-columns, lock-columns-left, grid-line-width, grid-lines, and highlight-row-line-width missing, integer, and non-negative validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, grid/header/row numeric parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2454 Studio launch drawing/buffer value diagnostics localization shipped: button-count, curvature, draw-mode, draw-style, draw-width, fill-style, scale-mode, buffer-mode, buffer-mode-override, and data-session missing, integer, and non-negative validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, drawing/buffer/session numeric parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2453 Studio launch drag/OLE value diagnostics localization shipped: drag-mode, OLE drag-mode, OLE drop-mode, OLE drop-effects, and OLE drop text-insertion missing, integer, and non-negative validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, drag/OLE/drop numeric parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2452 Studio launch list scalar value diagnostics localization shipped: multi-select boolean validation and row-source-type, bound-column, column-count, style, list-index, and left-column integer validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, list scalar parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2451 Studio launch text/media/source/list value diagnostics localization shipped: caption, picture, OLE/media/icon, record/source, tooltip/status/control, row-source, column-width, and column-lines/integral-height/incremental-search boolean validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, text/media/source/list parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2450 Studio launch layout/state value diagnostics localization shipped: layout mode missing values, grid/delta numeric validation, starting-tab-index integer validation, and tab/visibility/enabled/read-only/locked boolean validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, numeric/boolean parsing semantics, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2449 Studio launch core argument value diagnostics localization shipped: missing values after core launch options from path through field-value, invalid selection-context, invalid record integer, and field-value name=value syntax diagnostics now route through the portable C++ localization catalog while preserving CLI option names, selection-context tokens, syntax tokens, JSON/status contracts, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2448 Studio launch command-mode diagnostics localization shipped: anchor-selector mode, duplicate property command, duplicate object command, and mixed object/property command validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, command exclusivity semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2447 Studio launch sizing/z-order diagnostics localization shipped: resizable, add-line-feeds, always-on-top, and always-on-bottom validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, boolean semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2446 Studio launch selection/marker diagnostics localization shipped: lock-screen, hide-selection, allow-cell-selection, delete-mark, record-mark, split-bar, highlight-row, panel-link, allow-header-sizing, and allow-row-sizing validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, boolean semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2445 Studio launch max/auto behavior diagnostics localization shipped: max-width, max-left, max-top, auto-center, auto-size, auto-release, continuous-scroll, dockable, clip-controls, and sparse validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, numeric/boolean semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2444 Studio launch font diagnostics localization shipped: dynamic-font-outline, dynamic-font-shadow, font-name, font-size, font-bold, font-italic, font-underline, font-strikethru, font-outline, and font-shadow validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, font property semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2443 Studio launch dynamic expression diagnostics localization shipped: dynamic-input-mask, dynamic-line-height, dynamic-alignment, dynamic-current-control, dynamic-font-name, dynamic-font-size, dynamic-font-bold, dynamic-font-italic, dynamic-font-underline, and dynamic-font-strikethru validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, dynamic expression semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2442 Studio launch form appearance diagnostics localization shipped: special-effect, scroll-bars, window-state, show-window, title-bar, mouse-pointer, picture-margin, picture-position, picture-spacing, and picture-selection-display validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, numeric semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2441 Studio launch form bounds/style diagnostics localization shipped: min-height, min-width, max-height, movable, half-height-caption, MDI-form, back-style, border-style, border-width, and border-color validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, numeric semantics, parser behavior, and default en-US output.
- 2026-06-25: Localization/#2440 Studio launch window flag diagnostics localization shipped: closable, control-box, allow-output, bind-controls, auto-verb-menu, desktop, key-preview, mac-desktop, max-button, and min-button validation diagnostics now route through the portable C++ localization catalog while preserving CLI option names, JSON/status contracts, parser semantics, and default en-US output.
- 2026-06-24: Localization/#2395 Studio product subsystem descriptor localization shipped: subsystem display titles, parity-scope prose, and modern-editor-direction prose now route through the portable C++ localization catalog with en-US keys while preserving subsystem ids, host-kind values, current-status values, Copperfin component identifiers, VFP-equivalent identifiers, JSON fields, CLI options, and host parser behavior.
- 2026-06-24: Localization/#2394 Studio host usage localization shipped: `copperfin_studio_host` usage output now routes human-facing usage prefixes, alternate-command labels, object-entry labels, and selection-context token summary text through the portable C++ localization catalog with en-US keys while preserving command names, CLI flags, placeholders, selection-context tokens, JSON contracts, and host parser behavior as invariant data.
- 2026-06-24: Localization/#2393 Studio document-open diagnostic localization shipped: missing asset path errors from `open_document` now route through the portable C++ localization catalog with an en-US key while preserving paths, asset family identifiers, DBF fields, JSON keys, CLI options, and host contracts.
- 2026-06-24: Localization/#2392 xAsset executable-model diagnostic localization shipped: missing table-preview and unsupported executable xAsset family errors now route through the portable C++ localization catalog with en-US keys while preserving xAsset family identifiers, DBF fields, action identifiers, generated routine names, file extensions, JSON keys, and runtime contracts.
- 2026-06-24: Localization/#2391 runtime host debug-command localization shipped: runtime-host debug/xAsset command diagnostics, xAsset bootstrap fallback prose, compatibility-launcher notes, and debug-loop security denial text now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving debug command tokens, xAsset action identifiers, paths, line numbers, CLI/status fields, and runtime-host contracts; malformed breakpoint command parsing now returns the intended localized invalid-command diagnostic instead of aborting.
- 2026-06-24: Localization/#2390 runtime packaging diagnostic localization shipped: runtime package materialization, runtime host source validation, file-copy/write failures, package-root creation failures, and native wrapper primary-output build diagnostics now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving paths, output-kind enum values, binary names, CMake command behavior, and machine-readable contracts.
- 2026-06-24: Localization/#2389 platform profile diagnostic localization shipped: query translator unsupported SQL-shape diagnostics and federation AI-planner fallback diagnostics now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving SQL syntax, backend identifiers, federation mode values, planning modes, JSON/schema keys, and machine-readable contracts.
- 2026-06-24: Localization/#2388 security subsystem diagnostic localization shipped: SHA-256, secret provider, audit stream, external process policy, and process-hardening user-facing diagnostics now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving environment variable names, paths, executable names, policy identifiers, hashes, audit fields, enum values, and platform/API identifiers as invariant data.
- 2026-06-24: Localization/#2387 asset inspector validation localization shipped: DBF structural validation, memo sidecar validation, DBC catalog validation, and companion index parse validation prose now routes through the portable C++ localization catalog with en-US keys and named placeholders while preserving validation codes, severity values, asset family values, JSON contracts, file extensions, and VFP/DBF field identifiers.
- 2026-06-24: Localization/#2386 asset inspector path/DBC export localization shipped: missing asset path, missing DBC path, unreadable DBC, and DBC header parse failure prose now routes through the portable C++ localization catalog with en-US keys and named placeholders while preserving asset family values, severity values, JSON contracts, file extensions, and validation codes.
- 2026-06-24: Localization/#2385 DBF row/header mutation localization shipped: deleted-flag, truncate, pack, memo-pack, and zap validation/write error prose now routes through the portable C++ localization catalog with en-US keys while preserving DBF file formats and machine-readable contracts.
- 2026-06-24: Localization/#2384 DBF append/replacement localization shipped: append-table and record field replacement open, missing target field, memo-sidecar-path, table write, and memo sidecar write error prose now routes through the portable C++ localization catalog with en-US keys while preserving DBF field identifiers, memo sidecar formats, and machine-readable contracts.
- 2026-06-24: Localization/#2383 DBF table schema mutation localization shipped: add/drop/alter field validation prose now routes through the portable C++ localization catalog with en-US keys while preserving DBF field identifiers, file formats, and machine-readable contracts.
- 2026-06-24: Localization/#2382 DBF table creation/open/write localization shipped: table open, header-length, create-field validation, field width validation, record/schema count mismatch, and initial table/memo write failure prose now routes through the portable C++ localization catalog with en-US keys and named placeholders while preserving DBF field type identifiers, file formats, and machine-readable contracts.
- 2026-06-24: Localization/#2381 DBF table record/value validation localization shipped: record truncation, record index, field value coercion, table-data truncation, and record-layout validation prose now routes through the portable C++ localization catalog with en-US keys while preserving DBF field types, field names, format examples, file formats, and machine-readable contracts.
- 2026-06-24: Localization/#2380 VFP index probe localization shipped: IDX, NDX, MDX, extension/type, path, open, and read failure prose now routes through the portable C++ localization catalog with en-US keys while preserving index kind values, file extensions, tag metadata, key/FOR expressions, normalization/collation hints, and machine-readable contracts.
- 2026-06-24: Localization/#2379 DBF/CDX header probe localization shipped: DBF and CDX header parse/open/read/probe error prose now routes through the portable C++ localization catalog with en-US keys while preserving xBase/FoxPro format identifiers, file extensions, parser tokens, schema keys, and machine-readable contracts.
- 2026-06-24: Localization/#2378 visual asset editor group/subtree localization shipped: remaining visual asset editor subtree duplicate, group container, and ungroup container validation prose now routes through the portable C++ localization catalog with en-US keys and named placeholders while preserving DBF field names, object identities, record indexes, and JSON contracts.
- 2026-06-24: Localization/#2377 visual asset editor storage/undo localization shipped: visual asset editor storage, memo sidecar, undo-journal, selected-field exposure, property source/target, and method-name validation prose now routes through the portable C++ localization catalog with en-US keys and named placeholders while preserving DBF field names, VFP property/method identifiers, file extensions, record indexes, boolean literals, and JSON contracts.
- 2026-06-24: Localization/#2376 visual asset editor property-assignment label localization shipped: shared text/scalar visual object property assignment validation now routes empty-selection, duplicate-selection, and property-label display fragments through the portable C++ localization catalog with en-US keys and named placeholders while preserving VFP property identifiers, DBF field names, boolean literals, record indexes, and JSON contracts.
- 2026-06-24: Localization/#2375 visual asset editor empty-operation/mode localization shipped: visual asset editor empty path, empty property/method/object batch operations, unsupported method/property placement, unsupported visual object alignment/resize/distribution/snap/nudge/reorder placement modes, reparent selector/self/descendant validation, and rollback-failure prose now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving operation tokens, placement/mode tokens, property identifiers, DBF field names, record indexes, and JSON contracts.
- 2026-06-24: Localization/#2374 visual asset editor geometry/numeric localization shipped: visual asset editor geometry field, distribution, snapping, nudging, tab-order, finite font-size, and non-negative numeric property validation prose now route through the portable C++ localization catalog with en-US keys and named placeholders while preserving property identifiers, DBF field names, geometry values, operation tokens, record indexes, and JSON contracts.
- 2026-06-24: Localization/#2373 visual asset editor lookup/identity localization shipped: visual asset editor method/property/object lookup, ambiguity, identity-field, replacement-identity, record-availability, and create/duplicate/reorder identity errors now route through the portable C++ localization catalog with en-US keys while preserving DBF field names, method/property identifiers, object identities, record indexes, operation tokens, and JSON contracts.
- 2026-06-24: Localization/#2372 toolbox creation localization shipped: Studio toolbox object creation planning, dispatch-derived creation, batch creation, dispatch readiness, and catalog readiness prose now route through the portable C++ localization catalog with en-US keys while preserving toolbox item ids, object names, context tokens, command arguments, dispatch state, batch counts, mutation state, and JSON contracts.
- 2026-06-24: Localization/#2371 designer dispatch localization shipped: aggregate Studio designer dispatch planning, execution preflight, and execution-readiness catalog prose now route through the portable C++ localization catalog with en-US keys while preserving selection-context tokens, child action/builder/toolbox ids, command tokens, dispatch counts, execution state, dry-run state, mutation state, and JSON contracts.
- 2026-06-24: Localization/#2370 designer invocation-admission localization shipped: aggregate Studio designer invocation-admission validation prose now routes through the portable C++ localization catalog with an en-US key while preserving selection-context tokens, action ids, builder ids, toolbox item ids, command tokens, counts, dry-run state, mutation state, and JSON contracts.
- 2026-06-24: Localization/#2369 selection-context builder localization shipped: Studio selection-context builder launch, invocation-admission catalog, dispatch catalog, and dispatch execution-readiness prose now route through the portable C++ localization catalog with en-US keys while preserving selection-context tokens, builder ids, command tokens, entry points, dispatch state, counts, and JSON contracts.
- 2026-06-24: Localization/#2368 builder invocation/dispatch localization shipped: Studio builder invocation-admission, dispatch planning, dispatch catalogs, execution-readiness, and default execution failure prose now route through the portable C++ localization catalog with en-US keys while preserving builder ids, kind/context tokens, command tokens, entry points, dispatch arguments, execution state, counts, and JSON contracts.
- 2026-06-24: E3/#2367 builder registry localization shipped: Studio builder and wizard registry display titles, descriptions, launch validation, and launch catalog error prose now route through the portable C++ localization catalog with en-US keys while preserving builder ids, kind/context tokens, VFP-equivalent identifiers, Copperfin component names, entry points, launch counts, dry-run state, mutation state, and JSON contracts.
- 2026-06-24: E3/#2366 toolbox dispatch localization shipped: Studio toolbox dispatch planning, selection-context catalog, execution-readiness, and execution failure prose now route through the portable C++ localization catalog with en-US keys while preserving toolbox item ids, VFP class names, command tokens, dispatch arguments, context tokens, execution state, counts, and JSON contracts.
- 2026-06-24: E3/#2365 toolbox invocation-admission localization shipped: Studio toolbox invocation-admission planning, direct catalog, and selection-context catalog error prose now route through the portable C++ localization catalog with en-US keys while preserving toolbox item ids, VFP class names, command tokens, context tokens, admission state, counts, and JSON contracts.
- 2026-06-24: E3/#2364 toolbox palette localization shipped: Studio toolbox palette display titles, categories, descriptions, and direct launch error prose now route through the portable C++ localization catalog with en-US keys while preserving toolbox item ids, VFP class names, base class names, default prefixes, context tokens, report/label filtering, and JSON contracts.
- 2026-06-24: E3/#2363 editor action invocation-admission localization shipped: Studio editor-action invocation-admission planning and catalog error prose now route through the portable C++ localization catalog with en-US keys while preserving action ids, command tokens, target surfaces, admission state, counts, and JSON contracts.
- 2026-06-24: E3/#2362 editor action dispatch localization shipped: Studio editor-action dispatch planning, catalog readiness, and execution failure prose now route through the portable C++ localization catalog with en-US keys while preserving action ids, command tokens, target surfaces, dispatch arguments, execution state, and JSON contracts.
- 2026-06-24: E3/#2361 editor action localization shipped: Studio editor-action labels, descriptions, and direct launch/catalog error prose now route through the portable C++ localization catalog with en-US keys while preserving action ids, selection context tokens, command tokens, target surfaces, report/label catalog behavior, and JSON contracts.
- 2026-06-24: E3/#2360 missing-geometry summary preview JSON coverage deepened: Studio host regressions now prove report/label rows without layout geometry fields preserve zero available live/deleted preview metadata in summary JSON while retaining selected live/deleted object preview metadata, null field provenance, section counts, and label identity.
- 2026-06-24: E3/#2359 normalized classification settings preview JSON coverage deepened: Studio host regressions now prove selected root-settings responses for fractional and trimmed report/label layout classification rows preserve normalized live/deleted preview metadata while retaining selected settings provenance, section/object metadata, and label identity.
- 2026-06-24: E3/#2358 negative classification preview JSON coverage deepened: Studio host regressions now prove negative report/label layout classification rows preserve unavailable zero live/deleted preview metadata in summary JSON plus selected-record responses while retaining no-fabricated section/object selection state, valid root settings, and label identity.
- 2026-06-24: E3/#2357 dot-leading classification preview JSON coverage deepened: Studio host regressions now prove dot-leading report/label layout classification rows preserve unavailable zero live/deleted preview metadata in summary JSON plus selected-record responses while retaining no-fabricated section/object selection state, valid root settings, and label identity.
- 2026-06-24: E3/#2356 invalid classification preview JSON coverage deepened: Studio host regressions now prove invalid report/label layout classification rows preserve unavailable zero live/deleted preview metadata in summary JSON plus selected-record responses while retaining no-fabricated section/object selection state, preserved root settings, and label identity.
- 2026-06-24: E3/#2355 oversized numeric preview JSON coverage deepened: Studio host regressions now prove report/label rows with oversized layout numerics preserve zero available live/deleted preview metadata in summary JSON plus selected live/deleted object responses while retaining default-zero object geometry, zero section heights, and label identity.
- 2026-06-24: E3/#2354 malformed numeric preview JSON coverage deepened: Studio host regressions now prove report/label rows with malformed layout numerics preserve zero available live/deleted preview metadata in summary JSON plus selected live/deleted object responses while retaining default-zero object geometry, zero section heights, and label identity.
- 2026-06-24: E3/#2353 negative-dimension preview JSON coverage deepened: Studio host regressions now prove report/label rows with negative geometry dimensions preserve clamped non-inverted live/deleted preview metadata in summary JSON plus selected live/deleted object responses while retaining zero-width/height object geometry, clamped section heights, and label identity.
- 2026-06-24: Localization/#2352 runtime host bridge error localization shipped: `copperfin_runtime_host` now routes bridge-invocation validation and artifact error prose through the portable C++ catalog while preserving bridge mode/status keys, media/schema contracts, request/response artifact formats, and generated response behavior.
- 2026-06-24: Localization/#2351 runtime host top-level error localization shipped: `copperfin_runtime_host` now routes command validation, federation-mode, manifest-loading, and direct security-gate error prose through the portable C++ catalog while preserving runtime modes, CLI/federation tokens, permission IDs, stdout/status keys, and parser/runtime contracts.
- 2026-06-24: Localization/#2350 build host usage and command error localization shipped: `copperfin_build_host` now routes its usage text and direct command error prose through the portable C++ catalog, supports locale selection for that surface, and preserves CLI option names, environment variables, stdout/status keys, output-kind values, and build/package contracts as locale-invariant tokens.
- 2026-06-24: Localization/#2349 runtime host usage localization shipped: `copperfin_runtime_host` now routes its human-facing usage text through the portable C++ catalog, supports `--locale` for that surface, and preserves CLI option names, debug-command tokens, federation machine values, and runtime parser behavior as locale-invariant contracts.
- 2026-06-24: Localization/#1779 portable C++ localization infrastructure added: `cf_localization` now loads UTF-8 `en-US` catalogs with deterministic locale fallback, named placeholders, pseudo-localization, package locale installation, and `copperfin_inspect` usage text routed through the catalog while preserving machine-readable inspection fields and locale-invariant VFP parser behavior.
- 2026-06-24: E3/#2347 fractional geometry preview JSON coverage deepened: Studio host regressions now prove report/label rows with fractional geometry values preserve integer-portion live/deleted preview metadata in summary JSON plus selected live/deleted object responses while retaining integer-portion object geometry, section heights, and label identity.
- 2026-06-24: E3/#2346 unknown band preview JSON coverage deepened: Studio host regressions now prove report/label section rows with unknown band OBJCODE values preserve full live/deleted preview metadata in summary JSON plus selected live/deleted section responses while retaining explicit other-band fallback metadata, section counts, and label identity.
- 2026-06-24: E3/#2345 unsupported OBJTYPE summary preview JSON coverage deepened: Studio host regressions now prove report/label rows with unsupported OBJTYPE codes preserve unavailable zero live/deleted preview metadata in summary JSON while complementing existing no-selection coverage, non-fabricated entity counts, and label identity.
- 2026-06-24: E3/#2344 missing classification summary preview JSON coverage deepened: Studio host regressions now prove report/label rows with missing classification fields and OBJCODE-only schemas preserve unavailable zero live/deleted preview metadata in summary JSON while complementing existing no-selection coverage, non-inferred entity counts, and label identity.
- 2026-06-24: E3/#2343 missing object OBJCODE preview JSON coverage deepened: Studio host regressions now prove report/label object rows without an OBJCODE schema preserve full live/deleted object preview metadata in summary JSON plus selected live/deleted object responses while retaining default object-code metadata, null object-code provenance, unplaced object counts, and label identity.
- 2026-06-24: E3/#2342 missing section OBJCODE preview JSON coverage deepened: Studio host regressions now prove report/label section rows without an OBJCODE schema preserve full live/deleted preview metadata in summary JSON plus selected live/deleted section responses while retaining default title-band metadata, null object-code provenance, expression metadata, and label identity.
- 2026-06-24: E3/#2341 missing section geometry preview JSON coverage deepened: Studio host regressions now prove report/label section rows without VPOS/HEIGHT geometry schema preserve available zero live/deleted preview metadata in summary JSON plus selected live/deleted section responses while retaining null geometry provenance, band-kind counts, expression metadata, and label identity.
- 2026-06-24: E3/#2340 missing object title preview JSON coverage deepened: Studio host regressions now prove report/label object rows without a title/identity schema preserve live/deleted object preview metadata in summary JSON plus selected live/deleted object responses while retaining synthesized record titles, null title provenance, containing-section metadata, object-kind counts, and label identity.
- 2026-06-24: E3/#2339 missing object expression preview JSON coverage deepened: Studio host regressions now prove report/label object rows without an EXPR schema preserve live/deleted object preview metadata in summary JSON plus selected live/deleted object responses while retaining null expression provenance, containing-section metadata, object-kind counts, and label identity.
- 2026-06-24: E3/#2338 missing section expression preview JSON coverage deepened: Studio host regressions now prove report/label section rows without an EXPR schema preserve live/deleted section preview metadata in summary JSON plus selected live/deleted section responses while retaining null expression provenance, section geometry, band-kind counts, and label identity.
- 2026-06-24: E3/#2337 missing root expression preview JSON coverage deepened: Studio host regressions now prove report/label settings rows without a root EXPR schema preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining direct-field setting provenance, page setup, and label identity.
- 2026-06-24: E3/#2336 unresolved geometry memo preview JSON coverage deepened: Studio host regressions now prove unresolved report/label geometry memo placeholders preserve zero available live/deleted preview metadata in summary JSON plus selected live object responses while retaining zero non-inverted geometry defaults, field provenance, placeholder suppression, and label identity.
- 2026-06-24: E3/#2335 unresolved unplaced-object memo preview JSON coverage deepened: Studio host regressions now prove unresolved unplaced report/label object memo placeholders preserve live object preview metadata and unavailable zero deleted preview metadata in summary JSON plus selected unplaced object responses while retaining placeholder suppression, unplaced object counts, empty object expressions/highlights, null containing sections, and label identity.
- 2026-06-24: E3/#2334 unresolved deleted-object memo preview JSON coverage deepened: Studio host regressions now prove unresolved deleted report/label object memo placeholders preserve live section and deleted object preview metadata in summary JSON plus selected deleted object responses while retaining placeholder suppression, empty object expressions/highlights, null containing sections, and label identity.
- 2026-06-24: E3/#2333 unresolved section memo preview JSON coverage deepened: Studio host regressions now prove unresolved report/label section memo placeholders preserve live and deleted section preview metadata in summary JSON plus selected live/deleted section responses while retaining expression suppression, null memo provenance, section geometry, and label identity.
- 2026-06-24: E3/#2332 unresolved memo placeholder preview JSON coverage deepened: Studio host regressions now prove unresolved report/label memo placeholders preserve live-only preview metadata in summary JSON plus selected live/deleted root and object responses while retaining placeholder suppression, null settings, empty object expressions, and label identity.
- 2026-06-24: E3/#2331 mixed-case settings memo preview JSON coverage deepened: Studio host regressions now prove mixed-case report/label settings memo names preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining case-insensitive summaries, source spelling, memo provenance, and label identity.
- 2026-06-24: E3/#2330 CR-only settings memo preview JSON coverage deepened: Studio host regressions now prove CR-only report/label settings memo lines preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining parsed setup summaries, memo-line provenance, and label identity.
- 2026-06-24: E3/#2329 invalid-first duplicate settings preview JSON coverage deepened: Studio host regressions now prove invalid-first duplicate report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining no-fabricated-summary behavior, duplicate provenance, and label identity.
- 2026-06-24: E3/#2328 duplicate settings preview JSON coverage deepened: Studio host regressions now prove duplicate report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining first-match summary precedence, duplicate provenance, and label identity.
- 2026-06-24: E3/#2327 malformed settings memo preview JSON coverage deepened: Studio host regressions now prove malformed report/label settings memo lines preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining skipped-line filtering, valid memo provenance, and label identity.
- 2026-06-24: E3/#2326 blank settings memo preview JSON coverage deepened: Studio host regressions now prove blank report/label settings memo values preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining no-fabricated-setup summaries, memo provenance, and label identity.
- 2026-06-24: E3/#2325 fractional settings memo preview JSON coverage deepened: Studio host regressions now prove fractional report/label settings memo values preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining integer-derived setup summaries, memo provenance, and label identity.
- 2026-06-24: E3/#2324 invalid settings memo preview JSON coverage deepened: Studio host regressions now prove invalid report/label settings memo values preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining no-fabricated-setup summaries, memo provenance, and label identity.
- 2026-06-24: E3/#2323 dot-leading direct settings preview JSON coverage deepened: Studio host regressions now prove dot-leading direct report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining no-fabricated-setup summaries, dot-leading raw setting provenance, and label identity.
- 2026-06-24: E3/#2322 oversized direct settings preview JSON coverage deepened: Studio host regressions now prove oversized direct report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining no-fabricated-setup summaries, oversized raw setting provenance, and label identity.
- 2026-06-24: E3/#2321 fractional direct settings preview JSON coverage deepened: Studio host regressions now prove fractional direct report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining integer-derived setup summaries, fractional raw setting provenance, and label identity.
- 2026-06-24: E3/#2320 trimmed direct settings preview JSON coverage deepened: Studio host regressions now prove whitespace-trimmed direct report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining parsed setup summaries, trimmed raw setting provenance, and label identity.
- 2026-06-24: E3/#2319 mixed invalid direct settings preview JSON coverage deepened: Studio host regressions now prove mixed valid/invalid direct report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining setup summaries for valid siblings, raw malformed setting provenance, and label identity.
- 2026-06-24: E3/#2318 invalid direct page setup preview JSON coverage deepened: Studio host regressions now prove malformed direct ORIENTATION/PAPERSIZE/TOPMARGIN report/label settings preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining raw setting provenance and label identity.
- 2026-06-24: E3/#2317 missing root OBJCODE settings preview JSON coverage deepened: Studio host regressions now prove report/label root/settings rows with missing OBJCODE schema preserve unavailable zero live/deleted preview metadata in summary JSON plus selected live/deleted settings responses while retaining settings provenance, page setup metadata, and label identity.
- 2026-06-24: E3/#2316 missing OBJCODE selection preview JSON coverage deepened: Studio host regressions now prove selected report/label sections and objects from missing OBJCODE schemas preserve live/deleted preview metadata while retaining default band/object-code metadata, null provenance, section/object selection state, and label identity.
- 2026-06-24: E3/#2315 unsupported OBJTYPE selection preview JSON coverage deepened: Studio host regressions now prove selected report/label records with unsupported OBJTYPE codes preserve unavailable zero live/deleted preview metadata while retaining no-selection JSON and label identity.
- 2026-06-24: E3/#2314 missing classification schema selection preview JSON coverage deepened: Studio host regressions now prove selected report/label records from missing classification-field and OBJCODE-only schemas preserve unavailable zero live/deleted preview metadata while retaining no-selection JSON and label identity.
- 2026-06-24: E3/#2313 missing-geometry selection preview JSON coverage deepened: Studio host regressions now prove selected live and deleted report/label layout objects from rows without geometry columns preserve available zero live/deleted preview metadata alongside zero geometry defaults, null field provenance, section resolution, and label identity.
- 2026-06-24: E3/#2312 normalized classification preview JSON coverage deepened: Studio host regressions now prove fractional and whitespace-trimmed report/label layout classifications preserve live and deleted preview metadata across summary JSON plus selected live/deleted section and object responses while retaining label identity and normalized section/object metadata.
- 2026-06-24: E3/#2311 extended report/label object-kind preview JSON coverage deepened: Studio host regressions now prove rectangle, picture, and variable report/label layout object summaries and stable-selected objects preserve live preview metadata and deleted picture preview bounds while retaining label identity and object-kind metadata.
- 2026-06-24: E3/#2310 live detail header/footer object containment preview JSON coverage deepened: Studio host regressions now prove report/label detail-header and detail-footer object containment summaries and stable-selected contained objects preserve live preview metadata and suppress deleted preview availability alongside containing-section metadata.
- 2026-06-24: E3/#2309 deleted detail header/footer object expression selection preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object expression reads preserve live preview metadata, expose deleted preview metadata, and keep containing-section metadata null alongside selected deleted expression provenance.
- 2026-06-24: E3/#2308 deleted detail header/footer object expression mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header expression updates and detail-footer expression clears preserve live preview metadata, expose deleted preview metadata, and keep containing-section metadata null alongside refreshed deleted expression provenance and undo metadata.
- 2026-06-24: E3/#2307 live detail header/footer object expression mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header expression updates and detail-footer expression clears preserve live preview metadata and suppress deleted preview availability alongside expression provenance, undo, and containing-section metadata.
- 2026-06-24: E3/#2306 deleted detail header/footer object vertical distribution preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object vertical distributions preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside multi-object geometry and undo metadata.
- 2026-06-24: E3/#2305 live detail header/footer object vertical distribution preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object vertical distributions preserve live preview metadata and suppress deleted preview availability alongside multi-object geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2304 deleted detail header/footer object distribution preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object distributions preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside multi-object geometry and undo metadata.
- 2026-06-24: E3/#2303 live detail header/footer object distribution preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object distributions preserve live preview metadata and suppress deleted preview availability alongside multi-object geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2302 deleted detail header/footer object nudge preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object nudges preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside translated geometry and undo metadata.
- 2026-06-24: E3/#2301 live detail header/footer object nudge preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object nudges preserve live preview metadata and suppress deleted preview availability alongside translated geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2300 deleted detail header/footer object snap preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object snaps preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside rounded geometry and undo metadata.
- 2026-06-24: E3/#2299 live detail header/footer object snap preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object snaps preserve live preview metadata and suppress deleted preview availability alongside rounded geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2298 deleted detail header/footer object resize preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object resizes preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside undo metadata.
- 2026-06-24: E3/#2297 live detail header/footer object resize preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object resizes preserve live preview metadata and suppress deleted preview availability alongside geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2296 deleted detail header/footer object align preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object alignments preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained alongside undo metadata.
- 2026-06-24: E3/#2295 live detail header/footer object align preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object alignments preserve live preview metadata and suppress deleted preview availability alongside geometry, undo, and containing-section metadata.
- 2026-06-24: E3/#2294 deleted detail header/footer object reorder preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object reorders preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained.
- 2026-06-24: E3/#2293 live detail header/footer object reorder preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object reorders preserve live preview metadata and suppress deleted preview availability alongside refreshed selected object order and containing-section metadata.
- 2026-06-24: E3/#2292 deleted detail header/footer object rename preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object renames preserve live preview metadata, expose deleted preview metadata, and keep selected deleted object geometry uncontained.
- 2026-06-24: E3/#2291 live detail header/footer object rename preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object renames preserve live preview metadata and suppress deleted preview availability alongside retained selected object geometry and containing-section metadata.
- 2026-06-24: E3/#2290 deleted detail header/footer object duplicate preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer object duplicates preserve live preview metadata, expose deleted preview metadata, and keep selected deleted duplicate geometry uncontained.
- 2026-06-24: E3/#2289 live detail header/footer object duplicate preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer object duplicates preserve live preview metadata and suppress deleted preview availability alongside refreshed selected duplicate geometry and containing-section object counts.
- 2026-06-24: E3/#2288 detail header/footer object delete/restore preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header object deletes and detail-footer object restores preserve live preview metadata, expose retained deleted-object preview bounds, and keep selected object/containing-section state coherent.
- 2026-06-24: E3/#2287 deleted detail header/footer object geometry mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header VPOS updates and detail-footer HEIGHT clears preserve live preview metadata, refresh deleted preview bounds, and keep containing-section metadata null alongside selected deleted-object geometry.
- 2026-06-24: E3/#2286 live detail header/footer object geometry mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header VPOS updates and detail-footer HEIGHT clears preserve live preview metadata and suppress deleted preview availability alongside refreshed selected-object geometry and containing-section metadata.
- 2026-06-24: E3/#2285 deleted detail header/footer object font option mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header FONTSIZE updates and detail-footer MODE clears preserve live preview metadata, expose deleted preview bounds, and keep containing-section metadata null alongside refreshed selected deleted-object option provenance.
- 2026-06-24: E3/#2284 live detail header/footer object font option mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header FONTSIZE updates and detail-footer MODE clears preserve live preview metadata and suppress deleted preview availability alongside refreshed selected-object option provenance and containing-section metadata.
- 2026-06-24: E3/#2283 deleted detail header/footer object font mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header FONTFACE updates and detail-footer FONTFACE clears preserve live preview metadata, expose deleted preview bounds, and keep containing-section metadata null alongside refreshed selected deleted-object font provenance.
- 2026-06-24: E3/#2282 live detail header/footer object font mutation preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header FONTFACE updates and detail-footer FONTFACE clears preserve live preview metadata and suppress deleted preview availability alongside refreshed selected-object font provenance and containing-section metadata.
- 2026-06-24: E3/#2281 deleted detail header/footer object font selection preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer contained-object font reads preserve live preview metadata, expose deleted preview bounds, and keep containing-section metadata null alongside selected deleted-object font provenance.
- 2026-06-24: E3/#2280 live detail header/footer object font selection preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer contained-object font reads preserve live preview metadata and suppress deleted preview availability alongside selected-object font provenance and containing-section metadata.
- 2026-06-24: E3/#2279 live detail header/footer object expression selection preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer contained-object expression reads preserve live preview metadata and suppress deleted preview availability alongside selected-object expression and containing-section metadata.
- 2026-06-24: E3/#2278 detail header section delete/restore preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header deletes shrink live preview bounds, expose deleted preview bounds, and restores clear deleted preview availability while preserving mutation state, selected-section geometry, object containment, and label identity.
- 2026-06-24: E3/#2277 detail header/footer section top update preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer VPOS updates refresh live preview bounds and preserve deleted preview metadata alongside mutation state, undo metadata, selected-section geometry, and label identity.
- 2026-06-24: E3/#2276 detail header/footer section height update preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer HEIGHT updates preserve live/deleted preview metadata alongside mutation state, undo metadata, selected-section geometry, and label identity.
- 2026-06-24: E3/#2275 detail header/footer section selection preview JSON coverage deepened: Studio host regressions now prove stable-selected report/label detail-header and detail-footer section reads expose live and deleted preview-bounds metadata alongside selected-section kind metadata and label identity.
- 2026-06-24: E3/#2274 section restore preview JSON coverage deepened: Studio host regressions now prove record-selected label section restores and stable report/label section restores expose live preview bounds, clear deleted preview availability, and preserve restored selected-section metadata, section counts, object membership, and label identity.
- 2026-06-24: E3/#2273 section delete preview JSON coverage deepened: Studio host regressions now prove label record-selected section deletes and stable report/label section deletes preserve live preview bounds, expose deleted preview bounds, and preserve deleted selected-section metadata, section counts, unplaced-object accounting, and label identity.
- 2026-06-24: E3/#2272 live section height clear preview JSON coverage deepened: Studio host regressions now prove report/label section HEIGHT clears by record selection refresh live preview-bounds metadata, preserve deleted preview bounds, and retain label identity alongside selected-section geometry and section/object counts.
- 2026-06-24: E3/#2271 deleted group section expression preview JSON coverage deepened: Studio host regressions now prove deleted report/label group-header and group-footer section expression read responses expose live and deleted preview-bounds metadata alongside selected deleted-section expression metadata.
- 2026-06-24: E3/#2270 deleted group section expression mutation preview JSON coverage deepened: Studio host regressions now prove deleted report/label group-header and group-footer expression updates and clears by record selection preserve live and deleted preview-bounds metadata alongside refreshed selected deleted-section expression metadata.
- 2026-06-24: E3/#2269 live group section expression mutation preview JSON coverage deepened: Studio host regressions now prove report/label group-header and group-footer expression updates and clears by record selection preserve live preview-bounds metadata and suppress deleted preview bounds alongside refreshed selected-section expression metadata.
- 2026-06-24: E3/#2268 record-selected group section preview JSON coverage deepened: Studio host regressions now prove record-selected report/label group-header section selection exposes live preview-bounds metadata, suppresses deleted preview bounds, and preserves selected-section expression metadata, section counts, selection classification, and label identity.
- 2026-06-24: E3/#2267 stable-selected live detail header/footer section preview JSON coverage deepened: Studio host regressions now prove stable-selected live report/label detail-header and detail-footer section selection exposes live and deleted preview-bounds metadata alongside live section selection metadata, selected-section geometry, deleted-section count preservation, and label identity.
- 2026-06-24: E3/#2266 stable-selected deleted detail header/footer section preview JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer section selection exposes live and deleted preview-bounds metadata alongside deleted section selection metadata, selected-section geometry, live-section preservation, and label identity.
- 2026-06-24: E3/#2265 stable-selected deleted detail header/footer section expression mutation-metadata JSON coverage deepened: Studio host regressions now prove stable-selected deleted report/label detail-header and detail-footer expression updates and clears expose committed state, mutation state, undo availability, expression undo labels, preserved deleted state, refreshed selected deleted-section expression metadata, deleted-section sibling metadata, live-section preservation, live/deleted preview bounds, stale-expression suppression, and label identity.
- 2026-06-24: E3/#2264 deleted detail header/footer section expression mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header expression updates and detail-footer expression clears by record selection expose committed state, mutation state, undo availability, expression undo labels, preserved deleted state, refreshed deleted-section and selected-section expression metadata, live-section preservation, and label identity.
- 2026-06-24: E3/#2263 stable-selected live detail header/footer section expression mutation-metadata JSON coverage deepened: Studio host regressions now prove stable-selected live report/label detail-header and detail-footer expression updates and clears expose committed state, mutation state, undo availability, expression undo labels, refreshed selected-section expression metadata, sibling-section metadata, live/deleted preview bounds, stale-expression suppression, and label identity.
- 2026-06-24: E3/#2262 live detail header/footer section expression mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header expression updates and detail-footer expression clears by record selection expose committed state, mutation state, undo availability, expression undo labels, refreshed selected-section expression metadata, sibling-section metadata, live preview bounds, and label identity.
- 2026-06-24: E3/#2261 deleted detail header/footer object expression mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header expression updates and detail-footer expression clears expose committed state, mutation state, undo availability, expression undo labels, preserved deleted state, refreshed deleted-object and selected-object expression metadata, containing-section nullability, stale-expression suppression, memo-field provenance, and label identity.
- 2026-06-24: E3/#2260 live detail header/footer object expression mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header expression updates and detail-footer expression clears expose committed state, mutation state, undo availability, expression undo labels, refreshed selected-object expression metadata, containing-section metadata, stale-expression suppression, memo-field provenance, and label identity.
- 2026-06-24: E3/#2259 deleted detail header/footer object vertical distribution mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object vertical distributions expose committed state, mutation state, undo availability, vertical-position undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2258 live detail header/footer object vertical distribution mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header and detail-footer object vertical distributions expose committed state, mutation state, undo availability, vertical-position undo labels, refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2257 deleted detail header/footer object distribution mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object horizontal distributions expose committed state, mutation state, undo availability, horizontal-position undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2256 live detail header/footer object distribution mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful horizontal distribution commands, with regressions proving live report/label detail-header and detail-footer object distributions expose that metadata alongside refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2255 deleted detail header/footer object nudge mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object nudges expose committed state, mutation state, undo availability, nudge undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2254 live detail header/footer object nudge mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful nudge commands, with regressions proving live report/label detail-header and detail-footer object nudges expose that metadata alongside refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2253 deleted detail header/footer object snap mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object snaps expose committed state, mutation state, undo availability, snap undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2252 live detail header/footer object snap mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful snap-to-grid commands, with regressions proving live report/label detail-header and detail-footer object snaps expose that metadata alongside refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2251 deleted detail header/footer object resize mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object resizes expose committed state, mutation state, undo availability, resize undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2250 live detail header/footer object resize mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful resize commands, with regressions proving live report/label detail-header and detail-footer object resizes expose that metadata alongside refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2249 deleted detail header/footer object align mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer object alignments expose committed state, mutation state, undo availability, horizontal-position undo labels, preserved deleted-state geometry, containing-section nullability, live/deleted preview metadata, and label identity.
- 2026-06-24: E3/#2248 live detail header/footer object align mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful align commands, with regressions proving live report/label detail-header and detail-footer object alignments expose that metadata alongside refreshed selected-object geometry, containing-section metadata, and label identity.
- 2026-06-24: E3/#2247 deleted detail header/footer section reorder mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer section reorders expose committed state, mutation state, undo availability, empty undo labels, preserved live/deleted section counts, preview metadata, physical deleted-record movement, preserved identities, deleted-state preservation, and selected moved deleted-section geometry.
- 2026-06-24: E3/#2246 live detail header/footer section reorder mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful reorder commands, with regressions proving live report/label detail-header and detail-footer section reorders expose that metadata alongside preserved section counts, preview metadata, physical record movement, preserved identities, and selected moved-section geometry.
- 2026-06-24: E3/#2245 deleted detail header/footer section rename mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer section renames expose committed state, mutation state, undo availability, renamed-identity undo labels, preserved live/deleted section counts, preview metadata, replacement identities, deleted-state preservation, and selected renamed deleted-section geometry.
- 2026-06-24: Documentation status cleanup aligned the top-level README and product charter with the standing Requirements Recovery goal, replaced the stale early build-order README section with the current implementation focus, and removed a duplicated packaged-runtime limitation.
- 2026-06-24: E3/#2244 live detail header/footer section rename mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful rename commands, with regressions proving live report/label detail-header and detail-footer section renames expose that metadata alongside preserved section counts, preview metadata, replacement identities, and selected renamed-section geometry.
- 2026-06-24: E3/#2243 deleted detail header/footer section duplicate mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer section duplicates expose committed state, mutation state, undo availability, empty undo labels, appended deleted duplicate sections, refreshed deleted section counts/heights, preview metadata, and selected duplicated deleted-section geometry.
- 2026-06-24: E3/#2242 live detail header/footer section duplicate mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful duplicate commands, with regressions proving live report/label detail-header and detail-footer section duplicates expose that metadata alongside appended live duplicate sections, refreshed kind counts, preview metadata, and selected duplicated-section geometry.
- 2026-06-24: E3/#2241 live detail footer section delete/restore preview-bounds mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-footer section delete/restore preview-bounds refreshes expose committed state, mutation state, undo availability, empty undo labels, refreshed live/deleted preview bounds, section counts, object placement, and selected-section geometry.
- 2026-06-24: E3/#2240 live detail header/footer section delete/restore mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits committed state, mutation state, undo availability, and undo labels after successful delete/restore commands, with regressions proving live report/label detail-header and detail-footer section delete/restore expose that metadata alongside refreshed section buckets, object containment, and selected-section state.
- 2026-06-24: E3/#2239 deleted detail header/footer section top-clear mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer VPOS clears expose committed state, mutation state, undo availability, top-property undo labels, blanked deleted-section tops, refreshed deleted preview bounds, and zero/default selected deleted-section geometry.
- 2026-06-24: E3/#2238 deleted detail header/footer section height-clear mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer HEIGHT clears expose committed state, mutation state, undo availability, height-property undo labels, blanked deleted-section heights, refreshed deleted preview bounds, and zero-height selected deleted-section geometry.
- 2026-06-24: E3/#2237 deleted detail header/footer section top-update mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer VPOS updates expose committed state, mutation state, undo availability, top-property undo labels, persisted deleted-section tops, refreshed deleted preview bounds, and selected deleted-section geometry.
- 2026-06-24: E3/#2236 deleted detail header/footer section height-update mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted report/label detail-header and detail-footer HEIGHT updates expose committed state, mutation state, undo availability, height-property undo labels, persisted deleted-section heights, refreshed deleted preview bounds, and selected deleted-section geometry.
- 2026-06-24: E3/#2235 live detail header/footer section top-clear mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header and detail-footer VPOS clears expose committed state, mutation state, undo availability, top-property undo labels, blanked section tops, refreshed preview bounds, and zero/default selected-section geometry.
- 2026-06-24: E3/#2234 live detail header/footer section height-clear mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header and detail-footer HEIGHT clears expose committed state, mutation state, undo availability, height-property undo labels, blanked section heights, refreshed preview bounds, and zero-height selected-section geometry.
- 2026-06-24: E3/#2233 live detail header/footer section top-update mutation-metadata JSON coverage deepened: Studio host regressions now prove live report/label detail-header and detail-footer VPOS updates expose committed state, mutation state, undo availability, top-property undo labels, persisted section tops, and refreshed selected-section geometry.
- 2026-06-24: E3/#2232 live detail header/footer section height-update mutation-metadata JSON coverage deepened: Studio host generic document JSON now emits property-mutation committed state, mutation state, undo availability, and undo labels after successful property commands, with regressions proving live report/label detail-header and detail-footer HEIGHT updates expose that metadata alongside persisted section heights and refreshed selected-section geometry.
- 2026-06-24: E3/#2231 failed deleted report/label visual-object rename-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted rename-batch identity-collision failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report collision errors, roll back earlier identity mutations, and preserve deleted-state layout identities.
- 2026-06-24: E3/#2230 failed live report/label visual-object rename-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove live rename-batch identity-collision failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report collision errors, roll back earlier identity mutations, and preserve layout identities.
- 2026-06-24: E3/#2229 failed deleted report/label visual-object update-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted update-batch missing-selector failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report missing-selector errors, roll back earlier property mutations, and preserve deleted-state layout properties.
- 2026-06-24: E3/#2228 failed live report/label visual-object update-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove live update-batch missing-selector failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report missing-selector errors, roll back earlier property mutations, and preserve layout properties.
- 2026-06-24: E3/#2227 failed deleted report/label visual-object reorder-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted reorder-batch missing-target failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report missing-target errors, roll back earlier reorders, and preserve deleted-state layout rows.
- 2026-06-24: E3/#2226 failed live report/label visual-object reorder-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove live reorder-batch missing-target failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report missing-target errors, roll back earlier reorders, and preserve layout rows.
- 2026-06-24: E3/#2225 failed deleted report/label visual-object duplicate-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted duplicate-batch collision failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report collision errors, roll back earlier duplicates, and preserve deleted-state layout rows.
- 2026-06-24: E3/#2224 failed live report/label visual-object duplicate-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove live duplicate-batch collision failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report collision errors, roll back earlier duplicates, and preserve layout rows.
- 2026-06-24: E3/#2223 failed deleted report/label visual-object duplicate-subtree mutation-metadata JSON coverage deepened: Studio host regressions now prove deleted duplicate-subtree collision failures keep duplicate-subtree results null, suppress stale committed and mutation metadata, suppress stale undo fields, report collision errors, and preserve deleted-state layout rows.
- 2026-06-24: E3/#2222 failed live report/label visual-object duplicate-subtree mutation-metadata JSON coverage deepened: Studio host regressions now prove collision and missing-selector duplicate-subtree failures keep duplicate-subtree results null, suppress stale committed and mutation metadata, suppress stale undo fields, report errors, and preserve layout rows.
- 2026-06-24: E3/#2221 failed deleted report/label visual-property reorder-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector reorder-batch failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2220 failed deleted report/label visual-property reorder mutation-metadata JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector reorder failures keep reorder results null, suppress stale committed and mutation metadata, suppress stale undo fields, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2219 failed deleted report/label visual-property rename-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector rename-batch failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2218 failed deleted report/label visual-property rename mutation-metadata JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector rename failures keep rename results null, suppress stale committed and mutation metadata, suppress stale undo fields, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2217 failed deleted report/label visual-property move-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove expression and geometry missing-selector move-batch failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, roll back earlier moves, and preserve deleted-state and property values.
- 2026-06-24: E3/#2216 failed deleted report/label visual-property move mutation-metadata JSON coverage deepened: Studio host regressions now prove missing-selector move failures keep move results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2215 failed deleted report/label visual-property copy-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove expression and geometry missing-selector copy-batch failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, roll back earlier copies, and preserve deleted-state and property values.
- 2026-06-24: E3/#2214 failed deleted report/label visual-property copy mutation-metadata JSON coverage deepened: Studio host regressions now prove missing-selector copy failures keep copy results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2213 failed deleted report/label visual-property clear-batch mutation-metadata JSON coverage deepened: Studio host regressions now prove missing-selector clear-batch failures keep batch results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, roll back earlier clears, and preserve deleted-state and property values.
- 2026-06-24: E3/#2212 failed deleted report/label visual-property clear mutation-metadata JSON coverage deepened: Studio host regressions now prove missing-selector clear failures keep clear results null, suppress stale committed and mutation metadata, suppress stale undo fields, report selector errors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2211 failed deleted report/label visual-property reorder-batch undo-label JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector reorder-batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2210 failed deleted report/label visual-property reorder undo-label JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector reorder failures keep reorder results null, do not advertise undo availability, suppress stale undo labels, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2209 failed deleted report/label visual-property rename-batch undo-label JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector rename-batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2208 failed deleted report/label visual-property rename undo-label JSON coverage deepened: Studio host regressions now prove direct-field and missing-selector rename failures keep rename results null, do not advertise undo availability, suppress stale undo labels, report errors, and preserve deleted-state and DBF-backed fields.
- 2026-06-24: E3/#2207 failed deleted report/label visual-property move-batch undo-label JSON coverage deepened: Studio host regressions now prove expression and geometry missing-selector move-batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, roll back earlier moves, and preserve deleted-state and property values.
- 2026-06-24: E3/#2206 failed deleted report/label visual-property move undo-label JSON coverage deepened: Studio host regressions now prove missing-selector move failures keep move results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2205 failed deleted report/label visual-property copy-batch undo-label JSON coverage deepened: Studio host regressions now prove expression and geometry missing-selector copy-batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, roll back earlier copies, and preserve deleted-state and property values.
- 2026-06-24: E3/#2204 failed deleted report/label visual-property copy undo-label JSON coverage deepened: Studio host regressions now prove missing-selector copy failures keep copy results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2203 failed deleted report/label visual-property clear-batch undo-label JSON coverage deepened: Studio host regressions now prove missing-selector clear-batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, roll back earlier clears, and preserve deleted-state and property values.
- 2026-06-24: E3/#2202 failed deleted report/label visual-property clear undo-label JSON coverage deepened: Studio host regressions now prove missing-selector clear failures keep clear results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, and preserve deleted-state and property values.
- 2026-06-24: E3/#2201 failed report/label update-batch undo-label JSON coverage deepened: Studio host regressions now prove live and deleted visual-object update-batch rollback failures keep update-batch results null, do not advertise undo availability, suppress stale undo labels, report missing selectors, and preserve rolled-back property and deleted-state behavior.
- 2026-06-24: E3/#2200 failed report/label duplicate-subtree undo-label JSON coverage deepened: Studio host regressions now prove live duplicate-subtree collision and missing-selector failures plus deleted duplicate-subtree collision failures keep duplicate-subtree results null, do not advertise undo availability, suppress stale undo labels, report errors, and preserve object state.
- 2026-06-24: E3/#2199 failed report/label visual-object batch rollback undo-label JSON coverage deepened: Studio host regressions now prove live and deleted rename, duplicate, and reorder batch failures keep batch results null, do not advertise undo availability, suppress stale undo labels, report collision or missing-target errors, and preserve rolled-back object state.
- 2026-06-24: E3/#2198 deleted report/label visual-property geometry move-batch rollback JSON coverage deepened: Studio host regressions now prove failed deleted geometry move batches keep move-batch results null, do not advertise undo availability, report missing selectors, preserve original source and target coordinates, and preserve deleted state.
- 2026-06-24: E3/#2197 deleted report/label visual-property geometry copy-batch rollback JSON coverage deepened: Studio host regressions now prove failed deleted geometry copy batches keep copy-batch results null, do not advertise undo availability, report missing selectors, preserve original coordinates, and preserve deleted state.
- 2026-06-24: E3/#2196 deleted report/label visual-property geometry move-batch JSON coverage deepened: Studio host regressions now prove deleted geometry move batches expose committed state, mutation state, undo availability, geometry undo labels, moved coordinates, source clearing, refreshed selected-object geometry, and preserved deleted state.
- 2026-06-24: E3/#2195 deleted report/label visual-property geometry copy-batch JSON coverage deepened: Studio host regressions now prove deleted geometry copy batches expose committed state, mutation state, undo availability, geometry undo labels, copied coordinates, refreshed selected-object geometry, and preserved deleted state.
- 2026-06-24: E3/#2194 deleted report/label visual-property geometry move JSON coverage deepened: Studio host regressions now prove deleted geometry moves expose committed state, mutation state, undo availability, geometry undo labels, moved coordinates, source clearing, refreshed selected-object geometry, and preserved deleted state.
- 2026-06-24: E3/#2193 deleted report/label visual-property geometry copy JSON coverage deepened: Studio host regressions now prove deleted geometry copies expose committed state, mutation state, undo availability, geometry undo labels, copied coordinates, refreshed selected-object geometry, and preserved deleted state.
- 2026-06-24: E3/#2192 deleted report/label visual-property geometry clear mutation-state JSON coverage deepened: Studio host regressions now prove deleted geometry clears expose committed state, mutation state, undo availability, geometry undo labels, refreshed preview bounds, and preserved deleted state.
- 2026-06-24: E3/#2191 deleted report/label visual-property move-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted move-batch missing-selector failures keep move-batch results null, preserve deleted state and object properties, roll back earlier moves, and do not advertise undo availability.
- 2026-06-24: E3/#2190 deleted report/label visual-property move failure undo-state JSON coverage deepened: Studio host regressions now prove deleted visual-property move missing-selector failures keep move results null, preserve deleted state and object properties, and do not advertise undo availability.
- 2026-06-24: E3/#2189 deleted report/label visual-property copy-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted copy-batch missing-selector failures keep copy-batch results null, preserve deleted state and object properties, roll back earlier copies, and do not advertise undo availability.
- 2026-06-24: E3/#2188 deleted report/label visual-property copy failure undo-state JSON coverage deepened: Studio host regressions now prove deleted visual-property copy missing-selector failures keep copy results null, preserve deleted state and object properties, and do not advertise undo availability.
- 2026-06-24: E3/#2187 deleted report/label visual-property clear failure undo-state JSON coverage deepened: Studio host regressions now prove deleted visual-property clear missing-selector failures keep clear results null, preserve deleted state and object properties, and do not advertise undo availability.
- 2026-06-24: E3/#2186 deleted report/label visual-property clear-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted clear-batch missing-selector failures keep clear-batch results null, preserve deleted state and object properties, roll back earlier property clears, and do not advertise undo availability.
- 2026-06-24: E3/#2185 deleted report/label visual-object update-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted update-batch missing-selector failures keep update-batch results null, preserve deleted state and object properties, roll back earlier property mutations, and do not advertise undo availability.
- 2026-06-24: E3/#2184 live report/label visual-object update-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove live update-batch missing-selector failures keep update-batch results null, preserve object properties, roll back earlier property mutations, and do not advertise undo availability.
- 2026-06-24: E3/#2183 deleted report/label visual-object reorder-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted reorder-batch missing-target failures keep reorder-batch results null, preserve deleted state and object ordering, roll back earlier reorder mutations, and do not advertise undo availability.
- 2026-06-24: E3/#2182 live report/label visual-object reorder-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove live reorder-batch missing-target failures keep reorder-batch results null, preserve object ordering, roll back earlier reorder mutations, and do not advertise undo availability.
- 2026-06-24: E3/#2181 deleted report/label visual-object duplicate-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted duplicate-batch collision failures keep duplicate-batch results null, preserve deleted state and object ordering, roll back earlier duplicate mutations, and do not advertise undo availability.
- 2026-06-23: E3/#2180 live report/label visual-object duplicate-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove live duplicate-batch collision failures keep duplicate-batch results null, preserve identities and object ordering, roll back earlier duplicate mutations, and do not advertise undo availability.
- 2026-06-23: E3/#2179 deleted report/label visual-object rename-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove deleted rename-batch collision failures keep rename-batch results null, preserve identities, deleted state, and object ordering, roll back earlier mutations, and do not advertise undo availability.
- 2026-06-23: E3/#2178 live report/label visual-object rename-batch rollback undo-state JSON coverage deepened: Studio host regressions now prove live rename-batch collision failures keep rename-batch results null, preserve identities and object ordering, roll back earlier mutations, and do not advertise undo availability.
- 2026-06-23: E3/#2177 deleted report/label visual-object duplicate-subtree rejection undo-state JSON coverage deepened: Studio host regressions now prove deleted duplicate-subtree collision failures keep duplicate-subtree results null, preserve deleted state and layout rows, report errors, and do not advertise undo availability.
- 2026-06-23: E3/#2176 live report/label visual-object duplicate-subtree rejection undo-state JSON coverage deepened: Studio host regressions now prove collision and missing-selector duplicate-subtree failures keep duplicate-subtree results null, preserve layout rows, report errors, and do not advertise undo availability.
- 2026-06-23: E3/#2175 deleted report/label visual-object duplicate-subtree root-parent JSON coverage deepened: Studio host regressions now prove deleted report/label visual-object duplicate-subtree JSON exposes copied root parent names alongside copied root identity, execution metadata, preserved deleted state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2174 deleted report/label visual-object duplicate-subtree execution metadata JSON coverage deepened: Studio host regressions now prove deleted report/label visual-object duplicate-subtree JSON exposes affected counts, committed state, mutation state, no undo availability, and empty undo labels alongside copied root metadata, preserved deleted state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2173 live report/label visual-object duplicate-subtree empty undo-label JSON coverage deepened: Studio host regressions now prove live report/label visual-object duplicate-subtree JSON exposes empty undo labels when undo is unavailable alongside copied root metadata, affected counts, mutation state, appended copied identities, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2172 deleted report/label visual-object reorder-batch empty undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-object reorder-batch JSON exposes empty undo labels when undo is unavailable alongside affected counts, mutation state, preserved deleted state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2171 live report/label visual-object reorder-batch empty undo-label JSON coverage deepened: Studio host regressions now prove live report/label visual-object reorder-batch JSON exposes empty undo labels when undo is unavailable alongside affected counts, mutation state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2170 deleted report/label visual-object duplicate-batch empty undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-object duplicate-batch JSON exposes empty undo labels when undo is unavailable alongside affected counts, mutation state, appended deleted duplicate identities, preserved deleted state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2169 live report/label visual-object duplicate-batch empty undo-label JSON coverage deepened: Studio host regressions now prove live report/label visual-object duplicate-batch JSON exposes empty undo labels when undo is unavailable alongside affected counts, mutation state, appended duplicate identities, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2168 live report/label visual-object rename-batch undo-label JSON coverage deepened: Studio host regressions now prove live report/label visual-object rename-batch JSON exposes stable renamed-identity undo labels alongside affected counts, mutation state, undo availability, renamed identities, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2167 deleted report/label visual-object rename-batch undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-object rename-batch JSON exposes stable renamed-identity undo labels alongside affected counts, mutation state, undo availability, renamed identities, preserved deleted state, object ordering, and refreshed preview metadata.
- 2026-06-23: E3/#2166 deleted report/label visual-property reorder-batch rejection undo-state JSON coverage deepened: Studio host regressions now prove rejected deleted report/label visual-property reorder-batch JSON does not advertise undo availability alongside stale-result suppression, direct-field and selector errors, preserved DBF-backed fields, deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2165 deleted report/label visual-property reorder rejection undo-state JSON coverage deepened: Studio host regressions now prove rejected deleted report/label visual-property reorder JSON does not advertise undo availability alongside stale-result suppression, direct-field and selector errors, preserved DBF-backed fields, deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2164 deleted report/label visual-property rename-batch rejection undo-state JSON coverage deepened: Studio host regressions now prove rejected deleted report/label visual-property rename-batch JSON does not advertise undo availability alongside stale-result suppression, direct-field and selector errors, preserved DBF-backed fields, deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2163 deleted report/label visual-property rename rejection undo-state JSON coverage deepened: Studio host regressions now prove rejected deleted report/label visual-property rename JSON does not advertise undo availability alongside stale-result suppression, direct-field and selector errors, preserved DBF-backed fields, deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2162 deleted report/label visual-property move-batch undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-property move-batch JSON exposes stable moved-property undo labels alongside affected counts, mutation state, undo availability, moved values, preserved deleted state, source clearing, and refreshed preview metadata.
- 2026-06-23: E3/#2161 deleted report/label visual-property move undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-property move JSON exposes stable moved-property undo labels alongside affected counts, mutation state, undo availability, moved values, preserved deleted state, source clearing, and refreshed preview metadata.
- 2026-06-23: E3/#2160 deleted report/label visual-property copy-batch undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-property copy-batch JSON exposes stable copied-property undo labels alongside affected counts, mutation state, undo availability, copied values, preserved deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2159 deleted report/label visual-property copy undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-property copy JSON exposes stable copied-property undo labels alongside affected counts, mutation state, undo availability, copied values, preserved deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2158 deleted report/label visual-property clear-batch undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label visual-property clear-batch JSON exposes the latest undo label alongside affected counts, mutation state, undo availability, preserved deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2157 deleted report/label visual-property clear undo-label JSON coverage deepened: Studio host regressions now prove deleted report/label expression and geometry visual-property clears expose stable undo labels alongside affected counts, mutation state, undo availability, preserved deleted state, and refreshed preview metadata.
- 2026-06-23: E3/#2156 stable report/label visual-object update-batch undo-label JSON coverage deepened: Studio host regressions now prove live and deleted report/label stable visual-object update-batch JSON exposes the latest undo label alongside aggregate affected counts, mutation state, undo availability, persisted property updates, and refreshed report/label preview metadata.
- 2026-06-23: E3/#2155 stable label selection toolbox batch create-dispatch catalog field payload coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create-dispatch catalogs expose caller label caption payloads and batch dispatch arguments inside the stable selected-context batch dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2154 stable label selection toolbox batch create-plan catalog field payload coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create-plan catalogs expose caller label caption payloads inside the stable selected-context batch catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2153 stable report selection toolbox batch create-dispatch catalog field payload coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create-dispatch catalogs expose caller report label caption payloads and batch dispatch arguments inside the stable selected-context batch dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2152 stable report selection toolbox batch create-plan catalog field payload coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create-plan catalogs expose caller report label caption payloads inside the stable selected-context batch catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2151 stable label selection toolbox create-dispatch catalog field payload coverage deepened: Studio host regressions now prove label-expression selection toolbox create-dispatch catalogs expose caller label caption payloads and dispatch arguments inside the stable selected-context dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2150 stable label selection toolbox create-plan catalog field payload coverage deepened: Studio host regressions now prove label-expression selection toolbox create-plan catalogs expose caller label caption payloads inside the stable selected-context catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2149 stable report selection toolbox create-dispatch catalog field payload coverage deepened: Studio host regressions now prove report-expression selection toolbox create-dispatch catalogs expose caller report label caption payloads and dispatch arguments inside the stable selected-context dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2148 stable report selection toolbox create-plan catalog field payload coverage deepened: Studio host regressions now prove report-expression selection toolbox create-plan catalogs expose caller report label caption payloads inside the stable selected-context catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2147 stable report toolbox batch create-dispatch catalog field payload coverage deepened: Studio host regressions now prove direct report-context toolbox batch create-dispatch catalogs expose caller report label caption payloads and batch dispatch arguments inside the stable report batch dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2146 stable report toolbox batch create-plan catalog field payload coverage deepened: Studio host regressions now prove direct report-context toolbox batch create-plan catalogs expose caller report label caption payloads inside the stable report batch catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2145 stable report toolbox create-dispatch catalog field payload coverage deepened: Studio host regressions now prove direct report-context toolbox create-dispatch catalogs expose caller report label caption payloads and dispatch arguments inside the stable report dispatch catalog envelope while preserving admitted non-dry-run mutation intent, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2144 roadmap latest-child guidance aligned: Active roadmap and handoff guidance now consistently points past stale `#2142` text after the `#2143` report toolbox create-plan catalog field payload slice, while preserving `#2143` as the latest implementation coverage reference.
- 2026-06-23: E3/#2143 stable report toolbox create-plan catalog field payload coverage deepened: Studio host regressions now prove direct report-context toolbox create-plan catalogs expose caller report label caption payloads inside the stable report catalog envelope while remaining dry-run, non-mutating, and free of form-only TextBox metadata.
- 2026-06-23: E3/#2142 stable report toolbox create-from-dispatch plan coverage added: Studio host regressions now prove explicit report-expression toolbox create-from-dispatch plans expose stable report-context create-plan payloads, dispatch-provided report toolbox context, caller report label parent and caption payloads, dry-run/non-mutating state, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2141 stable report toolbox batch create-from-dispatch plan coverage deepened: Studio host regressions now prove explicit report-expression toolbox batch create-from-dispatch plans expose stable report-context batch create-plan payloads, dispatch-provided report toolbox context, caller report label parent and caption payloads, dry-run/non-mutating state, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2140 stable label toolbox batch create-from-dispatch plan coverage deepened: Studio host regressions now prove explicit label-expression toolbox batch create-from-dispatch plans expose stable report-context batch create-plan payloads, dispatch-provided report toolbox context, caller label parent and caption payloads, dry-run/non-mutating state, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2139 stable label toolbox create-from-dispatch plan coverage deepened: Studio host regressions now prove explicit label-expression toolbox create-from-dispatch plans expose stable report-context create-plan payloads, dispatch-provided report toolbox context, caller label parent and caption payloads, dry-run/non-mutating state, no asset mutation, and no form-only TextBox metadata.
- 2026-06-23: E3/#2138 stable report toolbox batch create-from-dispatch execution coverage deepened: Studio host regressions now prove explicit report-expression toolbox batch create-from-dispatch execution exposes stable report-context batch and create-result payloads, caller report label caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2137 stable report toolbox create-from-dispatch execution coverage deepened: Studio host regressions now prove explicit report-expression toolbox create-from-dispatch execution exposes stable report-context create-result payloads, caller report label caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2136 report toolbox batch dispatch-from-dispatch plan coverage deepened: Studio host regressions now prove explicit report-expression toolbox batch dispatch-from-dispatch plans expose stable report-context batch dispatch payloads, caller report label parent and caption payloads, preserved batch-create dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, and no form-only TextBox metadata.
- 2026-06-23: E3/#2135 report toolbox create-dispatch-from-dispatch plan coverage deepened: Studio host regressions now prove explicit report-expression toolbox create-dispatch-from-dispatch plans expose stable report-context dispatch-plan payloads, caller report label parent and caption payloads, preserved create dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, and no form-only TextBox metadata.
- 2026-06-23: E3/#2134 label toolbox batch dispatch-from-dispatch plan coverage deepened: Studio host regressions now prove explicit label-expression toolbox batch dispatch-from-dispatch plans expose stable report-context batch dispatch payloads, caller label parent and caption payloads, preserved batch-create dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, and no form-only TextBox metadata.
- 2026-06-23: E3/#2133 label toolbox create-dispatch-from-dispatch plan coverage deepened: Studio host regressions now prove explicit label-expression toolbox create-dispatch-from-dispatch plans expose stable report-context dispatch-plan payloads, caller label parent and caption payloads, preserved create dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, and no form-only TextBox metadata.
- 2026-06-23: E3/#2132 label toolbox batch create-from-dispatch execution coverage deepened: Studio host regressions now prove explicit label-expression toolbox batch create-from-dispatch execution exposes stable report-context batch and create-result payloads, caller label parent and caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2131 label toolbox create-from-dispatch execution coverage deepened: Studio host regressions now prove explicit label-expression toolbox create-from-dispatch execution exposes stable report-context create-result payloads, caller label parent and caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2130 label selection toolbox batch create execution coverage deepened: Studio host regressions now prove label-expression selection toolbox batch creates expose stable selected-context result payloads, caller label parent and caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2129 label selection toolbox create execution coverage deepened: Studio host regressions now prove label-expression selection toolbox creates expose stable selected-context result payloads, caller label parent and caption payloads, persisted report-safe label captions, empty create errors, admitted non-dry-run mutation intent, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2128 label selection toolbox batch create-dispatch catalog coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create-dispatch catalogs expose stable selected-context catalog payloads, nested label batch dispatches, launch and dispatch state, caller label parent payloads, admitted non-dry-run mutation intent, no asset mutation, ready/blocked summaries, and no form-only TextBox dispatches.
- 2026-06-23: E3/#2127 label selection toolbox batch create-plan catalog coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create-plan catalogs expose stable selected-context catalog payloads, nested label batch plans, launch state, caller label parent payloads, dry-run/non-mutating state, ready/blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2126 label selection toolbox create-dispatch catalog coverage deepened: Studio host regressions now prove label-expression selection toolbox create-dispatch catalogs expose stable selected-context catalog payloads, launch and dispatch state, caller label parent payloads, admitted non-dry-run mutation intent, no asset mutation, ready/blocked summaries, and no form-only TextBox dispatches.
- 2026-06-23: E3/#2125 label selection toolbox create-plan catalog coverage deepened: Studio host regressions now prove label-expression selection toolbox create-plan catalogs expose stable selected-context catalog payloads, launch state, caller label parent payloads, dry-run/non-mutating state, ready/blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2124 label selection toolbox batch create-dispatch plan coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create-dispatch plans expose stable selected-context result payloads, nested label batch dispatch plans, caller label parent and caption dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, empty blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2123 label selection toolbox create-dispatch plan coverage deepened: Studio host regressions now prove label-expression selection toolbox create-dispatch plans expose stable selected-context result payloads, caller label parent dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, empty blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2122 label selection toolbox batch create-plan coverage deepened: Studio host regressions now prove label-expression selection toolbox batch create plans expose stable selected-context result payloads, nested label batch plans, caller label parent and caption payloads, ready/blocked summaries, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2121 label selection toolbox create-plan coverage deepened: Studio host regressions now prove label-expression selection toolbox create plans expose stable selected-context result payloads, caller label parent payloads, ready/blocked summaries, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2120 report selection toolbox batch create-plan coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create plans expose stable selected-context result payloads, nested report batch plans, caller report parent and caption payloads, ready/blocked summaries, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2119 report selection toolbox create-plan coverage deepened: Studio host regressions now prove report-expression selection toolbox create plans expose stable selected-context result payloads, caller report parent payloads, ready/blocked summaries, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2118 report selection toolbox batch create-dispatch plan coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create-dispatch plans expose stable selected-context result payloads, nested report batch dispatch plans, caller report parent and caption dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, empty blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2117 report selection toolbox create-dispatch plan coverage deepened: Studio host regressions now prove report-expression selection toolbox create-dispatch plans expose stable selected-context result payloads, caller report parent dispatch arguments, admitted non-dry-run mutation intent, non-executing/no-mutation state, empty blocked summaries, and no form-only TextBox plans.
- 2026-06-23: E3/#2116 report selection toolbox batch create execution coverage deepened: Studio host regressions now prove report-expression selection toolbox batch creates expose stable selected-context result payloads, caller report parent and caption payloads, persisted report label captions, empty create errors, mutation count, and no form-only TextBox metadata.
- 2026-06-23: E3/#2115 report selection toolbox create execution coverage deepened: Studio host regressions now prove report-expression selection toolbox creates expose stable selected-context result payloads, caller report parent and caption payloads, persisted report label captions, empty create errors, admitted non-dry-run mutation intent, and no form-only TextBox metadata.
- 2026-06-23: E3/#2114 report selection toolbox batch create-dispatch catalog coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create-dispatch catalogs expose stable selected-context catalog payloads, nested report batch dispatches, caller report parent payloads, admitted non-dry-run mutation intent, and no form-only TextBox dispatches without mutating assets.
- 2026-06-23: E3/#2113 report selection toolbox batch create-plan catalog coverage deepened: Studio host regressions now prove report-expression selection toolbox batch create-plan catalogs expose stable selected-context catalog payloads, nested report batch plans, caller report parent payloads, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2112 report selection toolbox create-dispatch catalog coverage deepened: Studio host regressions now prove report-expression selection toolbox create-dispatch catalogs expose stable selected-context catalog payloads, launch and dispatch state, caller report parent payloads, admitted non-dry-run mutation intent, and no form-only TextBox dispatches without mutating assets.
- 2026-06-23: E3/#2111 report selection toolbox create-plan catalog coverage deepened: Studio host regressions now prove report-expression selection toolbox create-plan catalogs expose stable selected-context catalog payloads, launch state, caller report parent payloads, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2110 direct report toolbox batch dispatch catalog coverage deepened: Studio host regressions now prove direct report-context toolbox batch dispatch catalogs expose stable catalog payloads, nested report batch dispatches, caller report parent payloads, admitted non-dry-run mutation intent, and no form-only TextBox dispatches without mutating assets.
- 2026-06-23: E3/#2109 direct report toolbox batch create-plan catalog coverage deepened: Studio host regressions now prove direct report-context toolbox batch create-plan catalogs expose stable catalog payloads, nested report batch plans, caller report parent payloads, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2108 direct report toolbox create-dispatch catalog coverage deepened: Studio host regressions now prove direct report-context toolbox create-dispatch catalogs expose stable catalog payloads, report dispatch counts, caller report parent payloads, admitted non-dry-run mutation intent, and no form-only TextBox dispatches without mutating assets.
- 2026-06-23: E3/#2107 direct report toolbox create-plan catalog coverage deepened: Studio host regressions now prove direct report-context toolbox create-plan catalogs expose stable catalog payloads, report plan counts, caller report parent payloads, dry-run/non-mutating state, and no form-only TextBox plans.
- 2026-06-23: E3/#2106 report toolbox batch create-from-dispatch execution coverage deepened: Studio host regressions now prove direct report-expression toolbox batch create-from-dispatch execution preserves report label field payloads, persists the created label caption, summarizes empty create errors, and continues to exclude form-only TextBox metadata.
- 2026-06-23: E3/#2105 report toolbox create-from-dispatch execution coverage deepened: Studio host regressions now prove direct report-expression toolbox create-from-dispatch execution preserves report label field payloads, persists the created label caption, summarizes empty create errors, and continues to exclude form-only TextBox metadata.
- 2026-06-23: E3/#2104 direct report toolbox create execution coverage deepened: Studio host regressions now prove direct report-context toolbox creates append exactly one report-safe label object, preserve label parent and caption payloads, summarize created label identities, and exclude form-only TextBox metadata.
- 2026-06-23: E3/#2103 direct report toolbox create dispatch-plan routing coverage shipped: Studio host regressions now prove direct report-context toolbox create dispatch plans expose report-safe label plans, deterministic label create arguments, report context propagation, label parent/field payloads, and no form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2102 direct report toolbox create-plan routing coverage shipped: Studio host regressions now prove direct report-context toolbox create plans expose report-safe label plans, generated label identities, label parent/field payloads, plan-ready summaries, dry-run state, and no form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2101 direct report toolbox batch create-plan routing coverage shipped: Studio host regressions now prove direct report-context toolbox batch create plans expose report-safe label plans, generated label identities, label parent/field payloads, plan-ready summaries, dry-run state, and no form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2100 direct report toolbox batch dispatch-plan routing coverage shipped: Studio host regressions now prove direct report-context toolbox batch dispatch plans expose report-safe label batch plans, deterministic batch-create dispatch arguments, report context propagation, label parent/field payloads, and no form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2099 direct report toolbox batch create execution coverage shipped: Studio host regressions now prove direct report-context toolbox batch creates append report-safe label objects, summarize created label identities, preserve label parent/field payloads, and exclude form-only TextBox metadata.
- 2026-06-23: E3/#2098 explicit label toolbox batch create-dispatch-from-dispatch plan routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox batch create-dispatch-from-dispatch plans resolve report toolbox contexts, expose report-safe label batch dispatch plans, preserve label field/parent dispatch arguments, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2097 explicit report toolbox batch create-dispatch-from-dispatch plan routing coverage shipped: Studio host regressions now prove report_expression toolbox batch create-dispatch-from-dispatch plans resolve report toolbox contexts, expose report-safe label batch dispatch plans, preserve label field/parent dispatch arguments, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2096 explicit label toolbox batch create-from-dispatch routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox batch create-from-dispatch execution resolves report toolbox contexts, creates report-safe label objects, summarizes created label identities, preserves label field/parent overrides, and excludes form-only TextBox metadata.
- 2026-06-23: E3/#2095 explicit label toolbox batch create-from-dispatch plan routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox batch create-from-dispatch plans resolve report toolbox contexts, expose report-safe label batch plans, preserve label field/parent overrides, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2094 explicit report toolbox batch create-from-dispatch plan routing coverage shipped: Studio host regressions now prove report_expression toolbox batch create-from-dispatch plans resolve report toolbox contexts, expose report-safe label batch plans, preserve label field/parent overrides, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2093 explicit label toolbox create-dispatch-from-dispatch plan routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox create-dispatch-from-dispatch plans resolve report toolbox contexts, expose report-safe label dispatch plans, preserve label field/parent dispatch arguments, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2092 explicit report toolbox create-dispatch-from-dispatch plan routing coverage shipped: Studio host regressions now prove report_expression toolbox create-dispatch-from-dispatch plans resolve report toolbox contexts, expose report-safe label dispatch plans, preserve label field/parent dispatch arguments, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2091 explicit label toolbox create-from-dispatch routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox create-from-dispatch execution resolves report toolbox contexts, creates report-safe label objects, summarizes created label identities, preserves label field/parent overrides, and excludes form-only TextBox metadata.
- 2026-06-23: E3/#2090 explicit label toolbox create-from-dispatch plan routing coverage shipped: Studio host regressions now prove explicit label_expression toolbox create-from-dispatch plans resolve report toolbox contexts, expose report-safe label plans, preserve label field/parent overrides, and exclude form-only TextBox plans without mutating assets.
- 2026-06-23: E3/#2089 explicit label toolbox batch create-plan catalog routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox batch create-plan catalogs preserve label selections, resolve report toolbox contexts, expose report-safe label plan catalog entries, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2088 explicit label toolbox batch dispatch-catalog routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox batch create-dispatch catalogs preserve label selections, resolve report toolbox contexts, expose report-safe label dispatch catalog entries, preserve report dispatch context arguments, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2087 explicit label toolbox batch create-plan routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox batch create plans preserve label selections, resolve report toolbox contexts, expose report-safe label plans, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2086 explicit label toolbox batch dispatch-plan routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox batch create-dispatch plans preserve label selections, resolve report toolbox contexts, expose report-safe label dispatch plans, preserve report dispatch arguments, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2085 explicit label toolbox batch create routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox batch creates preserve label selections, resolve report toolbox contexts, create report-safe label objects, summarize created label identities, and exclude form-only TextBox metadata.
- 2026-06-23: E3/#2084 explicit label toolbox create routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox creates preserve label selections, resolve report toolbox contexts, create report-safe label objects, summarize created label identities, and exclude form-only TextBox metadata.
- 2026-06-23: E3/#2083 explicit label toolbox create-dispatch catalog routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox create-dispatch catalogs preserve label selections, resolve report toolbox contexts, include report-safe label dispatch entries, preserve report dispatch contexts, and exclude form-only TextBox dispatches.
- 2026-06-23: E3/#2082 explicit label toolbox create-dispatch plan routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox create-dispatch plans preserve label selections, resolve report toolbox contexts, include report-safe label dispatches, preserve report dispatch arguments, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2081 explicit label toolbox create-plan routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox create plans preserve label selections, resolve report toolbox contexts, include report-safe label plans, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2080 explicit label toolbox create-plan catalog routing coverage shipped: Studio host regressions now prove explicit label_expression selection toolbox create-plan catalogs preserve label selections, resolve report toolbox contexts, include report-safe label plans, and exclude form-only TextBox plans.
- 2026-06-23: E3/#2079 explicit report/label toolbox identity coverage shipped: Studio host regressions now prove explicit report_expression and label_expression selections expose report-safe label/image/line/shape toolbox item identities while continuing to exclude form-only TextBox metadata.
- 2026-06-23: E3/#2078 explicit report/label expression context metadata coverage shipped: Studio host regressions now prove explicit report_expression and label_expression selections expose expression-editor, toolbox, data-environment action metadata, and report-safe toolbox item counts while excluding form-only TextBox toolbox metadata.
- 2026-06-23: E3/#2077 default report/label toolbox metadata coverage shipped: Studio host regressions now prove default report and label expression contexts expose report-safe label/image/line/shape toolbox metadata and exclude form-only TextBox toolbox metadata.
- 2026-06-23: E3/#2076 default report/label expression designer-action metadata coverage shipped: Studio host regressions now prove default report and label expression contexts expose expression-editor, toolbox, and data-environment editor actions while retaining their context-specific builders.
- 2026-06-23: E3/#2075 report DataEnvironment designer-context metadata coverage shipped: Studio host regressions now prove report DataEnvironment symbol routing exposes data-environment editor-action metadata and zero toolbox metadata while preserving data-environment builder routing.
- 2026-06-23: E3/#2074 report/label DataEnvironment designer-context isolation coverage shipped: Studio host regressions now prove report and label DataEnvironment selections route away from report/label expression contexts and do not leak report-builder or label-wizard metadata while preserving data-environment builder metadata.
- 2026-06-23: E3/#2073 default report designer-context coverage shipped: Studio host regressions now prove default `.frx` report JSON serializes the report-expression context, preserves report document identity, exposes report-builder metadata, and does not leak label-wizard metadata.
- 2026-06-23: E3/#2072 explicit report-expression designer-context coverage shipped: Studio host regressions now prove explicit `--selection-context report_expression` JSON serializes the report-expression context, exposes report-builder metadata, and does not leak label-wizard metadata.
- 2026-06-23: E3/#2071 stable deleted group-footer object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted group-footer report/label object JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, retains deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2070 stable group-footer object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live group-footer report/label object JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, retains containing group-footer metadata, and retains label identity.
- 2026-06-23: E3/#2069 stable deleted group-header object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted group-header report/label object JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, retains deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2068 stable group-header object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live group-header report/label object JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, retains containing group-header metadata, and retains label identity.
- 2026-06-23: E3/#2067 stable deleted summary object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted summary report/label object JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, retains deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2066 stable summary object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live summary report/label object JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, retains containing summary section metadata, and retains label identity.
- 2026-06-23: E3/#2065 stable deleted visual-property reorder-batch rejection preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property reorder-batch rejection JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining rejected deleted selected-object context and label identity.
- 2026-06-23: E3/#2064 stable deleted visual-property reorder rejection preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property reorder rejection JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining rejected deleted selected-object context and label identity.
- 2026-06-23: E3/#2063 stable deleted visual-property rename-batch rejection preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property rename-batch rejection JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining rejected deleted selected-object context and label identity.
- 2026-06-23: E3/#2062 stable deleted visual-property rename rejection preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property rename rejection JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining rejected deleted selected-object context and label identity.
- 2026-06-23: E3/#2061 stable deleted visual-property move preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property move JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while refreshing moved deleted selected-object context and retaining label identity.
- 2026-06-23: E3/#2060 stable deleted visual-property copy preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property copy JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while refreshing copied deleted selected-object context and retaining label identity.
- 2026-06-23: E3/#2059 stable deleted visual-property clear preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property clear JSON preserves live `previewBounds*` summaries, refreshes deleted `deletedPreviewBounds*` summaries after clearing deleted-row geometry, preserves cleared deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2058 stable deleted visual-property move-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property move-batch JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while refreshing moved deleted selected-object context and retaining label identity.
- 2026-06-23: E3/#2057 stable deleted visual-property copy-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property copy-batch JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while refreshing copied deleted selected-object context and retaining label identity.
- 2026-06-23: E3/#2056 stable deleted visual-property clear-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-property clear-batch JSON preserves live `previewBounds*` summaries, refreshes deleted `deletedPreviewBounds*` summaries after clearing deleted-row geometry, preserves cleared deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2055 stable deleted object reorder-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-object reorder-batch JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, refreshes selected deleted-object order context, and retains label identity.
- 2026-06-23: E3/#2054 stable live object reorder-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live visual-object reorder-batch JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, refreshes selected-object order context, and retains label identity.
- 2026-06-23: E3/#2053 stable deleted object duplicate-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-object duplicate-batch JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, refreshes selected copied-deleted-object context, and retains label identity.
- 2026-06-23: E3/#2052 stable live object duplicate-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live visual-object duplicate-batch JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, refreshes selected copied-object context, and retains label identity.
- 2026-06-23: E3/#2051 stable deleted object rename-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-object rename-batch JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, refreshes deleted selected-object identity context, and retains label identity.
- 2026-06-23: E3/#2050 stable live object rename-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live visual-object rename-batch JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` summaries unavailable/zero, refreshes selected-object identity context, and retains label identity.
- 2026-06-23: E3/#2049 stable deleted object update-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label visual-object update-batch JSON preserves live `previewBounds*` summaries, refreshes deleted `deletedPreviewBounds*` summaries after deleted geometry edits, preserves deleted selected-object context, and retains label identity.
- 2026-06-23: E3/#2048 stable live object update-batch preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live visual-object update-batch JSON refreshes live `previewBounds*` summaries after batch geometry edits while preserving deleted `deletedPreviewBounds*` summaries, selected-object context, and label identity.
- 2026-06-23: E3/#2047 stable deleted-object subtree duplicate preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted report/label object subtree duplicate JSON preserves live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries, refreshes deleted copied-object context, and retains label identity.
- 2026-06-23: E3/#2046 stable object subtree duplicate preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label object subtree duplicate JSON preserves live `previewBounds*` summaries, keeps deleted `deletedPreviewBounds*` unavailable/zero, refreshes selected copied-object context, and retains label identity.
- 2026-06-23: E3/#2045 stable object subtree delete/restore preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label object subtree delete and restore JSON preserves live `previewBounds*` summaries and exposes/clears deleted `deletedPreviewBounds*` summaries while moving the selected layout object between live/deleted state, preserving selected-object provenance, and retaining label identity.
- 2026-06-23: E3/#2044 stable mixed deleted-states preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label `--deleted-states` mixed batch delete and restore JSON preserves refreshed live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while moving settings, section, and layout-object records between live/deleted state and retaining label identity.
- 2026-06-23: E3/#2043 stable object deleted-states preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label `--deleted-states` batch delete and restore JSON preserves live `previewBounds*` summaries and refreshes deleted `deletedPreviewBounds*` summaries while moving layout objects between live/deleted state and retaining label identity.
- 2026-06-23: E3/#2042 stable deleted-states settings/section preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label `--deleted-states` batch delete and restore JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while moving settings plus section records between live/deleted state and retaining label identity.
- 2026-06-23: E3/#2041 stable-selected settings delete/restore preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label settings delete and restore JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while moving settings between live/deleted state, preserving selected-settings provenance, and retaining label identity.
- 2026-06-23: E3/#2040 record-selected settings delete/restore preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label settings delete and restore JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while moving settings between live/deleted state and retaining label identity.
- 2026-06-23: E3/#2039 stable-selected settings-memo mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `EXPR` settings memo update/clear JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2038 record-selected settings-memo mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `EXPR` settings memo update/clear JSON preserves live `previewBounds*` summaries and deleted `deletedPreviewBounds*` summaries while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2037 stable-selected column-setup mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `EXPR` column setup update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2036 record-selected column-setup mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `EXPR` column setup update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2035 stable-selected column-spacing mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `COLSPACING` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2034 record-selected column-spacing mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `COLSPACING` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2033 stable-selected column-width mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `COLWIDTH` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2032 record-selected column-width mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `COLWIDTH` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2031 stable-selected column-count mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `COLS` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2030 record-selected column-count mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `COLS` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining column setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2029 stable-selected bottom-margin mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `BOTMARGIN` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2028 record-selected bottom-margin mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `BOTMARGIN` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2027 stable-selected horizontal-grid mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `GRIDH` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2026 record-selected horizontal-grid mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `GRIDH` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2025 stable-selected vertical-grid mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `GRIDV` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2024 record-selected vertical-grid mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `GRIDV` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2023 stable-selected orientation mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `ORIENTATION` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2022 record-selected orientation mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `ORIENTATION` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2021 stable-selected paper-size mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `PAPERSIZE` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2020 record-selected paper-size mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `PAPERSIZE` update/clear JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2019 stable-selected page-margin mutation preview-metadata coverage shipped: Studio host regressions now prove stable-selected report/label live and deleted `TOPMARGIN` update/clear JSON preserves refreshed live `previewBounds*` summaries and deleted-object `deletedPreviewBounds*` summaries while retaining page setup values, selected-settings provenance, and label identity.
- 2026-06-23: E3/#2018 record-selected page-margin mutation preview-metadata coverage shipped: Studio host regressions now prove record-selected report/label live and deleted `TOPMARGIN` update/clear JSON preserves refreshed live `previewBounds*` summaries and deleted-object `deletedPreviewBounds*` summaries while retaining page setup values, setting provenance, selected-settings state, and label identity.
- 2026-06-23: E3/#2017 report/label column setup preview-metadata coverage shipped: Studio host regressions now prove report and label column setup JSON keeps live/deleted `previewBounds*` availability false with zero bounds for setup-only FRX/LBX assets while preserving column setup values, setting provenance, and label identity.
- 2026-06-23: E3/#2016 stable-selected generic report/label settings preview-metadata coverage shipped: Studio host regressions now prove generic stable-selected live/deleted root settings JSON for FRX/LBX assets preserves live `previewBounds*` summaries, preserves deleted object `deletedPreviewBounds*` summaries, and retains selected-settings provenance, page setup state, unavailable section/object selections, and label identity.
- 2026-06-23: E3/#2015 stable-selected generic report/label object preview-metadata coverage shipped: Studio host regressions now prove generic stable-selected live/deleted object JSON for FRX/LBX assets preserves live `previewBounds*` summaries, preserves/exposes deleted object `deletedPreviewBounds*` summaries, and retains selected-object metadata, containing-section metadata/nullability, unavailable section/settings selections, and label identity.
- 2026-06-23: E3/#2014 stable-selected generic report/label section preview-metadata coverage shipped: Studio host regressions now prove generic stable-selected detail section JSON for FRX/LBX assets preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable for live selections, preserves live unplaced-object `previewBounds*` summaries for deleted section selections, exposes deleted section `deletedPreviewBounds*` summaries, and retains selected-section metadata, unplaced accounting, null object/settings selections, and label identity.
- 2026-06-23: E3/#2013 stable-selected deleted page-header report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted page-header section JSON for FRX/LBX assets preserves remaining live `previewBounds*` summaries, exposes deleted page-header/fixture `deletedPreviewBounds*` summaries, retains selected deleted page-header section metadata, live/deleted counts, unplaced accounting, null object/settings selections, and label identity.
- 2026-06-23: E3/#2012 stable-selected page-header report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected page-header section JSON for FRX/LBX assets preserves live `previewBounds*` summaries, preserves fixture deleted `deletedPreviewBounds*` summaries, retains selected page-header section metadata and object membership, keeps object/settings selections unavailable, and retains label identity.
- 2026-06-23: E3/#2011 selected deleted page-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-header label section JSON retains label identity, preserves remaining live detail/page-footer `previewBounds*` summaries, exposes deleted page-header `deletedPreviewBounds*` summaries, and retains selected deleted page-header section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2010 selected deleted page-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-header report section JSON preserves remaining live detail/page-footer `previewBounds*` summaries, exposes deleted page-header `deletedPreviewBounds*` summaries, and retains selected deleted page-header section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2009 selected page-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-header label section JSON retains label identity, preserves full page-header/detail/page-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected page-header section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2008 selected page-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-header report section JSON preserves full page-header/detail/page-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected page-header section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2007 selected deleted column-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted column-footer label section JSON retains label identity, preserves remaining live column-header/detail `previewBounds*` summaries, exposes deleted column-footer `deletedPreviewBounds*` summaries, and retains selected deleted column-footer section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2006 selected deleted column-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted column-footer report section JSON preserves remaining live column-header/detail `previewBounds*` summaries, exposes deleted column-footer `deletedPreviewBounds*` summaries, and retains selected deleted column-footer section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2005 selected column-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live column-footer label section JSON retains label identity, preserves full column-header/detail/column-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected column-footer section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2004 selected column-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live column-footer report section JSON preserves full column-header/detail/column-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected column-footer section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2003 selected deleted column-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted column-header label section JSON retains label identity, preserves remaining live detail/column-footer `previewBounds*` summaries, exposes deleted column-header `deletedPreviewBounds*` summaries, and retains selected deleted column-header section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2002 selected deleted column-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted column-header report section JSON preserves remaining live detail/column-footer `previewBounds*` summaries, exposes deleted column-header `deletedPreviewBounds*` summaries, and retains selected deleted column-header section metadata plus live sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2001 selected column-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live column-header label section JSON retains label identity, preserves full column-header/detail/column-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected column-header section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#2000 selected column-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live column-header report section JSON preserves full column-header/detail/column-footer `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected column-header section metadata plus sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1999 selected deleted page-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-footer label section JSON retains label identity, preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted page-footer section, and retains selected deleted page-footer section metadata plus live title/detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1998 selected deleted page-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-footer report section JSON preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted page-footer section, and retains selected deleted page-footer section metadata plus live title/detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1997 selected page-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-footer label section JSON retains label identity, preserves full-layout `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected page-footer section metadata plus title/detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1996 selected page-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-footer report section JSON preserves full-layout `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected page-footer section metadata plus title/detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1995 selected deleted title label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted title label section JSON retains label identity, preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted title section, and retains selected deleted title section metadata plus live detail/page-footer sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1994 selected deleted title report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted title report section JSON preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted title section, and retains selected deleted title section metadata plus live detail/page-footer sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1993 selected title label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live title label section JSON retains label identity, preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected title section metadata plus live detail/page-footer sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1992 selected title report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live title report section JSON preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected title section metadata plus live detail/page-footer sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1991 selected deleted summary label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted summary label section JSON retains label identity, preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted summary section, and retains selected deleted summary section metadata plus live detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1990 selected deleted summary report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted summary report section JSON preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted summary section, and retains selected deleted summary section metadata plus live detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1989 selected summary label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live summary label section JSON retains label identity, preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected summary section metadata plus live detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1988 selected summary report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live summary report section JSON preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected summary section metadata plus live detail sibling context with null object/settings selection metadata.
- 2026-06-23: E3/#1987 selected deleted group-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted group-footer label section JSON retains label identity, preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted group-footer section, and retains selected deleted group-footer expression metadata plus live group-header/detail context with null object/settings selection metadata.
- 2026-06-23: E3/#1986 selected deleted group-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted group-footer report section JSON preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted group-footer section, and retains selected deleted group-footer expression metadata plus live group-header/detail context with null object/settings selection metadata.
- 2026-06-23: E3/#1985 selected deleted group-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted group-header label section JSON retains label identity, preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted group-header section, and retains selected deleted group-header expression metadata plus live detail/group-footer context with null object/settings selection metadata.
- 2026-06-23: E3/#1984 selected deleted group-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted group-header report section JSON preserves remaining live `previewBounds*` summaries, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted group-header section, and retains selected deleted group-header expression metadata plus live detail/group-footer context with null object/settings selection metadata.
- 2026-06-23: E3/#1983 selected group-footer label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live group-footer label section JSON retains label identity, preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected group-footer expression metadata plus sibling group-header/detail context with null object/settings selection metadata.
- 2026-06-23: E3/#1982 selected group-footer report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live group-footer report section JSON preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected group-footer expression metadata plus sibling group-header/detail context with null object/settings selection metadata.
- 2026-06-23: E3/#1981 selected group-header label section preview-metadata coverage shipped: Studio host regressions now prove record-selected live group-header label section JSON retains label identity, preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected group-header expression metadata plus sibling detail/group-footer context with null object/settings selection metadata.
- 2026-06-23: E3/#1980 selected group-header report section preview-metadata coverage shipped: Studio host regressions now prove record-selected live group-header report section JSON preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected group-header expression metadata plus sibling detail/group-footer context with null object/settings selection metadata.
- 2026-06-23: E3/#1979 selected deleted summary label object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted summary label object JSON retains label identity, preserves live `previewBounds*` summaries for remaining live sections, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted summary object, and retains selected deleted-object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1978 selected deleted summary report object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted summary report object JSON preserves live `previewBounds*` summaries for remaining live sections, exposes deleted `deletedPreviewBounds*` summaries for the selected deleted summary object, and retains selected deleted-object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1977 selected summary label object preview-metadata coverage shipped: Studio host regressions now prove record-selected live summary label object JSON retains label identity, preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected summary object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1976 selected summary report object preview-metadata coverage shipped: Studio host regressions now prove record-selected live summary report object JSON preserves live `previewBounds*` summaries, keeps deleted preview metadata unavailable when no deleted geometry exists, and retains selected summary object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1975 selected deleted page-header label object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-header label object JSON retains label identity, preserves live `previewBounds*` summaries, expands deleted `deletedPreviewBounds*` summaries to include the deleted page-header object, and retains selected deleted-object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1974 selected deleted page-header report object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted page-header report object JSON preserves live `previewBounds*` summaries, expands deleted `deletedPreviewBounds*` summaries to include the deleted page-header object, and retains selected deleted-object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1973 selected page-header label object preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-header label object JSON retains label identity, preserves live `previewBounds*` summaries, deleted `deletedPreviewBounds*` summaries, selected page-header object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1972 selected page-header report object preview-metadata coverage shipped: Studio host regressions now prove record-selected live page-header report object JSON preserves live `previewBounds*` summaries, deleted `deletedPreviewBounds*` summaries, selected page-header object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1971 selected deleted unplaced label object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted unplaced label object JSON retains label identity, preserves live `previewBounds*` summaries for remaining live layout geometry, expands deleted `deletedPreviewBounds*` summaries to include the deleted unplaced object, and retains selected deleted-unplaced object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1970 selected deleted unplaced report object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted unplaced report object JSON preserves live `previewBounds*` summaries for remaining live layout geometry, expands deleted `deletedPreviewBounds*` summaries to include the deleted unplaced object, and retains selected deleted-unplaced object metadata with null section/settings/containing-section metadata.
- 2026-06-23: E3/#1969 selected unplaced label object preview-metadata coverage shipped: Studio host regressions now prove record-selected live unplaced label object JSON retains label identity, preserves live `previewBounds*` summaries spanning live placed and unplaced geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected unplaced-object metadata and null section/settings/containing-section metadata.
- 2026-06-23: E3/#1968 selected unplaced report object preview-metadata coverage shipped: Studio host regressions now prove record-selected live unplaced report object JSON preserves live `previewBounds*` summaries spanning live placed and unplaced geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected unplaced-object metadata and null section/settings/containing-section metadata.
- 2026-06-23: E3/#1967 selected deleted label object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted label object JSON retains label identity, preserves live `previewBounds*` summaries for remaining live layout geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected deleted-object metadata and null section/settings/containing-section metadata.
- 2026-06-23: E3/#1966 selected label object preview-metadata coverage shipped: Studio host regressions now prove record-selected live label object JSON retains label identity, preserves live `previewBounds*` summaries for live section/object geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1965 selected deleted report object preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted report object JSON preserves live `previewBounds*` summaries for remaining live layout geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected deleted-object metadata and null section/settings/containing-section metadata.
- 2026-06-23: E3/#1964 selected report object preview-metadata coverage shipped: Studio host regressions now prove record-selected live report object JSON preserves live `previewBounds*` summaries for live section/object geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-object metadata, containing-section metadata, and null section/settings selection metadata.
- 2026-06-23: E3/#1963 selected deleted label section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted label section JSON retains label identity, preserves live `previewBounds*` summaries for remaining live geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted section geometry while retaining selected deleted-section metadata and null settings/object selection metadata.
- 2026-06-23: E3/#1962 selected label section preview-metadata coverage shipped: Studio host regressions now prove record-selected label section JSON retains label identity, preserves live `previewBounds*` summaries for live section/object geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-section metadata and null settings/object selection metadata.
- 2026-06-23: E3/#1961 selected deleted report section preview-metadata coverage shipped: Studio host regressions now prove record-selected deleted report section JSON preserves live `previewBounds*` summaries for remaining live layout geometry and deleted `deletedPreviewBounds*` summaries for deleted section geometry while retaining selected deleted-section metadata and null settings/object selection metadata.
- 2026-06-23: E3/#1960 selected report section preview-metadata coverage shipped: Studio host regressions now prove record-selected report section JSON preserves live `previewBounds*` summaries for live section/object geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-section metadata and null settings/object selection metadata.
- 2026-06-23: E3/#1959 selected deleted label settings preview-metadata coverage shipped: Studio host regressions now prove selected deleted label settings/root JSON retains label identity, preserves live `previewBounds*` summaries for live layout geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining deleted selected-settings provenance and null section/object selection metadata.
- 2026-06-23: E3/#1958 selected label settings preview-metadata coverage shipped: Studio host regressions now prove selected label settings/root JSON retains label identity, preserves live `previewBounds*` summaries for live layout geometry, and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-settings provenance and null section/object selection metadata.
- 2026-06-23: E3/#1957 selected deleted report settings preview-metadata coverage shipped: Studio host regressions now prove selected deleted report settings/root JSON preserves live `previewBounds*` summaries for live layout geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining deleted selected-settings provenance and null section/object selection metadata.
- 2026-06-23: E3/#1956 selected report settings preview-metadata coverage shipped: Studio host regressions now prove selected report settings/root JSON preserves live `previewBounds*` summaries spanning live section/object geometry and exposes deleted `deletedPreviewBounds*` summaries for deleted layout geometry while retaining selected-settings provenance and null section/object selection metadata.
- 2026-06-23: E3/#1955 stable deleted orphaned detail report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted detail objects orphaned by a deleted containing section preserve live `previewBounds*` summaries spanning remaining live section/object geometry and expose deleted `deletedPreviewBounds*` summaries for deleted section/object geometry, including the selected deleted object, while retaining selected-object metadata, null containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1954 stable orphaned detail report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected detail objects orphaned by a deleted containing section preserve live `previewBounds*` summaries spanning remaining live section/object geometry and expose deleted `deletedPreviewBounds*` summaries for deleted section/object geometry, while retaining selected-object metadata, null containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1953 stable orphaned page-header report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected page-header objects orphaned by a deleted containing section preserve live `previewBounds*` summaries spanning remaining live section/object geometry and expose deleted `deletedPreviewBounds*` summaries for deleted section/object geometry, while retaining selected-object metadata, null containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1952 stable deleted page-header report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted page-header object selection preserves live `previewBounds*` summaries spanning remaining live section/object geometry and exposes deleted `deletedPreviewBounds*` summaries covering the selected deleted page-header object plus fixture deleted geometry, while retaining selected-object metadata, null containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1951 stable page-header report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected page-header object selection exposes live `previewBounds*` summaries spanning section and live object geometry while preserving deleted `deletedPreviewBounds*` summaries from the fixture, selected-object metadata, containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1950 stable deleted unplaced report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted unplaced object selection preserves live `previewBounds*` summaries spanning placed and unplaced live geometry and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted unplaced object, while retaining selected-object metadata, null containing-section metadata, expression provenance, and label identity.
- 2026-06-23: E3/#1949 stable live unplaced report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live unplaced object selection exposes live `previewBounds*` summaries spanning placed and unplaced live geometry while preserving deleted `deletedPreviewBounds*` summaries from the fixture, selected-object metadata, null containing-section metadata, and label identity.
- 2026-06-23: E3/#1948 stable deleted column-footer report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted column-footer object selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted column-footer object, while retaining selected-object metadata and label identity.
- 2026-06-23: E3/#1947 stable live column-footer report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live column-footer object selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted objects or sections exist, while retaining selected-object metadata, containing-section metadata, and label identity.
- 2026-06-23: E3/#1946 stable deleted column-header report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted column-header object selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted column-header object, while retaining selected-object metadata and label identity.
- 2026-06-23: E3/#1945 stable live column-header report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live column-header object selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted objects or sections exist, while retaining selected-object metadata, containing-section metadata, and label identity.
- 2026-06-23: E3/#1944 stable deleted page-footer report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted page-footer object selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted page-footer object, while retaining selected-object metadata and label identity.
- 2026-06-23: E3/#1943 stable live page-footer report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live page-footer object selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted objects or sections exist, while retaining selected-object metadata, containing-section metadata, and label identity.
- 2026-06-23: E3/#1942 stable deleted title-band report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted title-band object selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted title-band object, while retaining selected-object metadata and label identity.
- 2026-06-23: E3/#1941 stable live title-band report/label object preview-metadata coverage shipped: Studio host regressions now prove stable-selected live title-band object selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted objects or sections exist, while retaining selected-object metadata, containing-section metadata, and label identity.
- 2026-06-23: E3/#1940 stable deleted column-footer report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted column-footer section selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted column-footer section, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1939 stable live column-footer report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected live column-footer section selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1938 stable deleted column-header report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted column-header section selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted column-header section, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1937 stable live column-header report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected live column-header section selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1936 stable deleted page-footer report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted page-footer section selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted page-footer section, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1935 stable live page-footer report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected live page-footer section selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1934 stable deleted title report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted title section selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted title section, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1933 stable live title report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected live title section selection exposes live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1932 stable deleted summary report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted summary section selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted summary section, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1931 stable live summary report/label section preview-metadata coverage shipped: Studio host regressions now prove stable-selected live summary section selection preserves live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section metadata and label identity.
- 2026-06-23: E3/#1930 stable deleted group-header/footer report/label section expression preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted group-header and group-footer section expression selection preserves remaining live `previewBounds*` summaries and exposes deleted `deletedPreviewBounds*` summaries for the selected deleted group section, while retaining selected-section expression metadata and label identity.
- 2026-06-23: E3/#1929 stable live group-header/footer report/label section expression preview-metadata coverage shipped: Studio host regressions now prove stable-selected group-header and group-footer section expression selection preserves live `previewBounds*` summaries and does not fabricate deleted preview bounds when no deleted sections exist, while retaining selected-section expression metadata and label identity.
- 2026-06-23: E3/#1928 stable deleted Detail Header/Footer report/label section expression preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted section EXPR updates and clears preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while refreshing selected deleted-section expression metadata, preserving deleted state, and retaining label identity.
- 2026-06-23: E3/#1927 stable live Detail Header/Footer report/label section expression preview-metadata coverage shipped: Studio host regressions now prove stable-selected live section EXPR updates and clears preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while refreshing selected-section expression metadata and retaining label identity.
- 2026-06-23: E3/#1926 evidence-backfill child row shipped: `docs/23-phase-a-dependency-breakdown.md` now records the completed #1925 evidence-maintenance child and this status-row cleanup in the Current Issue Tree Status table, keeping live issue closure evidence aligned with repo guidance.
- 2026-06-23: E3/#1925 layout-object preview evidence-row backfill shipped: `docs/23-phase-a-dependency-breakdown.md` now includes the missing Current Issue Tree Status rows for already-closed #1883-#1896 E3/#24 layout-object preview-metadata slices, preserving the dependency evidence trail without reopening completed implementation work.
- 2026-06-23: E3/#1924 stable-selected deleted report/label section top clear preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX deleted section VPOS clears preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` to the cleared deleted section top, preserve selected deleted-section geometry, and retain label identity.
- 2026-06-23: E3/#1923 stable-selected deleted report/label section top update preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX deleted section VPOS edits preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` with the moved deleted section, preserve selected deleted-section geometry, and retain label identity.
- 2026-06-23: E3/#1922 stable-selected deleted report/label section height clear preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX deleted section HEIGHT clears preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` to the zero-height deleted section, preserve selected deleted-section geometry, and retain label identity.
- 2026-06-23: E3/#1921 stable-selected deleted report/label section height update preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX deleted section HEIGHT edits preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` with the resized deleted section, preserve selected deleted-section geometry, and retain label identity.
- 2026-06-23: E3/#1920 record-selected deleted report/label section height clear preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX deleted section HEIGHT clears preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` to the zero-height deleted section, preserve deleted-section selection geometry, and retain label identity.
- 2026-06-23: E3/#1919 record-selected deleted report/label section height update preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX deleted section HEIGHT edits preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` with the resized deleted section, preserve deleted-section selection geometry, and retain label identity.
- 2026-06-23: E3/#1918 record-selected deleted report/label section top clear preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX deleted section VPOS clears preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` to the cleared deleted section top, preserve deleted-section selection geometry, and retain label identity.
- 2026-06-23: E3/#1917 record-selected deleted report/label section top update preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX deleted section VPOS edits preserve live `previewBounds*` for unplaced live objects, refresh deleted `deletedPreviewBounds*` with the moved deleted section, preserve deleted-section selection geometry, and retain label identity.
- 2026-06-23: E3/#1916 record-selected report/label section top clear preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX section VPOS clears refresh selected-section geometry while preserving live `previewBounds*`, deleted `deletedPreviewBounds*`, section totals, object placement counts, and label identity.
- 2026-06-23: E3/#1915 record-selected report/label section top update preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX section VPOS edits move selected-section geometry while preserving live `previewBounds*`, deleted `deletedPreviewBounds*`, section totals, object placement counts, and label identity.
- 2026-06-23: E3/#1914 stable-selected report/label section top clear preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX section VPOS clears refresh live `previewBounds*` to the default section top/bottom, avoid fabricating deleted preview bounds, preserve section totals and object placement counts, refresh selected-section geometry, and retain label identity.
- 2026-06-23: E3/#1913 stable-selected report/label section top update preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX section VPOS edits refresh live `previewBounds*` with the moved section, avoid fabricating deleted preview bounds, preserve section totals and object placement counts, refresh selected-section geometry, and retain label identity.
- 2026-06-23: E3/#1912 stable-selected report/label section height clear preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX section HEIGHT clears refresh live `previewBounds*` to the zero-height section plus unplaced objects, avoid fabricating deleted preview bounds, refresh object placement counts and selected-section geometry, and retain label identity.
- 2026-06-23: E3/#1911 stable-selected report/label section height update preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX section HEIGHT edits refresh live `previewBounds*` with the updated section bottom, avoid fabricating deleted preview bounds, refresh section totals and selected-section geometry, and retain label identity.
- 2026-06-23: E3/#1910 record-selected report/label section height update preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX section HEIGHT edits refresh section totals and selected-section geometry while preserving live `previewBounds*`, deleted `deletedPreviewBounds*`, placed-object counts, and label identity.
- 2026-06-23: E3/#1909 stable-selected report/label layout-object top clear deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX VPOS clears preserve live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-23: E3/#1908 record-selected report/label layout-object top clear deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX VPOS clears preserve live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-23: E3/#1907 stable-selected report/label layout-object top update deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX VPOS edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object unplaced metadata, and retain label identity.
- 2026-06-23: E3/#1906 record-selected report/label layout-object top update deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX VPOS edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object unplaced metadata, and retain label identity.
- 2026-06-22: E3/#1905 stable-selected report/label layout-object height clear deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX HEIGHT clears preserve live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale height/bottom values, and retain label identity.
- 2026-06-22: E3/#1904 record-selected report/label layout-object height clear deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX HEIGHT clears preserve live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale height/bottom values, and retain label identity.
- 2026-06-22: E3/#1903 stable-selected report/label layout-object height update deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX HEIGHT edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1902 record-selected report/label layout-object height update deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX HEIGHT edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1901 stable-selected report/label layout-object left clear deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX HPOS clears refresh live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale left/right values, and retain label identity.
- 2026-06-22: E3/#1900 record-selected report/label layout-object left clear deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX HPOS clears refresh live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale left/right values, and retain label identity.
- 2026-06-22: E3/#1899 stable-selected report/label layout-object left update deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX HPOS edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1898 record-selected report/label layout-object left update deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX HPOS edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1897 stable-selected report/label layout-object width clear deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX WIDTH clears refresh live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale width/right values, and retain label identity.
- 2026-06-22: E3/#1896 record-selected report/label layout-object width clear deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX WIDTH clears refresh live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata without stale width/right values, and retain label identity.
- 2026-06-22: E3/#1895 stable-selected report/label layout-object width update deleted-preview coverage shipped: Studio host regressions now prove stable-selected FRX/LBX WIDTH edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1894 record-selected report/label layout-object width update deleted-preview coverage shipped: Studio host regressions now prove FRX/LBX WIDTH edits expand live `previewBounds*`, preserve deleted `deletedPreviewBounds*`, refresh selected-object section metadata, and retain label identity.
- 2026-06-22: E3/#1893 stable deleted report/label layout-object restore-as-unplaced preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX edited deleted layout-object restore-as-unplaced reports complete live `previewBounds*`, clears deleted preview availability when no deleted objects remain, keeps selected restored-object metadata out of fabricated containing sections, and retains label identity.
- 2026-06-22: E3/#1892 record-selected deleted report/label layout-object restore-as-unplaced preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX edited deleted layout-object restore-as-unplaced reports complete live `previewBounds*`, clears deleted preview availability when no deleted objects remain, keeps selected restored-object metadata out of fabricated containing sections, and retains label identity.
- 2026-06-22: E3/#1891 stable deleted report/label layout-object restore preview-metadata coverage shipped: Studio host regressions now prove stable-selected FRX/LBX edited deleted layout-object restores refresh live `previewBounds*` with the restored object, clear deleted preview availability when no deleted objects remain, rehydrate selected live-object section metadata, and retain label identity.
- 2026-06-22: E3/#1890 edited unplaced report/label layout-object restore preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX edited unplaced layout-object restores refresh live `previewBounds*` with restored out-of-section geometry, preserve deleted `deletedPreviewBounds*` for remaining deleted objects, keep selected restored-object metadata out of fabricated containing sections, and retain label identity.
- 2026-06-22: E3/#1889 edited unplaced report/label layout-object delete preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX edited unplaced layout-object deletes refresh live `previewBounds*` after removing the out-of-section object, expand deleted `deletedPreviewBounds*` with the edited unplaced geometry, preserve selected deleted-object metadata without fabricated containing sections, and retain label identity.
- 2026-06-22: E3/#1888 edited report/label layout-object restore preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX edited layout-object restores refresh live `previewBounds*` with the restored edited object, preserve deleted `deletedPreviewBounds*` for remaining deleted objects, rehydrate selected live-object section metadata, and retain label identity.
- 2026-06-22: E3/#1887 edited report/label layout-object delete preview-metadata coverage shipped: Studio host regressions now prove FRX/LBX edited layout-object deletes refresh live `previewBounds*` after removing the object, expand deleted `deletedPreviewBounds*` with the edited deleted-object geometry, preserve selected deleted-object metadata, and retain label identity.
- 2026-06-22: E3/#1886 live edited report/label layout-object snap preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited FRX/LBX layout-object snap preserves refreshed live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while updating selected-object geometry, keeping section containment metadata valid, and retaining label identity.
- 2026-06-22: E3/#1885 live edited report/label layout-object resize preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited FRX/LBX layout-object resize preserves refreshed live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while updating selected-object geometry, keeping section containment metadata valid, and retaining label identity.
- 2026-06-22: E3/#1884 live edited report/label layout-object align preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited FRX/LBX layout-object alignment preserves refreshed live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while updating selected-object geometry, keeping section containment metadata valid, and retaining label identity.
- 2026-06-22: E3/#1883 live edited report/label layout-object nudge preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited FRX/LBX layout-object nudges preserve refreshed live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while updating selected-object geometry, keeping section containment metadata valid, and retaining label identity.
- 2026-06-22: E3/#1882 live edited unplaced report/label layout-object rename preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited unplaced FRX/LBX layout-object renames preserve refreshed `previewBounds*` summaries, keep renamed selected-object geometry out of section membership without fabricated containing-section metadata, preserve replacement identity and edited geometry, and retain label identity.
- 2026-06-22: E3/#1881 live edited unplaced report/label layout-object duplicate preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited unplaced FRX/LBX layout-object duplicates preserve refreshed `previewBounds*` summaries, keep copied selected-object geometry out of section membership without fabricated containing-section metadata, preserve appended replacement identity and edited geometry, and retain label identity.
- 2026-06-22: E3/#1880 live edited unplaced report/label layout-object reorder preview-metadata coverage shipped: Studio host regressions now prove stable-selected live edited unplaced FRX/LBX layout-object reorders preserve refreshed `previewBounds*` summaries, keep selected-object geometry out of section membership without fabricated containing-section metadata, preserve physical object order changes and edited geometry, and retain label identity.
- 2026-06-22: E3/#1879 live detail header/footer report/label section reorder preview-metadata coverage shipped: Studio host regressions now prove stable-selected live Detail Header/Footer FRX/LBX section reorders preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while moving physical live section records, preserving live/deleted section counts/totals, selecting the moved section, and retaining label identity.
- 2026-06-22: E3/#1878 live detail header/footer report/label section rename preview-metadata coverage shipped: Studio host regressions now prove stable-selected live Detail Header/Footer FRX/LBX section renames preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while replacing the live section `UNIQUEID`, preserving live/deleted section counts/totals, selecting the renamed section, and retaining label identity.
- 2026-06-22: E3/#1877 live detail header/footer report/label section duplicate preview-metadata coverage shipped: Studio host regressions now prove stable-selected live Detail Header/Footer FRX/LBX section duplicates preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while appending live replacement-`UNIQUEID` section records, refreshing live section counts/totals, selecting the duplicated section, and retaining label identity.
- 2026-06-22: E3/#1876 deleted detail header/footer report/label section reorder preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted Detail Header/Footer FRX/LBX section reorders preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while moving physical deleted section records, preserving deleted section counts/totals, selecting the moved deleted section, and retaining label identity.
- 2026-06-22: E3/#1875 deleted detail header/footer report/label section rename preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted Detail Header/Footer FRX/LBX section renames preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while replacing the deleted section `UNIQUEID`, preserving deleted section counts/totals, selecting the renamed deleted section, and retaining label identity.
- 2026-06-22: E3/#1874 deleted detail header/footer report/label section duplicate preview-metadata coverage shipped: Studio host regressions now prove stable-selected deleted Detail Header/Footer FRX/LBX section duplicates preserve live `previewBounds*` and deleted `deletedPreviewBounds*` summaries while appending deleted replacement-`UNIQUEID` section records, refreshing deleted section counts/totals, selecting the duplicated deleted section, and retaining label identity.
- 2026-06-22: E3/#1873 deleted report/label visual-property reorder stable-selector rejection coverage shipped: Studio host regressions now prove `--visual-property-reorder --property-name EXPR --placement first --unique-id ... --json` rejects deleted flat FRX/LBX layout object DBF-backed fields by stable selector, preserves deleted state and DBF values, keeps live sibling rows intact, refreshes selected deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and reports missing stable selectors without DBF changes.
- 2026-06-22: E3/#1872 deleted report/label visual-property rename stable-selector rejection coverage shipped: Studio host regressions now prove `--visual-property-rename --property-name EXPR --new-property-name ... --unique-id ... --json` rejects deleted flat FRX/LBX layout object DBF-backed fields by stable selector, preserves deleted state and source DBF values, avoids creating target properties, keeps live sibling rows intact, refreshes selected deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and reports missing stable selectors without DBF changes.
- 2026-06-22: E3/#1871 deleted report/label visual-property clear stable-selector coverage shipped: Studio host regressions now prove `--visual-property-clear --property-name EXPR --unique-id ... --json` clears deleted flat FRX/LBX layout object expressions by stable selector, preserves deleted state, keeps live sibling rows intact, refreshes selected cleared deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and leaves deleted-row DBF state unchanged when a stable selector is missing.
- 2026-06-22: E3/#1870 deleted report/label visual-property move stable-selector coverage shipped: Studio host regressions now prove `--visual-property-move --property-name EXPR --source-unique-id ... --target-unique-id ... --replace-existing true --json` moves deleted flat FRX/LBX layout object expressions by stable selector, preserves deleted state, keeps live sibling rows intact, clears selected source expressions, refreshes selected moved target deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and leaves deleted-row DBF state unchanged when a stable selector is missing.
- 2026-06-22: E3/#1869 deleted report/label visual-property copy stable-selector coverage shipped: Studio host regressions now prove `--visual-property-copy --property-name EXPR --source-unique-id ... --target-unique-id ... --replace-existing true --json` copies deleted flat FRX/LBX layout object expressions by stable selector, preserves deleted state and source expressions, keeps live sibling rows intact, refreshes selected copied target/source deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and leaves deleted-row DBF state unchanged when a stable selector is missing.
- 2026-06-22: E3/#1868 deleted report/label visual-property copy-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-property-copy-batch --property-name EXPR --source-unique-id ... --target-unique-id ... --replace-existing true --json` copies deleted flat FRX/LBX layout object expressions by stable selector, preserves deleted state and source expressions, keeps live sibling rows intact, refreshes selected copied target/source deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row property copies when a later selector is missing.
- 2026-06-22: E3/#1867 deleted report/label visual-property move-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-property-move-batch --property-name EXPR --source-unique-id ... --target-unique-id ... --replace-existing true --json` moves deleted flat FRX/LBX layout object expressions by stable selector, preserves deleted state, keeps live sibling rows intact, clears selected source expressions, refreshes selected moved target deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row property moves when a later selector is missing.
- 2026-06-22: E3/#1866 deleted report/label visual-property reorder-batch stable-selector rejection coverage shipped: Studio host regressions now prove `--visual-property-reorder-batch --property-name EXPR/WIDTH --placement first/last --unique-id ... --json` rejects deleted flat FRX/LBX layout object DBF-backed fields by stable selector, preserves deleted state and DBF values, keeps live sibling rows intact, refreshes selected deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and reports missing stable selectors without DBF changes.
- 2026-06-22: E3/#1865 deleted report/label visual-property rename-batch stable-selector rejection coverage shipped: Studio host regressions now prove `--visual-property-rename-batch --property-name EXPR/WIDTH --new-property-name ... --unique-id ... --json` rejects deleted flat FRX/LBX layout object DBF-backed fields by stable selector, preserves deleted state and source DBF values, avoids creating target properties, keeps live sibling rows intact, refreshes selected deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and reports missing stable selectors without partial DBF changes.
- 2026-06-22: E3/#1864 deleted report/label visual-property clear-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-property-clear-batch --property-name EXPR/WIDTH/HPOS --unique-id ... --json` clears deleted flat FRX/LBX layout object properties by stable selector, preserves deleted state, keeps live sibling rows intact, refreshes selected cleared deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row property clears when a later selector is missing.
- 2026-06-22: E3/#1863 deleted report/label visual-object update-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-update-batch --selected-unique-id middle-field-guid --property-name EXPR/WIDTH ... --selected-unique-id right-field-guid --property-name EXPR/HPOS ... --json` updates deleted flat FRX/LBX layout object rows by stable selector, preserves deleted state, keeps live sibling rows intact, persists direct and memo-backed properties, refreshes selected updated deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row property mutations when a later selector is missing.
- 2026-06-22: E3/#1862 deleted report/label visual-object rename-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-rename-batch --selected-unique-id middle-field-guid --new-unique-id middle-renamed-guid --selected-unique-id right-field-guid --new-unique-id right-renamed-guid --json` renames deleted flat FRX/LBX layout object rows by stable selector, preserves deleted state, keeps live sibling rows intact, persists replacement identities without changing physical order, refreshes selected renamed deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row identity mutations when a later replacement identity collides.
- 2026-06-22: E3/#1861 deleted report/label visual-object reorder-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-reorder-batch --selected-unique-id right-field-guid --placement before --target-unique-id left-field-guid --selected-unique-id middle-field-guid --placement after --target-unique-id right-field-guid --json` reorders deleted flat FRX/LBX layout object rows by stable selector, preserves deleted state, keeps live sibling rows intact, persists physical order, refreshes selected moved deleted-object report-layout metadata after reopen without fabricating containing sections, retains label identity, and rolls back earlier deleted-row reorder mutations when a later target selector is missing.
- 2026-06-22: E3/#1860 deleted report/label visual-object duplicate-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-duplicate-batch --selected-unique-id middle-field-guid --new-unique-id middle-deleted-copy-guid --selected-unique-id right-field-guid --new-unique-id right-deleted-copy-guid --json` duplicates deleted flat FRX/LBX layout object rows by stable selector, preserves copied deleted state, keeps live sibling rows intact, refreshes selected deleted copied-object report-layout metadata after reopen, retains label identity, and rolls back earlier deleted-row duplicates when a later replacement identity collides.
- 2026-06-22: E3/#1859 deleted report/label visual-object duplicate-subtree stable-selector coverage shipped: Studio host regressions now prove `--visual-object-duplicate-subtree --unique-id middle-field-guid --replacement-source-unique-id middle-field-guid --new-unique-id middle-deleted-copy-guid --json` duplicates deleted flat FRX/LBX layout object rows by stable selector, preserves the copied deleted state, keeps live sibling rows intact, refreshes selected deleted copied-object report-layout metadata after reopen, retains label identity, and leaves report/label rows untouched when replacement identities collide.
- 2026-06-22: E3/#1858 report/label visual-object duplicate-subtree stable-selector coverage shipped: Studio host regressions now prove `--visual-object-duplicate-subtree --unique-id middle-field-guid --replacement-source-unique-id middle-field-guid --new-unique-id middle-copy-guid --json` duplicates a flat FRX/LBX layout object row by stable selector, appends replacement object identity while preserving sibling rows, refreshes selected copied-object report-layout metadata after reopen, retains label identity, and leaves report/label rows untouched when replacement identities collide or a stable selector is missing.
- 2026-06-22: E3/#1857 report/label visual-object subtree deleted-state stable-selector coverage shipped: Studio host regressions now prove `--subtree-deleted-state --unique-id middle-field-guid --subtree-deleted true/false --json` deletes and restores flat FRX/LBX layout object rows by stable selector, preserves sibling layout rows, refreshes selected deleted/live report-object metadata after reopen, retains label identity, and leaves report/label rows untouched when a stable selector is missing.
- 2026-06-22: Codex guidance cleanup shipped: `agents.md` is now the short operating rulebook, `agent-handoff.md` is a compact continuation brief, `remaining-work.md` is explicitly deprecated as an active planning source, `docs/23-phase-a-dependency-breakdown.md` keeps historical Phase A evidence behind a clear current E3/#24 directive, `docs/22-vfp-language-reference-coverage.md` is marked as reference coverage rather than slice-selection authority, and `issues.txt` is labeled as a local snapshot only.
- 2026-06-22: Localization/#1856 VSIX launch/workflow dialog localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover missing asset path, missing Studio host, Studio launch failure, open-PJX-first, and workflow launcher dialog text; `CopperfinAssetEditorControl` routes those static dialog messages through `CopperfinLocalization` while leaving dynamic external error payloads unchanged; designer smoke coverage proves Spanish/Portuguese formatted dialog output without opening modal UI or loading sample assets.
- 2026-06-22: Localization/#1855 VSIX snapshot and undo status localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover undo labels, undo execution/completion/failure status text, snapshot unavailable/loaded status text, undo-available suffixes, and property update status text; `CopperfinAssetEditorControl` formats those messages through `CopperfinLocalization` while preserving default English behavior; designer smoke coverage proves Spanish/Portuguese formatted status output without loading sample assets.
- 2026-06-22: Localization/#1854 catalog parity coverage shipped: `CopperfinLocalization` now exposes a normalized catalog-key view for tests, and language-service coverage proves English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs have identical key sets with no blank localized values while preserving unknown-key fallback behavior.
- 2026-06-22: Localization/#1853 VSIX host-mode subtitle localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover the standalone Studio shell subtitle and `CopperfinAssetEditorControl.ApplyHostMode` reuses catalog-backed embedded/standalone subtitles; designer smoke coverage proves Spanish/Portuguese subtitle switching between embedded Visual Studio mode and standalone Studio shell mode without loading sample assets.
- 2026-06-22: Localization/#1852 VSIX asset-family guidance localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover form, class library, report, label, menu, project, and generic asset-family guidance text; `CopperfinAssetEditorControl.BuildGuidanceText` consumes those keys while preserving stable asset-family identifiers; designer smoke coverage proves Spanish/Portuguese guidance resolution for every static case without loading sample assets.
- 2026-06-22: Localization/#1851 VSIX explorer list column-header localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover Object, Type, Record, Section, Objects, Top, Item, Group, Items, and Excluded headers; `CopperfinAssetEditorControl` consumes those keys during construction and report/project mode switches; designer smoke coverage proves Spanish/Portuguese initial and synthetic project-mode headers without loading sample assets.
- 2026-06-22: Localization/#1850 VSIX project workspace placeholder localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover initial Task List, Code References, Data Explorer, Object Browser, Toolbox, Builders, Coverage, and Database pane placeholder text; `CopperfinAssetEditorControl` consumes those keys in construction and reset paths while preserving default English behavior; designer smoke coverage proves Spanish/Portuguese placeholder text without loading a sample project.
- 2026-06-22: Localization/#1849 VSIX project command and debugger chrome localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover Build/Run/Debug Copperfin Project buttons, debugger Start Session/Continue/Step/Next/Out buttons, snapshot loading status, debugger ready/starting/updating/unavailable statuses, and the start-session-first prompt; `CopperfinAssetEditorControl` consumes those keys while preserving default English behavior; designer smoke coverage proves Spanish/Portuguese command, status, and debugger guidance labels without loading a sample project.
- 2026-06-22: Localization/#1848 VSIX project workspace tab localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover Summary, Debugger, Task List, Code References, Data Explorer, Object Browser, Toolbox, Builders, Coverage, Database, and the Hide project records object-browser option; `CopperfinAssetEditorControl` consumes those keys while preserving default English behavior; designer smoke coverage proves Spanish/Portuguese tab and option labels without loading a sample project.
- 2026-06-22: Localization/#1847 VSIX editor shell chrome localization shipped: shared English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) catalogs now cover the embedded Visual Studio asset editor title, subtitle, guidance text, and Open/Reveal/Refresh shell buttons; `CopperfinAssetEditorControl` consumes a localization instance while preserving default English behavior; designer smoke coverage executes Spanish/Portuguese chrome checks without loading an asset.
- 2026-06-22: Localization/#1846 Studio asset-kind display-name localization shipped: shared .NET localization catalogs now include English, Spanish Latin America (`es-419`), and Brazilian Portuguese (`pt-BR`) labels for project, form, class library, report, label, menu, and generic assets; standalone Studio window/status text formats those catalog-backed labels through the existing extension classifier; portable tests prove English preservation, Spanish/Portuguese lookup, and unsupported-locale fallback.
- 2026-06-22: E3/#1845 report/label visual-object reorder-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-reorder-batch --selected-unique-id ... --placement ... --target-unique-id ... --json` batches reorder FRX/LBX layout object rows by stable selectors, persists physical object order, refreshes selected report-object section metadata after reopen, retains label identity, exposes committed batch metadata, and rolls back earlier object order mutations when a later target selector is missing.
- 2026-06-22: E3/#1844 report/label visual-object duplicate-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-duplicate-batch --selected-unique-id ... --new-unique-id ... --json` batches duplicate FRX/LBX layout object rows by stable selectors, append duplicate identities while preserving original object order, refresh selected duplicate report-object JSON after reopen, retain label identity, expose committed batch metadata, and roll back earlier duplicate appends when a later replacement identity collides.
- 2026-06-22: E3/#1843 report/label visual-object rename-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-rename-batch --selected-unique-id ... --new-unique-id ... --json` batches rename FRX/LBX layout object rows by stable selectors, preserve object counts and physical order while replacing identities, refresh selected-object/report-object JSON after reopen, retain label identity, expose batch mutation/undo metadata, and roll back earlier object identity mutations when a later replacement collides.
- 2026-06-22: E3/#1842 report/label visual-object update-batch stable-selector coverage shipped: Studio host regressions now prove `--visual-object-update-batch --selected-unique-id field-guid/label-guid --property-name ... --property-value ... --json` batches update FRX/LBX layout object rows by stable selectors, persist direct and memo-backed property changes, refresh selected report-object metadata after reopen, retain label identity, expose batch mutation/undo metadata, and roll back earlier object mutations when a later stable selector is missing.
- 2026-06-22: E3/#1841 mixed report/label deleted-state batch stable-selector coverage shipped: Studio host regressions now prove a single `--deleted-states` batch can delete and restore FRX/LBX settings, section, and layout object rows by `settings-guid`/`section-guid`/`field-guid`, refresh live/deleted settings, section, object counts and metadata, retain label identity, avoid fabricated selected report metadata, and roll back earlier mixed-kind mutations when a later stable selector is missing.
- 2026-06-22: E3/#1840 report/label object deleted-state batch stable-selector coverage shipped: Studio host regressions now prove `--deleted-states --deleted-state-target-unique-id field-guid/label-guid --deleted-state true/false --json` batches delete and restore FRX/LBX layout object rows by stable selectors, refresh live/placed/unplaced/deleted object counts plus live section and deleted-object metadata, retain label identity, avoid fabricated selected report metadata, and roll back earlier object mutations when a later stable selector is missing.
- 2026-06-22: E3/#1839 report/label deleted-state batch stable-selector coverage shipped: Studio host regressions now prove `--deleted-states --deleted-state-target-unique-id settings-guid/section-guid --deleted-state true/false --json` batches delete and restore FRX/LBX settings plus section rows, refresh report-layout setting/section counts and page setup summaries, retain label identity, avoid fabricated selected report metadata, and roll back earlier batch mutations when a later stable selector is missing.
- 2026-06-22: E3/#1838 report/label column-setup memo stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate the `EXPR` memo through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name EXPR --json`, persist/clear the backing memo, refresh column setup summaries plus live/deleted and selected settings provenance, preserve column setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1837 report/label column-spacing stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `COLSPACING` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name COLSPACING --json`, persist/clear the backing field, refresh column setup summaries and selected settings provenance, preserve column setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1836 report/label column-width stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `COLWIDTH` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name COLWIDTH --json`, persist/clear the backing field, refresh column setup summaries and selected settings provenance, preserve column setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1835 report/label column-count stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `COLS` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name COLS --json`, persist/clear the backing field, refresh column setup summaries and selected settings provenance, preserve column setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1834 report/label paper-size stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `PAPERSIZE` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name PAPERSIZE --json`, persist/clear the backing field, refresh paper-size summaries and selected settings provenance, preserve page setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1833 report/label orientation stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `ORIENTATION` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name ORIENTATION --json`, persist/clear the backing field, refresh orientation summaries and selected settings provenance, preserve page setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1832 report/label horizontal-grid stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `GRIDH` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name GRIDH --json`, persist/clear the backing field, refresh live/deleted and selected settings provenance, preserve page setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1831 report/label vertical-grid stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `GRIDV` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name GRIDV --json`, persist/clear the backing field, refresh live/deleted and selected settings provenance, preserve page setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1830 report/label bottom-margin stable-selector coverage shipped: Studio host regressions now prove stable-selected live and deleted FRX/LBX settings records mutate direct `BOTMARGIN` through `--set-property/--clear-property --unique-id settings-guid/deleted-settings-guid --property-name BOTMARGIN --json`, persist/clear the backing field, refresh live/deleted and selected settings provenance, preserve page setup or deleted-setting counts as appropriate, and retain label identity.
- 2026-06-22: E3/#1829 deleted report/label page-margin stable-selector coverage shipped: Studio host regressions now prove stable-selected deleted FRX/LBX settings records mutate direct `TOPMARGIN` through `--set-property/--clear-property --unique-id deleted-settings-guid --property-name TOPMARGIN --json`, persist/clear the backing field, refresh deleted and selected settings provenance, preserve unrelated section/object metadata, and retain label identity.
- 2026-06-22: E3/#1828 live report/label page-margin stable-selector coverage shipped: Studio host regressions now prove stable-selected live FRX/LBX settings records mutate direct `TOPMARGIN` through `--set-property/--clear-property --unique-id settings-guid --property-name TOPMARGIN --json`, persist/clear the backing field, refresh page setup summaries, selected settings provenance, and retain label identity.
- 2026-06-22: E3/#1827 deleted report/label settings memo stable-selector coverage shipped: Studio host regressions now prove stable-selected deleted FRX/LBX settings records mutate `EXPR` through `--set-property/--clear-property --unique-id deleted-settings-guid --property-name EXPR --json`, persist/clear the backing memo, refresh deleted and selected settings provenance, preserve unrelated section/object metadata, and retain label identity.
- 2026-06-22: E3/#1826 live report/label settings memo stable-selector coverage shipped: Studio host regressions now prove stable-selected live FRX/LBX settings records mutate `EXPR` through `--set-property/--clear-property --unique-id settings-guid --property-name EXPR --json`, persist/clear the backing memo, refresh page setup summaries, selected settings provenance, and retain label identity.
- 2026-06-22: E3/#1825 generic deleted report/label section stable-selector geometry coverage shipped: Studio host regressions now prove stable-selected generic deleted FRX/LBX sections mutate `HEIGHT` and `VPOS` through `--set-property/--clear-property --unique-id deleted-section-guid --json`, persist/clear the backing fields, preserve deleted-section selection/counts, refresh selected deleted-section geometry, keep former objects unplaced without fabricated containing sections, and retain label identity.
- 2026-06-22: E3/#1824 generic live report/label section stable-selector geometry coverage shipped: Studio host regressions now prove stable-selected generic live FRX/LBX sections mutate `HEIGHT` and `VPOS` through `--set-property/--clear-property --unique-id section-guid --json`, persist/clear the backing fields, refresh section totals, selected-section geometry, object placement and relative geometry, and retain label identity.
- 2026-06-22: E3/#1823 detail header/footer report/label section top-clear preview-bounds stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live and deleted Detail Header/Footer FRX/LBX section `VPOS` clears through `--clear-property --unique-id ... --property-name VPOS --json` refresh live `previewBounds*` and deleted `deletedPreviewBounds*`, preserve section counts/totals, refresh selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1822 detail header/footer report/label section height-clear preview-bounds stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live and deleted Detail Header/Footer FRX/LBX section `HEIGHT` clears through `--clear-property --unique-id ... --property-name HEIGHT --json` refresh live `previewBounds*` and deleted `deletedPreviewBounds*`, preserve section counts/totals, refresh selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1821 detail header/footer report/label section delete/restore preview-bounds stable-selection coverage shipped: Studio host regression coverage now proves stable-selected Detail Footer FRX/LBX section delete/restore operations through `--delete-object/--restore-object --unique-id ... --json` shrink and expand live `previewBounds*`, expose and clear `deletedPreviewBounds*`, preserve selected-section metadata, refresh object placement/containment accounting, and retain label identity.
- 2026-06-22: E3/#1820 deleted detail header/footer report/label section preview-bounds stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX section geometry edits through `--set-property --unique-id ... --property-name HEIGHT/VPOS --json` refresh document-level `deletedPreviewBounds*` values, preserve live preview bounds and live section metadata, preserve deleted state, persist edited section fields, refresh selected deleted-section geometry, preserve selection kind, and retain label identity.
- 2026-06-22: E3/#1819 detail header/footer report/label section preview-bounds stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX section geometry edits through `--set-property --unique-id ... --property-name HEIGHT/VPOS --json` refresh document-level `previewBounds*` values, preserve deleted-preview bounds, persist edited section fields, refresh selected-section geometry, preserve selection kind, and retain label identity.
- 2026-06-22: E3/#1818 deleted detail header/footer report/label section reorder stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections reordered by `--reorder-object --unique-id ... --placement ... --target-unique-id ... --json` move physical deleted section records while preserving section `UNIQUEID`, deleted state, and expression data, preserve live/deleted section counts and height totals, select the moved deleted section in returned JSON, preserve live Detail metadata, and retain label identity.
- 2026-06-22: E3/#1817 deleted detail header/footer report/label section rename stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections renamed by `--rename-object --unique-id ... --new-unique-id ... --json` replace the section `UNIQUEID` without appending records, preserve deleted state and section expression data, preserve live/deleted section counts and height totals, select the renamed deleted section in returned JSON, preserve live Detail metadata, and retain label identity.
- 2026-06-22: E3/#1816 deleted detail header/footer report/label section duplicate stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections duplicated by `--duplicate-object --unique-id ... --new-unique-id ... --json` append deleted replacement-`UNIQUEID` section records, preserve section expression data, refresh deleted section counts and height totals, select the duplicated deleted section in returned JSON, preserve live Detail metadata, and retain label identity.
- 2026-06-22: E3/#1815 detail header/footer report/label section reorder stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections reordered by `--reorder-object --unique-id ... --placement ... --target-unique-id ... --json` move physical section records while preserving section `UNIQUEID` and expression data, keep geometry-derived section ordering coherent, preserve live/deleted section counts and height totals, select the moved section in returned JSON, preserve deleted-section metadata, and retain label identity.
- 2026-06-22: E3/#1814 detail header/footer report/label section rename stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections renamed by `--rename-object --unique-id ... --new-unique-id ... --json` replace the section `UNIQUEID` without appending records, preserve section expression data, preserve live/deleted section counts and height totals, select the renamed section in returned JSON, preserve deleted-section metadata, and retain label identity.
- 2026-06-22: E3/#1813 detail header/footer report/label section duplicate stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections duplicated by `--duplicate-object --unique-id ... --new-unique-id ... --json` append live replacement-`UNIQUEID` section records, preserve section expression data, refresh live section counts, kind buckets, and height totals, select the duplicated section in returned JSON, preserve deleted-section metadata, and retain label identity.
- 2026-06-22: E3/#1812 detail header/footer report/label section delete/restore stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections deleted by `--delete-object --unique-id ... --json` move into deleted-section metadata, preserve selected-section availability/kind, and move former section objects to unplaced metadata, while deleted Detail Header/Footer sections restored by `--restore-object --unique-id ... --json` return to live-section metadata, rehydrate placed-object containing-section metadata, preserve report/label identity, and refresh selected-section object counts.
- 2026-06-22: E3/#1811 deleted detail header/footer report/label section expression stable-selection edit coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections updated and cleared by `--set-property/--clear-property --unique-id ... --property-name EXPR --json` preserve deleted state, persist refreshed expression memo values or return blank selected deleted-section expression metadata, preserve live/deleted section counts, preserve deleted selected-section availability and section selection kind, preserve live Detail and deleted sibling section metadata, avoid stale selected expression leakage, and retain label identity.
- 2026-06-22: E3/#1810 detail header/footer report/label section expression stable-selection edit coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections updated and cleared by `--set-property/--clear-property --unique-id ... --property-name EXPR --json` persist refreshed expression memo values or return blank selected-section expression metadata, preserve live/deleted section counts, preserve selected-section availability and section selection kind, preserve sibling section expressions, avoid stale selected expression leakage, and retain label identity.
- 2026-06-22: E3/#1809 deleted detail header/footer report/label section top stable-selection clear coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections cleared by `--clear-property --unique-id ... --property-name VPOS --json` blank VPOS fields, preserve live/deleted section counts and height totals, preserve deleted selected-section availability and section selection kind, return zero/default deleted selected-section top/bottom geometry, and retain label identity.
- 2026-06-22: E3/#1808 deleted detail header/footer report/label section top stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections updated by `--set-property --unique-id ... --property-name VPOS --json` persist refreshed VPOS fields, preserve live/deleted section counts and height totals, preserve deleted selected-section availability and section selection kind, return updated deleted selected-section top/bottom geometry, and retain label identity.
- 2026-06-22: E3/#1807 detail header/footer report/label section top stable-selection clear coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections cleared by `--clear-property --unique-id ... --property-name VPOS --json` blank VPOS fields, preserve live/deleted section height totals, preserve selected-section availability and section selection kind, return zero/default top selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1806 detail header/footer report/label section top stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections updated by `--set-property --unique-id ... --property-name VPOS --json` persist refreshed VPOS fields, preserve live/deleted section height totals, preserve selected-section availability and section selection kind, return updated top/bottom selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1805 deleted detail header/footer report/label section height stable-selection clear coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections cleared by `--clear-property --unique-id ... --property-name HEIGHT --json` blank HEIGHT fields, preserve live/deleted section counts, preserve live section height totals, refresh deleted-section height totals to remaining/zero-height deleted sections, preserve deleted selected-section availability and section selection kind, return zero-height deleted selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1804 deleted detail header/footer report/label section height stable-selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX sections updated by `--set-property --unique-id ... --property-name HEIGHT --json` persist refreshed HEIGHT fields, preserve live/deleted section counts, preserve live section height totals, refresh deleted-section height totals, preserve deleted selected-section availability and section selection kind, return updated deleted selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1803 detail header/footer report/label section height stable-selection clear coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections cleared by `--clear-property --unique-id ... --property-name HEIGHT --json` blank the HEIGHT fields, refresh live section height totals to the remaining/zero-height sections, preserve deleted-section height totals, preserve selected-section availability and section selection kind, return zero-height selected-section geometry, and retain label identity.
- 2026-06-22: E3/#1802 detail header/footer report/label section height stable-selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX sections updated by `--set-property --unique-id ... --property-name HEIGHT --json` persist refreshed HEIGHT fields, refresh live section height totals, preserve deleted-section height totals, preserve selected-section availability and section selection kind, return updated selected-section geometry, and retain label identity.
- 2026-06-22: Localization/#1801 standalone Studio Spanish/Portuguese string-catalog slice shipped: added a documented localization contract and release checklist, introduced a shared .NET `CopperfinLocalization` catalog with English fallback, Spanish Latin America (`es-419`) and Brazilian Portuguese (`pt-BR`) entries, wired standalone Studio menu/dialog/status strings through deterministic `--locale`/`COPPERFIN_UI_LOCALE` selection, and added portable tests for locale normalization, Spanish/Portuguese lookup, missing-locale fallback, missing-key fallback, and invariant formatting. Parent `#1779` remains open for CLI/native diagnostics, Studio host JSON display-text policy, full VSIX UI, installer text, docs/help, and generated-template localization.
- 2026-06-22: E3/#1800 deleted detail header/footer report/label object vertical distribution containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode vertical --distribute-target-unique-id ... --json` preserve deleted state, persist refreshed VPOS geometry, preserve HPOS/WIDTH/HEIGHT, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1799 detail header/footer report/label object vertical distribution containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode vertical --distribute-target-unique-id ... --json` persist refreshed VPOS geometry, preserve HPOS/WIDTH/HEIGHT, keep containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1798 deleted detail header/footer report/label object distribution containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode horizontal --distribute-target-unique-id ... --json` preserve deleted state, persist refreshed HPOS geometry, preserve VPOS/WIDTH/HEIGHT, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1797 detail header/footer report/label object distribution containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode horizontal --distribute-target-unique-id ... --json` persist refreshed HPOS geometry, preserve VPOS/WIDTH/HEIGHT, keep containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1796 deleted detail header/footer report/label object nudge containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects nudged by `--nudge-object --nudge-mode both --delta-hpos ... --delta-vpos ... --nudge-target-unique-id ... --json` preserve deleted state, persist refreshed HPOS/VPOS geometry, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1795 detail header/footer report/label object nudge containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects nudged by `--nudge-object --nudge-mode both --delta-hpos ... --delta-vpos ... --nudge-target-unique-id ... --json` persist refreshed HPOS/VPOS geometry, preserve WIDTH/HEIGHT, keep containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1794 deleted detail header/footer report/label object snap containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects snapped by `--snap-object --snap-mode both --grid-width ... --grid-height ... --snap-target-unique-id ... --json` preserve deleted state, persist refreshed HPOS/VPOS geometry, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1793 detail header/footer report/label object snap containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects snapped by `--snap-object --snap-mode both --grid-width ... --grid-height ... --snap-target-unique-id ... --json` persist refreshed HPOS/VPOS geometry, preserve WIDTH/HEIGHT, keep containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1792 deleted detail header/footer report/label object resize containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects resized by `--resize-object --resize-mode size --anchor-unique-id ... --resize-target-unique-id ... --json` preserve deleted state, persist refreshed WIDTH/HEIGHT geometry, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1791 detail header/footer report/label object resize containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects resized by `--resize-object --resize-mode size --anchor-unique-id ... --resize-target-unique-id ... --json` persist refreshed WIDTH/HEIGHT geometry, preserve HPOS/VPOS, keep containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1790 deleted detail header/footer report/label object alignment containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects aligned by `--align-object --alignment-mode left --anchor-unique-id ... --align-target-unique-id ... --json` preserve deleted state, persist refreshed geometry, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1789 detail header/footer report/label object alignment containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects aligned by `--align-object --alignment-mode left --anchor-unique-id ... --align-target-unique-id ... --json` persist refreshed geometry, preserve containing-section ids/record indexes plus section-relative coordinates and object counts, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1788 deleted detail header/footer report/label object reorder containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects reordered by `UNIQUEID` preserve deleted state and object metadata, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh selected-object JSON after physical record movement, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1787 detail header/footer report/label object reorder containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects reordered by `UNIQUEID` preserve containing-section membership, refresh section object indexes/counts according to reordered same-geometry records, keep selected-object/selected-section JSON non-failing, and retain label identity.
- 2026-06-22: E3/#1786 deleted detail header/footer report/label object rename containment coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects renamed by `UNIQUEID` replace the requested identity without appending records, preserve deleted state and object metadata, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh deleted-object/selected-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1785 detail header/footer report/label object rename containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects renamed by `UNIQUEID` replace the requested identity without appending records, preserve object metadata, keep containing-section metadata and section object counts intact, refresh selected-object/selected-section JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1784 deleted detail header/footer report/label object duplicate containment coverage shipped: visual-object duplication now preserves the source record deleted flag for appended copies, and Studio host regression coverage proves stable-selected deleted Detail Header/Footer FRX/LBX objects duplicated by `UNIQUEID` append deleted replacement-`UNIQUEID` records, preserve object metadata, keep containing-section metadata unavailable/null instead of fabricating live section membership, refresh deleted-object/selected-object JSON, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1783 detail header/footer report/label object duplicate containment coverage shipped: Studio host regression coverage now proves stable-selected Detail Header and Detail Footer FRX/LBX objects duplicated by `UNIQUEID` append replacement-`UNIQUEID` records, preserve object metadata, remain placed in the correct containing section, refresh section object indexes/counts, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1782 detail header/footer report/label object delete/restore containment coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header FRX/LBX objects delete into the deleted-object collection while clearing containing-section membership, and stable-selected deleted Detail Footer objects restore into live containing-section membership with refreshed section-relative metadata, object counts, non-failing inspection, and label identity.
- 2026-06-22: E3/#1781 deleted detail header/footer report/label object geometry edit coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects support `VPOS` set and `HEIGHT` clear operations by `UNIQUEID`, preserve deleted state, persist refreshed or blank geometry fields, refresh deleted-object and selected-object bounds, keep containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1780 detail header/footer report/label object geometry edit coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects support `VPOS` set and `HEIGHT` clear operations by `UNIQUEID`, persist refreshed or blank geometry fields, refresh selected-object bounds and section-relative coordinates, preserve containing-section metadata/object counts, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1778 deleted detail header/footer report/label object font-option edit coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects support `FONTSIZE` set and `MODE` clear operations by `UNIQUEID`, preserve deleted state, persist refreshed field values or blank clears, refresh deleted-object and selected-object highlight provenance, keep containing-section metadata unavailable/null instead of fabricating live section membership, remove stale cleared-option highlights, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1777 detail header/footer report/label object font-option edit coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects support `FONTSIZE` set and `MODE` clear operations by `UNIQUEID`, persist refreshed field values or blank clears, refresh selected-object highlight provenance, preserve containing-section metadata, remove stale cleared-option highlights, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1776 deleted detail header/footer report/label object-font edit coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects support `FONTFACE` set/clear operations by `UNIQUEID`, preserve deleted state, persist refreshed memo values or blank clears, refresh deleted-object and selected-object highlight provenance, keep containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1775 detail header/footer report/label object-font edit coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects support `FONTFACE` set/clear operations by `UNIQUEID`, persist refreshed memo values or blank clears, refresh selected-object highlight provenance, preserve containing-section metadata, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1774 deleted detail header/footer report/label object-font selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects expose deleted-object and selected-object highlight counts plus `FONTFACE`, `FONTSIZE`, and `MODE` provenance while preserving deleted state, expression metadata, unavailable/null containing-section metadata, non-failing inspection, and label identity.
- 2026-06-22: E3/#1773 detail header/footer report/label object-font selection coverage shipped: Studio host regression coverage now proves stable-selected live Detail Header/Footer FRX/LBX objects expose selected-object highlight counts plus `FONTFACE`, `FONTSIZE`, and `MODE` provenance while preserving expression metadata, containing-section metadata, non-failing inspection, and label identity.
- 2026-06-22: E3/#1772 deleted detail header/footer report/label object-expression edit coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects support `EXPR` set/clear operations by `UNIQUEID`, preserve deleted state, refresh deleted-object and selected-object expression text/provenance, keep containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1771 deleted detail header/footer report/label object-expression selection coverage shipped: Studio host regression coverage now proves stable-selected deleted Detail Header/Footer FRX/LBX objects expose deleted-object and selected-object expression text/provenance, preserve deleted state, keep containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1770 detail header/footer report/label object-expression edit coverage shipped: Studio host regression coverage now proves stable-selected Detail Header/Footer FRX/LBX objects support `EXPR` set/clear operations by `UNIQUEID`, persist refreshed memo values or blank clears, refresh selected-object expression text/provenance plus containing-section metadata, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1769 detail header/footer report/label object-expression selection coverage shipped: Studio host regression coverage now proves stable-selected live FRX/LBX objects placed inside Detail Header and Detail Footer bands expose selected-object expression text, field/memo provenance, object kind, containing-section metadata, non-failing inspection, and label identity.
- 2026-06-22: E3/#1768 stable detail header/footer report/label section-expression selection coverage shipped: Studio host regression coverage now proves live Detail Header and Detail Footer FRX/LBX sections selected by `UNIQUEID` expose selected-section expression text, field/memo provenance, object-code and geometry metadata, null unrelated report-selection surfaces, non-failing inspection, and label identity.
- 2026-06-22: E3/#1767 stable deleted detail header/footer report/label section-selection coverage shipped: Studio host regression coverage now proves deleted Detail Header and deleted Detail Footer FRX/LBX sections selected by `UNIQUEID` expose deleted-section and selected-section metadata, expression provenance, section counts, null unrelated report-selection surfaces, non-failing inspection, and label identity.
- 2026-06-22: E3/#1766 deleted detail header/footer report/label section-expression edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted Detail Header `EXPR` records and `--clear-property` on deleted Detail Footer `EXPR` records refresh deleted-section and selected-section expression JSON, persist or clear memo provenance correctly, preserve live Detail sibling metadata, keep inspection non-failing, and retain label identity.
- 2026-06-22: E3/#1765 detail header/footer report/label section-expression edit coverage shipped: Studio host regression coverage now proves `--set-property` and `--clear-property` on Detail Header and Detail Footer FRX/LBX section `EXPR` records refresh selected-section expression JSON, persist or clear memo provenance correctly, preserve sibling section expressions, and retain label identity.
- 2026-06-22: E3/#1764 detail header/footer report/label object-containment coverage shipped: Studio host regression coverage now proves stable-selected live FRX/LBX objects inside Detail Header and Detail Footer bands retain containing-section ids, section-relative coordinates, section object index/count metadata, selected containing-section JSON, and label identity.
- 2026-06-22: E3/#1763 detail header/footer report/label section-kind coverage shipped: Studio host regression coverage now proves valid FRX/LBX Detail Header and Detail Footer band rows surface as `detail_header` and `detail_footer` summary buckets and stable selected-section metadata, including deleted detail-footer buckets while retaining label identity.
- 2026-06-22: E3/#1762 extended report/label object-kind coverage shipped: Studio host regression coverage now proves valid FRX/LBX rectangle, picture, and variable `OBJTYPE` layout objects surface as expected `objectKind` metadata in summary and stable selected-object JSON, including live, unplaced, and deleted kind buckets while retaining label identity.
- 2026-06-22: E3/#1761 negative report/label layout classification coverage shipped: Studio host regression coverage now proves negative FRX/LBX `OBJTYPE` classification values keep inspection non-failing, do not fabricate root settings, live/deleted bands, layout objects, or preview bounds, degrade explicit record selection to no selected report metadata, and retain label identity.
- 2026-06-22: E3/#1760 dot-leading report/label layout classification coverage shipped: Studio host regression coverage now proves dot-leading FRX/LBX `OBJTYPE`/`OBJCODE` classification values keep inspection non-failing, do not fabricate root settings, live/deleted bands, layout objects, or preview bounds, degrade explicit record selection to no selected report metadata, and retain label identity.
- 2026-06-22: E3/#1759 trimmed report/label layout classification coverage shipped: Studio host regression coverage now proves whitespace-padded FRX/LBX `OBJTYPE`/`OBJCODE` classification values trim before deriving root settings, live/deleted bands, and live/deleted layout objects while selected settings/sections/objects resolve, inspection stays non-failing, and label identity is retained.
- 2026-06-22: E3/#1758 fractional report/label layout classification coverage shipped: Studio host regression coverage now proves fractional FRX/LBX `OBJTYPE`/`OBJCODE` classification values derive root settings, live/deleted bands, and live/deleted layout objects from integer portions while selected settings/sections/objects resolve, inspection stays non-failing, and label identity is retained.
- 2026-06-22: E3/#1757 mixed-case report/label settings memo-name coverage shipped: report layout summaries now match known memo-derived setting names case-insensitively while preserving source spelling in selected-settings metadata, and Studio host regression coverage proves lower/mixed-case FRX/LBX page/column settings parse with source-line and memo provenance.
- 2026-06-22: E3/#1756 CR-only report/label settings memo-line coverage shipped: report layout parsing now treats carriage-return-only memo separators as real setting lines, and Studio host regression coverage proves CR-only `EXPR` memo settings parse into separate FRX/LBX page/column settings with source-line and memo provenance while retaining label identity.
- 2026-06-22: E3/#1755 invalid-first duplicate report/label settings precedence coverage shipped: Studio host regression coverage now proves duplicate root `EXPR` memo settings whose first matching value is invalid do not fabricate page/column summary availability from later valid duplicates or direct fields, while every invalid and valid duplicate remains inspectable with source-order provenance and label identity is retained.
- 2026-06-22: E3/#1754 duplicate report/label settings precedence coverage shipped: Studio host regression coverage now proves duplicate root `EXPR` memo settings plus same-name direct setting fields keep all entries inspectable in source order, while page/column summaries derive from the first live memo-derived setting and label identity is retained.
- 2026-06-22: E3/#1753 malformed report/label settings memo-line coverage shipped: Studio host regression coverage now proves root `EXPR` memo lines without `=` and lines whose setting names trim empty are skipped for FRX/LBX settings metadata, while later valid memo-derived page/column settings still parse with original source-line and memo provenance and label identity is retained.
- 2026-06-22: E3/#1752 blank report/label settings memo coverage shipped: Studio host regression coverage now proves blank and whitespace-only root `EXPR` memo values for FRX/LBX page setup, margin/grid, and column setup settings remain inspectable with memo provenance without fabricating page or column setup availability, while label identity is retained.
- 2026-06-22: E3/#1751 fractional report/label settings memo coverage shipped: Studio host regression coverage now proves whitespace-trimmed fractional root `EXPR` memo values for FRX/LBX page setup, margin/grid, and column setup fields derive summary setup values from integer portions while preserving fractional selected-settings source values with memo provenance and retaining label identity.
- 2026-06-22: E3/#1750 invalid report/label settings memo coverage shipped: Studio host regression coverage now proves malformed, oversized, and dot-leading root `EXPR` memo values for FRX/LBX page setup, margin/grid, and column setup fields remain inspectable with field, memo-block, and source-line provenance without fabricating page or column setup availability, while label identity is retained.
- 2026-06-22: E2/#1749 invalid selected-object raw-code coverage shipped: Studio host document JSON now has regression coverage proving malformed, oversized, and dot-leading visual object `OBJTYPE`/`OBJCODE` source values keep inspection non-failing, default parsed `objectTypeCode`/`objectCode` to zero, and remain inspectable through selected-object and full object-list property metadata.
- 2026-06-22: E3/#1748 dot-leading report/label direct-setting field coverage shipped: Studio host regression coverage now proves decimal text without an integer portion in direct FRX/LBX page setup, margin/grid, and column setup fields remains inspectable in live/deleted selected-settings metadata without fabricating page or column setup availability, while label identity is retained.
- 2026-06-22: E3/#1747 oversized report/label direct-setting field coverage shipped: Studio host regression coverage now proves oversized integer text in direct FRX/LBX page setup, margin/grid, and column setup fields remains inspectable in live/deleted selected-settings metadata without fabricating page or column setup availability, while label identity is retained.
- 2026-06-22: E3/#1746 fractional report/label direct-setting field coverage shipped: Studio host regression coverage now proves fractional numeric text in direct FRX/LBX page setup, margin/grid, and column setup fields derives setup summaries from integer portions while live/deleted selected-settings metadata preserves fractional source values and label identity is retained.
- 2026-06-22: E3/#1745 trimmed report/label direct-setting field coverage shipped: Studio host regression coverage now proves leading/trailing whitespace in direct FRX/LBX page setup, margin/grid, and column setup fields is trimmed before setup summaries are derived, while live/deleted selected-settings metadata preserves normalized values and label identity is retained.
- 2026-06-22: E3/#1744 mixed invalid report/label direct-setting field coverage shipped: Studio host regression coverage now proves valid direct FRX/LBX page setup, margin/grid, and column setup fields still drive setup summaries when sibling direct fields contain malformed non-numeric values, while malformed fields remain inspectable as raw live/deleted selected-settings metadata.
- 2026-06-22: E3/#1743 blank report/label direct-setting field coverage shipped: Studio host regression coverage now proves blank and whitespace-only FRX/LBX page setup, margin/grid, and column setup direct fields stay skipped from summary settings, live/deleted selected-settings metadata, and page/column setup availability while inspection remains non-failing and label identity is retained.
- 2026-06-22: E3/#1742 mixed report/label direct-setting memo placeholder coverage shipped: Studio host regression coverage now proves valid direct FRX/LBX page setup, margin/grid, and column setup fields still produce summary setup availability and live/deleted selected-settings metadata when sibling direct fields contain unresolved memo placeholder text, while placeholder fields stay suppressed and never leak into JSON.
- 2026-06-22: E3/#1741 unresolved report/label direct-setting memo placeholder coverage shipped: Studio host regression coverage now proves unresolved memo placeholder text in direct FRX/LBX page setup, margin/grid, and column setup fields keeps summary and live/deleted settings selection JSON non-failing, suppresses selected settings, avoids fabricating page or column setup availability, avoids leaking placeholder text, and retains label identity.
- 2026-06-22: E3/#1740 unresolved report/label geometry memo placeholder coverage shipped: Studio host regression coverage now proves unresolved memo placeholder text in FRX/LBX section/object HPOS/VPOS/WIDTH/HEIGHT fields keeps summary and selection JSON non-failing, defaults section/object geometry to zero-origin zero-size non-inverted bounds with field provenance, avoids leaking placeholder text, preserves live/deleted object counts, and retains label identity.
- 2026-06-22: E3/#1739 unresolved unplaced report/label layout-object memo placeholder coverage shipped: Studio host regression coverage now proves unresolved EXPR/FONTFACE/FONTSIZE/MODE memo placeholder text on live unplaced FRX/LBX layout objects keeps unplaced-object summary and selection JSON non-failing, suppresses expression/highlight text, avoids leaking placeholder text, preserves object geometry/metadata, avoids fabricating containing-section metadata, and retains label identity.
- 2026-06-22: E3/#1738 unresolved deleted report/label layout-object memo placeholder coverage shipped: Studio host regression coverage now proves unresolved EXPR/FONTFACE/FONTSIZE/MODE memo placeholder text on deleted FRX/LBX layout objects keeps deleted-object summary and selection JSON non-failing, suppresses expression/highlight text, avoids leaking placeholder text, preserves deleted object geometry/metadata, avoids fabricating containing-section metadata, and retains label identity.
- 2026-06-22: E3/#1737 unresolved report/label section memo placeholder coverage shipped: Studio host regression coverage now proves unresolved FRX/LBX section EXPR memo placeholder text keeps live/deleted section summary and selection JSON non-failing, suppresses expression text/provenance, avoids leaking placeholder text, preserves section counts/heights/band metadata, and retains label identity.
- 2026-06-22: E3/#1736 unresolved report/label memo placeholder coverage shipped: Studio host regression coverage now proves unresolved FRX/LBX memo placeholder text in root settings and layout-object expression/font/mode fields keeps JSON inspection non-failing, does not leak placeholder text into summary or selection JSON, suppresses root settings/page setup availability, preserves object selection, and retains label identity.
- 2026-06-22: E3/#1735 invalid report/label direct margin/grid field coverage shipped: Studio host regression coverage now proves malformed direct BOTMARGIN/GRIDV/GRIDH fields on FRX/LBX root settings rows keep JSON inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep page setup availability false with inert default numeric margin/grid summary values, and retain label identity.
- 2026-06-22: E3/#1734 invalid report/label direct column setup field coverage shipped: Studio host regression coverage now proves malformed direct COLS/COLWIDTH/COLSPACING fields on FRX/LBX root settings rows keep JSON inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep column setup availability false with inert default numeric summary values, and retain label identity.
- 2026-06-22: E3/#1733 invalid report/label direct page setup field coverage shipped: Studio host regression coverage now proves malformed direct ORIENTATION/PAPERSIZE/TOPMARGIN fields on FRX/LBX root settings rows keep JSON inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep page setup availability false with inert default numeric summary values, and retain label identity.
- 2026-06-22: E3/#1732 unsupported report/label OBJTYPE code coverage shipped: Studio host regression coverage now proves FRX/LBX rows with structurally valid but unsupported OBJTYPE values keep JSON inspection non-failing, do not fabricate root settings, sections, layout objects, preview bounds, or selected report metadata, and retain label identity.
- 2026-06-22: E3/#1731 missing report/label OBJTYPE schema with OBJCODE coverage shipped: Studio host regression coverage now proves FRX/LBX rows with OBJCODE present but no OBJTYPE field keep JSON inspection non-failing, do not infer root settings, sections, layout objects, or preview bounds from OBJCODE-only rows, degrade explicit record selection to no selected report metadata, and retain label identity.
- 2026-06-22: E3/#1730 missing report/label root OBJCODE schema coverage shipped: Studio host regression coverage now proves FRX/LBX root/settings rows with valid OBJTYPE but no OBJCODE field keep JSON inspection non-failing, preserve EXPR-derived and direct page setup/settings metadata, populate selected-settings JSON for live/deleted explicit record selection, and retain label identity.
- 2026-06-22: E3/#1729 missing report/label object OBJCODE schema coverage shipped: Studio host regression coverage now proves FRX/LBX object rows with valid OBJTYPE but no OBJCODE field keep JSON inspection non-failing, preserve OBJTYPE-derived object kinds, serialize default object-code metadata with null provenance, preserve unplaced live/deleted object counts and selected-object JSON, and retain label identity.
- 2026-06-22: E3/#1728 missing report/label section OBJCODE schema coverage shipped: Studio host regression coverage now proves FRX/LBX section rows with OBJTYPE=9 but no OBJCODE field keep JSON inspection non-failing, default to Title band metadata with null object-code provenance, preserve geometry and selected-section JSON, and retain label identity.
- 2026-06-22: E3/#1727 missing report/label section geometry schema coverage shipped: Studio host regression coverage now proves FRX/LBX section rows without VPOS/HEIGHT fields keep JSON inspection non-failing, preserve live/deleted band and expression metadata, default selected-section geometry to zero with null field provenance, and retain label identity.
- 2026-06-22: E3/#1726 missing report/label object title schema coverage shipped: Studio host regression coverage now proves FRX/LBX layout object rows without NAME, EXPR, or UNIQUEID fields keep JSON inspection non-failing, synthesize `Record N` titles with null title/expression provenance, preserve live/deleted object geometry, selected-object JSON, containing-section behavior, and label identity.
- 2026-06-22: E3/#1725 missing report/label object EXPR schema coverage shipped: Studio host regression coverage now proves FRX/LBX layout object rows without an EXPR column keep JSON inspection non-failing, preserve live/deleted object metadata, geometry, selected-object JSON, containing-section behavior, null expression provenance, and label identity.
- 2026-06-22: E3/#1724 missing report/label section EXPR schema coverage shipped: Studio host regression coverage now proves FRX/LBX section rows without an EXPR column keep JSON inspection non-failing, preserve live/deleted band title/kind metadata, section geometry, selected-section JSON, null expression provenance, and label identity.
- 2026-06-22: E3/#1723 missing report/label root EXPR schema coverage shipped: Studio host regression coverage now proves FRX/LBX root settings rows without an EXPR column keep JSON inspection non-failing, preserve direct ORIENTATION/PAPERSIZE/TOPMARGIN settings in live and deleted settings arrays, keep page setup summaries available from direct fields, populate selected-settings metadata, and retain label identity.
- 2026-06-22: E3/#1722 unknown report/label band-code fallback coverage shipped: Studio host regression coverage now proves FRX/LBX band rows with structurally valid but unknown OBJCODE values keep JSON inspection non-failing, serialize as `Other Band` / `bandKind: "other"` in live and deleted section JSON, preserve section geometry, preview/deleted-preview bounds, counts, selected-section metadata, and label identity.
- 2026-06-22: E3/#1721 missing report/label layout classification field coverage shipped: Studio host regression coverage now proves FRX/LBX rows without OBJTYPE/OBJCODE columns keep JSON inspection non-failing, do not infer root settings, sections, objects, or preview bounds from unrelated rows, degrade explicit record selection to no selected report metadata, and retain label identity.
- 2026-06-22: E3/#1720 missing report/label layout geometry field coverage shipped: Studio host regression coverage now proves FRX/LBX rows without HPOS/VPOS/WIDTH/HEIGHT columns keep JSON inspection non-failing, default selected-object and preview/deleted-preview geometry to zero-origin zero-size non-inverted bounds, serialize selected-object geometry field provenance as null, preserve live/deleted object counts, and retain label identity.
- 2026-06-22: E3/#1719 fractional report/label layout geometry coverage shipped: Studio host regression coverage now proves FRX/LBX rows with fractional section/object geometry fields keep JSON inspection non-failing, use integer portions consistently for selected-object and preview/deleted-preview bounds, preserve live/deleted object counts, and retain label identity.
- 2026-06-22: E3/#1718 invalid report/label layout classification fallback coverage shipped: Studio host regression coverage now proves FRX/LBX rows with malformed and out-of-range OBJTYPE/OBJCODE classification values keep JSON inspection non-failing, do not create live/deleted sections or layout objects, degrade explicit record selection to no selected report metadata, preserve root settings, and retain label identity.
- 2026-06-21: E3/#1717 oversized report/label layout numeric fallback coverage shipped: Studio host regression coverage now proves FRX/LBX rows with out-of-range section/object geometry fields keep JSON inspection non-failing, default overflowed selected-object geometry to zero-origin zero-size non-inverted bounds, preserve preview/deleted-preview bounds, preserve live/deleted object counts, and retain label identity.
- 2026-06-21: E3/#1716 malformed report/label layout numeric fallback coverage shipped: Studio host regression coverage now proves FRX/LBX rows with malformed section/object geometry fields keep JSON inspection non-failing, default malformed selected-object geometry to zero-origin zero-size non-inverted bounds, preserve preview/deleted-preview bounds, preserve live/deleted object counts, and retain label identity.
- 2026-06-21: E3/#1715 negative report/label layout dimension clamp coverage shipped: Studio host regression coverage now proves FRX/LBX rows with negative section/object WIDTH/HEIGHT values keep JSON inspection non-failing, clamp live and deleted selected-object geometry to non-inverted zero-size bounds, preserve preview/deleted-preview bounds, preserve live/deleted object counts, and retain label identity.
- 2026-06-21: F1/#1714 Visual Studio launch-contract numeric selector error coverage shipped: `test_studio_host` now proves the VS launch parser rejects malformed and missing `--record`, `--line`, and `--column` values with explicit parser errors, covering the numeric selection error-management path without changing launch-contract behavior.
- 2026-06-21: E3/#1713 out-of-range explicit record report/label selector guard shipped: Studio document opening now clears launch-selection record availability when an FRX/LBX `--record` request is beyond the available table records, while preserving the requested record index for diagnostics and keeping explicit record selectors from falling back to a conflicting `--unique-id`. Regression coverage proves out-of-range report and label record selectors keep JSON inspection non-failing, expose `selectedReportSelectionKind: none`, clear selected object/section/settings/containing-section metadata to false/null, preserve layout counts and label identity, and avoid stale stable-selector leakage.
- 2026-06-21: E3/#1712 deep explicit record report/label selector support shipped: Studio document opening now parses at least through an explicitly requested record index instead of capping report/label JSON inspection at the default preview window, and regression coverage proves `--record 10` selects deep FRX/LBX layout object, section, and root settings rows with selected JSON populated, unrelated selected surfaces false/null, counts preserved, and label identity retained.
- 2026-06-21: E3/#1711 missing `UNIQUEID` report/label selector coverage shipped: Studio host regression coverage now proves FRX/LBX tables without a `UNIQUEID` column keep stable `--unique-id` JSON inspection non-failing, expose explicit no-selection metadata instead of selecting arbitrary rows, preserve section/settings/object counts, and retain label identity.
- 2026-06-21: E3/#1710 blank stable report/label selector coverage shipped: Studio host regression coverage now proves whitespace-only FRX/LBX `--unique-id` requests keep JSON inspection non-failing, expose explicit no-selection metadata instead of matching blank stored `UNIQUEID` fields, leave selected object/section/settings/containing-section surfaces false/null, and retain label identity.
- 2026-06-21: E3/#1709 deep live/deleted ambiguous stable report/label selector coverage shipped: Studio host regression coverage now proves duplicate `UNIQUEID` values split between live preview-window rows and deleted deeper FRX/LBX object, section, and root settings rows keep JSON inspection non-failing, expose explicit no-selection metadata instead of selecting the live preview row, preserve live/deleted object/section/settings counts beyond the preview window, and retain label identity.
- 2026-06-21: E3/#1708 deep ambiguous stable report/label section and settings selector coverage shipped: Studio host regression coverage now proves duplicate section and root settings `UNIQUEID` values split between preview-window rows and deeper FRX/LBX rows keep JSON inspection non-failing, expose explicit no-selection metadata instead of selecting the preview-window row, preserve live section/settings/object counts beyond the preview window, and retain label identity.
- 2026-06-21: E3/#1707 deep stable report/label section and settings selector coverage shipped: Studio host regression coverage now proves FRX/LBX section rows and root settings rows beyond the old preview limit can be selected by stable `--unique-id`, populating selected section/settings JSON, keeping unrelated selected surfaces false/null, preserving live counts beyond the preview window, and retaining label identity.
- 2026-06-21: E3/#1706 deep ambiguous stable report/label selector coverage shipped: Studio host regression coverage now proves duplicate live `UNIQUEID` values split between the default FRX/LBX preview window and a deeper layout-object row keep JSON inspection non-failing, expose explicit no-selection metadata instead of selecting the preview-window match, preserve full live object counts beyond the preview window, and retain label identity.
- 2026-06-21: E3/#1705 deep stable report/label selector lookup shipped: Studio host report/label `--unique-id` opens now load the full FRX/LBX table for selector resolution instead of only the default preview window, and regression coverage proves a deep layout object beyond the first eight preview records can be selected with populated selected-object and containing-section JSON while label identity is preserved.
- 2026-06-21: E3/#1704 record precedence over stable report/label selector JSON coverage shipped: Studio host regression coverage now proves explicit `--record` selection remains authoritative when a conflicting `--unique-id` is also supplied for FRX/LBX report-layout assets, preserving selected section/settings JSON, keeping conflicting selected-object surfaces false/null, and retaining label identity.
- 2026-06-21: E3/#1703 trimmed stable report/label selector JSON coverage shipped: Studio host regression coverage now proves padded stored `UNIQUEID` values on FRX/LBX layout object, section, and root settings rows can be selected by trimmed case-varied `--unique-id` requests, with the correct selected object/section/settings JSON populated, unrelated selected surfaces left false/null, and label identity preserved.
- 2026-06-21: E3/#1702 live/deleted ambiguous stable report/label selector JSON coverage shipped: Studio host regression coverage now proves duplicate `UNIQUEID` values split across live and deleted FRX/LBX layout objects, section records, and root settings records keep JSON inspection non-failing, expose `selectedReportSelectionKind: none`, clear selected object/section/settings/containing-section metadata to false/null, and preserve live/deleted counts plus label identity without selecting either duplicate row.
- 2026-06-21: E3/#1701 ambiguous stable report/label section and settings selection JSON coverage shipped: Studio host regression coverage now proves duplicate `UNIQUEID` values on FRX/LBX section records and root settings records keep JSON inspection non-failing, expose `selectedReportSelectionKind: none`, clear selected object/section/settings/containing-section metadata to false/null, and preserve section/settings/page-setup counts plus label identity without selecting an arbitrary row.
- 2026-06-21: A3/#1700 DBF error-management evidence references aligned: `test_dbf_table` no longer uses stale GitHub-style `#259`-`#271` assertion tokens for GAP-01/GAP-02/GAP-03 coverage that now point at unrelated closed issues, and `docs/safety/test-coverage-gap-analysis.md` now records the DBF malformed-header, numeric-boundary, and staged-write rollback tests that already exist while preserving the remaining thin spots. Fresh validation passed with `git diff --check`, `c++ -std=c++20 -Iinclude -fsyntax-only tests/test_dbf_table.cpp`, `make -C build test_dbf_table`, and `./build/tests/test_dbf_table`.
- 2026-06-21: E3/#1699 ambiguous stable report/label selection JSON guard shipped: Studio host report/label stable selector resolution now refuses duplicate `UNIQUEID` matches instead of selecting an arbitrary row, and regression coverage proves ambiguous FRX/LBX selectors keep JSON inspection non-failing, expose `selectedReportSelectionKind: none`, clear selected object/section/settings/containing-section metadata to false/null, and preserve live/deleted layout counts plus label identity.
- 2026-06-21: E3/#1698 missing stable report/label selection JSON coverage shipped: Studio host regression coverage now proves missing FRX/LBX `--unique-id` selectors on report/label assets keep JSON inspection non-failing, expose `selectedReportSelectionKind: none`, clear selected object/section/settings/containing-section metadata to false/null, and preserve live/deleted layout counts plus label identity without leaking stale selection data.
- 2026-06-21: E3/#1697 stable-selector deleted summary report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside summary-section geometry seeded with `UNIQUEID=summary-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1696 stable-selector summary report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside summary sections seeded with `UNIQUEID=summary-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, summary containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1695 stable-selector deleted summary report/label section selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX summary sections seeded with `UNIQUEID=summary-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted summary band metadata, live sibling detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1694 stable-selector summary report/label section selection coverage shipped: Studio host regression coverage now proves FRX/LBX summary sections seeded with `UNIQUEID=summary-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, summary band metadata, sibling detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1693 stable-selector deleted group-footer report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside group-footer geometry seeded with `UNIQUEID=group-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1692 stable-selector deleted group-header report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside group-header geometry seeded with `UNIQUEID=group-header-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1691 stable-selector group-footer report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside group-footer sections seeded with `UNIQUEID=group-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, group-footer containing-section metadata with expression provenance, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1690 stable-selector group-header report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside group-header sections seeded with `UNIQUEID=group-header-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, group-header containing-section metadata with expression provenance, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1689 stable-selector deleted column-footer report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside column-footer geometry seeded with `UNIQUEID=column-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1688 stable-selector deleted column-header report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside column-header geometry seeded with `UNIQUEID=column-header-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1687 stable-selector deleted page-footer report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside page-footer geometry seeded with `UNIQUEID=page-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1686 stable-selector deleted title-band report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX label objects inside title-band geometry seeded with `UNIQUEID=title-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1685 stable-selector column-footer report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside column-footer sections seeded with `UNIQUEID=column-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, column-footer containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1684 stable-selector column-header report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside column-header sections seeded with `UNIQUEID=column-header-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, column-header containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1683 stable-selector page-footer report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside page-footer sections seeded with `UNIQUEID=page-footer-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, page-footer containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1682 stable-selector title-band report/label object selection coverage shipped: Studio host regression coverage now proves FRX/LBX label objects inside title sections seeded with `UNIQUEID=title-label-guid` can be selected by `--unique-id`, exposing selected-object availability/kind, title containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1681 stable-selector deleted column-footer report/label section selection coverage shipped: Studio host regression coverage now proves deleted column-footer FRX/LBX sections seeded with `UNIQUEID=column-footer-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted column-footer band metadata, live sibling column-header/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1680 stable-selector deleted column-header report/label section selection coverage shipped: Studio host regression coverage now proves deleted column-header FRX/LBX sections seeded with `UNIQUEID=column-header-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted column-header band metadata, live sibling detail/column-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1679 stable-selector deleted page-footer report/label section selection coverage shipped: Studio host regression coverage now proves deleted page-footer FRX/LBX sections seeded with `UNIQUEID=page-footer-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted page-footer band metadata, live sibling title/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1678 stable-selector deleted title report/label section selection coverage shipped: Studio host regression coverage now proves deleted title FRX/LBX sections seeded with `UNIQUEID=title-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1677 stable-selector column-footer report/label section selection coverage shipped: Studio host regression coverage now proves column-footer FRX/LBX sections seeded with `UNIQUEID=column-footer-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, column-footer band metadata, live sibling column-header/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1676 stable-selector column-header report/label section selection coverage shipped: Studio host regression coverage now proves column-header FRX/LBX sections seeded with `UNIQUEID=column-header-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, column-header band metadata, live sibling detail/column-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1675 stable-selector page-footer report/label section selection coverage shipped: Studio host regression coverage now proves page-footer FRX/LBX sections seeded with `UNIQUEID=page-footer-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, page-footer band metadata, live sibling title/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1674 stable-selector title report/label section selection coverage shipped: Studio host regression coverage now proves title FRX/LBX sections seeded with `UNIQUEID=title-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1673 stable-selector deleted orphaned detail report/label object selection coverage shipped: Studio host regression coverage now proves deleted detail FRX/LBX label objects whose containing detail section is also deleted can still be selected by `--unique-id deleted-label-guid`, with selected-object availability/kind, expression provenance, geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, sibling page-header metadata, and label identity preserved without a record selector.
- 2026-06-21: E3/#1672 stable-selector orphaned detail report/label object selection coverage shipped: Studio host regression coverage now proves live detail FRX/LBX field objects whose containing detail section is deleted can still be selected by `--unique-id field-guid`, with selected-object availability/kind, expression provenance, geometry, live/unplaced/deleted object counts, null selected section/settings metadata, null containing-section metadata, sibling page-header metadata, and label identity preserved without a record selector.
- 2026-06-21: E3/#1671 stable-selector orphaned page-header report/label object selection coverage shipped: Studio host regression coverage now proves live page-header FRX/LBX label objects whose containing page-header section is deleted can still be selected by `--unique-id label-guid`, with selected-object availability/kind, expression provenance, geometry, live/unplaced/deleted object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved without a record selector.
- 2026-06-21: E3/#1670 stable-selector deleted page-header report/label object selection coverage shipped: Studio host regression coverage now proves deleted page-header FRX/LBX label objects selected by `--unique-id label-guid` expose selected-object availability/kind, expression provenance, geometry, live/deleted/placed object counts, null selected section/settings metadata, null containing-section metadata, and label identity without a record selector.
- 2026-06-21: E3/#1669 stable-selector deleted group-footer report/label section selection coverage shipped: Studio host regression coverage now proves deleted group-footer FRX/LBX sections selected by `--unique-id group-footer-guid` expose selected-section availability/kind, deleted group-footer band metadata, expression provenance, live/deleted section counts, sibling group-header/detail metadata, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1668 stable-selector deleted group-header report/label section selection coverage shipped: Studio host regression coverage now proves deleted group-header FRX/LBX sections selected by `--unique-id group-header-guid` expose selected-section availability/kind, deleted group-header band metadata, expression provenance, live/deleted section counts, sibling detail/footer metadata, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1667 stable-selector group-footer report/label section selection coverage shipped: Studio host regression coverage now proves live group-footer FRX/LBX sections seeded with `UNIQUEID=group-footer-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, group-footer band metadata, expression provenance, sibling group-header/detail section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1666 stable-selector group-header report/label section selection coverage shipped: Studio host regression coverage now proves live group-header FRX/LBX sections seeded with `UNIQUEID=group-header-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, group-header band metadata, expression provenance, sibling detail/footer section counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1665 stable-selector deleted page-header report/label section selection coverage shipped: Studio host regression coverage now proves deleted page-header FRX/LBX sections seeded with `UNIQUEID=deleted-page-header-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, deleted page-header band metadata, live/deleted section counts, unplaced object accounting for former section members, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1664 stable-selector page-header report/label section selection coverage shipped: Studio host regression coverage now proves live page-header FRX/LBX sections seeded with `UNIQUEID=page-header-section-guid` can be selected by `--unique-id`, exposing selected-section availability/kind, page-header band metadata, section object membership, live/deleted section and object counts, null selected object/settings metadata, and label identity without a record selector.
- 2026-06-21: E3/#1663 stable-selector page-header report/label object selection coverage shipped: Studio host regression coverage now proves live page-header FRX/LBX layout objects selected by `--unique-id label-guid` expose selected-object availability/kind, label expression/provenance, page-header containing-section metadata, section-relative geometry, label identity, and live/deleted object counts without a record selector.
- 2026-06-21: E3/#1662 stable-selector deleted unplaced report/label object selection coverage shipped: Studio host regression coverage now proves deleted FRX/LBX layout objects moved outside section bounds and selected by `--unique-id deleted-label-guid` expose selected-object availability/kind, live/deleted/unplaced object counts, deleted-unplaced accounting, null containing-section metadata, label identity, geometry, and provenance without a record selector.
- 2026-06-21: E3/#1661 stable-selector unplaced report/label object selection coverage shipped: Studio host regression coverage now proves live unplaced FRX/LBX layout objects selected by `--unique-id unplaced-line-guid` expose selected-object availability/kind, live/deleted/unplaced object counts, null containing-section metadata, label identity, geometry, and provenance without a record selector.
- 2026-06-21: E3/#1660 stable-selector report/label object selection coverage shipped: Studio host regression coverage now proves live FRX/LBX objects selected by `--unique-id field-guid` and deleted FRX/LBX label objects selected by `--unique-id deleted-label-guid` expose selected-object availability/kind, live/deleted object counts, live containing-section metadata, deleted-object null containing-section metadata, label identity, geometry, and provenance without a record selector.
- 2026-06-21: E3/#1659 stable-selector report/label section selection coverage shipped: Studio host regression coverage now proves live and deleted FRX/LBX sections seeded with stable ids can be selected for JSON inspection by `--unique-id`, preserving selected-section availability/kind, live/deleted section counts, unplaced object accounting, null settings/object selection, label identity, and section metadata without a record selector.
- 2026-06-21: E3/#1658 stable-selector report/label root-settings selection coverage shipped: Studio host regression coverage now proves live and deleted root FRX/LBX settings seeded with stable ids can be selected for JSON inspection by `--unique-id`, preserving selected-settings availability/kind, live/deleted setting counts, page setup state, null section/object selection, label identity, and setting provenance without a record selector.
- 2026-06-21: E3/#1657 stable-selector deleted report/label root-settings restore coverage shipped: Studio host regression coverage now proves deleted root FRX/LBX settings seeded with a stable id can be restored by `--restore-object --unique-id`, clearing the DBF deleted flag, restoring live setting/page setup summaries, clearing deleted setting counts, and rehydrating selected-settings JSON.
- 2026-06-21: E3/#1656 stable-selector report/label root-settings delete coverage shipped: Studio host regression coverage now proves root FRX/LBX settings seeded with a stable id can be deleted by `--delete-object --unique-id`, setting the DBF deleted flag, clearing live setting/page setup summaries, exposing deleted and selected deleted-settings metadata, and preserving section/deleted-object state.
- 2026-06-21: E3/#1655 stable-selector report/label section delete coverage shipped: Studio host regression coverage now proves live FRX/LBX sections seeded with a stable id can be deleted by `--delete-object --unique-id`, setting the DBF deleted flag, refreshing live/deleted section counts, moving former section objects to unplaced membership without fabricated containing-section ids, and selecting the deleted section in JSON.
- 2026-06-21: E3/#1654 stable-selector deleted report/label section restore coverage shipped: Studio host regression coverage now proves deleted FRX/LBX sections seeded with a stable id can be restored by `--restore-object --unique-id`, clearing the DBF deleted flag, restoring live/deleted section counts, moving former unplaced objects back into section membership, and rehydrating selected-section JSON.
- 2026-06-21: E3/#1653 stable-selector edited deleted report/label layout-object unplaced restore coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object edited by stable id into out-of-section geometry can be restored by stable id, clearing the DBF deleted flag while preserving edited unplaced geometry, refreshing preview bounds/counts, and keeping selected-object containing-section metadata unavailable/null.
- 2026-06-21: E3/#1652 stable-selector edited deleted report/label layout-object restore coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object edited by stable id for HPOS/VPOS/HEIGHT can be restored by stable id, clearing the DBF deleted flag while preserving edited geometry and rehydrating live selected-object section metadata.
- 2026-06-21: E3/#1651 stable-selector deleted report/label layout-object top-position clear coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object VPOS can be cleared by stable id through `--clear-property --unique-id deleted-label-guid --property-name VPOS`, preserving the DBF deleted flag while removing stale deleted-object and selected-object top/bottom metadata.
- 2026-06-21: E3/#1650 stable-selector deleted report/label layout-object top-position update coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object VPOS can be updated by stable id through `--set-property --unique-id deleted-label-guid --property-name VPOS`, preserving the DBF deleted flag while refreshing deleted-object and selected-object top/bottom metadata.
- 2026-06-21: E3/#1649 stable-selector deleted report/label layout-object height clear coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object HEIGHT can be cleared by stable id through `--clear-property --unique-id deleted-label-guid --property-name HEIGHT`, preserving the DBF deleted flag while removing stale deleted-object and selected-object height/bottom metadata.
- 2026-06-21: E3/#1648 stable-selector deleted report/label layout-object height update coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object HEIGHT can be updated by stable id through `--set-property --unique-id deleted-label-guid --property-name HEIGHT`, preserving the DBF deleted flag while refreshing deleted-object and selected-object height/bottom metadata.
- 2026-06-21: E3/#1647 stable-selector deleted report/label layout-object left-position clear coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object HPOS can be cleared by stable id through `--clear-property --unique-id deleted-label-guid --property-name HPOS`, preserving the DBF deleted flag while removing stale deleted-object and selected-object left/right metadata.
- 2026-06-21: E3/#1646 stable-selector deleted report/label layout-object left-position update coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object HPOS can be updated by stable id through `--set-property --unique-id deleted-label-guid --property-name HPOS`, preserving the DBF deleted flag while refreshing deleted-object and selected-object left/right metadata.
- 2026-06-21: E3/#1645 stable-selector deleted report/label layout-object width clear coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object WIDTH can be cleared by stable id through `--clear-property --unique-id deleted-label-guid --property-name WIDTH`, preserving the DBF deleted flag while removing stale deleted-object and selected-object width/right metadata.
- 2026-06-21: E3/#1644 stable-selector deleted report/label layout-object width update coverage shipped: Studio host regression coverage now proves a deleted FRX/LBX layout object WIDTH can be updated by stable id through `--set-property --unique-id deleted-label-guid --property-name WIDTH`, preserving the DBF deleted flag while refreshing deleted-object and selected-object width/right metadata.
- 2026-06-21: E3/#1643 stable-selector report/label layout-object top-position clear coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object VPOS can be cleared by stable id through `--clear-property --unique-id field-guid --property-name VPOS`, blanking the VPOS direct field and refreshing selected-object section membership plus top/bottom metadata without a record selector.
- 2026-06-21: E3/#1642 stable-selector report/label layout-object top-position update coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object VPOS can be updated by stable id through `--set-property --unique-id field-guid --property-name VPOS`, refreshing selected-object top/bottom metadata, unplaced counts, and preview top/height bounds without a record selector.
- 2026-06-21: E3/#1641 stable-selector report/label layout-object height clear coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object HEIGHT can be cleared by stable id through `--clear-property --unique-id field-guid --property-name HEIGHT`, blanking the HEIGHT direct field and refreshing selected-object height/bottom metadata without leaking stale geometry.
- 2026-06-21: E3/#1640 stable-selector report/label layout-object height update coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object HEIGHT can be updated by stable id through `--set-property --unique-id field-guid --property-name HEIGHT`, refreshing selected-object height/bottom metadata and preview bottom/height bounds without a record selector.
- 2026-06-21: E3/#1639 stable-selector report/label layout-object left-position clear coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object HPOS can be cleared by stable id through `--clear-property --unique-id label-guid --property-name HPOS`, blanking the HPOS direct field and refreshing selected-object left/right-edge metadata plus preview left/width bounds without a record selector.
- 2026-06-21: E3/#1638 stable-selector report/label layout-object left-position update coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object HPOS can be updated by stable id through `--set-property --unique-id label-guid --property-name HPOS`, refreshing selected-object left/right-edge metadata and preview left/width bounds without a record selector.
- 2026-06-21: E3/#1637 stable-selector report/label layout-object width clear coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object WIDTH can be cleared by stable id through `--clear-property --unique-id field-guid --property-name WIDTH`, refreshing selected-object width/right-edge metadata and preview bounds without leaking stale geometry.
- 2026-06-21: E3/#1636 stable-selector report/label layout-object width update coverage shipped: Studio host regression coverage now proves a live FRX/LBX layout object WIDTH can be updated by stable id through `--set-property --unique-id field-guid --property-name WIDTH`, refreshing selected-object width/right-edge metadata and preview bounds without a record selector.
- 2026-06-21: E3/#1635 stable-selector report/label layout-object unplacement coverage shipped: Studio host regression coverage now proves a section-contained FRX/LBX layout object can be moved out of live sections by stable id through `--set-property --unique-id field-guid --property-name VPOS`, refreshing placed/unplaced counts and clearing selected-object containing-section metadata without a record selector.
- 2026-06-21: E3/#1634 stable-selector report/label layout-object placement coverage shipped: Studio host regression coverage now proves an initially unplaced FRX/LBX layout object can be moved into a live section by stable id through `--set-property --unique-id unplaced-line-guid --property-name VPOS`, refreshing placed/unplaced counts and selected-object containing-section metadata without a record selector.
- 2026-06-21: E3/#1633 stable-selector report/label layout-object font metadata clear coverage shipped: Studio host regression coverage now proves `--clear-property --unique-id field-guid --property-name FONTFACE` removes FRX/LBX object font highlights and returns refreshed `selectedReportObject` highlight plus containing-section metadata without leaking stale font values.
- 2026-06-21: E3/#1632 stable-selector report/label layout-object font metadata update coverage shipped: Studio host regression coverage now proves `--set-property --unique-id field-guid --property-name FONTFACE` persists FRX/LBX object font memo edits and returns refreshed `selectedReportObject` highlight plus containing-section metadata without a record selector.
- 2026-06-21: E3/#1631 stable-selector report/label layout-object expression clear coverage shipped: Studio host now refreshes post-property-clear selection to the edited FRX/LBX object, and regression coverage proves `--clear-property --unique-id field-guid --property-name EXPR` blanks the expression and returns refreshed `selectedReportObject` expression plus containing-section metadata without a record selector.
- 2026-06-21: E3/#1630 stable-selector report/label layout-object expression update coverage shipped: Studio host now refreshes post-property-update selection to the edited FRX/LBX object, and regression coverage proves `--set-property --unique-id field-guid --property-name EXPR` persists the expression and returns refreshed `selectedReportObject` expression plus containing-section metadata without a record selector.
- 2026-06-21: E3/#1629 live-edited report/label layout-object distribution coverage shipped: Studio host now refreshes post-distribution selection to the distributed FRX/LBX object, and regression coverage proves edited WIDTH/HEIGHT survive stable-id horizontal distribution while computed HPOS coordinates and containing-section metadata appear in refreshed `selectedReportObject` JSON without a record selector.
- 2026-06-21: E3/#1628 live-edited report/label layout-object nudge coverage shipped: Studio host now refreshes post-nudge selection to the nudged FRX/LBX object, and regression coverage proves edited WIDTH/HEIGHT survive stable-id nudging while delta-adjusted HPOS/VPOS coordinates and containing-section metadata appear in refreshed `selectedReportObject` JSON without a record selector.
- 2026-06-21: E3/#1627 live-edited report/label layout-object snap coverage shipped: Studio host now refreshes post-snap selection to the snapped FRX/LBX object, and regression coverage proves edited WIDTH/HEIGHT survive stable-id grid snapping while rounded HPOS/VPOS coordinates and containing-section metadata appear in refreshed `selectedReportObject` JSON without a record selector.
- 2026-06-21: E3/#1626 live-edited report/label layout-object resize coverage shipped: Studio host now refreshes post-resize selection to the resized FRX/LBX object, and regression coverage proves edited HPOS survives stable-id resizing while anchor WIDTH/HEIGHT dimensions and containing-section metadata appear in refreshed `selectedReportObject` JSON without a record selector.
- 2026-06-21: E3/#1625 live-edited report/label layout-object align coverage shipped: Studio host now refreshes post-align selection to the aligned FRX/LBX object, and regression coverage proves edited HPOS/WIDTH/HEIGHT geometry plus containing-section metadata survive stable-id alignment without a record selector in refreshed `selectedReportObject` JSON.
- 2026-06-21: E3/#1624 live-edited unplaced report/label layout-object reorder coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited outside section bands can be physically reordered with edited HPOS/VPOS/HEIGHT geometry preserved, the reordered object selected, and selected containing-section JSON kept unavailable/null.
- 2026-06-21: E3/#1623 live-edited report/label layout-object reorder coverage shipped: Studio host now refreshes post-reorder selection to the reordered FRX/LBX object, and regression coverage proves edited HPOS/WIDTH/HEIGHT geometry plus containing-section metadata survive physical record reorder in refreshed `selectedReportObject` JSON.
- 2026-06-21: E3/#1622 live-edited unplaced report/label layout-object rename coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited outside section bands can be renamed with edited HPOS/VPOS/HEIGHT geometry preserved, the renamed object selected, and selected containing-section JSON kept unavailable/null.
- 2026-06-21: E3/#1621 live-edited report/label layout-object rename coverage shipped: Studio host now refreshes post-rename selection to the renamed FRX/LBX object, and regression coverage proves edited HPOS/WIDTH/HEIGHT geometry plus containing-section metadata survive identity rename in refreshed `selectedReportObject` JSON.
- 2026-06-21: E3/#1620 live-edited unplaced report/label layout-object duplicate coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited outside section bands can be duplicated with edited HPOS/VPOS/HEIGHT geometry preserved, the duplicate selected, and selected containing-section JSON kept unavailable/null.
- 2026-06-21: E3/#1619 live-edited report/label layout-object duplicate coverage shipped: Studio host now refreshes post-duplicate selection to the new FRX/LBX object, and regression coverage proves duplicates preserve edited HPOS/WIDTH/HEIGHT geometry plus containing-section metadata in refreshed `selectedReportObject` JSON.
- 2026-06-21: E3/#1618 live-edited unplaced deleted report/label layout-object restore coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited outside section bands, deleted, and restored come back as live unplaced objects with edited geometry preserved, preview/unplaced counts refreshed, and selected containing-section metadata unavailable/null.
- 2026-06-21: E3/#1617 live-edited deleted report/label layout-object restore coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited through host geometry properties, deleted, and then restored preserve edited geometry, leave deleted-object counts, and rehydrate selected live containing-section metadata with section-relative geometry.
- 2026-06-21: E3/#1616 edited unplaced report/label layout-object delete geometry coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited into an unplaced geometry can then be deleted, preserve edited HPOS/VPOS/HEIGHT fields, expand deleted preview bounds, refresh deleted placed/unplaced accounting, and keep selected containing-section metadata unavailable/null.
- 2026-06-21: E3/#1615 edited report/label layout-object delete geometry coverage shipped: Studio host regression coverage now proves live FRX/LBX layout objects edited through host geometry properties can then be deleted, preserve edited HPOS/WIDTH/HEIGHT fields, refresh `deletedObjects` and `selectedReportObject` geometry, and keep selected containing-section metadata unavailable/null.
- 2026-06-21: E3/#1614 edited deleted report/label layout-object unplaced restore coverage shipped: Studio host regression coverage now proves deleted FRX/LBX layout objects edited outside live section bands restore as unplaced objects, preserve edited geometry, expand preview bounds, leave `deletedObjects`, and keep selected containing-section metadata unavailable/null.
- 2026-06-21: E3/#1613 edited deleted report/label layout-object restore coverage shipped: Studio host regression coverage now proves deleted FRX/LBX layout objects can be edited while deleted, restored through the host, retain edited geometry fields, leave `deletedObjects`, and rehydrate live selected containing-section metadata with section object indexes/counts.
- 2026-06-21: E3/#1612 deleted report/label layout-object top-position clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `VPOS` fields blanks through the generic property path, refreshes `deletedObjects` and selected deleted-object top/bottom-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1611 deleted report/label layout-object top-position edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `VPOS` fields persists through the generic property path, refreshes `deletedObjects` and selected deleted-object top/bottom-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1610 deleted report/label layout-object height clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `HEIGHT` fields blanks through the generic property path, refreshes `deletedObjects` and selected deleted-object height/bottom-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1609 deleted report/label layout-object height edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `HEIGHT` fields persists through the generic property path, refreshes `deletedObjects` and selected deleted-object height/bottom-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1608 deleted report/label layout-object left-position clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `HPOS` fields blanks through the generic property path, refreshes `deletedObjects` and selected deleted-object left/right-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1607 deleted report/label layout-object left-position edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `HPOS` fields persists through the generic property path, refreshes `deletedObjects` and selected deleted-object left/right-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1606 deleted report/label layout-object width clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `WIDTH` fields clears through the generic property path, refreshes `deletedObjects` and selected deleted-object width/right-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1605 deleted report/label layout-object width edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `WIDTH` fields persists through the generic property path, refreshes `deletedObjects` and selected deleted-object width/right-edge JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1604 deleted report/label layout-object font clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `FONTFACE` memos clears through the generic property path, refreshes `deletedObjects` and selected deleted-object highlight/provenance JSON down to the remaining expression highlight, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1603 deleted report/label layout-object font edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `FONTFACE` memos persists through the generic property path, refreshes `deletedObjects` and selected deleted-object highlight/provenance JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1602 deleted report/label layout-object expression clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX layout-object `EXPR` memos clears through the generic property path, refreshes `deletedObjects` and selected deleted-object JSON with fallback titles/provenance, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1601 deleted report/label layout-object expression edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX layout-object `EXPR` memos persists through the generic property path, refreshes `deletedObjects` and selected deleted-object JSON, and keeps containing-section metadata unavailable instead of fabricating live section membership.
- 2026-06-21: E3/#1600 deleted report/label column setup memo clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root column setup `EXPR` memos blanks the memo carrier through the generic property path, removes deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1599 deleted report/label column setup memo edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root column setup `EXPR` memos persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1598 deleted report/label column-spacing field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root column setup `COLSPACING` fields blanks through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1597 deleted report/label column-spacing field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root column setup `COLSPACING` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1596 deleted report/label column-width field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root column setup `COLWIDTH` fields blanks through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1595 deleted report/label column-width field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root column setup `COLWIDTH` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1594 deleted report/label column-count field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root column setup `COLS` fields blanks through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1593 deleted report/label column-count field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root column setup `COLS` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted column settings, and keeps live page/column setup unavailable.
- 2026-06-21: E3/#1592 deleted report/label paper-size field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `PAPERSIZE` fields blanks through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, preserves remaining memo-derived deleted settings, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1591 deleted report/label paper-size field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `PAPERSIZE` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1590 deleted report/label orientation field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `ORIENTATION` fields clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1589 deleted report/label orientation field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `ORIENTATION` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1588 deleted report/label horizontal-grid field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `GRIDH` fields clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1587 deleted report/label horizontal-grid field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `GRIDH` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1586 deleted report/label vertical-grid field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `GRIDV` fields clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1585 deleted report/label vertical-grid field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `GRIDV` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1584 deleted report/label bottom-margin field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `BOTMARGIN` fields clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1583 deleted report/label bottom-margin field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `BOTMARGIN` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable.
- 2026-06-21: E3/#1582 deleted report/label top-margin field clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `TOPMARGIN` fields clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable while preserving section/object metadata.
- 2026-06-21: E3/#1581 deleted report/label top-margin field edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `TOPMARGIN` fields persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable while preserving section/object metadata.
- 2026-06-21: E3/#1580 deleted report/label settings memo clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX root settings `EXPR` records clears through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable while preserving section/object metadata.
- 2026-06-21: E3/#1579 deleted report/label settings memo edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX root settings `EXPR` records persists through the generic property path, refreshes `deletedSettings` and selected deleted-settings JSON, and keeps live page setup/settings unavailable while preserving section/object metadata.
- 2026-06-21: E3/#1578 deleted report/label section top clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX section `VPOS` records clears through the generic property path, refreshes deleted-section and selected deleted-section top/bottom geometry, and preserves unplaced object accounting without fabricating live section order.
- 2026-06-21: E3/#1577 deleted report/label section top edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX section `VPOS` records persists through the generic property path, refreshes deleted-section and selected deleted-section top/bottom geometry, and preserves unplaced object accounting without fabricating live section order.
- 2026-06-21: E3/#1576 deleted report/label section height clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX section `HEIGHT` records clears through the generic property path, refreshes deleted-section and selected deleted-section geometry, and preserves unplaced object accounting without fabricating live section order.
- 2026-06-21: E3/#1575 deleted report/label section height edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX section `HEIGHT` records persists through the generic property path, refreshes deleted-section and selected deleted-section geometry, and preserves unplaced object accounting without fabricating live section order.
- 2026-06-21: E3/#1574 deleted report/label group footer expression edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX group-footer `EXPR` records persists through the generic property path, refreshes both `deletedSections` and selected deleted-section expression/provenance JSON, and preserves live group-header metadata.
- 2026-06-21: E3/#1573 deleted report/label group footer expression clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX group-footer `EXPR` records clears both `deletedSections` and selected deleted-section expression/provenance JSON while preserving live group-header metadata.
- 2026-06-21: E3/#1572 deleted report/label group section expression clear coverage shipped: Studio host regression coverage now proves `--clear-property` on deleted FRX/LBX group-header `EXPR` records clears both `deletedSections` and selected deleted-section expression/provenance JSON while preserving live sibling sections.
- 2026-06-21: E3/#1571 deleted report/label group section expression edit coverage shipped: Studio host regression coverage now proves `--set-property` on deleted FRX/LBX group-header `EXPR` records persists through the generic property path and refreshes both `deletedSections` and selected deleted-section JSON.
- 2026-06-21: E3/#1570 deleted report/label group footer expression coverage shipped: Studio host regression coverage now proves deleted FRX/LBX group footers retain `EXPR` text plus field/memo provenance in `deletedSections` and selected deleted-section JSON while preserving live group-header expression metadata.
- 2026-06-21: E3/#1569 deleted report/label group section expression coverage shipped: Studio host regression coverage now proves deleted FRX/LBX group sections retain `EXPR` text plus field/memo provenance in `deletedSections` and selected deleted-section JSON while preserving live sibling section metadata.
- 2026-06-21: E3/#1568 report/label group footer expression edit/clear coverage shipped: Studio host regression coverage now proves `--set-property` and `--clear-property` on FRX/LBX group-footer `EXPR` records refresh selected-section expression JSON, preserve footer geometry, and leave sibling group-header expression provenance intact.
- 2026-06-21: E3/#1567 report/label group section expression edit/clear coverage shipped: Studio host regression coverage now proves `--set-property` and `--clear-property` on FRX/LBX group-section `EXPR` records refresh selected-section expression JSON, preserve section geometry and sibling group expressions, allocate new memo provenance on updates, and clear expression provenance on clears.
- 2026-06-21: E3/#1566 report/label group section expression JSON shipped: `StudioReportSectionSnapshot` now carries group section expressions with field/memo provenance, and Studio host report/label JSON exposes those fields for full layout sections plus selected-section payloads. Focused model and host JSON regressions cover FRX/LBX group headers and footers without fabricating expressions on detail sections.
- 2026-06-21: `issues.txt` was regenerated from the live open GitHub issue export after direct verification showed omitted rows `#274`, `#277`, `#279`, and `#280` are open, while stale local rows `#7`, `#8`, `#13`-`#18`, and `#38`-`#43` are closed. No issue state changed in this slice.
- 2026-06-21: Local issue snapshot alignment removed stale open rows for closed historical `#92`-`#94`, `#97`-`#101`, and closed parent rows `#19`-`#22` after live GitHub verification. Parent `#23` and later roadmap rows remain open where GitHub still reports them open.
- 2026-06-21: Local issue snapshot alignment removed stale open rows for `#150`-`#161` after live GitHub verification showed the B/C prompt-sized slices are already closed with shipped historical regression evidence.
- 2026-06-21: Local issue snapshot alignment removed stale open rows for `#178`-`#193` and `#200`-`#203` after live GitHub verification showed those G/H/I prompt-sized slices are already closed. Portability rows `#194`-`#199` remain open because GitHub still reports them open.
- 2026-06-21: Local issue snapshot alignment removed stale open rows for `#162`-`#177` after live GitHub verification showed the D1/D2/D3/E1/E2/E3/F1/F2 prompt-sized slices are already closed and existing docs record their focused regression coverage. No issue state changed in this slice.
- 2026-06-21: E2/#1403-#1405 evidence-backed closure batch completed: toolbox palette launch-plan, toolbox create-batch plan, and toolbox create-plan summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unsupported toolbox selections, unknown or missing selection contexts, invalid toolbox context tokens, unknown toolbox items, unavailable selected-context items, blocked batch-plan diagnostics without fabricated item ids, dry-run/non-mutating planning, and no asset mutation on rejected planning paths. The closed `#1203`-`#1447` E2 evidence-audit range is now contiguous after the already-closed `#1406`-`#1447` rows.
- 2026-06-21: E2/#1399-#1402 evidence-backed closure batch completed: designer launch-surface, invocation-admission, dispatch, and execution selected-context summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unknown or missing selection contexts, invalid admission booleans, unsupported toolbox contexts surfaced as blocked selected-context summaries, dry-run/unadmitted child dispatch errors, failed child process diagnostics, non-executing dispatch planning, and non-mutating summaries. The closed `#1203`-`#1402` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1395-#1398 evidence-backed closure batch completed: editor-action launch-plan, invocation-admission, dispatch, and execution summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes wrong-context and unavailable actions, unknown or missing selection contexts, missing action ids, invalid boolean/numeric values, dry-run and unadmitted dispatch/execution rejection, missing launch-command parser failures, nonzero child process exit reporting, non-executing dispatch planning, and non-mutating summaries. The closed `#1203`-`#1398` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1391-#1394 evidence-backed closure batch completed: builder launch-plan, builder invocation-admission, builder dispatch, and builder execution summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes invalid context tokens, ambiguous builder/selection context requests, unavailable or wrong-context builders, dry-run and unadmitted dispatch/execution rejection, missing launch-command parser failures, nonzero child process exit reporting, non-executing dispatch planning, and non-mutating summaries. The closed `#1203`-`#1394` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1388-#1390 evidence-backed closure batch completed: toolbox invocation-admission, toolbox dispatch, and toolbox execution summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unsupported toolbox contexts, invalid admission booleans, invalid records, missing contexts, dry-run/unadmitted dispatch rejection, missing launch-command parser failures, unadmitted execution rejection, nonzero toolbox process exit reporting, non-executing dispatch planning, and non-mutating execution summaries. The closed `#1203`-`#1390` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1386-#1387 evidence-backed closure batch completed: toolbox create-dispatch plan and toolbox batch create-dispatch plan summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes non-admitted create dispatches, unavailable toolbox items, invalid batch plans, invalid admission tokens, orphan batch-item options, stale dispatch-argument suppression, non-executing dispatch planning, and no asset mutation. The closed `#1203`-`#1387` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1382-#1385 evidence-backed closure batch completed: toolbox create, toolbox batch-create, toolbox create-from-dispatch, and toolbox batch create-from-dispatch result summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unavailable toolbox items, non-admitted dispatches, duplicate identity rejection, lower-layer invalid-field failures, stale result suppression, and no mutation or partial mutation on rejected create paths. The closed `#1203`-`#1385` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1379-#1381 evidence-backed closure batch completed: selection toolbox create-batch-plan catalog, toolbox create-batch-dispatch catalog, and selection toolbox create-batch-dispatch catalog status summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unsupported-selection planner errors, aggregate plan/dispatch blocked errors without fabricated item rows, null dispatch payloads on rejected dispatches, stale-argument suppression, dry-run/non-mutating catalog behavior, and no asset mutation. The closed `#1203`-`#1381` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1374-#1378 evidence-backed closure batch completed: toolbox create-plan catalog, selection toolbox create-plan catalog, toolbox create-dispatch catalog, selection toolbox create-dispatch catalog, and toolbox create-batch-plan catalog status summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes parser rejections, non-admitted create-dispatch blockers, stale-argument suppression, aggregate batch-plan errors, dry-run/non-mutating catalog behavior, and no asset mutation. The closed `#1203`-`#1378` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1368-#1373 evidence-backed closure batch completed: toolbox palette launch catalog, toolbox invocation-admission catalog, toolbox dispatch catalog, toolbox dispatch-execution catalog, selection toolbox dispatch catalog, and selection toolbox invocation-admission catalog status/readiness summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes unsupported toolbox contexts, ready/blocked item summaries, admission/dispatch/execution blocked errors, dry-run/non-mutating catalog behavior, and no toolbox/editor/builder process or UI execution. The closed `#1203`-`#1373` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1361-#1367 evidence-backed closure batch completed: builder invocation-admission, builder dispatch, builder dispatch-execution, editor-action launch, editor-action invocation-admission, editor-action dispatch, and editor-action dispatch-execution catalog status/readiness summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes ready/blocked ids, representative admission/dispatch/execution blocked errors, dry-run/non-mutating catalog behavior, and no editor/builder/toolbox process or UI execution. The closed `#1203`-`#1367` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1356-#1360 evidence-backed closure batch completed: designer dispatch-execution, dispatch, invocation-admission, launch-surface catalog context summaries plus builder launch catalog status summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes ready/blocked context and builder summaries, representative blocked errors for unsupported toolbox and admission failures, dry-run/non-mutating catalog behavior, and no editor/builder/toolbox process or UI execution. The closed `#1203`-`#1360` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1347-#1355 evidence-backed closure batch completed: observed process exit-code normalization, aggregate child launch/target/descriptor metadata, toolbox item descriptors, and failed-child command-token/executed-command/exit-code summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes normalized nonzero process exits, preserved child launch metadata on failure, reporting-only child metadata fallback, failed-child token/command/exit summaries, and no execution behavior changes. The closed `#1203`-`#1355` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1340-#1346 evidence-backed closure batch completed: failed aggregate designer execution child metadata, failed editor/toolbox metadata coverage, child execution/error/success counts, failed-child summaries, and failed-child error summaries were closed after current-head code/test evidence review and focused local validation. Error management coverage includes reporting-only fallback metadata on child process failure, category-specific error/success counts, failed-child ids/errors, no stale aggregate success metadata, and preserved successful sibling child state. The closed `#1203`-`#1346` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1334-#1339 evidence-backed closure batch completed: aggregate designer dispatch/execution catalog argument preservation plus execution child command, identity, dispatch-argument, and state JSON were closed after current-head code/test evidence review and focused local validation. Error management coverage includes non-mutating dispatch/execution catalog inspection, no child executor/process/UI launch for catalog paths, preserved no-stale failure behavior, explicit child execution state, and failed-child process status reporting. The closed `#1203`-`#1339` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1332 and #1333 evidence-backed closure batch completed: aggregate designer dispatch execution catalog planning and Studio host JSON exposure were closed after current-head code/test evidence review and focused local validation. Error management coverage includes explicit aggregate execution-admission failures, dry-run/no-admitted-dispatch failures, dispatch-plan error propagation, invalid execution-admission parser errors, non-mutating readiness state, and no child executor/process/UI launch. The closed `#1203`-`#1333` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1325, #1327, #1328, #1329, and #1331 evidence-backed closure batch completed: aggregate designer dispatch execution host JSON, builder dispatch execution catalog host JSON, editor-action dispatch execution catalog planning/host JSON, and toolbox dispatch execution catalog host JSON were closed after current-head code/test evidence review and focused local validation. Error management coverage includes parser failures, explicit execution-admission failures, dry-run dispatch readiness failures, no process launch for readiness catalogs, nonzero child execution summaries, stale metadata suppression, and no asset mutation. The closed `#1203`-`#1331` E2 evidence-audit range is now contiguous.
- 2026-06-21: E2/#1314, #1315, #1317, #1319, #1321, and #1323 evidence-backed closure batch completed: dispatch-sourced toolbox object creation/batch creation host JSON, toolbox palette launch catalog host JSON, and builder/editor-action/toolbox dispatch execution host JSON were closed after current-head code/test evidence review and focused local validation. Error management coverage includes parser failures, admission failures without launch, nonzero process reporting, unavailable dispatch items, duplicate identities, stale metadata suppression, and no partial mutation on rejected create paths. The local issue snapshot also dropped already-closed #1316, #1318, #1320, #1322, #1324, #1326, and #1330 rows.
- 2026-06-21: E2/#1098 evidence-backed closure completed: the host `--picture-object` wrapper is now closed after current-head code/test evidence review confirmed stable selector routing through `set_visual_object_picture`, parser error handling, rollback/no-mutation failure behavior, and focused local validation.
- 2026-06-21: E2/#1305, #1307, #1309, and #1311 evidence-backed closure batch completed: selection-context toolbox object batch create-plan, batch create-dispatch plan, single create execution, and batch create execution host JSON surfaces were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1313` evidence-audit range contiguous after already-closed implementation siblings.
- 2026-06-21: E2/#1300-#1303 evidence-backed closure batch completed: selection-context toolbox object create planning/create-dispatch planning plus host JSON exposure were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1304` evidence-audit range contiguous after already-closed `#1304`.
- 2026-06-21: E2/#1294-#1299 evidence-backed closure batch completed: selection-context toolbox object create-dispatch, batch create-plan, and batch create-dispatch catalog planning plus host JSON exposure were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1299` evidence-audit range contiguous.
- 2026-06-21: E2/#1290-#1293 evidence-backed closure batch completed: selection-context toolbox dispatch catalog planning/host JSON and selection-context toolbox object create-plan catalog planning/host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1293` evidence-audit range contiguous.
- 2026-06-21: E2/#1283-#1289 evidence-backed closure batch completed: editor/designer/toolbox dispatch catalog routing plus toolbox invocation-admission catalog planning/host JSON and selection-context toolbox invocation-admission planning/host JSON are now closed after live-state review, current-head evidence review, and focused local validation, making the closed `#1203`-`#1289` evidence-audit range contiguous.
- 2026-06-21: E2/#1277-#1282 evidence-backed closure batch completed: selection-context builder launch catalogs plus editor-action launch and invocation-admission catalogs, including host JSON exposure, were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1282` evidence-audit range contiguous.
- 2026-06-21: E2/#1271-#1276 evidence-backed closure batch completed: builder invocation-admission/dispatch catalogs plus selection-context invocation-admission/dispatch catalog planning and host JSON exposure were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1276` evidence-audit range contiguous.
- 2026-06-21: E2/#1268-#1270 evidence-backed closure batch completed: builder launch catalog planning, builder launch catalog host JSON, and builder invocation-admission catalog planning are all closed after current-head/live-state review and focused local validation, making the closed `#1203`-`#1270` evidence-audit range contiguous.
- 2026-06-21: E2/#1264-#1267 evidence-backed closure batch completed: toolbox object create/batch dispatch planning from admitted palette dispatch plus host JSON exposure were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1267` evidence-audit range contiguous.
- 2026-06-21: E2/#1260-#1263 evidence-backed closure batch completed: toolbox object creation and batch planning from admitted palette dispatch plus host JSON exposure were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1263` evidence-audit range contiguous.
- 2026-06-21: E2/#1257-#1259 evidence-backed closure batch completed: toolbox object batch create plan catalog planning, host JSON, and coverage documentation were closed after current-head code/docs/test evidence review and focused local validation, making the closed `#1203`-`#1259` evidence-audit range contiguous.
- 2026-06-21: E2/#1253-#1256 evidence-backed closure batch completed: toolbox object create dispatch catalog planning/host JSON and toolbox object batch create dispatch catalog planning/host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1256` evidence-audit range contiguous.
- 2026-06-21: E2/#1250-#1252 evidence-backed closure batch completed: toolbox object create dispatch planning host JSON, toolbox object batch create dispatch planning, and toolbox object batch create dispatch planning host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1252` evidence-audit range contiguous.
- 2026-06-21: E2/#1242, #1244, #1246, and #1248 evidence-backed closure batch completed: toolbox object creation plan/catalog host JSON, toolbox object batch creation planning host JSON, and toolbox object batch creation host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1249` evidence-audit range contiguous after already-closed `#1241`, `#1243`, `#1245`, `#1247`, and `#1249`.
- 2026-06-21: E2/#1238-#1240 evidence-backed closure batch completed: aggregate designer dispatch host JSON, aggregate designer dispatch catalog planning, and aggregate designer dispatch catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1240` evidence-audit range contiguous after already-closed `#1237`.
- 2026-06-21: E2/#1234 and #1236 evidence-backed closure batch completed: toolbox palette dispatch host JSON and toolbox palette dispatch catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1236` evidence-audit range contiguous.
- 2026-06-21: E2/#1230 and #1232 evidence-backed closure batch completed: builder dispatch host JSON and builder dispatch catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1232` evidence-audit range contiguous.
- 2026-06-21: E2/#1226 and #1228 evidence-backed closure batch completed: editor-action dispatch host JSON and editor-action dispatch catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1228` evidence-audit range contiguous.
- 2026-06-21: E2/#1220, #1222, and #1224 evidence-backed closure batch completed: toolbox invocation-admission host JSON, aggregate designer invocation-admission host JSON, and designer invocation-admission catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1224` evidence-audit range contiguous.
- 2026-06-21: E2/#1216 and #1218 evidence-backed closure batch completed: builder invocation-admission host JSON and editor-action invocation-admission host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1219` evidence-audit range contiguous.
- 2026-06-21: E2/#1212 and #1214 evidence-backed closure batch completed: combined designer launch-surface host JSON and designer launch-surface catalog host JSON were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1215` evidence-audit range contiguous.
- 2026-06-21: E2/#1207 and #1209 evidence-backed closure batch completed: selection-context editor action launch planning and selection-context toolbox palette launch planning were closed after current-head code/test evidence review and focused local validation, making the closed `#1203`-`#1210` evidence-audit range contiguous.
- 2026-06-21: E2/#1204, #1206, #1208, and #1210 evidence-backed closure batch completed: Studio builder, selection-context builder, editor-action, and toolbox palette launch-plan host JSON exposure slices were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1192-#1195 evidence-backed closure batch completed: dynamic-font-underline, dynamic-font-strikethru, dynamic-font-outline, and dynamic-font-shadow Studio host wrappers were closed after current-head code/test evidence review and focused local validation, making the closed `#1102`-`#1202` evidence-audit range contiguous.
- 2026-06-21: E2/#1187-#1191 evidence-backed closure batch completed: dynamic-current-control, dynamic-font-name, dynamic-font-size, dynamic-font-bold, and dynamic-font-italic Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1175-#1177 evidence-backed closure batch completed: picture-selection-display, dynamic-input-mask, and dynamic-line-height Studio host wrappers were closed after current-head code/test evidence review and focused local validation, making the closed `#1102`-`#1186` evidence-audit range contiguous.
- 2026-06-21: E2/#1164-#1165 evidence-backed closure batch completed: border-color and link-master Studio host wrappers were closed after current-head code/test evidence review and focused local validation, making the closed `#1102`-`#1174` evidence-audit range contiguous.
- 2026-06-21: E2/#1159-#1163 evidence-backed closure batch completed: half-height-caption, mdi-form, back-style, border-style, and border-width Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1154-#1158 evidence-backed closure batch completed: max-top, min-button, min-height, min-width, and movable Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1149-#1153 evidence-backed closure batch completed: mac-desktop, max-button, max-height, max-width, and max-left Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1144-#1148 evidence-backed closure batch completed: whats-this-button, auto-verb-menu, bind-controls, desktop, and key-preview Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1139-#1143 evidence-backed closure batch completed: tab-orientation, display-orientation, help-context-id, whats-this-help-id, and whats-this-help Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1134-#1138 evidence-backed closure batch completed: fill-color, list-item-id, form-set-class, default-file-path, and initial-selected-alias Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1129-#1133 evidence-backed closure batch completed: record-source-type, record-source, column-order, highlight-style, and child-order Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1124-#1128 evidence-backed closure batch completed: lock-columns-left, grid-line-width, grid-lines, highlight-row-line-width, and partition Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1119-#1123 evidence-backed closure batch completed: data-session, grid-line-color, header-height, row-height, and lock-columns Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1114-#1118 evidence-backed closure batch completed: draw-width, fill-style, scale-mode, buffer-mode, and buffer-mode-override Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1108-#1113 evidence-backed closure batch completed: OLE drop text-insertion, hide-selection, button-count, curvature, draw-mode, and draw-style Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: E2/#1086, #1101, and #1105-#1107 evidence-backed closure batch completed: allow-cell-selection, OLE drag-picture, OLE drag-mode, OLE drop-mode, and OLE drop-effects Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-21: Windows installer build-host portability cleanup shipped. `copperfin_build_host` now reads `COPPERFIN_RUNTIME_HOST_PATH` through `_dupenv_s` on Windows instead of deprecated `getenv`, and the runtime-host fallback path now converts explicitly to `std::string`, addressing the Windows installer MSVC `C4996` and `C2440` failures seen in the previous run.
- 2026-06-21: Native CMake Validation timeout aligned with current Windows MSVC runtime. The native workflow now allows 60 minutes instead of 30 minutes after the latest head showed Linux/macOS native validation passing while the Windows MSVC leg was cancelled at the previous 30-minute job timeout.
- 2026-06-20: E2/#1099-#1100 and #1102-#1104 evidence-backed closure batch completed: down-picture, disabled-picture, mouse-icon, drag-icon, and drag-mode Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1092-#1097 evidence-backed closure batch completed: allow-header-sizing, allow-row-sizing, resizable, add-line-feeds, always-on-top, and always-on-bottom Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1087-#1091 evidence-backed closure batch completed: delete-mark, record-mark, split-bar, highlight-row, and panel-link Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1082-#1085 evidence-backed closure batch completed: dockable, clip-controls, sparse, and lock-screen Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1078-#1081 evidence-backed closure batch completed: auto-center, auto-size, auto-release, and continuous-scroll Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: Windows installer MSVC parser buildability cleanup shipped: `prg_engine_parser.cpp` now splits late file/storage/table-transfer/default statement parsing into helper classifiers, reducing the giant command-dispatch chain that hosted Windows hit as MSVC `C1061` while preserving existing parser behavior. Focused validation passed with parser syntax/object compilation, archive relink, focused PRG engine test relinks, and `test_prg_engine_control_flow`, `test_prg_engine_data_io`, `test_prg_engine_arrays`, `test_prg_engine_file_io_functions`, and `test_prg_engine_table_structure`.
- 2026-06-20: E2/#1166-#1169 evidence-backed closure batch completed: special effect, scroll bars, window state, and show window Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1170-#1174 evidence-backed closure batch completed: title bar, mouse pointer, picture margin, picture position, and picture spacing Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1183-#1186 evidence-backed closure batch completed: font strikethru, outline, shadow, and dynamic-alignment Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1178-#1182 evidence-backed closure batch completed: font name, size, bold, italic, and underline Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1201-#1202 evidence-backed closure batch completed: deleted-states and subtree-deleted-state Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1196-#1200 evidence-backed closure batch completed: column widths, column lines, integral height, incremental search, and multi-select Studio host wrappers were closed after current-head code/test evidence review and focused local validation.
- 2026-06-20: E2/#1406-#1407 evidence-backed closure batch completed: selected-context builder catalog status summaries and selected builder dispatch-execution catalog JSON were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1443-#1447 evidence-backed closure batch completed: visual property reorder/reorder-batch, visual object reparent-batch, property update-batch, and multi-object update-batch Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1439-#1442 evidence-backed closure batch completed: visual property move/move-batch and rename/rename-batch Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1435-#1438 evidence-backed closure batch completed: visual property clear/copy and clear-batch/copy-batch Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1433-#1434 evidence-backed closure batch completed: visual method move-batch and reorder-batch Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1428-#1432 evidence-backed closure batch completed: visual method move/reorder plus delete/rename/copy batch Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1423-#1427 evidence-backed closure batch completed: visual method query, update, delete, rename, and copy Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1419-#1422 evidence-backed closure batch completed: visual object children, descendants, ancestors, and method-list Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1416-#1418 evidence-backed closure batch completed: visual property query, visual property list, and visual object outline Studio host JSON surfaces were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1413-#1415 evidence-backed closure batch completed: menu command editor action propagation, toolbox palette query host JSON, and visual property filter host JSON were closed after current-head evidence review and focused local validation.
- 2026-06-20: E2/#1408-#1412 evidence-backed closure batch completed: selected toolbox dispatch-execution catalog JSON, selected execution usage text, data-environment property-grid actions, toolbox palette filtering, and searchable visual property filtering were closed after focused local validation confirmed the shipped coverage.
- 2026-06-20: E3/#1565 report/label section top clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 2 --property-name VPOS` on FRX/LBX sections refreshes selected section top/bottom geometry and section-relative object metadata without leaking stale section top values.
- 2026-06-20: E3/#1564 report/label section height clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 1 --property-name HEIGHT` on FRX/LBX sections refreshes selected section geometry, section-height totals, and placed/unplaced object counts without leaking stale section height.
- 2026-06-20: E3/#1563 report/label layout-object top-position clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 3 --property-name VPOS` on FRX/LBX layout objects refreshes selected object top/bottom metadata, containing-section placement, and preview bounds without leaking stale geometry.
- 2026-06-20: E3/#1562 report/label layout-object height clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 3 --property-name HEIGHT` on FRX/LBX layout objects refreshes selected object height/bottom-edge metadata without leaking stale geometry.
- 2026-06-20: Windows CI portability cleanup shipped: Studio registry/palette sources now include `<iterator>` for `std::back_inserter`, `test_build_host_output` uses `_dupenv_s` on Windows instead of unsafe `getenv`, and the large Studio/visual-asset test translation units request MSVC `/bigobj`.
- 2026-06-20: E3/#1561 report/label layout-object left-position clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 4 --property-name HPOS` on FRX/LBX layout objects refreshes selected object left/right-edge metadata and preview bounds without leaking stale geometry.
- 2026-06-20: E3/#1560 report/label layout-object width clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 3 --property-name WIDTH` on FRX/LBX layout objects refreshes selected object width/right-edge metadata and preview bounds without leaking stale geometry.
- 2026-06-20: E3/#1559 report/label layout-object font clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 3 --property-name FONTFACE` on FRX/LBX layout objects refreshes selected object JSON with cleared font/highlight metadata while preserving object selection and expression provenance without leaking stale font values.
- 2026-06-20: E3/#1558 report/label layout-object expression clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 3 --property-name EXPR` on FRX/LBX layout objects refreshes selected object JSON with an empty expression, keeps the object selection/section context, and does not leak stale memo-derived expression values.
- 2026-06-20: E3/#1557 report/label column setup memo clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name EXPR` on FRX/LBX root column setup clears memo-derived column setup metadata from refreshed `reportLayout` JSON without leaking stale column count, width, spacing, or setting provenance.
- 2026-06-20: E3/#1556 report/label settings memo clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name EXPR` on FRX/LBX root settings clears memo-derived page setup metadata from refreshed `reportLayout` JSON without leaking stale orientation, paper-size, bottom-margin, or grid values while preserving direct top-margin provenance.
- 2026-06-20: E3/#1555 report/label column-spacing field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name COLSPACING` blanks direct FRX/LBX column setup fields and refreshed `reportLayout` JSON clears column-spacing availability while preserving memo-derived column count/width, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1554 report/label column-width field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name COLWIDTH` blanks direct FRX/LBX column setup fields and refreshed `reportLayout` JSON clears column-width availability while preserving memo-derived column count/spacing, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1553 report/label column-count field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name COLS` blanks direct FRX/LBX column setup fields and refreshed `reportLayout` JSON clears column-count availability while preserving memo-derived column width/spacing, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1552 report/label horizontal-grid field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name GRIDH` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears horizontal-grid availability while preserving memo-derived margins, vertical grid spacing, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1551 report/label vertical-grid field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name GRIDV` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears vertical-grid availability while preserving memo-derived margins, horizontal grid spacing, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1550 report/label bottom-margin field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name BOTMARGIN` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears bottom-margin availability while preserving memo-derived top margin, grid spacing, setting counts, and remaining field provenance.
- 2026-06-20: E3/#1549 report/label top-margin field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name TOPMARGIN` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears top-margin availability while preserving memo-derived orientation, paper size, bottom margin, grid spacing, setting counts, and remaining field provenance.
- 2026-06-20: Documentation/Mermaid status cleanup shipped: `docs/02-architecture.md`, `docs/05-roadmap.md`, `docs/23-phase-a-dependency-breakdown.md`, `docs/24-system-uml.md`, `agent-handoff.md`, and `remaining-work.md` now label E2 as an open evidence-audit queue, preserve live child-state warnings, and say to create the next prompt-sized E3 report/label child instead of a misleading native child before continuing `#24`.
- 2026-06-20: E3/#1548 report/label orientation field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name ORIENTATION` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears orientation availability while preserving memo-derived paper size, margins, grid spacing, setting counts, and field provenance.
- 2026-06-20: E3/#1547 report/label paper-size field clear coverage shipped: Studio host regression coverage now proves `--clear-property --record 0 --property-name PAPERSIZE` blanks direct FRX/LBX page setup fields and refreshed `reportLayout` JSON clears paper-size availability while preserving memo-derived orientation, margins, grid spacing, setting counts, and field provenance.
- 2026-06-20: E3/#1545 report/label paper-size field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name PAPERSIZE` persists direct FRX/LBX page setup field edits and refreshed `reportLayout` JSON updates paper-size metadata while preserving memo-derived orientation, margins, grid spacing, setting counts, and field provenance.
- 2026-06-20: E3/#1544 report/label orientation field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name ORIENTATION` persists direct FRX/LBX page setup field edits and refreshed `reportLayout` JSON updates orientation metadata while preserving memo-derived paper size, margins, grid spacing, setting counts, and field provenance.
- 2026-06-20: E3/#1543 report/label horizontal-grid field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name GRIDH` persists direct FRX/LBX page setup field edits and refreshed `reportLayout` JSON updates horizontal grid metadata while preserving memo-derived margins and vertical grid spacing.
- 2026-06-20: E3/#1542 report/label vertical-grid field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name GRIDV` persists direct FRX/LBX page setup field edits and refreshed `reportLayout` JSON updates vertical grid metadata while preserving memo-derived margins and horizontal grid spacing.
- 2026-06-20: E3/#1541 report/label bottom-margin field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name BOTMARGIN` persists direct FRX/LBX page setup field edits and refreshed `reportLayout` JSON updates bottom margin metadata while preserving memo-derived top margin and grid spacing.
- 2026-06-20: E3/#1540 report/label column-spacing field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name COLSPACING` persists direct FRX/LBX column setup field edits and refreshed `reportLayout` JSON updates column spacing metadata while preserving memo-derived column count and width.
- 2026-06-20: E3/#1539 report/label column-width field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name COLWIDTH` persists direct FRX/LBX column setup field edits and refreshed `reportLayout` JSON updates column width metadata while preserving memo-derived column count and spacing.
- 2026-06-20: E3/#1538 report/label column-count field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name COLS` persists direct FRX/LBX column setup field edits and refreshed `reportLayout` JSON updates column count metadata while preserving memo-derived column width and spacing.
- 2026-06-20: E3/#1537 report/label page-margin field editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name TOPMARGIN` persists direct FRX/LBX page-setup field edits and refreshed `reportLayout` JSON updates top margin metadata while preserving memo-derived page setup fields.
- 2026-06-20: Documentation and Mermaid status diagrams now align with the current issue tree. Roadmap/security notes distinguish open security parents from the closed `#190`-`#193` child queue, system UML interop now reflects launcher-stub/polyglot-artifact maturity, and backlog Mermaid diagrams now use issue-tree status labels instead of stale percentage-heavy execution cues.
- 2026-06-20: Native toolbox-creation catalog palette-order cleanup shipped: batch plan/dispatch catalog tests now assert the actual form toolbox palette order, where `label` takes the first appended target (`before_count`) and `textbox` follows it, preserving generated-name reservation without assuming textbox is first.
- 2026-06-20: E3/#1536 report/label layout-object top preview-bounds coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name VPOS` can move an FRX/LBX object above all live section bands and refreshed `reportLayout` JSON updates selected-object top/bottom metadata, document preview top/height bounds, and unplaced selected-object metadata.
- 2026-06-20: E3/#1535 report/label layout-object height preview-bounds coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name HEIGHT` refreshes FRX/LBX selected-object height/bottom-edge metadata and document preview bottom/height bounds while preserving section membership.
- 2026-06-20: E3/#1534 report/label layout-object left preview-bounds coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name HPOS` refreshes FRX/LBX selected-object left/right-edge metadata and document preview left/width bounds while preserving section membership.
- 2026-06-20: E3/#1533 report/label layout-object width preview-bounds coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name WIDTH` refreshes FRX/LBX selected-object width/right-edge metadata and document preview bounds while preserving section membership.
- 2026-06-20: E3/#1532 report/label layout-object unplacement transition coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name VPOS` can move a section-contained FRX/LBX object outside all live section bands and refreshed `reportLayout` JSON updates placed/unplaced counts, clears selected containing-section availability, and serializes unplaced selected-object metadata.
- 2026-06-20: E3/#1531 report/label layout-object placement transition coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name VPOS` can move an initially unplaced FRX/LBX object into a live section band and refreshed `reportLayout` JSON updates placed/unplaced counts, selected containing-section metadata, section-relative coordinates, and section object counts.
- 2026-06-20: E3/#1530 report/label layout-object font metadata editing coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name FONTFACE` replaces FRX/LBX object font memo payloads, persists the memo value, and refreshes selected `reportLayout` highlight metadata while preserving containing-section context.
- 2026-06-20: E3/#1529 report/label layout-object expression editing coverage shipped: Studio host regression coverage now proves `--set-property --record <object> --property-name EXPR` replaces FRX/LBX object expression memo payloads, persists the memo value, and refreshes selected `reportLayout` object expression, title, provenance, and containing-section metadata.
- 2026-06-20: E3/#1528 report/label column-setup editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name EXPR` replaces FRX/LBX `COLS`/`COLWIDTH`/`COLSPACING` memo payloads, persists the memo value, and refreshes `reportLayout` column setup values plus setting provenance.
- 2026-06-20: E3/#1527 report/label settings-memo editing coverage shipped: Studio host regression coverage now proves `--set-property --record 0 --property-name EXPR` replaces FRX/LBX settings memo payloads, persists the memo value, and refreshes `reportLayout` page setup values plus memo/field setting provenance.
- 2026-06-20: E3/#1526 report/label section-top editing coverage shipped: Studio host regression coverage now proves `--set-property --record <section> --property-name VPOS` persists FRX/LBX section top edits and refreshed `reportLayout` JSON reflects the new section top, bottom edge, section-relative object coordinates, and preserved membership.
- 2026-06-20: E3/#1525 report/label section-height editing coverage shipped: Studio host regression coverage now proves `--set-property --record <section> --property-name HEIGHT` persists FRX/LBX section height edits and refreshed `reportLayout` JSON reflects the new section height, bottom edge, section-height total, and preserved object membership.
- 2026-06-20: E3/#1524 report/label deleted preview bounds JSON shipped: `StudioReportLayoutSnapshot` now derives deleted layout bounds from deleted sections and deleted objects, and `copperfin_studio_host --json` exposes deleted preview availability, edges, width, and height under `reportLayout` without changing live preview bounds.
- 2026-06-20: E3/#1523 report/label deleted placement summary JSON shipped: `StudioReportLayoutSnapshot` now derives deleted objects that still fall inside report/label section bands separately from deleted objects outside all bands, and `copperfin_studio_host --json` exposes `deletedPlacedObjectCount` and `deletedUnplacedObjectCount` under `reportLayout` without changing detailed object payloads.
- 2026-06-20: E3/#1522 report/label placed-object summary JSON shipped: `StudioReportLayoutSnapshot` now derives the live section-contained object count, and `copperfin_studio_host --json` exposes `placedObjectCount` under `reportLayout` while preserving existing live/unplaced/deleted object surfaces.
- 2026-06-20: E3/#1521 report/label section-height summary JSON shipped: `StudioReportLayoutSnapshot` now derives live and deleted section height totals from existing section snapshots, and `copperfin_studio_host --json` exposes them under `reportLayout` without changing section mutation semantics.
- 2026-06-20: Native toolbox-creation planner assertion cleanup shipped: batch planning/catalog tests now expect the first reserved target record to equal the append point (`before_count`) and later targets to advance from there, matching the execution assertions and implementation semantics.
- 2026-06-20: Native macOS toolbox-creation fixture cleanup shipped: `test_toolbox_creation` now gives its synthetic `UNIQUEID` field enough width for the GUID-style identifiers asserted by the creation/batch/dispatch tests, avoiding platform-dependent failures when fresh hosted builds enforce DBF character-field width.
- 2026-06-20: Native macOS Studio host execution-test portability cleanup shipped: `test_studio_host_json` now uses platform-available success/failure launch commands (`/usr/bin/true` and `/usr/bin/false` on macOS, existing `/bin/...` paths elsewhere) while keeping exact JSON command assertions synchronized with the chosen path.
- 2026-06-20: E2/#1283, #1284, and #1287 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped editor-action, designer, and toolbox dispatch-catalog routing through shared invocation-admission catalog seams. Parent #23 remains open for remaining E2 children.
- 2026-06-20: E2/#1233, #1235, #1237, #1268, and #1270 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped toolbox dispatch, aggregate designer dispatch, builder launch catalog, and builder invocation-admission catalog coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: Native macOS FLL export inspection follow-up shipped: Mach-O symbol normalization now preserves the VFP FLL `_FoxTable` registration symbol after `nm` reports it as `__FoxTable`, while still filtering unrelated residual underscore-prefixed symbols.
- 2026-06-20: E2/#1225, #1227, #1229, and #1231 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped editor-action and builder invocation dispatch planning plus dispatch catalog coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: E2/#1215, #1217, #1219, #1221, and #1223 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped builder, editor-action, toolbox, aggregate designer, and catalog invocation-admission planning coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: E2/#1203, #1205, #1211, and #1213 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped Studio builder launch planning, selection-context builder planning, combined designer launch-surface planning, and launch-surface catalog coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: Native macOS CI harness follow-up shipped: runtime-host binding temp roots now strip trailing separators before constructing expected rebound paths, and macOS native export inspection now ignores residual underscore-prefixed Mach-O/C++ symbols after C-symbol normalization.
- 2026-06-20: Native macOS CI harness portability cleanup shipped: runtime-host binding checks now match rebound package paths literally instead of assuming same-line key/value output, native export inspection uses macOS `nm -gU` with underscore normalization, and the remaining runtime-pipeline dotnet compile-check child no longer closes stdout/stderr before opening its build log.
- 2026-06-20: E2/#1241, #1243, #1245, #1247, and #1249 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped toolbox object planning, catalog, batch planning, batch creation, and create-dispatch planning coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: E2/#1324, #1326, and #1330 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped aggregate designer dispatch execution seam plus builder/toolbox dispatch execution catalog coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: E2/#1316, #1318, #1320, and #1322 were closed in GitHub after existing implementation comments and fresh focused validation confirmed their shipped toolbox palette launch catalog plus builder/editor/toolbox dispatch execution seam coverage. Parent #23 remains open for remaining E2 children.
- 2026-06-20: Installer CI portability cleanup shipped: Windows native installer builds now read the DBF atomic-write failure-injection environment variables through a small `_dupenv_s`-backed helper instead of triggering MSVC `getenv` warning-as-error failures, and `vs_launch_contract.cpp` now parses floating-point launch arguments with `strtod` instead of `std::from_chars(double)` so hosted macOS libc++ can compile the Studio host path.
- 2026-06-20: Native CI runtime-pipeline harness cleanup shipped: the C# transpilation compile-check child process now opens its dotnet build log before redirecting stdout/stderr, avoiding bad file descriptors if log creation fails under hosted CTest capture.
- 2026-06-20: E2/#1304, #1306, #1308, #1310, #1312, and #1313 were closed in GitHub after evidence-backed local verification confirmed their shipped `test_toolbox_creation` coverage and repo docs; parent #23 remains open for remaining designer/toolbox children.
- 2026-06-19: CI buildability cleanup shipped: the GNU-only `vs_launch_contract.cpp` debug compile options now probe `-fzero-init-padding-bits=standard` before using it so older hosted GCC builds can compile, and the VSIX sources no longer depend on ambiguous `IServiceProvider` resolution, `record struct`/`IsExternalInit`, C# range/index syntax, or newer `string.Split` overloads under the `net472` Visual Studio extension target.
- 2026-06-19: E3/#1520 report/label section-kind summary JSON shipped: `StudioReportLayoutSnapshot` now derives compact live and deleted section band-kind count buckets, and `copperfin_studio_host --json` exposes those summaries for `.frx` and `.lbx` designer surfaces without changing existing section/object/settings payloads.
- 2026-06-19: E3/#1519 report/label object-kind summary JSON shipped: `StudioReportLayoutSnapshot` now derives compact live, unplaced, and deleted object-kind count buckets, and `copperfin_studio_host --json` exposes those summaries for `.frx` and `.lbx` designer surfaces without changing the existing section/object/settings payloads.
- 2026-06-19: E2/#1448-#1451 evidence-backed issue cleanup completed: duplicate-batch, rename-batch, reorder-batch, and subtree-duplicate Studio host JSON slices were closed in GitHub after their implementation commits, focused `test_studio_host_json` coverage, and validation evidence were verified; planning docs now reflect the closed live issue state while keeping parent `#23` open.
- 2026-06-19: E3/#1518 report/label column setup summary JSON shipped: `StudioReportLayoutSnapshot` now derives compact non-mutating column setup fields from live report/label settings, and `copperfin_studio_host --json` exposes column count, column width, and column spacing for `.frx` and `.lbx` designer surfaces while preserving provenance-rich settings arrays.
- 2026-06-19: E3/#1517 report/label page setup summary JSON shipped: `StudioReportLayoutSnapshot` now derives compact non-mutating page setup fields from live report/label settings, and `copperfin_studio_host --json` exposes orientation, paper size, margins, and grid spacing for `.frx` and `.lbx` designer surfaces while preserving provenance-rich settings arrays.
- 2026-06-19: E3/#1516 report/label layout preview-bounds JSON shipped: `StudioReportLayoutSnapshot` now derives non-mutating preview bounds and live object counts from live sections plus live placed/unplaced layout objects, and `copperfin_studio_host --json` exposes the metrics for `.frx` and `.lbx` designer surfaces.
- 2026-06-19: E3/#1515 selected deleted report settings no-section/object JSON coverage shipped: Studio host regression coverage now proves selected deleted `.frx` report settings rows preserve deleted-settings selection while explicitly clearing selected-section, selected-object, and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1514 selected report settings no-section/object JSON coverage shipped: Studio host regression coverage now proves selected live `.frx` report settings rows preserve settings selection while explicitly clearing selected-section, selected-object, and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1513 selected deleted report section no-object/settings JSON coverage shipped: Studio host regression coverage now proves selected deleted `.frx` report section rows preserve deleted-section selection while explicitly clearing selected-object, containing-object-section, and selected-settings JSON surfaces.
- 2026-06-19: E3/#1512 selected report section no-object/settings JSON coverage shipped: Studio host regression coverage now proves selected live `.frx` report section rows preserve section selection while explicitly clearing selected-object, containing-object-section, and selected-settings JSON surfaces.
- 2026-06-19: E3/#1511 selected unplaced report object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected live unplaced `.frx` report layout object rows preserve unplaced-object selection and null containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1510 selected deleted report object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected deleted `.frx` report layout object rows preserve deleted-object selection and null containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1509 selected report object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected live `.frx` report layout object rows preserve object selection and containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1508 selected unplaced label object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected live unplaced `.lbx` label layout object rows preserve unplaced object selection and null containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1507 selected deleted label object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected deleted `.lbx` label layout object rows preserve deleted-object selection and null containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1506 selected label object no-section/settings JSON coverage shipped: Studio host regression coverage now proves selected live `.lbx` label layout object rows preserve object selection and containing-section metadata while explicitly clearing selected-section and selected-settings JSON surfaces.
- 2026-06-19: E3/#1505 selected deleted label settings no-section/object JSON coverage shipped: Studio host regression coverage now proves selected deleted `.lbx` label settings rows preserve deleted-settings selection while explicitly clearing selected-section, selected-object, and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1504 selected label settings no-section/object JSON coverage shipped: Studio host regression coverage now proves selected `.lbx` label settings rows preserve settings selection while explicitly clearing selected-section, selected-object, and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1503 selected deleted label section no-object selection JSON coverage shipped: Studio host regression coverage now proves selected deleted `.lbx` label section rows preserve deleted-section selection while explicitly clearing selected-object and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1502 selected label section no-object selection JSON coverage shipped: Studio host regression coverage now proves selected `.lbx` label section rows preserve label identity and section selection while explicitly clearing selected-object and containing-object-section JSON surfaces.
- 2026-06-19: E3/#1501 unselected label layout summary JSON parity coverage shipped: Studio host regression coverage now proves `.lbx` label layout launches without selected records still expose shared report-layout summary JSON, label identity, live settings/sections, deleted/unplaced object counts, setting provenance, and explicit no-selection metadata.
- 2026-06-19: E3/#1500 selected deleted label object JSON parity coverage shipped: Studio host regression coverage now proves pre-existing deleted `.lbx` label layout object rows still expose selected object JSON, object selection kind/availability, deleted-object counts, and explicit null containing-section metadata while preserving `isLabel`.
- 2026-06-19: E3/#1499 selected unplaced label object JSON parity coverage shipped: Studio host regression coverage now proves live unplaced `.lbx` label layout object rows still expose selected object JSON, object selection kind/availability, unplaced-object counts, and explicit null containing-section metadata while preserving `isLabel`.
- 2026-06-19: E3/#1498 selected deleted label section JSON parity coverage shipped: Studio host regression coverage now proves deleted `.lbx` label section rows still expose selected section JSON, section selection kind/availability, deleted-section counts/provenance, and unplaced-object semantics while preserving `isLabel`.
- 2026-06-19: E3/#1497 selected deleted label settings JSON parity coverage shipped: Studio host regression coverage now proves deleted `.lbx` label root/settings rows still expose `selectedReportSettings`, selected settings kind/availability, deleted-setting counts/provenance, and section/object metadata while preserving `isLabel`.
- 2026-06-19: E3/#1496 selected label settings JSON parity coverage shipped: Studio host regression coverage now proves `.lbx` label root/settings rows expose `selectedReportSettings` through the shared report-layout selection JSON surface, preserving `isLabel`, settings provenance, section metadata, and layout-object metadata.
- 2026-06-19: E3/#1495 label settings restore mutation coverage shipped: Studio host regression coverage now proves deleted `.lbx` label root/settings rows can be restored by record selection through `--restore-object`, clearing the LBX delete flag, restoring live/deleted settings counts, keeping selected settings JSON available, and preserving section/object metadata while preserving `isLabel`.
- 2026-06-19: E3/#1494 label settings delete-state mutation coverage shipped: Studio host regression coverage now proves `.lbx` label root/settings rows can be marked deleted by record selection through `--delete-object`, preserving `isLabel`, moving settings into deleted-setting metadata, keeping selected deleted-settings JSON available, and preserving section/object metadata.
- 2026-06-19: E3/#1493 label section restore mutation coverage shipped: Studio host regression coverage now proves deleted `.lbx` label section rows can be restored by record selection through `--restore-object`, clearing the LBX delete flag, restoring live/deleted section counts, returning former unplaced objects to section membership, and preserving `isLabel`.
- 2026-06-19: E3/#1492 label section delete-state mutation coverage shipped: Studio host regression coverage now proves `.lbx` label section rows can be marked deleted by record selection through `--delete-object`, preserving `isLabel`, moving the section into deleted-section metadata, keeping selected deleted-section JSON available, and leaving former section objects unplaced without fabricated containing-section metadata.
- 2026-06-19: E3/#1491 label layout object rename identity mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout object `UNIQUEID` values can be renamed by stable selector through `--rename-object`, preserving object count/order while replacing the identity and refreshing live section membership JSON while preserving `isLabel`.
- 2026-06-19: E3/#1490 label layout object duplicate mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be duplicated by stable selector through `--duplicate-object`, append a copied LBX object with the requested replacement `UNIQUEID`, and refresh live section membership/index/count JSON while preserving `isLabel`.
- 2026-06-19: E3/#1489 label layout object reorder mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be reordered by stable selector through `--reorder-object`, persist the new LBX physical object order, and refresh section object index/count JSON while preserving `isLabel`.
- 2026-06-19: E3/#1488 label layout object distribution mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be distributed horizontally by stable selectors through `--distribute-object`, mutate the middle LBX `HPOS`, preserve vertical geometry and size, and refresh label report-layout JSON while preserving `isLabel`.
- 2026-06-19: E3/#1487 label layout object restore mutation coverage shipped: Studio host regression coverage now proves deleted `.lbx` label layout objects can be restored by stable selector through `--restore-object`, clear the LBX delete flag, move back into live section membership, and expose containing-section metadata while preserving `isLabel`.
- 2026-06-19: E3/#1486 label layout object delete-state mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be marked deleted by stable selector through `--delete-object`, persist the LBX delete flag, move into deleted-object metadata, and keep selected deleted-object JSON available while preserving `isLabel`.
- 2026-06-19: E3/#1485 label layout object snap-to-grid mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be snapped by stable selector through `--snap-object --snap-mode both`, mutating `HPOS`/`VPOS`, preserving size, and refreshing label report-layout JSON geometry while preserving `isLabel`.
- 2026-06-19: E3/#1484 label layout object resize mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be resized by stable selector through `--resize-object --resize-mode size`, mutating `WIDTH`/`HEIGHT`, preserving position, and refreshing label report-layout JSON spans while preserving `isLabel`.
- 2026-06-19: E3/#1483 label layout object alignment mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be aligned by stable selector through `--align-object --alignment-mode left`, mutating `HPOS`, preserving vertical geometry, and refreshing label report-layout JSON while preserving `isLabel`.
- 2026-06-19: E3/#1482 label layout object nudge mutation coverage shipped: Studio host regression coverage now proves `.lbx` label layout objects can be nudged by stable selector through `--nudge-object`, mutating `HPOS`/`VPOS` and refreshing label report-layout JSON geometry while preserving `isLabel`.
- 2026-06-19: E3/#1481 label layout JSON parity coverage shipped: Studio host regression coverage now proves synthetic `.lbx` label assets expose `reportLayout.isLabel`, live settings/sections/objects, selected object metadata, and selected section metadata through the same report-layout JSON surface as `.frx` reports.
- 2026-06-19: E3/#1480 unplaced report object selection JSON coverage shipped: Studio host regression coverage now proves `--json --record <layout-object-record>` classifies live unplaced report/label layout objects as `object`, exposes selected-object metadata, and keeps containing-section metadata explicitly absent.
- 2026-06-19: E3/#1479 deleted report object selection JSON coverage shipped: Studio host regression coverage now proves `--json --record <layout-object-record>` still classifies DBF-deleted report/label layout objects as `object`, exposes deleted selected-object metadata, and keeps containing-section metadata explicitly absent.
- 2026-06-19: E3/#1478 deleted report section selection JSON coverage shipped: Studio host regression coverage now proves `--json --record <section-record>` still classifies DBF-deleted report/label sections as `section`, exposes deleted selected-section metadata, and keeps live/deleted section counts distinct.
- 2026-06-19: E3/#1477 deleted report settings selection JSON coverage shipped: Studio host regression coverage now proves `--json --record <settings-record>` still classifies DBF-deleted report/label root settings as `settings`, exposes selected settings provenance, and keeps live/deleted settings counts distinct.
- 2026-06-19: E3/#1476 report settings restore mutation coverage shipped: Studio host regression coverage now proves a deleted report/label root settings row can be restored by record selection through `--restore-object`, clears the FRX delete flag, and refreshes report-layout JSON back to live settings metadata while preserving sections and layout objects.
- 2026-06-19: E3/#1475 report settings delete-state mutation coverage shipped: Studio host regression coverage now proves a report/label root settings row can be deleted by record selection through `--delete-object`, persists the FRX delete flag, and refreshes report-layout JSON into deleted-settings metadata while preserving sections and layout objects.
- 2026-06-19: E3/#1474 report section restore mutation coverage shipped: Studio host regression coverage now proves a deleted report/label section row can be restored by record selection through `--restore-object`, clears the FRX delete flag, and refreshes report-layout JSON back to live section metadata with object membership restored.
- 2026-06-19: E3/#1473 report section delete-state mutation coverage shipped: Studio host regression coverage now proves a report/label section row can be deleted by record selection through `--delete-object`, persists the FRX delete flag, and refreshes report-layout JSON into deleted-section metadata with former section objects unplaced.
- 2026-06-19: E3/#1472 report layout object rename identity mutation coverage shipped: Studio host regression coverage now proves `--rename-object` can update FRX report layout object `UNIQUEID` values by stable selector while preserving physical record order and live report-layout section membership.
- 2026-06-19: E3/#1471 report layout object duplicate mutation coverage shipped: Studio host regression coverage now proves `--duplicate-object` can duplicate FRX report layout objects by stable selectors, persist replacement `UNIQUEID` values, and refresh report-layout JSON section membership for the appended copy.
- 2026-06-19: E3/#1470 report layout object reorder mutation coverage shipped: Studio host regression coverage now proves `--reorder-object --placement before` can reorder FRX report layout objects by stable selectors and that report-layout JSON reflects the updated physical record order through section object indexes.
- 2026-06-19: E3/#1469 report layout object distribution mutation coverage shipped: Studio host regression coverage now proves `--distribute-object --distribution-mode horizontal` can redistribute FRX report layout object `HPOS` via stable selectors and that report-layout JSON reflects updated coordinates and section object metadata.
- 2026-06-19: E3/#1468 report layout object restore mutation coverage shipped: Studio host regression coverage now proves `--restore-object` can restore deleted FRX report layout objects via stable selectors and that report-layout JSON moves them back into live section membership.
- 2026-06-19: E3/#1467 report layout object delete-state mutation coverage shipped: Studio host regression coverage now proves `--delete-object` can mark FRX report layout objects deleted via stable selectors and that report-layout JSON moves them out of live section membership while preserving selected-object metadata.
- 2026-06-19: E3/#1466 report layout object snap-to-grid mutation coverage shipped: Studio host regression coverage now proves `--snap-object --snap-mode both` can mutate FRX report layout object `HPOS`/`VPOS` via stable selectors and that report-layout JSON reflects updated coordinates and spans.
- 2026-06-19: E3/#1465 report layout object resize mutation coverage shipped: Studio host regression coverage now proves `--resize-object --resize-mode size` can mutate FRX report layout object `WIDTH`/`HEIGHT` via stable selectors and that report-layout JSON reflects updated object spans.
- 2026-06-19: E3/#1464 report layout object alignment mutation coverage shipped: Studio host regression coverage now proves `--align-object --alignment-mode left` can mutate FRX report layout object `HPOS` via stable selectors and that report-layout JSON reflects updated horizontal geometry.
- 2026-06-19: E3/#1463 report layout object nudge mutation coverage shipped: Studio host regression coverage now proves `--nudge-object` can mutate FRX report layout object `HPOS`/`VPOS` via stable selectors and that report-layout JSON reflects updated absolute, edge, and section-relative geometry.
- 2026-06-19: E3/#1462 report layout right-edge metadata shipped: report/label layout objects now carry explicit right-edge coordinates through the core model and Studio host JSON.
- 2026-06-19: E3/#1461 report layout bottom-edge metadata shipped: report/label sections and layout objects now carry explicit bottom-edge coordinates, including section-relative object bottoms, through the core model and Studio host JSON.
- 2026-06-19: E3/#1460 report section order metadata shipped: `StudioReportSectionSnapshot` and `copperfin_studio_host --json` now expose zero-based section order and live section count for report/label sections.
- 2026-06-19: E3/#1459 report object section-order metadata shipped: `StudioLayoutObjectSnapshot` and `copperfin_studio_host --json` now expose zero-based object order and containing section object count for section-contained report/label objects.
- 2026-06-19: E3/#1458 report object containing-section metadata shipped: `StudioLayoutObjectSnapshot` and `copperfin_studio_host --json` now expose containing section id, containing section record index, and section-relative top for section-contained report/label objects.
- 2026-06-19: E3/#1457 report-selection kind JSON shipped: `copperfin_studio_host --json` now emits `selectedReportSelectionAvailable` and `selectedReportSelectionKind` (`settings`, `section`, `object`, or `none`) for report/label designer selections.
- 2026-06-19: E3/#1456 selected report-settings JSON shipped: `copperfin_studio_host --json --record <root-settings-record>` now emits `selectedReportSettingsAvailable` and provenance-rich `selectedReportSettings` for report/label root settings.
- 2026-06-19: E3/#1455 selected report-object containing-section JSON shipped: `copperfin_studio_host --json --record <layout-object-record>` now emits `selectedReportObjectSectionAvailable` and a provenance-rich `selectedReportObjectSection` for section-contained report/label layout objects.
- 2026-06-19: E2/#1408 selected toolbox dispatch-execution catalog JSON shipped: `copperfin_studio_host --selection-toolbox-dispatch-execution-catalog --json` now exposes selected-context toolbox execution-readiness planning with compact `executionReadyItemIds`, `executionBlockedItemIds`, and `executionBlockedErrors` beside detailed toolbox item/readiness metadata.
- 2026-06-19: E2/#1407 selected builder dispatch-execution catalog JSON shipped: `copperfin_studio_host --selection-builder-dispatch-execution-catalog --json` now exposes selected-context builder execution-readiness planning with compact `executionReadyBuilderIds`, `executionBlockedBuilderIds`, and `executionBlockedErrors` beside detailed launch/admission/dispatch readiness entries.
- 2026-06-19: E2/#1406 Studio host selection builder catalog status summaries shipped: `copperfin_studio_host --selection-builder-launch-catalog --json`, `--selection-builder-invocation-admission-catalog --json`, and `--selection-builder-dispatch-catalog --json` now emit compact ready/blocked builder-id and error arrays beside existing detailed selected-context builder catalog metadata.
- 2026-06-19: E2/#1405 Studio host toolbox create-plan summaries shipped: `copperfin_studio_host --toolbox-create-plan ... --json` and `--selection-toolbox-create-plan ... --json` success output now emits `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing direct single create-plan metadata.
- 2026-06-19: E2/#1404 Studio host toolbox create-batch plan summaries shipped: `copperfin_studio_host --toolbox-create-batch-plan ... --json` and `--selection-toolbox-create-batch-plan ... --json` now emit `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing direct batch create-plan metadata.
- 2026-06-19: E2/#1403 Studio host toolbox palette launch-plan summaries shipped: `copperfin_studio_host --toolbox-palette-launch-plan ... --json` success output now emits `launchReadySelectionContexts`, `launchBlockedSelectionContexts`, and `launchBlockedErrors` beside existing direct toolbox palette launch-plan metadata.
- 2026-06-19: E2/#1402 Studio host designer execution summaries shipped: `copperfin_studio_host --designer-execute ... --json` success-result output now emits `executionReadySelectionContexts`, `executionBlockedSelectionContexts`, and `executionBlockedErrors` beside existing direct aggregate designer execution metadata.
- 2026-06-19: E2/#1401 Studio host designer dispatch summaries shipped: `copperfin_studio_host --designer-dispatch ... --json` success output now emits `dispatchOkSelectionContexts`, `dispatchBlockedSelectionContexts`, and `dispatchBlockedErrors` beside existing direct aggregate designer dispatch metadata.
- 2026-06-19: E2/#1400 Studio host designer invocation-admission summaries shipped: `copperfin_studio_host --designer-invocation-admission ... --json` success output now emits `admissionOkSelectionContexts`, `admissionBlockedSelectionContexts`, and `admissionBlockedErrors` beside existing direct aggregate designer invocation-admission metadata.
- 2026-06-19: E2/#1399 Studio host designer launch-surface summaries shipped: `copperfin_studio_host --designer-launch-surfaces ... --json` success output now emits `launchReadySelectionContexts`, `launchBlockedSelectionContexts`, and `launchBlockedErrors` beside existing direct aggregate designer launch-surface metadata.
- 2026-06-19: E2/#1398 Studio host editor action execution summaries shipped: `copperfin_studio_host --editor-action-execute ... --json` success output now emits `executionReadyActionIds`, `executionBlockedActionIds`, and `executionBlockedErrors` beside existing direct editor-action execution metadata.
- 2026-06-19: E2/#1397 Studio host editor action dispatch summaries shipped: `copperfin_studio_host --editor-action-dispatch ... --json` success output now emits `dispatchReadyActionIds`, `dispatchBlockedActionIds`, and `dispatchBlockedErrors` beside existing direct editor-action dispatch metadata.
- 2026-06-19: E2/#1396 Studio host editor action invocation-admission summaries shipped: `copperfin_studio_host --editor-action-invocation-admission ... --json` success output now emits `admissionReadyActionIds`, `admissionBlockedActionIds`, and `admissionBlockedErrors` beside existing direct editor-action invocation-admission metadata.
- 2026-06-19: E2/#1395 Studio host editor action launch-plan summaries shipped: `copperfin_studio_host --editor-action-launch-plan ... --json` success output now emits `launchReadyActionIds`, `launchBlockedActionIds`, and `launchBlockedErrors` beside existing direct editor-action launch-plan metadata.
- 2026-06-19: E2/#1394 Studio host builder execution summaries shipped: `copperfin_studio_host --builder-execute ... --json` success output now emits `executionReadyBuilderIds`, `executionBlockedBuilderIds`, and `executionBlockedErrors` beside existing direct builder execution metadata.
- 2026-06-19: E2/#1393 Studio host builder dispatch summaries shipped: `copperfin_studio_host --builder-dispatch ... --json` success output now emits `dispatchReadyBuilderIds`, `dispatchBlockedBuilderIds`, and `dispatchBlockedErrors` beside existing direct builder dispatch metadata.
- 2026-06-19: E2/#1392 Studio host builder invocation-admission summaries shipped: `copperfin_studio_host --builder-invocation-admission ... --json` success output now emits `admissionReadyBuilderIds`, `admissionBlockedBuilderIds`, and `admissionBlockedErrors` beside existing direct builder invocation-admission metadata.
- 2026-06-19: E2/#1391 Studio host builder launch-plan summaries shipped: `copperfin_studio_host --builder-launch-plan ... --json` success output now emits `launchReadyBuilderIds`, `launchBlockedBuilderIds`, and `launchBlockedErrors` beside existing direct builder launch-plan metadata.
- 2026-06-19: E2/#1390 Studio host toolbox execution summaries shipped: `copperfin_studio_host --toolbox-execute --json` success output now emits `executionReadyItemIds`, `executionBlockedItemIds`, and `executionBlockedErrors` beside existing direct toolbox execution metadata.
- 2026-06-19: E2/#1389 Studio host toolbox dispatch summaries shipped: `copperfin_studio_host --toolbox-dispatch --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing direct toolbox dispatch metadata.
- 2026-06-19: E2/#1388 Studio host toolbox invocation-admission summaries shipped: `copperfin_studio_host --toolbox-invocation-admission --json` now emits `admissionReadyItemIds`, `admissionBlockedItemIds`, and `admissionBlockedErrors` beside existing direct toolbox palette invocation-admission metadata.
- 2026-06-19: E2/#1387 Studio host toolbox batch create-dispatch plan summaries shipped: `copperfin_studio_host --toolbox-create-batch-dispatch-plan ... --json` and `--selection-toolbox-create-batch-dispatch-plan ... --json` now emit `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing non-catalog toolbox object batch create-dispatch metadata.
- 2026-06-19: E2/#1386 Studio host toolbox create-dispatch plan summaries shipped: `copperfin_studio_host --toolbox-create-dispatch-plan ... --json` and `--selection-toolbox-create-dispatch-plan ... --json` now emit `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing non-catalog toolbox object create-dispatch metadata.
- 2026-06-19: E2/#1385 Studio host toolbox batch create-from-dispatch result summaries shipped: `copperfin_studio_host --toolbox-create-batch-from-dispatch ... --json` now emits `createdObjectNames`, `createdUniqueIds`, and `createErrors` beside existing dispatch-sourced toolbox object batch-create metadata.
- 2026-06-19: E2/#1384 Studio host toolbox create-from-dispatch result summaries shipped: `copperfin_studio_host --toolbox-create-from-dispatch ... --json` now emits `createdObjectNames`, `createdUniqueIds`, and `createErrors` beside existing dispatch-sourced toolbox object create metadata.
- 2026-06-19: E2/#1383 Studio host toolbox batch-create result summaries shipped: `copperfin_studio_host --toolbox-create-batch ... --json` and `--selection-toolbox-create-batch ... --json` now emit `createdObjectNames`, `createdUniqueIds`, and `createErrors` beside existing direct toolbox object batch-create metadata.
- 2026-06-19: E2/#1382 Studio host toolbox create result summaries shipped: `copperfin_studio_host --toolbox-create ... --json` and `--selection-toolbox-create ... --json` now emit `createdObjectNames`, `createdUniqueIds`, and `createErrors` beside existing direct toolbox object create metadata.
- 2026-06-19: E2/#1381 Studio host selection toolbox create-batch-dispatch catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-create-batch-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed selected-context toolbox object batch create-dispatch catalog metadata.
- 2026-06-19: E2/#1380 Studio host toolbox create-batch-dispatch catalog status summaries shipped: `copperfin_studio_host --toolbox-create-batch-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed toolbox object batch create-dispatch catalog metadata.
- 2026-06-19: E2/#1379 Studio host selection toolbox create-batch-plan catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-create-batch-plan-catalog --json` now emits `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing detailed selected-context toolbox object batch create-plan catalog metadata.
- 2026-06-19: E2/#1378 Studio host toolbox create-batch-plan catalog status summaries shipped: `copperfin_studio_host --toolbox-create-batch-plan-catalog --json` now emits `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing detailed toolbox object batch create-plan catalog metadata.
- 2026-06-19: E2/#1377 Studio host selection toolbox create-dispatch catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-create-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed selected-context toolbox object create-dispatch catalog metadata.
- 2026-06-19: E2/#1376 Studio host toolbox create-dispatch catalog status summaries shipped: `copperfin_studio_host --toolbox-create-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed toolbox object create-dispatch catalog metadata.
- 2026-06-19: E2/#1375 Studio host selection toolbox create-plan catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-create-plan-catalog --json` now emits `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing detailed selected-context toolbox object create-plan catalog metadata.
- 2026-06-19: E2/#1374 Studio host toolbox create-plan catalog status summaries shipped: `copperfin_studio_host --toolbox-create-plan-catalog --json` now emits `planReadyItemIds`, `planBlockedItemIds`, and `planBlockedErrors` beside existing detailed toolbox object create-plan catalog metadata.
- 2026-06-19: E2/#1373 Studio host selection toolbox invocation-admission catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-invocation-admission-catalog --json` now emits `admissionReadyItemIds`, `admissionBlockedItemIds`, and `admissionBlockedErrors` beside existing detailed selected-context toolbox invocation-admission metadata.
- 2026-06-19: E2/#1372 Studio host selection toolbox dispatch catalog status summaries shipped: `copperfin_studio_host --selection-toolbox-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed selection toolbox dispatch metadata.
- 2026-06-19: E2/#1371 Studio host toolbox dispatch execution catalog readiness summaries shipped: `copperfin_studio_host --toolbox-dispatch-execution-catalog --json` now emits `executionReadyItemIds`, `executionBlockedItemIds`, and `executionBlockedErrors` beside existing detailed per-item dispatch-execution metadata.
- 2026-06-19: E2/#1370 Studio host toolbox dispatch catalog status summaries shipped: `copperfin_studio_host --toolbox-dispatch-catalog --json` now emits `dispatchReadyItemIds`, `dispatchBlockedItemIds`, and `dispatchBlockedErrors` beside existing detailed toolbox dispatch metadata.
- 2026-06-19: E2/#1369 Studio host toolbox invocation-admission catalog status summaries shipped: `copperfin_studio_host --toolbox-invocation-admission-catalog --json` now emits `admissionReadyItemIds`, `admissionBlockedItemIds`, and `admissionBlockedErrors` beside existing detailed toolbox item and palette invocation-admission metadata.
- 2026-06-19: E2/#1368 Studio host toolbox palette launch catalog status summaries shipped: `copperfin_studio_host --toolbox-palette-launch-catalog --json` now emits `launchReadySelectionContexts`, `launchBlockedSelectionContexts`, and `launchBlockedErrors` beside existing detailed per-context toolbox palette launch entries.
- 2026-06-19: E2/#1367 Studio host editor action dispatch execution catalog readiness summaries shipped: `copperfin_studio_host --editor-action-dispatch-execution-catalog --json` now emits `executionReadyActionIds`, `executionBlockedActionIds`, and `executionBlockedErrors` beside existing detailed per-action execution-readiness entries.
- 2026-06-19: E2/#1366 Studio host editor action dispatch catalog status summaries shipped: `copperfin_studio_host --editor-action-dispatch-catalog --json` now emits `dispatchReadyActionIds`, `dispatchBlockedActionIds`, and `dispatchBlockedErrors` beside existing detailed per-action dispatch entries.
- 2026-06-19: E2/#1365 Studio host editor action invocation-admission catalog status summaries shipped: `copperfin_studio_host --editor-action-invocation-admission-catalog --json` now emits `admissionReadyActionIds`, `admissionBlockedActionIds`, and `admissionBlockedErrors` beside existing detailed per-action invocation-admission entries.
- 2026-06-19: E2/#1364 Studio host editor action launch catalog status summaries shipped: `copperfin_studio_host --editor-action-launch-catalog --json` now emits `launchReadyActionIds`, `launchBlockedActionIds`, and `launchBlockedErrors` beside existing detailed per-action launch entries.
- 2026-06-19: E2/#1363 Studio host builder dispatch execution catalog readiness summaries shipped: `copperfin_studio_host --builder-dispatch-execution-catalog --json` now emits `executionReadyBuilderIds`, `executionBlockedBuilderIds`, and `executionBlockedErrors` beside existing detailed per-builder execution-readiness entries.
- 2026-06-19: E2/#1362 Studio host builder dispatch catalog status summaries shipped: `copperfin_studio_host --builder-dispatch-catalog --json` now emits `dispatchReadyBuilderIds`, `dispatchBlockedBuilderIds`, and `dispatchBlockedErrors` beside existing detailed per-builder dispatch entries.
- 2026-06-19: E2/#1361 Studio host builder invocation-admission catalog status summaries shipped: `copperfin_studio_host --builder-invocation-admission-catalog --json` now emits `admissionReadyBuilderIds`, `admissionBlockedBuilderIds`, and `admissionBlockedErrors` beside existing detailed per-builder invocation-admission entries.
- 2026-06-19: E2/#1360 Studio host builder launch catalog status summaries shipped: `copperfin_studio_host --builder-launch-catalog --json` now emits `launchReadyBuilderIds`, `launchBlockedBuilderIds`, and `launchBlockedErrors` beside existing detailed per-builder launch entries.
- 2026-06-19: E2/#1359 Studio host designer launch-surface catalog status summaries shipped: `copperfin_studio_host --designer-launch-surface-catalog --json` now emits `launchReadySelectionContexts`, `launchBlockedSelectionContexts`, and `launchBlockedErrors` beside existing detailed per-context launch-surface entries.
- 2026-06-19: E2/#1358 Studio host designer invocation-admission catalog status summaries shipped: `copperfin_studio_host --designer-invocation-admission-catalog --json` now emits `admissionOkSelectionContexts`, `admissionBlockedSelectionContexts`, and `admissionBlockedErrors` beside existing detailed per-context invocation admission entries.
- 2026-06-19: E2/#1357 Studio host designer dispatch catalog status summaries shipped: `copperfin_studio_host --designer-dispatch-catalog --json` now emits `dispatchOkSelectionContexts`, `dispatchBlockedSelectionContexts`, and `dispatchBlockedErrors` beside existing detailed per-context dispatch entries.
- 2026-06-19: E2/#1356 Studio host designer dispatch execution catalog readiness summaries shipped: `copperfin_studio_host --designer-dispatch-execution-catalog --json` now emits `readySelectionContexts`, `blockedSelectionContexts`, and `blockedExecutionErrors` beside existing context counts and detailed per-context readiness entries.
- 2026-06-19: E2/#1355 Studio host aggregate designer failed-child exit-code summaries shipped: `copperfin_studio_host --designer-execute --json` now emits numeric `failedEditorActionExitCodes`, `failedBuilderExitCodes`, and compact `toolboxExitCode` fields beside failed-child ids/tokens/commands/errors and detailed child rows.
- 2026-06-19: E2/#1354 Studio host aggregate designer failed-child executed-command summaries shipped: `copperfin_studio_host --designer-execute --json` now emits `failedEditorActionExecutedCommands`, `failedBuilderExecutedCommands`, and compact `toolboxExecutedCommand` fields beside failed-child ids/tokens/errors and detailed child rows.
- 2026-06-19: E2/#1353 Studio host aggregate designer failed-child command-token summaries shipped: `copperfin_studio_host --designer-execute --json` now emits `failedEditorActionCommandTokens`, `failedBuilderCommandTokens`, and compact `toolboxCommandToken` fields beside existing failed-child id/error summaries and detailed child rows.
- 2026-06-19: E2/#1352 Studio host aggregate designer child descriptor-detail JSON shipped: `copperfin_studio_host --designer-execute --json` now emits editor-action child descriptions plus builder child VFP9-equivalent, Copperfin component, and description metadata beside existing aggregate child descriptor and execution fields.
- 2026-06-19: E2/#1351 Studio host aggregate designer child descriptor metadata JSON shipped: `copperfin_studio_host --designer-execute --json` now emits editor-action child label/kind fields and builder child title/kind fields beside existing child identity, target metadata, dispatch, launch, execution, and failure telemetry.
- 2026-06-19: E2/#1350 Studio host aggregate designer toolbox child item-descriptor JSON shipped: `copperfin_studio_host --designer-execute --json` now emits the toolbox child `items` descriptor array beside `itemCount`, target metadata, dispatch arguments, launch command, executed command, and normalized exit-code fields.
- 2026-06-19: E2/#1349 Studio host aggregate designer child target-metadata JSON shipped: `copperfin_studio_host --designer-execute --json` now emits child-level editor-action target surface/source location metadata, builder entry point and target identity metadata, and toolbox target identity metadata beside existing child dispatch, launch, execution, and failure fields.
- 2026-06-19: E2/#1348 Studio host aggregate designer child launch-command JSON shipped: `copperfin_studio_host --designer-execute --json` now emits per-child `launchCommand` fields for editor-action, builder/wizard, and toolbox execution entries beside dispatch arguments, executed commands, normalized exit codes, and existing top-level launch-command fields, making each aggregate child entry self-contained for host consumers.
- 2026-06-19: E2/#1347 Studio host process-exit normalization shipped: `copperfin_studio_host` now normalizes `std::system(...)` wait statuses before reporting `observedExitCode`, so individual builder, editor-action, toolbox, and aggregate designer execution JSON report child process exit code `1` for `/bin/false` instead of platform wait-status encoding.
- 2026-06-19: E2/#1346 Studio host aggregate designer execution failed-child error summary JSON shipped: `copperfin_studio_host --designer-execute --json` now reports compact `failedEditorActionErrors`, `failedBuilderErrors`, and `toolboxError` fields beside failed child ids and toolbox failure state, so host consumers can diagnose failed child categories without scanning full child result arrays.
- 2026-06-19: E2/#1345 Studio host aggregate designer execution failed-child summary JSON shipped: `copperfin_studio_host --designer-execute --json` now reports compact `failedEditorActionIds`, `failedBuilderIds`, and `toolboxFailed` fields beside aggregate and per-category execution telemetry, so host consumers can identify failed child categories without scanning full child result arrays.
- 2026-06-19: E2/#1344 Studio host aggregate designer execution child-success-count JSON shipped: `copperfin_studio_host --designer-execute --json` now reports explicit `editorActionExecutedCount`, `builderExecutedCount`, and `toolboxExecutedCount` fields beside aggregate execution/error totals, per-category entry counts, and per-category error counts, so host consumers can distinguish partial-success distribution without scanning child result arrays.
- 2026-06-19: E2/#1343 Studio host aggregate designer execution child-error-count JSON shipped: `copperfin_studio_host --designer-execute --json` now reports explicit `editorActionErrorCount`, `builderErrorCount`, and `toolboxErrorCount` fields beside aggregate execution/error counts and per-category child entry counts, so host consumers can distinguish child failure distribution without scanning child result arrays.
- 2026-06-19: E2/#1342 Studio host aggregate designer execution child-count JSON shipped: `copperfin_studio_host --designer-execute --json` now reports explicit `editorActionExecutionCount`, `builderExecutionCount`, and `toolboxExecutionCount` fields beside aggregate execution/error counts, including failed child-process paths that use the reporting-only dispatch-plan fallback.
- 2026-06-19: E2/#1341 Studio host failed aggregate editor/toolbox execution metadata coverage shipped: `test_studio_host_json` now explicitly proves `copperfin_studio_host --designer-execute --json` preserves planned editor-action and toolbox child identity/context, command token, dispatch arguments, and executed-command metadata when those child process launches fail.
- 2026-06-19: E2/#1340 Studio host failed aggregate designer execution child-metadata JSON shipped: `copperfin_studio_host --designer-execute --json` now uses the pre-execution aggregate dispatch plan as a reporting-only fallback when a child process fails, preserving failed child builder identity, command token, dispatch arguments, and executed-command strings without changing the core no-stale aggregate execution result model.
- 2026-06-19: E2/#1339 Studio host aggregate designer execution child-state JSON shipped: `copperfin_studio_host --designer-execute --json` now reports per-child execution admission, launch, dry-run, and mutation state for aggregate editor-action, builder/wizard, and toolbox execution entries, aligning aggregate child summaries with the individual execution endpoint state model.
- 2026-06-19: E2/#1338 Studio host aggregate designer execution child dispatch-argument JSON shipped: `copperfin_studio_host --designer-execute --json` now reports raw per-child dispatch argument arrays for aggregate editor-action, builder/wizard, and toolbox execution entries, so hosts can consume child argv contracts without parsing shell-quoted executed command strings.
- 2026-06-19: E2/#1337 Studio host aggregate designer execution child-identity JSON shipped: `copperfin_studio_host --designer-execute --json` now reports per-child editor action ids, builder ids, toolbox context, command tokens, and related selection/context metadata beside child executed commands and exit status so aggregate execution output can be mapped back to child dispatch plans without parsing command strings.
- 2026-06-19: E2/#1336 Studio host aggregate designer dispatch catalog dispatch-argument JSON shipped: `copperfin_studio_host --designer-dispatch-catalog` now preserves nested editor-action, builder/wizard, and toolbox dispatch command tokens plus dispatch arguments in each aggregate dispatch-catalog context, so host consumers can inspect exact non-executing child launch contracts without switching to execution-readiness catalog output.
- 2026-06-19: E2/#1335 Studio host aggregate designer execution child-command JSON shipped: `copperfin_studio_host --designer-execute --json` now reports per-child `executedCommand` values for editor-action, builder/wizard, and toolbox executions, reconstructing the exact launched shell commands from each child dispatch argument vector while preserving existing aggregate launch-command fields and failure behavior.
- 2026-06-19: E2/#1334 Studio host aggregate designer dispatch execution catalog dispatch-argument JSON shipped: `copperfin_studio_host --designer-dispatch-execution-catalog` now preserves nested editor-action, builder/wizard, and toolbox dispatch command tokens plus dispatch arguments in each aggregate execution-catalog context, so host consumers can inspect exact non-mutating child launch contracts without executing UI/processes or mutating assets.
- 2026-06-19: E2/#1333 Studio host aggregate designer dispatch execution catalog JSON shipped: `copperfin_studio_host --designer-dispatch-execution-catalog` now exposes non-mutating aggregate designer execution-readiness catalogs with explicit editor/builder/toolbox invocation admission flags, explicit aggregate execution admission, per-context dispatch/readiness/error metadata, parser validation, dry-run/no-admitted-dispatch failure propagation, and no child executor/process/UI launch.
- 2026-06-19: E2/#1332 aggregate designer dispatch execution catalog planning shipped: `plan_studio_designer_dispatch_execution_catalog` now composes aggregate designer dispatch catalogs with explicit aggregate execution-admission readiness, preserving per-context dispatch metadata while reporting execution-ready counts, preflight error counts, dry-run/non-mutating state, unadmitted execution errors, aggregate dispatch-plan errors, dry-run/no-admitted-dispatch failures, and no child executor/process/UI launch.
- 2026-06-19: E2 toolbox-create host JSON validation repair shipped: toolbox create/create-dispatch parser helpers now reject missing option values when the next token is another `--option`, preserving the intended missing-item diagnostics for `#1301`, `#1303`, and `#1309`; the synthetic toolbox-create SCX fixture now allows the explicit long `UNIQUEID` values exercised by `#1311`, `#1314`, and `#1315` without weakening DBF field-width validation.
- 2026-06-19: E2/#1331 Studio host toolbox dispatch execution catalog JSON shipped: `copperfin_studio_host --toolbox-dispatch-execution-catalog` now exposes non-mutating toolbox execution-readiness catalogs with explicit palette-invocation and toolbox-execution admission flags, invocation/dispatch metadata, per-item readiness/error metadata, parser validation, dry-run failure propagation, and no toolbox process launch.
- 2026-06-19: E2/#1330 toolbox dispatch execution catalog planning shipped: `plan_studio_toolbox_dispatch_execution_catalog` now summarizes toolbox dispatch execution readiness without launching toolbox processes, preserving invocation/dispatch/item metadata while requiring explicit execution admission and reporting per-item preflight errors for unadmitted, dry-run, incomplete, or empty-context catalogs.
- 2026-06-19: E2/#1329 Studio host editor-action dispatch execution catalog JSON shipped: `copperfin_studio_host --editor-action-dispatch-execution-catalog` now exposes non-mutating editor execution-readiness catalogs with explicit editor-invocation and editor-action execution admission flags, per-action launch/admission/dispatch/readiness metadata, parser validation, dry-run failure propagation, and no editor process launch.
- 2026-06-19: E2/#1328 editor action dispatch execution catalog planning shipped: `plan_studio_editor_action_dispatch_execution_catalog` now summarizes context editor dispatch execution readiness without launching editors, preserving launch/admission/dispatch metadata while requiring explicit execution admission and reporting per-action preflight errors for unadmitted, dry-run, incomplete, or empty-context catalogs.
- 2026-06-19: E2/#1327 Studio host builder dispatch execution catalog JSON shipped: `copperfin_studio_host --builder-dispatch-execution-catalog` now exposes non-mutating builder/wizard execution-readiness catalogs with explicit UI-launch and builder-execution admission flags, per-builder launch/admission/dispatch/readiness metadata, parser validation, dry-run failure propagation, and no builder process launch.
- 2026-06-19: E2/#1326 builder dispatch execution catalog planning shipped: `plan_studio_builder_dispatch_execution_catalog` now summarizes builder/wizard dispatch execution readiness without launching builders, preserving launch/admission/dispatch metadata while requiring explicit execution admission and reporting per-builder preflight errors for unadmitted, dry-run, incomplete, or empty-context catalogs.
- 2026-06-19: E2/#1325 Studio host aggregate designer dispatch execution JSON shipped: `copperfin_studio_host --designer-execute` now runs admitted aggregate designer dispatch plans through the host with explicit aggregate execution admission, separate editor-action/builder/toolbox launch commands, child process status summaries, execution/error counts, and deterministic no-stale-metadata failures for parser, admission, dispatch preflight, and child process errors.
- 2026-06-19: E2/#1324 aggregate designer dispatch execution seam shipped: `execute_studio_designer_dispatch` now composes editor-action, builder, and toolbox dispatch execution seams behind strict aggregate preflight, requiring explicit admission, an error-free non-dry-run dispatch plan, and all required child executors before launch, while preserving child execution results and preventing stale aggregate executed metadata on validation or child failures.
- 2026-06-19: E2/#1323 Studio host toolbox dispatch execution JSON shipped: `copperfin_studio_host --toolbox-execute` now executes admitted toolbox palette dispatch contracts through the host with explicit palette-invocation and toolbox execution admission, shell command construction from dispatch arguments, observed exit-code reporting, item metadata reporting, and deterministic no-stale-metadata failures for parser, admission, unsupported-context, and process errors.
- 2026-06-19: E2/#1322 toolbox dispatch execution seam shipped: `execute_studio_toolbox_dispatch` now executes admitted toolbox palette dispatch contracts through an injected executor, preserving dispatch metadata, launch observation output/exit/error state, executed state, item metadata, and mutation reporting while rejecting unadmitted, stale, incomplete, failed-launch, and nonzero-exit executions without stale metadata.
- 2026-06-19: E2/#1321 Studio host editor-action dispatch execution JSON shipped: `copperfin_studio_host --editor-action-execute <id>` now executes admitted context-editor action dispatch contracts through the host with explicit editor-invocation and editor-action execution admission, shell command construction from dispatch arguments, observed exit-code reporting, and deterministic no-stale-metadata failures for parser, admission, and process errors.
- 2026-06-19: E2/#1320 editor action dispatch execution seam shipped: `execute_studio_editor_action_dispatch` now executes admitted editor-action dispatch contracts through an injected executor, preserving dispatch metadata, launch observation output/exit/error state, executed state, and mutation reporting while rejecting unadmitted, stale, incomplete, failed-launch, and nonzero-exit executions without stale metadata.
- 2026-06-19: E2/#1319 Studio host builder dispatch execution JSON shipped: `copperfin_studio_host --builder-execute <id>` now executes admitted builder/wizard dispatch contracts through the host with explicit UI-launch and builder-execution admission, shell command construction from dispatch arguments, observed exit-code reporting, and deterministic no-stale-metadata failures for parser, admission, and process errors.
- 2026-06-19: E2/#1318 builder dispatch execution seam shipped: `execute_studio_builder_dispatch` now executes admitted builder/wizard dispatch contracts through an injected executor, preserving dispatch metadata, launch observation output/exit state, executed state, and mutation reporting while rejecting unadmitted, stale, incomplete, failed-launch, and non-zero-exit executions without stale metadata.
- 2026-06-19: E2/#1317 Studio host toolbox palette launch catalog JSON shipped: `copperfin_studio_host --toolbox-palette-launch-catalog` now exposes non-mutating toolbox palette launch catalogs with context counts, launch/error counts, per-context availability, nested launch metadata, filtered item descriptors, parser validation, and deterministic unsupported-context errors.
- 2026-06-19: E2/#1316 toolbox palette launch catalog planning shipped: `plan_studio_toolbox_palette_launch_catalog` now catalogs toolbox palette launch availability across known Studio editor selection contexts, preserving shared asset/object metadata, filtered item metadata, launch/error counts, dry-run state, non-mutating state, and deterministic unsupported-context errors without stale palette metadata.
- 2026-06-18: E2/#1313 toolbox object batch creation execution from admitted palette dispatch is now implemented and closed. `create_visual_objects_from_toolbox_dispatch` composes existing admitted toolbox palette dispatch batch planning with the VFP visual-object batch append path, preserving dispatch-sourced toolbox context, selected-parent defaults, ordered generated or explicit object identities, unique ids, parent metadata, caller field values, lower-level batch create-result metadata, dry-run state, and mutation state. Focused coverage proves admitted ordered visual TextBox/CommandButton/TextBox execution, report-safe Label execution, persisted caller fields, non-admitted dry-run dispatch rejection without stale created-object metadata, unavailable dispatch item rejection without stale metadata, duplicate planned identity rejection without mutation, lower-layer invalid-field failure reporting without stale batch-create metadata, and no partial mutation on rejected or failed batch commands. Validation passed with `git diff --check`, changed-object compilation for `src/studio/toolbox_creation.cpp` and `tests/test_toolbox_creation.cpp`, direct archive refresh for `libcf_design_model.a`, direct relink of `test_toolbox_creation`, and `./build/tests/test_toolbox_creation`.
- 2026-06-18: E2/#1312 toolbox object creation execution from admitted palette dispatch is now implemented and closed. `create_visual_object_from_toolbox_dispatch` composes existing admitted toolbox palette dispatch planning with the VFP visual-object append path, preserving dispatch-sourced toolbox context, selected-parent defaults, generated or explicit object identity, unique id, parent metadata, caller field values, lower-level create-result metadata, dry-run state, and mutation state. Focused coverage proves admitted visual TextBox execution, report-safe Label execution, persisted caller fields, non-admitted dry-run dispatch rejection without stale identity metadata, unavailable dispatch item rejection without stale identity metadata, lower-layer invalid-field failure reporting without stale create metadata, and no mutation on rejected or failed commands. Validation passed with `git diff --check`, changed-object compilation for `src/studio/toolbox_creation.cpp` and `tests/test_toolbox_creation.cpp`, direct archive refresh for `libcf_design_model.a`, direct relink of `test_toolbox_creation`, and `./build/tests/test_toolbox_creation`.
- 2026-06-18: E2/#1311 Studio host selection-context toolbox object batch creation JSON is now implemented. `copperfin_studio_host --selection-toolbox-create-batch --selection-context <token>` accepts ordered `--toolbox-item` groups with optional per-item object name, unique id, parent name, and repeatable field values, routes through `create_visual_objects_from_toolbox_selection`, and emits selected Studio context, resolved toolbox context, launch-plan status/error, nested batch-plan metadata, lower-level batch-create result metadata, record indexes, created-object identities, dry-run state, mutation state, and clean error state. Parser validation rejects missing paths, missing/unknown selection contexts, empty item lists, item options before a toolbox item, malformed field values, and unknown options before mutation. Focused coverage proves ordered visual-object TextBox/CommandButton/TextBox batch creation, report-expression Label creation, generated-name reservation, explicit per-item identity/parent/field propagation, persisted caller fields, unavailable report TextBox rejection without stale create metadata, unsupported menu-selection rejection without stale create metadata, parser failures, and no mutation on rejected commands. Validation passed with `git diff --check`, changed-object compilation for `apps/copperfin_studio_host/main.cpp` and `tests/test_studio_host_json.cpp`, direct relink of `copperfin_studio_host` and `test_studio_host_json`, and `timeout 120 ./build/tests/test_studio_host_json ./build/copperfin_studio_host`. Issue closure remains pending explicit user approval if the no-close-without-approval standing rule is active.
- 2026-06-18: E2/#1310 selection-context toolbox object batch creation execution is now implemented and closed. `create_visual_objects_from_toolbox_selection` resolves visual-object and report-expression selections through `plan_visual_objects_from_toolbox_selection`, preserves selected Studio context, resolved toolbox context, launch metadata, nested batch-plan metadata, generated-name reservation, explicit per-item identity, unique id, parent metadata, field values, item counts, record indexes, created-object identity metadata, execution dry-run state, mutation state, and lower-level batch-create results, then reuses the existing VFP visual-object batch append path for accepted mutations. Focused coverage proves ordered visual TextBox/CommandButton/TextBox creation, report Label creation, explicit identity/parent/field-value propagation, persisted caller fields, unavailable report TextBox rejection without stale create metadata, unsupported menu-selection rejection without stale create metadata, and no mutation on rejected commands. Validation passed with `git diff --check`, changed-object compilation for `src/studio/toolbox_creation.cpp` and `tests/test_toolbox_creation.cpp`, direct archive refresh for `libcf_design_model.a`, direct relink of `test_toolbox_creation`, and `./build/tests/test_toolbox_creation`.
# Changelog

## 2026-06-18

- E2/#1315 Studio host toolbox object batch creation from admitted palette dispatch JSON shipped: `copperfin_studio_host --toolbox-create-batch-from-dispatch --toolbox-item <id> --selection-context <token>` now executes dispatch-sourced ordered batch toolbox creation through the host, preserving nested batch-plan metadata, created-object metadata, record indexes, generated or explicit per-item identity/parent fields, field values, parser validation, persisted caller fields, and deterministic dispatch/admission/duplicate/field failure handling without stale metadata or partial mutation.
- E2/#1314 Studio host toolbox object creation from admitted palette dispatch JSON shipped: `copperfin_studio_host --toolbox-create-from-dispatch <id> --selection-context <token>` now executes dispatch-sourced single-object toolbox creation through the host, preserving dispatch/create/create-result metadata, selected-parent defaults, generated or explicit identity/parent fields, field values, parser validation, persisted caller fields, and deterministic dispatch/admission/field failure handling without stale metadata or partial mutation.
- E2/#1309 Studio host selection-context toolbox object creation JSON shipped: `copperfin_studio_host --selection-toolbox-create <id> --selection-context <token>` now executes selected-context single-object toolbox creation with launch/create/create-result metadata, generated-name reservation, explicit identity/parent fields, field values, parser validation, persisted caller fields, and deterministic unavailable/unsupported rejection without stale metadata or failure-path mutation.
- E2/#1308 selection-context toolbox object creation execution shipped: `create_visual_object_from_toolbox_selection` now resolves selected Studio contexts through selected-context toolbox create planning before appending one object through the existing VFP visual object creation path, preserving launch/create metadata, generated-name reservation, explicit identity/parent fields, field values, mutation state, created-object identity, and deterministic unavailable/unsupported rejection without stale metadata or failure-path mutation.
- E2/#1307 Studio host selection-context toolbox object batch create-dispatch JSON shipped: `copperfin_studio_host --selection-toolbox-create-batch-dispatch-plan --selection-context <token>` now exposes non-executing selected-context ordered batch toolbox create-dispatch plans with launch/batch/dispatch metadata, deterministic dispatch arguments, generated-name reservation, explicit per-item identity/parent fields, field values, parser validation, admission-sensitive rejection, selected-context unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1306 selection-context toolbox object batch create-dispatch planning shipped: `plan_visual_object_batch_create_dispatch_from_toolbox_selection` now composes selected-context ordered batch create planning with non-executing batch create-dispatch planning, preserving launch/batch/dispatch metadata, generated-name reservation, explicit per-item identity/parent fields, field values, deterministic host arguments, admission-sensitive rejection, selected-context unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1305 Studio host selection-context toolbox object batch create-plan JSON shipped: `copperfin_studio_host --selection-toolbox-create-batch-plan --selection-context <token>` now exposes non-mutating selected-context ordered batch toolbox create plans with launch/batch metadata, generated-name reservation, explicit per-item identity/parent fields, field values, parser validation, selected-context unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1304 selection-context toolbox object batch create planning shipped: `plan_visual_objects_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before ordered batch toolbox create planning, preserving launch/batch metadata, generated-name reservation, explicit per-item identity/parent fields, field values, selected-context unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1303 Studio host selection-context toolbox object create-dispatch JSON shipped: `copperfin_studio_host --selection-toolbox-create-dispatch-plan <id> --selection-context <token>` now exposes non-executing selected-context single-item toolbox create-dispatch plans with launch/create/dispatch metadata, deterministic host arguments, admission-sensitive rejection, unavailable-item rejection, unsupported-selection rejection, and parser validation without mutation.
- E2/#1302 selection-context toolbox object create-dispatch planning shipped: `plan_visual_object_create_dispatch_from_toolbox_selection` now composes selected-context single-item toolbox create planning with non-executing create-dispatch planning, preserving launch/create/dispatch metadata, deterministic host arguments, admission-sensitive errors, unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1301 Studio host selection-context toolbox object create-plan JSON shipped: `copperfin_studio_host --selection-toolbox-create-plan <id> --selection-context <token>` now exposes non-mutating selected-context single-item toolbox create plans with resolved toolbox metadata, launch-plan status, nested create-plan metadata, parser validation, selected-context unavailable-item rejection, and deterministic unsupported-selection rejection.
- E2/#1300 selection-context toolbox object create planning shipped: `plan_visual_object_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before non-mutating single-item toolbox create planning, preserving launch/create metadata, generated or explicit identity/parent fields, caller field values, selected-context unavailable-item rejection, and unsupported-selection rejection without mutation.
- E2/#1299 Studio host selection-context toolbox object batch create-dispatch catalog JSON shipped: `copperfin_studio_host --selection-toolbox-create-batch-dispatch-catalog --selection-context <token>` now exposes non-executing selected-context ordered batch create-dispatch catalogs with resolved toolbox metadata, launch-plan status, nested batch/dispatch plans, admission-sensitive parser validation, and deterministic unsupported-selection rejection.
- E2/#1298 selection-context toolbox object batch create-dispatch catalog planning shipped: `plan_visual_object_batch_create_dispatch_catalog_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before non-executing ordered batch create-dispatch catalog planning, preserving launch/batch/dispatch metadata, admission-sensitive errors, generated-name reservation, field-value propagation, and unsupported-selection rejection without mutation.
- E2/#1297 Studio host selection-context toolbox object batch create-plan catalog JSON shipped: `copperfin_studio_host --selection-toolbox-create-batch-plan-catalog --selection-context <token>` now exposes non-mutating selected-context ordered batch create-plan catalogs with resolved toolbox metadata, launch-plan status, nested batch plans, parser validation, and deterministic unsupported-selection rejection.
- E2/#1296 selection-context toolbox object batch create-plan catalog planning shipped: `plan_visual_object_batch_catalog_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before non-mutating ordered batch create-plan catalog planning, preserving launch/batch metadata, generated-name reservation, field-value propagation, and unsupported-selection rejection without mutation.
- E2/#1295 Studio host selection-context toolbox object create-dispatch catalog JSON shipped: `copperfin_studio_host --selection-toolbox-create-dispatch-catalog --selection-context <token>` now exposes non-executing selected-context toolbox create-dispatch catalogs with resolved toolbox metadata, launch-plan status, per-item create/dispatch plans, admission-sensitive parser validation, and deterministic unsupported-selection rejection.
- E2/#1294 selection-context toolbox object create-dispatch catalog planning shipped: `plan_visual_object_create_dispatch_catalog_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before non-executing create-dispatch catalog planning, preserving launch/create/dispatch metadata, admission-sensitive dispatch errors, and unsupported-selection rejection without mutation.
- E2/#1293 Studio host selection-context toolbox object create-plan catalog JSON shipped: `copperfin_studio_host --selection-toolbox-create-plan-catalog --selection-context <token>` now exposes non-mutating selected-context toolbox create-plan catalogs with resolved toolbox metadata, launch-plan status, per-item create plans, parser validation, and deterministic unsupported-selection rejection.
- E2/#1292 selection-context toolbox object create-plan catalog planning shipped: `plan_visual_object_catalog_from_toolbox_selection` now resolves selected Studio contexts through toolbox palette launch planning before non-mutating create-plan catalog planning, preserving launch/create metadata and unsupported-selection rejection.
- E2/#1291 Studio host selection-context toolbox palette dispatch catalog JSON shipped: `copperfin_studio_host --selection-toolbox-dispatch-catalog --selection-context <token>` now exposes non-executing selected-context toolbox dispatch catalogs with resolved toolbox metadata, launch/admission/dispatch status, dispatch arguments, parser validation, and deterministic unsupported-selection rejection.
- E2/#1290 selection-context toolbox palette dispatch catalog planning shipped: `plan_studio_toolbox_dispatch_catalog_for_selection` now resolves selected Studio contexts through the shared selection toolbox admission catalog before non-executing dispatch planning, preserving launch/admission/dispatch metadata and unsupported-selection rejection without mutation.
- E2/#1289 Studio host selection-context toolbox palette invocation admission catalog JSON shipped: `copperfin_studio_host --selection-toolbox-invocation-admission-catalog --selection-context <token>` now exposes non-executing selected-context toolbox admission catalogs with resolved toolbox metadata, launch/admission status, parser validation, and deterministic unsupported-selection rejection.
- E2/#1288 selection-context toolbox palette invocation admission catalog planning shipped: `plan_studio_toolbox_invocation_admission_catalog_for_selection` now resolves selected Studio contexts to toolbox palettes before non-executing admission planning, preserving launch/admission metadata and rejecting unsupported selections without mutation.
- E2/#1287 toolbox palette dispatch catalog planning now routes through `plan_studio_toolbox_invocation_admission_catalog`, keeping item filtering and palette-admission metadata single-sourced before dispatch planning while preserving non-executing public dispatch catalog behavior.
- E2/#1286 Studio host toolbox palette invocation admission catalog JSON shipped: `copperfin_studio_host --toolbox-invocation-admission-catalog --toolbox-context <token>` now exposes non-executing toolbox admission catalogs with item descriptors, command tokens, selected asset/object metadata, requested palette-admission state, dry-run/non-mutating status, and parser validation.
- E2/#1285 toolbox palette invocation admission catalog planning shipped: `plan_studio_toolbox_invocation_admission_catalog` now builds non-executing per-toolbox-context admission catalogs while preserving command tokens, selected asset/object metadata, item descriptors, requested palette-admission state, and dry-run/non-mutating status.
- E2/#1284 aggregate designer dispatch catalog planning now routes through `plan_studio_designer_invocation_admission_catalog`, keeping per-context launch/admission metadata single-sourced before aggregate dispatch planning while preserving non-executing public dispatch catalog behavior.
- E2/#1283 editor-action dispatch catalog planning now routes through `plan_studio_editor_action_invocation_admission_catalog`, keeping launch/admission metadata single-sourced before dispatch planning while preserving non-executing public dispatch catalog behavior.
- E2/#1282 Studio host selection-context editor-action invocation admission catalog JSON shipped: `copperfin_studio_host --editor-action-invocation-admission-catalog --selection-context <token>` now exposes non-executing selected-context editor-action invocation admission catalogs with action metadata, selected-object/editor metadata, command tokens, target surfaces, source position metadata, requested admission state, dry-run/non-mutating status, and parser validation.
- E2/#1281 selection-context editor-action invocation admission catalog planning shipped: `plan_studio_editor_action_invocation_admission_catalog` now builds non-executing per-context admission catalogs while preserving action metadata, selected-object/editor metadata, command tokens, target surfaces, source position metadata, requested admission state, and dry-run/non-mutating status.
- E2/#1280 Studio host selection-context editor-action launch catalog JSON shipped: `copperfin_studio_host --editor-action-launch-catalog --selection-context <token>` now exposes non-executing selected-context editor-action launch catalogs with action metadata, selected-object/editor metadata, command tokens, target surfaces, source position metadata, and parser validation.
- E2/#1279 selection-context editor-action launch catalog planning shipped: `plan_studio_editor_action_launch_catalog` now builds non-executing editor-action launch catalogs for one Studio selection context while preserving action metadata, selected-object/editor metadata, command tokens, target surfaces, and source position metadata.
- E2/#1278 Studio host selection-context builder/wizard launch catalog JSON shipped: `copperfin_studio_host --selection-builder-launch-catalog --selection-context <token>` now exposes non-executing selected-context builder launch catalogs with mixed visual form/control builders, menu builders, VFP-equivalent metadata, and parser validation.
- E2/#1277 selection-context builder/wizard launch catalog planning shipped: `plan_studio_builder_launch_catalog_for_selection` now builds non-executing builder launch catalogs for one Studio selection context while preserving mixed visual form/control builders and context-specific menu/data-environment builder metadata.
- E2/#1276 Studio host selection-context builder/wizard dispatch catalog JSON shipped: `copperfin_studio_host --selection-builder-dispatch-catalog --selection-context <token>` now exposes non-executing selected-context builder dispatch catalogs with mixed visual form/control builders, menu builders, dispatch arguments, dry-run rejection state, and parser validation.
- E2/#1275 selection-context builder/wizard dispatch catalog planning shipped: `plan_studio_builder_dispatch_catalog_for_selection` now builds non-executing builder dispatch catalogs for one Studio selection context while preserving mixed visual form/control builders, menu builders, admission state, dispatch metadata, and dry-run rejection behavior.
- E2/#1274 Studio host selection-context builder/wizard invocation admission catalog JSON shipped: `copperfin_studio_host --selection-builder-invocation-admission-catalog --selection-context <token>` now exposes non-executing selected-context builder admission catalogs with mixed visual form/control builders, menu builders, dry-run/admission state, and parser validation.
- E2/#1273 selection-context builder/wizard invocation admission catalog planning shipped: `plan_studio_builder_invocation_admission_catalog_for_selection` now builds non-executing builder admission catalogs for one Studio selection context while preserving mixed visual form/control builders and context-specific menu/data-environment builder metadata.
- E2/#1272 builder/wizard dispatch catalog planning now routes through `plan_studio_builder_invocation_admission_catalog`, keeping launch/admission metadata single-sourced before dispatch planning while preserving non-executing public dispatch catalog behavior.
- E2/#1271 Studio host builder/wizard invocation admission catalog JSON shipped: `copperfin_studio_host --builder-invocation-admission-catalog --builder-context <token>` now exposes non-executing per-context builder admission catalogs with aggregate counts, UI-admission state, dry-run/non-mutating state, and per-builder launch/admission metadata while rejecting invalid context, record, and boolean options before planning.
- E2/#1270 builder/wizard invocation admission catalog planning shipped: `plan_studio_builder_invocation_admission_catalog` now composes per-context builder launch planning with invocation admission, preserving per-builder metadata and reporting aggregate admission counts, dry-run state, and non-mutating status without spawning UI or mutating assets.
- E2/#1269 Studio host builder/wizard launch catalog JSON shipped: `copperfin_studio_host --builder-launch-catalog --builder-context <token>` now exposes non-executing per-context builder launch catalogs with aggregate counts, dry-run/non-mutating state, and per-builder launch metadata while rejecting invalid context, record, and invocation-only options before planning.

## 2026-06-17

- E2/#1075 host allow-output-object command shipped: `copperfin_studio_host --allow-output-object` now assigns ALLOWOUTPUT logical state through stable target selectors and `--allow-output true|false`, rejects invalid boolean values and ambiguous allow-output-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/dynamic-fore-color-object/closable-object/control-box-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1074 host control-box-object command shipped: `copperfin_studio_host --control-box-object` now assigns CONTROLBOX logical state through stable target selectors and `--control-box true|false`, rejects invalid boolean values and ambiguous control-box-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/dynamic-fore-color-object/closable-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1073 host closable-object command shipped: `copperfin_studio_host --closable-object` now assigns CLOSABLE logical state through stable target selectors and `--closable true|false`, rejects invalid boolean values and ambiguous closable-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/dynamic-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1072 host current-control-object command shipped: `copperfin_studio_host --current-control-object` now assigns CURRENTCONTROL text through stable target selectors and `--current-control <value>`, rejects ambiguous current-control-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/dynamic-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1071 host dynamic-fore-color-object command shipped: `copperfin_studio_host --dynamic-fore-color-object` now assigns raw DYNAMICFORECOLOR expressions through stable target selectors and `--dynamic-fore-color <expr>`, preserves expression text without integer parsing or quoted-string serialization, rejects ambiguous dynamic-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1070 host dynamic-back-color-object command shipped: `copperfin_studio_host --dynamic-back-color-object` now assigns raw DYNAMICBACKCOLOR expressions through stable target selectors and `--dynamic-back-color <expr>`, preserves expression text without integer parsing or quoted-string serialization, rejects ambiguous dynamic-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1069 host disabled-fore-color-object command shipped: `copperfin_studio_host --disabled-fore-color-object` now assigns non-negative DISABLEDFORECOLOR values through stable target selectors and `--disabled-fore-color <n>`, rejects negative values and ambiguous disabled-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1068 host disabled-back-color-object command shipped: `copperfin_studio_host --disabled-back-color-object` now assigns non-negative DISABLEDBACKCOLOR values through stable target selectors and `--disabled-back-color <n>`, rejects negative values and ambiguous disabled-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1067 host fore-color-object command shipped: `copperfin_studio_host --fore-color-object` now assigns non-negative FORECOLOR values through stable target selectors and `--fore-color <n>`, rejects negative values and ambiguous fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1066 host back-color-object command shipped: `copperfin_studio_host --back-color-object` now assigns non-negative BACKCOLOR values through stable target selectors and `--back-color <n>`, rejects negative values and ambiguous back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1065 host highlight-fore-color-object command shipped: `copperfin_studio_host --highlight-fore-color-object` now assigns non-negative HIGHLIGHTFORECOLOR values through stable target selectors and `--highlight-fore-color <n>`, rejects negative values and ambiguous highlight-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1064 host highlight-back-color-object command shipped: `copperfin_studio_host --highlight-back-color-object` now assigns non-negative HIGHLIGHTBACKCOLOR values through stable target selectors and `--highlight-back-color <n>`, rejects negative values and ambiguous highlight-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1063 host item-fore-color-object command shipped: `copperfin_studio_host --item-fore-color-object` now assigns non-negative ITEMFORECOLOR values through stable target selectors and `--item-fore-color <n>`, rejects negative values and ambiguous item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1062 host item-back-color-object command shipped: `copperfin_studio_host --item-back-color-object` now assigns non-negative ITEMBACKCOLOR values through stable target selectors and `--item-back-color <n>`, rejects negative values and ambiguous item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1061 host disabled-item-fore-color-object command shipped: `copperfin_studio_host --disabled-item-fore-color-object` now assigns non-negative DISABLEDITEMFORECOLOR values through stable target selectors and `--disabled-item-fore-color <n>`, rejects negative values and ambiguous disabled-item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1060 host disabled-item-back-color-object command shipped: `copperfin_studio_host --disabled-item-back-color-object` now assigns non-negative DISABLEDITEMBACKCOLOR values through stable target selectors and `--disabled-item-back-color <n>`, rejects negative values and ambiguous disabled-item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1059 host selected-item-fore-color-object command shipped: `copperfin_studio_host --selected-item-fore-color-object` now assigns non-negative SELECTEDITEMFORECOLOR values through stable target selectors and `--selected-item-fore-color <n>`, rejects negative values and ambiguous selected-item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1058 host selected-item-back-color-object command shipped: `copperfin_studio_host --selected-item-back-color-object` now assigns non-negative SELECTEDITEMBACKCOLOR values through stable target selectors and `--selected-item-back-color <n>`, rejects negative values and ambiguous selected-item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1057 host selected-fore-color-object command shipped: `copperfin_studio_host --selected-fore-color-object` now assigns non-negative SELECTEDFORECOLOR values through stable target selectors and `--selected-fore-color <n>`, rejects negative values and ambiguous selected-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1056 host selected-back-color-object command shipped: `copperfin_studio_host --selected-back-color-object` now assigns non-negative SELECTEDBACKCOLOR values through stable target selectors and `--selected-back-color <n>`, rejects negative values and ambiguous selected-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1055 host display-value-object command shipped: `copperfin_studio_host --display-value-object` now assigns DISPLAYVALUE text through stable target selectors and `--display-value <value>`, including quoted text passed as one argv value, rejects ambiguous display-value-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1054 host left-column-object command shipped: `copperfin_studio_host --left-column-object` now assigns non-negative LEFTCOLUMN values through stable target selectors and `--left-column <n>`, rejects negative values and ambiguous left-column-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1053 host list-index-object command shipped: `copperfin_studio_host --list-index-object` now assigns non-negative LISTINDEX values through stable target selectors and `--list-index <n>`, rejects negative values and ambiguous list-index-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1052 host style-object command shipped: `copperfin_studio_host --style-object` now assigns non-negative STYLE values through stable target selectors and `--style <n>`, rejects negative values and ambiguous style-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1051 host column-count-object command shipped: `copperfin_studio_host --column-count-object` now assigns non-negative COLUMNCOUNT values through stable target selectors and `--column-count <n>`, rejects negative values and ambiguous column-count-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1050 host bound-column-object command shipped: `copperfin_studio_host --bound-column-object` now assigns non-negative BOUNDCOLUMN values through stable target selectors and `--bound-column <n>`, rejects negative values and ambiguous bound-column-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- Planning guidance clarified: `docs/23-phase-a-dependency-breakdown.md` now explicitly names E2/#23 as the active continuation queue after `#1049` and marks any instruction redirecting agents back to closed Phase A safety/runtime slices (`#151`-`#153`, `#92`-`#94`) as stale unless the issue tracker shows reopened regression evidence.
- E2/#1049 host row-source-type-object command shipped: `copperfin_studio_host --row-source-type-object` now assigns non-negative ROWSOURCETYPE values through stable target selectors and `--row-source-type <n>`, rejects negative values and ambiguous row-source-type-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/ungroup-object/property command combinations before mutation, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1048 host row-source-object command shipped: `copperfin_studio_host --row-source-object` now assigns ROWSOURCE values, including space-containing argv values, through stable target selectors and `--row-source <value>`, rejects ambiguous row-source-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1047 host format-object command shipped: `copperfin_studio_host --format-object` now assigns FORMAT values, including space-containing argv values, through stable target selectors and `--format <value>`, rejects ambiguous format-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1046 host input-mask-object command shipped: `copperfin_studio_host --input-mask-object` now assigns INPUTMASK values, including space-containing argv values, through stable target selectors and `--input-mask <value>`, rejects ambiguous input-mask-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1045 host control-source-object command shipped: `copperfin_studio_host --control-source-object` now assigns CONTROLSOURCE values, including space-containing argv values, through stable target selectors and `--control-source <value>`, rejects ambiguous control-source-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1044 host status-bar-text-object command shipped: `copperfin_studio_host --status-bar-text-object` now assigns STATUSBARTEXT values, including space-containing argv values, through stable target selectors and `--status-bar-text <value>`, rejects ambiguous status-bar-text-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1043 host tooltip-text-object command shipped: `copperfin_studio_host --tooltip-text-object` now assigns TOOLTIPTEXT values, including space-containing argv values, through stable target selectors and `--tooltip-text <value>`, rejects ambiguous tooltip-text-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1042 host caption-object command shipped: `copperfin_studio_host --caption-object` now assigns CAPTION text values, including space-containing argv values, through stable target selectors and `--caption <value>`, rejects ambiguous caption-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1041 host locked-object command shipped: `copperfin_studio_host --locked-object` now assigns LOCKED logical values through stable target selectors and `--locked true|false`, rejects ambiguous locked-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1040 host read-only-object command shipped: `copperfin_studio_host --read-only-object` now assigns READONLY logical values through stable target selectors and `--object-read-only true|false` without changing the global `--read-only` launch flag, rejects ambiguous read-only-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1039 host enabled-object command shipped: `copperfin_studio_host --enabled-object` now assigns ENABLED logical values through stable target selectors and `--enabled true|false`, rejects ambiguous enabled-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1038 host visibility-object command shipped: `copperfin_studio_host --visibility-object` now assigns VISIBLE logical values through stable target selectors and `--visible true|false`, rejects ambiguous visibility-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1037 host tab-stop-object command shipped: `copperfin_studio_host --tab-stop-object` now assigns TABSTOP logical values through stable target selectors and `--tab-stop true|false`, rejects ambiguous tab-stop-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, missing selectors, missing values, duplicate targets, or incomplete requests.
- E2/#1036 host tab-order-object command shipped: `copperfin_studio_host --tab-order-object` now assigns sequential TABINDEX values through stable target selectors and optional `--starting-tab-index`, rejects ambiguous tab-order-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, duplicate targets, negative starting indexes, or incomplete requests.
- E2/#1035 host nudge-object command shipped: `copperfin_studio_host --nudge-object` now nudges selected visual objects with stable target selectors, `--nudge-mode horizontal|vertical|both`, `--delta-hpos`, and `--delta-vpos`, rejects ambiguous nudge-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, zero deltas, unsupported modes, or incomplete requests.
- E2/#1034 host snap-object command shipped: `copperfin_studio_host --snap-object` now snaps selected visual objects to grid dimensions with stable target selectors, `--snap-mode horizontal|vertical|both`, `--grid-width`, and `--grid-height`, rejects ambiguous snap-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, invalid grids, unsupported modes, or incomplete requests.
- E2/#1033 host distribute-object command shipped: `copperfin_studio_host --distribute-object` now distributes selected visual objects with stable target selectors and `--distribution-mode horizontal|vertical`, rejects ambiguous distribute-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing targets, too few targets, unsupported modes, or incomplete requests.
- E2/#1032 host resize-object command shipped: `copperfin_studio_host --resize-object` now resizes selected visual objects to stable anchor selectors with `--resize-mode width|height|size`, accepts repeated stable target selectors, rejects ambiguous resize-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing anchors, missing targets, or incomplete requests.
- E2/#1031 host align-object command shipped: `copperfin_studio_host --align-object` now aligns selected visual objects to stable anchor selectors with `--alignment-mode left|right|top|bottom|horizontal-center|vertical-center`, accepts repeated stable target selectors, rejects ambiguous align-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing anchors, missing targets, or incomplete requests.
- E2/#1030 host group-object command shipped: `copperfin_studio_host --group-object` now creates rollback-safe group containers from repeated `--field-value` assignments, reparents stable child selectors from `--group-child-object-name` and `--group-child-unique-id`, rejects ambiguous group-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/ungroup-object/property command combinations, and preserves no-mutation behavior for missing children or incomplete requests.
- E2/#1029 host ungroup-object command shipped: `copperfin_studio_host --ungroup-object` now ungroups selected containers by record index, object name, or unique id, rejects ambiguous ungroup-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/property command combinations, and preserves no-mutation behavior for missing or empty containers.
- E2/#1028 host reorder-object command shipped: `copperfin_studio_host --reorder-object` now reorders visual objects by record index, object name, or unique id, accepts front/back/before/after placement plus relative target object-name/unique-id selectors, rejects ambiguous reorder-object/delete/restore/duplicate/rename-object/reparent-object/property command combinations, and preserves no-mutation behavior for missing targets or unsupported placements.
- E2/#1027 host reparent-object command shipped: `copperfin_studio_host --reparent-object` now reparents visual objects by record index, object name, or unique id, accepts parent object-name/unique-id selectors or `--clear-parent`, rejects ambiguous reparent-object/delete/restore/duplicate/rename-object/property command combinations, and preserves no-mutation behavior for missing source or parent targets.
- E2/#1026 host rename-object command shipped: `copperfin_studio_host --rename-object` now renames visual object identity fields by record index, object name, or unique id, accepts replacement object/name/unique-id values, rejects ambiguous rename-object/delete/restore/duplicate/property command combinations, and preserves no-mutation behavior for missing targets.
- E2/#1025 host duplicate-object command shipped: `copperfin_studio_host --duplicate-object` now duplicates visual objects by record index, object name, or unique id, accepts replacement object/name/unique-id values, rejects ambiguous duplicate/delete/restore/property command combinations, and preserves no-mutation behavior for missing targets.
- E2/#1024 host restore-object command shipped: `copperfin_studio_host --restore-object` now clears visual-object deletion state by record index, object name, or unique id, rejects ambiguous delete/restore/property command combinations, and preserves no-mutation behavior for missing targets.
- E2/#1023 host delete-object command shipped: `copperfin_studio_host --delete-object` now marks visual objects deleted by record index, object name, or unique id, rejects ambiguous delete/property command combinations, and preserves no-mutation behavior for missing targets.
- E2/#1022 host rename-property command shipped: `copperfin_studio_host --rename-property` now renames memo-backed visual object properties by record index, object name, or unique id, rejects ambiguous property commands, and preserves no-mutation behavior for missing targets.
- E2/#1021 host clear-property command shipped: `copperfin_studio_host --clear-property` now clears visual object properties by record index, object name, or unique id, rejects ambiguous set/clear requests, and preserves no-mutation behavior for missing targets.
- E2/#1020 stable-selector host property edits shipped: `copperfin_studio_host --set-property` now accepts `--object-name` and `--unique-id` selectors and routes them through the visual asset editor, preserving record-index compatibility while avoiding DBF row-order dependence.
- E2/#1019 toolbox creation context filtering shipped: Studio toolbox descriptor creation now accepts an optional toolbox context and rejects items outside that context without mutation, and `copperfin_studio_host --toolbox-create` exposes the same filter through `--toolbox-context`.
- E2/#1018 Studio host toolbox-create JSON shipped: `copperfin_studio_host` now accepts `--toolbox-create` requests with optional object, unique-id, parent, and repeatable field values, delegates to `create_visual_object_from_toolbox_item`, and emits deterministic success/failure JSON without mutating assets on unknown toolbox ids.
- E2/#1017 toolbox descriptor visual creation shipped: Studio now exposes `create_visual_object_from_toolbox_item`, resolving toolbox palette metadata into rollback-safe VFP visual-object create requests with default name generation, descriptor class/baseclass mapping, parent/identity propagation, extra field values, and clean unknown-toolbox failures without mutation.
- Dependency guidance refresh shipped: `docs/23-phase-a-dependency-breakdown.md` now puts the live E2/#23 agent directive ahead of historical Phase A planning content and explicitly marks the old Phase A work-package and Gantt sections as non-actionable closure evidence.
- E2/#1016 report/label DataEnvironment context inference shipped: DataEnvironment symbols and selected DataEnvironment records now infer `data_environment` for FRX/LBX assets as well as forms/classes, while report/label expression defaults and explicit override precedence remain intact.
- E2/#1015 selected visual container context inference shipped: form/class launches that select container-like records such as PageFrame or Grid now infer `container_object` when no explicit context override is present, with DataEnvironment and explicit override precedence preserved.
- E2/#1014 container designer selection context shipped: explicit Studio launches can request `container_object`, exposing property-grid, method-editor, control/grid builders, and container-safe toolbox metadata without inheriting form/class/report/menu/project/data-environment builders.
- E2/#1013 menu designer selection context shipped: MNX/MNT documents now default to `menu_item`, explicit Studio launches accept the token, and menu contexts expose property-grid plus `menu-designer` builder metadata with zero toolbox items and no form/report builder leakage.
- E2/#1012 class designer selection context shipped: VCX/VCT documents now default to `class_designer`, explicit Studio launches accept the token, and class contexts expose property-grid, method-editor, class-safe toolbox, and `class-builder` metadata without inheriting form/control builders.
- E2/#1011 label designer selection context shipped: LBX/LBT documents now default to `label_expression`, explicit Studio launches accept the token, and label contexts expose shared expression editing plus `label-wizard` metadata without inheriting report-builder actions.
- E2/#967 selected Studio object JSON shipped: `copperfin_studio_host --json` now emits a `selectedObject` summary when explicit record selection matches a parsed designer object.
- E2/#966 selected-record DataEnvironment inference shipped: form/class launches targeting a parsed DataEnvironment record now infer `data_environment` designer context when no explicit override or symbol inference applies.
- E2/#965 Studio DataEnvironment symbol inference shipped: form/class launches targeting DataEnvironment methods now infer `data_environment` designer contexts before generic visual-method fallback.
- E2/#964 Studio launch selection metadata shipped: opened Studio documents and `copperfin_studio_host --json` now preserve launch symbol, line, column, and record-index selection metadata.
- E2/#963 Studio host method-symbol context inference shipped: form/class launches with method-like symbols now infer `visual_method` designer contexts unless an explicit `--selection-context` override is supplied.
- E2/#962 Studio host selection-context override shipped: `copperfin_studio_host` now accepts explicit `--selection-context` tokens and threads them through opened-document designer contexts and JSON output.
- E2/#961 Studio host designer-context JSON shipped: `copperfin_studio_host --json` now serializes opened-document designer contexts with stable selection-context tokens, editor actions, builder/wizard entries, and toolbox item metadata.
- E2/#960 opened-document designer contexts shipped: Studio document models now carry default designer-context aggregates for form/class, report/label, project, and data-environment assets.
- E2/#959 designer context aggregation shipped: Studio now exposes a single selection-context query that aggregates filtered editor actions, builder/wizard entries, and toolbox entries for visual object, report expression, project item, and data-environment designer selections.
- E2/#958 context-aware editor action registry shipped: Studio now exposes stable selection-context action descriptors and filtering for property grids, method editors, expression editors, builders, toolbox panes, data-environment editors, and project navigation.
- E2/#957 designer toolbox palette registry shipped: Studio now exposes stable VFP-compatible toolbox descriptors and context filtering for form, class, container, and report designer control-creation palettes.
- E2/#956 context-aware builder and wizard registry shipped: Studio now exposes stable VFP-compatible builder/wizard descriptors and context filtering for forms, classes, controls, reports, labels, projects, and data-environment workflows.
- E2/#955 selected visual object min-width assignment shipped: designer commands can assign caller-provided non-negative numeric `MinWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#954 selected visual object min-height assignment shipped: designer commands can assign caller-provided non-negative numeric `MinHeight` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#953 selected visual object max-top assignment shipped: designer commands can assign caller-provided non-negative numeric `MaxTop` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#952 selected visual object max-left assignment shipped: designer commands can assign caller-provided non-negative numeric `MaxLeft` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#951 selected visual object max-width assignment shipped: designer commands can assign caller-provided non-negative numeric `MaxWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#950 selected visual object max-height assignment shipped: designer commands can assign caller-provided non-negative numeric `MaxHeight` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#949 selected visual object lock-screen assignment shipped: designer commands can assign caller-provided FoxPro logical `LockScreen` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#948 selected visual object left-column assignment shipped: designer commands can assign caller-provided non-negative numeric `LeftColumn` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#947 selected visual object list-item-id assignment shipped: designer commands can assign caller-provided non-negative numeric `ListItemID` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#946 selected visual object form-set-class assignment shipped: designer commands can assign caller-provided text `FormSetClass` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted text serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#945 selected visual object default-file-path assignment shipped: designer commands can assign caller-provided text `DefaultFilePath` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted text serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#944 selected visual object initial-selected-alias assignment shipped: designer commands can assign caller-provided text `InitialSelectedAlias` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted text serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#943 selected visual object tab-orientation assignment shipped: designer commands can assign caller-provided non-negative numeric `TabOrientation` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#942 selected visual object display-orientation assignment shipped: designer commands can assign caller-provided non-negative numeric `DisplayOrientation` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#941 selected visual object help-context-id assignment shipped: designer commands can assign caller-provided non-negative numeric `HelpContextID` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#940 selected visual object resizable assignment shipped: designer commands can assign caller-provided FoxPro logical `Resizable` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#939 selected visual object fill-color assignment shipped: designer commands can assign caller-provided non-negative numeric `FillColor` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#938 selected visual object child-order assignment shipped: designer commands can assign caller-provided non-negative numeric `ChildOrder` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#937 selected visual object highlight-style assignment shipped: designer commands can assign caller-provided non-negative numeric `HighlightStyle` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#936 selected visual object column-order assignment shipped: designer commands can assign caller-provided non-negative numeric `ColumnOrder` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#935 selected visual object allow-row-sizing assignment shipped: designer commands can assign caller-provided FoxPro logical `AllowRowSizing` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#934 selected visual object allow-header-sizing assignment shipped: designer commands can assign caller-provided FoxPro logical `AllowHeaderSizing` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#933 selected visual object partition assignment shipped: designer commands can assign caller-provided non-negative numeric `Partition` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#932 selected visual object panel-link assignment shipped: designer commands can assign caller-provided FoxPro logical `PanelLink` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#931 selected visual object link-master assignment shipped: designer commands can assign caller-provided text `LinkMaster` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted text serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#930 selected visual object record-source assignment shipped: designer commands can assign caller-provided text `RecordSource` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted text serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#929 selected visual object record-source-type assignment shipped: designer commands can assign caller-provided non-negative numeric `RecordSourceType` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#928 selected visual object highlight-row-line-width assignment shipped: designer commands can assign caller-provided non-negative numeric `HighlightRowLineWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#927 selected visual object highlight-row assignment shipped: designer commands can assign caller-provided FoxPro logical `HighlightRow` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#926 selected visual object split-bar assignment shipped: designer commands can assign caller-provided FoxPro logical `SplitBar` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#925 selected visual object record-mark assignment shipped: designer commands can assign caller-provided FoxPro logical `RecordMark` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#924 selected visual object delete-mark assignment shipped: designer commands can assign caller-provided FoxPro logical `DeleteMark` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving `.T.`/`.F.` serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#923 selected visual object grid-lines assignment shipped: designer commands can assign caller-provided non-negative numeric `GridLines` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#922 selected visual object grid-line-width assignment shipped: designer commands can assign caller-provided non-negative numeric `GridLineWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#921 selected visual object lock-columns-left assignment shipped: designer commands can assign caller-provided non-negative numeric `LockColumnsLeft` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#920 selected visual object lock-columns assignment shipped: designer commands can assign caller-provided non-negative numeric `LockColumns` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#919 selected visual object row-height assignment shipped: designer commands can assign caller-provided non-negative numeric `RowHeight` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#918 selected visual object header-height assignment shipped: designer commands can assign caller-provided non-negative numeric `HeaderHeight` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#917 selected visual object grid-line-color assignment shipped: designer commands can assign caller-provided non-negative numeric `GridLineColor` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#916 selected visual object data-session assignment shipped: designer commands can assign caller-provided non-negative numeric `DataSession` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#915 selected visual object allow-output assignment shipped: designer commands can assign caller-provided logical `AllowOutput` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#914 selected visual object buffer-mode-override assignment shipped: designer commands can assign caller-provided non-negative numeric `BufferModeOverride` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#913 selected visual object buffer-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `BufferMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#912 selected visual object bind-controls assignment shipped: designer commands can assign caller-provided logical `BindControls` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#911 selected visual object auto-verb-menu assignment shipped: designer commands can assign caller-provided logical `AutoVerbMenu` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#910 selected visual object auto-release assignment shipped: designer commands can assign caller-provided logical `AutoRelease` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#909 selected visual object continuous-scroll assignment shipped: designer commands can assign caller-provided logical `ContinuousScroll` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#908 selected visual object dockable assignment shipped: designer commands can assign caller-provided logical `Dockable` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#907 selected visual object clip-controls assignment shipped: designer commands can assign caller-provided logical `ClipControls` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#906 selected visual object auto-size assignment shipped: designer commands can assign caller-provided logical `AutoSize` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.

## 2026-06-16

- E2/#905 selected visual object title-bar assignment shipped: designer commands can assign caller-provided non-negative numeric `TitleBar` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#904 selected visual object show-window assignment shipped: designer commands can assign caller-provided non-negative numeric `ShowWindow` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#903 selected visual object Mac desktop assignment shipped: designer commands can assign caller-provided logical `MacDesktop` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#902 selected visual object key-preview assignment shipped: designer commands can assign caller-provided logical `KeyPreview` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#901 selected visual object desktop assignment shipped: designer commands can assign caller-provided logical `Desktop` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#900 selected visual object auto-center assignment shipped: designer commands can assign caller-provided logical `AutoCenter` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#899 selected visual object WhatsThis-help-ID assignment shipped: designer commands can assign caller-provided non-negative numeric `WhatsThisHelpID` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#898 selected visual object WhatsThis-help assignment shipped: designer commands can assign caller-provided logical `WhatsThisHelp` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#897 selected visual object WhatsThis-button assignment shipped: designer commands can assign caller-provided logical `WhatsThisButton` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#896 selected visual object MDI form assignment shipped: designer commands can assign caller-provided logical `MDIForm` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#895 selected visual object half-height-caption assignment shipped: designer commands can assign caller-provided logical `HalfHeightCaption` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#894 selected visual object window-state assignment shipped: designer commands can assign caller-provided non-negative numeric `WindowState` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#893 selected visual object scroll-bars assignment shipped: designer commands can assign caller-provided non-negative numeric `ScrollBars` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#892 selected visual object scale-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `ScaleMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#891 selected visual object movable assignment shipped: designer commands can assign caller-provided logical `Movable` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#890 selected visual object min-button assignment shipped: designer commands can assign caller-provided logical `MinButton` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#889 selected visual object max-button assignment shipped: designer commands can assign caller-provided logical `MaxButton` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#888 selected visual object fill-style assignment shipped: designer commands can assign caller-provided non-negative numeric `FillStyle` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#887 selected visual object draw-width assignment shipped: designer commands can assign caller-provided non-negative numeric `DrawWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#886 selected visual object draw-style assignment shipped: designer commands can assign caller-provided non-negative numeric `DrawStyle` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#885 selected visual object draw-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `DrawMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#884 selected visual object curvature assignment shipped: designer commands can assign caller-provided non-negative numeric `Curvature` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#883 selected visual object control-box assignment shipped: designer commands can assign caller-provided logical `ControlBox` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#882 selected visual object closable assignment shipped: designer commands can assign caller-provided logical `Closable` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#881 selected visual object button-count assignment shipped: designer commands can assign caller-provided non-negative numeric `ButtonCount` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#880 selected visual object selected fore-color assignment shipped: designer commands can assign caller-provided non-negative numeric `SelectedForeColor` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#879 selected visual object selected back-color assignment shipped: designer commands can assign caller-provided non-negative numeric `SelectedBackColor` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#878 selected visual object allow-cell-selection assignment shipped: designer commands can assign caller-provided logical `AllowCellSelection` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#877 selected visual object hide-selection assignment shipped: designer commands can assign caller-provided logical `HideSelection` state through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#876 selected visual object OLE drop text-insertion assignment shipped: designer commands can assign caller-provided non-negative numeric `OLEDropTextInsertion` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#875 selected visual object OLE drop-effects assignment shipped: designer commands can assign caller-provided non-negative numeric `OLEDropEffects` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#874 selected visual object OLE drop-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `OLEDropMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#873 selected visual object OLE drag-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `OLEDragMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#872 selected visual object drag-mode assignment shipped: designer commands can assign caller-provided non-negative numeric `DragMode` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#871 selected visual object drag-icon assignment shipped: designer commands can assign caller-provided `DragIcon` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#870 selected visual object mouse-icon assignment shipped: designer commands can assign caller-provided `MouseIcon` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#869 selected visual object picture-selection-display assignment shipped: designer commands can assign caller-provided non-negative numeric `PictureSelectionDisplay` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#868 selected visual object picture-spacing assignment shipped: designer commands can assign caller-provided non-negative numeric `PictureSpacing` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#867 selected visual object picture-position assignment shipped: designer commands can assign caller-provided non-negative numeric `PicturePosition` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#866 selected visual object picture-margin assignment shipped: designer commands can assign caller-provided non-negative numeric `PictureMargin` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#865 selected visual object mouse-pointer assignment shipped: designer commands can assign caller-provided non-negative numeric `MousePointer` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#864 selected visual object special-effect assignment shipped: designer commands can assign caller-provided non-negative numeric `SpecialEffect` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#863 selected visual object border-color assignment shipped: designer commands can assign caller-provided non-negative numeric `BorderColor` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#862 selected visual object border-width assignment shipped: designer commands can assign caller-provided non-negative numeric `BorderWidth` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#861 selected visual object border-style assignment shipped: designer commands can assign caller-provided non-negative numeric `BorderStyle` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#860 selected visual object back-style assignment shipped: designer commands can assign caller-provided non-negative numeric `BackStyle` values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving unquoted numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#859 selected visual object OLE drag-picture assignment shipped: designer commands can assign caller-provided `OLEDragPicture` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#858 selected visual object disabled-picture assignment shipped: designer commands can assign caller-provided `DisabledPicture` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#857 selected visual object down-picture assignment shipped: designer commands can assign caller-provided `DownPicture` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#856 selected visual object picture assignment shipped: designer commands can assign caller-provided `Picture` text/path values through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#855 selected visual object always-on-bottom assignment shipped: designer commands can assign caller-provided logical `AlwaysOnBottom` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#854 selected visual object always-on-top assignment shipped: designer commands can assign caller-provided logical `AlwaysOnTop` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#853 selected visual object add-line-feeds assignment shipped: designer commands can assign caller-provided logical `AddLineFeeds` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#852 selected visual object sparse-state assignment shipped: designer commands can assign caller-provided logical `Sparse` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#851 selected visual object current-control assignment shipped: designer commands can assign caller-provided `CurrentControl` text to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#850 selected visual object dynamic current-control assignment shipped: designer commands can assign caller-provided raw `DynamicCurrentControl` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#849 selected visual object dynamic line-height assignment shipped: designer commands can assign caller-provided raw `DynamicLineHeight` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#848 selected visual object dynamic input-mask assignment shipped: designer commands can assign caller-provided raw `DynamicInputMask` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#847 selected visual object dynamic-alignment assignment shipped: designer commands can assign caller-provided raw `DynamicAlignment` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#846 selected visual object dynamic font-shadow assignment shipped: designer commands can assign caller-provided raw `DynamicFontShadow` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#845 selected visual object dynamic font-outline assignment shipped: designer commands can assign caller-provided raw `DynamicFontOutline` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#844 selected visual object dynamic font-strikethru assignment shipped: designer commands can assign caller-provided raw `DynamicFontStrikethru` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#843 selected visual object dynamic font-underline assignment shipped: designer commands can assign caller-provided raw `DynamicFontUnderline` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#842 selected visual object dynamic font-italic assignment shipped: designer commands can assign caller-provided raw `DynamicFontItalic` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#841 selected visual object dynamic font-bold assignment shipped: designer commands can assign caller-provided raw `DynamicFontBold` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#840 selected visual object dynamic font-size assignment shipped: designer commands can assign caller-provided raw `DynamicFontSize` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#839 selected visual object dynamic font-name assignment shipped: designer commands can assign caller-provided raw `DynamicFontName` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#838 selected visual object font-shadow assignment shipped: designer commands can assign caller-provided logical `FontShadow` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#837 selected visual object font-outline assignment shipped: designer commands can assign caller-provided logical `FontOutline` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#836 selected visual object font-strikethru assignment shipped: designer commands can assign caller-provided logical `FontStrikethru` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#835 selected visual object font-underline assignment shipped: designer commands can assign caller-provided logical `FontUnderline` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#834 selected visual object font-italic assignment shipped: designer commands can assign caller-provided logical `FontItalic` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#833 selected visual object font-bold assignment shipped: designer commands can assign caller-provided logical `FontBold` state to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving FoxPro logical serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#832 selected visual object font-size assignment shipped: designer commands can assign caller-provided finite non-negative `FontSize` values, including fractional sizes, to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving compact numeric serialization, duplicate selection rejection, and rollback-safe batch writes.
- E2/#831 selected visual object font-name assignment shipped: designer commands can assign caller-provided `FontName` text to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#830 selected visual object dynamic fore-color assignment shipped: designer commands can assign caller-provided raw `DynamicForeColor` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#829 selected visual object dynamic back-color assignment shipped: designer commands can assign caller-provided raw `DynamicBackColor` expressions to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#828 selected visual object disabled fore-color assignment shipped: designer commands can assign caller-provided numeric `DisabledForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#827 selected visual object disabled back-color assignment shipped: designer commands can assign caller-provided numeric `DisabledBackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#826 selected visual object fore-color assignment shipped: designer commands can assign caller-provided numeric `ForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#825 selected visual object back-color assignment shipped: designer commands can assign caller-provided numeric `BackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#824 selected visual object highlight fore-color assignment shipped: combo/list designer commands can assign caller-provided numeric `HighlightForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#823 selected visual object highlight back-color assignment shipped: combo/list designer commands can assign caller-provided numeric `HighlightBackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#822 selected visual object item fore-color assignment shipped: combo/list designer commands can assign caller-provided numeric `ItemForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#821 selected visual object item back-color assignment shipped: combo/list designer commands can assign caller-provided numeric `ItemBackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#820 selected visual object disabled-item fore-color assignment shipped: combo/list designer commands can assign caller-provided numeric `DisabledItemForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#819 selected visual object disabled-item back-color assignment shipped: combo/list designer commands can assign caller-provided numeric `DisabledItemBackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#818 selected visual object selected-item fore-color assignment shipped: combo/list designer commands can assign caller-provided numeric `SelectedItemForeColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#817 selected visual object selected-item back-color assignment shipped: combo/list designer commands can assign caller-provided numeric `SelectedItemBackColor` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#816 selected visual object display-value assignment shipped: combo/list designer commands can assign caller-provided `DisplayValue` text to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#815 selected visual object list-index assignment shipped: combo/list designer commands can assign caller-provided numeric `ListIndex` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#814 selected visual object style assignment shipped: combo/list designer commands can assign caller-provided numeric `Style` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#813 selected visual object multi-select assignment shipped: list designer commands can assign FoxPro logical `.T.`/`.F.` `MultiSelect` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#812 selected visual object incremental-search assignment shipped: combo/list designer commands can assign FoxPro logical `.T.`/`.F.` `IncrementalSearch` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#811 selected visual object integral-height assignment shipped: combo/list designer commands can assign FoxPro logical `.T.`/`.F.` `IntegralHeight` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#810 selected visual object column-lines assignment shipped: combo/list designer commands can assign FoxPro logical `.T.`/`.F.` `ColumnLines` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving duplicate selection rejection and rollback-safe batch writes.
- E2/#809 selected visual object column-widths assignment shipped: combo/list designer commands can assign caller-provided `ColumnWidths` text to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while preserving quoted serialized string values, duplicate selection rejection, and rollback-safe batch writes.
- E2/#808 selected visual object column-count assignment shipped: combo/list designer commands can assign caller-provided numeric `ColumnCount` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#807 selected visual object bound-column assignment shipped: combo/list designer commands can assign caller-provided numeric `BoundColumn` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#806 selected visual object row-source-type assignment shipped: combo/list designer commands can assign caller-provided numeric `RowSourceType` values to selected objects through direct fields, existing serialized properties, and writable absent memo-backed properties while rejecting negative values, duplicate selections, and rollback-unsafe failures.
- E2/#805 selected visual object row-source assignment shipped: combo/list designer commands can assign caller text to selected `RowSource` values, sharing the text-property path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#804 selected visual object format assignment shipped: designer commands can assign caller text to selected `Format` values, sharing the text-property path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#803 selected visual object input-mask assignment shipped: designer commands can assign caller text to selected `InputMask` values, sharing the text-property path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#802 selected visual object control-source assignment shipped: designer commands can assign caller text to selected `ControlSource` values, sharing the text-property path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#801 selected visual object status-bar text assignment shipped: designer commands can assign caller text to selected `StatusBarText` values, sharing the text-property path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#800 selected visual object tooltip text assignment shipped: designer commands can assign caller text to selected `ToolTipText` values, sharing the caption text preflight path for direct fields, serialized properties, absent memo-backed properties, duplicate selection rejection, and rollback-safe batch writes.
- E2/#799 selected visual object caption assignment shipped: designer commands can assign caller text to selected `Caption` values, storing raw DBF field text for direct fields and quoted VFP string literals for serialized properties while preserving rollback-safe batch writes.
- E2/#798 selected visual object locked-state assignment shipped: designer commands can assign FoxPro logical `.T.`/`.F.` `LOCKED` values to selected objects, including existing serialized properties, while rejecting duplicate selections before rollback-safe batch writes.
- E2/#797 selected visual object read-only assignment shipped: designer commands can assign FoxPro logical `.T.`/`.F.` `READONLY` values to selected objects, including existing serialized properties, while rejecting duplicate selections before rollback-safe batch writes.
- E2/#796 selected visual object enabled-state assignment shipped: designer commands can assign FoxPro logical `.T.`/`.F.` `ENABLED` values to selected objects, including existing serialized properties, while rejecting duplicate selections before rollback-safe batch writes.
- E2/#795 selected visual object visibility assignment shipped: designer visibility commands can assign FoxPro logical `.T.`/`.F.` `VISIBLE` values to selected objects while rejecting duplicate selections before rollback-safe batch writes.
- E2/#794 selected visual object tab-stop assignment shipped: designer tab-navigation commands can assign FoxPro logical `.T.`/`.F.` `TABSTOP` values to selected objects while rejecting duplicate selections before rollback-safe batch writes.
- E2/#793 selected visual object tab-order assignment shipped: designer tab-order commands can assign sequential `TABINDEX` values to selected objects in caller-provided order while rejecting duplicate selections before rollback-safe batch writes.
- E2/#792 selected visual object nudging shipped: designer nudge commands can move selected objects by horizontal `HPOS`, vertical `VPOS`, or both-axis deltas while preserving unrelated objects and routing coordinate writes through rollback-safe batch edits.
- E2/#791 selected visual object snap-to-grid shipped: designer snap commands can round selected objects to horizontal `HPOS`, vertical `VPOS`, or both-axis grid increments while preserving unrelated objects and routing coordinate writes through rollback-safe batch edits.
- E2/#790 selected visual object distribution shipped: designer distribute commands can space selected objects evenly by horizontal `HPOS` or vertical `VPOS`, preserving endpoints and routing interior coordinate writes through rollback-safe batch edits.
- E2/#789 selected visual object ungrouping shipped: designer ungroup commands can move immediate children to the container parent, clear root-level child parents, and mark the container deleted while preserving rollback and undo behavior for parent writes.
- E2/#788 selected visual object grouping shipped: designer grouping can create a container row and reparent selected objects under it while restoring original table/memo bytes on invalid containers or failed selections.
- E2/#787 selected visual object resize-to-anchor shipped: designer resize commands can set selected objects to an anchor width, height, or full size through `WIDTH`/`HEIGHT` while preserving batch rollback and undo semantics.
- E2/#786 selected visual object alignment shipped: designer alignment commands can align multiple selected objects to an anchor by left, right, top, bottom, horizontal-center, or vertical-center using `HPOS`/`VPOS`/`WIDTH`/`HEIGHT` while preserving batch rollback and undo semantics.
- E2/#785 batch selected visual object duplication shipped: designer copy/paste workflows can duplicate multiple selected rows in one rollback-safe operation, returning duplicate record indexes while preserving copied memo-backed properties, METHODS content, hierarchy/class metadata, and existing deleted flags.
- E2/#784 batch toolbox visual object creation shipped: designer/toolbox workflows can append multiple visual object rows atomically, returning created record indexes while validating field names and identity collisions across existing live/deleted rows plus pending creates before any write.
- E2/#783 batch selected visual object z-order reorders shipped: designer z-order workflows can reorder multiple selected visual objects in one atomic operation, supporting record-index, object-name, and `UNIQUEID` source selectors plus front/back/before/after placements while preserving deleted flags, memo-backed fields, and prior state on failed batches.
- E2/#782 batch selected visual object identity renames shipped: designer naming workflows can update `OBJNAME`, `NAME`, and `UNIQUEID` across multiple selected objects in one rollback-safe operation, preserving live/deleted identity collision checks, prior undo history when later items fail, and successful batch undo support.
- E2/#781 batch selected visual object reparenting shipped: container editors can reparent multiple selected objects in one rollback-safe operation, supporting parent object-name selection, parent `UNIQUEID` selection, root-level parent clearing, prior undo preservation when later items fail, and successful batch undo support.
- E2/#780 batch selected visual object method reorders shipped: method/event editors can reorder `PROCEDURE` and `FUNCTION` declarations across selected objects in one rollback-safe operation, supporting first/last/before/after placements while preserving method kind/body, unrelated methods, prior undo history when later items fail, and successful batch undo support.
- E2/#779 batch selected visual object method moves shipped: method/event editors can move `PROCEDURE` and `FUNCTION` declarations across selected objects in one rollback-safe operation, including target renames and explicit replacement while deleting sources, preserving unrelated methods, prior undo history when later items fail, and successful batch undo support.
- E2/#778 batch selected visual object method copies shipped: method/event editors can copy `PROCEDURE` and `FUNCTION` declarations across selected objects in one rollback-safe operation, including target renames and explicit replacement while preserving sources, unrelated methods, prior undo history when later items fail, and successful batch undo support.
- E2/#777 batch selected visual object method renames shipped: method/event editors can rename `PROCEDURE` and `FUNCTION` declarations across selected objects in one rollback-safe operation, preserving method kind/body and unrelated methods, rejecting missing/ambiguous/colliding/malformed batch items explicitly, preserving prior undo history when later items fail, and retaining successful batch undo support.
- E2/#776 batch selected visual object method deletes shipped: method/event editors can delete `PROCEDURE` and `FUNCTION` declarations across selected objects in one rollback-safe operation, preserving unrelated methods, rejecting missing/ambiguous/malformed batch items explicitly, preserving prior undo history when later items fail, and retaining successful batch undo support.
- E2/#775 batch selected visual object property moves shipped: multi-select property grids can move direct DBF-backed fields and memo-backed `PROPERTIES` assignments across selected objects in one rollback-safe operation, including target renames and explicit replacement while clearing sources, preserving undo support, and preserving prior undo history when later items fail.
- E2/#774 batch selected visual object property copies shipped: multi-select property grids can copy direct DBF-backed fields and memo-backed `PROPERTIES` assignments across selected objects in one rollback-safe operation, including target renames and explicit replacement while preserving sources, undo support, and prior undo history when later items fail.
- E2/#773 batch selected visual object memo-property reorders shipped: multi-select property grids can reorder memo-backed `PROPERTIES` assignments across selected objects in one rollback-safe operation, supporting first/last/before/after placements while preserving values, undo support, and prior undo history when later items fail.
- E2/#772 batch selected visual object memo-property renames shipped: multi-select property grids can rename memo-backed `PROPERTIES` assignments across selected objects in one rollback-safe operation, preserving values, case-insensitive source matching, undo support, and prior undo history when later items fail.
- E2/#771 batch selected visual object property clears shipped: multi-select property grids can clear direct DBF-backed fields and memo-backed `PROPERTIES` assignments across selected objects in one rollback-safe operation, preserving missing memo assignment no-op behavior, undo support, and prior undo history when later items fail.
- E2/#770 selected visual object memo-property reordering shipped: property grids can move one memo-backed `PROPERTIES` assignment to first, last, before another assignment, or after another assignment on a selected object by record-index, object-name, or `UNIQUEID`, preserving values and undo support while rejecting direct DBF fields and invalid reorder cases without mutation.
- E2/#769 selected visual object memo-property renaming shipped: property grids can rename one memo-backed `PROPERTIES` assignment on a selected object by record-index, object-name, or `UNIQUEID`, preserving values and undo support while rejecting direct DBF fields, empty names, missing sources, duplicate sources, target collisions, same-name renames, and missing `PROPERTIES` carriers.
- E2/#768 selected visual object property moves shipped: property grids can move direct DBF-backed or memo-backed `PROPERTIES` values between selected objects by record-index, object-name, or `UNIQUEID`, optionally rename target properties, require explicit replacement for existing targets, reject self-moves, clear sources only after target writes, and preserve undo support.
- E2/#767 selected visual object property copying shipped: property grids can copy direct DBF-backed or memo-backed `PROPERTIES` values from one selected object to another by record-index, object-name, or `UNIQUEID`, optionally rename the target property, require explicit replacement for existing targets, and preserve undo support.
- E2/#766 selected visual object property clearing shipped: property grids can clear direct DBF fields to empty values and remove memo-backed `PROPERTIES` assignments by selected record, object name, or `UNIQUEID`, with missing memo assignments treated as no-op/no-undo and successful clears remaining undo-backed.
- E2/#765 selected visual object method reordering shipped: method/event editors can move selected `PROCEDURE` or `FUNCTION` declarations to first, last, before another method, or after another method inside one selected object's `METHODS` memo while preserving bodies, kinds, unrelated methods, and undo support.
- E2/#764 selected visual object method moves shipped: method/event editors can move one selected `PROCEDURE` or `FUNCTION` between objects, optionally rename or replace the target method, delete the source method only after the target write succeeds, reject self-overwrites, and preserve undo-backed restoration.
- E2/#763 selected visual object method copying shipped: method/event editors can copy one selected object's `PROCEDURE` or `FUNCTION` into another selected object by record-index, object-name, or `UNIQUEID`, optionally rename or replace the target method, preserve target declaration kind on replacement, keep unrelated methods intact, and read full memo bodies without the prior 160-byte display cap.
- E2/#762 selected visual object method query shipped: method/event editors can open one selected `PROCEDURE` or `FUNCTION` declaration by name with source metadata, resolved record/deleted state, not-found semantics, ambiguity checks, and read-only behavior.
- E2/#761 selected visual object method renaming shipped: method/event editors can rename `PROCEDURE` and `FUNCTION` declarations in selected object `METHODS` memos while preserving bodies, rejecting collisions, and retaining undo support.
- E2/#760 selected visual object subtree duplication shipped: container copy/paste workflows can duplicate a selected root plus descendants with per-row replacement identities, copied parent rewrites, memo preservation, deleted-state preservation, and collision-safe validation before mutation.
- E2/#759 selected visual object ancestor listing shipped: container breadcrumbs can enumerate parent chains from immediate parent to root with depth metadata, ambiguity detection, cycle protection, and read-only behavior.
- E2/#758 selected visual object subtree deleted-state changes shipped: container delete/restore workflows can mark a selected object and all descendants deleted or live through rollback-safe batch flag updates.
- E2/#757 selected visual object descendant listing shipped: container trees and group operations can enumerate full descendant subtrees in pre-order with depth metadata, deleted-row visibility, and cycle/duplicate-entry protection.
- E2/#756 selected visual object child listing shipped: container editors can enumerate immediate children of a selected parent using existing selectors, including deleted child rows and outline metadata without creating undo history.
- E2/#755 selected visual object z-order reordering shipped: designers can move rows to front/back or before/after resolved target objects while preserving field values, memo contents, deleted flags, and stable selector behavior after record indexes change.
- E2/#754 selected visual object identity renaming shipped: designer naming workflows can update `OBJNAME`, `NAME`, and `UNIQUEID` together with collision checks across live and deleted rows while preserving undo-backed property semantics.
- E2/#753 rollback-safe batch visual object deleted states shipped: multi-select designer delete/restore operations can now mark multiple objects deleted or live while restoring prior flags if a later selection fails.
- E2/#752 rollback-safe batch visual object edits shipped: multi-select designer operations can now apply per-object geometry/property changes through existing undo semantics and roll back all successful batch changes when a later item fails.
- E2/#751 selected visual object reparenting shipped: container-editing workflows can move selected objects under another resolved parent object or clear the parent for root placement while rejecting missing parents and self-parenting through the existing undo-backed property path.
- E2/#750 toolbox visual object creation shipped: designers can append a live visual object row from caller-provided field values, including memo-backed properties and methods, with unknown-field rejection, identity collision safety, and deleted-row preservation.
- E2/#749 selected visual object duplication shipped: designer copy/paste workflows can append a live copy of a selected object with caller-provided replacement `OBJNAME`/`NAME`/`UNIQUEID` values while preserving existing properties, methods, hierarchy metadata, and identity collision safety.
- E2/#748 selected visual object method deletion shipped: method/event editors can now remove existing `METHODS` memo procedure/function blocks by selected object and method name while preserving unrelated methods and existing undo behavior.
- E2/#747 selected visual object method editing shipped: method/event editors can now update existing `METHODS` memo declarations or append new procedure/function declarations through the same selector and undo surfaces used by visual property edits.
- E2/#746 selected visual object method listing shipped: context-aware method/event editors can now enumerate `PROCEDURE` and `FUNCTION` declarations from a selected record's `METHODS` memo with source text, declaration line, memo block, and resolved record metadata.
- E2/#745 visual object outlines now include parsed `Caption` values from `PROPERTIES` memo assignments, giving designer selection panes user-facing labels while preserving existing identity metadata.
- E2/#744 visual object outlines now include hierarchy/class metadata, exposing trimmed `PARENT`, `CLASS`, and `BASECLASS` values when present while leaving missing metadata empty.
- E2/#743 visual object outline listing shipped: designer selection panes can now enumerate DBF visual object records with record indexes, deleted flags, `OBJNAME`/fallback `NAME` selection names, and stable `UNIQUEID` values.
- E2/#742 selector-aware visual property query/list results now report whether the resolved selected DBF record is deleted, so designer shells can display live versus tombstoned visual objects.
- E2/#741 selected visual object deleted-state edits shipped: designer delete/restore interactions can now set DBF deleted flags through record-index, object-name, or `UNIQUEID` selection without mutating unrelated records.
- E2/#740 failed multi-property visual edits now roll back earlier successful changes, restoring selected-object geometry and leaving no extra undo history when a later batch property fails.
- E2/#739 visual property query/list results now expose the resolved DBF record index so designer shells can reconcile object-name and `UNIQUEID` selections with the underlying asset row.
- E2/#738 visual property listings now carry editor metadata: direct entries expose DBF field types and memo-backed `PROPERTIES` assignments expose parsed source line indexes for context-aware property-grid editors.
- E2/#737 selector-aware visual property listing shipped: property-grid callers can now enumerate a selected object's direct DBF-backed fields plus parsed memo-backed `PROPERTIES` assignments without duplicating the raw carrier field or creating undo history.
- E2/#736 selector-aware visual property queries shipped: property-grid/context-aware editor callers can now read selected object property values using record-index, object-name, or `UNIQUEID` selectors with case-insensitive property-name resolution and no undo history.
- E2/#735 multi-property visual asset edits shipped: designer drag/resize-style callers can now submit multiple property changes through one selected-object request, reusing record-index, object-name, and `UNIQUEID` targeting with existing per-property undo compatibility.
- E2/#734 visual property names now match case-insensitively: direct DBF fields and memo-backed PROPERTIES assignments can be edited with VFP-style casing flexibility while preserving existing memo property-name spelling and undo behavior.
- E2/#733 unchanged visual property edits now behave as no-ops: memo-backed and direct DBF-field property updates that already match the requested value return success without rewriting asset bytes or creating undo history.
- E2/#732 UNIQUEID-targeted visual property edits shipped: designer/property-grid edits can now resolve selected objects by case-insensitive stable `UNIQUEID`, disambiguating duplicate names while preserving record-index, object-name, and undo behavior.
- E2/#731 object-name-targeted direct field edits shipped: selected-object property updates now have focused coverage for direct DBF fields such as HPOS, preserving unrelated objects and undoing via the resolved record index.
- E2/#730 object-name-targeted visual property edits shipped: visual asset property updates can now resolve selected objects by case-insensitive OBJNAME or fallback NAME, reject ambiguous/missing names, and preserve existing record-index and undo behavior.
- E1/#22 shared design model and memo-heavy round-trip preservation closure audit shipped: all prompt-sized shared design-model children #658-#729 are closed, and full validation passes with all 35 configured tests.
- E1/#729 report section id provenance shipped: report/label section snapshots now expose explicit missing-field and block-zero provenance for synthesized section ids while preserving OBJCODE-backed title and band-kind provenance.
- E1/#728 report document title provenance shipped: report/label layout snapshots now expose explicit missing-field and block-zero provenance for display-name-derived document titles, including unavailable non-report layouts.
- E1/#727 project group summary provenance shipped: aggregate project groups now expose source field and memo block metadata for group ids and titles, inheriting the first entry that establishes each group while preserving counts and sorting.
- E1/#726 build-plan project title provenance shipped: project build plans now mirror workspace project-title source field and memo block metadata, including fallback titles and live-header selection.
- E1/#725 project logical flag memo block provenance shipped: project entries and build plans now expose source memo block numbers for EXCLUDE, MAINPROG, LOCAL, DEBUG, ENCRYPT, SAVECODE, and NOLOGO flag metadata, including missing-field block-zero behavior.
- E1/#724 Studio raw type/platform memo block provenance shipped: generic object snapshots now expose source memo block numbers for raw OBJTYPE, OBJCODE, and PLATFORM metadata, including trimmed platform values and missing-field block-zero behavior.
- E1/#723 report object geometry memo block provenance shipped: report/label layout objects now expose source memo block numbers for object kind, raw OBJTYPE/OBJCODE, and HPOS/VPOS/WIDTH/HEIGHT metadata, including missing-field and deleted-object cases.
- E1/#722 report section memo block provenance shipped: report/label section snapshots now expose source memo block numbers for band title/kind, raw OBJCODE, VPOS, and HEIGHT metadata, including deleted sections.
- E1/#721 xAsset lifecycle memo block provenance shipped: startup/shutdown lifecycle steps now expose source memo block numbers for method-backed form/menu steps while synthetic activation and preview steps report block zero.
- E1/#720 xAsset action memo block provenance shipped: action bindings now expose memo block provenance for title sources and executable routine sources, including form method actions, menu command/procedure actions, and synthetic submenu actions.
- E1/#719 xAsset method memo block provenance shipped: extracted form/class methods, wrapped menu methods, embedded menu routines, and synthetic submenu activation methods now expose source memo block provenance alongside field and line metadata.
- E1/#718 Studio menu metadata memo block provenance shipped: normalized menu prompt, level, command, and message metadata now expose source memo block numbers, including unresolved placeholders and missing-field block-zero behavior.
- E1/#717 Studio object identity memo block provenance shipped: normalized object identity and display metadata now expose source memo block numbers for object names, hierarchy/class fields, titles, and subtitles while synthesized/suppressed metadata reports block zero.
- E1/#716 report layout object memo block provenance shipped: layout object titles and expressions now expose source memo block numbers, including selected EXPR titles, unresolved expression placeholders, deleted objects, and synthesized title fallbacks.
- E1/#715 project entry memo block provenance shipped: project entries and build-plan startup selections now expose source memo block numbers for entry names, relative paths, classifications/groups, keys, comments, and startup items while preserving placeholder suppression.
- E1/#714 project header memo block provenance shipped: project workspace and build-plan header metadata now expose source memo block numbers for project keys, home directories, output paths, and output-derived build labels while synthesized fallbacks report block zero.
- E1/#713 report named-value memo block provenance shipped: report settings and layout-object highlights now expose source memo block numbers for parsed EXPR values and direct fields, including deleted root settings, while preserving placeholder suppression.
- E1/#712 Studio property memo block provenance shipped: object property snapshots now expose source memo block numbers for direct DBF fields and blob-derived properties, preserving decoded, unresolved, and block-zero memo provenance without changing display values.
- E1/#711 DBF memo block provenance shipped: parsed DBF record values now expose stored memo block numbers for memo-backed fields, including unresolved/missing sidecar placeholders and empty block-zero memo pointers, while non-memo fields report block zero.
- E1/#710 report/label preview lifecycle provenance coverage shipped: focused xAsset tests now prove preview lifecycle steps mirror legacy startup lines, use preview kind metadata, and carry explicit missing source sentinels.
- E1/#709 xAsset lifecycle step provenance shipped: startup/shutdown plans now expose structured lifecycle steps with method source provenance and synthetic activation/preview commands using explicit missing sentinels while preserving legacy line/routine vectors.
- E1/#708 xAsset action binding provenance shipped: action bindings now expose title source field provenance plus routine source field/line provenance inherited from method-backed actions, with synthetic submenu routines using missing sentinels.
- E1/#707 xAsset method source-line provenance shipped: executable xAsset methods now retain memo declaration line indexes, wrapped field methods start at line zero, and synthetic submenu methods use the missing-line sentinel.
- E1/#706 xAsset method source-field provenance shipped: executable xAsset methods now retain the DBF field ordinal for form/class `METHODS` blobs and menu method fields, with synthetic submenu methods using the missing-field sentinel.
- E1/#705 Studio display metadata trimming shipped: generic object/menu snapshots now trim normalized active display metadata and ignore whitespace-only fallback candidates while preserving direct DBF property values unchanged.
- E1/#704 PJX project type-code normalization shipped: project workspace header detection and item classification now trim and uppercase `TYPE` codes for stable metadata.
- E1/#703 VFP project logical flag normalization shipped: PJX/PJT logical fields now parse trim- and case-insensitive VFP true values for build and entry flags.
- E1/#702 VFP-aware Studio display names shipped: `open_document` now derives display names from backslash-aware VFP filename parsing instead of host-only filesystem semantics.
- E1/#701 UNC project item path coverage shipped: focused project-workspace tests now prove UNC item paths normalize to project-relative text under the project directory and filename fallbacks outside it.
- E1/#700 VFP absolute project item path normalization shipped: PJX/PJT entries now normalize Windows/UNC-style absolute item paths independent of host OS, preserving project-relative paths when possible and filename fallbacks otherwise.
- E1/#699 deleted xAsset record handling shipped: executable xAsset models now skip DBF-deleted design rows for active root paths, methods, menu actions, shortcut detection, and activation fallback behavior.
- E1/#698 xAsset menu activation path-stem handling shipped: non-shortcut menu activation targets now use VFP/Windows-aware filename stems instead of POSIX-only path parsing.
- E1/#697 unresolved xAsset memo placeholder handling shipped: executable xAsset models now treat unresolved `<memo block N>` placeholders as absent text for object paths, wrapped method bodies, menu action titles, and submenu targets.
- E1/#696 unresolved object memo placeholder handling shipped: generic studio object snapshots now treat unresolved `<memo block N>` placeholders as absent text for active object/menu metadata, direct property values, and visual property-blob expansion.
- E1/#695 unresolved report memo placeholder handling shipped: FRX/LBX layout normalization now treats unresolved `<memo block N>` placeholders as absent text instead of active settings, object titles, expressions, or highlights.
- E1/#694 unresolved project memo placeholder handling shipped: PJX/PJT workspace normalization now treats unresolved `<memo block N>` placeholders as absent text instead of active project metadata, with Windows-style path stems used for fallback titles/outputs.
- E1/#693 deleted project startup candidate handling shipped: project build plans now prefer live startup/program entries while retaining deleted-only fallback compatibility and deleted-entry visibility.
- E1/#692 live project-header selection shipped: project workspace metadata now prefers non-deleted PJX/PJT header records while deleted headers remain visible in preserved entries and deleted-entry counts.
- E1/#691 deleted report root settings shipped: report/label layout snapshots now preserve DBF-deleted root settings in a dedicated collection without mixing them into live report settings.
- E1/#690 deleted report layout sections shipped: report/label layout snapshots now preserve DBF-deleted band records in a dedicated collection without changing visible section grouping.
- E1/#689 deleted report layout objects shipped: report/label layout snapshots now preserve DBF-deleted layout object records in a dedicated collection without changing visible section grouping.
- E1/#688 object snapshot deleted-state coverage shipped: studio object snapshot tests now prove deleted menu records remain visible with their DBF deleted flag and title provenance.
- E1/#687 project build deleted-entry counts shipped: `StudioProjectBuildPlan` summaries now expose deleted-source-row counts while keeping existing total and excluded item counts unchanged.
- E1/#686 project group deleted-entry counts shipped: project workspace group summaries now expose deleted-source-row counts while keeping existing item and excluded counts unchanged.
- E1/#685 project entry deleted-state preservation shipped: project workspace entries now retain DBF deleted-record state so designer tooling can see deleted PJX/PJT rows without changing grouping or build behavior.
- E1/#684 visual property blob line provenance shipped: parsed visual property assignments now preserve source memo line indexes, and studio property snapshots expose them for blob-derived properties while marking direct DBF fields with `StudioObjectMissingLineIndex`.
- E1/#683 project build output classification provenance shipped: `StudioProjectBuildPlan` now preserves source DBF field ordinals for `output_kind` and `build_target`, using `StudioProjectMissingFieldIndex` for synthesized output-path fallbacks.
- E1/#682 report layout classification provenance shipped: report/label layout snapshots now preserve source DBF field ordinals for normalized section title/band labels and layout object kind labels.
- E1/#681 project build startup provenance shipped: `StudioProjectBuildPlan` now preserves the source DBF field ordinal for the selected startup item, including excluded-asset fallback selections.
- E1/#680 project entry classification provenance shipped: project workspace entries now preserve source DBF field ordinals for normalized type/group classification metadata, distinguishing `TYPE`-derived and `NAME`-extension-derived classifications.
- E1/#679 project entry relative-path provenance shipped: `StudioProjectEntry` now preserves the source DBF field ordinal for normalized `relative_path` values, using `StudioProjectMissingFieldIndex` when absent.
- E1/#678 workspace project metadata provenance shipped: `StudioProjectWorkspace` now preserves source DBF field ordinals for project title/key, home directory, and output path metadata, using `StudioProjectMissingFieldIndex` for synthesized fallbacks.
- E1/#677 project entry flag provenance shipped: project workspace entries now preserve source DBF field ordinals for `EXCLUDE`, `MAINPROG`, and `LOCAL` flags, using `StudioProjectMissingFieldIndex` when absent.
- E1/#676 report setting memo-line provenance shipped: parsed report/label root `EXPR` settings now preserve source memo line indexes, with direct settings marked by `StudioReportMissingLineIndex`.
- E1/#675 report layout object title provenance shipped: report/label layout object snapshots now preserve source DBF field ordinals for friendly titles, using `StudioReportMissingFieldIndex` for synthesized titles.
- E1/#674 report layout missing-field provenance shipped: report/label layout snapshots now use an explicit `StudioReportMissingFieldIndex` sentinel instead of conflating absent fields with DBF ordinal `0`.
- E1/#673 object title/subtitle provenance shipped: Studio object snapshots now preserve source DBF field ordinals for friendly title and subtitle display values, including synthesized-title sentinel behavior.
- E1/#672 object identity/hierarchy provenance shipped: Studio object snapshots now preserve source DBF field ordinals for normalized object name, unique ID, parent, class, and baseclass metadata, including fallback-selected fields.
- E1/#671 raw visual metadata provenance shipped: Studio object snapshots now preserve source DBF field ordinals for raw `OBJTYPE`, `OBJCODE`, and `PLATFORM` metadata, using the explicit object missing-field sentinel when absent.
- E1/#670 object snapshot missing-field provenance shipped: normalized menu metadata now uses an explicit missing-field sentinel instead of conflating absent MNX/MNT fields with DBF field ordinal `0`.
- E1/#669 menu metadata provenance shipped: normalized menu prompt, level, command, and message fields now preserve source DBF field ordinals for round-trip-safe MNX/MNT designer edits.
- E1/#668 menu metadata normalization shipped: Studio object snapshots now expose menu prompt, level name, command, and message metadata for MNX/MNT designer workflows while preserving raw codes and field properties.
- E1/#667 visual object raw-code preservation shipped: Studio object snapshots now expose raw visual-record `OBJTYPE`, `OBJCODE`, and `PLATFORM` metadata for form/class designer round-trip workflows.
- E1/#666 report raw type-code preservation shipped: report/label section and layout-object snapshots now retain raw `OBJTYPE`/`OBJCODE` numeric values alongside derived UI labels for round-trip-safe designer workflows.
- E1/#665 report-object provenance shipped: report/label layout object snapshots now preserve source field ordinals for `OBJTYPE`, `EXPR`, `HPOS`, `VPOS`, `WIDTH`, and `HEIGHT`, supporting round-trip-safe object expression and geometry edits.
- E1/#664 report-section provenance shipped: report/label section snapshots now preserve source field ordinals for `OBJCODE`, `VPOS`, and `HEIGHT`, supporting round-trip-safe section geometry edits.
- E1/#663 project build-plan provenance shipped: normalized PJX build plans now preserve header field ordinals for project key, home directory, output path, and build flags, including explicit missing-field metadata.
- E1/#662 project-entry provenance shipped: normalized PJX workspace entries now preserve DBF field ordinals for type, name, key, and comments, with explicit missing-field metadata for absent source fields.
- E1/#661 report-layout provenance shipped: report/label layout settings and object highlights now carry their source record index and DBF field ordinal, preserving enough metadata for round-trip-safe designer edits.
- E1/#660 design-object identity metadata shipped: Studio object snapshots now expose object name, UNIQUEID, parent, class, and baseclass fields from DBF-family visual records for shared designer navigation and round-trip fidelity.
- E1/#659 design-property provenance shipped: Studio property snapshots now preserve original DBF field ordinals and mark values expanded from visual `PROPERTIES` blobs, giving designer workflows enough metadata to distinguish direct fields from parsed blob assignments.
- E1/#658 design-record snapshot fidelity shipped: Studio object snapshots now preserve empty and NULL DBF-family design fields instead of dropping them, while existing visual property blob expansion remains intact. Focused `test_studio_host` coverage proves empty memo-backed fields, NULL fields, and parsed visual properties survive in one normalized snapshot.
- D1/#637 bridge launch-environment dispatch shipped: generated DLL/FLL native-wrapper dispatch execution now carries `CopperfinRuntimeBridgeLaunchPlan::environment` into command construction, and runtime-host command lines now apply those environment assignments before launching the host. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output carries launch environment entries through dispatch and command construction.
- D1/#636 runtime-host bridge dispatch execution shipped: generated DLL/FLL native-wrapper launch helpers now quote and build a runtime-host command line from `CopperfinRuntimeBridgeDispatchExecution`, execute it through `std::system(...)`, capture the observed exit code, and mark launch success only when that exit code matches the expected exit code. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output no longer reports deterministic process-launch failure.
- D1/#635 bridge response schema-version validation shipped: generated DLL/FLL native-wrapper response contracts now include `schema_version`, and response validation now compares the response document's schema-version field with `CopperfinRuntimeBridgeResponseValidationPlan::expected_schema_version`, falling back when the field is absent or mismatched. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares and enforces response schema version at the validation seam.
- D1/#634 bridge response media-type validation shipped: generated DLL/FLL native-wrapper response validation now extracts the response document's `response_media_type` field and compares it with `CopperfinRuntimeBridgeResponseValidationPlan::expected_response_media_type`, falling back when the field is absent or mismatched. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output enforces response media type at the validation seam.
- D1/#633 bridge required response-field validation shipped: generated DLL/FLL native-wrapper response validation now checks `response_document` for every field declared by `CopperfinRuntimeBridgeResponseValidationPlan::required_response_fields` and falls back when any required response field is absent. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output enforces required response fields at the validation seam.
- D1/#632 bridge missing-response document evaluation shipped: generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_missing_response(...)`, and missing-response policy now treats the response as missing only when an existing response is required and the read document is empty. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output evaluates missing responses from the response document.
- D1/#631 bridge response-document validation routing shipped: generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_response_validation(...)`, and the validation evaluation surface records response-document availability from the response body. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output carries the response document into response validation.
- D1/#630 bridge response-contract expectation serialization shipped: generated DLL/FLL native-wrapper request JSON now carries expected response media type plus the response-field contract list from `CopperfinRuntimeBridgePayloadPlan::response_fields`, so runtime-host consumers can produce and validate the matching response envelope from the same wrapper request document. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the expected-response helpers and serializes each response-field contract item.
- D1/#629 bridge request-field serialization shipped: generated DLL/FLL native-wrapper request JSON now emits a `request_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::request_fields`, so runtime-host consumers can validate the exact request contract carried by the wrapper request document. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the request-fields helper and serializes each request-field contract item.
- D1/#628 bridge request-field contract metadata shipped: generated DLL/FLL native-wrapper `CopperfinRuntimeBridgePayloadPlan::request_fields` now declares the descriptor metadata fields serialized by the bridge request document, including routine kind, source provenance, parameter declaration style, parameter names, and schema version. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output keeps the request-field contract synchronized with descriptor metadata serialization.
- D1/#627 bridge-request descriptor metadata shipped: generated DLL/FLL native-wrapper request JSON now serializes routine kind, source provenance, parameter declaration style, parameter-name list, and schema version beside the existing export name, arity, request media type, and parameter value payloads. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the new request-field helpers and routes descriptor metadata into the bridge request document.
- D1/#626 exported-argument consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit stale `(void)` discards for DLL integer parameters or FLL `ParamBlk* parm` now that those exported arguments feed `copperfin_build_runtime_bridge_call(...)` parameter payloads. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL arguments are consumed through bridge call bindings and the false unused markers are absent in generated output.
- D1/#625 request-write execution consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)request_write_execution` discard now that `request_write_execution` feeds `copperfin_build_runtime_bridge_response_read_plan(...)`; the response-read plan now carries `request_write_succeeded` and falls back to the shared empty response document when request writing fails. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the write result is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#624 dispatch-execution consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)dispatch_execution` discard now that `dispatch_execution` feeds `copperfin_runtime_bridge_launch_process(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch result is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#623 process-launch consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)process_launch` discard now that `process_launch` feeds `copperfin_runtime_bridge_evaluate_host_failure(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch result is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#622 host-failure evaluation consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)host_failure` discard now that `host_failure` feeds `copperfin_runtime_bridge_evaluate_missing_response(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#621 missing-response evaluation consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)missing_response` discard now that `missing_response` feeds `copperfin_runtime_bridge_evaluate_response_validation(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation result is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#620 response-validation evaluation consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)response_validation_evaluation` discard now that `response_validation_evaluation` feeds `copperfin_runtime_bridge_admit_response_parse(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#619 response-parse admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)response_parse_admission` discard now that `response_parse_admission` feeds `copperfin_runtime_bridge_admit_interpreted_result(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#618 interpreted-result admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)interpreted_result_admission` discard now that `interpreted_result_admission` feeds `copperfin_runtime_bridge_admit_native_return(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#617 native-return admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)native_return_admission` discard now that `native_return_admission` feeds `copperfin_runtime_bridge_admit_outcome_selection(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#616 outcome-selection admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)outcome_selection_admission` discard now that `outcome_selection_admission` feeds `copperfin_runtime_bridge_admit_return_materialization(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#615 return-materialization admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_materialization_admission` discard now that `return_materialization_admission` feeds `copperfin_runtime_bridge_admit_return_emission(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#614 return-emission admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_emission_admission` discard now that `return_emission_admission` feeds `copperfin_runtime_bridge_admit_final_return_adoption(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#613 final-return-adoption admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)final_return_adoption_admission` discard now that `final_return_adoption_admission` feeds `copperfin_runtime_bridge_admit_return_activation(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#612 return-activation admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_activation_admission` discard now that `return_activation_admission` feeds `copperfin_runtime_bridge_admit_stub_return(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#611 stub-return admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)stub_return_admission` discard now that `stub_return_admission` feeds `copperfin_runtime_bridge_admit_placeholder_return_value(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#610 placeholder-return-value admission consumption shipped: generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)placeholder_return_value_admission` discard now that `placeholder_return_value_admission` feeds `copperfin_runtime_bridge_admit_placeholder_return_int(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent in DLL and FLL generated output.
- D1/#609 placeholder-return-int admission-driven execution shipped: generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_int_admission)` and remove the stale `(void)placeholder_return_int_admission` discard, so the explicit placeholder-return integer is produced from the admitted int surface before flowing into stub-emission admission and execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admitted-int executor route and absence of the old placeholder-value helper input in DLL and FLL generated output.
- D1/#608 stub-emission admission-to-execution routing shipped: generated DLL/FLL native-wrapper stubs now pass the explicit placeholder-return integer into `copperfin_runtime_bridge_admit_stub_emission(placeholder_return_int_admission, placeholder_return_int)` and execute stub emission from `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` instead of discarding the admitted surface and reusing direct placeholder-return inputs. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct admission-to-execution route and absence of the old `(void)stub_emission_admission` discard in DLL and FLL generated output.
- D1/#607 placeholder-return-int executor call-site shipped: generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_value)` after placeholder-return-int admission and pass the explicit integer into `copperfin_runtime_bridge_execute_stub_emission(placeholder_return_value, placeholder_return_int)` instead of keeping placeholder-return-int execution hidden inside stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct placeholder-return-int executor call-site and absence of the old hidden helper call in DLL and FLL generated output.
- D1/#606 stub-emission output-application call-site shipped: generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_apply_stub_emission_output(stub_emission_return_surface, ...)` after return-surface construction with the planned return adapter instead of keeping output application hidden inside `copperfin_runtime_bridge_emit_stub_return_shared(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct output-application call-site and absence of the obsolete shared stub-return emitter in DLL and FLL generated output.
- D1/#605 stub-emission return-surface call-site shipped: generated DLL/FLL native-wrapper stubs now build `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` after stub-emission execution and pass the explicit return-surface metadata into `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping return-surface construction hidden inside the shared stub-return emitter. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-surface construction call-site and two-input shared stub-return emitter call in DLL and FLL generated output.
- D1/#604 stub-emission executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_emission(placeholder_return_value_plan, placeholder_return_value)` after stub-emission admission and pass the emitted stub metadata into `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping stub-emission execution hidden inside the shared stub-return emitter. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-emission execution call-site and two-input shared stub-return emitter call in DLL and FLL generated output.
- D1/#603 placeholder-return-value executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_value(placeholder_return_value_plan)` after placeholder-return-value admission and pass the placeholder return metadata into shared stub-emission execution instead of keeping placeholder-return-value execution hidden inside stub-emission execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-return-value execution call-site and two-input shared stub-emission helper call in DLL and FLL generated output.
- D1/#602 stub-return executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_return(stub_return_plan)` after stub-return admission and pass the routed stub-return metadata into `CopperfinRuntimeBridgePlaceholderReturnValuePlan` instead of keeping stub-return execution hidden inside placeholder-return-value planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return execution call-site and two-input placeholder-return-value plan construction in DLL and FLL generated output.
- D1/#601 return-activation executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_activation(return_activation_plan)` after return-activation admission and pass the activated return metadata into `CopperfinRuntimeBridgeStubReturnPlan` instead of keeping return-activation execution hidden inside stub-return planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation execution call-site and two-input stub-return plan construction in DLL and FLL generated output.
- D1/#600 final-return-adoption executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_final_return_adoption(final_return_adoption_plan)` after final-return-adoption admission and pass the adopted return metadata into `CopperfinRuntimeBridgeReturnActivationPlan` instead of keeping final-return-adoption execution hidden inside return-activation planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption execution call-site and two-input return-activation plan construction in DLL and FLL generated output.
- D1/#599 return-emission executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_emission(return_emission_plan)` after return-emission admission and pass the emitted return block metadata into `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` instead of keeping return-emission execution hidden inside final-return-adoption planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission execution call-site and three-input final-return-adoption plan construction in DLL and FLL generated output.
- D1/#598 return-materialization executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_materialization(return_materialization_plan)` after return-materialization admission and pass the materialized return branch metadata into `CopperfinRuntimeBridgeReturnEmissionPlan` instead of keeping return-materialization execution hidden inside return-emission planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization execution call-site and two-input return-emission plan construction in DLL and FLL generated output.
- D1/#597 outcome-selection executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_outcome_selection(outcome_selection_plan)` after outcome-selection admission and pass the selected condition/diagnostics/native return metadata into `CopperfinRuntimeBridgeReturnMaterializationPlan` instead of keeping outcome-selection execution hidden inside return-materialization execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection execution call-site and two-input return-materialization plan construction in DLL and FLL generated output.
- D1/#596 native-return executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_native_return(native_return_plan)` after native-return admission and pass the resulting selected native return metadata into `CopperfinRuntimeBridgeOutcomeSelectionPlan` instead of keeping native-return execution hidden inside outcome-selection execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return execution call-site and two-input outcome-selection plan construction in DLL and FLL generated output.
- D1/#595 interpreted-result executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_interpreted_result(interpreted_result_plan)` after interpreted-result admission and pass the resulting success/fallback interpretation into `CopperfinRuntimeBridgeNativeReturnPlan` instead of keeping interpretation execution hidden inside native-return execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result execution call-site and three-input native-return plan construction in DLL and FLL generated output.
- D1/#594 response-parse executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_parse_response(response_parse_plan)` after response-parse admission and pass the resulting parsed `status`, `return_value`, and `diagnostics` values into `CopperfinRuntimeBridgeInterpretedResultPlan` instead of keeping parse execution hidden inside interpreted-result execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse execution call-site and two-input interpreted-result plan construction in DLL and FLL generated output.
- D1/#593 response-read executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_read_response(response_read_plan)` immediately after constructing the response-read plan and pass the resulting response document into `CopperfinRuntimeBridgeResponseArtifact` instead of seeding artifacts from the empty-response fallback. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read execution call-site and two-input response-artifact construction in DLL and FLL generated output.
- D1/#529 request-write executor call-site shipped: generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_write_request(request_write_plan)` immediately after constructing the request-write plan and explicitly discard the scaffold-only execution result before continuing into response-read planning, so request documents are materialized through the shared helper while the stub still follows the placeholder-return path. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper declaration, request-document write body, request-write execution call-site, and explicit discard in DLL and FLL generated output.
- D1/#592 descriptor wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDescriptor`, so exported DLL/FLL stubs now build that wrapper once before constructing the descriptor and then build invocations straight from the enriched descriptor instead of passing the wrapper separately at the invocation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the descriptor-plan wrapper contract path.
This changelog is the durable shipped-history ledger. Older entries were compiled from the dated implementation notes previously tracked in [remaining-work.md](remaining-work.md) before that file was deprecated as active planning input.

It is intentionally append-only and mirrors shipped history rather than planned work. For active priorities and issue sequencing, use [agent-handoff.md](agent-handoff.md) and [docs/23-phase-a-dependency-breakdown.md](docs/23-phase-a-dependency-breakdown.md).

## 2026-06-15

- D1/#591 invocation wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInvocation`, so exported DLL/FLL stubs now build that wrapper once before constructing the invocation and then build calls straight from the enriched invocation instead of passing the wrapper separately at the call seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the invocation-plan wrapper contract path.
- D1/#590 call wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeCall`, so exported DLL/FLL stubs now build that wrapper once before constructing the call and then build results straight from the enriched call instead of passing the wrapper separately at the result seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the call-plan wrapper contract path.
- D1/#589 result wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResult`, so exported DLL/FLL stubs now build that wrapper once before constructing the result and then build launch plans straight from the enriched result instead of passing the wrapper separately at the launch seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the result-plan wrapper contract path.
- D1/#588 launch wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeLaunchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the launch plan and then build observation plans straight from the enriched launch plan instead of passing the wrapper separately at the observation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch-plan wrapper contract path.
- D1/#587 observation wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeObservationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the observation plan and then build execution plans straight from the enriched observation plan instead of passing the wrapper separately at the execution seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the observation-plan wrapper contract path.
- D1/#586 execution wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeExecutionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the execution plan and then build transport plans straight from the enriched execution plan instead of passing the wrapper separately at the transport seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the execution-plan wrapper contract path.
- D1/#585 transport wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeTransportPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the transport plan and then build serialization plans straight from the enriched transport plan instead of passing the wrapper separately at the serialization seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the transport-plan wrapper contract path.
- D1/#584 serialization wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeSerializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the serialization plan and then build dispatch plans straight from the enriched serialization plan instead of passing the wrapper separately at the dispatch seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the serialization-plan wrapper contract path.
- D1/#583 dispatch wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDispatchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the dispatch plan and then build payload plans straight from the enriched dispatch plan instead of passing the wrapper separately at the payload seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch-plan wrapper contract path.
- D1/#582 payload wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePayloadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the payload plan and then build interpretation plans straight from the enriched payload plan instead of passing the wrapper separately at the interpretation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the payload-plan wrapper contract path.
- D1/#581 interpretation wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpretation plan and then build failure-policy plans straight from the enriched interpretation plan instead of passing the wrapper separately at the failure-policy seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpretation-plan wrapper contract path.
- D1/#580 failure-policy wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFailurePolicyPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the failure-policy plan and then build response-validation plans straight from the enriched failure-policy plan instead of passing the wrapper separately at the response-validation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the failure-policy-plan wrapper contract path.
- D1/#579 response-validation wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseValidationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-validation plan and then build request artifacts straight from the enriched response-validation plan instead of passing the wrapper separately at the request-artifact seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-validation-plan wrapper contract path.
- D1/#578 request-artifact wrapper propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the request artifact and then build request-write plans straight from the enriched request artifact instead of passing the wrapper separately at the request-write-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-artifact wrapper contract path.
- D1/#577 request-write wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestWritePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the request-write plan and then build response-read plans straight from the enriched request-write plan instead of passing the wrapper separately at the response-read-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-write-plan wrapper contract path.
- D1/#576 response-read wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseReadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-read plan and then build response artifacts straight from the enriched response-read plan instead of passing the wrapper separately at the response-artifact seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read-plan wrapper contract path.
- D1/#575 response-artifact wrapper propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the response artifact and then build response-parse plans straight from the enriched response artifact instead of passing the wrapper separately at the response-parse-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-artifact wrapper contract path.
- D1/#574 response-parse wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseParsePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-parse plan and then build interpreted-result plans straight from the enriched response-parse plan instead of passing the wrapper separately at the interpreted-result-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse-plan wrapper contract path.
- D1/#573 interpreted-result wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretedResultPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpreted-result plan and then build native-return plans straight from the enriched interpreted-result plan instead of passing the wrapper separately at the native-return-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result-plan wrapper contract path.
- D1/#572 native-return wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeNativeReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the native-return plan and then build outcome-selection plans straight from the enriched native-return plan instead of passing the wrapper separately at the outcome-selection-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return-plan wrapper contract path.
- D1/#571 outcome-selection wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeOutcomeSelectionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the outcome-selection plan and then build return-materialization plans straight from the enriched outcome-selection plan instead of passing the wrapper separately at the return-materialization-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection-plan wrapper contract path.
- D1/#570 return-materialization wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnMaterializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-materialization plan and then build return-emission plans straight from the enriched return-materialization plan instead of passing the wrapper separately at the return-emission-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization-plan wrapper contract path.
- D1/#569 return-emission wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnEmissionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-emission plan and then build final-return-adoption plans straight from the enriched return-emission plan instead of passing the wrapper separately at the final-return-adoption-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission-plan wrapper contract path.
- D1/#568 final-return-adoption wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the final-return-adoption plan and then build return-activation plans straight from the enriched final-return-adoption plan instead of passing the wrapper separately at the return-activation-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption-plan wrapper contract path.
- D1/#567 return-activation wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnActivationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-activation plan and then build stub-return plans straight from the enriched activation plan instead of passing the wrapper separately at the stub-return-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation-plan wrapper contract path.
- D1/#566 stub-return wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeStubReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the stub-return plan and then build placeholder-return-value plans straight from the enriched stub-return plan instead of passing the wrapper separately at the placeholder-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-plan wrapper contract path.
- D1/#565 stub-emission wrapper-plan propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the placeholder-return-value plan and then route placeholder returns straight through `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping separate output-specific stub-return wrapper helpers. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the plan-level wrapper contract path.
- D1/#564 stub-emission wrapper helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeStubEmissionWrapper` surface plus `copperfin_runtime_bridge_build_stub_emission_wrapper(...)` so the remaining output-specific `copperfin_runtime_bridge_emit_stub_return(...)` bodies now only construct one wrapper contract before handing off to the shared stub-emission emitter helper. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission wrapper helper path.
- D1/#563 stub-emission emitter helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_emit_stub_return_shared(...)` helper so the formerly duplicated DLL/FLL `copperfin_runtime_bridge_emit_stub_return(...)` bodies now flow through one scaffold-only emitter helper before output-specific wrapper forwarding. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission emitter helper path.
- D1/#562 stub-emission output-application helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_apply_stub_emission_output(...)` helper so shared stub-emission return-surface output flows through one scaffold-only helper before the final DLL/FLL return adapter is applied. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission output-application helper path.
- D1/#561 stub-emission return-surface helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` helper plus a narrow stub-emission return-surface surface so shared stub-emission execution output flows through one scaffold-only helper before DLL/FLL output-specific stub return application. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission return-surface helper path.
- D1/#560 stub-emission execution helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_stub_emission(...)` helper plus a narrow stub-emission execution surface so staged placeholder-return-value execution and placeholder-return-int execution flow through one scaffold-only helper before DLL/FLL output-specific stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission execution helper path.
- D1/#559 stub-emission admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_stub_emission(...)` helper plus a narrow stub-emission admission surface so staged placeholder-return-int admission output and final stub-emission contracts flow through one scaffold-only helper before output-specific stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission admission helper path.
- Managed compile warning cleanup shipped: `Copperfin.DesignerSmokeTests.csproj` now resolves Visual Studio SDK references through package restore instead of machine-local `HintPath` entries, and `CopperfinAssetEditorControl` now routes project/debug button actions through a guarded UI task dispatcher so the managed compile gate runs with `0 Warning(s)` and `0 Error(s)`.
- D1/#558 placeholder-return-int admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_placeholder_return_int(...)` helper plus a narrow placeholder-return-int admission surface so staged placeholder-return-value admission output and placeholder-return-int contracts flow through one scaffold-only helper before final stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-int admission helper path.
- D1/#557 placeholder-return-value admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_placeholder_return_value(...)` helper plus a narrow placeholder-return-value admission surface so staged stub-return admission output and placeholder-return-value contracts flow through one scaffold-only helper before placeholder-return-int routing. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-value admission helper path.
- D1/#556 stub-return admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_stub_return(...)` helper plus a narrow stub-return admission surface so staged return-activation admission output and stub-return contracts flow through one scaffold-only helper before placeholder-return-value planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-return admission helper path.
- D1/#555 return-activation admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_activation(...)` helper plus a narrow return-activation admission surface so staged final-return-adoption admission output and return-activation contracts flow through one scaffold-only helper before stub-return planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-activation admission helper path.
- D1/#554 final-return-adoption admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_final_return_adoption(...)` helper plus a narrow final-return-adoption admission surface so staged return-emission admission output and final-return-adoption contracts flow through one scaffold-only helper before return-activation planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared final-return-adoption admission helper path.
- D1/#553 return-emission admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_emission(...)` helper plus a narrow return-emission admission surface so staged return-materialization admission output and return-emission contracts flow through one scaffold-only helper before final-return-adoption planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-emission admission helper path.
- D1/#552 return-materialization admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_materialization(...)` helper plus a narrow return-materialization admission surface so staged outcome-selection admission output and return-materialization contracts flow through one scaffold-only helper before return-emission planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-materialization admission helper path.
- D1/#551 outcome-selection admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_outcome_selection(...)` helper plus a narrow outcome-selection admission surface so staged native-return admission output and outcome-selection contracts flow through one scaffold-only helper before return-materialization planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared outcome-selection admission helper path.
- D1/#550 native-return admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_native_return(...)` helper plus a narrow native-return admission surface so staged interpreted-result admission output and native-return contracts flow through one scaffold-only helper before outcome-selection planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared native-return admission helper path.
- D1/#549 interpreted-result admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_interpreted_result(...)` helper plus a narrow interpreted-result admission surface so staged response-parse admission output and interpreted-result contracts flow through one scaffold-only helper before native-return planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared interpreted-result admission helper path.
- D1/#548 response-parse admission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_response_parse(...)` helper plus a narrow response-parse admission surface so staged response-validation output and response-parse contracts flow through one scaffold-only helper before interpreted-result planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-parse admission helper path.
- D1/#547 response-validation evaluation helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_response_validation(...)` helper plus a narrow response-validation evaluation surface so staged missing-response output and response-validation contracts flow through one scaffold-only helper before response artifact handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-validation evaluation helper path.
- D1/#546 missing-response evaluation helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_missing_response(...)` helper plus a narrow missing-response evaluation surface so staged host-failure output and response-read policy contracts flow through one scaffold-only helper before response artifact handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared missing-response evaluation helper path.
- D1/#545 host-failure evaluation helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_host_failure(...)` helper plus a narrow host-failure evaluation surface so staged process-launch output and failure-policy contracts flow through one scaffold-only helper before response validation. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared host-failure evaluation helper path.
- D1/#544 process-launch helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_launch_process(...)` helper plus a narrow process-launch surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and deterministic launch outcome contracts flow through one scaffold-only helper before payload construction. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared process-launch helper path.
- D1/#543 dispatch-execution helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_dispatch(...)` helper plus a narrow dispatch-execution surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and expected-exit-code contracts flow through one scaffold-only helper before payload construction. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared dispatch-execution helper path.
- D1/#542 placeholder-return-int helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_placeholder_return_int(...)` helper so final placeholder integer selection flows through one scaffold-only helper before DLL direct return or FLL `_RetInt(...)` surface application. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-int execution helper path.
- D1/#541 placeholder-return-value helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_placeholder_return_value(...)` helper plus a narrow placeholder-return-value surface so stub-return metadata can flow through one scaffold-only placeholder-return contract before final DLL/FLL stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-value execution helper path.
- D1/#540 stub-return helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_stub_return(...)` helper plus a narrow stub-return surface so return-activation metadata can flow through one scaffold-only placeholder/deferred stub-return path before placeholder-return-value handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-return execution helper path.
- D1/#539 return-activation helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_activation(...)` helper plus a narrow return-activation surface so final-return-adoption metadata can flow through one scaffold-only active/deferred return-selection path before stub-return handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-activation execution helper path.
- D1/#538 final-return-adoption helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_final_return_adoption(...)` helper plus a narrow final-return-adoption surface so return-emission metadata can flow through one scaffold-only adopted-return-block path before return activation. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared final-return-adoption execution helper path.
- D1/#537 return-emission helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_emission(...)` helper plus a narrow return-emission surface so return-materialization metadata can flow through one scaffold-only emitted-branch-block path before final-return adoption. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-emission execution helper path.
- D1/#536 return-materialization helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_materialization(...)` helper plus a narrow return-materialization surface so outcome-selection metadata can flow through one scaffold-only success/fallback native return-statement materialization path before return emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-materialization execution helper path.
- D1/#535 outcome-selection helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_outcome_selection(...)` helper plus a narrow outcome-selection surface so native-return success/fallback selection can flow through one scaffold-only selected-condition path before return materialization. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared outcome-selection execution helper path.
- D1/#534 native-return helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_native_return(...)` helper plus a narrow native-return surface so interpreted success/fallback selection can flow through one scaffold-only native return-value/int-value selection path before outcome selection. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared native-return execution helper path.
- D1/#533 interpreted-result helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_interpreted_result(...)` helper plus a narrow interpreted-result surface so parsed response status can flow through one scaffold-only success/fallback selection path before native-return materialization. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared interpreted-result execution helper path.
- D1/#532 response-parse helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_parse_response(...)` helper plus a narrow parsed-response surface so staged response documents can flow through one scaffold-only field-extraction path for `status`, `return_value`, and `diagnostics`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-parse execution helper path.
- D1/#531 response-artifact read helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_read_response(...)` helper that returns an empty response document when the staged response artifact is missing or unreadable and otherwise reads the staged response payload through one executable helper path. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-read execution helper path.
- D1/#530 request-artifact write helper shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_write_request(...)` helper that creates the staged request directory when needed, writes `request_document` through one executable helper path, and returns deterministic write success for the future wrapper-launch flow. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared request-write execution helper path.
- D1/#528 FLL stub return-surface literal helperized: generated FLL native-wrapper scaffolds now emit `copperfin_build_runtime_bridge_fll_int_return_surface()` and route both FLL stub call sites (placeholder return binding and interpretation plan) through that helper instead of repeating the compound `"_RetInt(int)"` literal. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized FLL return-surface path.
- D1/#527 DLL stub wrapper-return-surface literal routed through existing native-int helper: the DLL generated stub no longer passes the bare `"int"` literal as the wrapper_return_surface to `copperfin_build_runtime_bridge_interpretation_plan`; it now calls `copperfin_build_runtime_bridge_native_int_return_surface()`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized return-surface path in the DLL stub.
- D1/#526 helperized request document JSON field name literals shipped: generated DLL/FLL native-wrapper scaffolds now route all 8 JSON field name string literals in `copperfin_build_runtime_bridge_request_document` through named shared helpers (4 new: `payload_shape_field_name`, `parameter_name_field_name`, `parameter_value_field_name`, `parameter_surface_field_name`; plus 4 existing helpers relocated earlier) so every request document field name is behind a named contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized field-name paths.
- D1/#525 helperized launch plan environment variable name literals shipped: generated DLL/FLL native-wrapper scaffolds now route four bare inline environment variable name literals through named shared helpers (`copperfin_runtime_bridge_library_export_env_var`, `routine_kind_env_var`, `source_path_env_var`, `parameter_count_env_var`) so `copperfin_build_runtime_bridge_launch_plan` consumes named env-var contracts instead of anonymous raw strings. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized env-var paths in direct pipeline and build-host paths.
- D1/#524 helperized invocation CLI argument flag literals shipped: generated DLL/FLL native-wrapper scaffolds now route eight bare inline flag string literals through named shared helpers (`copperfin_runtime_bridge_manifest_flag`, `library_export_flag`, `routine_kind_flag`, `source_path_flag`, `source_line_flag`, `parameter_declaration_flag`, `parameter_names_flag`, `parameter_count_flag`) so `copperfin_build_runtime_bridge_invocation` consumes named flag contracts instead of anonymous raw literals. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized flag paths in direct pipeline and build-host paths.
- D1/#523 helperized observation, transport, and activation plan inline contracts shipped: generated DLL/FLL native-wrapper scaffolds now route six inline policy-bearing literals through shared helpers (`copperfin_runtime_bridge_stdout_log_suffix`, `copperfin_runtime_bridge_stderr_log_suffix`, `copperfin_runtime_bridge_expected_exit_code`, `copperfin_runtime_bridge_request_artifact_suffix`, `copperfin_runtime_bridge_response_artifact_suffix`, `copperfin_runtime_bridge_activates_adopted_return_policy`). Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized paths in direct pipeline and build-host paths.
- D1/#522 helperized execution, failure, and IO plan policy boolean contracts shipped: generated DLL/FLL native-wrapper scaffolds now route six hardcoded `true` boolean literals through shared policy helpers (`copperfin_runtime_bridge_capture_stdout_policy`, `copperfin_runtime_bridge_capture_stderr_policy`, `copperfin_runtime_bridge_fail_on_nonzero_exit_policy`, `copperfin_runtime_bridge_fail_on_missing_response_policy`, `copperfin_runtime_bridge_ensure_parent_directory_policy`, `copperfin_runtime_bridge_require_existing_response_policy`) so all four plan-builder functions consume named policy contracts instead of anonymous `true` literals. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized boolean policy paths in direct pipeline and build-host paths.
- D1/#521 helperized activation-mode and adoption-mode token contracts shipped: generated DLL/FLL native-wrapper scaffolds now route the `"planned_activation_pending"` and `"replace_placeholder_return"` string literals through shared `copperfin_build_runtime_bridge_planned_activation_pending_mode` and `copperfin_build_runtime_bridge_replace_placeholder_return_mode` helpers, so all four usage sites (plan-builder construction and comparison sites in `copperfin_build_runtime_bridge_stub_return_plan`) consume the shared contracts instead of repeating the raw inline literals. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized mode-token paths in direct pipeline and build-host paths.
- D1/#520 helperized typed native return-expression construction shipped: generated DLL/FLL native-wrapper scaffolds now route the typed placeholder return-expression assembly through a shared `copperfin_build_runtime_bridge_typed_native_return_expression` helper, so `copperfin_build_runtime_bridge_return_statement(...)` delegates the prefix-substr + `(` + int_value_representation + `)` construction to that helper instead of repeating the inline tokens while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized typed return-expression construction path in direct pipeline and build-host paths.

## 2026-06-14

- D1/#519 helperized native return-statement framing contract shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared native return-statement framing helper, so `copperfin_build_runtime_bridge_return_statement(...)` routes its plain-`int`, typed placeholder, and representation fallback branches through one shared `return ...;` contract instead of repeating the raw inline framing tokens while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native return-statement framing path in direct pipeline and build-host paths.
- D1/#518 helperized native typed placeholder-signature token contract shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared native-`int` placeholder-signature helper, so `copperfin_build_runtime_bridge_return_statement(...)` consumes that shared token contract instead of repeating the raw inline `"(int)"` literal when matching typed return surfaces while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native-int placeholder-signature path in direct pipeline and build-host paths.
- D1/#517 helperized native typed-return placeholder token comparison shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared native-`int` return-surface helper, so `copperfin_build_runtime_bridge_return_statement(...)` consumes that shared token contract instead of repeating the raw inline `int` literal when materializing native return statements while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native-int return-surface path in direct pipeline and build-host paths.
- D1/#516 helperized empty response-document token contract shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared empty response-document helper, so `CopperfinRuntimeBridgeResponseArtifact` consumes that shared token contract instead of repeating the raw inline empty-string literal while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized empty-document token path in direct pipeline and build-host paths.
- D1/#515 helperized parsed-int default sentinel contract shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared parsed-int default helper, so `copperfin_parse_runtime_bridge_int_value_representation(...)` consumes that shared token contract instead of repeating the raw inline `-1` sentinel while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized parsed-int sentinel path in direct pipeline and build-host paths.
- D1/#514 helperized outcome-selection comparator token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared success/fallback comparator helpers, so `CopperfinRuntimeBridgeOutcomeSelectionPlan` consumes those shared token contracts instead of repeating raw inline comparator literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized comparator token path in direct pipeline and build-host paths.
- D1/#513 helperized request/response IO policy token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared request-write, response-read, and response-parse policy helpers, so `CopperfinRuntimeBridgeRequestWritePlan`, `CopperfinRuntimeBridgeResponseReadPlan`, and `CopperfinRuntimeBridgeResponseParsePlan` consume those shared token contracts instead of repeating raw inline policy literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized IO-policy token path in direct pipeline and build-host paths.
- D1/#512 helperized dispatch argv flag token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared request-path, response-path, request-media-type, response-media-type, and schema-version dispatch-argument helpers, so `CopperfinRuntimeBridgeDispatchPlan` consumes those shared token contracts instead of repeating raw inline argv flag literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized dispatch-flag token path in direct pipeline and build-host paths.
- D1/#511 helperized payload field-name token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared payload request/response field-name helpers, so `CopperfinRuntimeBridgePayloadPlan` consumes those shared token contracts instead of repeating raw inline field-name literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized payload field-name token path in direct pipeline and build-host paths.
- D1/#510 helperized serialization schema-version token contract shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared serialization schema-version helper, so the serialization plan consumes that shared token contract instead of repeating the raw inline `v1` literal while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized schema-version token path in direct pipeline and build-host paths.
- D1/#509 helperized serialization media-type token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared request/response serialization media-type helpers, so the serialization plan consumes those shared token contracts instead of repeating raw inline media-type literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized media-type token path in direct pipeline and build-host paths.
- D1/#508 helperized payload-shape token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared request/response payload-shape helpers, so the payload builder consumes those shared token contracts instead of repeating raw inline `bridge_request_v1` / `bridge_response_v1` literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized payload-shape token path in direct pipeline and build-host paths.
- D1/#507 helperized response field-name token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared response-status, response-value, and response-diagnostics field-name helpers, so payload and interpretation builders consume those shared token contracts instead of repeating raw inline string literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized response-field token path in direct pipeline and build-host paths.
- D1/#506 helperized failure/success token contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared failure-diagnostics and success-status token helpers, so failure-policy and response-validation builders consume those shared token contracts instead of repeating raw inline string literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized token-contract path in direct pipeline and build-host paths.
- D1/#505 helperized placeholder return binding/statement contracts shipped: generated DLL/FLL native-wrapper scaffolds now emit shared placeholder return-binding and placeholder return-statement helpers, so exported stubs feed `CopperfinRuntimeBridgeResult`, failure-policy fallback metadata, and final-return adoption from one shared placeholder return contract instead of repeating raw inline `-1` binding and return-statement literals while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized placeholder return-contract path in direct pipeline and build-host paths.
- D1/#504 placeholder-plan-routed helper-policy booleans shipped: generated DLL/FLL native-wrapper scaffolds now derive explicit placeholder-helper active/replacement policy booleans in `CopperfinRuntimeBridgeStubReturnPlan`, route those booleans through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and have the current placeholder-return helper consume them instead of reconstructing the same policy from `activation_mode` / `adoption_mode` string comparisons while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed helper-policy path in direct pipeline and build-host paths.
- D1/#503 placeholder-plan-routed activation/adoption mode contracts shipped: generated DLL/FLL native-wrapper scaffolds now flatten `activation_mode` and `adoption_mode` into `CopperfinRuntimeBridgeStubReturnPlan`, route those mode contracts through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and have the current placeholder-return helper consume them alongside the emitted-return, deferred-return, and fallback-value metadata while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed activation/adoption-mode contract path in direct pipeline and build-host paths.
- D1/#502 placeholder-plan-routed deferred return blocks shipped: generated DLL/FLL native-wrapper scaffolds now record the deferred return block directly in `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and the current placeholder-return helper now consumes that plan-level deferred-return contract alongside the emitted-return and fallback-value metadata while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed deferred-return-contract path in direct pipeline and build-host paths.
- D1/#501 placeholder-plan-routed emission contracts shipped: generated DLL/FLL native-wrapper scaffolds now record the placeholder-emission flag and emitted placeholder return statement directly in `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and the current placeholder-return helper now consumes that plan-level emission contract in addition to the fallback int value while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed emission-contract path in direct pipeline and build-host paths.
- D1/#500 stub-return-routed placeholder fallback metadata shipped: generated DLL/FLL native-wrapper scaffolds now record placeholder fallback integer/value metadata directly in `CopperfinRuntimeBridgeStubReturnPlan`, and `CopperfinRuntimeBridgePlaceholderReturnValuePlan` now consumes that stub-return metadata instead of reaching back through the deep native-return-plan chain, while preserving the current placeholder-emission behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-routed placeholder-fallback metadata path in direct pipeline and build-host paths.
- D1/#499 activation-routed deferred stub-return blocks shipped: generated DLL/FLL native-wrapper scaffolds now derive the inactive `deferred_return_block` in `CopperfinRuntimeBridgeStubReturnPlan` from `return_activation_plan.active_return_block` instead of reaching back to final-adoption metadata, while still emitting the placeholder return because activation remains disabled. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the activation-metadata-routed deferred-return path in direct pipeline and build-host paths.
- D1/#498 seeded inactive active-return blocks shipped: generated DLL/FLL native-wrapper scaffolds now carry the already-built adopted return block forward as the future `active_return_block` inside `CopperfinRuntimeBridgeReturnActivationPlan`, while keeping activation disabled so the scaffold still emits the placeholder return today. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the seeded active-return-block path in direct pipeline and build-host paths.
- D1/#497 branch-composed return-emission blocks shipped: generated DLL/FLL native-wrapper scaffolds now record an explicit fallback else-branch statement inside `CopperfinRuntimeBridgeReturnEmissionPlan`, and the emitted return block now composes from the prebuilt success/fallback branch statements instead of re-materializing the fallback return inline, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the branch-composed return-emission path in direct pipeline and build-host paths.
- D1/#496 typed native return-statement materialization shipped: generated DLL/FLL native-wrapper scaffolds now feed the already-parsed `success_int_value` / `fallback_int_value` through `copperfin_build_runtime_bridge_return_statement(...)` for the current `int` / `_RetInt(int)` return surfaces, while preserving the existing representation-based fallback path for any future non-`int` surfaces and keeping the scaffold-only deferred adopted-return behavior intact. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the typed return-materialization path in direct pipeline and build-host paths.
- D1/#495 parsed typed success propagation shipped: generated DLL/FLL native-wrapper scaffolds now reuse `copperfin_parse_runtime_bridge_int_value_representation(...)` to derive the typed success integer value in `CopperfinRuntimeBridgeNativeReturnPlan` from the existing success representation metadata instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parsed typed-success path in direct pipeline and build-host paths.
- D1/#494 parsed typed fallback propagation shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_parse_runtime_bridge_int_value_representation(...)` helper, and `CopperfinRuntimeBridgeNativeReturnPlan` now derives its typed fallback integer value from the existing fallback representation metadata instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parsed typed-fallback path in direct pipeline and build-host paths.
- D1/#493 typed native fallback propagation shipped: generated DLL/FLL native-wrapper scaffolds now carry a typed fallback integer value in `CopperfinRuntimeBridgeNativeReturnPlan`, and the downstream placeholder-return-value plan now consumes that upstream typed fallback instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the typed fallback propagation path in direct pipeline and build-host paths.
- D1/#492 wrapper-side placeholder-return-value plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgePlaceholderReturnValuePlan` helper layered on top of the stub-return plan, so each exported stub routes its executable placeholder return through a plan-backed fallback integer value instead of a hardcoded raw literal while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the plan-backed placeholder return path in direct pipeline and build-host paths.
- D1/#491 wrapper-side stub-return helpers shipped: generated DLL/FLL native-wrapper scaffolds now emit an executable `copperfin_runtime_bridge_emit_stub_return(...)` helper layered on top of the stub-return plan, so each exported stub routes its current placeholder return through a shared helper instead of a raw literal while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper-routed placeholder return path in direct pipeline and build-host paths.
- D1/#490 wrapper-side stub-return plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeStubReturnPlan` helper layered on top of the return-activation plan, so each exported stub carries an explicit contract for which placeholder return statement it emits today versus which adopted return block remains deferred without pretending to execute the host, parse a live response, branch at runtime, or replace the placeholder native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#489 wrapper-side return-activation plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnActivationPlan` helper layered on top of the final-return-adoption plan, so each exported stub carries an explicit future contract for whether that adopted return block would become active in the wrapper body without pretending to execute the host, parse a live response, branch at runtime, or replace the placeholder native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#488 wrapper-side final-return adoption plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` helper layered on top of the return-emission plan, so each exported stub carries an explicit future contract for replacing the current placeholder native return with the planned emitted success/fallback return block without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#487 wrapper-side return-emission plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnEmissionPlan` helper layered on top of the return-materialization plan, so each exported stub carries an explicit future contract for the complete conditional success/fallback return block it would emit for DLL/FLL surfaces without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#486 wrapper-side return-materialization plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnMaterializationPlan` helper layered on top of the outcome-selection plan, so each exported stub carries an explicit future contract for the exact success/fallback native `return ...;` statements it would materialize for DLL/FLL surfaces without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#485 wrapper-side outcome-selection plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeOutcomeSelectionPlan` helper layered on top of the native-return plan, so each exported stub carries an explicit future contract for choosing the success vs fallback native return path from parsed status/diagnostics metadata without pretending to execute the host, parse a live response, or branch at runtime yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#484 wrapper-side native-return plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeNativeReturnPlan` helper layered on top of the interpreted-result plan and existing return-binding metadata, so each exported stub carries an explicit future contract for mapping success and fallback result values onto the native DLL/FLL return surface without pretending to execute the host or materialize the final native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#483 wrapper-side interpreted-result plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInterpretedResultPlan` helper layered on top of the response-parse plan, so each exported stub carries an explicit future contract for combining the parsed response status, wrapper return surface, and fallback return value into a single interpreted-result shape without pretending to execute the host or materialize the final native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#482 wrapper-side response-parse plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseParsePlan` helper layered on top of the response-artifact builder, so each exported stub carries an explicit future contract for extracting structured response fields from the raw staged response document without pretending to read the filesystem, parse JSON, or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#481 wrapper-side response-artifact builders shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseArtifact` helper layered on top of the response-read plan, so each exported stub carries an explicit future contract for the raw staged response document that would be recovered after runtime-host launch without pretending to read the filesystem, parse the response, or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-artifact-bearing wrapper source in direct pipeline and build-host paths.
- D1/#480 wrapper-side response-read plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseReadPlan` helper layered on top of the request-write plan, so each exported stub carries an explicit future contract for where and how the staged response document would be read back after runtime-host launch without pretending to touch the filesystem, spawn the host, or parse the response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#479 wrapper-side request-write plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeRequestWritePlan` helper layered on top of the request-artifact builder, so each exported stub carries an explicit future contract for where and how the serialized request document would be written before runtime-host launch without pretending to touch the filesystem or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-write-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#478 wrapper-side request-artifact builders shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeRequestArtifact` model plus request-document/request-artifact helpers layered on top of the response-validation plan, so each exported stub carries an explicit future contract for the serialized request document that would be written to the planned request-path artifact without pretending to spawn the host or consume a response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-artifact-bearing wrapper source in direct pipeline and build-host paths.
- D1/#477 wrapper-side response-validation plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseValidationPlan` helper layered on top of the host-failure policy plan, so each exported stub carries an explicit future contract for response media-type/schema validation, required response fields, and success-status expectations without pretending to execute the host or parse a real response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-validation-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#476 wrapper-side host-failure policy plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeFailurePolicyPlan` helper layered on top of the result-interpretation plan, so each exported stub carries an explicit future contract for handling non-zero runtime-host exits and missing responses via deterministic diagnostics fallback (`runtime_host_failure`) plus placeholder return fallback wiring without pretending to execute the host or consume a real response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the failure-policy-bearing wrapper source in direct pipeline and build-host paths.
- D1/#475 wrapper-side result-interpretation plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInterpretationPlan` helper layered on top of the payload plan, so each exported stub carries an explicit future contract for mapping runtime-host response fields onto the wrapper return surface without pretending to execute or deserialize the host response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpretation-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#474 wrapper-side payload-shape plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgePayloadPlan` helper layered on top of the dispatch plan, so each exported stub carries an explicit future request/response envelope-shape contract (named payload shapes plus required fields) without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the payload-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#473 wrapper-side dispatch-argv plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeDispatchPlan` helper layered on top of the serialization plan, so each exported stub carries an explicit future runtime-host command-line contract for request/response artifact paths and encoding metadata without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#472 wrapper-side serialization-envelope plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeSerializationPlan` helper layered on top of the transport plan, so each exported stub carries an explicit future request/response encoding contract (media types plus schema version) without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the serialization-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#471 wrapper-side transport-artifact plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeTransportPlan` helper layered on top of the execution plan, so each exported stub carries an explicit future request/response artifact contract without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the transport-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#470 wrapper-side process-execution plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeExecutionPlan` helper layered on top of the observation plan, so each exported stub carries an explicit future executable-path, argv, and output-capture contract without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the execution-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#469 wrapper-side process-observation plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeObservationPlan` helper layered on top of the launch plan, so each exported stub carries explicit future stdout/stderr log paths and an expected exit-code contract without pretending to spawn or observe the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the observation-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#468 wrapper-side process-launch plans shipped: generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeEnvironmentVariable` / `CopperfinRuntimeBridgeLaunchPlan` helpers layered on top of the bridge result model, so each exported stub carries an explicit future working-directory and environment contract (`COPPERFIN_LIBRARY_EXPORT`, routine/source metadata, and parameter-count context) without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#467 wrapper-side return-binding plans shipped: generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeReturn` / `CopperfinRuntimeBridgeResult` helpers layered on top of the bridge call model, so each exported stub carries an explicit future result-marshalling contract (`int` for DLL/OCX placeholder returns and `_RetInt(int)` for FLL placeholder returns) without pretending to execute or marshal the real runtime-host result yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#466 wrapper-side call-parameter plans shipped: generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeParameter` / `CopperfinRuntimeBridgeCall` helpers layered on top of the invocation plan, so DLL stubs record their actual placeholder integer arguments and FLL stubs record the honest `ParamBlk*` call-surface pointer as a future binding contract without pretending to marshal or execute the runtime-host call yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parameter-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#465 wrapper-side runtime-host invocation plans shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInvocation` helper layered on top of the bridge descriptors, producing an explicit future runtime-host argv contract (`--manifest`, `--library-export`, routine/source metadata, and parameter metadata) for each exported routine without pretending the wrapper executes that plan yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the invocation-plan-bearing wrapper source in direct pipeline and build-host paths.
- D1/#464 wrapper-side bridge descriptors shipped: generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeDescriptor` helper that captures the exported routine identity, source metadata, parameter metadata, and the package-local manifest/runtime-host paths derived from the loaded wrapper module, so the scaffold carries an explicit future runtime-dispatch contract instead of leaving that bridge shape implicit. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the descriptor-bearing wrapper source in direct pipeline and build-host paths.
- D1/#463 package-local wrapper runtime-path helpers shipped: generated DLL/FLL native-wrapper scaffolds now derive sibling `app.cfmanifest` and `copperfin_runtime_host(.exe)` paths from the loaded module location, the host-toolchain compile check now matches the emitted CMake visibility policy, and native export-contract validation now ignores weak toolchain leakage so DLL/FLL wrapper builds are checked against only the deliberate exported callable surface. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper-bearing wrapper source, generated CMake metadata, compile/build validation, and export-contract preservation before and after materialization.
- D1/#462 debug-manifest feature-flag mirroring shipped: runtime debug manifests now mirror the remaining `feature_flag=` lines from the broad runtime manifest for DLL/FLL package plans, including launcher, host-compatibility, build-output, debug, and security flags, so downstream debug tooling can recover the same feature-rollout contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest feature-flag contract before and after materialization.
- D1/#461 debug-manifest language/AI summary mirroring shipped: runtime debug manifests now mirror the library-output language-integration summary, AI-feature summary, extensibility-guardrail summary, and the aggregate `language_integrations=` / `ai_features=` counts from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same extensibility-summary contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest language/AI-summary contract before and after materialization.
- D1/#460 debug-manifest .NET policy-summary mirroring shipped: runtime debug manifests now mirror the library-output `.NET` policy summary fields (`dotnet_enabled=`, `dotnet_story=`, allowlist/denylist summary and item lines, parity-matrix summary and item lines, and gateway decision lines) from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same extensibility-policy contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest `.NET` policy-summary contract before and after materialization.
- D1/#459 debug-manifest security-summary mirroring shipped: runtime debug manifests now mirror the library-output `runtime_host_sha256=` and `security_roles=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same integrity/security-summary contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest security-summary contract before and after materialization.
- D1/#458 debug-manifest security-metadata mirroring shipped: runtime debug manifests now mirror the library-output `security_enabled=`, `security_role=`, `security_mode=`, and `audit_log_path=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same security contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest security-metadata contract before and after materialization.
- D1/#454 debug-manifest package-root mirroring corrected on `main`: the current debug-manifest builder now actually emits the library-output `package_root=` field for DLL/FLL package plans, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now directly proves that contract before and after materialization.
- D1/#457 debug-manifest transpiled-C#-path mirroring shipped: runtime debug manifests now mirror the library-output `transpiled_csharp_path=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same transpilation-artifact path contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest transpiled-C#-path contract before and after materialization.
- D1/#456 debug-manifest AST/IR-path mirroring shipped: runtime debug manifests now mirror the library-output `ast_manifest_path=` and `ir_manifest_path=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same compiler-artifact path contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest AST/IR-path contract before and after materialization.
- D1/#455 debug-manifest content-root mirroring shipped: runtime debug manifests now mirror the library-output `content_root=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same package-content contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest content-root contract before and after materialization.
- D1/#454 debug-manifest package-root mirroring shipped: runtime debug manifests now mirror the library-output `package_root=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same package-location contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest package-root contract before and after materialization.
- D1/#453 debug-manifest project-path mirroring shipped: runtime debug manifests now mirror the library-output `project_path=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same project-location contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest project-path contract before and after materialization.
- D1/#452 debug-manifest project-title mirroring shipped: runtime debug manifests now mirror the library-output `project_title=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same project-identity contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest project-title contract before and after materialization.
- D1/#451 debug-manifest build-configuration mirroring shipped: runtime debug manifests now mirror the library-output `configuration=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same build-configuration contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest configuration contract before and after materialization.
- D1/#450 debug-manifest warning mirroring shipped: runtime debug manifests now mirror the existing `warning=` lines from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can see no-export and other package-plan diagnostics without reparsing only the runtime manifest. Focused `test_runtime_pipeline` coverage now proves the library-output warning path for both DLL and FLL debug manifests.
- D1/#449 debug-manifest asset-inventory mirroring shipped: runtime debug manifests now mirror the staged `asset=` inventory lines already emitted by the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same wrapper/source/supporting-file inventory without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest asset-inventory contract before and after materialization.
- D1/#448 debug-manifest extension-payload digest mirroring shipped: runtime debug manifests now mirror `extension_payload=` digest lines, including the built DLL/FLL primary output payload after materialization, so downstream debug tooling can verify the same packaged-output digest surface already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest extension-payload contract.
- D1/#447 debug-manifest library feature-flag mirroring shipped: runtime debug manifests now mirror the library-output feature flags (`build.output.library_contract`, `build.output.native_library_wrapper`, and `build.output.fll_api_contract` for FLL builds), so downstream debug tooling sees the same build-contract capability markers already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest feature-flag contract.
- D1/#446 debug-manifest compiler-contract digest mirroring shipped: runtime debug manifests now mirror `compiler_contract=` digest lines, including the generated library contract artifacts for DLL/FLL outputs, so downstream debug tooling can verify the same contract-file integrity surface already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest compiler-contract digest contract.
- D1/#445 debug-manifest export-surface mirroring shipped: runtime debug manifests now mirror the discovered library `export_symbol=` contract for DLL/FLL outputs, so downstream debug tooling can recover the same exported-routine surface already exposed through the broader runtime manifest without reparsing only the `.def` / API artifacts. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest export contract.
- D1/#444 debug-manifest primary-output state mirroring shipped: runtime debug manifests now record `primary_output_path` and `primary_output_materialized` for library outputs, so both the scaffolded pre-build state and the materialized build-host/runtime-pipeline state remain visible through the debug contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest path/state contract.
- D1/#443 debug-manifest library artifact path mirroring shipped: runtime debug manifests now record `module_definition_path` for DLL/FLL outputs and `fll_api_manifest_path` for FLL outputs, so downstream debug tooling can recover the same generated artifact-path contract already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` coverage now proves both the DLL and FLL debug-manifest paths are emitted.
- D1/#442 FLL callable metadata mirroring shipped: the broad runtime/debug manifests now mirror the existing FLL per-function contract metadata (`library_function_kind`, source provenance, parameter declaration style, parameter names, arity, and callable surface) instead of leaving that detail only in the dedicated `.fll.api` contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the FLL wrapper/build-host path surfaces that callable metadata through the broader manifests as well.
- D1/#441 exported routine parameter-declaration metadata shipped: DLL/FLL exported-routine contracts now preserve whether each routine used `PARAMETERS` or `LPARAMETERS`, and the generated FLL `CopperfinFoxInfoRecord` scaffold now carries that declaration-style metadata beside routine kind, source provenance, parameter names, and arity. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove `InitLibrary` preserves `lparameters` while `AddNumbers` preserves `parameters` through the wrapper/build-host path.
- D1/#440 FLL FoxInfo parameter-name metadata shipped: the generated FLL `CopperfinFoxInfoRecord` scaffold now carries per-routine parameter-name metadata alongside routine kind, source path, source line, and arity, so the honest FLL registration surface preserves more callable-contract detail without claiming full VFP loader ABI parity. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove `InitLibrary` and `AddNumbers` carry the expected `tcMode` and `tnLeft|tnRight` metadata in the generated wrapper source.
- I2/#193 AI/MCP permission-boundary fidelity shipped: `copperfin_runtime_host` now requires the existing `ai.mcp` role permission before it will enter AI-assisted federation-planning mode, using `COPPERFIN_SECURITY_ROLE` with a default of `developer`, so optional AI planning no longer bypasses the native security profile. Focused `tests/test_runtime_host_debug_output.cpp` coverage now adds `test_runtime_host_rejects_ai_federation_planning_without_ai_permission()`, and both `test_runtime_host_debug_output` and `test_federation_execution` pass from `build/`.
- I1/#192 extension/host boundary fidelity shipped: `copperfin_runtime_host` now requires security-verified `extension_payload` entries to rebind through their recorded package-relative paths instead of falling back to a same-named file at the package root, so nested payload digests can no longer be satisfied by basename collisions outside the recorded location. Focused `tests/test_runtime_host_debug_output.cpp` coverage now adds `test_runtime_host_rejects_extension_payload_basename_fallback()`, and both `test_runtime_host_debug_output` and `test_runtime_host_binding` pass from `build/`.
- I1/#191 generated-app control and audit fidelity shipped: `copperfin_runtime_host` now reports the effective rebound audit sink through `security.audit_log_path:` during generated-app startup, so deployed packages can see which package-local audit log is active after manifest rebinding. The dedicated `test_runtime_host_binding` smoke now proves both that the rebound `security_audit.log` file is written under the deployed package root and that the runtime host reports that rebound path in stdout.
- I1/#190 runtime/project policy and secret-handling fidelity shipped: `resolve_secret_reference()` now rejects malformed `env:` variable names containing whitespace, control characters, or `=`, so malformed secret references fail deterministically as invalid input instead of degrading into ambiguous missing-variable lookups. Focused `tests/test_security_controls.cpp` coverage now adds `test_secret_provider_rejects_malformed_env_var_names()`, and `test_security_controls` passes from `build/`.
- H3/#203 runtime interop data/state marshaling fidelity shipped: declared-DLL invocation now preserves bare caller `@var` references through expression parsing, native Windows `DECLARE` calls now keep non-by-ref string buffers alive across the call boundary, and native `STRING @` / numeric by-ref parameters now write back into Copperfin variables after the DLL returns when both the declaration and callsite are by-ref. Focused `tests/test_prg_engine_seek_index.cpp` coverage now adds `test_declared_dll_string_byref_argument_writeback()` to prove `DECLARE INTEGER lstrcpyA(STRING @, STRING)` mutates `@cBuffer` end to end.
- H3/#189 MCP/AI and sidecar workflow fidelity shipped: runtime manifests now emit MCP/AI/sidecar workflow fidelity fields (`language_integration*`, `ai_feature*`, `dotnet_policy_*_item`, `dotnet_parity_matrix_item`, and `extensibility_guardrail*`), making external workflow boundaries and policy decisions manifest-visible, and `test_runtime_pipeline` coverage in `test_materialize_runtime_package()` now asserts these fields directly.
- H3/#200 SQL Server translation-dialect fidelity shipped: `translate_fox_sql_to_backend()` now emits SQL Server-compatible boolean literals by mapping FoxPro `.T.`/`.F.` to `1`/`0`, and `tests/test_query_translator.cpp` now includes `test_boolean_literals_are_dialect_sensitive()` to prove SQL Server predicate output avoids generic `TRUE`/`FALSE`.

- H3/#188 host-integration runtime-host resolution shipped with focused host validation: `copperfin_build_host` now resolves the runtime host from its executable directory (with env/override precedence) and normalizes `.exe` vs non-extension host naming for portability, and focused `tests/test_build_host_output` coverage now adds `run_default_runtime_host_resolution_smoke()` to prove executable builds still succeed when `COPPERFIN_RUNTIME_HOST_PATH` is unset.
- D1/#439 generated FLL `FoxInfo` routine-metadata scaffold shipped with focused pipeline and host validation: the generated `CopperfinFoxInfoRecord` scaffold now carries routine kind plus source-path/source-line fields instead of only name/entrypoint/arity, the FLL wrapper source now populates that metadata for each exported routine, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove the richer `FoxInfo` table survives both direct pipeline materialization and the build-host path without claiming full VFP loader ABI parity.
- H2/#187 planning-policy integration for federation execution is now shipped: `FederationExecutionRequest` now carries `enable_ai_assistance`, `require_ai_assistance`, and `policy_audit_enabled`; execution planning now reports deterministic planning metadata (`planning_mode`, `ai_assisted`, `deterministic_translation_succeeded`, `planning_policy_allows_ai`, `planning_policy_audit_enabled`) and `copperfin_runtime_host` now exposes `--federation-planning-enable`, `--federation-planning-require`, and `--federation-planning-audit` so deterministic translation, policy-gated AI fallback, and audit visibility are explicit. Focused `tests/test_federation_execution.cpp` coverage now includes `test_plan_rejects_with_ai_policy_disabled`, `test_plan_allows_ai_fallback_metadata`, `test_plan_requires_ai_fallback_metadata`, and `test_plan_tracks_policy_audit_toggle`.
- D1/#438 exported routine source-provenance contract preservation shipped with focused pipeline and host validation: DLL/FLL callable contracts now preserve the declaring PRG file path and source line for each exported routine, the dedicated DLL/FLL manifests plus the broader runtime manifest now emit `function_source=` / `library_function_source=` lines, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove that provenance survives the wrapper/build-host path end to end without claiming full source-level debugging parity.
- D1/#437 library callable routine-kind contract preservation shipped with focused pipeline and host validation: DLL/FLL callable contracts now preserve whether each exported PRG routine originated from `PROCEDURE` or `FUNCTION` instead of flattening everything into a generic function-shaped surface, the dedicated DLL/FLL manifests plus the broader runtime manifest now emit `function_kind=` / `library_function_kind=` lines, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove that routine-kind metadata survives the wrapper/build-host path end to end without claiming full return-type parity.
- D1/#436 library callable parameter-name contract preservation shipped with focused pipeline and host validation: DLL/OCX wrapper stubs now preserve declared PRG `PARAMETERS` / `LPARAMETERS` names in their generated callable signatures instead of generic `argN` placeholders, the dedicated DLL/FLL API manifests plus the broader runtime manifest now record per-routine parameter-name lists through `function_parameters=` / `library_function_parameters=` lines, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove those names survive the wrapper/build-host path end to end without pretending full type-marshalling parity already exists.
- D1/#435 dedicated DLL callable-contract manifest shipped with focused pipeline and host validation: DLL/OCX package plans now emit a standalone `.dll.api`-style contract artifact beside the generated wrapper assets, that contract records export names, VFP calling convention, routine arity, and placeholder call-surface details, runtime/debug manifests plus build-host output now report its path explicitly, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove the built DLL export surface stays synchronized with that dedicated contract rather than only the broader runtime manifest.
- D1/#434 arity-shaped DLL callable stubs and contracts shipped with focused pipeline and host validation: generated DLL/OCX wrappers now accept placeholder arguments matching discovered PRG routine arity instead of always using no-argument entrypoints, runtime/build manifests now record per-routine `library_function_arity=` plus `library_function_call_surface=` contract lines beside the earlier `vfp_declare_default` calling-convention marker, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove the DLL wrapper/build-host path preserves that richer callable contract without pretending full type-marshalling parity already exists.
- D1/#433 VFP-shaped DLL calling-convention scaffold shipped with focused pipeline and host validation: generated DLL/OCX wrappers now emit an explicit `COPPERFIN_VFP_DLL_CALL` macro that maps to `__stdcall` on 32-bit Windows and a neutral fallback elsewhere, non-FLL library routine stubs now use that calling-convention contract, runtime/build manifests now record `library_callable_convention=vfp_declare_default`, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove the DLL wrapper/build-host path preserves that calling-convention contract without pretending full marshalling parity already exists.
- D1/#432 ParamBlk/_RetInt-shaped FLL callable scaffold shipped with focused pipeline and host validation: generated FLL wrappers now move their callable surface toward the FoxPro/VFP API model by emitting `ParamBlk*` routine stubs that return through a placeholder `_RetInt` helper, the emitted FLL API manifest now records the callable signature plus default return helper and per-function call-surface lines, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove that richer callable contract survives the wrapper/build-host path without pretending full VFP runtime API interoperability is already finished.
- D1/#431 FoxInfo/FoxTable FLL registration scaffold shipped with focused pipeline and host validation: generated FLL wrappers now export both a `FoxInfo()` loader entrypoint and `_FoxTable` registration symbol, the emitted `.def` and `.fll.api` contracts now declare that richer registration surface explicitly, per-routine arity now flows into the FLL API contract, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now prove the wrapper/build-host path preserves the expanded FLL export contract end to end without pretending full VFP loader interoperability already exists.
- D1/#430 honest FXP primary-output contract materialization shipped with focused pipeline and host validation: FXP packages now materialize a deterministic Copperfin logical-statement contract file at the requested `.fxp` output path, record it as the primary output plus an extension payload digest, and keep the token manifest as the source-of-truth contract without pretending the result is already a FoxPro/VFP-native FXP binary, while `test_runtime_pipeline` and `test_build_host_output` now both prove the emitted `.fxp` contract preserves the tokenized statement data end to end.
- D1/#429 honest APP archive primary-output materialization shipped with focused pipeline and host validation: APP packages now materialize a deterministic Copperfin content-archive file at the requested `.app` output path, record it as the primary output plus an extension payload digest, and keep the staged-content manifest as the source-of-truth contract without pretending the result is already a FoxPro/VFP-native APP binary, while `test_runtime_pipeline` and `test_build_host_output` now both prove the emitted archive carries the staged startup/supporting asset payloads end to end.
- D1/#428 build-host wrapper-build integration shipped with focused host validation: `copperfin_build_host` now invokes `build_runtime_package_primary_output(...)` for materialized DLL/FLL/OCX-style packages before reporting success, so the real build-host entrypoint now materializes the requested native library artifact, rewrites the runtime/debug manifests to the built output, and reports `primary_output_materialized=true`, while the new process-level `test_build_host_output` smoke proves both DLL and FLL builds produce the requested primary output path and preserve their declared export contracts through the host path itself.
- D1/#427 runtime-pipeline wrapper-build integration shipped with focused pipeline validation: the runtime pipeline now exposes `build_runtime_package_primary_output(...)` for materialized library-output packages, the helper drives the generated wrapper project into the requested primary output path, marks `primary_output_materialized=true`, records the built artifact as an extension payload, and rewrites the runtime/debug manifests to match, while `test_runtime_pipeline` now proves both DLL and FLL package builds preserve their declared export contracts through that pipeline helper.
- D1/#426 generated wrapper build-script emission shipped with focused pipeline validation: library-output packages now emit turnkey `build_wrapper.sh` and `build_wrapper.ps1` scripts beside the generated wrapper source/CMake metadata, runtime/debug manifests now record those script paths, and `test_runtime_pipeline` now proves the scripts are emitted, describe the intended CMake invocation, and that the POSIX script can materialize the requested primary output path while preserving the existing export-contract checks, further reducing friction around honest DLL/FLL package generation without overstating loader compatibility.
- D1/#425 generated wrapper primary-output placement validation shipped with focused pipeline validation: the emitted wrapper-local `CMakeLists.txt` now routes generated DLL/FLL builds to the package root via explicit output-directory properties, and `test_runtime_pipeline` now proves the generated-CMake path materializes the requested primary output path itself while preserving the filename-identity and export-contract checks, moving the honest library-output pipeline closer to a usable package layout without overstating loader compatibility.
- D1/#424 generated wrapper filename-identity validation shipped with focused pipeline validation: the emitted wrapper-local `CMakeLists.txt` now preserves the requested DLL/FLL artifact basename and suffix (`PREFIX ""`, requested `.dll` / `.fll` suffix), and `test_runtime_pipeline` now proves the generated-CMake path produces `LibraryDemo.dll` / `LibraryDemo.fll` while keeping the existing export-contract checks intact, moving the honest library-output pipeline one step closer to FoxPro/VFP-shaped deliverables without overstating loader compatibility.
- D1/#423 generated-CMake export-contract validation shipped with focused pipeline validation: `test_runtime_pipeline` now inspects the shared-library artifacts produced through the emitted wrapper-local `CMakeLists.txt` and proves those generated-CMake DLL/FLL smoke artifacts export exactly the symbol sets declared by the generated `.def` / `.fll.api` contracts, closing the gap between wrapper-source validation and wrapper-project validation without overstating full FoxPro/VFP loader compatibility.
- D1/#422 generated wrapper CMake build validation shipped with focused pipeline validation: `test_runtime_pipeline` now configures and builds the emitted wrapper-local `CMakeLists.txt` through `cmake -S` / `cmake --build` and proves the generated wrapper project produces the expected shared-library artifact under host CMake/toolchain support, so the build pipeline now validates the emitted wrapper build metadata itself rather than only compiling the wrapper source directly.
- D1/#421 end-to-end wrapper export-contract synchronization shipped with focused pipeline validation: FLL contracts now explicitly declare `FoxInfo` in both the generated `.def` and `.fll.api` outputs, and `test_runtime_pipeline` now proves the compiled DLL/FLL wrapper smoke artifacts export exactly the same symbol sets declared by the generated contract files, tightening the library-output pipeline around a single consistent export surface without overstating full FoxPro/VFP loader compatibility.
- D1/#420 compiled wrapper export-surface validation shipped with focused pipeline validation: `test_runtime_pipeline` now inspects host-built DLL/FLL wrapper smoke artifacts with `nm -D --defined-only` and proves the compiled stubs actually export the expected entrypoints (`InitLibrary`, `AddNumbers`, and `FoxInfo` for FLL), so the build pipeline now validates symbol-surface fidelity in addition to scaffold emission and compileability without overstating full FoxPro/VFP loader compatibility.
- D1/#419 native wrapper compile-smoke validation shipped with focused pipeline validation: `test_runtime_pipeline` now detects a host C++ toolchain and proves the generated DLL/FLL wrapper scaffolds compile as shared-library stubs, so the build pipeline now verifies the emitted native wrapper sources are buildable without overstating them as FoxPro/VFP-compatible runtime bridges.
- D1/#418 native DLL/FLL wrapper scaffold packaging shipped with focused pipeline validation: library-style runtime packages now emit an honest generated native-wrapper scaffold (`wrapper/<stem>_wrapper.cpp`) plus wrapper-local `CMakeLists.txt` build metadata alongside the existing `.def` / `.fll.api` contracts, runtime/debug manifests now record those wrapper artifact paths plus a `build.output.native_library_wrapper` feature flag, and focused `test_runtime_pipeline` coverage now proves DLL/FLL plans and materialized packages carry the wrapper source/build contracts without pretending the final FoxPro/VFP-compatible binary bridge already exists.
- D1/#417 PJX/PJT and DBC/DCT round-trip coverage shipped with focused asset-editor validation: `visual_asset_editor` now infers `.dct` memo sidecars for `.dbc` assets alongside the existing `.pjt` project pairing, and `test_visual_asset_editor` now proves representative direct-field plus memo-backed edits on synthetic `PJX/PJT` and `DBC/DCT` fixtures remain readable and fully undo back to their original serialized values.
- 2026-06-14: H1/#185 relational backend compatibility translation shipped with focused query-translator validation. `translate_fox_sql_to_backend` now remaps legacy FoxPro SQL functions across SQLite, PostgreSQL, SQL Server, and Oracle toward backend-native forms (`LEN`→`LENGTH`, `NVL`/`IFNULL`/`ISNULL`→`COALESCE`/`ISNULL`/`NVL`, `IFNULL`→`COALESCE`), and `test_query_translator` now includes focused `test_legacy_function_compatibility_across_backends()` coverage in `main()`.
- 2026-06-14: H2/#186 document/vector mapping fidelity shipped with focused platform-model coverage. `database_model` now exposes direct lookup helpers (`database_connector_by_id`, `query_translation_path_by_id`), and `test_platform_models` now verifies document/vector connector-family mapping and deterministic-first `foxsql-document` / `foxsql-vector` / `xbase-browse-document` translation-path behavior.
- B2/#409 explicit engine concurrency policy shipped with focused runtime coverage: Copperfin now has a centralized `docs/25-engine-concurrency-policy.md` defining named in-memory critical sections, ascending normalized lock ordering, and the "no blocking while holding a critical section" rule, the runtime source now points directly at that policy beside the shared enforcement helper, and focused `test_prg_engine_table_mutation` coverage now proves an explicit contested `RLOCK()` fails fast under the policy with no retry-backoff events.
- D2/#416 xAsset-aware pause-state breakpoint metadata shipped with focused host coverage: `copperfin_runtime_host` now enriches ordinary pause-state breakpoint listings with `debug.breakpoint[n].xasset.action_id` and `debug.breakpoint[n].xasset.title` when an active breakpoint maps back to an xAsset action, so debugger clients no longer need a separate `break:list` round-trip to recover designer-facing breakpoint identity, and `test_runtime_host_debug_output` now proves that metadata appears during ordinary xAsset pause output with no explicit inventory command.
- D2/#415 xAsset-aware breakpoint inventory shipped with focused host coverage: `copperfin_runtime_host` now enriches breakpoint inventory output with `debug.breakpoint[n].xasset.action_id` and `debug.breakpoint[n].xasset.title` when a listed breakpoint maps to an xAsset action, reusing the same executable-model/bootstrap resolver already used for action-id breakpoint commands, and `test_runtime_host_debug_output` now proves `break:list` round-trips action metadata for xAsset breakpoints.
- D2/#414 xAsset action-id breakpoint commands shipped with focused host coverage: `copperfin_runtime_host` now supports `break:add-action:<action-id>` and `break:remove-action:<action-id>` during xAsset bootstrap sessions, reusing the existing executable-model action bindings plus generated bootstrap source to resolve action ids into real engine breakpoints, and `test_runtime_host_debug_output` now proves add/remove-by-action-id behavior end to end.
- D2/#413 runtime-host single-breakpoint removal shipped with focused host coverage: `copperfin_runtime_host` now supports `break:remove:<file:line>` during debug sessions, `PrgRuntimeSession` now exposes an engine-backed `remove_breakpoint()` path using the same normalized breakpoint identity as add/list, and `test_runtime_host_debug_output` now proves removing one breakpoint leaves unrelated breakpoints active and still pauses on the surviving line.
- D2/#412 runtime-host breakpoint-management commands shipped with focused host coverage: `copperfin_runtime_host` now supports `break:add:<file:line>`, `break:clear`, and `break:list` during debug sessions, the underlying runtime session now exposes deduplicated engine-backed breakpoint inventory through `list_breakpoints()`, and `test_runtime_host_debug_output` now proves add/list/clear plus a live managed-breakpoint pause without restarting the host.
- D2/#411 xAsset debugger action-identity output shipped with focused host coverage: `copperfin_runtime_host` now maps paused xAsset bootstrap frames back onto the executable-model action binding and prints `debug.xasset.action_id`, `debug.xasset.record_index`, `debug.xasset.kind`, and `debug.xasset.title` alongside the existing pause metadata, while the new `test_runtime_host_debug_output` process-level smoke proves a breakpoint inside a nested SCX action reports the originating designer-facing action identity.
- D1/#410 compiler-contract digest manifesting shipped with focused pipeline coverage: materialized runtime packages now record deterministic `compiler_contract=` digest lines for generated contract artifacts such as `.ast.json`, `.ir.json`, `.transpiled.cs`, and output-kind-specific contract files when present, so downstream tooling can verify that copied package contents still match the declared compiler/runtime contract, and focused `test_runtime_pipeline` coverage now proves the digest entries for generated AST/IR/C# artifacts end to end.
- D1/#215 C# xAsset class/object scaffold generation shipped with focused pipeline coverage: the transpilation artifact now also emits compileable C# scaffolding for VCX/SCX-style class/object assets using the existing xAsset executable model, root-object lifecycle methods and nested object methods now surface as deterministic C# members with ordered `RunStartup()` / `RunShutdown()` wrappers, untranslated xAsset bodies stay honest via explicit manual-port exceptions, and focused `test_runtime_pipeline` coverage now proves both the emitted class/object scaffold and a direct `dotnet build` compile check for the generated artifact.
- D1/#214 C# transpilation artifact generation shipped with focused pipeline coverage: runtime packages now emit a target-specific `.transpiled.cs` artifact whenever `.NET`-oriented output is requested, the generated source now maps first-pass procedural PRG constructs (`LOCAL`, assignment, `DO`, `WAIT WINDOW`, and `RETURN`) into deterministic C# scaffolding with stable routine naming, the runtime manifest now records that artifact path plus a `build.output.csharp_transpilation` feature flag, and focused `test_runtime_pipeline` coverage now proves both the emitted source contract and a direct `dotnet build` compile check for the generated artifact.
- D1/#213 IR emission and instruction mapping shipped with focused pipeline coverage: runtime packages now emit a target-specific `.ir.json` artifact alongside the selected output, the IR schema records PRG-relative files, routines, stable statement opcode names, and mapped instruction operands/metadata derived from parsed statements, the runtime manifest now records that IR artifact path as part of the shared compiler contract, and focused `test_runtime_pipeline` coverage now proves the opcode mapping on representative `LOCAL`, assignment, `DO`, and `WAIT WINDOW` statements.
- D1/#212 AST emission schema and fidelity shipped with focused pipeline coverage: runtime packages now always emit a target-specific `.ast.json` artifact alongside the selected output, the AST schema records project title, output kind, PRG-relative paths, routines, and logical statement line/text metadata, the runtime manifest now records that AST artifact path as part of the compiler contract, and focused `test_runtime_pipeline` coverage now proves the AST schema and statement fidelity on a representative PRG source.
- D1/#211 APP archive generation and manifest fidelity shipped with focused workspace/pipeline coverage: project workspaces now infer `.app` outputs as Visual FoxPro application archives, runtime package plans now emit a dedicated `.app.contents` contract only for APP targets, materialized APP packages now record staged content membership and startup fidelity without pretending the primary `.app` archive already exists, and focused `test_project_workspace` plus `test_runtime_pipeline` coverage now proves the APP archive contract end to end.
- D1/#210 FXP tokenized compilation fidelity shipped with focused workspace/pipeline coverage: project workspaces now infer `.fxp` outputs as Visual FoxPro tokenized programs, runtime package plans now emit a dedicated `.fxp.tokens` contract only for FXP targets, materialized FXP packages now capture parsed PRG logical statements by scope/line/text without pretending the primary `.fxp` binary already exists, and focused `test_project_workspace` plus `test_runtime_pipeline` coverage now proves the FXP token contract end to end.
- D1/#209 FoxPro/VFP FLL extension API generation shipped with focused workspace/pipeline coverage: project workspaces now infer `.fll` outputs as Visual FoxPro libraries, runtime package plans now emit an explicit `.fll.api` contract only for FLL targets, materialized FLL packages now carry both the `.def` export map and a `SET LIBRARY TO` / `RELEASE LIBRARY`-oriented API manifest populated from discovered PRG `PROCEDURE` / `FUNCTION` names, and focused `test_project_workspace` plus `test_runtime_pipeline` coverage now proves the FLL contract without pretending the binary itself is already emitted.
- D1/#208 FoxPro/VFP DLL export-definition packaging shipped with focused workspace/pipeline coverage: project workspaces now infer non-`.exe` output kinds from the configured `OUTFILE`, runtime package plans now preserve the configured library filename instead of forcing `.exe`, DLL/FLL/OCX outputs now switch into an explicit `foxpro_library_definition` packaging mode rather than faking a runnable binary, and `test_runtime_pipeline` now proves the package emits a real `.def` file populated from discovered PRG `PROCEDURE` / `FUNCTION` names while honestly recording that the primary DLL binary is still not materialized.
- D1/#207 FoxPro/VFP-compatible runtime resource binding shipped with focused host-binding coverage: `copperfin_runtime_host` now rebinds stale manifest paths back onto the deployed package root for startup source, working directory, audit log, and hashed executable payload verification, so moved native packages no longer depend on the original build-time absolute paths or the caller’s current working directory, and the new `test_runtime_host_binding` CTest smoke proves a relocated security-enabled package with nested payloads still launches successfully from an unrelated working directory.
- D1/#206 FoxPro/VFP-compatible native entrypoint packaging shipped with focused pipeline and host-smoke coverage: native-host packages now materialize a project-named executable entrypoint beside `app.cfmanifest` and `copperfin_runtime_host.exe`, the package manifest now carries a digest for that native entrypoint as part of the packaged executable payload set, `test_runtime_pipeline` now proves the native-fallback package emits and records that entrypoint, and a direct `DemoApp.exe` smoke run proves `copperfin_runtime_host` auto-discovers a sibling manifest when launched through the packaged entrypoint with no explicit `--manifest` argument.
- D1/#205 FRX/LBX/MNX round-trip serialization fidelity shipped with focused asset-editor coverage: `test_visual_asset_editor` now proves synthetic `LBX/LBT` and `MNX/MNT` assets survive direct-field plus memo-field edits, remain readable after the writes, and fully undo back to their original serialized values with no lingering undo residue, extending the existing `FRX` regression across the remaining memo-backed report/menu families.
- D1/#204 SCX/VCX round-trip serialization fidelity shipped with focused asset-editor coverage: `test_visual_asset_editor` now proves a synthetic `VCX/VCT` class library survives adding a new serialized property, remains readable after the write, and cleanly undoes the added property without disturbing the existing serialized blob, locking class-library round-trip safety down beside the existing `SCX` regression.
- D2/#408 paused-session watch evaluation shipped with focused debugger coverage: `PrgRuntimeSession` now exposes watch-expression evaluation against the current paused frame/global/cursor context, `copperfin_runtime_host` now accepts `--debug-command watch:<expr>` without resuming execution, `test_prg_engine` now locks local/global/cursor-field plus empty-watch handling down directly, and a direct runtime-host smoke invocation proves `watch:nLocal` reports the paused worker-frame value.
- D1/#278 deterministic parity manifest and feature-flag routing shipped with focused pipeline coverage: runtime/debug manifests now record the effective launcher mode plus fallback reason, emit explicit feature-flag lines for rollout/host-compatibility/debug/security gates, requested `.NET` launcher emission now falls back deterministically to the native runtime host when `.NET` output is unavailable, `test_runtime_pipeline` now locks both the `.NET` route and native-fallback route down directly, and the managed compile gate passes via `test_managed_compile`.
- D2/#407 step cursor-inspection fidelity shipped with focused debugger coverage: `test_prg_engine` now explicitly proves `step_into`, `step_over`, and `step_out` preserve the selected work area, alias map, and open-cursor inspection state (work area, record position, record count, and source path) alongside the existing statement-text and frame-routing contract across a simple `DO worker` flow.
- D2/#406 breakpoint cursor-inspection fidelity shipped with focused debugger coverage: `test_prg_engine` now explicitly proves a breakpoint pause preserves the selected work area, alias map, and open-cursor inspection state (work area, record position, record count, and source path) alongside the existing statement-text contract.
- D2/#405 step pause-state fidelity shipped with focused debugger coverage: `test_prg_engine` now explicitly proves `step_into`, `step_over`, and `step_out` each pause with `DebugPauseReason::step` and preserve the expected current/next statement text plus caller-vs-callee frame routing across a simple `DO worker` flow.
- D2/#404 breakpoint statement-text fidelity shipped with focused debugger coverage: breakpoint pauses now have explicit regression coverage proving the debug state preserves the current statement text alongside the existing line/frame/locals contract, and `test_prg_engine` now locks both the ordinary in-routine breakpoint and first-line breakpoint cases down directly.
- D1/#403 launcher argument-forwarding fidelity shipped with focused pipeline coverage: the generated .NET launcher now preserves ordinary application arguments instead of dropping everything except `--debug`, while keeping the manifest injection and debug-flag normalization contract intact, and `test_runtime_pipeline` now locks that launcher source contract down directly.
- B2/#402 critical-section policy enforcement shipped with focused runtime coverage: `ENTER CRITICAL` now enforces ascending normalized lock ordering for nested acquisitions, shipped blocking wait surfaces now reject blocking while a critical section is held (`AWAIT`, positive-duration `SLEEP`, and lock-retry/backoff paths under contention), and focused `test_prg_engine_control_flow` plus `test_prg_engine_table_mutation` coverage now proves deterministic `runtime.critical.order_violation` / `runtime.critical.blocking_violation` diagnostics instead of deadlock-prone hangs.
- A3/#271 REPROCESS contention semantics shipped with focused runtime coverage: `SET('REPROCESS')` now defaults to `AUTOMATIC`, lock acquisition paths now honor per-session `REPROCESS` retry budgets across `RLOCK()`, `FLOCK()` / `LOCK()`, `REPLACE`, `APPEND BLANK`, `DELETE`, and `RECALL`, conflicting attempts now emit deterministic `runtime.lock_retry` / `runtime.lock_timeout` diagnostics instead of busy-spinning, and `test_prg_engine_table_mutation` plus `test_prg_engine_work_areas` now prove cross-session contention, retry counts, timeout errors, and data-session isolation/restoration.
- B2/#273 undo-menu and managed-compile validation shipped with focused coverage: the Visual Studio asset editor pane now routes `Undo` through focused text-edit undo or host-backed command undo as appropriate, the native studio host now exposes command-undo availability/labels and executes `--undo-mode command`, `visual_asset_editor` now persists reversible property-edit journals for forms/reports/labels, and the native CTest suite now includes `test_managed_compile` so `Copperfin.LanguageServiceTests`, `Copperfin.Studio`, and `Copperfin.DesignerSmokeTests` must all compile under `dotnet build` during routine validation.
- G2/#401 project-method reference and rename fidelity shipped with focused standalone coverage: `CopperfinProjectInsightClient` now records class-scoped methods under their fully qualified symbol name and maps qualified plus instance-style method calls back to that symbol for reference collection and rename preview, and `Copperfin.LanguageServiceTests` now proves both behaviors.
- G3/#400 source-derived open-cursor alias ingestion shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now infers the default work-area alias from plain `USE <path>` opens and ingests literal `SQLEXEC(..., ..., "alias")` target cursors, and `Copperfin.LanguageServiceTests` now proves both aliases surface in completion and quick-info metadata.
- G1/#399 cross-file project-boundary resolution fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` already indexes symbols across the project root, and `Copperfin.LanguageServiceTests` now proves a procedure defined in one project file resolves from a sibling project file and surfaces the defining signature help instead of leaving that boundary behavior implicit.
- G1/#398 include-driven define resolution fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now follows `#INCLUDE` edges with cycle protection and resolves symbols from included headers even when the header lives outside the project root, and `Copperfin.LanguageServiceTests` now verifies external-header `#DEFINE` navigation and description lookup.
- G1/#397 instance-style method resolution fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now falls back from instance-style tokens such as `oEditor.SaveOrder` to a unique project method with the same trailing name, while refusing ambiguous matches across multiple classes, and `Copperfin.LanguageServiceTests` now verifies both the unique-match and ambiguity cases.
- G3/#396 project method member-completion metadata shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now surfaces project-defined class methods as `member` completions in dotted/member-access contexts and ranks them ahead of the generic fallback member catalog while preserving the originating class path in the description, and `Copperfin.LanguageServiceTests` now verifies that `oEditor.` behavior.
- G1/#395 project method signature-help fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now indexes class-scoped `PROCEDURE` / `FUNCTION` members under their fully-qualified class path, so exact tokens such as `app.customer.editor.SaveOrder` resolve to the method definition and surface method-specific signature help, and `Copperfin.LanguageServiceTests` now verifies both behaviors.
- G3/#183 completion-ranking and context-metadata fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now preserves higher-priority context-specific completion entries instead of overwriting them with generic global symbol entries, so `SELECT`-context aliases stay ahead of unrelated project procedures and keep their context-aware descriptions, and `Copperfin.LanguageServiceTests` now verifies that ranking/description behavior.
- G3/#182 IntelliSense metadata-ingestion fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now ingests cursor aliases created through `CREATE CURSOR` and `SELECT ... INTO CURSOR`, so metadata-driven completions and token descriptions include source-created cursor names rather than only `USE ... ALIAS` aliases, and `Copperfin.LanguageServiceTests` now verifies both ingestion paths.
- G2/#181 rename/refactoring fidelity shipped with focused standalone coverage: `CopperfinProjectInsightClient` now builds a rename preview over the shared definition/reference index, normalizing dotted tokens such as `oToolbar.SaveOrder` back to the project symbol and returning the defining declaration plus all collected call sites, while `Copperfin.LanguageServiceTests` now verifies direct and dotted references both participate in the preview.
- G2/#180 definition/reference-navigation fidelity shipped with focused standalone coverage: `CopperfinProjectInsightClient` now performs a definition pass before reference collection so project-defined procedures/functions become navigable from ordinary call sites, and `Copperfin.LanguageServiceTests` now verifies both direct `SaveOrder(...)` and dotted `oToolbar.SaveOrder(...)` references are captured against the project symbol.
- G1/#179 language-service signature-help fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now derives project procedure/function signatures from adjacent `LPARAMETERS` / `PARAMETERS` declarations and reuses them for dotted invocation fallback, while `Copperfin.LanguageServiceTests` now verifies raw parameter-text preservation and dotted-call signature lookup.
- G1/#178 language-service symbol-resolution fidelity shipped with focused standalone coverage: `FoxProIntelliSenseCatalog` now resolves dotted/member tokens through the longest matching project-symbol prefix before falling back to the trailing member name, and `Copperfin.LanguageServiceTests` now verifies dotted class-prefix navigation, trailing procedure fallback, and quick-info description reuse.

## 2026-06-13

- D2/#20 child slice `#164` advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies debug source roots preserve source-first ordering when source and content roots differ, keeping watch/locals lookup roots deterministic.
- D1/#19 child slice `#163` advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies startup DBF companion assets are staged along with the required startup asset, while the manifest still reports the startup asset as copied.
- D1/#19 child slice `#162` advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies the generated launcher contract itself, including manifest forwarding, `--debug` propagation, package-directory working directory, and the sanitized assembly-name contract in the emitted launcher project.

## 2026-05-12

- Macro/eval slice `#97` advanced across child issue `#380` with focused proof-hardening for nested `TEXTMERGE()` eval surfaces: `test_prg_engine_string_math_functions` now uses a true second-hop holder chain inside merged `EVAL(&macro)` content, verifying `TEXTMERGE('Hello <<EVAL(&cEvalExprDeepHolder)>>!')`.
- Macro/eval slice `#97` advanced across child issue `#379` with focused proof-hardening for `STORE ... TO &target` array-element assignment: `test_prg_engine_arrays` now uses a true second-hop holder chain for the macro target path, verifying `STORE 6 TO &cMacroTargetDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#378` with focused proof-hardening for `DO &macro` routine targets: `test_prg_engine` now uses a true second-hop holder chain for the routine-target path, verifying `DO &cProcDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#377` with focused proof-hardening for the `macro_deep.prg` harness: `test_prg_engine` now uses true second-hop holder chains for the row-expression, field-expression, assignment-target, and `DO ... WITH` argument paths, verifying forms such as `aData[&cRowExprDeepHolder]`, `LEN(&cFieldExprDeepHolder)`, `&cTargetDeepHolder = 'MACROASSIGN'`, and `DO testparam WITH &cParamExprDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#376` with focused proof-hardening for local SQL-style `FOR` expression macros: `test_prg_engine_table_mutation` now uses true second-hop holder chains for the delete/update expression paths, verifying `DELETE FROM People FOR &cDeleteExprDeepHolder` and `UPDATE People SET NAME = 'Thirty' FOR &cUpdateExprDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#375` with focused proof-hardening for SQL cursor `FOR` expression macros: `test_prg_engine_sql_cursors` now uses true second-hop holder chains for the locate/delete/recall expression paths, verifying forms such as `LOCATE FOR &cLocateExprDeepHolder IN sqlcust` and `DELETE FOR &cDeleteExprDeepHolder IN sqlcust`.
- Macro/eval slice `#97` advanced across child issue `#374` with focused proof-hardening for array helper source macros: `test_prg_engine_arrays` now uses a true second-hop holder chain for the source-array branch, verifying forms such as `ALEN(&cArrayNameDeepHolder)` and `&cArrayNameDeepHolder[2,1]`.
- Macro/eval slice `#97` advanced across child issue `#373` with focused proof-hardening for alias-qualified dynamic `AGE` field macros: `test_prg_engine_functions` now uses a true second-hop field-name holder chain for the deepest `AGE` branch, verifying `&cAliasDeepHolder..&cAgeNestedFieldHolder`.
- Macro/eval slice `#97` advanced across child issue `#372` with focused proof-hardening for direct macro self-reference fallback: `test_prg_engine_functions` now uses a true second-hop holder chain for the self-reference branch, verifying `&cSelfRefDeepHolder` still resolves to the literal fallback text `cSelfRef`.
- Macro/eval slice `#97` advanced across child issue `#371` with focused proof-hardening for direct macro fallback text: `test_prg_engine_functions` now uses a true second-hop holder chain for the fallback-text branch, verifying `&cFallbackExprDeepHolder` when the expanded text should remain raw string content.
- Macro/eval slice `#97` advanced across child issue `#370` with focused proof-hardening for the first `COPY TO ARRAY` macro target: `test_prg_engine_data_io` now uses a true second-hop holder chain for the `macroarr` export/readback path, verifying forms such as `&cMacroArrayDeepHolder` alongside the existing deeper companion branch.
- Macro/eval slice `#97` advanced across child issue `#369` with focused proof-hardening for array-helper target names: `test_prg_engine_arrays` now uses true second-hop holder chains for the `ALINES` target-array path and `ACOPY` target-array path, verifying forms such as `&cLinesNameDeepHolder` and `&cCopyNameDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#368` with focused proof-hardening for the first macro-expanded `SCATTER TO` / `GATHER FROM` round-trip: `test_prg_engine_data_io` now uses a true second-hop holder chain for `&cMacroRowNameDeepHolder` across both scatter and gather plus the adjacent macro array reads.
- Macro/eval slice `#97` advanced across child issue `#367` with focused proof-hardening for alias-qualified `AGE` macro reads: `test_prg_engine_functions` now uses a true second-hop holder chain for `&cAliasDeepHolder..&cAgeFieldHolder`, verifying the dynamic `AGE` branch through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#366` with focused proof-hardening for direct alias-qualified macro reads: `test_prg_engine_functions` now uses true second-hop holder chains for `&cAliasDeepHolder..NAME` and `&cAliasDeepHolder..AGE`, verifying direct alias-qualified field reads through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#365` with focused proof-hardening for literal `&stem.suffix` expansion: `test_prg_engine_functions` now uses a true second-hop holder chain for `&cStemDeepHolder.World`, verifying the fallback expanded-string branch through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#364` with focused proof-hardening for `LPARAMETERS` macro defaults: `test_prg_engine_functions` now uses a true second-hop holder chain for `LPARAMETERS nValue = &cDefaultExprDeepHolder`, verifying default-expression binding through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#363` with focused proof-hardening for full SQL `LOOKUP()` macro argument coverage: `test_prg_engine_runtime_surface_functions` now verifies `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, &cAliasDeepHolder, &cTagDeepHolder)`, proving all four SQL macro arguments together through true second-hop holder chains.
- Macro/eval slice `#97` advanced across child issue `#362` with focused proof-hardening for numeric `LOOKUP()` return expressions: `test_prg_engine_runtime_surface_functions` now uses a true second-hop holder chain for `LOOKUP(&cAgeExprDeepHolder, &cSearchExpr, cAlias, cTag)`, verifying the typed numeric return-expression path through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#361` with focused proof-hardening for round-trip `COPY TO ARRAY` macro targets: `test_prg_engine_data_io` now reuses the existing true second-hop holder chain on both sides of the round-trip harness, verifying `COPY TO ARRAY &cTempArrayDeepHolder` alongside `APPEND FROM ARRAY &cTempArrayDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#360` with focused proof-hardening for temporal `COPY TO ARRAY` / `APPEND FROM ARRAY` macros: `test_prg_engine_data_io` now uses a true second-hop holder chain for `&cArrayNameDeepHolder` on the date/datetime round-trip path, verifying typed temporal array transfer through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#359` with focused proof-hardening for `SCATTER NAME` / `GATHER NAME` macro object targets: `test_prg_engine_data_io` now uses a true second-hop holder chain for `SCATTER ... NAME &cObjectNameDeepHolder` and `GATHER NAME &cObjectNameDeepHolder`, verifying the existing additive object-target path through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#358` with focused proof-hardening for `DISPLAY` / `LIST STRUCTURE` macro `IN` targets: `test_prg_engine_data_io` now uses a true second-hop holder chain for `DISPLAY STRUCTURE IN &cAliasDeepHolder` and `LIST STRUCTURE IN &cAliasDeepHolder`, verifying raw/resolved target detail through the shared cursor-designator event path.
- Macro/eval slice `#97` advanced across child issue `#357` with focused proof-hardening for macro-expanded array subscripts and function arguments: `test_prg_engine` now uses true second-hop holder chains for `aData[&cRowExprDeepHolder]` and `LEN(&cFieldExprDeepHolder)` in the `macro_deep.prg` harness.
- Macro/eval slice `#97` advanced across child issue `#356` with focused proof-hardening for macro assignment targets: `test_prg_engine` now uses a true second-hop holder chain for `&target = value`, verifying `&cTargetDeepHolder = 'MACROASSIGN'` through the shared assignment-target resolver.
- Macro/eval slice `#97` advanced across child issue `#355` with focused proof-hardening for `DO ... WITH &macro` argument binding: `test_prg_engine` now uses a true second-hop holder chain for the macro-expanded argument path, verifying `DO testparam WITH &cParamExprDeepHolder` through the shared call-argument binding path.
- Macro/eval slice `#97` advanced across child issue `#354` with focused proof-hardening for `DO &macro` routine targets: `test_prg_engine` now uses a true second-hop holder chain for the macro-expanded routine target, verifying `DO &cProcDeepHolder` through the shared command-target dispatch path.
- Macro/eval slice `#97` advanced across child issue `#353` with focused proof-hardening for `USE IN &alias` closure: `test_prg_engine` now uses a true second-hop holder chain for the macro-expanded alias target, verifying `USE IN &cAliasExprDeepHolder` through the shared alias-target runtime-state path.
- Macro/eval slice `#97` advanced across child issue `#352` with focused proof-hardening for alias-qualified macro field access: `test_prg_engine_functions` now uses a true second-hop holder chain on the alias side in the deepest alias-qualified field-access and `LOCATE FOR` cases, verifying forms such as `&cAliasDeepHolder..&cNestedFieldHolder`.
- Macro/eval slice `#97` advanced across child issue `#351` with focused proof-hardening for local SQL-style `FOR &expr` flows: `test_prg_engine_table_mutation` now uses true second-hop holder chains for `DELETE FROM` and `UPDATE ... FOR` filter expressions, verifying forms such as `&cDeleteExprDeepHolder` and `&cUpdateExprDeepHolder` against the shared local SQL-style filter path and event payloads.
- Macro/eval slice `#97` advanced across child issue `#350` with focused proof-hardening for SQL cursor `FOR &expr` flows: `test_prg_engine_sql_cursors` now uses true second-hop holder chains for `LOCATE`, `DELETE`, and `RECALL` `FOR` expressions, verifying forms such as `&cLocateExprDeepHolder` and `&cDeleteExprDeepHolder` against the shared SQL/result-cursor filter path.
- Macro/eval slice `#97` advanced across child issue `#349` with focused proof-hardening for `STORE ... TO &target` array-element assignment: `test_prg_engine_arrays` now uses a true second-hop holder chain for the macro-expanded array-element target path, verifying `STORE 6 TO &cMacroTargetDeepHolder` through the shared assignment-target resolver.
- Macro/eval slice `#97` advanced across child issue `#348` with a shared array-function parser fix plus focused second-hop proof-hardening: `parse_primary()` now prefers shipped array-helper function calls over same-named runtime arrays when `(` follows an identifier, avoiding misparsing `ALINES(...)` as array element access on an existing `aLines` target, and `test_prg_engine_arrays` now verifies true second-hop macro-expanded array helper/access forms across `ALINES`, `ALEN`, `AELEMENT`, `ACOPY`, and `&array[...]` / `&array(...)`.
- Macro/eval slice `#97` advanced across child issue `#347` with focused proof-hardening for `SCATTER TO` / `GATHER FROM` array-name macros: `test_prg_engine_data_io` now uses a true second-hop holder chain for the array-name path, verifying `&cMacroRowNameDeepHolder` through a real intermediate `cMacroRowNameSecondHopHolder` instead of a first-hop-only alias mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#346` with focused proof-hardening for nested-property `SCATTER/GATHER NAME` macros: `test_prg_engine_data_io` now uses true second-hop holder chains for both macro-expanded object-path segments, verifying forms such as `&cHolderDeepHolder.&cChildDeepHolder` through real intermediate holders instead of first-hop-only aliases mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#345` with focused proof-hardening for direct `&macro` expression indirection: `test_prg_engine_functions` now uses a true second-hop holder chain for the direct indirection case, verifying `&cMacroNameDeepHolder` through a real intermediate `cMacroNameHolder`.
- Macro/eval slice `#97` advanced across child issue `#344` with focused proof-hardening for command-path merged text: `test_prg_engine_control_flow` now uses true second-hop holder chains for `TEXT TO ... TEXTMERGE` nested `EVAL()` and `&macro` interpolation, verifying forms such as `<<EVAL(&cNameExprDeepHolder)>>` and `<<&cFieldExprDeepHolder>>` through real intermediate holders.
- Macro/eval slice `#97` advanced across child issue `#343` with focused proof-hardening for string/eval macro surfaces: `test_prg_engine_string_math_functions` now uses true second-hop holder chains for `TEXTMERGE()` source-template and delimiter arguments plus `EXECSCRIPT()` `RETURN &macro` and script-source paths, verifying forms such as `&cTemplateDeepHolder`, `&cLeftDelimDeepHolder`, `&cExecReturnDeepHolder`, and `&cExecScriptDeepHolder` through real intermediate holder variables.
- Macro/eval slice `#97` advanced across child issue `#342` with focused proof-hardening for command array-name targets: `test_prg_engine_data_io` now uses true second-hop holder chains for `COPY TO ARRAY` and `APPEND FROM ARRAY`, verifying forms such as `&cMacroArrayDeepHolder` and `&cTempArrayDeepHolder` instead of earlier first-hop-only holders mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#341` with focused proof-hardening for aggregate command targets: `test_prg_engine_control_flow` now uses true second-hop holder chains for `COUNT`, `SUM`, and `CALCULATE` `TO` targets, verifying forms such as `&cCountDeepHolder`, `&cSumDeepHolder`, and `&cCalcDeepHolder` instead of the earlier first-hop-only holders mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#340` with focused proof-hardening for SQL cursor `SET` text macros: `test_prg_engine_sql_cursors` now uses true second-hop holder chains for `SET FIELDS TO` and `SET FILTER TO`, verifying forms such as `&cFieldsSpecDeepHolder` and `&cFilterExprDeepHolder` instead of earlier first-hop-only holders mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#339` with focused proof-hardening for runtime-surface path-text evaluation: `test_prg_engine_runtime_surface_functions` now uses a true second-hop holder chain for `EVALUATE()` runtime-state path text, verifying `EVALUATE(&cEvalPathExprDeepHolder)` instead of the earlier first-hop-only holder mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#338` with focused proof-hardening for the headless command detail lane: `test_prg_engine_data_io` now uses true second-hop holder chains for `INPUT` / `ACCEPT`, `WAIT WINDOW`, and `DISPLAY` / `LIST` target-detail checks, so the existing `resolve_runtime_target_identifier()` / `evaluate_cursor_designator_expression()` coverage now verifies forms such as `&cInputTargetDeepHolder`, `&cWaitTargetDeepHolder`, and `&cAliasDeepHolder` instead of first-hop-only holders mislabeled as second-hop.
- Macro/eval slice `#97` advanced across child issue `#337` with a small shared `SET` string-value fix plus focused second-hop runtime-surface coverage: `evaluate_set_string_value()` now reuses bounded raw macro expansion before falling back to expression evaluation, so second-hop holder forms such as `SET MARK TO &cMarkTargetDeepHolder` preserve final text tokens like `"-"` instead of collapsing them through expression parsing, and `test_prg_engine_runtime_surface_functions` now verifies second-hop `SET PATH`, `SET MARK`, `SET DECIMALS`, `SET COLLATE`, `SET NULL`, and `SET ANSI` value macros.
- Macro/eval slice `#97` advanced across child issue `#336` with a small shared `SET` text-macro fix plus focused second-hop SQL cursor coverage: `expand_set_text_macro()` now chases bounded bare-identifier holder hops after the initial `&macro` expansion so stored text such as `&cFieldsSpecHolder -> cFieldsSpec -> LIKE N*` and `&cFilterExprHolder -> cFilterExpr -> AMOUNT >= 20` resolves through the shared `SET FIELDS` / `SET FILTER` path, and `test_prg_engine_sql_cursors` now verifies second-hop `SET FIELDS TO &...` visible-field metadata plus second-hop `SET FILTER TO &... IN sqlcust` positioning and field lookup parity.
- Macro/eval slice `#97` advanced across child issue `#335` with a small shared target-resolution guardrail fix plus focused second-hop aggregate coverage: `resolve_runtime_target_identifier()` now stops before scalar values such as a current `PUBLIC` binding when expanding `&target`, preserving assignment identity across `RELEASE ALL`, and `test_prg_engine_control_flow` now verifies second-hop aggregate `TO` targets for `COUNT`, `SUM`, and `CALCULATE` forms such as `&cCountHolder`, `&cSumHolder`, and `&cCalcHolder`.
- Macro/eval slice `#97` advanced across child issue `#334` with a small shared target-resolution fix plus focused second-hop coverage: `resolve_runtime_target_identifier()` now chases bounded holder hops for `TO &target` assignment/detail paths, and `test_prg_engine_data_io` now verifies second-hop target detail for `INPUT`, `ACCEPT`, and `WAIT WINDOW` plus second-hop `IN` target detail for `DISPLAY` / `LIST`.
- Macro/eval slice `#97` advanced across child issue `#333` with focused second-hop holder coverage: `test_prg_engine_data_io` now verifies `SCATTER TO` / `GATHER FROM` preserve nested holder indirection for command array-name targets such as `&cMacroRowNameDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#332` with a small command object-target helper fix plus focused second-hop coverage: `parse_command_object_target_path()` now chases bounded string-holder hops while stopping before runtime object-reference strings, and `test_prg_engine_data_io` now verifies `SCATTER NAME` / `GATHER NAME` preserve second-hop nested holder indirection for object targets such as `&cHolderDeepHolder.&cChildDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#331` with focused second-hop holder coverage: `test_prg_engine_data_io` now verifies the shared command array-name resolver preserves nested holder indirection for `COPY TO ARRAY` and `APPEND FROM ARRAY` forms such as `&cMacroArrayHolder` and `&cTempArrayHolder`.
- Macro/eval slice `#97` advanced across child issue `#330` with focused second-hop holder coverage: `test_prg_engine_runtime_surface_functions` now verifies `EVALUATE(&cEvalPathExprHolder)` preserves nested holder indirection before evaluating runtime-state path text such as `SET('PATH')`.
- Macro/eval slice `#97` advanced across child issue `#329` with focused second-hop holder coverage: `test_prg_engine_functions` now verifies direct `&macro` expression indirection preserves nested holder hops such as `&cMacroNameDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#328` with focused second-hop holder coverage: `test_prg_engine` now verifies runtime-state `&macro` field-name resolution preserves nested indirection through a second holder hop such as `&cFieldExprDeepHolder`.
- Macro/eval slice `#97` advanced across child issue `#327` with a small `LOOKUP()` miss-path typing fix plus focused second-hop coverage: `LOOKUP()` now derives miss defaults from the fully-resolved macro return-expression text without over-rewriting non-macro expressions, and `test_prg_engine_runtime_surface_functions` now verifies second-hop holder miss paths preserve numeric `0` and logical `.F.` defaults.
- Macro/eval slice `#97` advanced across child issue `#326` with focused second-hop holder coverage: `test_prg_engine_runtime_surface_functions` now verifies local `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)` preserves nested macro indirection through a second holder hop while keeping selected-alias context stable.
- Macro/eval slice `#97` advanced across child issue `#325` with focused second-hop holder coverage: `test_prg_engine_string_math_functions` now verifies the combined nested-surface harness preserves second-hop holder indirection across `EVAL()` and `EXECSCRIPT()` inputs before reaching stored expression or script text.
- Macro/eval slice `#97` advanced across child issue `#324` with focused second-hop holder coverage: `test_prg_engine_control_flow` now verifies command-path `TEXT TO ... TEXTMERGE` preserves nested `EVAL(&holder)` and `&macro` interpolation through second-hop holders such as `<<EVAL(&cNameExprDeepHolder)>>` and `<<&cFieldExprDeepHolder>>`.
- Macro/eval slice `#97` advanced across child issue `#323` with focused second-hop holder coverage: `test_prg_engine_string_math_functions` now verifies `EXECSCRIPT('RETURN &cExecReturnDeepHolder')` preserves nested macro indirection through a second holder hop before return-expression evaluation.
- Macro/eval slice `#97` advanced across child issue `#322` with focused second-hop holder coverage: `test_prg_engine_string_math_functions` now verifies `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)` preserves nested macro indirection through a second holder hop before delimiter-based interpolation.
- Macro/eval slice `#97` advanced across child issue `#321` with focused second-hop holder coverage: `test_prg_engine_string_math_functions` now verifies `TEXTMERGE(&cTemplateDeepHolder)` preserves nested macro indirection through a second holder hop before source-template interpolation.
- Macro/eval slice `#97` advanced across child issue `#320` with focused second-hop holder coverage: `test_prg_engine_string_math_functions` now verifies `EXECSCRIPT(&cExecScriptDeepHolder)` preserves nested macro indirection through a second holder hop before first-pass `RETURN` evaluation.
- Macro/eval slice `#97` advanced across child issue `#319` with focused second-hop holder coverage: `test_prg_engine_runtime_surface_functions` now verifies SQL `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)` preserves nested macro indirection through a second holder hop while keeping non-target selection state stable.
- Macro/eval slice `#97` advanced across child issue `#318` with focused second-hop holder coverage: `test_prg_engine_runtime_surface_functions` now verifies SQL `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)` preserves nested macro indirection through a second holder hop while keeping non-target selection state stable.
- Macro/eval slice `#97` advanced across child issue `#317` with focused second-hop holder coverage: `test_prg_engine_runtime_surface_functions` now verifies `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)` preserves nested macro indirection through a second holder hop while keeping selected-alias context stable.
- Macro/eval slice `#97` advanced across child issue `#316` with focused second-hop holder coverage: `test_prg_engine` and `test_prg_engine_runtime_surface_functions` now verify `EVAL(&cEvalExprDeepHolder)` and `EVALUATE(&cEvalExprDeepHolder)` preserve nested macro indirection through a second holder hop before runtime-state evaluation.
- Macro/eval slice `#97` advanced across child issue `#315` with focused second-hop holder coverage: `test_prg_engine_functions` now verifies `&stem.suffix`, trailing-dot, and adjacent embedded-identifier forms such as `&cTypeDeepHolder.Name`, `&cFieldDeepHolder.`, and `m&cType2DeepHolder.ID` preserve nested macro indirection through a second holder hop.
- Macro/eval slice `#97` advanced across child issue `#314` with focused second-hop holder coverage: `test_prg_engine_functions` now verifies alias-qualified forms such as `&cAliasHolder..&cNestedFieldHolder` preserve nested macro indirection through a second holder hop in both direct reads and `LOCATE FOR` expressions.
- Macro/eval slice `#97` advanced across child issue `#313` with a small embedded-identifier fix plus focused coverage: `parse_identifier()` now recursively expands embedded macro holders before concatenating identifier fragments, and `test_prg_engine_functions` now verifies nested-holder forms such as `m&cType2Holder.ID`.
- Macro/eval slice `#97` advanced across child issue `#312` with focused SQL designator coverage: `test_prg_engine_runtime_surface_functions` now verifies `LOOKUP(..., &cAliasHolder, &cTagHolder)` preserves nested macro indirection for SQL alias/tag designators while keeping non-target selection state stable.
- Macro/eval slice `#97` advanced across child issue `#311` with focused alias-qualified coverage: `test_prg_engine_functions` now verifies nested-holder forms such as `&cAliasHolder..NAME` and `&cAliasHolder..&cNameFieldHolder` preserve macro indirection through alias-qualified field access and `LOCATE FOR` expressions.
- Macro/eval slice `#97` advanced across child issue `#310` with a small parser stem-expansion fix plus focused dot-suffix coverage: `parse_macro_reference()` now recursively expands the macro stem before suffix concatenation, and `test_prg_engine_functions` now verifies nested holder forms such as `&cTypeHolder.Name` and `&cFieldHolder.` preserve macro indirection before identifier resolution.
- Macro/eval slice `#97` advanced across child issue `#309` with a small `LOOKUP()` designator fix plus focused coverage: nested macro-expanded alias/tag designators now chase memory-variable holder indirection without leaking into generic field/alias resolution, and `test_prg_engine_runtime_surface_functions` now verifies `LOOKUP(..., &cAliasHolder, &cTagHolder)` preserves nested macro indirection while keeping selected-alias context stable.
- Macro/eval slice `#97` advanced across child issue `#308` with focused `LOOKUP()` nested-indirection coverage: `test_prg_engine_runtime_surface_functions` now verifies `LOOKUP(&cReturnExprHolder, &cSearchExprHolder, ...)` preserves nested macro indirection for return and search expression text while keeping selected-alias context stable.
- Macro/eval slice `#97` advanced across child issue `#307` with focused nested-indirection coverage: `test_prg_engine` now verifies `EVAL(&cExprHolder)` preserves nested macro indirection and still evaluates final runtime-state expression text such as `ALIAS()`.
- Macro/eval slice `#97` advanced across child issue `#306` with a small shared-eval helper fix plus focused nested-indirection coverage: `EVAL()` / `EVALUATE()` now chase simple macro-expanded bare-identifier hops before evaluating the final stored expression text, and `test_prg_engine_runtime_surface_functions` now verifies `EVALUATE(&cExprHolder)` preserves nested macro indirection.
- Macro/eval slice `#97` advanced across child issue `#305` with focused `EXECSCRIPT()` source coverage: `test_prg_engine_string_math_functions` now verifies `EXECSCRIPT(&cScriptExpr)` preserves macro-expanded script text before first-pass `RETURN` evaluation over current runtime state.
- Macro/eval slice `#97` advanced across child issue `#304` with a small macro-fallback fix plus focused `TEXTMERGE()` source coverage: `parse_macro_reference()` now falls back to raw expanded string text when macro-expanded content is not a valid expression, and `test_prg_engine_string_math_functions` now verifies `TEXTMERGE(&cTemplateExpr)` preserves macro-expanded source-template text before merge interpolation over current runtime state.
- Macro/eval slice `#97` advanced across child issue `#303` with focused runtime-state `EVALUATE()` coverage: `test_prg_engine_runtime_surface_functions` now verifies `EVALUATE(&cExpr)` preserves macro-expanded runtime-state expression text such as `SET('PATH')` instead of collapsing it before evaluation.
- Macro/eval slice `#97` advanced across child issue `#302` with focused `EXECSCRIPT()` return-path coverage: `test_prg_engine_string_math_functions` now verifies `EXECSCRIPT('RETURN &cExpr')` preserves macro-expanded return-expression evaluation over current runtime state.
- Macro/eval slice `#97` advanced across child issue `#301` with focused delimiter-argument coverage: `test_prg_engine_string_math_functions` now verifies `TEXTMERGE(..., &cLeft, &cRight)` preserves macro-expanded delimiter designators while still evaluating nested `EVAL()` and `&macro` expressions inside the merged segments.
- Macro/eval slice `#97` advanced across child issue `#300` with focused custom-delimiter `TEXTMERGE()` coverage: `test_prg_engine_string_math_functions` now verifies `{|...|}` merges preserve nested `EVAL()` and `&macro` interpolation, including recursive custom-delimiter re-entry over stored expression text.
- Macro/eval slice `#97` advanced across child issue `#299` with focused `EVALUATE()` synonym coverage: `test_prg_engine_runtime_surface_functions` now verifies `EVALUATE(&cExpr)` preserves macro-expanded expression text through the shared eval helper instead of collapsing it too early.
- Macro/eval slice `#97` advanced across child issue `#298` with a small `EVAL()` helper fix plus focused coverage: `EVAL()` now preserves simple `&macro` variable text before evaluating the resulting expression, and `test_prg_engine` now verifies `EVAL(&cExpr)` still evaluates runtime-state expression text such as `ALIAS()` in the current context.
- Macro/eval slice `#97` advanced across child issue `#297` with focused `LOOKUP()` miss-path coverage: `test_prg_engine_runtime_surface_functions` now verifies macro-expanded return expressions still preserve typed default miss behavior (`0` for numeric, `.F.` for logical/string-return paths) instead of collapsing into stringly fallback behavior.
- Macro/eval slice `#97` advanced across child issue `#296` with a small `LOOKUP()` designator fix plus focused SQL coverage: `LOOKUP()` now prefers simple `&macro` variable text for alias/tag designators before falling back to full expression evaluation, and `test_prg_engine_runtime_surface_functions` now verifies macro-expanded return/search/alias/tag arguments on synthetic SQL cursors.
- Macro/eval slice `#97` advanced across child issue `#295` with focused `LOOKUP()` macro-expression coverage: `test_prg_engine_runtime_surface_functions` now verifies macro-expanded return and search expression text still resolves through the helper while preserving selected-alias context.
- Macro/eval slice `#97` advanced across child issue `#294` with focused command-path `TEXT ... TEXTMERGE` coverage: `test_prg_engine_control_flow` now verifies merged block interpolation still resolves nested `EVAL()` and `&macro` expressions inside `<<...>>` segments.
- Macro/eval slice `#97` advanced across child issue `#293` with focused nested expansion coverage: `test_prg_engine_string_math_functions` now verifies nested `EVAL(EVAL(...))` expression chaining, `TEXTMERGE()` interpolation through `EVAL(...)`, and macro-expanded `EXECSCRIPT()` return-expression flows over stored expression text.
- Order/seek slice `#92` advanced across child issue `#292` with focused SQL/result-cursor default `PADR(...)` and decimal `STR(...)` coverage: `test_prg_engine_sql_cursors` now verifies representative default right-padding keys and decimal-width numeric-format keys resolve and position on the expected synthetic SQL rows.
- Order/seek slice `#92` advanced across child issue `#291` with focused SQL/result-cursor default-padding and `STR()` variant coverage: `test_prg_engine_sql_cursors` now verifies representative `PADL(name, n)` default-padding keys and default-width `STR(amount)` keys resolve and position on the expected synthetic SQL rows.
- Order/seek slice `#92` advanced across child issue `#290` with focused SQL/result-cursor `RIGHT(...)`/`STR(...)` temporary-order coverage: `test_prg_engine_sql_cursors` now verifies representative suffix-extraction and numeric-formatting seek expressions resolve and position on the expected synthetic SQL rows.
- Order/seek slice `#92` advanced across child issue `#289` with focused SQL/result-cursor command-path derived-order coverage: `test_prg_engine_sql_cursors` now verifies representative `SET ORDER TO UPPER(LEFT(...))` and `SET ORDER TO UPPER(PADR(...))` command flows position `SEEK` on the expected synthetic SQL rows.
- Order/seek slice `#92` advanced across child issue `#288` with focused SQL/result-cursor derived-string temporary-order coverage: `test_prg_engine_sql_cursors` now verifies representative `LEFT(...)`, `SUBSTR(...)`, and `PADL(...)` one-off SQL seek expressions resolve and position on the expected rows.
- Order/seek slice `#92` advanced across child issue `#287` with focused SQL/result-cursor `SET NEAR` session-isolation coverage: `test_prg_engine_sql_cursors` now verifies a missed SQL seek in one data session honors `SET NEAR ON`, a fresh second session retains default `SET NEAR OFF` EOF positioning, and restoring the first session restores its near-positioning behavior.
- Order/seek slice `#92` advanced across child issue `#286` with focused SQL/result-cursor `SET EXACT` coverage: `test_prg_engine_sql_cursors` now verifies SQL temporary-order prefix seeks succeed with `SET EXACT OFF`, fail with `SET EXACT ON`, leave EOF set on the failed exact seek, and remain scoped to the current data session.
- Order/seek slice `#92` advanced across child issue `#285` with focused SQL/result-cursor targeted-collation coverage: `test_prg_engine_sql_cursors` now verifies that `SET ORDER TO NAME IN sqlcust` plus `SEEK('bravo', 'sqlcust', 'NAME')` respects `SET COLLATE TO GENERAL`, positions only the targeted SQL cursor on the case-folded match, and preserves the selected non-target alias/record pointer.
- D3/#166 build/run orchestration fidelity advanced with focused runtime-pipeline coverage: runtime-host source validation now occurs before asset staging, and `test_runtime_pipeline` now verifies invalid host inputs fail materialization without staging startup assets.
- D3/#167 deploy and migration-report fidelity shipped with focused runtime-pipeline coverage: `test_runtime_pipeline` now locks the packaging/manifest diagnostics contract around launcher emission, runtime-host bundling, and report-facing manifest keys.
- E1/#168 normalized design-model fidelity shipped with focused project-workspace coverage: `test_project_workspace` now locks header-derived workspace normalization, grouping, startup selection, and excluded-item fallback behavior.
- E1/#169 memo-heavy round-trip preservation shipped with focused DBF-table coverage: `test_dbf_table` now locks SCX/SCT memo sidecar repair and payload preservation through a full round-trip.
- E2/#170 designer interaction fidelity shipped with focused visual-asset-editor coverage: `test_visual_asset_editor` now locks memo-backed property-bag rewrites and direct-field edits through the parsed visual-asset table contract.
- E2/#171 builder and context-editor fidelity shipped with focused studio-host coverage: `test_studio_host` now locks launch parsing and context-aware document-open behavior through the host boundary.
- E3/#172 report/label layout fidelity shipped with focused report-layout coverage: `test_report_layout` now locks band grouping, object capture, and expression surface fidelity through the parsed report contract.
- E3/#173 report/label property-editing fidelity shipped with focused visual-asset-editor coverage: `test_visual_asset_editor` now locks report-field editing and memo-backed property-bag rewrites through the parsed visual-asset table contract.
- F1/#174 Visual Studio pane and tool-window parity shipped with focused product-subsystems coverage: `test_product_subsystems` now locks the planned toolbox/task-pane entry in the shell registry alongside the implemented surfaces.
- F1/#175 editor/designer integration parity shipped with focused studio-host coverage: `test_studio_host` now locks launch parsing and context-aware open-document behavior through the Visual Studio host boundary.
- F2/#176 standalone workspace and project-shell parity shipped with focused project-workspace coverage: `test_project_workspace` now locks grouping, startup selection, and excluded-asset fallback behavior through the project-shell contract.
- F2/#177 standalone build/debug workflow parity shipped with focused runtime-pipeline coverage: `test_runtime_pipeline` now locks runtime package materialization, launcher forwarding, and debug-plan derivation through the standalone workflow contract.
- D2/#165 breakpoint and stepping fidelity shipped with focused runtime coverage: `prg_engine` now allows first-line breakpoints to trigger after stop-on-entry continue, while preserving one-shot same-location breakpoint skip on immediate resume, and `test_prg_engine` now locks that behavior.
- D2/#164 watches/locals evaluation fidelity advanced with focused runtime-pipeline coverage: debug launch `source_roots` now normalize and deduplicate overlapping paths, and `test_runtime_pipeline` now locks single-root manifest output when source and content resolve identically.
- D1/#163 semantic manifest and asset-contract slice advanced with focused runtime-pipeline coverage: runtime manifest `asset=` entries now include a terminal copied-state field (`true`/`false`), and `test_runtime_pipeline` now verifies staged startup assets report copied true while excluded non-runtime assets report copied false.
- C5/#162 build/run workflow diagnostics slice advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies unresolved startup-record indices emit expected warnings and keep startup debug capabilities disabled.
- C4/#161 project build-inclusion slice advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies startup program assets are staged into package content even when the startup entry is marked excluded.
- C4/#160 project startup resolution slice advanced with focused runtime-pipeline coverage: `test_runtime_pipeline` now verifies uppercase `.PRG` startup sources still enable breakpoint and step-debug capabilities.
- C3/#159 menu lifecycle/cleanup slice advanced with focused executable-model coverage: `test_xasset_methods` now verifies single cleanup shutdown emission and exact cleanup routine/line identity after menu startup activation.
- C3/#158 menu dispatch/action slice advanced with focused executable-model coverage: `test_xasset_methods` now verifies startup setup→activate ordering and action routine bindings for menu item dispatch.
- C2/#157 label execution/preview slice advanced with focused executable-model coverage: `test_xasset_methods` now verifies label preview models emit only the direct startup preview command with no synthetic methods/actions or shutdown lines.
- C2/#156 report execution/output slice advanced with focused executable-model coverage: `test_xasset_methods` now verifies report preview models emit only the direct startup preview command with no synthetic methods/actions or shutdown lines.
- C1/#155 form data-environment/object-graph slice advanced with focused executable-model coverage: `test_xasset_methods` now verifies root-form selection ignores comment/data-environment records and preserves nested object-path action identifiers.
- C1/#154 form/class lifecycle slice advanced with explicit routine-order contract coverage: `test_xasset_methods` now verifies startup/shutdown sequence ordering in executable models for form assets (`BeforeOpenTables/OpenTables -> Load/Init/Activate`, shutdown `Destroy -> CloseTables`) and class-library assets (`Load -> Init`, then `Destroy`).
- Structural table-operation slice `#94` advanced with focused memo-rollback pointer stability coverage: `test_prg_engine_table_structure` now verifies `PACK MEMO` rollback preserves `RECNO()` position while restoring memo sidecar bytes and on-disk readability.
- SQL/result-cursor slice `#93` advanced with focused selected-cursor JSON-import filter coverage: `test_prg_engine_sql_cursors` now verifies `APPEND FROM ... TYPE JSON FOR ...` applies `FOR` filtering and appends only matching rows.
- Order/seek slice `#92` optimizer-support contract coverage deepened: `test_prg_engine_index_seek_optimization` now verifies stable `IndexSeekPlan::ExecutionStrategy` enum values and default `IndexOrderCandidate` initialization used by index-planning diagnostics.
- Field-transfer slice `#100` advanced with focused array transfer-filtering coverage: `test_prg_engine_data_io` now verifies `SCATTER FIELDS LIKE N* TO <array>` includes only wildcard-matching fields and `GATHER FROM <array> FIELDS EXCEPT AGE` updates selected fields while preserving excluded `AGE`.
- Runtime safety slice `#151` advanced with deeper repeated-continue coverage: focused `test_prg_engine_control_flow` now validates two-fault `CONTINUE` stability in a non-default data session (`SET DATASESSION TO 2`), including selected-alias and record-position preservation after each pause/resume.
- Runtime diagnostics slice `#152` advanced with repeated-fault stack-frame coverage: focused `test_prg_engine_control_flow` now validates intermediate caller-frame metadata refresh across back-to-back nested faults, including correct routine/line (resume-PC) updates per pause.
- Runtime diagnostics slice `#153` advanced with repeated-fault normalization coverage: focused `test_prg_engine_control_flow` now validates `AERROR()`/`ERROR()`/`MESSAGE()`/`LINENO()`/`PROGRAM()` refresh coherently across two sequential `ON ERROR` faults, including per-fault line and message updates.
- Runtime-state slice `#98` advanced with additional `SET()` session-isolation coverage: focused `test_prg_engine_runtime_surface_functions` now validates default/update/restore behavior for `FDOW`, `FWEEK`, `POINT`, `SEPARATOR`, and `CURRENCY` across `SET DATASESSION` transitions.
- Memory-variable slice `#99` advanced with additional `RELEASE ALL` shadow-lifetime coverage: focused `test_prg_engine_control_flow` now validates that `PRIVATE` shadow bindings are cleared while saved outer globals are restored in-frame and preserved after routine return.
- Headless interaction slice `#101` advanced with additional target-scope coverage: focused `test_prg_engine_data_io` now validates `INPUT`/`ACCEPT` `TO LOCAL` assignments stay local, receive deterministic empty-string headless results, and do not leak globals after routine return.
- SQL/result-cursor slice `#93` advanced with selected-cursor non-DBF export coverage: focused `test_prg_engine_sql_cursors` now validates `COPY TO ... TYPE JSON FIELDS ...` on selected SQL/result cursors preserves alias/pointer state while materializing filtered JSON output.
- Structural table-operation slice `#94` advanced with rollback pointer-stability coverage: focused `test_prg_engine_table_structure` now validates `ALTER TABLE ... ROLLBACK` preserves open-cursor `RECNO()` position while restoring schema/readability.
- Help-file alignment update landed in `docs/23-vfp-help-and-component-mining.md`: active help-mining targets now explicitly map shipped `#93/#94/#99/#101` behaviors to the remaining `#100` field-transfer residual investigation surface.
- Runtime safety slice `#150` advanced: fault metadata snapshots now include originating data-session id, and recovery paths (`RETRY`, `RESUME`, and normal `ON ERROR` unwind) now restore captured data-session/cursor state before post-fault execution resumes. Focused `test_prg_engine_control_flow` coverage now guards against handler-side `SET DATASESSION` / `SELECT` state drift leaking past the fault boundary.
- Phase A query-path optimization advanced with a trademark-neutral `Index Seek Optimizer` lane. Runtime now analyzes simple `FOR` filter expressions, scores available index orders, and attempts a seek-first execution path in `locate_next_matching_record(...)` before falling back to linear scan; a new focused `test_prg_engine_index_seek_optimization` target now locks down the optimizer type/plan surface and build wiring. In the same pass, a `SET FIELDS` regression in alias-qualified hidden-field lookups was fixed so hidden fields evaluate to empty values instead of aborting script execution, restoring `test_prg_engine_work_areas` parity.
- E1/#91 child slices `#275` and `#276` are now shipped. The platform extensibility profile now carries an explicit .NET parity matrix (with `exact`/`adapted`/`intentionally_not_supported` tiers, rationale tags, and verification references), plus allowlist/denylist policy rules for managed interop. A policy-driven gateway (`evaluate_dotnet_interop_call`) now returns deterministic `allow` / `fallback_native` / `reject` decisions based on policy list membership, latency budget, and reflection-on-untrusted guardrails. Runtime package manifests now expose policy and gateway diagnostics via `dotnet_policy_*`, `dotnet_parity_matrix_entries`, and `dotnet_gateway_*` fields. Focused `test_platform_models` and `test_runtime_pipeline` coverage now locks down the defaults and decision outputs.
- Planning queue update: opened .NET parity umbrella `#274` with child slices `#275`-`#280` to close major .NET-vs-stack capability gaps without inheriting undesirable legacy behavior. The queue codifies parity allowlist/denylist policy, perf/security eligibility gates for .NET calls, C++ vs C# wrapper routing strategy, deterministic manifest/feature flags, interop threat-model enforcement, and a first high-value parity surface pack for FP/VFP-friendly facades.
- H3/#93 child queue issue `#267` is now shipped. Selected SQL/result cursor `APPEND FROM TYPE JSON` now materializes JSON-sourced rows into in-memory remote/result cursor rows using key-to-field-name mapping, `FIELDS` filtering, and `FOR` expression filtering. Previously this path returned a "DBF source only" stub error. Focused `test_prg_engine_sql_cursors` coverage now locks down JSON → remote-cursor append parity.
- H3/#93 child queue issue `#268` is now shipped. Selected SQL/result cursor `APPEND FROM TYPE CSV` (and DELIMITED) now materializes delimited-text rows into in-memory remote/result cursor rows using positional field mapping, header-row skip for CSV, `FIELDS` filtering, and `FOR` expression filtering. Previously this path returned a "DBF source only" stub error. Focused `test_prg_engine_sql_cursors` coverage now locks down CSV/DELIMITED → remote-cursor append parity.
- H3/#93 child queue issue `#263` is now shipped. `COPY TO` no longer event-stubs when the selected cursor is a synthetic SQL/result cursor with no backing DBF path: runtime export now materializes DBF output directly from remote/result cursor schema+rows (including width inference across all remote rows), preserves selected alias/record pointer state, and is locked down by new focused `test_prg_engine_sql_cursors` coverage.
- H3/#93 child queue issue `#264` is now shipped. `APPEND FROM` no longer event-stubs on selected SQL/result cursors: first-pass DBF source append now mutates in-memory remote/result rows using destination field-name mapping and `FIELDS` filtering, while unsupported non-DBF source types now return deterministic runtime diagnostics instead of silent stubs. Focused `test_prg_engine_sql_cursors` coverage now locks down selected SQL/result cursor append parity.
- H3/#93 child queue issue `#265` is now shipped. `COPY STRUCTURE TO` now exports schema from selected SQL/result cursors through the canonical cursor-descriptor path, including metadata-only remote cursors with zero data rows. Focused `test_prg_engine_sql_cursors` coverage now proves schema-only DBF export parity for SQL metadata cursors.
- H3/#93 child queue issue `#266` is now shipped. Selected SQL/result cursor `APPEND FROM` now applies `FOR` filtering during DBF-row append instead of ignoring the clause, so only rows matching the expression in target-cursor context are materialized. Focused `test_prg_engine_sql_cursors` coverage now locks down `APPEND FROM ... FOR ...` filtering parity for selected SQL/result cursors.
- H3/#97, #99, #94, #100, and #93 advanced across slice issues `#258`-`#262`. `test_prg_engine_functions` now locks down alias-qualified macro field indirection such as `&cAlias..&cField`, including nested memory-variable indirection and command-context use. `test_prg_engine_control_flow` now proves macro-expanded assignment targets preserve `PRIVATE` / `PUBLIC` identity across `RELEASE` flows instead of collapsing to current values. `test_prg_engine_table_structure` now explicitly proves `ALTER TABLE` and `PACK MEMO` remain readable after transaction rollback. `test_prg_engine_data_io` now adds wildcard `FIELDS LIKE` / `FIELDS EXCEPT` proof coverage for `SCATTER/GATHER MEMVAR` and DBF `COPY TO` / `APPEND FROM` transfer flows. `test_prg_engine_sql_cursors` now proves targeted `APPEND BLANK IN <sql-alias>` mutation preserves the selected alias and non-target result-cursor pointer while mutating only the requested SQL cursor.
- H3/#92 collation/order residual coverage advanced with focused `test_prg_engine_seek_index` parity for plain temporary orders targeted via `IN <alias|work area>`. New regression coverage proves that `SET ORDER TO NAME IN People` with `SEEK(..., 'People', 'NAME')` follows `SET COLLATE TO GENERAL` case-folding expectations while preserving the selected non-target alias and record pointer.
- Gap-03 staged-write failure-injection and rollback-hardening coverage advanced across slice issues `#266`, `#267`, and `#268` in `test_dbf_table`. New focused tests verify that injected DBF promote failures preserve original table bytes and clean staged artifacts, injected memo sidecar write failures roll back consistently without leaking `.cptmp`/`.cpbak` files, and staged rollback paths preserve on-disk state when promotion fails mid-flow.
- Additional DBF header guardrail coverage advanced across three focused slices in `test_dbf_table`, verifying rejection of zero record-length headers, rejection of header lengths below the DBF minimum, and explicit rejection of header-length claims that exceed physical file size.
- `src/vfp/dbf_table.cpp` staged-write internals now include deterministic test-only failure-injection checkpoints (`temp-open`, `before-backup`, `before-promote`) gated by `COPPERFIN_TEST_FAIL_WRITE_PATH_CONTAINS` and `COPPERFIN_TEST_FAIL_WRITE_STAGE`, enabling repeatable rollback validation without host-specific disk-fault harnesses.
- Gap-driven DBF robustness coverage advanced across slice issues `#260`, `#261`, `#262`, `#263`, `#264`, and `#265` in `test_dbf_table`. New focused tests now lock down: currency max/min boundary round-trip for `Y` fields (`#260`), NaN/+INF/-INF write/read survivability for `B` fields (`#261`), descriptor parsing bounded by declared `header_length` (`#262`), rejection of record-width/field-layout mismatches during mutation (`#263`), stable placeholder diagnostics for malformed memo sidecar metadata (`#264`), and tolerance of full 11-byte non-null-terminated field names (`#265`).
- Runtime DBF parsing was hardened in `src/vfp/dbf_table.cpp` so descriptor scanning stops at the declared header boundary, preventing descriptor-shaped bytes beyond `header_length` from being interpreted as schema.

## 2026-05-11 (Runtime Diagnostics)

- Gap-06 security-control edge coverage advanced across slice issues `#247`, `#255`, `#245`, `#248`, and `#252`. `test_security_controls` now verifies that `role_has_permission` returns `false` for an unknown role and for an empty-string role, returns `false` for an empty permission string and for a nonexistent permission, that `resolve_secret_reference` returns a non-ok result when the named environment variable is absent, that corrupting the hash field of the last audit log entry causes the next appended entry to record a different chain hash (tamper detectability), and that `append_immutable_audit_event` returns a non-ok result when the parent directory cannot be created. All focused assertions pass.
- Gap-08 transaction and work-area coverage advanced across slice issues `#251` and `#249`. `test_prg_engine_table_mutation` now verifies that a `ROLLBACK` after a single `INSERT INTO` returns the table to its pre-transaction record count and that the first record is unchanged, confirming both in-memory state and the physical DBF. `test_prg_engine_work_areas` now verifies that opening the same table in two aliases via `USE ... ALIAS Area1 IN 0` followed by `USE ... ALIAS Area2 AGAIN IN 0`, replacing a field via Area1, and then reading back from Area2 in the same data session returns the mutated value and that the mutation is persisted on disk.
- Gap-04 empty-table coverage extended across slice issues `#239`, `#238`, and gap-09b `#246`. `test_prg_engine_control_flow` now verifies that `GO TOP` and `GO BOTTOM` on empty tables complete without crashing and correctly leave both `BOF()` and `EOF()` true. `test_prg_engine_data_io` now verifies that `COPY TO` on a zero-record source produces a readable empty DBF file, that `APPEND FROM` that empty DBF succeeds and yields zero additional records, and that a SQL pass-through fault followed by `AERROR()` returns a non-zero row count, non-zero error code, and non-empty error message.
- Gap-05 field-write edge coverage advanced across slice issues `#242`, `#243`, and `#244`. Focused `test_prg_engine_table_mutation` coverage now verifies that `REPLACE` truncates over-width character values to field width, exact-width character values round-trip without mutation, and memo fields accept `REPLACE ... WITH ''` as an empty-string update.
- Runtime write semantics were tightened so character overflow truncation is scoped to `REPLACE` command paths only, while `INSERT INTO` retains strict overflow failures and rollback behavior. This preserves existing insert-rollback safety contracts while aligning replacement behavior with the new gap-05 coverage.
- Gap-04 empty-table runtime behavior gained focused regression coverage across slice issues `#237`, `#241`, and `#240`. `test_prg_engine_control_flow` now verifies that `SCAN` bodies do not execute over empty tables and preserve `EOF()` state, aggregate helpers (`COUNT()`, `SUM()`, `AVERAGE()`) return deterministic zero values on empty tables, and `LOCATE FOR .T.` on empty tables keeps `FOUND()` false with `EOF()` true. Focused control-flow validation passes.
- Gap-07 call-depth guardrail boundary precision advanced across slice issues `#254`, `#250`, and `#253`. Focused `test_prg_engine_control_flow` coverage now asserts that nested routine chains exactly at the configured `MAX_CALL_DEPTH` succeed, one-over-limit chains fail with the expected guardrail error, and `config.fpw` custom call-depth overrides enforce that same boundary behavior. Runtime guardrail evaluation in [src/runtime/prg_engine_session.inl](src/runtime/prg_engine_session.inl) was corrected for off-by-one behavior so the configured limit is treated as an inclusive boundary.

## 2026-05-11

- Critical-path Phase A runtime diagnostics advanced across slice issues `#151`, `#152`, `#153`, `#257`, `#258`, and `#259`. `test_prg_engine_control_flow` gains five new focused regression tests: cursor record position is preserved across a multi-fault CONTINUE cycle (#151), intermediate call-stack frame line numbers at a fault pause reflect the resume PC rather than zero (#152), AERROR() columns agree with ERROR()/MESSAGE()/LINENO()/PROGRAM() diagnostic functions inside an ON ERROR handler (#153), division by zero in a PRG expression now dispatches a runtime error pause rather than silently producing +Infinity (#257, guardrail added to `src/runtime/prg_engine_expression.inl`), and a numeric field overflow REPLACE attempt is either diagnosed or safely bounded rather than silently stored out-of-range (#258). `test_dbf_table` gains one new focused regression test: a DBF file whose header claims more records than the file can physically contain is either rejected or clamped to the physically available records without an out-of-bounds read (#259, GAP-02).

## 2026-05-03

- Added a lightweight DO-178C-inspired, issue-only traceability discipline to [remaining-work.md](remaining-work.md), including a mapped checklist by active lane/family (`#92`-`#101`, `#13`/`#14`, `#15`-`#18`, `#19`-`#21`, `#22`-`#26`, `#27`-`#29`, `#30`-`#32`/`#57`/`#91`, `#33`/`#34`, and `#35`-`#37`), a stable `RQ-*`/`VR-*` identifier schema, and closure gates that work without pull requests.
- Extended [remaining-work.md](remaining-work.md) with an aircraft-grade safety-critical documentation profile that treats documentation changes as potentially safety-relevant, adds `DQ-*`/`DV-*`/`HZ-*` trace identifiers, and requires hazard linkage, misuse analysis, independent review evidence, simulation/walkthrough proof, and rollback/notification planning for investigation-ready closure.
- Added an issue form template at [.github/ISSUE_TEMPLATE/traceability-slice.yml](.github/ISSUE_TEMPLATE/traceability-slice.yml) so prompt-sized slices can capture requirement IDs, verification IDs, evidence, and safety/security impact in a consistent format.
- Added a dedicated safety documentation issue form at [.github/ISSUE_TEMPLATE/safety-critical-documentation-change.yml](.github/ISSUE_TEMPLATE/safety-critical-documentation-change.yml) for investigation-grade documentation changes with required hazard linkage, procedural delta mapping, misuse analysis, independent review evidence, and rollback/field notification planning.
- Added an optional pull request template at [.github/pull_request_template.md](.github/pull_request_template.md) that mirrors the same traceability fields (`RQ-*`, `VR-*`, evidence, compatibility delta, and checklist) and now includes explicit safety-case fields for documentation changes (`DQ-*`, `DV-*`, `HZ-*`, independent review, misuse analysis, and rollback communication planning).
- Added a controlled hazard register at [docs/safety/hazard-register.md](docs/safety/hazard-register.md) with stable `HZ-*` identifiers, severity scale, investigation checklist, and starter hazards for runtime debug recovery and command-guidance ambiguity.
- Updated agent guidance in [agents.md](agents.md) and [.github/agents/copperfin-vfp-runtime.agent.md](.github/agents/copperfin-vfp-runtime.agent.md) so automation must apply `DQ-*`/`DV-*`/`HZ-*` evidence, hazard-register linkage, misuse analysis, independent review evidence, and investigation-ready closure for safety-relevant documentation changes.
- Tightened issue templates to require hazard-register linkage for documentation traceability fields in [.github/ISSUE_TEMPLATE/traceability-slice.yml](.github/ISSUE_TEMPLATE/traceability-slice.yml) and [.github/ISSUE_TEMPLATE/safety-critical-documentation-change.yml](.github/ISSUE_TEMPLATE/safety-critical-documentation-change.yml).
- Added [scripts/validate-safety-traceability.ps1](scripts/validate-safety-traceability.ps1), a release-oriented validator that checks safety-documentation issue evidence for `DQ-*`/`DV-*`/`HZ-*` linkage, verifies hazard ids against the controlled register, enforces investigation-evidence section presence, and can fetch issues directly from the GitHub API using issue numbers.
- Added manual workflow gate [.github/workflows/safety-traceability-gate.yml](.github/workflows/safety-traceability-gate.yml) so maintainers can run the validator with the release issue set and retain an artifacted validation report.
- Expanded [docs/safety/hazard-register.md](docs/safety/hazard-register.md) with primary hazard identifiers for data corruption (`HZ-data-corruption-01`), system failure (`HZ-system-failure-01`), and runtime crashes (`HZ-runtime-crash-01`), and documented release-gate usage of the validator.
- Updated [agents.md](agents.md), [agent-handoff.md](agent-handoff.md), and [.github/agents/copperfin-vfp-runtime.agent.md](.github/agents/copperfin-vfp-runtime.agent.md) to require safety traceability gate execution and report retention during release-tag preparation.
- Added a full user-feedback intake template suite under [.github/ISSUE_TEMPLATE](.github/ISSUE_TEMPLATE) covering bug reports, runtime crash/system failure reports, data corruption reports, safety-incident feedback, documentation feedback, feature requests, and general feedback so incoming reports are structured and triage-ready.
- Added [.github/ISSUE_TEMPLATE/config.yml](.github/ISSUE_TEMPLATE/config.yml) with contact links for private security reporting and safety traceability guidance.
- Added feedback triage rubric at [docs/safety/triage-rubric.md](docs/safety/triage-rubric.md), mapping all eight issue template types to severity classification, initial response targets, escalation paths, triage ownership, and hazard register cross-references.
- Added test coverage gap analysis at [docs/safety/test-coverage-gap-analysis.md](docs/safety/test-coverage-gap-analysis.md) identifying nine gap categories (GAP-01 through GAP-09) with hazard register linkage, 31 proposed new test functions, and recommended implementation order prioritised by HZ-data-corruption-01 risk.

## 2026-05-01

- Slice issue `#150` is now closed. The runtime engine now correctly persists data session state during both script-level faults and catastrophic C++ exceptions (e.g. `LOG(-1)`) by snapshotting the `DataSessionState` to the `error_metadata_stack`. The `RETRY` command has been enhanced to pop the error metadata stack and restore the snapshotted session state, ensuring correct recovery of cursors and work areas after an error handler intervenes. The parser was also refactored to flatten the long command dispatch chain, resolving MSVC C1061 limits. Focused `test_prg_engine_control_flow` coverage now validates fault persistence, clean session recovery, and `RETRY` loop completion.

## 2026-04-30

- The nearby `FIELDS` lane deepened again toward completion. Command-level `FIELDS` parsing had already been widened beyond the first `SCATTER` / `GATHER` fix so single-field forms such as `SCATTER FIELDS NAME MEMVAR`, `SCATTER FIELDS NAME NAME oRow`, `GATHER MEMVAR FIELDS NAME`, and `GATHER NAME oRow FIELDS NAME` keep treating `NAME` as the selected field instead of misparsing it as the command's `NAME` clause, and that parser hardening also applies across adjacent `BROWSE`, `COPY TO`, `COPY TO ARRAY`, `APPEND FROM`, and `APPEND FROM ARRAY` families so keyword-named fields such as `TYPE` still survive `FIELDS <name>` extraction. On top of that, runtime field-visibility and browse metadata now honor `LIKE` / `EXCEPT` filters through `SET FIELDS TO LIKE ...`, `SET FIELDS TO EXCEPT ...`, and inline `BROWSE FIELDS ...` forms, while focused `test_prg_engine_data_io` plus `test_prg_engine_work_areas` coverage now locks down deeper `FIELDS LIKE` / `FIELDS EXCEPT` behavior across `SCATTER/GATHER NAME`, browse event payloads, field lookup visibility, and adjacent copy/import array flows.
- The same near-complete record-view lane now covers `DISPLAY` / `LIST` `RECORDS` metadata at the same fidelity level as `BROWSE`. Parser support now extracts `IN`, `FIELDS`, and `FOR` clauses from record-display forms, and the headless `runtime.display` / `runtime.list` event payloads now surface effective alias/work-area, record-count, active filter, and effective visible-field lists, including session `SET FIELDS TO LIKE ...` state and inline `FIELDS EXCEPT ...` overrides. Focused `test_prg_engine_data_io` coverage validates both commands.
- The same headless local-data lane now deepens adjacent `EDIT` / `CHANGE` record-context metadata instead of leaving them behind as shallow event stubs. When a selected cursor exists, `runtime.edit` and `runtime.change` now surface effective alias/work-area, record-count, active filter, and visible-field metadata alongside existing memo/field-list detail, so those host-facing editing commands line up better with the richer shipped `BROWSE` / `DISPLAY` / `LIST` record-view events. Focused `test_prg_engine_data_io` coverage validates both commands.
- Issue `#92` advanced on the critical path instead of only in the synthetic SQL lane. Local DBF-backed indexed-search now accepts first-pass ad hoc order expressions such as `SET ORDER TO UPPER(NAME)`, `SEEK('bravo', 'People', 'UPPER(NAME)')`, and descending one-off probes like `SEEK('beta', 'People', 'UPPER(NAME) DESCENDING')`, deriving the same normalization/collation hints already used on SQL result cursors instead of rejecting non-tag expressions. Focused `test_prg_engine_seek_index` coverage validates command/function matches, descending `SET NEAR` behavior, `ORDER()` readback, and emitted runtime order/seek metadata.
- The same `#92` local temporary-order slice now has focused non-selected-target parity coverage, matching the earlier synthetic SQL `IN <alias>` search/order checks. Local `SET ORDER TO UPPER(NAME) IN People`, `SEEK 'CHARLIE' IN People`, and descending `SET ORDER ... IN People DESCENDING` plus `SET NEAR` now have regression coverage proving that the targeted local cursor moves while the selected non-target cursor alias, order state, and record pointer stay intact. `test_prg_engine_seek_index` remains green.
- The same local temporary-order slice now also locks down helper-path parity for `INDEXSEEK()` over ad hoc expressions. Focused local coverage now proves `INDEXSEEK(.F./.T., 'UPPER(NAME)')` pointer semantics plus descending `SET NEAR` miss positioning without permanently changing `ORDER()`, matching the already-shipped SQL temporary-order helper coverage. `test_prg_engine_seek_index` remains green.
- Issue `#99` advanced with tighter `PRIVATE` / `RELEASE` semantics. Releasing a name that is currently shadowed by a `PRIVATE` declaration now restores the saved outer binding immediately instead of leaving the name undefined until frame teardown, which keeps in-scope memory-variable behavior aligned with the existing `PRIVATE` masking model. Focused `test_prg_engine_control_flow` coverage passes.
- The same `#99` memory-variable lane now reaches arrays on wildcard `RELEASE ALL` paths instead of only scalar/global bindings. `RELEASE ALL LIKE ...` and `RELEASE ALL EXCEPT ...` now consider array-only names as release candidates while still preserving pinned `PUBLIC` bindings. Focused `test_prg_engine_control_flow` coverage passes.
- `CLEAR MEMORY` now also clears deferred `PRIVATE` restoration state instead of allowing cleared outer bindings to reappear when the current frame later unwinds. That keeps cleared memory state stable across nested routine returns and closes another `#99` lifetime edge case. Focused `test_prg_engine_control_flow` coverage passes.
- The same `#99` release/clear lane now reaches current-frame `LOCAL` bindings on `RELEASE ALL`, and reassigning those released locals no longer leaks replacement globals after the routine returns. This closes a local-scope lifetime gap that sat underneath the earlier scalar/global-focused release implementation. Focused `test_prg_engine_control_flow` coverage passes.
- `CLEAR MEMORY` now also clears current-frame `LOCAL` bindings instead of only global/public/private state, while preserving local declaration routing so later in-scope reassignment still stays local and does not leak globals after return. Focused `test_prg_engine_control_flow` coverage passes.
- The same `#99` memory-file lane now has a much deeper `SAVE TO` / `RESTORE FROM` surface instead of only scalar global round-trips. `SAVE TO` now serializes runtime arrays with dimensions and preserves `PUBLIC` scope markers, `RESTORE FROM` now recreates arrays and `PUBLIC` bindings, additive restore now populates current-frame `LOCAL` bindings instead of hiding restored values behind stale locals, and non-additive restore now clears stale arrays plus deferred `PRIVATE` shadow state instead of leaving pre-restore memory artifacts behind. Focused `test_prg_engine_data_io` coverage passes.
- Slice issues `#123` and `#124` are now closed. Macro-expanded identifier handling now preserves alias-qualified field continuations such as `&cAlias..NAME` instead of truncating at the dot terminator, and recursive `TEXTMERGE(..., .T.)` expansion now keeps merging until the text stabilizes instead of stopping after a single extra pass. Focused `test_prg_engine_functions` and `test_prg_engine_string_math_functions` coverage passes.
- Slice issues `#125` and `#126` are now closed. Releasing a current-frame `LOCAL` that shadows an outer binding now reveals the outer value instead of erasing it, `RELEASE ALL` preserves those shadowed outer globals/publics, and `SAVE TO` no longer writes a `PUBLIC` scope marker when the visible saved binding is actually a shadowing `LOCAL`/`PRIVATE` over an outer public. Focused `test_prg_engine_control_flow` and `test_prg_engine_data_io` coverage passes.
- Slice issues `#117` and `#118` are now closed. `ALTER TABLE ... ADD COLUMN ... DEFAULT ...` now backfills existing rows instead of leaving freshly added defaulted fields blank, and transaction rollback now prunes stale `ALTER TABLE`-introduced field rules from live local cursors so post-rollback inserts run against the restored schema instead of trying to write phantom fields. Focused `test_prg_engine_table_structure` and `test_prg_engine_table_mutation` coverage passes.
- Slice issues `#127` and `#128` are now closed. `SCATTER NAME` / `GATHER NAME` now expand macro-driven nested object-path segments such as `&cHolder.&cChild`, and `APPEND FROM ARRAY` now serializes typed date/datetime values through the same DBF field contract used by `GATHER` instead of treating every array cell as an opaque string. Focused `test_prg_engine_data_io` coverage passes.
- Issue `#92` advanced with a first-pass runtime collation step instead of metadata-only `SET COLLATE` handling. Plain string indexed seeks now case-fold through the current non-`MACHINE` session collation when an active order does not already carry an explicit case-folding expression hint, so `SET COLLATE TO GENERAL` can make `SEEK 'bravo'` find a `NAME` key stored as `BRAVO` without requiring `UPPER(...)` order expressions. Focused `test_prg_engine_seek_index` coverage passes.
- The same `#92` indexed-search lane now consumes a broader first-pass grounded `FOR` expression family instead of only honoring `DELETED()` filters. Local `SEEK` now respects simple string and numeric comparisons such as `NAME = 'BRAVO'` and `AGE >= 20` embedded in loaded order metadata, including `SET NEAR` miss positioning against the surviving filtered candidate set. Focused `test_prg_engine_seek_index` coverage passes.
- Slice issue `#103` is now closed. Synthetic SQL/result-cursor plain-string `SEEK` now has focused parity coverage for non-`MACHINE` session collations, proving that `SET COLLATE TO GENERAL` case-folds plain `NAME`-style temporary-order searches on SQL cursors just as it already did on local DBF-backed cursors. Focused `test_prg_engine_sql_cursors` coverage passes.
- Slice issues `#129`, `#130`, `#115`, and `#116` are now closed. `INPUT` / `ACCEPT` now mirror `WAIT`-style resolved prompt/target payload fidelity, `DISPLAY` / `LIST RECORDS` now preserve raw and resolved `IN` target detail, SQL/result cursors now honor macro-expanded `SET FIELDS TO &cSpec` visible-field metadata and `SET FILTER TO &cExpr IN <alias>` filter flows, and SQL cursor `LOCATE` / `DELETE` / `RECALL FOR &cExpr` paths now evaluate macro-expanded boolean expressions instead of treating them as truthy strings. Focused `test_prg_engine_data_io` and `test_prg_engine_sql_cursors` coverage passes.
- Slice issues `#119` and `#120` are now closed. The aggregate event lane now emits the same resolved cursor/view state already used by the richer headless command surfaces: `runtime.calculate`, `runtime.count`, `runtime.sum`, `runtime.average`, and `runtime.total` now include targeted cursor/work-area, visible fields, active filter, aggregate scope, `FOR` / `WHILE`, output-variable, and raw/resolved `IN` target detail where applicable. The adjacent helper lane now also has focused `LOOKUP()` parity coverage over synthetic SQL/result cursors, including temporary field-order probes, targeted-cursor movement, and preservation of a separately selected local work area. Focused `test_prg_engine_control_flow` and `test_prg_engine_runtime_surface_functions` coverage pass.
- Slice issues `#121` and `#122` are now closed. DBC/container fidelity now resolves same-base `DCT`/`DCX` companions and exported `TABLE` `.dbf` assets case-insensitively on case-sensitive filesystems, and `export_database_as_json(...)` now prefers the catalog `DATABASE` object name over the raw `.dbc` stem while preserving decoded `PROPERTIES` memo bags and table-row snapshots. Focused `test_vfp_assets` coverage passes.
- Slice issues `#131` and `#132` are now closed. OLE object property assignment now round-trips through runtime object state instead of collapsing to placeholder `ole:` strings, and the seeded `Scripting.Dictionary` compatibility surface now supports first-pass `Add`, `Exists`, `Item`, `Remove`, `RemoveAll`, and live `Count` updates for deterministic collection-style behavior. Focused `test_prg_engine_runtime_surface_functions` coverage passes.
- Slice issues `#133` and `#134` are now closed. `NEWOBJECT()` now preserves library/server activation detail in runtime object state and `ole.newobject` events, while `GETOBJECT()` now resolves class-vs-source targeting more faithfully and reuses matching existing attachments instead of always registering duplicate handles for the same running class or same file/class pair. Focused `test_prg_engine_runtime_surface_functions` coverage passes.
- Slice issues `#135` and `#136` are now closed, and parent issue `#12` is now closed with them. Missing seeded-automation property reads and method invocations no longer fail silently: unresolved OLE reads/invokes and missing `Scripting.Dictionary` members now route through the normal runtime fault path as `1429` automation errors, so `ON ERROR` / `AERROR()` and `TRY/CATCH/FINALLY` can trap them while preserving object/session usability afterward. Focused `test_prg_engine_control_flow` coverage passes.
- Slice issues `#142` and `#143` are now closed. Thrown expression faults such as `LOG(-1)` and `ACOS(2)` now have explicit pause-state coverage proving that error pauses preserve the actual faulting line, statement text, and message, and that continuing after one or more thrown-expression faults keeps the session alive while refreshing pause metadata on each new failure instead of leaving stale line/statement/message state behind. Focused `test_prg_engine_control_flow` coverage passes.
- Slice issues `#144` and `#145` are now closed. Error-pause debugger metadata now uses the real faulting line for the top stack frame instead of the post-incremented next statement when nested routines fault, and focused regression coverage now locks down nested-routine stack-frame fidelity plus repeated nested-fault metadata refresh across multiple continues. Focused `test_prg_engine_control_flow` coverage passes.
- Slice issues `#146`, `#147`, `#148`, and `#149` are now closed, and parent lane `#95` is now closed with them. Command-level aggregate targets now resolve `TO &macro` identifiers in `COUNT` / `SUM` / `AVERAGE` and `CALCULATE`, `CALCULATE` now preserves `WHILE` clause metadata and execution semantics, `DISPLAY` / `LIST RECORDS` now retain `WHILE` clause payload detail, `DISPLAY` / `LIST STRUCTURE` now honor `IN <alias|work area>` targeting with raw/resolved target detail, `.NULL.` now parses as a real expression literal instead of leaking into OLE-property lookup, and focused helper coverage now locks down variable-driven `LOOKUP(..., cAlias, cTag)` targeting. Focused `test_prg_engine_control_flow`, `test_prg_engine_runtime_surface_functions`, and `test_prg_engine_data_io` coverage passes.
- Slice issues `#102`, `#104`, and `#107` are now closed. Synthetic SQL/result cursors now honor first-pass `FOR` filters on ad hoc temporary orders, so probes such as `SEEK(..., 'sqlcust', "UPPER(NAME) FOR NAME = 'BRAVO'")` and command-path `SET ORDER TO UPPER(NAME) FOR ...` constrain the candidate set and `SET NEAR` positioning the same way the shipped local grounded-order path already did. That same temporary-order lane now also consumes grounded field-type metadata for one-off numeric field orders such as `SET ORDER TO AGE` / `SET ORDER TO AMOUNT`, so local and SQL `SET NEAR` probes no longer fall back to lexicographic ordering on numeric keys. The adjacent runtime-state lane now has explicit `SET('FIELDS')` data-session isolation coverage alongside the already-shipped `PATH` / `DEFAULT` checks, proving that field-visibility readback and lookup restoration stay session-local across `SET DATASESSION` switches. Focused `test_prg_engine_sql_cursors`, `test_prg_engine_seek_index`, and `test_prg_engine_work_areas` coverage pass.
- Issue `#98` runtime-state readback deepened in the adjacent date/time lane. `SET('DATE')` now defaults to `MDY` instead of falling through to `OFF`, and focused `test_prg_engine_date_time_functions` coverage now locks down default/readback plus `SET DATASESSION` isolation/restoration for `DATE`, `CENTURY`, `MARK`, `HOURS`, and `SECONDS`.
- The same `#98` runtime-state lane now locks down six more session-scoped `SET()` surfaces that were already implemented but not yet defended with focused regression coverage: `COLLATE`, `NULL`, `ANSI`, `REPROCESS`, `MULTILOCKS`, and `EXCLUSIVE`. Focused `test_prg_engine_runtime_surface_functions` and `test_prg_engine_table_mutation` coverage now validates default/readback plus `SET DATASESSION` isolation/restoration for each of those options.
- Slice issues `#105` and `#106` are now closed. Formatting-state session isolation is now explicitly defended for `MEMOWIDTH`, `POINT`, `SEPARATOR`, and `CURRENCY`, and adjacent calendar/week state is now explicitly defended for `FDOW` and `FWEEK` across `SET DATASESSION` switches. The runtime also fixed a shared `SET()` readback bug where numeric/string session options such as `FDOW=1` could be misreported as `ON` after restoration. Focused `test_prg_engine_string_math_functions` and `test_prg_engine_date_time_functions` coverage passes.
- The adjacent headless input/wait lane deepened instead of leaving `INPUT`, `ACCEPT`, and `WAIT` behind as event-only stubs. `INPUT` / `ACCEPT` now reuse the same selected-cursor/view metadata surfaced by the richer local-data commands and assign deterministic empty-string headless results through `TO <target>` variables, while `WAIT` now captures common `WINDOW`, prompt, `TIMEOUT`, `NOWAIT`, `NOCLEAR`, and `TO <target>` detail and likewise assigns a deterministic empty-string target result in headless mode. Focused `test_prg_engine_data_io` coverage validates the expanded event payloads and target-assignment behavior.
- The same headless interaction lane deepened again across five adjacent command surfaces instead of stopping at `WAIT`. `KEYBOARD` now preserves common `PLAIN` / `CLEAR` flags in its runtime event payload, while `DISPLAY` / `LIST` `STRUCTURE` forms now surface selected-cursor schema metadata and `DISPLAY` / `LIST` `STATUS` forms now surface data-session/open-cursor plus selected-view metadata. Focused `test_prg_engine_data_io` coverage validates the expanded event detail for `KEYBOARD`, `DISPLAY STRUCTURE`, `DISPLAY STATUS`, `LIST STRUCTURE`, and `LIST STATUS`.
- The adjacent `DISPLAY/LIST MEMORY` slice now moved beyond a mode-only event stub. Headless `runtime.display` / `runtime.list` payloads for `MEMORY` forms now summarize visible memory-variable state by scope (`public`, `private`, `local`, ordinary global), include first-pass type/value detail for each visible memvar, and also surface runtime array names plus dimensions. Focused `test_prg_engine_data_io` coverage validates both commands across mixed public/private/local/global variables and declared arrays.
- Issue #7/#8 closure work advanced through the same headless command lane instead of stopping at mode-level metadata. `WAIT` and `KEYBOARD` payloads now carry resolved runtime values alongside their source expressions, which closes a real macro/eval gap for host-driven integrations; `DISPLAY/LIST MEMORY` now also surface shadowed bindings, object previews, and scoped array detail instead of flattening everything into a single visible memvar list. Focused `test_prg_engine_data_io` coverage validates the richer wait/keyboard expression handling plus memory-object/array/shadowing detail.
- Financial/misc expression-function batch shipped. The runtime now supports first-pass `HEX()` (uppercase hex without prefix), `FV()`, `PV()`, and `PAYMENT()` financial helpers in `prg_engine_numeric_functions.cpp`; first-pass `NEWID()` (UUID v4 via mt19937_64), `CPCURRENT()` / `CPCONVERT()` / `CPDBF()` code-page stubs, and headless dialog/context stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()` / `VARREAD()` in `prg_engine_runtime_surface_functions.cpp`; and first-pass `TEXTMERGE()` with configurable delimiters and recursive evaluation support, first-pass `EXECSCRIPT()` RETURN-expression pass-through, and first-pass `LOOKUP()` seek-and-evaluate over local alias/tag indexes in `prg_engine_expression.inl`. Focused `test_prg_engine_string_math_functions` and `test_prg_engine_runtime_surface_functions` coverage validates all 14 new helpers.

## 2026-04-29

- SQL pass-through helper breadth advanced with two adjacent runtime batches on the existing per-session connection model. The runtime now supports first-pass `SQLCANCEL()`, `SQLCOMMIT()`, `SQLROLLBACK()`, `SQLTABLES()`, and `SQLCOLUMNS()` alongside the shipped `SQLCONNECT` / `SQLEXEC` / `SQLDISCONNECT` / `SQLROWCOUNT` / `SQLPREPARE` / `SQLGETPROP` / `SQLSETPROP` slice. Connection metadata now tracks last SQL action plus first-pass transaction-dirty and cancel-requested flags, those helpers emit deterministic `sql.cancel` / `sql.commit` / `sql.rollback` events, and the new metadata helpers open deterministic synthetic remote cursors for FoxPro-style and native-style catalog probing, including schema-preserving empty native metadata cursors. Focused `test_prg_engine_sql_cursors` coverage validates the helper surface, metadata cursor schemas, and session-isolation behavior, while the expression parser now accepts both single-quoted and double-quoted string literals in those calls instead of spinning on double-quoted arguments.
- The adjacent SQL metadata lane extended again with first-pass `SQLDATABASES()`. The runtime now exposes a deterministic synthetic database catalog over the existing session-local remote-cursor machinery, preserving explicit two-column metadata schema (`DATABASE_NAME`, `REMARKS`) and `LastSqlAction='databases'` state on the connection. Focused `test_prg_engine_sql_cursors` coverage now validates `SQLDATABASES()` alongside the earlier `SQLTABLES()` / `SQLCOLUMNS()` metadata helpers.
- `SQLGETPROP()` / `SQLSETPROP()` breadth advanced on the same session-local handle model instead of jumping to deeper provider emulation. The runtime now exposes deterministic `Connected`, `ConnectHandle`, `ConnectString`, `CurrentCatalog`, `LastCursorAlias`, `LastResultCount`, `Asynchronous`, `BatchMode`, and `DispWarnings` behavior on top of the shipped provider/timeout/prepared-command properties, with `CurrentCatalog` inferred from the synthetic catalog set and normalized on update. Focused `test_prg_engine_sql_cursors` coverage validates property round-tripping, last-cursor/result metadata after `SQLEXEC`, exact `sql.setprop` events, and cross-session handle rejection.
- The adjacent SQL catalog/property/navigation lane advanced as a coordinated batch. The runtime now adds deterministic first-pass `SQLPRIMARYKEYS()` and `SQLFOREIGNKEYS()` over the synthetic Northwind catalog, including schema-preserving empty metadata cursors for misses and `sql.primarykeys` / `sql.foreignkeys` events. The connection-property surface also widened again with deterministic `ConnectName`, `DispLogin`, `Transactions`, `WaitTime`, and `PacketSize` support on top of the existing session-local SQL handle state. Focused `test_prg_engine_sql_cursors` coverage now also locks down targeted backward/boundary SQL cursor navigation (`GO BOTTOM IN`, `SKIP -1 IN`, targeted `BOF()` / `EOF()`) to preserve selected alias/pointer state while moving the requested remote cursor.
- Issue #8 macro/runtime follow-up trio shipped. Logical-line preprocessing now keeps `&&` inside nested double-quoted, bracketed, and braced text instead of truncating those expressions as comments; `PARAMETERS` / `LPARAMETERS` now honor default-value expressions when callers omit trailing arguments, including macro-expanded defaults such as `LPARAMETERS x = &cExpr`; and focused regression coverage now locks down macro-expanded `FOR &cExpr` filters for SQL-style `DELETE FROM` and `UPDATE` command forms. `test_prg_engine_functions` and `test_prg_engine_table_mutation` pass.
- Local DBF mutation closed an `APPEND BLANK` layout gap. Tables that include otherwise readable opaque/unsupported field families (for example synthetic `W` fields) no longer hard-fail `APPEND BLANK`; the append path now zero-initializes those field bytes conservatively and grows the table normally, which keeps runtime mutation flowing over mixed-layout tables without overclaiming direct typed write support for those field families. Focused `test_dbf_table` and `test_prg_engine_table_mutation` coverage passes.
- Adjacent local-data parity slices shipped for opaque writes and first-pass spreadsheet interchange. Shared DBF direct writes now accept conservative opaque/binary payloads on otherwise readable unsupported field families: callers can clear with `NULL`, write raw text that fits the field width, or use `0x`-prefixed hex payloads, while `APPEND BLANK` still zero-initializes those field bytes. Runtime/local-command coverage also now includes a first-pass SpreadsheetML lane for `COPY TO ... TYPE XLS` and `APPEND FROM ... TYPE XLS`, reusing existing `FIELDS` filtering and shared append/replace mutation paths instead of adding an event-only stub. Focused `test_dbf_table`, `test_prg_engine_table_mutation`, and `test_prg_engine_data_io` coverage passes.
- `SCATTER NAME` object-state parity advanced with real additive reuse semantics. The parser now recognizes `ADDITIVE` on `SCATTER`, and `SCATTER NAME <object> ADDITIVE` now reuses an existing runtime object when one is already bound instead of always allocating a fresh `Empty` object. Existing non-field properties are preserved while matching field properties are refreshed from the current record and missing field properties are added. Focused `test_prg_engine_data_io` coverage passes.
- The next adjacent interchange/object slice shipped. `COPY TO ... TYPE DIF` and `APPEND FROM ... TYPE DIF` now have a real first-pass interchange lane using a conservative DIF-style text serializer/parser with header-row emission and import header skipping, flowing through the same `FIELDS` filtering and shared append/replace DBF mutation path as the other non-DBF formats. `SCATTER NAME` / `GATHER NAME` now also resolve macro-expanded object variable names such as `NAME &cObjectName`, so object-target command forms line up better with the macro-expanded array-name behavior already shipped nearby. Focused `test_prg_engine_data_io` coverage passes.
- Another adjacent interchange/object slice shipped. `COPY TO ... TYPE SYLK` and `APPEND FROM ... TYPE SYLK` now have a real first-pass text interchange lane using a conservative SYLK-style serializer/parser with header-row emission and import header skipping, again reusing existing `FIELDS` filtering and the shared append/replace DBF mutation path. `SCATTER NAME` / `GATHER NAME` also now resolve nested object-property targets such as `oHolder.Row`, so runtime object scattering/gathering can operate on an object already stored inside another object property instead of only top-level variables. Focused `test_prg_engine_data_io` coverage passes.
- The next adjacent interchange/object slice shipped. `COPY TO ... TYPE TAB` and `APPEND FROM ... TYPE TAB` now reuse the delimited-text substrate with tab separators as a real first-pass interchange lane instead of requiring callers to spell the broader `DELIMITED` form. `SCATTER NAME` / `GATHER NAME` also now create missing nested object targets such as `oHolder.Row` on demand during scatter, so nearby object-path runtime flows no longer require those child objects to be pre-seeded before they can participate. Focused `test_prg_engine_data_io` coverage passes.
- The next deeper `SCATTER/GATHER` and modern interchange slice shipped. `SCATTER NAME` without `ADDITIVE` now replaces an existing target object on nested paths instead of mutating it in place and leaking unrelated stale properties through the old object instance. In parallel, `COPY TO ... TYPE JSON` and `APPEND FROM ... TYPE JSON` now provide a first-pass modern object-array interchange lane with typed numeric/logical emission and import by matching field names through the shared append/replace DBF mutation path. Focused `test_prg_engine_data_io` coverage passes.

## 2026-04-27

- Issue #7 parser and PUBLIC-release parity advanced. The shared top-level command keyword scanner now ignores keywords inside double-quoted strings, bracketed expressions, and braced blocks, and the CSV-like command splitter now keeps braced block commas together. `PUBLIC` declarations now track public identity, so `RELEASE ALL`, `RELEASE ALL LIKE`, and `RELEASE ALL EXCEPT` preserve public-pinned scalar variables and arrays while still releasing matching non-public bindings. Focused `test_prg_engine_control_flow` coverage validates the helper and runtime behavior.
- Issue #7/#8 runtime-state and command-surface chunk advanced. `UNLOCK RECORD <n> [IN <alias>]` now releases only the requested record lock while preserving file/table locks and other record locks. Common `SET` options now normalize/evaluate more VFP-like RHS values for literals, variables, and macro-expanded expressions across `PATH`, `MARK`, `DECIMALS`, `COLLATE`, `NULL`, `ANSI`, and related date/numeric/string state. Macro-expanded array names now work across `SCATTER TO`, `GATHER FROM`, `COPY TO ARRAY`, and `APPEND FROM ARRAY` instead of creating or reading arrays literally named `&cName`. Focused `test_prg_engine_table_mutation`, `test_prg_engine_runtime_surface_functions`, and `test_prg_engine_data_io` coverage validates the slice.
- Copilot follow-up audit found and fixed two runtime correctness gaps. Durable DBF transaction journaling now covers additional direct mutation paths (`PACK MEMO`, `CREATE TABLE`, `ALTER TABLE`, `APPEND FROM ARRAY`, `APPEND FROM` DBF/SDF/CSV/delimited, and `GATHER`) instead of only the core helper paths, and rollback refresh now closes local cursors whose backing table was removed by replay. `STORE` now reuses the ordinary assignment-target path, so scalar, direct array-element, macro-expanded array-element, `m.` namespace, and OLE property targets stay consistent with normal assignment. Focused `test_prg_engine_table_mutation` and `test_prg_engine_arrays` coverage validates the fixes.
- Command-level aggregate scope depth advanced for `COUNT`. The stale non-`ALL/FOR/TO` limitation path is gone; `COUNT REST`, `COUNT NEXT <n>`, `COUNT RECORD <n>`, and `COUNT WHILE <expr>` now run through the shared aggregate scope collector while preserving existing visibility/filter/deleted semantics and `TO ARRAY` assignment behavior. Focused `test_prg_engine_control_flow` and `test_prg_engine_aggregate_array_functions` coverage pass.
- `SCATTER` / `GATHER` field-type parity advanced for date/datetime values. `SCATTER` now converts `D` and `T` fields into runtime-facing `MM/DD/YYYY` and `MM/DD/YYYY HH:MM:SS` strings, `SCATTER ... BLANK` now produces blank string values for those field types, and `GATHER MEMVAR` / `GATHER FROM <array>` now restore date/datetime/blank values back into DBF storage consistently. Focused `test_prg_engine_data_io` coverage passes.
- Runtime array semantics gained macro-expanded identifier depth. Helper entry points such as `ALEN(&cArrayName)`, `ALINES(&cTargetName, ...)`, and `ACOPY(&cSourceName, &cTargetName, ...)` now resolve macro-expanded array identifiers through the existing bounded macro path, and direct macro-expanded element access now works for both bracket and paren syntax (`&cArrayName[2,1]`, `&cArrayName(1,2)`). Focused `test_prg_engine_arrays` coverage passes.
- Runtime diagnostics depth advanced with handler-stable fault snapshots. During `ON ERROR` handler execution, `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `SYS(2018)`, and `AERROR()` now remain bound to the original fault even if the handler triggers and catches a secondary runtime error. The snapshot is released on handler return and on `RETRY` / `RESUME` unwinds. Focused `test_prg_engine_control_flow` coverage passes.
- Expression macro semantics gained a deeper bounded-indirection lane. `&macro` expansion now follows identifier chains before expression evaluation (for common indirect patterns such as `name -> expressionVar -> expressionText`) while deterministically breaking self-referential/cyclic chains to avoid recursion. Existing non-evaluable fallback behavior remains intact (string fallback), and focused `test_prg_engine_functions` coverage now validates direct expansion, one-level indirection, cyclic-safety behavior, and non-expression fallback.
- Expression/runtime helper depth gained adjacent `TYPE()` and `TRANSFORM()` compatibility behavior. `TYPE('<expr>')` now strips fully wrapping parentheses before classification/evaluation so resolvable parenthesized and array-element expressions report concrete type codes where available, while preserving bare-array `A` detection. `TRANSFORM()` now includes first-pass `@Z` (blank zero output) and `@B` (left-justified trimmed output) semantics in addition to existing picture handling. Focused `test_prg_engine_functions` coverage validates the new behavior.
- Native command-surface depth gained adjacent `CALL` compatibility improvements. Parser support now recognizes `CALL <target> WITH ...` argument forms (instead of treating the whole clause as a target identifier), and dispatcher behavior now resolves external `.prg` targets for `CALL` similarly to `DO` when local routines are absent. Focused `test_prg_engine_control_flow` coverage validates `CALL ... WITH` LPARAMETER binding plus external `CALL <file> WITH @var` BYREF write-back behavior.
- Aggregate expression parity gained a focused no-expression compatibility lane. `COUNT()` now returns visible-row counts without requiring explicit expressions, and no-expression `SUM()` / `AVG()` / `MIN()` / `MAX()` now use the first numeric-compatible field (`N/F/B/I/Y`) in cursor field order under existing visibility/filter semantics. Deterministic fallback behavior was added when no numeric-compatible fields exist (`SUM`/`AVG` => `0`, `MIN`/`MAX` => empty value), and focused `test_prg_engine_aggregate_array_functions` coverage now locks down both normal and non-numeric-field paths.
- First-pass dialog-helper semantics were deepened for `GETFILE`, `PUTFILE`, `GETDIR`, and `INPUTBOX`. Parser extraction now handles common parenthesized/positional forms in addition to clause-keyword forms, and dispatcher behavior now assigns deterministic empty-string results to `TO <target>` variables in headless mode while preserving event emission (`runtime.getfile`, `runtime.putfile`, `runtime.getdir`, `runtime.inputbox`). Focused `test_prg_engine_data_io` coverage validates clause extraction and target-assignment behavior across local/global/`m.` variable forms.
- Runtime command-surface depth gained first-pass headless dialog-helper command support for `GETFILE`, `PUTFILE`, `GETDIR`, and `INPUTBOX`. The parser now recognizes these command forms and captures prompt/title/default/filter/target metadata where present; the dispatcher emits non-blocking `runtime.getfile`, `runtime.putfile`, `runtime.getdir`, and `runtime.inputbox` events with structured detail payloads for host-driven UI integration. Focused `test_prg_engine_data_io` coverage validates one-event-per-command behavior and detail capture.
- Runtime command-surface depth gained first-pass session-scoped stack semantics for `PUSH KEY` / `POP KEY`, `PUSH MENU` / `POP MENU`, and `PUSH POPUP` / `POP POPUP`. The parser now recognizes all six forms, per-session stack state tracks key/menu/popup markers, and dispatcher events (`runtime.push_*` / `runtime.pop_*`) include stack depth plus empty-pop signaling for deterministic host behavior. Focused `test_prg_engine_control_flow` coverage validates depth transitions and safe empty-pop behavior.
- Runtime-surface helper compatibility gained a first-pass XML cursor bridge with `CURSORTOXML()` and `XMLTOCURSOR()`. The helper path now supports deterministic Copperfin XML serialization for cursor rows/fields, file or in-memory transfer in `CURSORTOXML()`, and first-pass `XMLTOCURSOR()` materialization from that same XML shape with row-count return semantics. Runtime events (`runtime.cursortoxml`, `runtime.xmltocursor`) plus warning events on invalid input are now emitted, and focused `test_prg_engine_runtime_surface_functions` coverage validates round-trip behavior and invalid-input safety.
- Runtime compatibility gained a first-pass memory-persistence hardening lane for `SAVE TO` / `RESTORE FROM`. The `.mem` serializer now escapes delimiter-sensitive and control characters (`\\`, `=`, `:`, `\n`, `\r`, `\t`) for round-trip-safe string values, restore now symmetrically unescapes persisted payloads, and explicit empty values are now preserved with an `E` type marker instead of collapsing into ambiguous empty strings. `RESTORE FROM` numeric parsing now rejects trailing-garbage payloads, and focused `test_prg_engine_data_io` coverage now locks down `ALL EXCEPT` filtering, auto `.mem` extension behavior, escaped value round-trips, non-`ADDITIVE` global clearing semantics, and numeric trailing-garbage fallback handling.
- Runtime object reflection helpers advanced beyond the initial stub lane. `AMEMBERS()` now supports first-pass flag unions (`0` all, `1` properties, `2` methods, `4` events) with deterministic case-insensitive sorting, `ACLASS()` now emits canonical class token plus `OBJECT`, and `PEMSTATUS()` now checks member existence across properties/methods/events with first-pass read-only handling for seeded `Scripting.Dictionary` `COUNT`. Runtime OLE object state now tracks reflection-only method/event containers, with first-pass seeded member metadata for `Scripting.Dictionary` methods (`ADD`, `EXISTS`, `ITEM`, `REMOVE`, `REMOVEALL`, `KEYS`, `ITEMS`) and properties (`COUNT`, `COMPAREMODE`). Focused `test_prg_engine_runtime_surface_functions` coverage validates the new flag filtering, class-token extraction, and attribute checks.
- Runtime object reflection helpers advanced beyond the initial stub lane. `AMEMBERS()` now supports first-pass flag unions (`0` all, `1` properties, `2` methods, `4` events) with deterministic case-insensitive sorting, `ACLASS()` now emits canonical class token plus `OBJECT`, and `PEMSTATUS()` now checks member existence across properties/methods/events with first-pass read-only handling for seeded `Scripting.Dictionary` `COUNT`. Runtime OLE object state now tracks reflection-only method/event containers, with first-pass seeded member metadata for `Scripting.Dictionary` methods (`ADD`, `EXISTS`, `ITEM`, `REMOVE`, `REMOVEALL`, `KEYS`, `ITEMS`) and properties (`COUNT`, `COMPAREMODE`). Focused `test_prg_engine_runtime_surface_functions` coverage validates the new flag filtering, class-token extraction, and attribute checks.
- Fix SET('PATH') test expectations (88d1d4b). Codex f700a50 correctly changed SET('PATH') not-found readback to return "" (VFP-accurate). Two assertions in `tests/test_prg_engine.cpp` updated to use `.empty()` instead of `== "OFF"`. 29/29 green.
- Wave 5 (47f66d3) — four parallel slices shipped:

## 2026-04-26

- Interactive local-data runtime coverage gained a first-pass headless `BROWSE` lane. The parser/dispatcher now accept `BROWSE` plus common `IN`, `FIELDS`, and `FOR` clauses, resolve the targeted local cursor/work area through the existing designator path, reuse session `SET FILTER` and `SET FIELDS` state for the effective view, and emit `runtime.browse` events with alias/work-area, position, record-count, field-list, current filter, and inline clause metadata for later host/UI wiring. Focused `test_prg_engine_data_io` coverage validates selected-cursor and targeted-cursor browse event payloads.
- SQL/OLE `AERROR()` compatibility gained a deeper captured-payload lane without widening the broader fault model. The runtime now snapshots SQL failure context (handle/detail, provider or operation context, and command/target payload where present) plus OLE/automation fault context (member-path detail, source object identifier, and failing action text) so the `1526` and `1429` `AERROR()` rows stop returning placeholder trailing columns when Copperfin already knows the failing payload. Focused `test_prg_engine_control_flow` coverage validates provider/target capture for `SQLEXEC()` faults and source/action capture for OLE assignment faults.
- `CREATE CURSOR` now uses a real temp-backed local DBF path instead of the old in-memory stub. The dispatcher reuses parsed field declarations plus the shared field-rule mapping used by `CREATE TABLE`, so temp cursors participate in normal local-table flows (`APPEND BLANK`, `REPLACE`, `INSERT INTO`, `AFIELDS`, `FCOUNT`, `FSIZE`, `RECCOUNT`) while preserving `DEFAULT` and `NOT NULL` metadata through the existing insert/validation path. Focused `test_prg_engine_table_structure` coverage validates local schema introspection, persisted temp-backed mutation, default application, and rollback on `NOT NULL` failures.
- `ON ERROR` / `AERROR()` compatibility gained a deeper normal-runtime fault lane without widening the command surface. The runtime now snapshots the faulting work area alongside existing fault location/procedure state, and normal seven-column `AERROR()` rows now carry fault work area, source line, procedure, and failing statement text instead of dropping columns 5-7 on the floor. Focused `test_prg_engine_control_flow` coverage validates that handler-side `SELECT 0` work-area changes no longer clobber the captured fault payload.
- Local DBF transaction durability gained a first-pass journal lane. Root `BEGIN TRANSACTION` now initializes an on-disk per-session journal, local DBF mutations capture one-time preimage backups for companion files (`.dbf`/`.fpt`/`.cdx`), `ROLLBACK` replays backups and refreshes active local cursors, and runtime startup now scans/replays pending journals to undo interrupted transactions after process loss. Focused `test_prg_engine_table_mutation` coverage now validates explicit rollback restoration plus startup crash-replay restoration for interrupted transactions.
- Runtime context/introspection helpers gained a deterministic first-pass compatibility lane. Expression evaluation now supports `PCOUNT()` (current frame argument count) and `PUTENV(<name>, <value>)` alongside the existing `GETENV(<name>)` helper, and `SYS()` now includes first-pass deterministic responses for `SYS(3)`, `SYS(7)`, `SYS(11)`, and `SYS(13)`. Focused `test_prg_engine_runtime_surface_functions` coverage validates routine-frame argument counting, environment set/read/clear behavior, and non-empty deterministic `SYS(3)`/`SYS(7)` values with stable placeholder counters for `SYS(11)`/`SYS(13)`.
- Transaction-processing compatibility gained a first-pass in-process lane. The parser/dispatcher now support `BEGIN TRANSACTION`, `END TRANSACTION`, and `ROLLBACK`; runtime state tracks nested transaction depth per data session; and `TXNLEVEL()` now reports the active nesting depth through the expression path. Focused PRG-engine regression coverage validates nesting behavior, rollback-to-zero semantics, runtime transaction event emission, and `SET DATASESSION` isolation/restoration of transaction levels. This intentionally stops short of durable DBF transaction journaling/undo replay.
- Locking semantics gained a first-pass in-process runtime foundation. `SET REPROCESS` and `SET MULTILOCKS` now round-trip through `SET()`, open cursor state can be record-locked or table-locked through `RLOCK()`, `FLOCK()` / `LOCK()`, queried through `ISRLOCKED()` / `ISFLOCKED()`, and released through `UNLOCK` or `UNLOCK ALL`. Focused `test_prg_engine_table_mutation` coverage validates state readback, current/named cursor locks, and multi-cursor unlock behavior. This deliberately stops short of cross-process OS locking.
- Table-maintenance safety gained first-pass `SET EXCLUSIVE` and `USE ... SHARED|EXCLUSIVE` support. `SET('EXCLUSIVE')` defaults to `ON`, local `USE` cursors inherit the session default unless an explicit open mode is provided, and local `PACK`/`PACK MEMO`/`ZAP` now require an exclusive cursor while synthetic remote/result cursors keep their existing in-memory behavior. Focused `test_prg_engine_table_mutation` coverage validates shared-cursor guard failures plus explicit exclusive override success.
- Work-area field visibility gained first-pass `SET FIELDS` support. `SET('FIELDS')` defaults to `OFF`; `SET FIELDS TO <field-list>` enables a session-scoped visible-field list for field lookup, `SET FIELDS OFF` disables the restriction, `SET FIELDS ON` restores the prior list, and `SET FIELDS TO ALL` restores unrestricted lookup while preserving readback. Focused `test_prg_engine_work_areas` coverage validates bare and alias-qualified field lookup hiding/restoration over a local DBF cursor.
- Numeric picture formatting gained first-pass `SET POINT TO`, `SET SEPARATOR TO`, and `SET CURRENCY TO` state. `SET('POINT')` now defaults to `.`, `SET('SEPARATOR')` defaults to `,`, `SET('CURRENCY')` defaults to `$`, explicit symbols round-trip through `SET()`, and `TRANSFORM(<number>, <picture>)` applies the configured decimal point, grouped thousands separator, and currency symbol when the picture includes numeric decimal/group/currency markers. Focused `test_prg_engine_string_math_functions` coverage passes.
- Week-number runtime state gained first-pass `SET FDOW TO` and `SET FWEEK TO` support. `SET('FDOW')` and `SET('FWEEK')` default to `1`, validate configured ranges, and now provide the default first-day and first-week modes for `WEEK(date)` when callers omit explicit arguments; explicit `WEEK(date, firstDay[, firstWeekMode])` arguments still override session state. Focused `test_prg_engine_date_time_functions` coverage passes.
- Date/time display state gained first-pass `SET HOURS TO 12/24` and `SET SECONDS ON/OFF` support. `SET('HOURS')` now defaults to `24`, `SET('SECONDS')` defaults to `ON`, and `TIME()` plus date/time string helpers use the configured 12-hour/24-hour and seconds-display settings while preserving the existing 24-hour-with-seconds default. Focused `test_prg_engine_date_time_functions` coverage passes.
- Date separator runtime state gained first-pass `SET MARK TO` support. `SET('MARK')` now defaults to `/`, explicit separators such as `-` and `.` round-trip through `SET()`, and the date/time helper path uses the configured separator when formatting and parsing common `MDY`/`DMY`/`YMD` date values. Focused `test_prg_engine_date_time_functions` coverage passes.
- Date/time expression helpers now honor first-pass `SET DATE TO` and `SET CENTURY` runtime state. `CTOD()`, `DTOC()`, `CTOT()`, `TTOC()`, `TTOD()`, `DTOT()`, `DATE()`, and `DATETIME()` keep the existing MDY/four-digit default, while explicit `DMY`/`BRITISH`-style and `YMD`/`ANSI`-style date orders plus two-digit century-off formatting round-trip through `SET()` and the date/time helper surface. Focused `test_prg_engine_date_time_functions` coverage passes.
- File system helper coverage gained `FILESIZE()` expression function. The implementation resolves relative paths against the runtime default directory, returns file size in bytes for existing files, and returns 0 for missing files or empty arguments. Focused `test_prg_engine_runtime_surface_functions` coverage validates relative/absolute paths and missing-file behavior. This completes the first-pass file metadata helper surface alongside the existing `FILE()`, `HOME()`, and path helpers.
- String expression memo-line coverage now includes session-scoped `SET MEMOWIDTH TO` and `_MLINE` state wiring. `MEMLINES()` and `MLINE()` consume the active session memo width, `_MLINE` exposes the current setting, and focused string/math regression coverage validates width-dependent wrapping behavior plus line extraction after runtime width changes.
- Runtime configuration parity for memo-width state now includes `SET('MEMOWIDTH')` reporting. Session-scoped memo width values now surface through the `SET()` expression path (default `50`, updated values after `SET MEMOWIDTH TO ...`), with focused string/math coverage validating default and post-update reads alongside `_MLINE` and width-sensitive memo wrapping behavior.
- Runtime-surface file metadata helpers now honor `SET PATH` search entries for relative `FILE()` and `FILESIZE()` probes after checking the current default directory, so path-only files are discoverable through configured search paths. String helper parity also gained first-pass `STRTRAN()` occurrence controls via optional start-occurrence and replacement-count arguments, with focused runtime-surface and string/math regression coverage.
- Session-state regression coverage now validates `SET PATH` isolation and restoration alongside existing `SET NEAR`/`SET DEFAULT` checks. Focused runtime tests confirm `FILE()` path-only probes are session-local: they fail before `SET PATH`, succeed after configuration in the owning session, remain unavailable in a fresh data session, and restore when returning to the configured session.
- Session-state regression coverage now also locks down `SET('EXACT')` and `SET('DELETED')` isolation/restoration behavior across `SET DATASESSION` transitions. Focused PRG-engine coverage verifies default `OFF` values in fresh sessions, `ON` reporting after enabling in the owning session, and restored `ON` state when switching back.
- Memo-line helper parity gained first-pass optional line-length arguments for `MEMLINES()` and `MLINE()`. `MEMLINES(source, width)` now supports explicit positive widths (with non-positive values falling back to session/default memo width), and `MLINE(source, line, startOffset, width)` now accepts an explicit width override in the fourth argument. Focused string/math coverage validates override and fallback behavior.
- Memo-line helper compatibility depth now includes first-pass option semantics beyond width overrides. `MEMLINES(source, width, tabWidth, flags)` and `MLINE(source, line, startOffset, width, tabWidth, flags)` now support optional explicit tab expansion when `tabWidth` is provided and opt-in LF hard-break splitting via `flags` bit `1`, while preserving legacy behavior when those options are omitted. Focused string/math regression coverage validates tab-expansion, LF-break, and default-compat behavior.
- Session-state SET parity now normalizes boolean option forms for `NEAR`, `EXACT`, and `DELETED` beyond plain `ON/OFF`. Runtime handling now accepts `SET ... TO` forms and logical-token variants (`.T./.F.`, `true/false`, `1/0`, `yes/no`) with consistent `SET()` reporting and runtime-flag evaluation, and focused session-state regression coverage now validates toggle and restoration behavior across `SET DATASESSION` transitions.
- Session-state SET parity now also treats `TALK`, `SAFETY`, and `ESCAPE` as boolean runtime flags in the dispatcher normalization lane. `SET ... TO` logical-token variants (`.T./.F.`, `true/false`, `1/0`, `yes/no`) now round-trip through `SET()` as `ON`/`OFF` for these options too, and focused PRG-engine regression coverage validates cross-session isolation and restoration across `SET DATASESSION` transitions.
- Expression helper coverage gained another batched utility slice: portable `DEFAULTEXT()` and first-pass `CURDIR()` in the path helper dispatch, plus numeric `RGB()` color packing and first-pass `RAND()` generation with explicit reseeding for negative or positive seed arguments. Focused path and string/math regression targets validate extension preservation/appending, runtime working-directory exposure, color packing/clamping, and RAND range behavior.
- Runtime schema/work-area introspection gained a higher-impact adjacent slice. `FIELD()` and `FSIZE()` now resolve against local DBF-backed cursors and synthetic SQL result cursors through the expression callback path, including post-`ALTER TABLE` local schema widths and synthetic SQL field widths. `AFIELDS()` now reuses the shared cursor descriptor path, so synthetic SQL cursor schemas populate array metadata too. `AUSED()` now populates a two-column runtime array of aliases and work-area numbers for the current data session. Focused array, table-structure, and SQL-cursor regression targets pass.
- Runtime-surface host/environment helper coverage gained first-pass `HOME()`, `OS()`, `DISKSPACE()`, and `DRIVETYPE()` plus adjacent `SYS(2003)`, `SYS(2004)`, `SYS(2020)`, and `SYS(2023)` path/disk variants. The implementation uses Copperfin's runtime default directory where VFP host-install paths do not exist, filesystem-backed available-space probes, and conservative local-drive typing. Focused runtime-surface regression coverage passes.
- Low-level file I/O function family added as a new compatibility lane. `FOPEN`, `FCLOSE`, `FREAD`, `FWRITE`, `FGETS`, `FPUTS`, `FSEEK`, `FTELL`, `FEOF`, `FFLUSH`, `FCHSIZE`, `FILETOSTR`, and `STRTOFILE` are now implemented in `prg_engine_file_io_functions.cpp` and dispatched through the runtime-surface evaluator. Handle table, path resolution against the runtime default directory, binary and text I/O modes, append vs. truncate semantics for `STRTOFILE`, and `FCHSIZE` platform branching (POSIX `ftruncate`/Win32 `_chsize_s`) are all covered. Focused `test_prg_engine_file_io_functions` regression coverage passes.
- Expression helper coverage gained a larger mixed utility batch instead of another one-off: expression-list `MIN()` / `MAX()` now work for numeric and string argument lists without stealing one-argument cursor aggregate behavior; `STRCONV(..., 7)` / `STRCONV(..., 8)` provide first-pass lower/upper casing while DBCS/Unicode modes pass through unchanged; and `ISLEADBYTE()` returns false under the current single-byte runtime model. Focused string/type/aggregate regression targets pass.
- Type/null expression helper coverage gained first-pass `ISBLANK()` for expression values. Character values containing only whitespace and runtime empty values return true; numeric and logical values return false until DBF field state can preserve pristine blank metadata separately from stored zero/false values. Focused `test_prg_engine_functions` coverage validates the current expression-level behavior.
- String expression parity gained first-pass phonetic helpers `SOUNDEX()` and `DIFFERENCE()` in the extracted `prg_engine_string_functions` dispatch. The implementation returns four-character Soundex keys and scores `DIFFERENCE()` from 0 to 4 by positional Soundex similarity, with focused coverage for the VFP reference examples (`Tamar`, `ted`, `Smith`, and `Schmidt`).
- Runtime-surface bit helper parity gained the adjacent `BITCLEAR()`, `BITSET()`, and `BITTEST()` helpers with VFP-style zero-based bit positions over 32-bit signed values. Existing `BITAND()`, `BITOR()`, and `BITXOR()` now also accept more than two arguments. Focused runtime-surface coverage validates set/clear/test behavior, high-bit signed results, and variadic boolean operations.
- Numeric expression parity gained the next manifest-backed helper batch: `SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, and `LOG10()`. The functions live in the extracted `prg_engine_numeric_functions` dispatch, and focused `test_prg_engine_string_math_functions` coverage validates degree/radian conversion, inverse trig round-trips, VFP-style `ATN2(y, x)` argument order, and base-10 logarithms.
- shutdown-handler compatibility improved for event-loop apps. `QUIT` now implicitly exits `READ EVENTS` restoration paths (`waiting_for_events=false`, `restore_event_loop_after_dispatch=false`) so explicit cleanup lines like `CLEAR EVENTS` are no longer required during shutdown handlers. Legacy cleanup code remains harmless when present. Regression tests `test_shutdown_handler_quit_exits_event_loop_without_clear_events` and `test_shutdown_handler_cleanup_code_remains_harmless` cover both paths.
- QUIT now performs explicit runtime resource cleanup to reduce lingering file/connection locks and shutdown-risked corruption windows. Confirmed QUIT now closes all open work-area cursors/aliases across data sessions, disconnects SQL connection state, releases OLE/API interop state, unloads declared DLL handles on Windows, and force-closes outstanding `FOPEN` handles via a new `close_all_file_io_handles()` utility. Added regression coverage in `test_quit_closes_open_database_and_runtime_handles` to verify database/session cleanup and that post-QUIT `FCLOSE(<previous_handle>)` returns `-1`.
- `CLOSE` command cleanup depth now also releases non-DBF runtime handles for `CLOSE ALL` and `CLOSE DATABASES`: active SQL connection state is disconnected, tracked OLE objects are cleared, and outstanding `FOPEN` handles are force-closed through `close_all_file_io_handles()`. This makes explicit shutdown-style close flows safer against lingering file/connection locks even without `QUIT`. Focused regression `test_close_all_releases_runtime_handles` validates SQL/OLE release and verifies follow-up `FCLOSE(<previous_handle>)` returns `-1`.
- control-flow shutdown compatibility now includes first-pass `ON SHUTDOWN`. The runtime records a shutdown handler clause, executes `ON SHUTDOWN DO <routine> [WITH ...]` before the final `QUIT` unwind, supports the common inline `ON SHUTDOWN CLEAR EVENTS` form, and prevents recursive multi-quit loops when the shutdown routine itself calls `QUIT`. Focused regression coverage validates inline `CLEAR EVENTS` handling and an event-loop cleanup procedure that runs `CLEAR EVENTS`, `CLOSE DATABASES ALL`, and nested `QUIT` before final shutdown completion.
- shutdown introspection/inline compatibility deepened again. `ON('SHUTDOWN')` now returns the active shutdown clause the same way `ON('ERROR')` reports the active error handler, and inline `ON SHUTDOWN` clauses now also recognize close-style cleanup commands (`CLOSE ALL`, `CLOSE TABLES`, `CLOSE DATABASE(S) [ALL]`) in addition to `CLEAR EVENTS`. Focused regression coverage validates `ON('SHUTDOWN')` readback and inline `ON SHUTDOWN CLOSE DATABASES ALL` execution before final quit completion.

## 2026-04-21

- Runtime-surface expression dispatch was reduced again by extracting `CAST()`, bitwise helpers (`BITAND()` / `BITOR()` / `BITXOR()` / `BITNOT()` / `BITLSHIFT()` / `BITRSHIFT()`), binary conversion helpers (`BINTOC()` / `CTOBIN()`), and the current first-pass `CURSORGETPROP()` / `CURSORSETPROP()` / lock-key probe stubs into dedicated `prg_engine_runtime_surface_functions` sources. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down those helpers outside the legacy catch-all paths.
- Runtime source-size cleanup continued by extracting portable path expression helpers into dedicated `prg_engine_path_functions` sources. The focused path regression target guards `FULLPATH()`, `JUST*`, `FORCE*`, and `ADDBS()` behavior while `prg_engine_expression.inl` drops to roughly 1.5K lines.
- Date/time conversion parity gained compact sortable datetime input support for `CTOT()` and `TTOD()`. Focused `test_prg_engine_date_time_functions` coverage now accepts `YYYYMMDDHHMMSS` input and normalizes it to `MM/DD/YYYY HH:MM:SS` or `MM/DD/YYYY` output respectively.
- Runtime source-size cleanup continued by extracting numeric math expression helpers into dedicated `prg_engine_numeric_functions` sources. The focused string/math regression target guards the move while `prg_engine_expression.inl` drops to roughly 1.5K lines.
- `STREXTRACT()` expression parity tightened with focused TDD coverage against the Visual FoxPro flag semantics: bit `1` enables case-insensitive delimiter search, bit `2` allows a missing end delimiter to return the trailing content, and bit `4` includes delimiters in the returned expression.
- Focused expression-function test splitting continued by moving runtime-surface utility coverage (`EVALUATE()`, `TRANSFORM()`, `TYPE()`, and `SET()`) into dedicated `test_prg_engine_runtime_surface_functions`. The original `test_prg_engine_functions` target is now narrowed to type/null helper coverage.
- Runtime source-size cleanup continued by extracting type/null and simple conditional expression helpers into dedicated `prg_engine_type_functions` sources. The now-narrow `test_prg_engine_functions` target guards `EMPTY()`, `VARTYPE()`, `NVL()`, and related behavior while `prg_engine_expression.inl` drops to roughly 1.6K lines.
- Expression `PADL()` / `PADR()` / `PADC()` truncation parity tightened with focused TDD coverage in `test_prg_engine_string_math_functions`. `PADL()` now keeps the rightmost characters on overflow, `PADR()` keeps the leftmost characters, and `PADC()` keeps the centered slice while preserving existing default/custom padding behavior.
- Focused expression-function test splitting continued by moving aggregate `TO ARRAY` coverage into dedicated `test_prg_engine_aggregate_array_functions`. The mixed `test_prg_engine_functions` target now keeps only type/null and runtime-surface expression coverage.
- Runtime source-size cleanup continued by extracting the main string helper expression dispatch into dedicated `prg_engine_string_functions` sources. The focused string/math regression target guards the move while reducing `prg_engine_expression.inl` by another roughly 270 lines.
- `TTOC()` expression conversion parity gained focused compact output support: `TTOC(datetime, 1)` now returns deterministic `YYYYMMDDHHMMSS` output while preserving default normalized datetime behavior, and date-only compact input is pinned to midnight output.
- Expression `STR()` gained first-pass width/decimal formatting parity under focused TDD coverage. The runtime now honors `STR(value, width)`, `STR(value, width, decimals)`, right-justifies into the requested width, rounds fixed decimals, and emits asterisks for overflow.
- Focused expression-function test splitting continued by moving string/math helper coverage into dedicated `test_prg_engine_string_math_functions`. The split isolates the `LEN`/`LEFT`/`RIGHT`/case/trim/math/search/word/`STR()` utility batch from the remaining aggregate and runtime-surface coverage.
- `GETWORDCOUNT()` / `GETWORDNUM()` expression parity tightened with focused TDD coverage. Delimiter arguments now behave as delimiter-character sets instead of one literal delimiter substring, and default delimiters now include common whitespace such as tabs and line breaks.
- Runtime source-size cleanup continued by extracting callback-backed runtime-surface expression helpers from `prg_engine_expression.inl` into `prg_engine_runtime_surface_functions`: `FILE()`, `SYS()`, `MESSAGE()`, `AERROR()`, `EVAL()` / `EVALUATE()`, `SET()`, `ERROR()`, `PROGRAM()`, `LINENO()`, `VERSION()`, `ON()`, and `MESSAGEBOX()`. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down the extracted file/system/configuration helpers, while existing `test_prg_engine_control_flow` coverage continues guarding the error-state helpers.
- `DTOC()` expression conversion parity gained focused support for optional compact output mode: `DTOC(date, 1)` now returns deterministic `YYYYMMDD` output while preserving default normalized `MM/DD/YYYY` behavior.
- Runtime source-size cleanup continued by extracting expression date/time helper dispatch from `prg_engine_expression.inl` into dedicated `prg_engine_date_time_functions` sources. The move keeps the existing focused `test_prg_engine_functions` date/time coverage as the regression guard while reducing the expression parser include by roughly 545 lines.
- Focused expression-function test splitting continued by moving date/time expression regressions into a dedicated `test_prg_engine_date_time_functions` target. The original `test_prg_engine_functions` target now keeps the remaining path/string/math/type/null/aggregate/runtime-surface batches smaller, while the date/time target owns constructor, conversion, week, Julian, and invalid-input coverage.
- Focused expression-function test splitting continued again by moving portable path helper coverage into a dedicated `test_prg_engine_path_functions` target. The split keeps Windows-style and POSIX-style `JUST*` / `FORCE*` regressions isolated from the remaining mixed expression-function batches.
- Date/time conversion invalid-input semantics were tightened with focused TDD coverage in `test_prg_engine_functions`: `JTOD()` / `JTOT()` now reject out-of-runtime-range Julian values instead of materializing impossible negative-year dates, `DTOJ()` / `CTOD()` reject trailing garbage after slash or compact date input, and `TTOJ()` now preserves datetime input support through explicit datetime parsing.
- The first-pass `WEEK()` expression helper now accepts a third argument for first-week-of-year mode in addition to the existing first-day argument: mode `1` keeps Jan-1-containing week behavior, mode `2` uses first full week semantics, and mode `3` uses first-4-day-week semantics. Focused `test_prg_engine_functions` coverage now locks down deterministic year-boundary and cross-year rollover behavior for these modes, including January rollback into the prior week-year and late-December mode-3 rollover into week 1.
- Expression-function date/time conversion coverage gained first-pass `STOD()` and `TTOS()` in `test_prg_engine_functions`. `STOD()` now accepts compact `YYYYMMDD` inputs and returns normalized `MM/DD/YYYY` dates with empty-string output for malformed inputs; `TTOS()` now emits deterministic sortable `YYYYMMDDHHMMSS` strings from datetime or date inputs (date-only values normalize to midnight).
- Expression-function date/time coverage gained constructor-argument support for `DATE()` and `DATETIME()` in `test_prg_engine_functions`. The runtime now accepts `DATE(year, month, day)` and `DATETIME(year, month, day[, hour[, minute[, second]]])` with first-pass range validation and deterministic `MM/DD/YYYY` and `MM/DD/YYYY HH:MM:SS` output formatting, while preserving existing no-argument current-date/current-datetime behavior.
- Expression-function date/time coverage gained another adjacent helper batch in `test_prg_engine_functions`: first-pass `WEEK()`, `QUARTER()`, and `EOMONTH()`. The runtime now computes week-of-year values with optional first-day offset (`WEEK(date, firstDay)`), quarter-of-year extraction from parsed date inputs, and end-of-month date materialization with optional month deltas (`EOMONTH(date, nMonths)`) while preserving existing `MM/DD/YYYY` string-date semantics.

## 2026-04-18

- command-level aggregate follow-through gained first-pass `TO ARRAY` parity for `COUNT`, `SUM`, and `AVERAGE` using the shared runtime array assignment path. Focused `test_prg_engine_functions` coverage now validates one-element array assignment, scope-clause behavior (`ALL`/`REST`/`NEXT`/`RECORD`), `FOR` filters, `IN` alias targeting, and malformed `TO ARRAY` diagnostics.
- Expression-function date/time coverage gained the next adjacent conversion batch in `test_prg_engine_functions`: `CTOT()`, `DTOT()`, `TTOD()`, `HOUR()`, `MINUTE()`, and `SEC()`. The runtime now parses first-pass `MM/DD/YYYY HH:MM:SS` datetime strings for conversion and extraction helpers, emits normalized `MM/DD/YYYY HH:MM:SS` output for `CTOT()` / `DTOT()`, and supports component extraction from both time-only and datetime string inputs for deterministic regression assertions.
- Expression-function date/time helper coverage gained the next small adjacent batch in `test_prg_engine_functions`: `DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, and `MDY()`. The runtime now parses `MM/DD/YYYY` (plus compact `YYYYMMDD`) for these helpers, supports first-pass `DOW(..., <firstDayOfWeek>)` shifting, clamps `GOMONTH()` to month-end on overflow days (for example, `01/31 + 1` month), and keeps `SECONDS()` as second-of-day output for deterministic numeric assertions.
- `ASCAN()` predicate search now preserves unquoted braced block-style arguments through expression parsing, so forms like `ASCAN(aValues, {|x| x > 8}, -1, -1, -1, 16)` work in addition to stringified predicates. Array declaration/element and array metadata/text helper regressions moved from the legacy PRG catch-all into `test_prg_engine_arrays`. A new `test_prg_engine_functions` target now starts the standalone expression-function split and covers portable string parsing for `JUSTPATH()`, `JUSTFNAME()`, `JUSTSTEM()`, `JUSTEXT()`, and new `JUSTDRIVE()` across Windows-style and POSIX-style paths.
- Expression-function test splitting continued: string/math and type/null function coverage moved from `test_prg_engine` into `test_prg_engine_functions`. The same focused target now covers the next small manifest-derived utility batch: `FORCEEXT()`, `FORCEPATH()`, and first-pass case-insensitive `CHRTRANC()` behavior. Local focused validation passed for both `test_prg_engine_functions` and the trimmed legacy `test_prg_engine`.
- Array parity now has a dedicated `test_prg_engine_arrays` regression target. `ASCAN()` gained first-pass predicate-expression search under flag `16`, including stringified `{|x| ...}` block-style predicates and temporary `_ASCANVALUE` / `_ASCANINDEX` / `_ASCANROW` / `_ASCANCOLUMN` metadata restored after the scan. Focused coverage also locks down common two-dimensional `ACOPY()` workflows: whole-row copies via `AELEMENT()` + `ALEN(..., 2)` and column-helper copies through one-element `ACOPY()` calls.
- Array parity now covers numeric-aware `ASORT()` ordering and VFP-style `ADEL()`/`AINS()` row and column behavior for two-dimensional arrays, including false-filled trailing or inserted slots. Focused PRG regression coverage exercises numeric sorting, bounded/windowed sorting, two-dimensional row sorts, row deletion/insertion, and column deletion/insertion.
- Runtime array parity advanced again: `ASCAN()` now understands search columns plus case-insensitive, exactness override, and row-return flags; `ASORT()` now supports start/count windows, descending order, case-insensitive keys, and two-dimensional row sorting by the start element's column. The Linux validation warning noise was reduced by replacing several partial designated initializers in runtime code, moving Windows-only DLL parameter helpers behind the Windows guard, formatting date/time strings without fixed buffers, and suppressing intentional test harness aggregate-default warnings locally in `test_prg_engine.cpp`.

## 2026-04-17

- GitHub-hosted validation now has a dedicated native CMake workflow across Linux, macOS, and Windows, current Node 24-compatible action versions, manual-only Windows deep validation for native/VSIX/Studio/smoke-test build coverage, and fixed installer artifact uploads for CPack outputs. Runtime array parity also gained start/count-bounded `ASCAN()` scans over row-major array storage with focused regression coverage.
- Linux-side validation is now available from a repo-local `.codex-venv` CMake install, with local generated artifacts ignored and a `scripts/validate-posix.sh` wrapper that runs under `zsh`, `bash`, or `sh`. The first Linux runs exposed portability issues in Windows-only process includes, PowerShell test launching, path-separator-specific exporter assertions, DBF memo-sidecar directory reads, project-workspace Windows-path fallback handling, runtime-host canonical-name validation, and non-Windows SHA-256 hashing; those were fixed without changing the Windows-first product direction. The full native CTest suite now builds and passes under the local Linux CMake/Ninja toolchain.
- The PRG expression-function surface gained the next small official string/comparison utility batch: occurrence-aware `AT()` / `RAT()`, first-pass case-insensitive `ATC()` / `RATC()`, line-oriented `ATLINE()` / `ATCLINE()` / `RATLINE()`, wildcard `LIKE()`, `INLIST()`, and `PROPER()`. Focused coverage was added to the existing string/math expression regression cluster.
- Structural local-table coverage now includes first-pass `ALTER TABLE ... ADD COLUMN`, implemented as a conservative DBF schema rewrite that appends one supported field while preserving existing values and deleted flags. `CREATE TABLE` field parsing also accepts the broader DBF write-surface type family (`B`/`DOUBLE`, `V`/`VARCHAR`, `Q`/`VARBINARY`) and nullable/default annotations as declaration syntax. Synthetic SQL result cursor parity now covers direct SQL-style `INSERT INTO <sqlcursor>` and `DELETE FROM <sqlcursor> WHERE ...` while preserving the selected cursor and target row state. Focused `test_prg_engine` coverage passes, and `test_dbf_table` remains green.
- The structural table slice now also covers `ALTER TABLE ... DROP COLUMN`, `ALTER TABLE ... ALTER COLUMN`, runtime-enforced `DEFAULT`/`NOT NULL` metadata for created or altered open cursors, and first-pass `PACK MEMO` memo-sidecar compaction. Structural field parsing moved out of the PRG engine monolith into `prg_engine_table_structure_helpers`, and table-structure runtime regressions moved into the dedicated `test_prg_engine_table_structure` executable so new coverage no longer grows the already-large main PRG test file. `test_dbf_table`, `test_prg_engine`, and `test_prg_engine_table_structure` pass.
- The large PRG-engine regression file has started its incremental split. Shared fixture helpers now live in `tests/prg_engine_test_support.{h,cpp}`. SQL result cursor coverage moved to `test_prg_engine_sql_cursors`, and local table mutation/update coverage moved to `test_prg_engine_table_mutation`, cutting `tests/test_prg_engine.cpp` from roughly 10,240 lines to roughly 7,460 lines while keeping a legacy catch-all for the remaining clusters. `test_prg_engine`, `test_prg_engine_sql_cursors`, `test_prg_engine_table_mutation`, and `test_prg_engine_table_structure` pass.
- Local table-maintenance commands now include first-pass `PACK` and `ZAP` support in the PRG runtime. `PACK` physically compacts local DBF-backed cursors by removing deleted records while preserving schema and row order for kept records; `ZAP` truncates local DBF-backed cursors to zero records while keeping the table appendable through the existing `APPEND BLANK` / `REPLACE` mutation path. Synthetic remote/result cursors get matching in-memory first-pass behavior. New focused `test_prg_engine` coverage verifies updated `RECCOUNT()`, readable DBF output, file-size shrinkage, event emission, and append-after-`ZAP` persistence; `test_dbf_table` and `test_prg_engine` pass.
- SQL-style local table mutation now includes first-pass `DELETE FROM <target> WHERE/FOR <expr>` and `INSERT INTO <target> [(fields)] VALUES (...)`. `DELETE FROM` uses a distinct statement kind so conditionless SQL-style deletes tombstone all target-visible rows instead of inheriting xBase bare-`DELETE` current-record semantics. `INSERT INTO` appends through the existing blank-row plus field-replacement path, maps omitted field lists by target schema order, and supports expression-driven cursor targets. The shared command CSV splitter now keeps commas inside both single- and double-quoted tokens together, while expression-level double-quoted string semantics remain a separate language-compatibility slice. Focused `test_prg_engine` coverage verifies expression target resolution, reversed field-list mapping, schema-order insertion, `DELETE FROM WHERE` tombstoning, event emission, selected-cursor preservation, and persisted DBF readability.
- `INSERT INTO` failure handling now rolls back the appended blank row when a later field write fails. Local DBF-backed inserts truncate the table back to the pre-insert record count; synthetic remote/result cursors resize the in-memory row set back to its original count. Focused `test_prg_engine` coverage verifies oversized character inserts pause on the field-write error, then continuing execution sees the original `RECCOUNT()`, original DBF file size, readable DBF output, and original row values.
- `CREATE TABLE` now has first-pass local DBF support for simple field declarations (`C`/`CHAR`, `N`/`NUMERIC`, `F`/`FLOAT`, `L`/`LOGICAL`, `D`/`DATE`, `I`/`INTEGER`, `Y`/`CURRENCY`, `T`/`DATETIME`, and `M`/`MEMO`). The command creates a DBF through `create_dbf_table_file`, opens the created table as a local cursor, and immediately participates in `INSERT INTO`, field lookup, and `RECCOUNT()`. Focused `test_prg_engine` coverage verifies created schema metadata, insert/read behavior, event emission, and persisted DBF readability.

## 2026-04-16

- The next combined runtime/data batch added first-pass `ALINES()`, `ADIR()`, and `AFIELDS()` over the runtime array substrate; broadened diagnostics so SQL/ODBC-like failures populate a VFP-style `1526` `AERROR()` row and OLE/automation-like failures populate a `1429` row; and expanded text import/export beyond SDF with VFP-style `COPY TO ... TYPE CSV`, `COPY TO ... DELIMITED`, `APPEND FROM ... TYPE CSV`, and `APPEND FROM ... DELIMITED`. CSV export now writes the field-name header row, quoted character fields, and unquoted numeric/logical fields; CSV import skips a matching header row. `DELIMITED` honors both field enclosure (`WITH '_'`) and separator (`WITH CHARACTER ';'`) options. Focused `test_prg_engine` coverage locks down array metadata functions, SQL/OLE `AERROR()` row shapes, and CSV/delimited round-trips.
- Runtime arrays now include the next adjacent helper family: `ACOPY()`, `AELEMENT()`, and `ASUBSCRIPT()` in addition to the existing mutator/search helpers. `ACOPY()` copies row-major windows between runtime arrays and can grow the target array when needed; `AELEMENT()` / `ASUBSCRIPT()` translate between 2D subscripts and VFP-style one-based element numbers. The diagnostics slice was corrected against the VFP reference shape: `AERROR(<array>)` now emits a one-row/seven-column array for normal runtime errors, with code, message, mixed-case error parameter, selected work area, and empty trigger/reserved columns; `SYS(2018)` exposes the uppercase error parameter. Focused `test_prg_engine` coverage locks down the array helper behavior and corrected `AERROR()`/`SYS(2018)` metadata.
- Runtime arrays now also support first-pass `DIMENSION` plus array-form `DECLARE`, direct element assignment through bracket and parenthesis syntax (`array[1] = ...`, `array[1,2] = ...`, `array(1) = ...`, `array(1,2) = ...`), and two-dimensional reads that preserve values across `ASIZE()` growth. The shared command CSV splitter was tightened so commas inside `[]` dimensions do not split declarations or argument lists prematurely. Focused `test_prg_engine` coverage locks down declared 1D/2D arrays, mixed bracket/paren reads and writes, and 2D resize preservation.
- Runtime arrays now support first-pass mutator/search helpers: `ASIZE()`, `ASCAN()`, `ADEL()`, `AINS()`, and `ASORT()` over arrays created by runtime paths such as `SCATTER TO` and `AERROR()`. `UPDATE` parsing also now covers `UPDATE SET ...` against the selected cursor and `UPDATE IN <alias> SET ...` in addition to the existing `UPDATE <alias> SET ...` form. Focused `test_prg_engine` coverage locks down the new array mutators and expanded update grammar. A later diagnostics correction restored `AERROR()` to the VFP-aligned seven-column shape.
- `AERROR(<array>)` now populates the first real structured runtime error array on top of the new runtime array substrate. The corrected first-pass shape is one row by seven columns for normal runtime errors: error code, message, mixed-case error parameter, selected work area, and empty trigger/reserved columns. This complements `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `SYS(2018)`, and `ON('ERROR')` and gives diagnostics/debugger work richer state to inspect without overclaiming a non-VFP nine-column shape. `UPDATE <alias> SET field = expr [, ...] WHERE/FOR <expr>` also shipped as a first-pass scoped mutation command over local and remote cursors, reusing the existing replacement engine while keeping legacy `REPLACE` current-record semantics intact. Focused `test_prg_engine` coverage locks down both `AERROR()` array access and scoped `UPDATE` mutation.
- `SCATTER` / `GATHER` parity moved beyond memvar-only basics. `SCATTER FIELDS ... MEMVAR`, `SCATTER ... MEMVAR BLANK`, and `GATHER MEMVAR FIELDS ... FOR ...` now honor field filtering, typed value conversion, typed blank values, and `FOR`-gated replacement. The runtime also has first-pass internal array storage for `SCATTER ... TO <array>` and `GATHER FROM <array>`, including one-dimensional `ALEN(<array>)`, `ALEN(<array>, 1/2)`, bracket reads (`array[1]`), and paren reads (`array(1)`). Focused `test_prg_engine` coverage locks down memvar and array round-trips, giving the upcoming `AERROR()` slice a real array substrate instead of the old `ALEN()` stub.
- `COPY TO ... TYPE SDF` and `APPEND FROM ... TYPE SDF` now have first-pass fixed-width text support. SDF export writes one CRLF-terminated row per visible local DBF record using selected field descriptor widths, with `FIELDS` and `FOR` clauses flowing through the existing DBF export selection path. SDF import reads fixed-width rows back into the current local DBF cursor by slicing against the current table schema and optional `FIELDS` selection, appending each row through the shared `append_blank_record_to_file` + `replace_record_field_value` mutation path. Focused `test_prg_engine` coverage locks down export and import round-trips.
- `m.` memory-variable namespace parity was tightened across assignment, lookup, expression evaluation, and declarations. `m.<name>`, `M.<name>`, and bare `<name>` now share the same local/global runtime binding instead of creating separate prefixed variables or falling through to OLE property resolution. `PUBLIC`/`LOCAL`/`PRIVATE`/`LPARAMETERS` declarations also canonicalize the prefix, and focused `test_prg_engine` coverage locks down prefixed-to-bare and bare-to-prefixed reads/writes.
- `COPY TO`, `COPY STRUCTURE TO`, and `APPEND FROM` moved from event-only stubs to full local-DBF implementations. `COPY TO` resolves and creates the destination file via `create_dbf_table_file`, respecting optional `FIELDS` and `FOR` clauses. `COPY STRUCTURE TO` follows the same path but skips record population (zero rows). `APPEND FROM` parses the source via `parse_dbf_table_from_file` then inserts each non-deleted source record using `append_blank_record_to_file` + `replace_record_field_value`. `GATHER MEMVAR` correctness was fixed: the old path built `"value"` string expressions and re-evaluated them (lossy for numeric/logical/date); the new path calls `replace_record_field_value` directly. A related assignment-handler bug was fixed: dotted identifiers with `m.` prefix were being incorrectly dispatched through the OLE property path; `m.` is now recognized as the VFP memory-variable namespace and stored as a plain variable. All four changes have focused regression tests in `test_prg_engine`; all tests pass.

## 2026-04-13

- status classification was re-aligned to the active issue backlog. Multiple areas previously marked green are now tracked as partial until the corresponding open issue groups are closed and validated.
- the old gray dependency callouts under Phase C were retired from the architecture diagram. Shared designers, Visual Studio integration, the standalone Studio shell, and the shipped language-service baseline are all now green, so those relationships are no longer tracked as unresolved blockers.
- Phase C reached green status. The repo now treats the build/package/debug pipeline, shared designers, Visual Studio integration, standalone Studio shell, and FoxPro language-service layer as implemented, with the subsystem registry updated to match the shipped Phase C surfaces.
- Phase B runtime parity surfaces reached green status. The shared xAsset runtime model now covers first-pass form/class startup and shutdown sequencing (`DataEnvironment` open/close hooks plus root-object lifecycle methods where present), report/label preview and `TO FILE` runtime lanes are both regression-covered, and menu bootstrap generation now includes setup, action dispatch, submenu activation wrapping, and cleanup with real-sample model validation.
- PRG execution-engine parity reached green status. The runtime now supports first-pass `WITH/ENDWITH` execution semantics for leading-dot member access, first-pass `TRY/CATCH/FINALLY/ENDTRY` control flow with handled-error continuation and `FINALLY` execution, and first-pass `DO ... WITH @var` reference semantics that write callee updates back to the caller.
- first-pass host/process hardening landed across build/runtime/studio/inspect executables. Windows process startup now applies DLL search-path hardening, security-enabled package materialization now rejects non-canonical runtime-host binary names, and generated launcher publish now avoids shell-based command execution.
- enterprise security controls now have an end-to-end baseline. Runtime packages now emit SHA-256 digests for the packaged runtime host and extension payload entries, runtime host verifies those digests before execution when security is enabled, external process launches are policy-gated through trusted signature/publisher allow-list checks, security-enabled release packaging enforces provider-backed signing-key references (`env:<NAME>`), immutable hash-chained audit events are emitted for build/runtime security actions, runtime/build hosts enforce role permissions for privileged operations, and CI now includes SBOM output plus a HIGH/CRITICAL CVE gate.
- database federation now has a first-pass deterministic translator slice in native code. Copperfin can now translate constrained Fox SQL `SELECT ... FROM ...` inputs into backend-targeted relational SQL variants for `sqlite`, `postgresql`, `sqlserver`, and `oracle`, with focused regression coverage in `test_query_translator` while deeper connector/runtime execution integration remains open.
- runtime federation integration now has a first-pass deterministic connector execution-planning lane. `copperfin_runtime_host` can now materialize federated query plans (`--federation-backend` + `--federation-query`) that consume the shared translator, emit backend/connector/target metadata, and return deterministic execution-command scaffolding with focused coverage in `test_federation_execution`, while live backend connector execution still remains open.

## 2026-04-14

- PRG engine parity extended to cover `FOR EACH/ENDFOR` iteration over declared arrays and scalar expressions, `RELEASE <varlist>`, `RELEASE ALL [LIKE/EXCEPT <pattern>]`, `CLEAR MEMORY`, `CLEAR ALL`, `CANCEL`, and `QUIT`. The `LoopState` struct now tracks FOR EACH snapshot state; `wildcard_match_insensitive` drives LIKE/EXCEPT pattern filtering; CANCEL and QUIT unwind the full call stack and emit `runtime.cancel`/`runtime.quit` events respectively. Focused regression coverage added in `test_prg_engine_control_flow`.
- QUIT statement now supports host-configurable confirmation. `RuntimeSessionOptions` gains `quit_confirm_callback` (`std::function<bool()>`); when set, QUIT calls it before unwinding — if the callback returns `false` the quit is cancelled and execution continues from the next statement, emitting a `runtime.quit_cancelled` event instead of `runtime.quit`. `copperfin_runtime_host` wires a console-based "Do you want to quit this application?" prompt that reads from stdin (EOF → allow quit). Regression test `test_quit_cancelled_by_callback` validates the cancellation path.

## 2026-04-12

- PRG structured-flow semantics now include first-pass `ELSEIF` branch support and `DO ... WITH` argument binding into `PARAMETERS`/`LPARAMETERS` locals for called routines, with focused regression coverage.
- the PRG engine split progressed further: parser loading now lives in `prg_engine_parser.cpp`, static diagnostics live in a shared `cf_prg_analysis` library, and the Studio document path now carries analyzer diagnostics for `.prg` files without introducing a runtime/design-model link cycle.
- `ON ERROR` compatibility now has a richer first-pass handler lane. `ON ERROR DO <routine> WITH ...` can pass evaluated handler arguments, and handler-visible `MESSAGE()`, `PROGRAM()`, `LINENO()`, and `ERROR()` now expose the failing statement context with focused runtime regression coverage.
- PRG runtime safety now includes configurable guardrails (`max_call_depth`, `max_executed_statements`, `max_loop_iterations`), cooperative scheduler yielding, and first-pass `CONFIG.FPW`/`CONFIG.FP` loading for guardrail and temp-directory policy. Runtime temp defaults now prefer OS-local temp rather than application/share paths.
- PRG runtime internals were split into dedicated modules for maintainability (`prg_engine_runtime_config.cpp` and `prg_engine_static_analysis.cpp`) to reduce monolith pressure in `prg_engine.cpp` while preserving full regression coverage.
- local mutation command parity now supports first-pass `WHILE` clause semantics across `REPLACE`, `DELETE`, and `RECALL` in addition to `FOR`/`IN` targeting. Runtime mutation walks now stop at the first failing `WHILE` boundary and preserve non-targeted records, with focused regression coverage on targeted-cursor write behavior.
- SQL pass-through now includes first-pass prepared execution and connection-property control surfaces. `SQLPREPARE`, `SQLEXEC(<handle>)`, `SQLGETPROP`, and `SQLSETPROP` are now wired through runtime connection state with provider/timeout/prepared-command metadata and event emission coverage.
- local query command parity now supports first-pass `WHILE` clause semantics on `LOCATE` and `SCAN`, including expression-driven `IN` targeting. Runtime cursor walks now stop at the first failing `WHILE` boundary, and focused regression coverage locks down targeted-cursor positioning and scan-iteration counts.
- SQL pass-through now has first-pass DML metadata coverage beyond cursor-only SELECT flows. Runtime `SQLEXEC()` now tracks rows-affected semantics for insert/update/delete-style commands and exposes the latest result through `SQLROWCOUNT(<handle>)`, while SQL connection state now infers provider hints (for example ODBC/OLE DB-style connect strings) for runtime inspection.
- command-level local mutation parity now includes first-pass `REPLACE ... FOR ... IN ...` coverage. Runtime execution now applies replacements across all matching visible records for the targeted cursor instead of only the current record, with focused regression coverage on matching/non-matching row behavior.
- reports/labels now support a first-pass non-preview render lane through `REPORT/LABEL ... TO FILE`. Runtime execution now emits render artifacts and continues script execution without entering event-loop preview mode, with focused runtime coverage for the TO FILE path.
- indexed-table local mutation guardrails were removed for production-flag and same-base companion-index tables. Shared DBF mutation APIs (`APPEND BLANK`, `REPLACE`, `DELETE`/`RECALL`) now execute on indexed tables, with focused DBF/runtime regressions locking down persisted writes and readable round-trip behavior.
- synthetic SQL remote-cursor parity now has focused mutation-command coverage. Runtime regressions now lock down `APPEND BLANK`, `REPLACE`, `DELETE FOR`, and `RECALL FOR` over `SQLEXEC()` result cursors, including appended-row value persistence and runtime event emission.
- PRG `TEXT` blocks now support first-pass `TEXTMERGE` interpolation. Runtime execution now resolves `<<expression>>` segments inside `TEXT ... ENDTEXT` blocks using the existing expression evaluator, and focused regression coverage locks down merged output values and runtime event emission.
- work-area/data-session runtime parity now has explicit cross-session isolation coverage. Regression checks now lock down that alias lookup is session-local, the same table can be opened in different sessions without collision, work-area `1` is independent per session, and `USE IN 1` only closes the current session's area 1 without bleeding into other sessions.
- DBC container inspection now surfaces first-pass catalog metadata instead of only header/companion checks. The inspector now loads container records, extracts normalized object-type/name/parent hints, emits per-type object counts plus bounded object previews, and reports structured warnings when catalog extraction fails or returns no catalog-object metadata.
- index inspection now captures richer per-tag metadata for both CDX/DCX and MDX. CDX/DCX probes now expose tag-page header marker hints (`flags`/entry counts) in addition to grounded tag-page key/`FOR` binding, while MDX probes now parse tag-table marker bytes (format/type/thread), tag-header page offsets, and first-pass key/`FOR` expressions from tag-header pages instead of name-only block hints.
- shared DBF mutation now supports first-pass `V`/`Q` var-length field create/replace/append flows using trailing-length storage semantics, with focused round-trip coverage for value updates plus blank append initialization while keeping unsupported-layout guards pinned to genuinely unsupported field types.
- shared DBF mutation now accepts a constrained `NULL` token across supported direct-write field families (`C`/`N`/`F`/`L`/`D`/`B`/`I`/`Y`/`T`) using first-pass clear/zero semantics, and date display decoding now renders all-space stored dates as empty values instead of synthetic `----` placeholders.
- staged DBF/memo writes now have focused cleanup coverage that locks down removal of temporary (`.cptmp`) and backup (`.cpbak`) artifacts after successful mutation writes.
- shared DBF mutation writes now use staged temporary-file swaps with backup/restore behavior to reduce partial-write risk during local-table mutation. Memo-backed replacements now also recover sidecar-path anomalies (for example, unexpected directory collisions at the memo sidecar path) while keeping DBF data readable and updated.
- shared DBF mutation now supports FoxPro `General (G)` and `Picture (P)` pointer fields using the same memo-sidecar write path as `M`. Create/replace/append flows now persist and reload G/P memo payloads, with focused round-trip coverage locking down pointer updates and blank-append pointer initialization.
- shared DBF mutation now supports FoxPro `Double (B)` field create/replace/append flows. The layer now enforces fixed-width 8-byte `B` storage, persists binary double values on write paths, decodes `B` values back through parser display output, and has focused round-trip coverage locking down positive/negative replacement plus blank-append zero initialization.
- local indexed-search now evaluates a constrained first-pass `STR(<numeric>[, <width>[, <decimals>]])` tag-expression path on shipped command/function `SEEK` flows. Loaded tags such as `UPPER(STR(AGE, 3))`, `UPPER(STR(AGE))`, and `UPPER(STR(AGE, 5, 1))` now produce fixed-width right-aligned keys with focused regression coverage for default-width and decimal seek behavior.
- local indexed-search now locks down first-pass default-space `PADL(...)` / `PADR(...)` tag-expression behavior in addition to explicit pad-character forms. This slice also preserves significant seek-key whitespace on command/function `SEEK`, which is required for padded-key tag matches.
- local indexed-search now evaluates first-pass padded tag expressions with `PADL(...)` / `PADR(...)` on the shipped `SEEK` path. Loaded tags such as `UPPER(PADL(NAME, 8, '0'))` and `UPPER(PADR(NAME, 8, '0'))` now produce derived keys for command/function `SEEK`, with focused regression coverage locking down padded-key matches.
- local selected-work-area reuse now has focused data-session round-trip coverage after closing the selected alias. Regression checks now lock down `USE IN <selected-alias>`, `SELECT(0)`, and restored plain `USE` behavior so the original session keeps reusing the emptied selected work area after switching away and back.
- local indexed-search now evaluates first-pass substring-style tag expressions with `RIGHT(...)` and `SUBSTR(...)` on the shipped `SEEK` path in addition to the earlier `LEFT(...)` slice. Loaded tags such as `UPPER(RIGHT(NAME, 3))` and `UPPER(SUBSTR(NAME, 2, 3))` now produce derived keys for command/function `SEEK`, and the shared CDX parser now also binds descriptive stored tag names such as `FULLNAME` to their hinted tag-page-local expressions even when the tag name does not resemble the key expression.
- local indexed-search now evaluates first-pass substring-style tag expressions with `LEFT(...)` on the shipped `SEEK` path. Loaded tags such as `UPPER(LEFT(NAME, 3))` now produce truncated normalized keys for command/function `SEEK`, and the synthetic CDX fixture coverage now writes realistic tag-page pointers so composite/tag-expression regressions keep exercising the parser/runtime seam the way FoxPro indexes expect.
- local indexed-search now evaluates first-pass composite tag expressions on the shipped `SEEK` path instead of only simple field/unary expressions. Concatenated expressions such as `UPPER(LAST+FIRST)` now produce real composite keys for command/function `SEEK`, with focused regression coverage locking down case-folded concatenated matches.
- first-pass local `TOTAL` parity now accepts FoxPro `Integer (I)` and `Currency (Y)` fields in addition to `N/F`. Output DBFs preserve fixed-width `I`/`Y` field layouts instead of widening them into invalid descriptors, and focused regression coverage now locks down grouped currency-plus-integer totals while preserving the current record position.
- local-table selection-flow parity now has focused data-session round-trip coverage alongside the existing SQL variant. Regression checks now lock down `SELECT 0`, plain `USE`, and restored selected-work-area/alias behavior across `SET DATASESSION` switches so each session keeps reusing its own selected empty local work area.
- synthetic SQL temporary-order probe coverage now also locks down the combined direction-suffix path. `SEEK()` / `INDEXSEEK(.T.)` with one-off designators such as `UPPER(NAME) DESCENDING` now have focused regression coverage proving case-folded descending near-positioning while still preserving the controlling order.
- synthetic SQL temporary-order expressions now derive and consume first-pass normalization metadata instead of treating every ad hoc order as raw string compare only. `SEEK()` with one-off order expressions such as `UPPER(NAME)` plus command-path `SET ORDER TO UPPER(NAME)` / `SEEK` now case-fold search keys against in-memory SQL rows, with focused regression coverage and runtime event metadata.
- synthetic SQL order-direction targeting now has focused non-selected-target `IN` coverage for `SET ORDER ... DESCENDING` composed with command-path `SEEK` plus `SET NEAR`. Regression checks now lock down targeted descending miss positioning while preserving the selected SQL alias and pointer.
- synthetic SQL targeted-filter parity now has focused `SET FILTER ... IN` coverage composed with `GO ... IN` / `SKIP ... IN`. Regression checks now lock down filtered targeted-cursor visibility and EOF-edge behavior while preserving the currently selected SQL alias and record pointer.
- synthetic SQL navigation parity now has focused non-selected-target `IN` coverage for `GO`, `SKIP`, and composing `LOCATE`. Regression checks now lock down targeted SQL pointer movement (including EOF-edge positioning) while preserving the currently selected SQL alias and record pointer.
- synthetic SQL mutation command-family parity now has focused non-selected-target `IN` coverage for `REPLACE`, `DELETE FOR`, and `RECALL`. Regression checks now lock down selected-cursor preservation (alias/pointer unchanged) while targeted-cursor row updates, tombstone transitions, and pointer restoration behave as expected.
- synthetic SQL scan-flow parity now has focused `SCAN ... IN <alias|work area>` coverage. Regression checks now lock down non-selected-target scan behavior so the targeted SQL cursor iterates and lands just past end-of-file while the currently selected SQL cursor alias and pointer remain unchanged.
- command-path `SET ORDER` / `SEEK` now has focused `IN <alias|work area>` parity coverage for synthetic SQL cursors. Regression checks now lock down non-selected-target behavior so targeted SQL cursor ordering/seeking updates the requested cursor while preserving the currently selected cursor alias and pointer.
- `APPEND BLANK` now has first-pass `IN <alias|work area>` command-path targeting support. The runtime can append into a non-selected synthetic SQL result cursor without switching the current selection, while still advancing the targeted cursor pointer, and focused regression coverage now locks down that behavior alongside follow-on `SET ORDER`/`SEEK` checks.
- synthetic SQL mutation parity now has focused indexed-search follow-through coverage. After `APPEND BLANK` + `REPLACE`, command-path `SET ORDER TO <expr>` / `SEEK` now stays pinned to the in-memory SQL row set and can find the appended row values, with regression coverage locking down the post-mutation `RECNO()`/field lookup behavior.
- synthetic SQL result cursors now have a writable mutation slice in the native runtime. `REPLACE`, `APPEND BLANK`, `DELETE`, and `RECALL` can mutate the in-memory result rows opened by `SQLEXEC()`, including command-level targeting coverage (`FOR`/`WHILE`), with focused regression coverage across cursor-state and row-value behavior.
- `TOTAL` now has remote-cursor compatibility coverage. `TOTAL ... IN <sql-alias>` can aggregate grouped output DBFs from synthetic SQL result rows using the same visibility/scope machinery as local cursors, with focused regression coverage.
- selected-alias replacement now has focused parity coverage. Replacing the currently selected alias with `USE ... IN <selected-alias>` is now locked down so the old alias lookup clears, the new alias stays in the same work area, the replacement cursor resets to its first record, and stale active-order state does not leak through that in-place swap.
- local indexed-search now consumes a first grounded `FOR`-filter hint on loaded orders. `SEEK` now skips records filtered out by an order's extracted `FOR` expression for the shipped `DELETED() = .F./.T.` cases, with focused regression coverage proving filtered-out keys no longer match and `SET NEAR` positions to the next visible indexed row.
- synthetic SQL result cursors now have command-path indexed-search parity in addition to helper-function probes. `SET ORDER TO <expr>` can establish a temporary remote order expression such as `NAME`, and command-level `SEEK` honors that order plus `SET NEAR` miss positioning with focused regression coverage.
- synthetic SQL result cursors now have an indexed-search bridge for one-off probes. `SEEK()` and `INDEXSEEK()` can evaluate temporary order expressions such as `NAME` against in-memory SQL result rows, including `INDEXSEEK(.F./.T.)` pointer semantics plus `SET NEAR` miss positioning, with focused regression coverage.
- function-path indexed-search parity now accepts one-off order-direction suffixes in order designators. `SEEK()` and `INDEXSEEK()` now parse trailing `ASCENDING` / `DESCENDING` in the order-designator argument and route that override through the same temporary-order seek path used by command-level probes, with focused runtime coverage locking down descending miss positioning via `SEEK(..., ..., 'TAG DESCENDING')`-style calls while preserving the controlling order.
- the local indexed-search runtime now consumes grounded normalization hints on the seek path instead of only surfacing them as metadata. `SEEK` and `SEEK()` now case-fold search keys for orders backed by `UPPER(...)` metadata, including descending orders and one-off tag/order probes, with focused regression coverage locking down normalized matches across command and function entry points.
- indexed-search parity now includes first-pass order-direction control on the shipped local `SET ORDER`/`SEEK` path. `SET ORDER TO ... [ASCENDING|DESCENDING]` now preserves descending state on the active order, one-off `SEEK ... TAG/ORDER ... [ASCENDING|DESCENDING]` probes can temporarily override direction without changing the controlling order, and focused regression coverage now locks down descending exact-match plus `SET NEAR` miss positioning.
- expression-driven cursor targeting now reaches beyond `SELECT` and `USE`. Variable-driven alias/work-area designators now flow through the shipped local `IN`-targeted data/search command family, including `SET ORDER`, `SEEK`, `LOCATE`, `SCAN`, `GO`, `SKIP`, `REPLACE`, `DELETE`, and `RECALL`, with focused regression coverage keeping those commands pinned to the targeted non-selected cursor while preserving the current selection.
- `SET DEFAULT TO` now behaves as a data-session-local runtime setting instead of one global value. A fresh `SET DATASESSION` now starts from the startup working directory unless it changes its own default path, restoring the original session also restores its changed default directory, and focused runtime coverage now locks that down with `SET('DEFAULT')` and relative `FILE()` checks across session switches.
- continuation guidance is now consolidated around the tracked backlog docs plus the canonical `agent-handoff.md` used by `scripts/drive-codex.ps1`. Stale throwaway prompt files should no longer be treated as backlog sources once their still-useful notes have been folded back into the repo docs.
- the earlier runtime-safety refactor that extracted `prg_engine_helpers.{h,cpp}` and `prg_engine_command_helpers.{h,cpp}` is now part of the shipped baseline, so follow-on runtime work should keep using those seams instead of rebuilding equivalent helper logic inside the `prg_engine.cpp` monolith.
- expression-based work-area targeting is now more consistent across adjacent runtime commands. `SELECT <expr>` and `USE ... IN <expr>` now route through the shared cursor-designator expression helper already used by `SET FILTER ... IN`, and focused runtime coverage now locks down numeric-variable and alias-variable targeting for both selection and replacement flows.
- `PRIVATE` variable scoping and `STORE` command are now implemented. `PRIVATE` saves the caller's global value before creating a fresh slot, makes that slot visible to callees (since it lives in globals), and restores the saved value when the declaring frame pops via the new `pop_frame()` helper that replaces all `stack.pop_back()` call sites except the one inside `pop_frame()` itself. `STORE <expr> TO var1, var2, ...` evaluates the expression once and assigns to all listed targets. Three focused regression tests lock down PRIVATE masking, PRIVATE callée visibility, and STORE multi-target assignment.

## 2026-04-11

- `NDX` key-domain metadata now drives a first narrow runtime compare slice. The runtime preserves `NDX` key-domain hints on loaded orders and now uses numeric-domain ordering for `SEEK`/`SET NEAR` behavior when that grounded header metadata is available, while leaving broader `CDX/DCX/IDX` collation semantics untouched.
- single-index probe metadata now goes one step beyond expression-derived hints. `IDX` and `NDX` probes surface opaque header sort-marker hints from already-read header bytes, and `NDX` probes now expose a key-domain hint derived from the numeric/date header flag, while intentionally avoiding invented named-collation mappings or runtime compare changes.
- indexed metadata now carries first-pass normalization/collation hints from the shared probe layer into runtime order state. Expression-derived hints such as `UPPER(...)` now flow through `SET ORDER`, cursor snapshot/restore, and temporary `SEEK ... TAG` overrides, and focused runtime coverage now verifies those hints through `runtime.order` and `runtime.seek` event detail without changing indexed compare behavior yet.
- read-only real-fixture validation now reaches beyond the original `customer.cdx` smoke test. Optional coverage now exercises additional installed VFP `CDX` and `DCX` samples plus the local `CHNGREAS.NDX` fixture so the shared index probe has broader real-world sanity checks without requiring checked-in binaries.
- `CDX/DCX` inspection now preserves conservative per-tag page hints from directory leaf entries and prefers those hinted page neighborhoods when binding first-pass key/`FOR` expressions. Focused adversarial coverage now proves earlier stray printable expressions no longer steal direct-probe tag metadata.
- `MDX` inspection is now less heuristic-heavy. The read-only probe now enumerates first-pass tag hints from non-header 512-byte metadata regions instead of whole-file scavenging, rejects obviously implausible all-zero headers or hint-free files, and exercises that tighter behavior through both direct probe and companion inspection coverage.
- structured asset validation now extends into DBF field descriptors and record layout. Readable DBF-family assets can now report missing descriptor terminators, malformed descriptor spans, overlapping or overflowing field layouts, record-length mismatches, and duplicate or invalid field names without failing inspection or blocking Studio document open.
- structured asset validation now extends into memo sidecars. Readable DBF-family assets can now report malformed memo-sidecar headers, invalid block-size metadata, out-of-range memo block pointers, and truncated referenced memo payloads without failing inspection or blocking Studio document open.
- the native asset inspector now carries structured validation findings without failing otherwise-readable DBF-family assets. `inspect_asset()` can now report header/file-size inconsistencies, truncated record storage, missing memo or structural companion files, and malformed companion index files through additive validation metadata that also flows into the Studio document model.

## 2026-04-10

- the native PRG expression/runtime slice now covers first-pass `EVAL()`, `SET()`, and `&macro` substitution plus expression-driven `USE IN` close targeting. Stored expression strings can now run through the evaluator against the current record/runtime context, macro-expanded field/alias names work in expression paths, `SET()` can inspect current session/default-directory state, and focused runtime coverage now locks down restored `SET` state across data sessions.
- native PRG command coverage now includes a first-pass literal `TEXT/ENDTEXT` slice. The parser/runtime can capture multiline text blocks into variables with `TEXT TO ... [ADDITIVE] [NOSHOW]`, preserves literal block lines without stripping FoxPro comment markers inside the block, and locks that down with focused runtime regression coverage.
- synthetic SQL result cursor auto-allocation now follows each data session's current selected work-area flow instead of always forcing another `IN 0` allocation. Focused runtime coverage now locks down `SELECT 0` plus `SQLEXEC()` behavior across two data sessions so each session reuses its own selected empty work area and restores that placement after switching back.
- work-area allocation now reuses freed areas instead of monotonically climbing forever, and `SELECT(0)` now reports the next available work area without consuming it. Focused runtime coverage locks down `USE IN`, `SELECT(0)`, and subsequent `USE ... IN 0` reopening so alias/work-area selection stays closer to practical VFP behavior after closing a cursor.
- synthetic SQL result cursors now participate in a first-pass read-only local-cursor flow. The runtime can evaluate synthetic SQL fields during `SET FILTER`, `GO TOP`, `LOCATE`, aggregate built-ins, and `CALCULATE`, and focused regression coverage now locks down that filter/locate/aggregate behavior against `SQLEXEC()` result cursors.
- command-level `SEEK` now accepts one-off `TAG` / `ORDER` overrides for local DBF-backed cursors without requiring a prior `SET ORDER`, which closes a focused indexed-search parity gap and keeps the active controlling order unchanged after the probe.
- indexed runtime metadata now preserves the actual inspected index file identity for loaded local-table orders. `ORDER(alias, 1)` now reports the real companion index path instead of assuming `.cdx`, `TAG(indexFile, ...)` now accepts supported xBase index extensions beyond `.cdx`, and focused runtime coverage locks that down with a synthetic `.idx` companion.

## 2026-04-09

- `CDX/DCX` inspection now uses first-pass real directory leaf-page parsing for tag names instead of whole-file tag-name scavenging. The asset parser now lifts stored tag names from plausible compound-index directory pages, attaches first-pass page-local key and `FOR` expression hints to those tags, and validates the shape against both synthetic fixtures and the real VFP `customer.cdx` sample.
- the shared `CDX/DCX` parser path now has focused `.dcx` regression coverage. Synthetic direct-probe coverage now locks down `DCX` tag, key-expression, and `FOR`-expression extraction, and `inspect_asset()` now has a companion-collection regression for `DBC` plus same-base `.dcx`.
- `MDX` probing now moves past file-level structural recognition. The read-only probe now surfaces first-pass tag-name hints from printable metadata runs, and same-base companion inspection exercises those hints through `inspect_asset()` while deeper layout parsing remains open.
- `APPEND BLANK` now fails fast on unsupported field layouts instead of guessing blank bytes for binary storage it does not understand. The shared DBF layer now rejects tables carrying unsupported field types during blank-record initialization, focused DBF/runtime coverage locks down the unchanged-file error path, and that closes another silent-corruption seam while broader field-type fidelity remains open.
- the shared DBF mutation layer now covers first-pass FoxPro `Currency (Y)` and `DateTime (T)` field writes. `create_dbf_table_file()`, `replace_record_field_value()`, and `append_blank_record_to_file()` now persist `Y` values as signed 4-decimal scaled integers, persist `T` values through the existing `julian:<day> millis:<milliseconds>` storage contract, initialize blank appended fixed-width values to zero bytes, and round-trip that behavior through focused DBF coverage.
- the shared DBF mutation layer now covers first-pass FoxPro `Integer (I)` field writes. `create_dbf_table_file()`, `replace_record_field_value()`, and `append_blank_record_to_file()` can now persist `I` fields as 4-byte little-endian signed values, initialize blank appended integers to zero, and round-trip that behavior through focused DBF coverage.
- the shared DBF mutation layer now fails fast on indexed tables instead of silently diverging from companion structural indexes. `append_blank_record_to_file()`, `replace_record_field_value()`, and `set_record_deleted_flag()` now reject DBFs marked with production indexes or carrying same-base `.cdx` companions, and focused DBF/runtime coverage locks down the error path while true `CDX` write/rebuild support remains open.
- the shared DBF layer now has first-pass memo write fidelity for `M` fields. `create_dbf_table_file()` and `replace_record_field_value()` can now persist FoxPro-compatible memo sidecars, blank memo fields append with zero pointers instead of corrupted placeholders, and focused DBF round-trip coverage now exercises memo-backed create/update/append flows.
- `SET NEAR` now has focused data-session restoration coverage. Missed-`SEEK` behavior is locked down across `SET DATASESSION` switches so session-local nearest-record behavior stays independent and restores correctly after returning to the original session.
- `SQLCONNECT()` handle numbering now restarts per data session instead of sharing one global handle sequence. Combined with the prior session-scoped SQL handle storage, this keeps synthetic SQL connection lifecycles closer to the local cursor/session model and adds focused regression coverage for cross-session handle lookup and restored SQL cursor visibility.
- synthetic SQL result cursors now have stronger data-session isolation. SQL connection handles are now scoped per data session instead of globally, and focused runtime coverage now locks down cross-session `SQLEXEC`/`SQLDISCONNECT` isolation plus restored SQL cursor lookup after `SET DATASESSION` switches.

## 2026-04-08

- plain `USE <table>` now reuses the current selected work area instead of behaving like `IN 0`, so opening into an empty selected area and replacing the selected cursor now happen in place with focused runtime regression coverage.
- `USE ... IN <alias|work area>` now preserves the current selected work area when replacing a different non-selected local cursor, and focused runtime coverage now locks down both replacement and close flows for that alias/work-area edge case.
- aggregate command follow-through now includes first-pass `IN <alias|work area>` targeting for command-level `COUNT`, `SUM`, and `AVERAGE`, and `TOTAL` can now target a non-selected local DBF cursor the same way. The runtime preserves both the selected cursor position and the targeted cursor position with focused regression coverage.
- first-pass `TOTAL` now works for local DBF-backed cursors. The native PRG runtime can write grouped DBF totals with `TOTAL TO ... ON ... FIELDS ...` plus basic visibility-aware `REST`/`FOR` semantics, and the DBF layer now has focused coverage for creating output tables without corrupting FoxPro-compatible files.
- aggregate command follow-through now covers first-pass scope-clause and `WHILE` semantics for command-level `COUNT`, `SUM`, and `AVERAGE`. `ALL`, `REST`, `NEXT n`, and `RECORD n` now run through the native visibility-aware cursor engine, preserve the selected record position, and have focused regression coverage.
- command-level aggregate follow-through now exists in the native PRG runtime. First-pass `COUNT`, `SUM`, and `AVERAGE` commands can assign visibility-aware results into variables with `FOR`/`TO`, the expression parser now handles multiplicative arithmetic needed by multi-expression aggregates, and quoted `IN` alias targets now resolve cleanly across adjacent aggregate/search commands such as `USE`, `CALCULATE`, `LOCATE`, and `SCAN`.
- first-pass command-level aggregates now exist in the native PRG runtime. `CALCULATE` can assign `COUNT()`, `SUM()`, `AVG()`, `MIN()`, and `MAX()` results into variables, supports `TO`/`INTO`, optional `FOR`, optional `IN` alias targeting, and reuses the shipped visibility-aware aggregate semantics with focused regression coverage.
- first-pass aggregate built-ins now exist in the native PRG runtime for local DBF-backed cursors. `COUNT()`, `SUM()`, `AVG()/AVERAGE()`, `MIN()`, and `MAX()` now evaluate across visible rows, respect `SET FILTER TO/OFF`, honor `SET DELETED ON`, and can target a named alias with focused regression coverage.
- native PRG branching now covers first-pass `DO CASE/CASE/OTHERWISE/ENDCASE`, including nested case blocks and case-driven execution inside `SCAN`, with focused runtime regression coverage.
- native PRG control flow now covers first-pass `DO WHILE/ENDDO` plus shared `LOOP`/`CONTINUE`/`EXIT` behavior across `DO WHILE`, `FOR`, and `SCAN`, with focused regression coverage for nested loops and cursor-backed iteration.
- persistent cursor filtering now has a first native runtime slice. `SET FILTER TO/OFF` is now cursor-scoped for local DBF-backed work areas, and `GO TOP/BOTTOM`, `SKIP`, `LOCATE`, `SCAN`, and `DELETE/RECALL FOR` now honor active filter visibility with regression coverage.
- local-table mutation/query flow moved forward materially in the native PRG engine. `LOCATE`, `SCAN ... ENDSCAN`, `REPLACE`, `APPEND BLANK`, `DELETE`, and `RECALL` now work against local DBF-backed cursors with persisted table writes, current-record field resolution, and regression coverage in both the DBF layer and runtime layer.
- `CDX/DCX` inspection moved beyond header-only probing. Copperfin now enumerates first-pass tag/expression hints from real file contents and surfaces them through the asset inspector and CLI.
- `GO`, `GOTO`, and `SKIP` cursor navigation now have runtime coverage in the native `PRG` engine tests, which closes one more piece of work-area behavior before broader table/session semantics.
- `USED()`, `DBF()`, and `FCOUNT()` now participate in the native cursor/session model for both local tables and synthetic SQL result cursors, with explicit regression coverage for data-session isolation and `USE IN`.
- `SELECT` now fails cleanly on missing aliases instead of inventing new work areas, and `USE AGAIN` / duplicate-open rules now participate in the native runtime with regression coverage.
- `SET ORDER TO TAG`, `SEEK`, and `FOUND()` now work as a first indexed-lookup slice for local DBF-backed cursors, using companion index metadata and explicit runtime regression coverage.
- the official Learn language-reference index is now part of the working parity process, with a generated command inventory plus `FOXHELP.DBF` as a fallback source for older or missing command-behavior details.
- the installed VFP CHM files now have a generated local keyword-to-topic index, so command behavior can be mined offline from `dv_foxhelp.chm` and `foxtools.chm` without treating the CHMs as opaque binaries.
- `SET NEAR ON/OFF` now participates in indexed `SEEK` behavior for local DBF-backed cursors, which closes another documented VFP search-behavior slice and gives the runtime a clearer path beyond exact-match-only index probes.
- `SEEK()`, `INDEXSEEK()`, `ORDER()`, and `TAG()` now have first-pass runtime coverage grounded in the installed VFP help topics, which expands the indexed-search surface from command-only behavior into documented helper functions.
- `SET LIBRARY TO`, `FoxToolVer()`, `MainHwnd()`, `RegFn32()`, and `CallFn()` now have a first-pass FoxTools compatibility bridge, giving Copperfin an initial modeled path for old DLL-call workflows without destabilizing the host runtime.
- `SET EXACT` now participates in both string comparison semantics and indexed `SEEK` behavior, and that setting is now scoped per data session instead of being treated as one global runtime toggle.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#381` with a focused custom-`TEXTMERGE()` nested-macro proof plus a shared `EVAL()` fallback fix. `test_prg_engine_string_math_functions` now uses true second-hop holder chains for custom-delimiter merged text covering both `EVAL(&cNameExprDeepHolder)` and `&cFieldExprDeepHolder`, while `evaluate_runtime_surface_function()` now falls back to the last valid bare identifier when macro-expanded `EVAL()` input over-expands into final scalar text instead of an expression. Focused `test_prg_engine_string_math_functions`, focused `test_prg_engine_runtime_surface_functions`, and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#382` with focused proof-hardening for alternate-delimiter merged nested macros. `test_prg_engine_string_math_functions` now uses true second-hop holder chains for both `EVAL(&cNameExprDeepHolder)` and `&cFieldExprDeepHolder` inside the `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)` branch, proving the merged content path stays correct while the delimiters are also macro-expanded. Focused `test_prg_engine_string_math_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#383` with focused proof-hardening for recursive custom-delimiter merged macros. `test_prg_engine_string_math_functions` now routes the recursively inserted template text through `&cRecursiveCustomExprDeepHolder`, and that recursive template now uses `EVAL(&cNameExprDeepHolder)`, proving the recursive `TEXTMERGE()` branch preserves true second-hop macro indirection end to end. Focused `test_prg_engine_string_math_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#384` with focused proof-hardening for nested `EXECSCRIPT()` eval content. `test_prg_engine_string_math_functions` now drives the script body through `RETURN EVAL(&cScriptExprDeepHolder)` while the script source still resolves through `EXECSCRIPT(&cScriptDeepHolder)`, proving nested script-eval surfaces preserve true second-hop macro indirection. Focused `test_prg_engine_string_math_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#385` with focused proof-hardening for direct nested `EVAL()` chains plus a shared runtime fix. `test_prg_engine_string_math_functions` now uses `EVAL(EVAL(cEvalExprDeepHolder))`, and `evaluate_runtime_surface_function()` now preserves the last identifier in bounded direct-holder `EVAL()` chains so nested evaluation stops on the final expression identifier instead of one hop too early or too late. Focused `test_prg_engine_string_math_functions`, focused `test_prg_engine_runtime_surface_functions`, and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#386` with focused proof-hardening for command-path recursive merged text plus a shared runtime fix. `test_prg_engine_control_flow` now routes recursive `TEXT TO ... TEXTMERGE` content through `&cRecursiveExprDeepHolder`, and the command-path `TEXTMERGE` dispatcher now reapplies `<<...>>` interpolation to a bounded fixed point so recursively inserted merged text resolves like the string `TEXTMERGE(..., .T.)` surface. Focused `test_prg_engine_control_flow` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#387` with focused proof-hardening for the numeric `LOOKUP()` all-argument path. `test_prg_engine_runtime_surface_functions` now drives the typed numeric return-expression branch through `&cAgeExprDeepHolder`, `&cSearchExprDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`, closing the remaining first-hop-only arguments in that numeric lane. Focused `test_prg_engine_runtime_surface_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#388` with focused proof-hardening for the local string-return `LOOKUP()` all-argument path. `test_prg_engine_runtime_surface_functions` now drives the selected-cursor string-return branch through `&cReturnExprDeepHolder`, `&cSearchExprDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`, closing the remaining first-hop-only alias/tag arguments in that local lane. Focused `test_prg_engine_runtime_surface_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#389` with focused proof-hardening for the `LOOKUP()` miss-default alias/tag path. `test_prg_engine_runtime_surface_functions` now drives the miss-default branch through `&cNumberExprDeepHolder` / `&cTextExprDeepHolder`, `&cMissingSearchDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`, closing the remaining hardcoded alias/tag designators in that miss-preservation lane. Focused `test_prg_engine_runtime_surface_functions` and the full CTest suite pass.
- 2026-06-13: Macro/eval slice `#97` advanced across child issue `#390` with focused proof-hardening for the SQL/result-cursor `LOOKUP()` miss path. `test_prg_engine_runtime_surface_functions` now drives the SQL miss-preservation lane through `&cReturnExprDeepHolder`, `&cMissingSearchDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`, proving the all-argument second-hop miss path returns the typed numeric default. Focused `test_prg_engine_runtime_surface_functions` and the full CTest suite pass.
- 2026-06-29: E3/#2818 stable-selected report/label group-header selected-objects evidence shipped: the new focused `test_studio_host_selected_group_header_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted group-header object selection preserves selected-object metadata, seeded group-header containing-section ids, group-expression metadata, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2819 stable-selected report/label group-footer selected-objects evidence shipped: the new focused `test_studio_host_selected_group_footer_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted group-footer object selection preserves selected-object metadata, seeded group-footer containing-section ids, group-expression metadata, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2820 stable-selected report/label title selected-objects evidence shipped: the new focused `test_studio_host_selected_title_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted title object selection preserves selected-object metadata, seeded title containing-section ids, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2821 stable-selected report/label page-footer selected-objects evidence shipped: the new focused `test_studio_host_selected_page_footer_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted page-footer object selection preserves selected-object metadata, seeded page-footer containing-section ids, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2822 stable-selected report/label column-header selected-objects evidence shipped: the new focused `test_studio_host_selected_column_header_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted column-header object selection preserves selected-object metadata, seeded column-header containing-section ids, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2823 stable-selected report/label column-footer selected-objects evidence shipped: the new focused `test_studio_host_selected_column_footer_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted column-footer object selection preserves selected-object metadata, seeded column-footer containing-section ids, preview bounds, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2824 stable-selected report/label unplaced selected-objects evidence shipped: the new focused `test_studio_host_selected_unplaced_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted unplaced object selection preserves selected-object metadata without containing-section membership, preview bounds, deleted-preview accounting, object-count accounting, and null section/settings payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2825 stable-selected report/label page-header selected-objects evidence shipped: the new focused `test_studio_host_selected_page_header_objects_stable` host regression now proves FRX/LBX stable-selected live and deleted page-header object selection preserves selected-object metadata, synthesized page-header containing-section ids, preview/deleted-preview bounds, expression provenance, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2826 orphaned report/label page-header selected-object evidence shipped: the new focused `test_studio_host_selected_page_header_objects_orphaned` host regression now proves FRX/LBX live page-header object selections keep deleted-section membership when the containing page-header band is deleted, while preserving preview/deleted-preview bounds, section/object-count accounting, expression provenance, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2827 orphaned report/label detail selected-object evidence shipped: the new focused `test_studio_host_selected_detail_objects_orphaned` host regression now proves FRX/LBX live and deleted detail object selections keep deleted-section membership when the containing detail band is deleted, while preserving preview/deleted-preview bounds, section/object-count accounting, field and expression provenance, and null section/settings selection payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2828 record-selected nested group-section evidence shipped: the new focused `test_studio_host_selected_nested_group_sections_record` host regression now proves FRX/LBX record-selected live inner nested group-header selection and deleted inner nested group-footer selection preserve seeded nested group ids, grouping metadata, partner-section deleted-state propagation, preview/deleted-preview bounds, and null object/settings payloads for both reports and labels without changing host JSON field names or production behavior.
- 2026-06-29: E3/#2829 section deleted-object-count evidence shipped: the new focused `test_studio_host_section_deleted_object_counts` host regression now proves FRX/LBX report and label section/object context JSON preserves seeded section ids, per-section deleted-object counts, deleted-section containing-object metadata, and null containing-section payloads for deleted unplaced objects without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2862 deleted report/label host-contract alignment shipped: the broad `test_studio_host_json` driver now matches the current shared FRX/LBX host JSON contract for deleted placed objects and deleted sections, preserving containing-section metadata on deleted object selections, preserving deleted-section ownership for contained objects, and reflecting current live/deleted preview and object-count accounting without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2863 deleted report/label section-geometry evidence extraction shipped: the new focused `test_studio_host_deleted_section_geometry` target now carries the deleted FRX/LBX section height/top update and clear host JSON coverage that previously lived in `test_studio_host_json_sections_geometry.cpp`, preserving the current deleted-section preview, containing-section, and object-accounting contract while shrinking the broad `test_studio_host_json` geometry shard after the recent build-time refactors.
- 2026-06-30: E3/#2896 report-settings fixture extraction shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_settings_diagnostics_and_misc.cpp`; shared ambiguous-settings and stable settings+section FRX/LBX fixture builders now live in `tests/test_studio_host_json_report_settings_shared_fixtures.cpp`, keeping the report-settings diagnostics coverage in its focused executable while preserving the current host JSON contract and the dependent deleted-state, schema-fallback, and unresolved-memo report targets.
- 2026-06-30: E3/#2897 report-object diagnostics broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_objects_diagnostics_and_misc.cpp`, leaving report-layout provenance, extended object-kind, object deleted-state, subtree deleted-state, object schema-fallback, and unresolved object-memo coverage owned by the focused `test_studio_host_report_layout_diagnostics` and `test_studio_host_report_deleted_states` executables without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2898 geometry preview-bounds broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_geometry_preview_bounds.cpp`, instead resolving its shared preview-bound expectations through `tests/test_studio_host_json_geometry_preview_helpers.cpp` while the focused report width/left, height/top, and detail-header/footer section preview-bound executables remain the owners of the geometry preview test bodies without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2899 report-section diagnostics broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_sections_diagnostics_and_misc.cpp`; the ambiguous stable section/settings selector coverage now lives in the focused `test_studio_host_report_section_selection_diagnostics` executable, while the broad suite resolves the remaining live section fixture dependency through `tests/test_studio_host_json_report_section_shared_fixtures.cpp` and the existing schema-fallback and unresolved-memo section diagnostics executables keep their current ownership without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2900 live report-section geometry broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_sections_geometry.cpp`, with the live report/label section height/top record-selection and stable-selection coverage now owned by the focused `test_studio_host_report_live_section_geometry` executable while reusing the existing report layout, geometry preview, and shared section-fixture helpers without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2901 report column-setup broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_settings_column_setup.cpp`, with report/label column-setup exposure and stable/deleted `COLS` host JSON mutation coverage now owned by the focused `test_studio_host_report_column_setup` executable while the existing `test_studio_host_report_direct_setting_fields` executable keeps the invalid direct column-setup field lane without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2902 report page-setup field broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_settings_page_layout_fields.cpp`, with report/label bottom-margin, grid, orientation, and paper-size stable/deleted host JSON mutation coverage now owned by the focused `test_studio_host_report_page_setup_fields` executable while the existing `test_studio_host_report_direct_setting_fields` executable keeps the invalid direct margin/grid field lane without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2903 report column-width field broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_geometry_column_width.cpp`, with report/label stable/deleted `COLWIDTH` host JSON mutation coverage now owned by the focused `test_studio_host_report_column_width_fields` executable while the existing record-selection lane remains in `test_studio_host_column_width_record_settings`, without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2904 label layout action broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_report_labels.cpp`, with label-layout parity and stable-selector nudge/align/resize/snap/delete/restore/distribute/reorder/duplicate/rename host JSON coverage now owned by the focused `test_studio_host_label_layout_actions` executable while reusing the existing shared FRX/LBX layout fixture builders and preview-bound helpers, without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2905 report layout action broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_objects_layout_and_expressions.cpp`, with report-layout stable-selector nudge/align/resize/snap/delete/restore/distribute/reorder/duplicate/rename coverage plus report object expression mutation/clear and edited-deleted restore coverage now owned by the focused `test_studio_host_report_layout_actions` executable while reusing the existing shared FRX/LBX layout fixture builders and preview-bound helpers, without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2906 report font-metadata broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_geometry_font_metadata.cpp`, with report-layout object font metadata/options coverage and detail-header/footer object font metadata/options coverage now owned by the focused `test_studio_host_report_font_metadata` executable while reusing the existing shared FRX/LBX layout fixtures, preview-bound helpers, and detail-header/footer object fixture providers, without changing host JSON field names or production behavior.
- 2026-06-30: E3/#2907 report layout placement broad-suite retirement shipped: the broad `test_studio_host_json` target no longer compiles `tests/test_studio_host_json_sections_move_and_placement.cpp`, with report-layout unplaced-to-section and section-to-unplaced record/stable host JSON coverage now owned by the focused `test_studio_host_report_layout_placement` executable while reusing the existing shared FRX/LBX layout fixtures and preview-bound helpers, without changing host JSON field names or production behavior.
- 2026-07-01: Durable shell-direction guidance was tightened in `agent-handoff.md`: future sessions should treat the VSIX `Command` window as a default bottom-tabbed tool window alongside Visual Studio 202x terminal/output-style panes, the standalone host as a modern dockable shell with dockable `Command` and `Terminal` panes, Windows standalone UX as the Visual Studio-grade reference point, and macOS/Linux shells as native-platform experiences guided by Apple and VS Code-class Linux conventions; no new backlog issue was needed because `#3050`, `#3051`, and `#2998` already cover that scope.
- 2026-07-02: E3/#3210 shared real-asset nudge preview-metadata hardening shipped: the real `by_author.FRX` and `STYLELBL.LBX` nudge smokes now capture source live preview bounds before opening the editor, assert the expected host-backed preview geometry after nudge and after full undo-driven restore, compare section continuity from the section model plus stable `UNIQUEID` identity instead of brittle list text/raw snapshot titles, and drain the follow-up undo entries that a two-axis nudge creates until original geometry is fully restored. The real-asset smoke toolchain resolver now also prefers the repo-local `build2` hosts ahead of `build` when no explicit host path is configured so focused Mono probes and local designer smokes do not silently bind to stale local binaries.
- 2026-07-04: Runtime pipeline/#3510 shipped on the `#110` lane with case-folded same-base companion staging for packaged assets. `materialize_runtime_package(...)` now resolves companion files case-insensitively before copying them, so uppercase or mixed-case real-world companions such as `.SCT`, `.PJT`, `.VCT`, `.FRT`, `.LBT`, `.MNT`, `.FPT`, and `.CDX` no longer get dropped or renamed during runtime packaging on case-sensitive filesystems. Focused `test_runtime_pipeline` coverage now proves uppercase xAsset and DBF companion staging preserves the resolved source casing in packaged output without changing manifest field names or other machine-readable package contracts.
- 2026-07-04: Runtime pipeline/#3511 shipped on the `#110` lane with focused startup-family package-contract proof for `MNX/MNT`, `FRX/FRT`, and `LBX/LBT`. `test_runtime_pipeline` now proves menu, report, and label startup assets stage their required sidecars even when marked excluded, preserve source-side startup provenance in `app.cfdebug`, and keep the existing xAsset breakpoint/step-debugging flags and copied-asset manifest contract stable without changing machine-readable manifest keys or debug fields.
- 2026-07-04: Native runtime/#3545 shipped on the `#3217` lane with first-pass built-in `BackColor` parity for headless native visual surfaces. The iterative PRG runtime now seeds a deterministic white `BackColor` default for native visual objects outside the OLE host path until per-class VFP defaults are modeled more precisely, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down direct and contained-object reads, writes, `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and declarative overrides without changing machine-readable runtime identifiers.
- 2026-07-04: Native runtime/#3546 shipped on the `#3217` lane with first-pass built-in `ForeColor` parity for headless native visual surfaces. The iterative PRG runtime now seeds a deterministic black `ForeColor` default for native visual objects outside the OLE host path until per-class VFP defaults are modeled more precisely, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down direct and contained-object reads, writes, `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and declarative overrides without changing machine-readable runtime identifiers.
- 2026-07-04: Native runtime/#3547 shipped on the `#3217` lane with first-pass built-in `ControlSource` parity for representative bound-entry controls. The iterative PRG runtime now seeds an empty-string `ControlSource` default for native `TextBox`, `ComboBox`, `EditBox`, `CheckBox`, and `Spinner` objects, blocks `ADDPROPERTY()` / `REMOVEPROPERTY()` shadowing on that built-in surface, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down direct and contained-object reads, writes, `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and declarative overrides without changing machine-readable runtime identifiers.
- 2026-07-04: Native runtime/#3548 shipped on the `#3217` lane with first-pass built-in `RowSource` parity for `ComboBox` and `ListBox`. The iterative PRG runtime now seeds an empty-string `RowSource` default for those controls, blocks `ADDPROPERTY()` / `REMOVEPROPERTY()` shadowing on that built-in surface, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down direct and contained-object reads, writes, `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and declarative overrides without changing machine-readable runtime identifiers.
- 2026-07-04: Native runtime/#3549 shipped on the `#3217` lane with first-pass built-in `RowSourceType` parity for `ComboBox` and `ListBox`. The iterative PRG runtime now seeds a deterministic `RowSourceType = 0` default for those controls, blocks `ADDPROPERTY()` / `REMOVEPROPERTY()` shadowing on that built-in surface, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down direct and contained-object reads, writes, `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` reflection, and declarative overrides without changing machine-readable runtime identifiers.
