Codex is currently working on the visual editor. Should I let it finish before tasking it on the critical-path items, or should I tell it to redirect its efforts to the critical path (or something in-between)?
This is worth thinking through carefully rather than just deferring. Here's the key tension:

Case for redirecting now:
The planning docs are explicit that #151 → #152 → #153 are a safety gate — the documented rule is "only then move to a new slice under #93 or #94." Codex is currently skipping that gate entirely. Runtime safety/diagnostics slices exist to protect everything downstream; leaving them open while doing designer work is a sequencing risk, not just a scheduling preference.

Also, the visual editor property work is maximally interruptible. Each commit is a single independent property setter (set_visual_object_X) with its own test. There's virtually no cross-slice context to lose — Codex can resume the property sweep exactly where it left off with zero ramp-up cost.

Case for letting it finish:
The visual editor slices are pure mechanical scaffolding — low risk, and Codex is clearly in a productive rhythm on them. If it's mid-slice on a specific open issue, finishing that one unit is cheaper than interrupting mid-work.

My recommendation: redirect, but cleanly.

Tell Codex to finish whatever individual slice it's currently executing (don't yank it mid-commit), then redirect to #151. The visual editor queue will still be there — it's not going anywhere, and the individual slices are trivially resumable. The runtime safety gate, on the other hand, is explicitly blocking the rest of the critical path per your own planning docs.

The middle ground to avoid: letting Codex "finish the current property sweep" is likely many more slices — the queue goes deep — and that framing lets the designer work crowd out the critical path indefinitely.