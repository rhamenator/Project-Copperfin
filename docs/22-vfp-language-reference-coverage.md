# VFP Language Reference Coverage

This document anchors Copperfin's command/runtime backlog to the official Visual FoxPro reference instead of memory.

## Primary Sources

- Microsoft Learn language index:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/dd5f4hyy(v=vs.71)>
- Microsoft Learn general reference:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/h9f898ae(v=vs.71)>
- Microsoft Learn VFP home page:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/mt490117(v=msdn.10)>
- Local fallback when Learn coverage is incomplete or too terse:
  - [`C:\vDosPlus\FPD26\FOXHELP.DBF`](C:/vDosPlus/FPD26/FOXHELP.DBF)
  - companion memo file in the same directory

## Generated Inventory

The generated command inventory lives here:

- [`docs/generated/vfp-language-reference-summary.json`](E:/Project-Copperfin/docs/generated/vfp-language-reference-summary.json)
- [`docs/generated/vfp-language-reference-commands.json`](E:/Project-Copperfin/docs/generated/vfp-language-reference-commands.json)
- [`docs/generated/vfp-language-reference-commands.txt`](E:/Project-Copperfin/docs/generated/vfp-language-reference-commands.txt)
- [`docs/generated/vfp-chm-index-summary.json`](E:/Project-Copperfin/docs/generated/vfp-chm-index-summary.json)
- [`docs/generated/vfp-chm-command-topics.json`](E:/Project-Copperfin/docs/generated/vfp-chm-command-topics.json)
- [`docs/generated/vfp-foxtools-topics.json`](E:/Project-Copperfin/docs/generated/vfp-foxtools-topics.json)
- [`docs/generated/vfp-chm-topic-manifest.json`](E:/Project-Copperfin/docs/generated/vfp-chm-topic-manifest.json)

Refresh it with:

```powershell
powershell -ExecutionPolicy Bypass -File E:\Project-Copperfin\scripts\export-vfp-reference-index.ps1
powershell -ExecutionPolicy Bypass -File E:\Project-Copperfin\scripts\export-vfp-chm-topic-index.ps1
```

Current official index snapshot from the Learn language-reference page:

- `429` command entries
- `413` function entries
- `323` property entries
- `83` method entries
- `72` system variable entries
- `69` event entries
- `22` object entries
- `4` preprocessor-directive entries
- `3` operator entries

Current local CHM snapshot from the installed VFP help set:

- `dv_foxhelp.chm` is the main product help and the command-behavior source that matters most
- `foxtools.chm` is supplemental and much smaller
- the local indexer now emits both a command-topic view and a broader topic manifest, so it can capture `foxtools` functions that are not indexed as `Command` topics
- the generated CHM outputs give local keyword-to-HTML mappings for offline behavior lookup, including `RegFn()`, `RegFn32()`, and `CallFn()`

## Current Copperfin Runtime Surface

The native runtime/parser currently has first-pass support for these command families:

- Runtime-host packaging/security notes — security-enabled package startup now preserves recorded extension-payload relative-path fidelity during digest verification, so same-named files at the package root cannot satisfy nested payload hashes outside the recorded package location
- Native-wrapper scaffold notes — generated DLL/FLL native-wrapper scaffolds now derive sibling `app.cfmanifest` and `copperfin_runtime_host(.exe)` paths from the loaded module location, emit shared `CopperfinRuntimeBridgeDescriptor`, `CopperfinRuntimeBridgeInvocation`, `CopperfinRuntimeBridgeParameter`, `CopperfinRuntimeBridgeCall`, `CopperfinRuntimeBridgeReturn`, `CopperfinRuntimeBridgeResult`, `CopperfinRuntimeBridgeEnvironmentVariable`, `CopperfinRuntimeBridgeLaunchPlan`, `CopperfinRuntimeBridgeObservationPlan`, `CopperfinRuntimeBridgeExecutionPlan`, `CopperfinRuntimeBridgeTransportPlan`, `CopperfinRuntimeBridgeSerializationPlan`, `CopperfinRuntimeBridgeDispatchPlan`, `CopperfinRuntimeBridgeDispatchExecution`, `CopperfinRuntimeBridgeProcessLaunch`, `CopperfinRuntimeBridgeHostFailureEvaluation`, `CopperfinRuntimeBridgeMissingResponseEvaluation`, `CopperfinRuntimeBridgeResponseValidationEvaluation`, `CopperfinRuntimeBridgePayloadPlan`, `CopperfinRuntimeBridgeInterpretationPlan`, `CopperfinRuntimeBridgeFailurePolicyPlan`, `CopperfinRuntimeBridgeResponseValidationPlan`, `CopperfinRuntimeBridgeRequestArtifact`, `CopperfinRuntimeBridgeRequestWritePlan`, `CopperfinRuntimeBridgeResponseReadPlan`, `CopperfinRuntimeBridgeResponseArtifact`, `CopperfinRuntimeBridgeResponseParsePlan`, `CopperfinRuntimeBridgeResponseParseAdmission`, `CopperfinRuntimeBridgeParsedResponse`, `CopperfinRuntimeBridgeInterpretedResultPlan`, `CopperfinRuntimeBridgeInterpretedResultAdmission`, `CopperfinRuntimeBridgeInterpretedResult`, `CopperfinRuntimeBridgeNativeReturnPlan`, `CopperfinRuntimeBridgeNativeReturnAdmission`, `CopperfinRuntimeBridgeNativeReturn`, `CopperfinRuntimeBridgeOutcomeSelectionPlan`, `CopperfinRuntimeBridgeOutcomeSelectionAdmission`, `CopperfinRuntimeBridgeOutcomeSelection`, `CopperfinRuntimeBridgeReturnMaterializationPlan`, `CopperfinRuntimeBridgeReturnMaterializationAdmission`, `CopperfinRuntimeBridgeReturnMaterialization`, `CopperfinRuntimeBridgeReturnEmissionPlan`, `CopperfinRuntimeBridgeReturnEmissionAdmission`, `CopperfinRuntimeBridgeReturnEmission`, `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, `CopperfinRuntimeBridgeFinalReturnAdoptionAdmission`, `CopperfinRuntimeBridgeFinalReturnAdoption`, `CopperfinRuntimeBridgeReturnActivationPlan`, `CopperfinRuntimeBridgeReturnActivationAdmission`, `CopperfinRuntimeBridgeReturnActivation`, `CopperfinRuntimeBridgeStubReturnPlan`, `CopperfinRuntimeBridgeStubReturnAdmission`, `CopperfinRuntimeBridgeStubReturn`, `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, `CopperfinRuntimeBridgePlaceholderReturnValueAdmission`, `CopperfinRuntimeBridgePlaceholderReturnValue`, `CopperfinRuntimeBridgePlaceholderReturnIntAdmission`, and placeholder-return-int helper surfaces that capture exported routine/source/parameter metadata plus explicit future runtime-host argv, argument-binding, return-binding, working-directory, environment, stdout/stderr log-path, expected-exit-code, executable-path, output-capture, request/response artifact, request/response encoding, final dispatch-argv, payload-shape, return-interpretation, host-failure-policy, response-validation, request-artifact, request-write, response-read, response-artifact, response-parse, response-parse admission, parsed-response, interpreted-result, interpreted-result admission, native-return, native-return admission, outcome-selection, outcome-selection admission, return-materialization, return-materialization admission, return-emission, return-emission admission, final-return-adoption, final-return-adoption admission, return-activation, return-activation admission, stub-return, stub-return admission, placeholder-return-value, placeholder-return-value admission, placeholder-return-int, and placeholder-return-int admission contracts over those package-local bridge paths; the serialization layer now emits the current request/response media-type contracts plus the current schema-version contract through shared helpers instead of repeating those raw inline tokens, the dispatch layer now also emits the current request-path, response-path, request-media-type, response-media-type, and schema-version argv flag contracts through shared helpers instead of repeating those raw inline tokens, the dispatch execution layer now also emits a shared `copperfin_runtime_bridge_execute_dispatch(...)` helper plus a narrow dispatch-execution surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and expected-exit-code contracts flow through one scaffold-only helper before payload construction instead of remaining implicit inside the nested dispatch plan, the process-launch layer now also emits a shared `copperfin_runtime_bridge_launch_process(...)` helper plus a narrow process-launch surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and deterministic launch outcome contracts flow through one scaffold-only helper before payload construction instead of remaining implicit around the dispatch-execution seam, the host-failure evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_host_failure(...)` helper plus a narrow host-failure evaluation surface so staged process-launch output and failure-policy contracts flow through one scaffold-only helper before response validation instead of remaining implicit around the failure-policy seam, the missing-response evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_missing_response(...)` helper plus a narrow missing-response evaluation surface so staged host-failure output and response-read policy contracts flow through one scaffold-only helper before response artifact handling instead of remaining implicit around the response-read seam, the response-validation evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_response_validation(...)` helper plus a narrow response-validation evaluation surface so staged missing-response output and response-validation contracts flow through one scaffold-only helper before response artifact handling instead of remaining implicit around the response-validation seam, the response-parse admission layer now also emits a shared `copperfin_runtime_bridge_admit_response_parse(...)` helper plus a narrow response-parse admission surface so staged response-validation output and response-parse contracts flow through one scaffold-only helper before interpreted-result planning instead of remaining implicit around the response-parse admission seam, the interpreted-result admission layer now also emits a shared `copperfin_runtime_bridge_admit_interpreted_result(...)` helper plus a narrow interpreted-result admission surface so staged response-parse admission output and interpreted-result contracts flow through one scaffold-only helper before native-return planning instead of remaining implicit around the interpreted-result admission seam, the native-return admission layer now also emits a shared `copperfin_runtime_bridge_admit_native_return(...)` helper plus a narrow native-return admission surface so staged interpreted-result admission output and native-return contracts flow through one scaffold-only helper before outcome-selection planning instead of remaining implicit around the native-return admission seam, the outcome-selection admission layer now also emits a shared `copperfin_runtime_bridge_admit_outcome_selection(...)` helper plus a narrow outcome-selection admission surface so staged native-return admission output and outcome-selection contracts flow through one scaffold-only helper before return-materialization planning instead of remaining implicit around the outcome-selection admission seam, the return-materialization admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_materialization(...)` helper plus a narrow return-materialization admission surface so staged outcome-selection admission output and return-materialization contracts flow through one scaffold-only helper before return-emission planning instead of remaining implicit around the return-materialization admission seam, the return-emission admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_emission(...)` helper plus a narrow return-emission admission surface so staged return-materialization admission output and return-emission contracts flow through one scaffold-only helper before final-return-adoption planning instead of remaining implicit around the return-emission admission seam, the final-return-adoption admission layer now also emits a shared `copperfin_runtime_bridge_admit_final_return_adoption(...)` helper plus a narrow final-return-adoption admission surface so staged return-emission admission output and final-return-adoption contracts flow through one scaffold-only helper before return-activation planning instead of remaining implicit around the final-return-adoption admission seam, the return-activation admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_activation(...)` helper plus a narrow return-activation admission surface so staged final-return-adoption admission output and return-activation contracts flow through one scaffold-only helper before stub-return planning instead of remaining implicit around the return-activation admission seam, the stub-return admission layer now also emits a shared `copperfin_runtime_bridge_admit_stub_return(...)` helper plus a narrow stub-return admission surface so staged return-activation admission output and stub-return contracts flow through one scaffold-only helper before placeholder-return-value planning instead of remaining implicit around the stub-return admission seam, the placeholder-return-value admission layer now also emits a shared `copperfin_runtime_bridge_admit_placeholder_return_value(...)` helper plus a narrow placeholder-return-value admission surface so staged stub-return admission output and placeholder-return-value contracts flow through one scaffold-only helper before placeholder-return-int routing instead of remaining implicit around the placeholder-return-value admission seam, the placeholder-return-int admission layer now also emits a shared `copperfin_runtime_bridge_admit_placeholder_return_int(...)` helper plus a narrow placeholder-return-int admission surface so staged placeholder-return-value admission output and placeholder-return-int contracts flow through one scaffold-only helper before final stub emission instead of remaining implicit around the placeholder-return-int admission seam, the request-write, response-read, and response-parse layers now also emit the current write-mode, read-mode, and parser-kind policy contracts through shared helpers instead of repeating those raw inline tokens, the request-write layer now also emits a shared `copperfin_runtime_bridge_execute_write_request(...)` helper so staged request-document writes run through one executable helper path that creates the parent directory when required and writes `plan.request_artifact.request_document` at the planned request artifact path instead of leaving request-write execution implicit, the response-read layer now also emits a shared `copperfin_runtime_bridge_execute_read_response(...)` helper so staged response-document reads run through one executable helper path that falls back to the shared empty-response contract when the staged artifact is missing or unreadable and otherwise streams the response payload from `plan.source_path` instead of leaving response-read execution implicit, the response-parse layer now also emits a shared `copperfin_runtime_bridge_execute_parse_response(...)` helper plus a narrow parsed-response surface so staged response documents run through one scaffold-only field-extraction path for the planned `status`, `return_value`, and `diagnostics` fields instead of leaving response-parse execution implicit, the interpreted-result layer now also emits a shared `copperfin_runtime_bridge_execute_interpreted_result(...)` helper plus a narrow interpreted-result surface so parsed response status can flow through one scaffold-only success/fallback return-value selection path before native-return materialization instead of leaving interpreted-result execution implicit, the native-return layer now also emits a shared `copperfin_runtime_bridge_execute_native_return(...)` helper plus a narrow native-return surface so interpreted success/fallback selection can flow through one scaffold-only native return-value/int-value selection path before outcome selection instead of leaving native-return execution implicit, the outcome-selection layer now also emits a shared `copperfin_runtime_bridge_execute_outcome_selection(...)` helper plus a narrow outcome-selection surface so native-return success/fallback selection can flow through one scaffold-only selected-condition path before return materialization instead of leaving outcome-selection execution implicit, the return-materialization layer now also emits a shared `copperfin_runtime_bridge_execute_return_materialization(...)` helper plus a narrow return-materialization surface so outcome-selection metadata can flow through one scaffold-only success/fallback native return-statement materialization path before return emission instead of leaving return-materialization execution implicit, the return-emission layer now also emits a shared `copperfin_runtime_bridge_execute_return_emission(...)` helper plus a narrow return-emission surface so return-materialization metadata can flow through one scaffold-only emitted-branch-block path before final-return adoption instead of leaving return-emission execution implicit, the final-return-adoption layer now also emits a shared `copperfin_runtime_bridge_execute_final_return_adoption(...)` helper plus a narrow final-return-adoption surface so return-emission metadata can flow through one scaffold-only adopted-return-block path before return activation instead of leaving final-return-adoption execution implicit, the return-activation layer now also emits a shared `copperfin_runtime_bridge_execute_return_activation(...)` helper plus a narrow return-activation surface so final-return-adoption metadata can flow through one scaffold-only active/deferred return-selection path before stub-return handling instead of leaving return-activation execution implicit, the stub-return layer now also emits a shared `copperfin_runtime_bridge_execute_stub_return(...)` helper plus a narrow stub-return surface so return-activation metadata can flow through one scaffold-only placeholder/deferred stub-return path before placeholder-return-value handling instead of leaving stub-return execution implicit, the placeholder-return-value layer now also emits a shared `copperfin_runtime_bridge_execute_placeholder_return_value(...)` helper plus a narrow placeholder-return-value surface so stub-return metadata can flow through one scaffold-only placeholder-return contract before final DLL/FLL stub emission instead of leaving placeholder-return-value execution implicit, the final stub-emission layer now also emits a shared `copperfin_runtime_bridge_execute_placeholder_return_int(...)` helper so final placeholder integer selection flows through one scaffold-only helper before DLL direct return or FLL `_RetInt(...)` surface application instead of staying inline inside the output-specific stub emitters, the shared parsed-int helper layer now also emits the current default integer sentinel through a shared helper instead of repeating the raw inline `-1` token inside the parser, the response-artifact layer now also emits the current empty response-document token through a shared helper instead of repeating the raw inline empty-string literal, the payload layer now emits the current request/response shape contracts plus the current request/response field-name contracts through shared helpers instead of repeating those raw inline tokens, the payload and interpretation layers now emit the current response `status`, `return_value`, and `diagnostics` field-name contracts through shared helpers instead of repeating those raw inline tokens across multiple builders, the failure-policy layer now records non-zero-exit handling, missing-response handling, a deterministic `runtime_host_failure` diagnostics fallback, and the placeholder return fallback value for each stub, and it now emits that diagnostics fallback through a shared helper instead of repeating the raw token inline, the response-validation layer records the expected response media type, schema version, required response fields, and the success-status token expected by the wrapper scaffold before it would accept a host response, and it now emits that success-status token through a shared helper instead of repeating the raw token inline, the request-artifact layer records the serialized request document that would be written to the planned request-path artifact, the request-write layer records the concrete target request path plus overwrite policy that would be used before launch, the response-read layer records the concrete staged response path plus text-read policy that would be used after launch, the response-artifact layer records the raw staged response document payload before any parsing or interpretation, the response-parse layer records the parser kind plus status/value/diagnostics field extraction contract that would be applied to that raw document, the interpreted-result layer records the combined success-status, wrapper-return-surface, and fallback-return contract that would drive the eventual native return path, the native-return layer now also records typed success and fallback integer values alongside the success/fallback native value representations plus final DLL/FLL return surface that would eventually materialize from that interpreted result, reusing `copperfin_parse_runtime_bridge_int_value_representation(...)` to derive those typed values from the existing success/fallback representation metadata instead of hardcoding them again, the return-materialization layer now also emits a shared native-`int` return-surface helper so the current plain-`int` branch no longer repeats that raw inline token, a shared native-`int` placeholder-signature helper so typed `_RetInt(int)`-style matching no longer repeats the raw inline `"(int)"` token, and a shared native return-statement framing helper so the plain-`int`, typed placeholder, and representation fallback branches now all reuse one `return ...;` contract instead of repeating the raw framing tokens before the function falls back to representation-based materialization for any non-`int` surfaces, the outcome-selection layer records the explicit success/fallback branch conditions plus diagnostics field that would govern which native return path is chosen, the return-materialization layer now routes the current `int` / `_RetInt(int)` success/fallback statements through those parsed typed integer values while preserving the existing representation-based fallback path for any non-`int` surfaces, the return-emission layer now records an explicit fallback else-branch statement and composes the emitted return block from the prebuilt success/fallback branch statements instead of re-materializing the fallback return inline, the final-return-adoption layer records how that emitted return block would replace the current placeholder native return in the generated wrapper stub, the return-activation layer now also seeds the future `active_return_block` from that adopted return block while keeping activation disabled so the scaffold still emits the placeholder return today, the stub-return layer now routes its inactive `deferred_return_block` through `return_activation_plan.active_return_block` instead of reaching back to final-adoption metadata, explicitly carries the placeholder fallback integer/value metadata needed by the current helper path, flattens the upstream `activation_mode` / `adoption_mode` contracts for downstream consumption, derives explicit placeholder-helper active/replacement policy booleans from that upstream metadata, and now also emits shared placeholder return-binding and placeholder return-statement helpers so exported stubs, failure-policy fallback metadata, and final-return adoption all consume one shared placeholder return contract instead of repeating raw inline `-1` binding and return-statement literals, while the placeholder-return-value layer carries those activation/adoption mode contracts plus the derived helper-policy booleans alongside the deferred return block, placeholder-emission flag, emitted placeholder return statement, and fallback value metadata so the current helper consumes the full plan-level activation/deferred/emitted/helper-policy contract instead of reconstructing the same policy from string comparisons in the helper body, and the host-toolchain compile check matches the emitted CMake visibility policy so wrapper build checks track only the deliberate callable/data export surface
- Native-wrapper stub-emission admission notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_admit_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionAdmission` surface so staged placeholder-return-int admission output and final stub-emission contracts flow through one scaffold-only helper before output-specific stub emission instead of remaining implicit around the final stub-emission seam
- Native-wrapper stub-emission execution notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_execute_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmission` surface so staged placeholder-return-value execution and placeholder-return-int execution flow through one scaffold-only helper before DLL/FLL output-specific stub emission instead of remaining implicit across the final stub-emission execution seam
- Native-wrapper stub-emission return-surface notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionReturnSurface` surface so shared stub-emission execution output flows through one scaffold-only helper before DLL/FLL output-specific stub return application instead of remaining implicit across the final stub-emission return-surface seam
- Native-wrapper stub-emission output-application notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_apply_stub_emission_output(...)` helper so shared stub-emission return-surface output flows through one scaffold-only helper before the final DLL/FLL return adapter is applied instead of remaining implicit inside the output-specific stub emitters
- Native-wrapper stub-emission emitter notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_emit_stub_return_shared(...)` helper so the formerly duplicated DLL/FLL `copperfin_runtime_bridge_emit_stub_return(...)` bodies now flow through one scaffold-only emitter helper before output-specific wrapper forwarding instead of remaining duplicated across the two output-specific emitters
- Native-wrapper stub-emission wrapper notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `CopperfinRuntimeBridgeStubEmissionWrapper` surface plus `copperfin_runtime_bridge_build_stub_emission_wrapper(...)` so the remaining output-specific `copperfin_runtime_bridge_emit_stub_return(...)` bodies now only construct one wrapper contract before handing off to the shared stub-emission emitter helper instead of forwarding raw return-surface and adapter inputs directly
- Native-wrapper stub-emission wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the placeholder-return-value plan and then route placeholder returns straight through `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping separate output-specific stub-return wrapper helpers
- Native-wrapper stub-return wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeStubReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the stub-return plan and then build placeholder-return-value plans straight from the enriched stub-return plan instead of passing the wrapper separately at the placeholder-plan seam
- Native-wrapper return-activation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnActivationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-activation plan and then build stub-return plans straight from the enriched activation plan instead of passing the wrapper separately at the stub-return-plan seam
- Native-wrapper final-return-adoption wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the final-return-adoption plan and then build return-activation plans straight from the enriched final-return-adoption plan instead of passing the wrapper separately at the return-activation-plan seam
- Native-wrapper return-emission wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnEmissionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-emission plan and then build final-return-adoption plans straight from the enriched return-emission plan instead of passing the wrapper separately at the final-return-adoption-plan seam
- Native-wrapper return-materialization wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnMaterializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-materialization plan and then build return-emission plans straight from the enriched return-materialization plan instead of passing the wrapper separately at the return-emission-plan seam
- Native-wrapper outcome-selection wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeOutcomeSelectionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the outcome-selection plan and then build return-materialization plans straight from the enriched outcome-selection plan instead of passing the wrapper separately at the return-materialization-plan seam
- Native-wrapper native-return wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeNativeReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the native-return plan and then build outcome-selection plans straight from the enriched native-return plan instead of passing the wrapper separately at the outcome-selection-plan seam
- Native-wrapper interpreted-result wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretedResultPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpreted-result plan and then build native-return plans straight from the enriched interpreted-result plan instead of passing the wrapper separately at the native-return-plan seam
- Native-wrapper response-parse wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseParsePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-parse plan and then build interpreted-result plans straight from the enriched response-parse plan instead of passing the wrapper separately at the interpreted-result-plan seam
- Native-wrapper response-artifact wrapper notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the response artifact and then build response-parse plans straight from the enriched response artifact instead of passing the wrapper separately at the response-parse-plan seam
- Native-wrapper response-read wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseReadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-read plan and then build response artifacts straight from the enriched response-read plan instead of passing the wrapper separately at the response-artifact seam
- Native-wrapper request-write wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestWritePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the request-write plan and then build response-read plans straight from the enriched request-write plan instead of passing the wrapper separately at the response-read-plan seam
- Native-wrapper request-artifact wrapper notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the request artifact and then build request-write plans straight from the enriched request artifact instead of passing the wrapper separately at the request-write-plan seam
- Native-wrapper bridge response-contract expectation notes — generated DLL/FLL native-wrapper request JSON now carries expected response media type plus a `response_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::response_fields`, so runtime-host bridge consumers can produce and validate the matching response envelope from the same wrapper request document
- Native-wrapper response-document validation-routing notes — generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_response_validation(...)`, and `CopperfinRuntimeBridgeResponseValidationEvaluation` records response-document availability from the response body so response-contract enforcement has the actual bridge payload at the validation seam
- Native-wrapper required response-field validation notes — generated DLL/FLL native-wrapper response validation now checks `response_document` for every field declared by `CopperfinRuntimeBridgeResponseValidationPlan::required_response_fields` and routes to the existing fallback return path when any required response field is absent
- Native-wrapper response media-type validation notes — generated DLL/FLL native-wrapper response validation now extracts the response document's `response_media_type` field and compares it with `CopperfinRuntimeBridgeResponseValidationPlan::expected_response_media_type`, routing to the existing fallback return path when the field is absent or mismatched
- Native-wrapper response schema-version validation notes — generated DLL/FLL native-wrapper response-field contracts now include `schema_version`, and response validation compares the response document's schema-version field with `CopperfinRuntimeBridgeResponseValidationPlan::expected_schema_version`, routing to the existing fallback return path when the field is absent or mismatched
- Native-wrapper runtime-host dispatch execution notes — generated DLL/FLL native-wrapper launch helpers now quote and build a runtime-host command line from `CopperfinRuntimeBridgeDispatchExecution`, execute it through `std::system(...)`, capture the observed exit code, and mark launch success only when that exit code matches the expected exit code instead of returning deterministic launch failure
- Native-wrapper launch-environment dispatch notes — generated DLL/FLL native-wrapper dispatch execution now carries `CopperfinRuntimeBridgeLaunchPlan::environment` into command construction, and runtime-host command lines now apply those environment assignments before launching the host
- Native-wrapper missing-response document-evaluation notes — generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_missing_response(...)`, and missing-response policy treats the response as missing only when an existing response is required and the read document is empty
- Native-wrapper bridge request-field serialization notes — generated DLL/FLL native-wrapper request JSON now emits a `request_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::request_fields`, so runtime-host bridge consumers can inspect the exact request contract carried by the wrapper request document
- Native-wrapper bridge request-field contract notes — generated DLL/FLL native-wrapper `CopperfinRuntimeBridgePayloadPlan::request_fields` now declares the same descriptor metadata fields serialized by the bridge request document, so the request-field contract stays synchronized with routine kind, source provenance, parameter declaration style, parameter-name, schema-version, export, arity, media-type, and parameter payload fields
- Studio builder/wizard registry notes — Studio now exposes a context-aware VFP-compatible builder and wizard registry for form, class, control, report, label, project, and data-environment designer surfaces, preserving `builder.app`, `ReportBuilder.app`, and `Wizards` equivalence metadata while giving modern designer shells stable Copperfin entry-point tokens to filter relevant actions by context.
- Studio toolbox palette notes — Studio now exposes a VFP-compatible toolbox palette registry for common form, class, container, and report designer controls, including VFP class/baseclass metadata, default object-name prefixes, container flags, and context filtering so modern shells can drive toolbox creation without hardcoded UI-only control lists.
- Studio context-editor action notes — Studio now exposes selection-context action metadata for property-grid, method-editor, expression-editor, builder, toolbox, data-environment, and project-navigation surfaces so designer hosts can choose relevant editor commands for visual objects, report expressions, project items, and data-environment selections without hardcoded UI conditionals.
- Studio designer-context aggregation notes — Studio now exposes a single active-selection context query that combines the context-aware editor action registry, builder/wizard registry, and toolbox palette registry so hosts can populate designer panes and commands from one filtered model for visual objects, report expressions, project items, and data-environment selections.
- Studio opened-document designer-context notes — opened Studio document models now carry default designer-context aggregates derived from asset kind, so form/class, report/label, project, and table/database-container assets expose relevant editor actions, builder/wizard entries, and toolbox entries directly from `open_document`.
- Studio host designer-context JSON notes — `copperfin_studio_host --json` now serializes opened-document designer contexts with stable selection-context tokens, editor action descriptors, builder/wizard descriptors, and toolbox item descriptors so external Studio and Visual Studio shells can consume the same context-aware model as in-process hosts.
- Studio host selection-context override notes — Studio launch requests now accept repeatable `--selection-context` tokens so external hosts can request active visual-object, visual-method, report-expression, project-item, or data-environment context surfaces explicitly instead of relying only on asset-kind defaults.
- Studio host method-symbol inference notes — form/class launches with method-like symbols such as `cmdSave.Click` now infer the visual-method designer context automatically when no explicit selection-context override is present, aligning default context-aware editor output with Visual Studio method navigation.
- Studio launch-selection metadata notes — opened Studio documents and Studio host JSON now preserve the requested selection symbol, line, column, and record index so designer hosts can correlate active context output with the Visual Studio navigation target.
- Studio DataEnvironment symbol inference notes — form/class launches targeting DataEnvironment symbols such as `Dataenvironment.OpenTables` now infer the data-environment designer context before generic visual-method fallback, keeping data-environment actions and builders visible for those navigation targets.
- Studio designer context count notes — designer-context results and Studio host JSON now expose editor-action, builder, and toolbox-item counts derived from the filtered context descriptor vectors.
- Studio visual context builder notes — visual-object designer contexts now expose the VFP-compatible form builder alongside existing control/grid builders, while visual-method contexts keep the narrower control-builder set.
- Studio label context notes — LBX/LBT documents now use an explicit `label_expression` designer context with shared expression editing, report-safe toolbox items, and label-wizard builder metadata instead of inheriting report-builder metadata.
- Studio class context notes — VCX/VCT documents now use an explicit `class_designer` designer context with property-grid, method-editor, class-safe toolbox items, and class-builder metadata instead of inheriting form/control builder metadata from the generic visual-object context.
- Studio menu context notes — MNX/MNT documents now use an explicit `menu_item` designer context with property-grid actions and menu-designer builder metadata, while reporting zero toolbox items and excluding unrelated form/report builder metadata.
- Studio container context notes — Studio launch requests can now target an explicit `container_object` designer context that exposes property-grid, method-editor, control/grid builder metadata, and container-safe toolbox items without inheriting top-level form/class/report/menu/project/data-environment builders.
- Studio selected-container inference notes — form/class launches with a selected Container, PageFrame, Page, Grid, or Column record now infer the `container_object` designer context when no explicit selection-context override is present, keeping nested container editing on container-safe builders and toolbox entries.
- Studio report/label DataEnvironment inference notes — DataEnvironment symbols and selected DataEnvironment records now infer the `data_environment` designer context for report and label assets as well as form/class assets, while expression-context defaults remain unchanged for ordinary FRX/LBX launches.
- Studio toolbox descriptor creation notes — Studio now resolves toolbox palette descriptors into rollback-safe VFP visual-object create requests through `create_visual_object_from_toolbox_item`, including default object-name generation, descriptor class/baseclass mapping, optional identity/parent metadata, caller-provided field values, and no-mutation failure for unknown toolbox item ids.
- Studio host toolbox-create JSON notes — `copperfin_studio_host` now exposes toolbox descriptor creation through `--toolbox-create` plus optional identity, parent, and repeatable field-value arguments, returning deterministic JSON result metadata for external designer shells.
- Studio toolbox creation context-filter notes — toolbox descriptor creation now accepts an optional toolbox context and rejects context-incompatible controls, such as form-only TextBox creation in report contexts, without mutating the asset; the Studio host exposes this through `--toolbox-context`.
- Visual object undo affected-count notes — visual property undo now surfaces an affected-object count through generic edit results, with missing-history failures reporting zero affected objects.
- Visual object base property-edit affected-count notes — base single and multi-property visual object edit APIs now surface affected-object counts through generic edit results, with failed requests reporting zero affected objects.
- Visual object structural-command affected-count notes — visual object reparent, rename, reorder, deleted-state, and subtree deleted-state commands now surface affected-object counts through generic edit results for designer tree operations, with failed requests reporting zero affected objects.
- Visual object property-command affected-count notes — visual property clear, copy, move, rename, and reorder commands now surface affected-object counts through generic edit results for single and batch property-grid operations, with failed requests reporting zero affected objects.
- Visual object method-command affected-count notes — visual method update, delete, rename, copy, move, and reorder commands now surface affected-object counts through generic edit results for single and batch code-pane operations, with failed requests reporting zero affected objects.
- Visual object helper-backed property affected-count notes — shared logical and non-negative numeric designer property command coverage now proves generic edit-result affected-object counts, with failed empty-selection requests reporting zero affected objects.
- Visual object text-property affected-count notes — additional text designer property commands now surface affected-object counts through generic edit results, with failed text property requests reporting zero affected objects.
- Visual object scalar-property affected-count notes — representative logical and text scalar designer property commands now surface affected-object counts through generic edit results, with failed scalar property requests reporting zero affected objects.
- Visual object tab-command affected-count notes — tab-order and tab-stop designer commands now surface affected-object counts through generic edit results, with failed tab command requests reporting zero affected objects.
- Visual object geometry-command affected-count notes — alignment, resize, distribution, snap-to-grid, and nudge operations now surface affected-object counts through generic edit results, with failed geometry commands reporting zero affected objects.
- Visual object batch-edit affected-count notes — rollback-safe batch visual object edits now report the number of edited objects through generic edit results, while failed or empty batch edit requests report zero affected objects.
- Visual object ungroup-result container identity notes — ungrouping now reports the selected container's resolved object name, unique id, and parent name alongside target-parent and child-count metadata, while failed ungroup requests return empty container identity metadata.
- Visual object group-result container identity notes — grouping now reports the created container's resolved object name, unique id, and parent name while preserving grouped-child-count reporting, and failed grouping requests return empty container identity metadata.
- Visual object subtree-duplicate-result root identity notes — selected-object subtree duplication now reports the appended copied root's resolved object name, unique id, and parent name while preserving copied-count reporting, and failed subtree-duplicate requests return empty root identity metadata.
- Visual object batch-duplicate-result identity notes — batch selected-object duplication now reports each appended duplicate's record index, resolved object name, unique id, and parent name in append order, while failed batch-duplicate requests return empty identity metadata.
- Visual object duplicate-result identity notes — selected-object duplication now reports the appended duplicate's resolved object name, unique id, and parent name directly from the duplicated visual asset record, while failed duplicate requests return empty identity metadata.
- Visual object batch-create-result identity notes — batch toolbox create operations now report each appended object's record index, resolved object name, unique id, and parent name in append order, while failed batch-create requests return empty identity metadata.
- Visual object create-result identity notes — toolbox create operations now report the appended object's resolved object name, unique id, and parent name directly from the created visual asset record, while failed create requests return empty identity metadata.
- Studio selected-record DataEnvironment inference notes — form/class launches with `--record` targeting parsed DataEnvironment object metadata now infer the data-environment designer context when no explicit override is present, while non-DataEnvironment selected records keep visual-object defaults.
- Studio selected-object JSON notes — Studio host JSON now emits a compact `selectedObject` summary for explicit record selections that match parsed designer objects, including direct selected-object property snapshots with stable `name`, `type`, `isNull`, and `value` fields, giving external hosts direct access to selected record/object identity and property-grid data without scanning the full object array.
- Studio object-list JSON notes — Studio host JSON now emits parsed designer-object identity fields directly on each `document.objects[]` entry, so object browsers can consume `objectName`, `uniqueId`, `parentName`, `className`, and `baseclassName` without reparsing the property list.
- Studio object hierarchy JSON notes — Studio object snapshots now derive direct child counts from parsed object-name/parent-name relationships, and Studio host JSON emits `childCount` for selected-object summaries and full object-list entries so designer tree panes can show hierarchy affordances without rescanning all objects.
- Studio object parent-link JSON notes — Studio object snapshots now resolve nullable parent record links from parsed object-name/parent-name relationships, and Studio host JSON emits `parentRecordIndex` for selected-object summaries and full object-list entries so designer tree panes can attach child nodes without resolving parent names themselves.
- Studio object-path JSON notes — Studio object snapshots now derive dot-separated object paths from parsed object names and resolved parent record links, and Studio host JSON emits `objectPath` for selected-object summaries and full object-list entries so designer tree/search panes can identify nested objects without rebuilding parent chains.
- Studio object-code metadata JSON notes — Studio host JSON now emits raw parsed object code metadata (`objectTypeCode`, `objectCode`, and `platform`) for selected-object summaries and full object-list entries so designer panes can distinguish object families without scanning DBF property rows.
- Studio selected-object deletion-state JSON notes — Studio host JSON now emits the parsed `deleted` boolean directly on non-null selected-object summaries, matching the full object-list contract so hosts can render selected deleted records without consulting `document.objects[]`.
- Studio property provenance JSON notes — Studio host JSON now emits property provenance (`fieldIndex`, `memoBlockNumber`, `derivedFromPropertyBlob`, and nullable `sourceLineIndex`) for selected-object and full object-list properties so property grids can locate the backing DBF field or serialized property source without reparsing asset records.
- Studio object property-count JSON notes — Studio host JSON now emits direct `propertyCount` values for selected-object summaries and full object-list entries so designer/property-grid panes can size or lazy-load rows without scanning the emitted property arrays first.
- Studio document object-count JSON notes — Studio host JSON now emits document-level `objectCount` and `deletedObjectCount` values derived from the emitted object snapshot vector so designer panes can size object browsers without scanning `document.objects[]` first.
- Studio document root-object-count JSON notes — Studio host JSON now emits document-level `rootObjectCount` from parsed object snapshots with no resolved parent record link so designer tree panes can size top-level nodes without scanning `document.objects[]` first.
- Studio selected-object availability JSON notes — Studio host JSON now emits document-level `selectedObjectAvailable`, set only when an explicit selected record resolves to a parsed object snapshot, so hosts can distinguish unmatched selected records from real selected objects without relying only on `selectedObject: null`.
- Studio object child-record-link JSON notes — Studio object snapshots now carry direct child record-index arrays, and Studio host JSON emits `childRecordIndexes` for selected-object summaries and full object-list entries so designer tree panes can expand nodes without scanning all objects for matching parent links.
- Studio document root-record-link JSON notes — Studio host JSON now emits document-level `rootRecordIndexes` from parsed object snapshots with no resolved parent record link so designer tree panes can seed top-level nodes without scanning `document.objects[]` first.
- Studio document leaf-record-link JSON notes — Studio host JSON now emits document-level `leafObjectCount` and `leafRecordIndexes` from parsed object snapshots with no child record links so designer tree panes can identify terminal nodes without scanning `document.objects[]` first.
- Studio object tree-depth JSON notes — Studio object snapshots now derive hierarchy depth from resolved parent record links, and Studio host JSON emits per-object `objectDepth` plus document-level `maxObjectDepth` so designer tree panes can indent and virtualize hierarchy rows without rebuilding parent chains.
- Studio object sibling-order JSON notes — Studio object snapshots now derive sibling index/count metadata from resolved parent record links, and Studio host JSON emits per-object `siblingIndex` and `siblingCount` so designer tree panes can navigate stable object ordering without regrouping client-side.
- Studio object ancestor-link JSON notes — Studio object snapshots now derive root-to-parent ancestor record links from resolved parent chains, and Studio host JSON emits per-object `ancestorRecordIndexes` so designer tree panes and context-aware editors can expand parent chains without reparsing paths or walking links client-side.
- Visual editor outline hierarchy notes — `VisualObjectSnapshot` now exposes resolved parent-record availability/index plus direct child counts across object, child, descendant, and ancestor listing APIs so designer object browsers can render hierarchy affordances without issuing extra per-row hierarchy calls.
- Visual editor outline path/order notes — `VisualObjectSnapshot` now exposes object path, hierarchy depth, ancestor record links, sibling index, and sibling count across object, child, descendant, and ancestor listing APIs so designer object browsers can render stable tree rows from one listing call.
- Visual editor outline count notes — `VisualObjectSnapshot` now exposes property and method counts, derived through the existing property and method parsers, so property grids and method browsers can size affordances from outline rows before loading per-object detail panes.
- Visual object grouping result notes — `VisualObjectGroupResult` now reports the number of selected objects reparented into the new container, while failed grouping paths continue to return zero and preserve rollback behavior.
- Visual object ungrouping result notes — `VisualObjectUngroupResult` now reports the target parent name and resolved parent-record metadata after successful ungroup operations, while root-level and failed ungroup paths report empty/unavailable parent metadata.
- Native-wrapper bridge-request descriptor metadata notes — generated DLL/FLL native-wrapper request JSON now serializes routine kind, source provenance, parameter declaration style, parameter-name list, and schema version beside the existing export name, arity, request media type, and parameter value payloads, so runtime-host bridge requests carry the same descriptor metadata already present in wrapper argv/env scaffolding
- Native-wrapper exported-argument consumption notes — generated DLL/FLL native-wrapper stubs now consume DLL integer parameters and FLL `ParamBlk* parm` through `copperfin_build_runtime_bridge_call(...)` parameter payloads without emitting false `(void)` unused markers for those exported arguments
- Native-wrapper request-write execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_write_request(request_write_plan)` immediately after constructing the request-write plan and pass the execution result into response-read planning, so request documents are materialized through the shared helper while failed request writes force deterministic empty-response fallback behavior
- Native-wrapper request-write execution consumption notes — generated DLL/FLL native-wrapper stubs now consume `request_write_execution` through `copperfin_build_runtime_bridge_response_read_plan(...)`; the response-read plan now carries `request_write_succeeded` and returns the shared empty response document when request writing fails
- Native-wrapper response-read execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_read_response(response_read_plan)` immediately after constructing the response-read plan and pass the resulting response document into `CopperfinRuntimeBridgeResponseArtifact` instead of seeding response artifacts from the empty-response fallback
- Native-wrapper dispatch-execution consumption notes — generated DLL/FLL native-wrapper stubs now consume `dispatch_execution` through `copperfin_runtime_bridge_launch_process(...)` without emitting a false `(void)dispatch_execution` unused marker
- Native-wrapper process-launch consumption notes — generated DLL/FLL native-wrapper stubs now consume `process_launch` through `copperfin_runtime_bridge_evaluate_host_failure(...)` without emitting a false `(void)process_launch` unused marker
- Native-wrapper host-failure evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `host_failure` through `copperfin_runtime_bridge_evaluate_missing_response(...)` without emitting a false `(void)host_failure` unused marker
- Native-wrapper missing-response evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `missing_response` through `copperfin_runtime_bridge_evaluate_response_validation(...)` without emitting a false `(void)missing_response` unused marker
- Native-wrapper response-validation evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `response_validation_evaluation` through `copperfin_runtime_bridge_admit_response_parse(...)` without emitting a false `(void)response_validation_evaluation` unused marker
- Native-wrapper response-parse execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_parse_response(response_parse_plan)` after response-parse admission and pass the parsed response into `CopperfinRuntimeBridgeInterpretedResultPlan` so parsed status, return-value, and diagnostics values flow forward through interpretation instead of parse execution staying hidden inside the interpreted-result helper
- Native-wrapper response-parse admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `response_parse_admission` through `copperfin_runtime_bridge_admit_interpreted_result(...)` without emitting a false `(void)response_parse_admission` unused marker
- Native-wrapper interpreted-result execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_interpreted_result(interpreted_result_plan)` after interpreted-result admission and pass the interpreted success/fallback result into `CopperfinRuntimeBridgeNativeReturnPlan` so native-return planning consumes explicit interpretation output instead of invoking interpretation internally
- Native-wrapper interpreted-result admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `interpreted_result_admission` through `copperfin_runtime_bridge_admit_native_return(...)` without emitting a false `(void)interpreted_result_admission` unused marker
- Native-wrapper native-return execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_native_return(native_return_plan)` after native-return admission and pass the selected native return metadata into `CopperfinRuntimeBridgeOutcomeSelectionPlan` so outcome selection consumes explicit native-return output instead of invoking native-return execution internally
- Native-wrapper native-return admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `native_return_admission` through `copperfin_runtime_bridge_admit_outcome_selection(...)` without emitting a false `(void)native_return_admission` unused marker
- Native-wrapper outcome-selection execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_outcome_selection(outcome_selection_plan)` after outcome-selection admission and pass the selected condition/diagnostics/native return metadata into `CopperfinRuntimeBridgeReturnMaterializationPlan` so return materialization consumes explicit outcome-selection output instead of invoking outcome-selection execution internally
- Native-wrapper outcome-selection admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `outcome_selection_admission` through `copperfin_runtime_bridge_admit_return_materialization(...)` without emitting a false `(void)outcome_selection_admission` unused marker
- Native-wrapper return-materialization execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_materialization(return_materialization_plan)` after return-materialization admission and pass the materialized return branch metadata into `CopperfinRuntimeBridgeReturnEmissionPlan` so return emission consumes explicit materialized-return output instead of invoking return-materialization execution internally
- Native-wrapper return-materialization admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_materialization_admission` through `copperfin_runtime_bridge_admit_return_emission(...)` without emitting a false `(void)return_materialization_admission` unused marker
- Native-wrapper return-emission execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_emission(return_emission_plan)` after return-emission admission and pass the emitted return block metadata into `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` so final-return adoption consumes explicit return-emission output instead of invoking return-emission execution internally
- Native-wrapper return-emission admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_emission_admission` through `copperfin_runtime_bridge_admit_final_return_adoption(...)` without emitting a false `(void)return_emission_admission` unused marker
- Native-wrapper final-return-adoption execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_final_return_adoption(final_return_adoption_plan)` after final-return-adoption admission and pass the adopted return metadata into `CopperfinRuntimeBridgeReturnActivationPlan` so return activation consumes explicit final-return-adoption output instead of invoking final-return-adoption execution internally
- Native-wrapper final-return-adoption admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `final_return_adoption_admission` through `copperfin_runtime_bridge_admit_return_activation(...)` without emitting a false `(void)final_return_adoption_admission` unused marker
- Native-wrapper return-activation execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_activation(return_activation_plan)` after return-activation admission and pass the activated return metadata into `CopperfinRuntimeBridgeStubReturnPlan` so stub-return handling consumes explicit return-activation output instead of invoking return-activation execution internally
- Native-wrapper return-activation admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_activation_admission` through `copperfin_runtime_bridge_admit_stub_return(...)` without emitting a false `(void)return_activation_admission` unused marker
- Native-wrapper stub-return execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_return(stub_return_plan)` after stub-return admission and pass the routed stub-return metadata into `CopperfinRuntimeBridgePlaceholderReturnValuePlan` so placeholder-return-value planning consumes explicit stub-return output instead of invoking stub-return execution internally
- Native-wrapper stub-return admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `stub_return_admission` through `copperfin_runtime_bridge_admit_placeholder_return_value(...)` without emitting a false `(void)stub_return_admission` unused marker
- Native-wrapper placeholder-return-value execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_value(placeholder_return_value_plan)` after placeholder-return-value admission and pass the placeholder return metadata into shared stub-emission execution so stub emission consumes explicit placeholder-return-value output instead of invoking placeholder-return-value execution internally
- Native-wrapper placeholder-return-value admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `placeholder_return_value_admission` through `copperfin_runtime_bridge_admit_placeholder_return_int(...)` without emitting a false `(void)placeholder_return_value_admission` unused marker
- Native-wrapper placeholder-return-int admission-driven execution notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_int_admission)` after placeholder-return-int admission and pass the explicit integer into stub emission, so placeholder-return-int output consumes the admitted int surface instead of re-reading placeholder-return-value metadata
- Native-wrapper stub-emission admission-to-execution notes — generated DLL/FLL native-wrapper stubs now pass the explicit placeholder-return integer into `copperfin_runtime_bridge_admit_stub_emission(placeholder_return_int_admission, placeholder_return_int)` and execute `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` from that admitted surface, so stub-emission execution consumes admission output instead of discarding it
- Native-wrapper stub-emission execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` after stub-emission admission and pass the emitted stub metadata into return-surface construction so stub-emission return handling consumes explicit stub-emission output instead of invoking stub-emission execution internally
- Native-wrapper stub-emission return-surface call-site notes — generated DLL/FLL native-wrapper stubs now build `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` after stub-emission execution and pass the explicit return-surface metadata into direct output application so final output handling consumes explicit return-surface output instead of constructing it internally
- Native-wrapper stub-emission output-application call-site notes — generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_apply_stub_emission_output(stub_emission_return_surface, ...)` after return-surface construction with the planned return adapter, so final DLL/FLL return application is visible at the generated wrapper call-site instead of hidden inside a shared stub-return emitter
- Native-wrapper descriptor wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDescriptor`, so exported DLL/FLL stubs now build that wrapper once before constructing the descriptor and then build invocations straight from the enriched descriptor instead of passing the wrapper separately at the invocation seam
- Native-wrapper invocation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInvocation`, so exported DLL/FLL stubs now build that wrapper once before constructing the invocation and then build calls straight from the enriched invocation instead of passing the wrapper separately at the call seam
- Native-wrapper call wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeCall`, so exported DLL/FLL stubs now build that wrapper once before constructing the call and then build results straight from the enriched call instead of passing the wrapper separately at the result seam
- Native-wrapper result wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResult`, so exported DLL/FLL stubs now build that wrapper once before constructing the result and then build launch plans straight from the enriched result instead of passing the wrapper separately at the launch seam
- Native-wrapper launch wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeLaunchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the launch plan and then build observation plans straight from the enriched launch plan instead of passing the wrapper separately at the observation seam
- Native-wrapper observation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeObservationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the observation plan and then build execution plans straight from the enriched observation plan instead of passing the wrapper separately at the execution seam
- Native-wrapper execution wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeExecutionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the execution plan and then build transport plans straight from the enriched execution plan instead of passing the wrapper separately at the transport seam
- Native-wrapper transport wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeTransportPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the transport plan and then build serialization plans straight from the enriched transport plan instead of passing the wrapper separately at the serialization seam
- Native-wrapper serialization wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeSerializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the serialization plan and then build dispatch plans straight from the enriched serialization plan instead of passing the wrapper separately at the dispatch seam
- Native-wrapper dispatch wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDispatchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the dispatch plan and then build payload plans straight from the enriched dispatch plan instead of passing the wrapper separately at the payload seam
- Native-wrapper payload wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePayloadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the payload plan and then build interpretation plans straight from the enriched payload plan instead of passing the wrapper separately at the interpretation seam
- Native-wrapper interpretation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpretation plan and then build failure-policy plans straight from the enriched interpretation plan instead of passing the wrapper separately at the failure-policy seam
- Native-wrapper failure-policy wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFailurePolicyPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the failure-policy plan and then build response-validation plans straight from the enriched failure-policy plan instead of passing the wrapper separately at the response-validation seam
- Native-wrapper response-validation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseValidationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-validation plan and then build request artifacts straight from the enriched response-validation plan instead of passing the wrapper separately at the request-artifact seam
- Runtime-host AI/MCP policy notes — AI-assisted federation planning now requires the native `ai.mcp` role permission through `COPPERFIN_SECURITY_ROLE` (defaulting to `developer`), so optional AI planner requests do not bypass the existing security profile
- FLL registration notes — generated `FoxInfo` metadata now also carries pipe-separated parameter-name metadata (`tcMode`, `tnLeft|tnRight`, and so on) beside routine kind, source provenance, and arity in the honest wrapper scaffold
- Exported routine metadata notes — DLL/FLL callable contracts now also preserve whether each exported routine used `PARAMETERS` or `LPARAMETERS`, and the FLL `FoxInfo` scaffold carries that declaration-style metadata too
- FLL broad-manifest notes — the runtime/debug manifests now mirror the existing FLL per-function callable metadata, and the debug manifest now also records the generated `.def` / `.fll.api` artifact paths, compiler-contract digests, library-output feature flags, extension-payload digests, staged `asset=` inventory lines, `warning=` diagnostics, the `configuration=` contract, the `project_title=` contract, the `project_path=` contract, the `package_root=` contract, the `content_root=` contract, the `ast_manifest_path=` contract, the `ir_manifest_path=` contract, the `transpiled_csharp_path=` contract, the `security_enabled=` / `security_role=` / `security_mode=` / `audit_log_path=` contract, the `runtime_host_sha256=` / `security_roles=` contract, the `.NET` policy summary contract (`dotnet_enabled=`, `dotnet_story=`, allowlist/denylist summaries and items, parity-matrix summaries and items, and gateway decisions), the language/AI/extensibility summary contract (`language_integration_count=`, repeated `language_integration=` lines, `ai_feature_count=`, repeated `ai_feature=` lines, `extensibility_guardrail_count=`, repeated `extensibility_guardrail=` lines, and aggregate `language_integrations=` / `ai_features=` lines), the remaining runtime `feature_flag=` contract lines (launcher, host-compatibility, build-output, debug, and security flags), discovered routine export symbols, plus the requested/materialized primary-output state, so downstream tooling does not need to parse only the dedicated `.fll.api` contract to recover routine-level contract detail, artifact locations, integrity digests, capability flags, packaged-output digests, staged file inventory, warning diagnostics, build configuration, project identity, project location, package location, package content location, compiler-artifact locations, transpilation-artifact location, security metadata, integrity/security summary, extensibility-policy summary, language/AI summary, feature-rollout summary, export surface, or output-state transitions
- `DO`, `DO FORM`, `REPORT FORM`, `LABEL FORM` — command-target coverage now also includes true second-hop macro-expanded routine dispatch such as `DO &cProcDeepHolder`, focused call-binding coverage now also includes true second-hop macro-expanded `WITH` arguments such as `DO testparam WITH &cParamExprDeepHolder`, shared assignment-target coverage now includes true second-hop `&target = value` forms such as `&cTargetDeepHolder = 'MACROASSIGN'`, and focused expression coverage now also includes true second-hop macro-expanded array subscripts and function arguments such as `aData[&cRowExprDeepHolder]` and `LEN(&cFieldExprDeepHolder)`
- `CALL` — first-pass command-path parity now includes `CALL <target> WITH ...` argument binding and external `.prg` target resolution when local routines are not found, including BYREF `@var` write-back through existing call-reference plumbing
- `ACTIVATE POPUP`, `ACTIVATE MENU`, `RELEASE POPUP`, `RELEASE MENU`
- `RETURN`
- `IF/ELSE/ENDIF`
- `DO CASE/CASE/OTHERWISE/ENDCASE`
- `FOR/ENDFOR`
- `DO WHILE/ENDDO`
- `WITH/ENDWITH`
- `TRY/CATCH/FINALLY/ENDTRY`
- `LOOP`, `CONTINUE`, `EXIT`
- `SPAWN`, `AWAIT`, `YIELD`, `ENTER CRITICAL`, `EXIT CRITICAL` — first-pass task/coordination surface with spawned worker completion, cooperative yield, and named in-memory critical sections; the current critical-section policy is now centralized in `docs/25-engine-concurrency-policy.md`, explicitly requires ascending normalized section-name order for nested acquisitions, and rejects blocking wait surfaces while a critical section is held (`AWAIT`, positive-duration `SLEEP`, and lock-retry/backoff under contention, including explicit contested `RLOCK()`), emitting deterministic runtime diagnostics instead of deadlock-prone hangs
- `TEXT/ENDTEXT` with first-pass literal `TEXT TO ... [ADDITIVE] [NOSHOW]` capture, including `TEXT TO ... TEXTMERGE` interpolation coverage for nested `EVAL()` and `&macro` expressions inside merged `<<...>>` segments, including true second-hop command-path forms such as `<<EVAL(&cNameExprDeepHolder)>>` and `<<&cFieldExprDeepHolder>>`, plus custom-delimiter `TEXTMERGE()` recursion over stored expression text, macro-expanded delimiter arguments, and macro-expanded source-template text, including true second-hop source-template forms such as `TEXTMERGE(&cTemplateDeepHolder)` and true second-hop delimiter forms such as `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)`; first-pass `EXECSCRIPT()` return-expression coverage now includes direct `RETURN &macro` expansion and macro-expanded script-source text over current runtime state, including true second-hop source-text forms such as `EXECSCRIPT(&cExecScriptDeepHolder)` and true second-hop return-expression forms such as `EXECSCRIPT('RETURN &cExecReturnDeepHolder')`; the combined nested-surface harness now also includes second-hop holder flows across `EVAL()` and `EXECSCRIPT()` inputs; shared `EVAL()` / `EVALUATE()` runtime-state coverage now includes macro-expanded expression text such as `ALIAS()` and `SET('PATH')` plus nested macro-indirection through holder variables, including second-hop forms such as `EVAL(&cEvalExprDeepHolder)`, `EVALUATE(&cEvalExprDeepHolder)`, `EVALUATE(&cEvalPathExprDeepHolder)`, runtime-state `&macro` field-name resolution such as `&cFieldExprDeepHolder`, and direct true second-hop `&macro` expression indirection such as `&cMacroNameDeepHolder`; `LOOKUP()` helper coverage now includes nested macro-indirection for return/search expression text plus alias/tag designators on local and SQL cursors, including second-hop expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`, second-hop numeric return-expression forms such as `LOOKUP(&cAgeExprDeepHolder, &cSearchExpr, ...)`, second-hop local designator forms such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`, second-hop miss-path default typing for numeric `0` and logical `.F.` return expressions, second-hop SQL expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`, full all-argument second-hop SQL forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, &cAliasDeepHolder, &cTagDeepHolder)`, and second-hop SQL designator forms such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`; parameter-default coverage now also includes true second-hop macro defaults such as `LPARAMETERS nValue = &cDefaultExprDeepHolder`; macro dot-suffix coverage now includes nested-holder forms before suffix concatenation through second-hop cases such as `&cTypeDeepHolder.Name`, `&cFieldDeepHolder.`, and the literal fallback branch `&cStemDeepHolder.World`; direct `&macro` expression indirection coverage now also includes a true second-hop fallback-text branch via `&cFallbackExprDeepHolder`; embedded macro identifier coverage now includes nested-holder forms such as `m&cType2Holder.ID` and `m&cType2DeepHolder.ID`; and alias-qualified macro field access coverage now includes direct true second-hop alias reads such as `&cAliasDeepHolder..NAME` / `&cAliasDeepHolder..AGE`, the dynamic `AGE` second-hop form `&cAliasDeepHolder..&cAgeFieldHolder`, plus nested-holder alias and field-name variables through forms such as `&cAliasDeepHolder..&cNestedFieldHolder`
- `LOCATE`
- `SCAN/ENDSCAN`
- `CALCULATE`
- `COUNT`
- `SUM`
- `AVERAGE`
- `TOTAL`
- `READ EVENTS`, `CLEAR EVENTS`
- `REPLACE`
- `APPEND BLANK`
- `DELETE`, `RECALL`
- `DELETE FROM`
- `INSERT INTO`
- `PACK`, `ZAP`
- `SET FILTER TO/OFF` — first-pass cursor-scoped filter state for local and synthetic SQL/result cursors, including macro-expanded expression text through the shared `SET` helper; current coverage now includes true second-hop holder forms such as `SET FILTER TO &cFilterExprDeepHolder IN sqlcust`
- `SET FIELDS TO/ON/OFF` — first-pass session-scoped field visibility for runtime field lookup, including list readback through `SET('FIELDS')`; the shipped visibility lane now also honors `LIKE` / `EXCEPT` filters for both direct field resolution and consumers such as `BROWSE`, including true second-hop holder forms such as `SET FIELDS TO &cFieldsSpecDeepHolder`
- `SET ORDER TO TAG`
- `SEEK`
- `GO` / `GOTO`
- `SKIP`
- `USE IN <alias>` alias-target closure now also has focused true second-hop macro proof, including forms such as `USE IN &cAliasExprDeepHolder`
- `BROWSE` — first-pass headless/runtime browse event lane over local/runtime cursors; honors selected or `IN`-targeted cursor context, reuses session `SET FILTER` / `SET FIELDS` state by default, now including `LIKE` / `EXCEPT` field-visibility filters, accepts inline `FIELDS` and `FOR` clause capture, and emits `runtime.browse` metadata for later host/UI wiring
- `EDIT`, `CHANGE`, `INPUT`, `ACCEPT` — first-pass headless interactive command lane that emits runtime events (`runtime.edit`, `runtime.change`, `runtime.input`, `runtime.accept`) with captured prompt/target/field metadata for host-driven UI integration; `EDIT` / `CHANGE` now also surface effective selected-cursor/view metadata when a local cursor is active, including visible fields and active filter state, while `INPUT` / `ACCEPT` now reuse that same selected-cursor/view metadata and assign deterministic empty-string headless results for `TO <target>` variables
- `WAIT`, `KEYBOARD`, `DISPLAY`, `LIST`, `INPUT`, `ACCEPT`, `CALCULATE`, `COUNT`, `SUM`, `AVERAGE`, `TOTAL` — first-pass headless interaction/event lane that emits `runtime.wait`, `runtime.keyboard`, `runtime.display`, `runtime.list`, `runtime.input`, `runtime.accept`, and aggregate runtime events with captured mode/clause payloads for host integration; `WAIT` now captures resolved prompt/timeout/target state plus source expressions for common `WAIT [WINDOW] ... [TIMEOUT ...] [TO <target>] [NOWAIT] [NOCLEAR]` forms and assigns deterministic empty-string headless `TO` results, including true second-hop holder target forms such as `TO &cWaitTargetDeepHolder`; `INPUT` / `ACCEPT` now mirror that resolved prompt/target payload fidelity and deterministic empty-string `TO` behavior, including true second-hop holder target forms such as `TO &cInputTargetDeepHolder`; `KEYBOARD` now captures resolved key payloads plus source expressions and common `PLAIN` / `CLEAR` flags, `DISPLAY` / `LIST` now surface adjacent `STRUCTURE` / `STATUS` / `MEMORY` metadata in addition to the richer `RECORDS` lane, `DISPLAY` / `LIST RECORDS` now also retain `WHILE` clause payload detail and true second-hop `IN` target detail such as `IN &cAliasDeepHolder`, `DISPLAY` / `LIST STRUCTURE` now also has true second-hop `IN` target proof via forms such as `IN &cAliasDeepHolder`, and aggregate command events now also expose targeted cursor/work-area, visible fields, active filter, scope, `FOR` / `WHILE`, output-variable, and raw/resolved `IN` target detail for host/runtime readback, including true second-hop aggregate `TO` target forms such as `TO &cCountDeepHolder`, `TO &cSumDeepHolder`, and `TO &cCalcDeepHolder`
- `GETFILE`, `PUTFILE`, `GETDIR`, `INPUTBOX` — first-pass headless dialog-helper command lane; parser now captures both common clause-keyword forms and parenthesized/positional argument forms for prompt/title/default/filter/target metadata, and dispatcher emits `runtime.getfile`, `runtime.putfile`, `runtime.getdir`, and `runtime.inputbox` events while assigning deterministic empty-string first-pass results to `TO <target>` variables in headless mode
- `PUSH KEY` / `POP KEY`, `PUSH MENU` / `POP MENU`, `PUSH POPUP` / `POP POPUP` — first-pass session-scoped key/menu/popup stack semantics with deterministic push/pop depth behavior and runtime events (`runtime.push_*` / `runtime.pop_*`) including empty-pop signaling
- `SAVE TO`, `RESTORE FROM` — first-pass memvar persistence lane with `.mem` auto-extension behavior, `LIKE`/`EXCEPT` filtering, `ADDITIVE` merge support, robust escaped text serialization, and explicit empty-value markers for stable round-trip restore behavior; saved `.mem` payloads now also avoid persisting `PUBLIC` scope markers when the visible binding is actually a shadowing `LOCAL` or `PRIVATE` over an outer public name
- `RELEASE` — first-pass public/global release semantics now preserve public-pinned scalar variables and arrays across `RELEASE ALL`, `RELEASE ALL LIKE`, and `RELEASE ALL EXCEPT`, while named and non-public release paths continue to remove ordinary bindings; releasing current-frame `LOCAL` names now also reveals shadowed outer bindings instead of erasing them
- `SELECT`
- `USE`, `USE IN`, `USE AGAIN`
- `SET DATASESSION TO`
- `SET DEFAULT TO`
- `SET` — first-pass runtime-state surface now includes second-hop holder macro value coverage across ordinary string/numeric/logical options in addition to the earlier direct-variable paths; focused regressions now lock down representative forms such as `SET PATH TO &cPathTargetDeepHolder`, `SET MARK TO &cMarkTargetDeepHolder`, `SET DECIMALS TO &cDecimalsTargetDeepHolder`, `SET COLLATE TO &cCollateTargetDeepHolder`, `SET NULL TO &cNullTargetDeepHolder`, and `SET ANSI TO &cAnsiTargetDeepHolder`
- `CLOSE ALL` / `CLOSE TABLES` / `CLOSE DATABASES` — first-pass close semantics now cover both open work-area cursors and non-DBF runtime handles for `ALL`/`DATABASES` scopes (SQL connection state, tracked OLE handles, and outstanding low-level `FOPEN` handles)
- `ON ERROR`
- `ON SHUTDOWN` — first-pass shutdown-handler semantics for `ON SHUTDOWN DO <routine> [WITH ...]` plus common inline compatibility forms including `ON SHUTDOWN CLEAR EVENTS` and close-style cleanup commands such as `ON SHUTDOWN CLOSE DATABASES ALL`; shutdown routines run before final `QUIT` completion and nested `QUIT` inside the shutdown routine is treated as the terminal quit rather than recursing indefinitely
- `PUBLIC` — first-pass public identity declaration for memory variables and arrays
- `LOCAL`
- `PRIVATE`
- `PARAMETERS` / `LPARAMETERS` — first-pass argument binding for `DO ... WITH ...`, including `@` by-reference write-back and default-value expressions when callers omit trailing arguments; default expressions now evaluate through the ordinary expression engine, so macro-expanded defaults such as `LPARAMETERS x = &cExpr` resolve consistently
- `DIMENSION` / array-form `DECLARE` — first-pass runtime arrays with one- and two-dimensional sizing
- `DECLARE ... IN <dll>` — first-pass Windows native / managed declaration surface, including bare `@var` call arguments and native by-ref write-back for declared `STRING @` and numeric parameters when the callsite also uses `@`
- `STORE` — evaluates the expression once and assigns through the same target semantics as ordinary assignment, including scalar variables, direct array elements, macro-expanded targets, `m.` memory variables, and OLE property targets
- `CREATE TABLE` — first-pass local DBF creation for field declarations (`C`/`N`/`F`/`B`/`L`/`D`/`I`/`Y`/`T`/`M`/`V`/`Q` plus common long-form names); nullable/default annotations are accepted by the parser, created/open cursors apply field defaults for omitted `INSERT INTO` fields, and `NOT NULL` fields are validated with append rollback on failure
- `CREATE CURSOR` — local temp-backed DBF cursor creation using the shared structural field-declaration parser and field-rule mapping; resulting cursors now participate in normal local-table mutation/schema flows (`APPEND BLANK`, `REPLACE`, `INSERT INTO`, `AFIELDS`, `FCOUNT`, `FSIZE`, `RECCOUNT`) instead of the old synthetic stub, and `DEFAULT` / `NOT NULL` metadata are enforced through the same open-cursor field-rule path used by `CREATE TABLE`
- `ALTER TABLE ... ADD/DROP/ALTER COLUMN` — first-pass local DBF schema rewrite that appends, removes, or rewrites one supported field while preserving row values and deleted flags where the resulting schema can store them; `ADD COLUMN ... DEFAULT ...` now also backfills existing rows with the declared default instead of leaving the new field blank
- structural rollback coverage now explicitly proves that `ALTER TABLE` rewrites and `PACK MEMO` compaction remain readable after `ROLLBACK`, so the structural lane is defended against transaction-unwind regressions as well as forward-mutation success
- `COPY TO` — full local DBF export via `create_dbf_table_file`; optional `FIELDS` clause filters columns, including single-field keyword-named selections such as `FIELDS TYPE` plus deeper `LIKE` / `EXCEPT` forms across the same copy-to/copy-to-array lane; optional `FOR` clause filters rows by visibility; first-pass `TYPE SDF` fixed-width text export plus VFP-style `TYPE CSV`, `TYPE TAB`, and `DELIMITED` text export, including CSV field-name headers, quoted character fields, tab-delimited rows, `WITH <enclosure>`, and `WITH CHARACTER <separator>` handling; first-pass `TYPE JSON` export emits a modern object-array JSON payload with typed numeric/logical values and string keys from field names; first-pass `TYPE XLS` export emits SpreadsheetML-style workbook XML with header-row and typed cell data; first-pass `TYPE DIF` export emits a conservative DIF-style text table with header-row emission and typed numeric/string cells; first-pass `TYPE SYLK` export emits a conservative SYLK-style text table with header-row emission and typed numeric/string cells; first-pass `COPY TO ARRAY <name>` populates a two-dimensional runtime array (rows=qualifying records, cols=fields) with optional `FIELDS` and `FOR` clauses, including macro-expanded array-name targets and true second-hop holder forms such as `&cMacroArrayDeepHolder`, `&cArrayNameDeepHolder`, and `&cTempArrayDeepHolder`
- selected synthetic SQL/result cursors now also materialize DBF output for plain `COPY TO` even when no backing local source path exists, deriving schema from remote field metadata (or remote rows) and preserving selected alias/record pointer state during export
- wildcard `FIELDS LIKE` / `FIELDS EXCEPT` coverage now explicitly reaches DBF `COPY TO` / `APPEND FROM` flows in addition to the earlier array-transfer and browse/name-object slices
- DBC metadata export now also has a first-pass whole-database JSON lane through the native asset-inspector API (`export_database_as_json`), including decoded binary `PROPERTIES` memo bags from `.dct` sidecars, case-insensitive same-base `DCT/DCX/DBF` resolution on case-sensitive filesystems, preference for the catalog `DATABASE` object name over the raw `.dbc` stem, and table-schema/table-row snapshots for resolvable local `TABLE` catalog entries
- `COPY STRUCTURE TO` — schema-only export (zero records); first-pass
- selected SQL/result cursors now also export schema-only DBF output through `COPY STRUCTURE TO`, including metadata-only remote cursors with zero rows
- `APPEND FROM` — full local DBF import via `parse_dbf_table_from_file`; inserts each non-deleted source record using `append_blank_record_to_file` + `replace_record_field_value`; optional `FIELDS` clause, including single-field keyword-named selections such as `FIELDS TYPE` plus deeper `LIKE` / `EXCEPT` forms across the same append/import array lane; first-pass `TYPE SDF` fixed-width text import plus first-pass `TYPE CSV`, `TYPE TAB`, and `DELIMITED` import with quoted-field parsing, tab-delimited rows, and matching CSV header skipping; first-pass `TYPE JSON` import reads the shipped object-array JSON lane and maps object keys to destination field names through the shared blank-row plus field-replacement path; first-pass `TYPE XLS` import reads the shipped SpreadsheetML-style workbook lane, skips a matching header row, and appends rows through the shared blank-row plus field-replacement path; first-pass `TYPE DIF` import reads the shipped DIF-style text lane, skips a matching header row, and appends rows through the same shared DBF mutation path; first-pass `TYPE SYLK` import reads the shipped SYLK-style text lane, skips a matching header row, and appends rows through that same shared DBF mutation path; first-pass `APPEND FROM ARRAY <name>` appends rows of a runtime array as new records (columns map positionally to destination fields with optional `FIELDS` filtering), including macro-expanded array-name sources, true second-hop holder forms such as `&cTempArrayDeepHolder` and `&cArrayNameDeepHolder`, and typed date/datetime serialization instead of string-only writes
- selected synthetic SQL/result cursors now also support first-pass DBF `APPEND FROM` mutation in-memory (field-name mapping plus `FIELDS` filtering) instead of event-only stubbing, while unsupported non-DBF source types return deterministic runtime diagnostics
- selected synthetic SQL/result cursors now also apply `FOR` filtering during DBF `APPEND FROM` mutation, so only rows matching the clause are materialized into the target cursor; focused SQL cursor macro-filter coverage now also includes true second-hop `FOR &expr` forms across `LOCATE`, `DELETE`, and `RECALL`, such as `&cLocateExprDeepHolder` and `&cDeleteExprDeepHolder`
- selected synthetic SQL/result cursors now also support `APPEND FROM TYPE JSON` and `APPEND FROM TYPE CSV` / `TYPE DELIMITED` in-memory mutation: JSON sources map object keys to remote cursor field names, CSV/DELIMITED sources map positional values with optional CSV header-row skip; both branches honor `FIELDS` filtering and the `FOR` clause; previously both paths returned a "DBF source only" stub error
- `SCATTER MEMVAR` / `SCATTER TO <array>` / `SCATTER NAME <object>` — snapshot current record fields to `m.<fieldname>` variables, runtime arrays, or object properties; `FIELDS`, `BLANK`, and `MEMO` are honored with typed values, including runtime-facing `MM/DD/YYYY` and `MM/DD/YYYY HH:MM:SS` strings for `D`/`T` fields; the parser now also disambiguates single-field filters such as `FIELDS NAME` from the command's own `NAME` clause for both memvar and object-target forms, and deeper regression coverage now exercises `LIKE` / `EXCEPT` filters on object-target flows; macro-expanded array-name targets are supported, including direct true second-hop holder forms such as `SCATTER TO &cMacroRowNameDeepHolder`; `SCATTER NAME &cObjectName`-style macro-expanded object variable targets are also supported, including direct true second-hop holder forms such as `SCATTER NAME &cObjectNameDeepHolder`; nested object-property targets such as `SCATTER NAME oHolder.Row` / `GATHER NAME oHolder.Row` are supported, including macro-expanded nested path segments such as `SCATTER NAME &cHolder.&cChild` and direct true second-hop holder forms such as `SCATTER NAME &cHolderDeepHolder.&cChildDeepHolder`; scatter now creates missing nested child-object targets on demand instead of requiring them to already exist; non-`ADDITIVE` scatter on an existing object target now replaces that target object instead of mutating it in place, while `SCATTER NAME <object> ADDITIVE` reuses an existing object when present and merges field properties onto it; first-pass
- `GATHER MEMVAR` / `GATHER FROM <array>` — write `m.<fieldname>` variables or array elements back to current record via direct `replace_record_field_value` calls (type-faithful, no string-quote round-trip); `FIELDS` and `FOR` are honored, including single-field `FIELDS NAME` filters on both memvar and `GATHER NAME <object>` forms plus deeper `EXCEPT` coverage on object-target flows, date/datetime/blank runtime strings now restore consistently into DBF storage, and macro-expanded array-name sources are supported, including direct true second-hop holder forms such as `&cMacroRowNameDeepHolder`, while `GATHER NAME <object>` macro object targets now also have direct true second-hop proof via forms such as `&cObjectNameDeepHolder`; first-pass
- wildcard `FIELDS LIKE` / `FIELDS EXCEPT` coverage now also explicitly defends the MEMVAR scatter/gather path instead of only the NAME-object and array-transfer paths
- `SCATTER MEMVAR BLANK` (and other BLANK forms) now succeeds on zero-record tables by deriving typed blank values from the cursor schema instead of requiring a current record; `blank_value_for_field` now correctly returns an empty string for character ('C') and varchar ('V') fields, matching VFP behavior where `SCATTER BLANK` creates `''` for character-type memvars
- `UPDATE` — first-pass local/remote cursor command forms, including `UPDATE <alias> SET ...`, `UPDATE SET ...` against the selected cursor, and `UPDATE IN <alias> SET ...`; `WHERE`/`FOR` scope clauses reuse scoped record replacement semantics, including macro-expanded `FOR &cExpr` filters
- `DELETE FROM` / `INSERT INTO` — first-pass SQL-style local/remote cursor mutation forms; `DELETE FROM <target> WHERE/FOR <expr>` tombstones matching target rows and defaults to all rows when no condition is supplied, including macro-expanded `FOR &cExpr` filters and true second-hop local forms such as `FOR &cDeleteExprDeepHolder`; `INSERT INTO <target> [(fields)] VALUES (...)` appends through the shared blank-row plus field-replacement path, including expression-driven cursor targets, schema-order mapping when the field list is omitted, selected-cursor preservation, local DBF persistence, and synthetic SQL result cursor parity
- `INSERT INTO` failure rollback — local and synthetic remote inserts now restore the previous record count when a field write fails after the blank-row append step, preventing a failed insert from leaving an extra blank row behind
- `PACK`, `PACK MEMO`, and `ZAP` — first-pass local DBF table-maintenance commands; `PACK` physically removes deleted records while preserving schema and kept-row order, `PACK MEMO` rewrites the memo sidecar from current memo values to compact stale memo blocks, and `ZAP` truncates the table to zero records while keeping it appendable; local cursors now honor first-pass exclusive-use guards from `SET EXCLUSIVE` / `USE ... SHARED|EXCLUSIVE`; synthetic remote/result cursors get matching in-memory behavior where applicable
- `SET REPROCESS`, `SET MULTILOCKS`, `RLOCK()`, `FLOCK()` / `LOCK()`, `ISRLOCKED()`, `ISFLOCKED()`, and `UNLOCK` — first-pass in-process lock-state tracking for open local/runtime cursors, including `SET()` readback, `UNLOCK ALL`, record-specific `UNLOCK RECORD <n>` release, `AUTOMATIC` default `REPROCESS` semantics, and bounded retry/timeout behavior across explicit lock attempts plus shipped local mutation paths; this establishes runtime-visible lock semantics but does not yet claim cross-process OS locking or transaction journaling
- `SET` runtime-state commands — first-pass normalized/evaluated handling for common state options including boolean options, `REPROCESS`, date/time formatting controls, `PATH`, `DECIMALS`, `COLLATE`, `MARK`, `POINT`, `SEPARATOR`, and `CURRENCY`; string/numeric RHS values can come from literals, variables, or macro-expanded expressions where supported, with `SET()` readback defaults for the shipped options
- `BEGIN TRANSACTION`, `END TRANSACTION`, `ROLLBACK`, and `TXNLEVEL()` — first-pass transaction nesting counters with per-data-session isolation, runtime event emission (`runtime.transaction.begin` / `runtime.transaction.end` / `runtime.transaction.rollback`), durable local DBF preimage journaling for shipped mutation paths, rollback cursor refresh/closure when replay removes a table, and startup replay of pending journals after interrupted transactions

Local DBF `APPEND BLANK` now also has a conservative opaque-field lane for otherwise readable unsupported record layouts: unknown/binary field bytes are zero-initialized instead of rejecting the whole append, which lets runtime mutation continue over mixed-layout tables while still avoiding overclaimed write-surface semantics for direct typed replacement on those field families.

Shared local DBF direct writes now also have a matching conservative opaque-field replacement lane for those same otherwise readable unsupported families: callers can write `NULL`, width-fitting raw text, or `0x`-prefixed hex payloads without inventing higher-level typed semantics for the underlying field family.

Command-level aggregate assignment now also includes first-pass `TO ARRAY` forms for `COUNT`, `SUM`, and `AVERAGE`, using one-dimensional runtime array assignment through the shared array helper path. Focused regression coverage validates scope-clause behavior (`ALL`/`REST`/`NEXT`/`RECORD`), `FOR`/`WHILE` filtering, `IN` alias targeting, `TO &macro` target resolution on the command aggregate lane including second-hop holder forms such as `&cCountHolder`, and malformed `TO ARRAY` diagnostics. `CALCULATE` now also preserves `WHILE` clause semantics on the same aggregate path. Aggregate expression parity also now includes omitted-expression forms: `COUNT()` returns visible-row counts, and no-expression `SUM()` / `AVG()` / `MIN()` / `MAX()` use the first numeric-compatible field (`N/F/B/I/Y`) with deterministic no-numeric-field fallbacks.

The `m.` variable namespace prefix is also now correctly handled as a memory-variable alias across assignment, lookup, expression evaluation, and declarations: `m.<name>`, `M.<name>`, and bare `<name>` share the same local/global runtime binding, and `m.<name>` no longer falls through to OLE property resolution.

The memory-variable lane now also restores saved outer bindings immediately when `RELEASE <name>` targets a currently shadowing `PRIVATE` variable, instead of waiting until the current frame unwinds.

Wildcard `RELEASE ALL LIKE ...` / `RELEASE ALL EXCEPT ...` handling now also covers array-only names rather than only scalar/global bindings, while continuing to preserve `PUBLIC` variables and arrays.

`CLEAR MEMORY` now also drops deferred `PRIVATE` saved bindings so cleared names do not reappear later when nested frames unwind.

`RELEASE ALL` now also clears current-frame `LOCAL` bindings instead of only global/array candidates, and later reassignment of those locals stays in local scope rather than leaking new globals.

`CLEAR MEMORY` now also clears current-frame `LOCAL` bindings while preserving local declaration routing for later in-scope reassignment.

The same memory-file lane now also has first-pass array/scope fidelity for `SAVE TO` / `RESTORE FROM`: saved `.mem` payloads now preserve runtime arrays with dimensions plus real visible-`PUBLIC` scope markers, additive restore now writes through visible `LOCAL` bindings instead of leaving restored values hidden behind stale locals, and non-additive restore now clears stale arrays and deferred `PRIVATE` shadow state before recreating the saved bindings.

That same memory-lifetime lane now also handles shadowed locals more faithfully: `RELEASE <name>` and `RELEASE ALL` remove current-frame `LOCAL` bindings without destroying the outer global/public value they were masking.

The indexed-search lane now also has a first-pass runtime collation step for plain string orders: when an active order does not already carry an explicit case-folding expression hint, non-`MACHINE` session collations such as `SET COLLATE TO GENERAL` now case-fold plain string seek comparisons instead of leaving `SET COLLATE` as metadata-only state.

That plain-string collation step is now explicitly defended on synthetic SQL/result cursors too: focused SQL seek coverage proves the same non-`MACHINE` session collation behavior on temporary `NAME`-style orders instead of leaving remote parity implied by the shared implementation.

The adjacent date/time runtime-state lane now also has stronger `SET()` readback fidelity: `SET('DATE')` defaults to `MDY` instead of falling through to `OFF`, and focused coverage now locks down data-session isolation/restoration for `DATE`, `CENTURY`, `MARK`, `HOURS`, and `SECONDS`.

The broader runtime-state lane now also has focused session-isolation/readback coverage for `COLLATE`, `NULL`, `ANSI`, `REPROCESS`, `MULTILOCKS`, and `EXCLUSIVE`, locking down their default values plus `SET DATASESSION` restoration behavior alongside the earlier shipped command semantics. The adjacent lock-contention slice now also proves that `REPROCESS` defaults to `AUTOMATIC`, that explicit numeric overrides bound retry counts deterministically, and that conflicting local lock/mutation attempts fail with timeout diagnostics instead of busy-spinning.

That same session-state lane now also explicitly defends formatting-state isolation/readback for `MEMOWIDTH`, `POINT`, `SEPARATOR`, and `CURRENCY`, plus adjacent calendar/week-state isolation/readback for `FDOW` and `FWEEK`. The shared `SET()` readback path also now preserves numeric/string values for these non-boolean settings instead of collapsing values like `1` to `ON` after session restoration.

Runtime arrays now have first-pass direct element reads and writes through both bracket and parenthesis syntax (`array[1]`, `array[1,2]`, `array(1)`, `array(1,2)`), including declared arrays and arrays resized through `ASIZE()`. Macro-expanded array identifiers now also work across adjacent helper and element-access paths such as `ALEN(&cArrayName)`, `ALINES(&cTargetName, ...)`, `ACOPY(&cSourceName, &cTargetName, ...)`, `&cArrayName[2,1]`, and `&cArrayName(1,2)`, and focused coverage now includes true second-hop forms such as `ALINES(&cLinesNameDeepHolder, ...)`, `ALEN(&cArrayNameDeepHolder)`, `AELEMENT(&cArrayNameDeepHolder, ...)`, `ACOPY(&cArrayNameDeepHolder, &cCopyNameDeepHolder, ...)`, `&cArrayNameDeepHolder[2,1]`, and `&cArrayNameDeepHolder(1,2)`. Shared assignment-target coverage now also includes true second-hop macro-expanded array-element targets such as `STORE 6 TO &cMacroTargetDeepHolder`. `parse_primary()` now also prefers shipped array-helper function calls over same-named runtime arrays when `(` follows an identifier, avoiding collisions such as an existing `aLines` array shadowing `ALINES(...)`. The array helper surface now also covers `ACOPY()` row-major copies over one- and two-dimensional arrays, `AELEMENT()` / `ASUBSCRIPT()` index conversion, `ASCAN()` start/count scans with column, case-insensitive, exactness, row-return, and first-pass predicate-expression search support, `ASORT()` start/count windows plus descending/case-insensitive/numeric-aware ordering and two-dimensional row sorting by the start element's column, and two-dimensional `ADEL()` / `AINS()` row/column shifting. Focused proof now also covers true second-hop target-array holder chains for `ALINES()` and `ACOPY()` via forms such as `&cLinesNameDeepHolder` and `&cCopyNameDeepHolder`. `ASCAN()` macro-expanded predicate fidelity and scan-metadata isolation are also now fully proven: variable-reference predicates, first-hop macro-expanded predicates, and second-hop holder-chain predicates all resolve correctly, and all four `_ASCAN*` metadata variables are restored on every exit path.

The current xAsset runtime surface behind those commands is now green at the first-pass runtime level: `DO FORM` is backed by generated `SCX/VCX` bootstrap sources that sequence `DataEnvironment` open/close hooks plus root-object lifecycle methods where present, `REPORT FORM` and `LABEL FORM` now both support preview/event-loop and `TO FILE` execution lanes, and menu assets now have executable startup/action/cleanup bootstrap modeling through the shared `MNX` xAsset path.

The current indexed-search slice covers `SET ORDER TO TAG`, first-pass `ASCENDING` / `DESCENDING` direction control on the local `SET ORDER` / `SEEK` path, grounded `UPPER(...)` seek-key normalization on that same search path, first-pass composite tag-expression evaluation for concatenated keys such as `UPPER(LAST+FIRST)`, first-pass substring-style tag evaluation for `LEFT(...)`, `RIGHT(...)`, and `SUBSTR(...)` expressions such as `UPPER(LEFT(NAME, 3))`, `UPPER(RIGHT(NAME, 3))`, and `UPPER(SUBSTR(NAME, 2, 3))`, first-pass grounded `FOR`-filtered order behavior for extracted `DELETED() = .F./.T.` hints, command/function `SEEK`, `INDEXSEEK()`, `ORDER()`, `TAG()`, `SET EXACT`, `SET NEAR`, function-order designator suffix parsing for `SEEK()` / `INDEXSEEK()` one-off `ASCENDING` / `DESCENDING` overrides, command-level one-off `SEEK ... TAG/ORDER` probes for local DBF-backed cursors, first-pass ad hoc local order expressions such as `SET ORDER TO UPPER(NAME)` and `SEEK(..., 'People', 'UPPER(NAME)')`, and descriptive stored CDX tag names such as `FULLNAME` now bind to their hinted tag-page-local expressions even when the tag name itself does not resemble the key expression. The runtime also preserves the actual inspected index-file identity for `ORDER(alias, 1)` plus `TAG(indexFile, ...)` across supported xBase index extensions instead of assuming `.cdx`.

That local indexed-search slice now also evaluates a broader grounded tag-expression family on the shipped `SEEK` path, including `LEFT(...)`, `RIGHT(...)`, `SUBSTR(...)`, `PADL(...)`, `PADR(...)`, and a constrained first-pass `STR(<numeric>[, <width>[, <decimals>]])` path captured from loaded tag metadata, including first-pass default-space `PADL/PADR` behavior. The seek path now also preserves significant search-key whitespace so padded tag expressions can match without trimming away meaningful leading/trailing spaces.

Grounded order `FOR` metadata is now consumed a little deeper on the same seek path instead of staying mostly descriptive. In addition to the earlier `DELETED()` filter support, local indexed seeks now honor first-pass top-level string and numeric comparison filters such as `NAME = 'BRAVO'` and `AGE >= 20`, including correct `SET NEAR` miss positioning against the surviving candidate set.

That same local temporary-order path now also has focused command-path `IN <alias|work area>` parity coverage for ad hoc expressions such as `UPPER(NAME)`, including selected-alias preservation and descending `SET NEAR` behavior on non-selected targeted cursors. Coverage now also explicitly defends plain-expression targeted orders (`SET ORDER TO NAME IN People`) under session collation changes, proving that `SET COLLATE TO GENERAL` case-folds `SEEK(..., 'People', 'NAME')` while preserving selected non-target alias/pointer state. Synthetic SQL/result cursors now also honor first-pass ad hoc `FOR` filters on that temporary-order lane, so probes such as `SEEK(..., 'sqlcust', "UPPER(NAME) FOR NAME = 'BRAVO'")` and command-path `SET ORDER TO UPPER(NAME) FOR ...` constrain the candidate set and `SET NEAR` miss positioning instead of treating the `FOR` tail as inert text.

That same ad hoc-order lane now also consumes grounded field-type metadata for plain numeric field expressions such as `SET ORDER TO AGE` and `SET ORDER TO AMOUNT`, so local and synthetic SQL `SET NEAR` probes choose the next numeric key instead of falling back to lexicographic string ordering for one-off numeric temporary orders.

That same local ad hoc-order lane now also has focused helper-path parity for `INDEXSEEK(.F./.T.)` over temporary expressions such as `UPPER(NAME)`, including descending `SET NEAR` miss positioning while preserving the controlling `ORDER()` state.

The expression/function layer currently has first-pass support for these VFP-facing built-ins:

- `COUNT()`, `SUM()`, `AVG()/AVERAGE()`, `MIN()`, `MAX()`
- first-pass `ALEN()` for runtime arrays created by `SCATTER TO <array>`, `AERROR(<array>)`, `DIMENSION` / array-form `DECLARE`, and `ASIZE()`, including total element count and dimension queries
- deeper first-pass `AERROR(<array>)` with VFP-aligned seven-column rows for normal runtime errors: code, message, mixed-case error parameter, faulting work area, fault line, fault procedure, and failing statement text; SQL/ODBC-like `1526` rows now preserve captured handle/detail plus provider-or-operation context and command/target payload when the runtime has them, and OLE/automation-like `1429` rows now preserve captured member-path detail, source object identifiers, and failing action text instead of leaving trailing columns as placeholders when Copperfin already knows that payload
- first-pass runtime array helpers over Copperfin runtime arrays: `ACOPY()`, `ADIR()`, `AELEMENT()`, `AFIELDS()` over local and synthetic SQL cursor schemas, `ALINES()`, `ASIZE()`, `ASCAN()`, `ADEL()`, `AINS()`, `ASORT()`, `ASUBSCRIPT()`, and `AUSED()`; the focused array regression target now exercises predicate-style `ASCAN()` searches, including unquoted block-style predicate arguments, common two-dimensional `ACOPY()` row/column workflows, and declared-array/metadata helper coverage separately from the legacy catch-all PRG-engine test
- `EVAL()` and deeper first-pass bounded-indirection `&macro` substitution in expression paths (identifier-chain expansion with deterministic recursion/cycle safety), now including alias-qualified field continuations such as `&cAlias..NAME`, macro-driven suffix forms such as `&cAlias..&cField`, and focused nested evaluation coverage where stored expression text feeds another `EVAL()` layer
- `SQLCONNECT()`, `SQLSTRINGCONNECT()`, `SQLEXEC()`, `SQLDISCONNECT()`, `SQLROWCOUNT()`, `SQLPREPARE()`, first-pass `SQLGETPROP()`, `SQLSETPROP()`, `SQLCANCEL()`, `SQLCOMMIT()`, `SQLROLLBACK()`, `SQLDATABASES()`, `SQLPRIMARYKEYS()`, `SQLFOREIGNKEYS()`, `SQLTABLES()`, and `SQLCOLUMNS()`
- `CREATEOBJECT()`, `GETOBJECT()` — first-pass runtime OLE activation parity now includes object-property round-trip storage, seeded `Scripting.Dictionary` collection behavior (`Add` / `Exists` / `Item` / `Remove` / `RemoveAll` / `Count`), `NEWOBJECT()` library/server activation metadata capture, `GETOBJECT()` class/source targeting plus matching-attachment reuse instead of unconditional duplicate-handle registration, and trapped seeded-automation missing-member faults so `ON ERROR` / `AERROR()` and `TRY/CATCH/FINALLY` can contain `1429`-style property/method failures without poisoning later object use
- reflection/runtime-surface object helpers: first-pass `COMPOBJ()`, `AMEMBERS()`, `ACLASS()`, `PEMSTATUS()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` with seeded `Scripting.Dictionary` member metadata for deterministic reflection behavior
- first-pass runtime-surface XML cursor bridge helpers: `CURSORTOXML()` and `XMLTOCURSOR()` over a deterministic Copperfin XML shape, including file-or-string transfer paths, row-count returns for imports, and `runtime.cursortoxml` / `runtime.xmltocursor` event emission with warning events on invalid inputs
- utility coverage already used by shipped tests and bootstrap paths, including occurrence-aware `AT()` / `RAT()`, first-pass case-insensitive `ATC()` / `RATC()`, line-oriented `ATLINE()` / `ATCLINE()` / `RATLINE()`, `LIKE()`, `INLIST()`, `PROPER()`, first-pass `STRCONV(..., 7/8)` casing modes, first-pass phonetic helpers (`SOUNDEX()` and `DIFFERENCE()`), first-pass memo-line helpers (`MEMLINES()` and `MLINE()` with session-scoped memo width, explicit width overrides, optional tab-width expansion, and opt-in LF hard-break flags), `CHRTRAN()` / first-pass `CHRTRANC()`, `STRTRAN()` with first-pass optional start-occurrence and replacement-count arguments, `SUBSTR()`, `ALLTRIM()`, `STREXTRACT()` with flag-aware delimiter handling, `STR()` with width/decimal formatting, `TRANSFORM()` numeric/picture formatting with first-pass `SET POINT`/`SET SEPARATOR`/`SET CURRENCY` support plus `@Z` blank-zero and `@B` trimmed-left-justified behavior, `PADL()` / `PADR()` / `PADC()` padding and overflow truncation, `GETWORDCOUNT()` / `GETWORDNUM()` with delimiter-set handling, `CHR()`, type/null helpers including first-pass expression-value `ISBLANK()`, real `.NULL.` literal parsing, and single-byte `ISLEADBYTE()` plus improved `TYPE('<expr>')` parenthesized-expression handling, cursor/schema helpers (`FIELD()` and `FSIZE()` over local DBF-backed and synthetic SQL cursors), numeric helpers (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`, `RAND()`, `RGB()`, and expression-list `MIN()` / `MAX()`), financial helpers (`FV()`, `PV()`, `PAYMENT()`), hex conversion (`HEX()`), runtime-surface helpers (`CAST()`, `BITAND()`, `BITOR()`, `BITXOR()`, `BITNOT()`, `BITCLEAR()`, `BITSET()`, `BITTEST()`, `BITLSHIFT()`, `BITRSHIFT()`, `BINTOC()`, `CTOBIN()`, first-pass host probes `HOME()`, `OS()`, `DISKSPACE()`, `DRIVETYPE()`, first-pass `CURSORGETPROP()` / `CURSORSETPROP()`, lock-key probes such as `NUMLOCK()`, first-pass UUID generation `NEWID()`, first-pass code-page helpers `CPCURRENT()` / `CPCONVERT()` / `CPDBF()`, first-pass headless dialog stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()`, first-pass read-context stub `VARREAD()`, and context/introspection helpers `PCOUNT()`, `GETENV()`, and first-pass `PUTENV(<name>, <value>)`), date/time helpers (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, constructor-argument forms of `DATE()` / `DATETIME()`, `STOD()`, `TTOS()`, `CTOT()` including compact sortable datetime input, `DTOC(..., 1)`, `TTOC(..., 1)`, `DTOT()`, `TTOD()` including compact sortable datetime input, `HOUR()`, `MINUTE()`, `SEC()`, first-pass `SET DATE`/`SET CENTURY`/`SET MARK` formatting and parsing for common MDY/DMY/YMD orders and date separators, first-pass `SET HOURS`/`SET SECONDS` display formatting for time and datetime output, first-pass `WEEK()` with optional first-day and first-week mode arguments including mode 2/3 cross-year rollover plus `SET FDOW`/`SET FWEEK` defaults, `QUARTER()`, `EOMONTH()`, `DTOJ()` / `TTOJ()` / `JTOD()` / `JTOT()` with first-pass invalid-input guards), portable string-parsed path helpers (`JUSTPATH()`, `JUSTFNAME()`, `JUSTSTEM()`, `JUSTEXT()`, `JUSTDRIVE()`, `FORCEEXT()`, `FORCEPATH()`, `DEFAULTEXT()`, and first-pass `CURDIR()`), file metadata helpers (`FILE()` and `FILESIZE()` with `SET PATH` search handling for relative probes), first-pass low-level file I/O helpers (`FOPEN`, `FCLOSE`, `FREAD`, `FWRITE`, `FGETS`, `FPUTS`, `FSEEK`, `FTELL`, `FEOF`, `FFLUSH`, `FCHSIZE`, `FILETOSTR`, `STRTOFILE` — handle table, path resolution, binary/text modes, POSIX/Win32 truncation), `SYS()` including first-pass deterministic `SYS(3)`, `SYS(7)`, `SYS(11)`, `SYS(13)`, `SYS(2003)`, `SYS(2004)`, `SYS(2020)`, and `SYS(2023)`, `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `VERSION()`, `ON()`, `SET()`, and `MESSAGEBOX()`, first-pass `TEXTMERGE()` with configurable `<<>>`-style delimiters and recursive evaluation support that now continues until the merged text stabilizes, first-pass `EXECSCRIPT()` RETURN-expression pass-through, and first-pass `LOOKUP()` seek-and-evaluate over local alias/tag indexes plus synthetic SQL/result cursors through temporary field-order probes, including variable-driven alias/tag designators

That SQL slice now includes session-scoped synthetic cursor materialization that follows the current selected work-area flow for each data session when `SQLEXEC()` auto-opens a result cursor, plus first-pass connection-state helpers around that same per-session handle model: `SQLROWCOUNT()`, `SQLPREPARE()`, `SQLGETPROP()` / `SQLSETPROP()`, `SQLCANCEL()` / `SQLCOMMIT()` / `SQLROLLBACK()`, and first-pass catalog-style metadata helpers `SQLDATABASES()` / `SQLPRIMARYKEYS()` / `SQLFOREIGNKEYS()` / `SQLTABLES()` / `SQLCOLUMNS()`. The current connection-property pass now exposes deterministic `Connected`, `ConnectHandle`, `ConnectString`, `ConnectName`, `CurrentCatalog`, `LastCursorAlias`, `LastResultCount`, `Asynchronous`, `BatchMode`, `DispWarnings`, `DispLogin`, `Transactions`, `WaitTime`, and `PacketSize` behavior over the existing session-local handle state, while the metadata-cursor pass exposes deterministic synthetic database, table, primary-key, foreign-key, and column catalogs for top-level catalog enumeration plus `TABLE` / `VIEW` / `SYSTEM TABLE` probing, FoxPro-style and native-style `SQLCOLUMNS()` layouts, empty metadata cursors that still preserve schema, and strict cross-session handle rejection for the connection-state helpers. The shipped remote-cursor layer lets synthetic SQL rows participate in field lookup, targeted command-path filtering (`SET FILTER ... IN`) composed with targeted command-path navigation (`GO ... IN`, `SKIP ... IN`, `LOCATE ... IN`), command-path `SCAN ... IN` targeted iteration, aggregate built-ins, command-level `TOTAL` output aggregation, `CALCULATE`, in-memory `REPLACE` / `APPEND BLANK` / `DELETE` / `RECALL` mutation, non-selected-target `REPLACE ... IN` / `DELETE FOR ... IN` / `RECALL FOR ... IN` command-path behavior, `APPEND BLANK IN <alias|work area>` targeted append behavior with selected-alias/pointer isolation coverage, one-off `SEEK()` / `INDEXSEEK()` probes that use temporary order expressions over the in-memory result rows, first-pass normalization-aware temporary SQL order expressions such as `UPPER(NAME)` including combined `DESCENDING` suffix probes, and command-path `SET ORDER TO <expr>` / `SEEK` behavior over those same synthetic rows including non-selected-target `SET ORDER ... IN ... [ASCENDING|DESCENDING]` / `SEEK ... IN` parity, targeted backward/boundary navigation coverage (`GO BOTTOM IN`, `SKIP -1 IN`, targeted `BOF()` / `EOF()`), and post-mutation seeks against appended in-memory rows.

The current session/runtime-state slice also now keeps `SET DEFAULT TO` data-session-local, so `SET('DEFAULT')` and relative path resolution restore correctly after `SET DATASESSION` switches instead of leaking one session's default directory into another, and focused regression coverage now locks down the local-table counterpart to the shipped SQL selection-flow behavior so `SELECT 0`, `USE IN <selected-alias>`, and plain `USE` keep reusing each session's selected empty work area after session switches. `SET('FIELDS')` field-visibility readback and lookup restoration are now explicitly defended on that same data-session boundary too, so per-session `SET FIELDS TO ...` state does not leak across `SET DATASESSION` switches. Boolean runtime `SET` options used by search/filter semantics (`NEAR`, `EXACT`, `DELETED`) and runtime flags (`STRICTDATE`, `ENGINEBEHAVIOR`, `OPTIMIZE`, `TALK`, `SAFETY`, `ESCAPE`, `CENTURY`, `SECONDS`) now also normalize `SET ... TO` forms and logical token variants (`.T./.F.`, `1/0`, `true/false`, `yes/no`) to stable runtime state and `SET()` readback values across data-session switches, while `SET DATE TO`, `SET MARK TO`, first-pass `SET HOURS TO`, and first-pass `SET FDOW`/`SET FWEEK` state now feed the date/time helper parse/format path for common MDY/DMY/YMD orders, separators, time display, and week-number defaults.

`ON ERROR` now also has a richer first-pass compatibility lane: `ON ERROR DO <routine> WITH ...` can pass evaluated handler arguments, and handlers can inspect first-pass runtime fault metadata through `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `AERROR()`, `SYS(2018)`, and `ON('ERROR')` while execution resumes after the handler returns. Normal `AERROR()` rows now preserve the original faulting work area even if the handler changes selection before inspecting the array, handler execution now keeps those fault metadata helpers bound to the original fault even if the handler triggers and catches a secondary runtime error, and thrown-expression faults such as `LOG(-1)` / `ACOS(2)` now have explicit pause-state and repeated-fault coverage so line/statement/message metadata refresh correctly across multiple continues. Static PRG diagnostics are also now wired through the Studio document-open path so analyzer findings surface to editor-facing consumers instead of staying runtime-only.

`ON SHUTDOWN` now has a first-pass compatibility lane as well. `QUIT` checks for an active shutdown handler and, if configured, executes `ON SHUTDOWN DO <routine> [WITH ...]` before the final quit cleanup/unwind. Common inline compatibility forms are also recognized, including `ON SHUTDOWN CLEAR EVENTS` and close-style cleanup commands such as `CLOSE ALL`, `CLOSE TABLES`, and `CLOSE DATABASE(S) [ALL]`. `ON('SHUTDOWN')` now reports the active shutdown clause for runtime introspection. Shutdown handlers can still perform explicit cleanup such as `CLEAR EVENTS`, `CLOSE DATABASES ALL`, and even a nested `QUIT`; Copperfin treats that nested quit as the final terminal quit instead of recursively dispatching shutdown again.

The current PRG-engine slice now also supports first-pass `WITH/ENDWITH` member binding for leading-dot object access and first-pass `TRY/CATCH/FINALLY/ENDTRY` control flow, plus first-pass `DO ... WITH @var` reference semantics so `LPARAMETERS` assignments can write back into caller variables where needed for FoxPro-style procedure behavior. Error-pause stack metadata now also uses the actual faulting line for the top frame when nested routines fault instead of the post-incremented next statement. Inline `&&` comments now stay out of nested double-quoted and braced text during logical-line parsing, so macro-heavy expression text is less likely to be truncated before execution.

## Immediate Runtime Backlog Derived From The Official Reference

The official command inventory is much larger than the current runtime. The deepest next command groups should stay focused on data/runtime compatibility first:

### Active Issue Mapping For This Runtime Backlog

- command-surface and expression/runtime-state depth: #7, #8
- OLE/COM and automation compatibility: #10, #11
- runtime fault containment and diagnostics beyond the now-closed automation lane: #13, #14
- shared design-model and memo-heavy designer asset preservation: #22
- document/vector and AI planning policy: #30, #31

### Runtime Arrays And Expressions

- keep array compatibility work focused on official helper semantics and real FoxPro migration idioms; the current surface covers declared arrays, table/array transfer commands, metadata helpers, mutation helpers, sorting, scanning, and common row/column copy workflows, but it is still a first-pass runtime model rather than a complete VFP array engine
- `ASCAN()` macro-expanded predicate fidelity and metadata isolation are now closed: variable-reference predicates (`ASCAN(arr, cVarPred, ...)`, first-hop macro-expanded predicates (`ASCAN(arr, &cMacroPred, ...)`), and second-hop holder-chain predicates (`ASCAN(arr, &cPredDeepHolder, ...)`) all resolve to the correct block expression, and all four `_ASCAN*` metadata variables (`_ASCANVALUE`, `_ASCANINDEX`, `_ASCANROW`, `_ASCANCOLUMN`) are fully restored to their pre-scan state on every exit path including match, no-match, and early-return; focused regression coverage proves each of these paths in `test_ascan_macro_expanded_predicate_and_metadata_cleanup` (issue #391)
- continue moving new array regressions into `tests/test_prg_engine_arrays.cpp` / `test_prg_engine_arrays` instead of expanding the legacy `test_prg_engine.cpp` catch-all
- continue moving standalone expression/function regressions into `tests/test_prg_engine_functions.cpp` / `test_prg_engine_functions` instead of expanding the legacy `test_prg_engine.cpp` catch-all
- keep expression-function additions in reference-derived batches rather than one helper at a time; after shipping `FORCEEXT()`, `FORCEPATH()`, `CHRTRANC()`, adjacent date/time batches (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, `CTOT()`, `DTOT()`, `TTOD()`, `HOUR()`, `MINUTE()`, `SEC()`), the numeric trig/log helper batch (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`), the first-pass phonetic string helpers (`SOUNDEX()`, `DIFFERENCE()`), the mixed utility batch (`MIN()` / `MAX()` expression lists, `STRCONV(..., 7/8)`, `ISLEADBYTE()`), and the path/color/random batch (`DEFAULTEXT()`, `CURDIR()`, `RGB()`, `RAND()`), continue with adjacent expression helpers from the reference inventory in similarly sized groups
- keep expression-function additions in reference-derived batches rather than one helper at a time; after shipping `FORCEEXT()`, `FORCEPATH()`, `CHRTRANC()`, adjacent date/time batches (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, `CTOT()`, `DTOT()`, `TTOD()`, `HOUR()`, `MINUTE()`, `SEC()`), the numeric trig/log helper batch (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`), the first-pass phonetic string helpers (`SOUNDEX()`, `DIFFERENCE()`), the mixed utility batch (`MIN()` / `MAX()` expression lists, `STRCONV(..., 7/8)`, `ISLEADBYTE()`), the path/color/random batch (`DEFAULTEXT()`, `CURDIR()`, `RGB()`, `RAND()`), and the financial/misc batch (`HEX()`, `FV()`, `PV()`, `PAYMENT()`, first-pass `TEXTMERGE()` with configurable delimiters, first-pass `EXECSCRIPT()` RETURN-pattern detection, first-pass `LOOKUP()` seek-and-evaluate, first-pass `NEWID()` UUID v4, `CPCURRENT()` / `CPCONVERT()` / `CPDBF()` code-page stubs, and headless stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()` / `VARREAD()`), continue with adjacent expression helpers from the reference inventory in similarly sized groups
- deepen `ISBLANK()` later when DBF field values preserve pristine blank-state metadata distinct from stored numeric zero or logical false; the current implementation covers expression-level empty/blank character values
- deepen `MLINE()` / `MEMLINES()` later with full compatibility flags and offset semantics beyond the current baseline; the current implementation now supports session-scoped `SET MEMOWIDTH` + `_MLINE`, explicit width overrides, optional tab-width expansion, CR hard breaks by default, optional LF hard-break mode via flag bit `1`, and optional start offsets

### Work Areas, Sessions, And Indexed Data

- finish the remaining alias/work-area edge cases across `SELECT`, `USE`, and nested data sessions after the first strict `USE ... IN <alias>` targeting pass, first-pass non-selected target preservation, plain `USE` current-work-area reuse, freed-work-area reuse plus side-effect-free `SELECT(0)` probing, stronger synthetic SQL cursor/session isolation, per-session SQL handle lifecycle cleanup, and the now-shipped session-local restoration coverage for `SET NEAR` and `SET DEFAULT TO`
- keep closing the remaining expression-driven work-area targeting edges after the shared designator-expression path now covers `SET FILTER ... IN`, `SELECT <expr>`, `USE ... IN <expr>`, and the shipped local `IN`-targeted data/search family (`SET ORDER`, `SEEK`, `LOCATE`, `SCAN`, `GO`, `SKIP`, `REPLACE`, `DELETE`, `RECALL`) for core local-cursor flows
- deepen order/search behavior beyond the current first-pass `SET ORDER TO TAG` / `SEEK` / `FOUND()` plus direction-control slice
- add adjacent data-navigation and data-search commands where VFP developers expect them to work together
- use the command inventory to pull the next search/index/data-session commands in families instead of one at a time

### Local Table Mutation And Query Flow

- continue beyond the new first-pass headless `BROWSE` lane toward the remaining interactive table-editing commands needed for real legacy business applications
- the shared DBF/FPT mutation path now covers shipped local-table write families used by current runtime flows: memo-backed pointer fields (`M`/`G`/`P`), fixed-width numeric/date families (`B`/`I`/`Y`/`T`), first-pass var-length families (`V`/`Q`), constrained `NULL` token mutation semantics, staged write safety for DBF/memo updates (including staged-artifact cleanup), physical `PACK` compaction, first-pass `PACK MEMO` sidecar rewrite/compaction, `ZAP` truncation, SQL-style insert rollback, first-pass `CREATE TABLE`, first-pass structural `ALTER TABLE`, and indexed-table plus unsupported-layout fail-fast guards until real structural/indexed storage fidelity exists
- extend the shipped filtering, browsing, locating, scanning, replacing, appending, SQL-style inserting/deleting, recalling, packing, zapping, first-pass structural `CREATE TABLE` / `ALTER TABLE`, aggregate-built-in, command-level `COUNT`/`SUM`/`AVERAGE` with first-pass scope/`WHILE`/`IN` plus first-pass `TO ARRAY`, first-pass `TOTAL` with `IN` targeting and local `N/F/I/Y` support, and `CALCULATE` families toward broader xBase parity
- deepen table-maintenance parity beyond the first-pass `PACK`/`ZAP`/`PACK MEMO` slice with real cross-process locking behavior, index rebuild/invalidated-order handling, and stronger diagnostics for unsafe structural layouts
- keep runtime semantics ahead of shell/designer work so the same engine can power both Visual Studio and the standalone IDE

### Native Control Flow

- extend the shipped `DO CASE/CASE/OTHERWISE/ENDCASE`, `DO WHILE/ENDDO`, `WITH/ENDWITH`, `TRY/CATCH/FINALLY`, `LOOP`/`CONTINUE`/`EXIT`, first-pass richer `ON ERROR`, and first-pass literal `TEXT/ENDTEXT` slice into the rest of the FoxPro control-flow surface
- keep tightening expression semantics around stored-expression evaluation, macro substitution, and runtime-state inspection beyond the first-pass `EVAL()` / `SET()` / `&macro` slice
- add the next adjacent control families beyond the shipped engine core as coherent batches instead of one syntax form at a time

### SQL Pass-Through And Remote Cursor Behavior

- add the broader SQL pass-through surface around the existing `SQLCONNECT` / `SQLEXEC` / `SQLDISCONNECT` slice
- bring remote cursor semantics closer to local cursor expectations where VFP does so
- use the general reference and local help corpus when Learn pages list a command but do not capture enough behavioral detail

### Automation And Compatibility Utilities

- keep host stability and debugger fault containment mandatory as the remaining Phase B runtime-safety work, now that the dedicated Phase A automation lane is closed

## How To Use The References

- Use the Learn language-reference index to decide what command/function families exist and to avoid missing major areas.
- Use the Learn general reference for file structures, table/index storage, and other low-level behavior.
- Use the generated local CHM command-topic index and topic manifest to jump from a command or `foxtools` function name to the installed VFP help page that describes its behavior.
- Use installed VFP 9 help and local source trees for product-era behavior and sample patterns.
- Use `FOXHELP.DBF` and its memo file as a fallback source for older semantics when Learn pages are missing, gated, or too shallow.
