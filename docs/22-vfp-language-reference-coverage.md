# VFP Language Reference Coverage

This document anchors Copperfin's command/runtime backlog to the official Visual FoxPro reference instead of memory.

This is a reference-coverage document, not the active slice-selection authority. Choose active work from live GitHub issue state, `agents.md`, `agent-handoff.md`, and `docs/23-phase-a-dependency-breakdown.md`; use this file when the chosen slice touches VFP/runtime language coverage.

- Runtime reflection note: documented VFP `PUTPEM(oObject, cPEMName, eValue)` is the canonical write-by-name function. Copperfin accepts `PUTPEM()` on the shared runtime path and retains the historical `SETPEM()` spelling as a compatibility alias; new VFP-facing examples and coverage should use `PUTPEM()`.
- Runtime/list-control focus note: native ListBox multi-select now preserves `ListIndex` as the focused row when `Selected(n) = .F.` clears that row, even when another row remains selected. Focused-row `ListItemID`, `Value`, and `DisplayValue` remain coherent with independent selection bits, while single-selection controls retain their existing fallback behavior; focused and derived-Init regressions are covered under #3217/#4420.

## Primary Sources

- Microsoft Learn language index:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/dd5f4hyy(v=vs.71)>
- Microsoft Learn general reference:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/h9f898ae(v=vs.71)>
- Microsoft Learn VFP home page:
  - <https://learn.microsoft.com/en-us/previous-versions/visualstudio/foxpro/mt490117(v=msdn.10)>
- Local fallback when Learn coverage is incomplete or too terse:
  - [`C:\vDosPlus\FPD26\FOXHELP.DBF`](C:/vDosPlus/FPD26/FOXHELP.DBF)
  - companion memo file in the same directory

## Generated Inventory

The generated command inventory lives here:

- [`docs/generated/vfp-language-reference-summary.json`](generated/vfp-language-reference-summary.json)
- [`docs/generated/vfp-language-reference-commands.json`](generated/vfp-language-reference-commands.json)
- [`docs/generated/vfp-language-reference-commands.txt`](generated/vfp-language-reference-commands.txt)
- [`docs/generated/vfp-chm-index-summary.json`](generated/vfp-chm-index-summary.json)
- [`docs/generated/vfp-chm-command-topics.json`](generated/vfp-chm-command-topics.json)
- [`docs/generated/vfp-foxtools-topics.json`](generated/vfp-foxtools-topics.json)
- [`docs/generated/vfp-chm-topic-manifest.json`](generated/vfp-chm-topic-manifest.json)

Refresh it with:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\export-vfp-reference-index.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\export-vfp-chm-topic-index.ps1
```

Current official index snapshot from the Learn language-reference page:

- `429` command entries
- `413` function entries
- `323` property entries
- `83` method entries
- `72` system variable entries
- `69` event entries
- `22` object entries
- `4` preprocessor-directive entries
- `3` operator entries

Current local CHM snapshot from the installed VFP help set:

- `dv_foxhelp.chm` is the main product help and the command-behavior source that matters most
- `foxtools.chm` is supplemental and much smaller
- the local indexer now emits both a command-topic view and a broader topic manifest, so it can capture `foxtools` functions that are not indexed as `Command` topics
- the generated CHM outputs give local keyword-to-HTML mappings for offline behavior lookup, including `RegFn()`, `RegFn32()`, and `CallFn()`

## Current Copperfin Runtime Surface

The native runtime/parser currently has first-pass support for these command families:

- Runtime/parser note: first-pass preprocessor handling now follows nearby `#INCLUDE` headers recursively with cycle protection, tolerates Windows-style backslash include paths, captures local and included `#DEFINE` constants, expands bare identifier constants outside string/bracket/brace literals, and now honors nested `#IF`, `#ELSE`, `#ENDIF`, `#IFDEF`, and `#IFNDEF` selection before ordinary PRG parsing. This intentionally covers common header-driven constant and guard/debug-selection paths such as `WINDOWTYPE_MODAL`, `DEFAULT_MBOX_TITLE_LOC`, and `COMPILEBEFORE` evidenced in mounted/extracted VFP9 and FFC sources, but it does not yet claim full FoxPro conditional-preprocessor parity or arbitrary compile-time function evaluation.
- Runtime/list-control note: first-pass native `ComboBox`/`ListBox` list-control row-state support now exists on the iterative PRG runtime path, directly evidenced by mounted VFP9 sample `samples/solution/forms/graphics/fdproc.prg`, extracted FFC usage in `_controls.vc2`, `_table.vc2`, `_internet.vc2`, `_ws3utils.vc2`, `_movers.vc2`, and extracted VFP source usage in `VFPSource/builders/wbgrid.prg`, plus the local VFP9 help topics for `AddItem Method (Visual FoxPro)`, `AddListItem Method`, `Clear Method`, `ListCount Property`, `List Property`, `ListItemID Property`, `NewIndex Property`, `NewItemID Property`, `RemoveItem Method`, `RemoveListItem Method`, `Selected Property`, and `SelectedID Property`. The shipped baseline covers same-session single-column runtime population, explicit 1-based indexed insertion, representative multicolumn `AddItem(..., nIndex, nColumn)` row insertion into the requested representative column, builtin `Clear()` behavior for `RowSourceType = 0` with a no-op gate on other row-source modes, read-only `ListCount`, read-only `NewIndex`, read-only `NewItemId`, representative single-column and multicolumn `List(nIndex, nColumn)` reads, representative writable `List(NewIndex, nColumn)` addressing across row insert/remove churn, writable `ListItemID` selection routing, representative `ListBox.Selected(nIndex)` reads/writes over preserved per-row selection bits, representative `SelectedID(nItemID)` reads/writes over the same live item-id row state, representative `RemoveListItem(nItemID)` removal by stable row id, built-in reflection for `BoundTo`, `ListBox.BoundColumn`, `ListBox.ColumnCount`, `ListBox.ColumnWidths`, `AddItem()` / `AddListItem()` / `Clear()` / `RemoveItem()` / `RemoveListItem()` plus `ListItemID`, `SETPEM(..., 'Selected(n)', ...)`, and `SETPEM(..., 'SelectedID(n)', ...)` through `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, and coherent `ListIndex` / `NewIndex` / `ListItemID` / `DisplayValue` synchronization when rows are inserted, shifted, selected, removed, or cleared. It intentionally does not yet claim full sorted-list semantics or full `MultiSelect` surface modeling; current runtime behavior returns an empty/zero result for unsupported remaining lanes instead of fabricating partial state.
- Runtime/pageframe note: representative same-session native `PageFrame` collection parity now exists on the iterative PRG runtime path, evidenced by the local VFP9 help topics for `Pages Property`, `PageCount Property`, and `ActivePage Property` together with repo-local extracted-source citations that use `Pages[m.j]`. The shipped baseline now synthesizes a hidden read-only `Pages` collection over live `Page` children, preserves `Pages.Count`, default-item `Pages(n)` access, bracket `Pages[n]` access, and `FOR EACH ... FOXOBJECT` iteration without leaking the synthetic `Collection` object into exported runtime state, keeps read-only `PageCount` synchronized through same-session page add/remove flows, and now exposes first-pass writable `ActivePage` selection with 1-based bounded page indexes, deterministic empty-page `0` behavior, and reflection coverage through `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()`.
- Runtime/tab-order note: first-pass native `TabIndex` property parity now exists on the iterative PRG runtime path for the documented VFP9 help applies-to set that Copperfin currently models as same-session native visual controls. Evidenced by the mounted VFP9 `TabIndex Property` topic from `dv_foxhelp.chm`, the shipped baseline seeds built-in `TabIndex` values by add-order, preserves class-defined overrides, keeps ordinary reads/writes and `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` coherent for plain, child, nested-child, and derived controls, and blocks `ADDPROPERTY()` / `REMOVEPROPERTY()` from shadowing or erasing the built-in surface. This slice is intentionally property-surface-only and does not yet claim full keyboard-navigation or tab-stop traversal parity.
- Runtime/tab-order note: first-pass native `TabStop` property parity now exists on the iterative PRG runtime path for the documented VFP9 help applies-to set that Copperfin currently models as same-session native visual controls. Evidenced by the mounted VFP9 `TabStop Property` topic from `dv_foxhelp.chm`, the shipped baseline seeds built-in `TabStop = .T.` state by default, preserves class-defined overrides, keeps ordinary reads/writes and `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` coherent for plain, child, nested-child, and derived controls, and blocks `ADDPROPERTY()` / `REMOVEPROPERTY()` from shadowing or erasing the built-in surface. This slice is intentionally property-surface-only and does not yet claim full keyboard-navigation or focus-traversal parity.
- E3 designer/tooling mounted real-sample root sort-setting round-trip guard note — Studio host regression coverage now runs against writable copies of the mounted VFP9 `invoice.frx` / `invoice.frt` and `cust.lbx` / `cust.lbt` samples, proving root `TAG` updates persist through the shared FRX/LBX host, refresh selected-settings JSON with stable record/field/memo provenance, raise live root-setting counts from five to six after set, and clear back to five live settings while removing `TAG` from selected settings without fabricating deleted settings. The same mounted-sample guard also documents the current memo-backed clear contract explicitly: a set-then-clear cycle leaves the visible `TAG` value empty but still advances the memo allocation path, so primary and sidecar hashes do not rewind to the original sample bytes.
- E3 designer/tooling deleted detail header/footer object vertical distribution JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode vertical --distribute-target-unique-id ... --json` preserve deleted state, persist refreshed vertical geometry, preserve horizontal geometry and size, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object vertical distribution JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode vertical --distribute-target-unique-id ... --json` persist refreshed vertical geometry, preserve horizontal geometry and size, keep containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object distribution JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode horizontal --distribute-target-unique-id ... --json` preserve deleted state, persist refreshed horizontal geometry, preserve vertical geometry and size, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object distribution JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects distributed by `--distribute-object --distribution-mode horizontal --distribute-target-unique-id ... --json` persist refreshed horizontal geometry, preserve vertical geometry and size, keep containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object nudge JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects nudged by `--nudge-object --nudge-mode both --delta-hpos ... --delta-vpos ... --nudge-target-unique-id ... --json` preserve deleted state, persist refreshed position geometry, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object nudge JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects nudged by `--nudge-object --nudge-mode both --delta-hpos ... --delta-vpos ... --nudge-target-unique-id ... --json` persist refreshed position geometry, preserve size geometry, keep containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object snap JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects snapped by `--snap-object --snap-mode both --grid-width ... --grid-height ... --snap-target-unique-id ... --json` preserve deleted state, persist refreshed position geometry, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object snap JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects snapped by `--snap-object --snap-mode both --grid-width ... --grid-height ... --snap-target-unique-id ... --json` persist refreshed position geometry, preserve size geometry, keep containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object resize JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects resized by `--resize-object --resize-mode size --anchor-unique-id ... --resize-target-unique-id ... --json` preserve deleted state, persist refreshed size geometry, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object resize JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects resized by `--resize-object --resize-mode size --anchor-unique-id ... --resize-target-unique-id ... --json` persist refreshed size geometry, preserve position geometry, keep containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object alignment JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects aligned by `--align-object --alignment-mode left --anchor-unique-id ... --align-target-unique-id ... --json` preserve deleted state, persist refreshed geometry, keep selected containing-section availability false/null instead of fabricating live section membership, refresh selected deleted-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object alignment JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects aligned by `--align-object --alignment-mode left --anchor-unique-id ... --align-target-unique-id ... --json` persist refreshed geometry, preserve containing-section ids/record indexes, section-relative coordinates, object indexes/counts, selected containing-section JSON, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object reorder JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects reordered by `--reorder-object --unique-id ... --placement ... --target-unique-id ... --json` preserve deleted state, expression and geometry metadata, selected-object JSON, null selected containing-section JSON, and label identity after physical record movement.
- E3 designer/tooling detail header/footer object reorder JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects reordered by `--reorder-object --unique-id ... --placement ... --target-unique-id ... --json` preserve containing-section membership, expression and geometry metadata, selected-object and selected containing-section JSON, and label identity while same-geometry objects in the same Detail Header/Footer section report section object indexes/counts according to the reordered record order.
- E3 designer/tooling deleted detail header/footer object rename JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects renamed by `--rename-object --unique-id ... --new-unique-id ... --json` replace the requested identity without appending records, preserve deleted state, expression, and geometry metadata, keep selected containing-section availability false/null instead of fabricating live section membership, refresh deleted-object and selected-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object rename JSON guard note — Studio host report/label layout JSON has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects renamed by `--rename-object --unique-id ... --new-unique-id ... --json` replace the requested identity without appending records, preserve expression and geometry metadata, preserve containing-section ids/record indexes plus section object indexes/counts, refresh selected-object and selected containing-section JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling deleted object duplicate JSON guard note — visual-object duplication now preserves the source DBF deleted flag for appended duplicates, and Studio host report/label layout JSON has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects duplicated by `--duplicate-object --unique-id ... --new-unique-id ... --json` append deleted replacement records, preserve expression and geometry metadata, keep selected containing-section availability false/null instead of fabricating live section membership, refresh deleted-object/selected-object JSON, keep inspection non-failing, and retain label identity.
- E3 designer/tooling ambiguous stable report-selection JSON guard note — Studio host report/label stable selector resolution now refuses duplicate `UNIQUEID` matches instead of selecting an arbitrary row, with regression coverage proving ambiguous FRX/LBX object, section, and settings selectors, including duplicates split across live/deleted rows and duplicate rows split between the default preview window and deeper layout/section/settings records, keep inspection non-failing, expose explicit no-selection metadata, clear selected object/section/settings/containing-section JSON to false/null, preserve live/deleted layout and page/setup counts, and retain label identity.
- E3 designer/tooling out-of-range record-selection JSON guard note — Studio host report/label explicit `--record` selection now clears launch-selection record availability when the requested FRX/LBX record is beyond the available table records, preserves the requested record index for diagnostics, avoids falling back to a conflicting stable `--unique-id`, and exposes explicit no-selection report metadata while preserving layout counts and label identity.
- E3 designer/tooling negative layout-dimension JSON guard note — Studio host report/label layout JSON now has regression coverage proving negative section heights and negative live/deleted object WIDTH/HEIGHT values keep FRX/LBX inspection non-failing, clamp selected-object and preview geometry to non-inverted zero-size bounds, preserve live/deleted counts, and retain label identity.
- E3 designer/tooling malformed numeric layout JSON guard note — Studio host report/label layout JSON now has regression coverage proving malformed section VPOS/HEIGHT and malformed live/deleted object HPOS/VPOS/WIDTH/HEIGHT values keep FRX/LBX inspection non-failing, default selected-object and preview geometry to non-inverted zero-origin zero-size bounds, preserve live/deleted counts, and retain label identity.
- E3 designer/tooling oversized numeric layout JSON guard note — Studio host report/label layout JSON now has regression coverage proving out-of-range section VPOS/HEIGHT and out-of-range live/deleted object HPOS/VPOS/WIDTH/HEIGHT values keep FRX/LBX inspection non-failing, default selected-object and preview geometry to non-inverted zero-origin zero-size bounds, preserve live/deleted counts, and retain label identity.
- E3 designer/tooling invalid classification JSON guard note — Studio host report/label layout JSON now has regression coverage proving malformed and out-of-range OBJTYPE/OBJCODE values keep FRX/LBX inspection non-failing, avoid creating false report sections or layout objects, degrade explicit record selection to no selected report metadata, preserve root settings, and retain label identity.
- E3 designer/tooling invalid direct page-setup JSON guard note — Studio host report/label layout JSON now has regression coverage proving malformed direct ORIENTATION/PAPERSIZE/TOPMARGIN values on FRX/LBX root settings rows keep inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep page setup availability false with inert default numeric summary values, and retain label identity.
- E3 designer/tooling invalid direct column-setup JSON guard note — Studio host report/label layout JSON now has regression coverage proving malformed direct COLS/COLWIDTH/COLSPACING values on FRX/LBX root settings rows keep inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep column setup availability false with inert default numeric summary values, and retain label identity.
- E3 designer/tooling invalid direct margin/grid JSON guard note — Studio host report/label layout JSON now has regression coverage proving malformed direct BOTMARGIN/GRIDV/GRIDH values on FRX/LBX root settings rows keep inspection non-failing, preserve raw setting provenance in summary and selected-settings JSON, keep page setup availability false with inert default numeric margin/grid summary values, and retain label identity.
- E3 designer/tooling unresolved report memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX memo placeholder text in root settings and layout-object expression/font/mode fields keeps inspection non-failing, stays suppressed from summary and selected metadata, does not fabricate settings/page setup availability, preserves selected-object metadata with empty expression/highlight state, and retains label identity.
- E3 designer/tooling unresolved report section memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX section EXPR memo placeholder text keeps summary and selected-section inspection non-failing, suppresses section expression text/provenance, avoids leaking placeholder text, preserves live/deleted section counts, heights, and band metadata, and retains label identity.
- E3 designer/tooling unresolved deleted report object memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX deleted layout-object EXPR/FONTFACE/FONTSIZE/MODE memo placeholder text keeps deleted-object summary and selected-object inspection non-failing, suppresses expression/highlight text, avoids leaking placeholder text, preserves deleted object geometry and metadata, keeps containing-section metadata unavailable/null, and retains label identity.
- E3 designer/tooling unresolved unplaced report object memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX live unplaced layout-object EXPR/FONTFACE/FONTSIZE/MODE memo placeholder text keeps unplaced-object summary and selected-object inspection non-failing, suppresses expression/highlight text, avoids leaking placeholder text, preserves object geometry and metadata, keeps containing-section metadata unavailable/null, and retains label identity.
- E3 designer/tooling unresolved report geometry memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX section and layout-object HPOS/VPOS/WIDTH/HEIGHT memo placeholder text keeps summary and selected-object inspection non-failing, defaults geometry to zero-origin zero-size non-inverted bounds with field provenance, avoids leaking placeholder text, preserves live/deleted object counts, and retains label identity.
- E3 designer/tooling unresolved report direct-setting memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving unresolved FRX/LBX direct page setup, margin/grid, and column setup memo placeholder text keeps summary and live/deleted selected-settings inspection non-failing, suppresses live/deleted settings, avoids fabricating page or column setup availability, avoids leaking placeholder text, and retains label identity.
- E3 designer/tooling mixed report direct-setting memo placeholder JSON guard note — Studio host report/label layout JSON now has regression coverage proving valid FRX/LBX direct page setup, margin/grid, and column setup fields keep producing summary setup availability and live/deleted selected-settings metadata when sibling fields contain unresolved memo placeholder text, while placeholder fields stay suppressed and do not leak into JSON.
- E3 designer/tooling blank report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving blank and whitespace-only FRX/LBX direct page setup, margin/grid, and column setup fields are skipped from summary settings, selected-settings metadata, and page/column setup availability while inspection remains non-failing and label identity is retained.
- E3 designer/tooling mixed invalid report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving valid FRX/LBX direct page setup, margin/grid, and column setup fields still produce setup summaries when sibling direct fields contain malformed non-numeric values, while malformed fields remain inspectable as raw live/deleted selected-settings metadata.
- E3 designer/tooling trimmed report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving leading/trailing whitespace around FRX/LBX direct page setup, margin/grid, and column setup fields is trimmed before summary setup derivation, while live/deleted selected-settings metadata exposes normalized values and label identity is retained.
- E3 designer/tooling fractional report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving fractional numeric text in FRX/LBX direct page setup, margin/grid, and column setup fields derives summary setup values from integer portions, while live/deleted selected-settings metadata preserves fractional source values and label identity is retained.
- E3 designer/tooling oversized report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving oversized integer text in FRX/LBX direct page setup, margin/grid, and column setup fields remains inspectable in live/deleted selected-settings metadata without fabricating page or column setup availability, while label identity is retained.
- E3 designer/tooling dot-leading report direct-setting JSON guard note — Studio host report/label layout JSON now has regression coverage proving decimal text without an integer portion in FRX/LBX direct page setup, margin/grid, and column setup fields remains inspectable in live/deleted selected-settings metadata without fabricating page or column setup availability, while label identity is retained.
- E3 designer/tooling detail header/footer object duplicate containment note — Studio host report/label layout JSON now has regression coverage proving stable-selected Detail Header and Detail Footer FRX/LBX objects duplicated by `UNIQUEID` append replacement-`UNIQUEID` records, preserve object metadata, remain placed in the correct containing sections, refresh section object indexes/counts, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object delete/restore containment note — Studio host report/label layout JSON now has regression coverage proving stable-selected live Detail Header FRX/LBX objects delete into the deleted-object collection with containing-section metadata unavailable/null, while stable-selected deleted Detail Footer FRX/LBX objects restore into live containing-section membership with refreshed section-relative metadata, object counts, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object geometry edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects support `VPOS` set and `HEIGHT` clear operations by `UNIQUEID`, preserve deleted state, persist refreshed or blank geometry fields, refresh deleted-object and selected-object bounds, keep selected containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object geometry edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects support `VPOS` set and `HEIGHT` clear operations by `UNIQUEID`, persist refreshed or blank geometry fields, refresh selected-object bounds and section-relative coordinates, preserve containing-section metadata/object counts, keep inspection non-failing, and retain label identity.
- E3 designer/tooling deleted detail header/footer object font-option edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects support `FONTSIZE` set and `MODE` clear operations by `UNIQUEID`, preserve deleted state, persist refreshed field values or blank clears, refresh deleted-object and selected-object highlight provenance, keep selected containing-section metadata unavailable/null instead of fabricating live section membership, remove stale cleared-option highlights, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object font-option edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects support `FONTSIZE` set and `MODE` clear operations by `UNIQUEID`, persist refreshed field values or blank clears, refresh selected-object highlight provenance, preserve containing-section metadata, remove stale cleared-option highlights, keep inspection non-failing, and retain label identity.
- E3 designer/tooling deleted detail header/footer object-font edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects support `FONTFACE` set/clear operations by `UNIQUEID`, preserve deleted state, persist refreshed or blank memo data, refresh deleted-object and selected-object highlight provenance, keep selected containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object-font edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects support `FONTFACE` set/clear operations by `UNIQUEID`, persist refreshed or blank memo data, refresh selected-object highlight provenance, preserve containing-section metadata, keep inspection non-failing, and retain label identity.
- E3 designer/tooling deleted detail header/footer object-font selection note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects expose deleted-object and selected-object highlight counts plus `FONTFACE`, `FONTSIZE`, and `MODE` provenance while preserving deleted state, expression metadata, unavailable/null containing-section metadata, non-failing inspection, and label identity.
- E3 designer/tooling detail header/footer object-font selection note — Studio host report/label layout JSON now has regression coverage proving stable-selected live Detail Header/Footer FRX/LBX objects expose selected-object highlight counts plus `FONTFACE`, `FONTSIZE`, and `MODE` provenance while preserving expression metadata, containing-section metadata, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer object-expression edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects support `EXPR` set/clear operations by `UNIQUEID`, preserve deleted state, refresh deleted-object and selected-object expression text/provenance, keep selected containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- E3 designer/tooling deleted detail header/footer object-expression selection note — Studio host report/label layout JSON now has regression coverage proving stable-selected deleted Detail Header/Footer FRX/LBX objects expose deleted-object and selected-object expression text/provenance, preserve deleted state, keep selected containing-section metadata unavailable/null instead of fabricating live section membership, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object-expression edit note — Studio host report/label layout JSON now has regression coverage proving stable-selected Detail Header/Footer FRX/LBX objects support `EXPR` set/clear operations by `UNIQUEID`, persist refreshed or blank memo data, refresh selected-object expression text/provenance plus containing-section metadata, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer object-expression selection note — Studio host report/label layout JSON now has regression coverage proving stable-selected live FRX/LBX objects placed inside Detail Header and Detail Footer bands expose selected-object expression text, field/memo provenance, object kind, containing-section ids/record indexes, section-relative coordinates, containing-section object counts, non-failing inspection, and label identity.
- E3 designer/tooling stable detail header/footer section-expression selection note — Studio host report/label layout JSON now has regression coverage proving live Detail Header and Detail Footer FRX/LBX sections selected by `UNIQUEID` expose selected-section expression text, field/memo provenance, object-code and geometry metadata, null unrelated report-selection surfaces, non-failing inspection, and label identity.
- E3 designer/tooling stable deleted detail header/footer section-selection note — Studio host report/label layout JSON now has regression coverage proving deleted Detail Header and deleted Detail Footer FRX/LBX sections selected by `UNIQUEID` expose deleted-section and selected-section metadata, expression provenance, section counts, null unrelated report-selection surfaces, non-failing inspection, and label identity.
- E3 designer/tooling deleted detail header/footer section-expression edit note — Studio host report/label layout JSON now has regression coverage proving `--set-property` on deleted Detail Header `EXPR` records and `--clear-property` on deleted Detail Footer `EXPR` records refresh deleted-section and selected-section expression JSON, persist/clear EXPR memo data, preserve live Detail sibling metadata, keep inspection non-failing, and retain label identity.
- E3 designer/tooling detail header/footer section-expression edit note — Studio host report/label layout JSON now has regression coverage proving `--set-property` and `--clear-property` refresh Detail Header and Detail Footer selected-section expression JSON, persist/clear EXPR memo data, preserve sibling section expressions, and retain label identity.
- E3 designer/tooling detail header/footer object containment note — Studio host report/label layout JSON now has regression coverage proving stable-selected FRX/LBX objects inside Detail Header and Detail Footer bands retain containing-section ids, section-relative coordinates, section object index/count metadata, selected containing-section JSON, and label identity.
- E3 designer/tooling detail header/footer section-kind note — Studio host report/label layout JSON now has regression coverage proving Detail Header and Detail Footer band rows surface as `detail_header` and `detail_footer` live/deleted section-kind buckets plus stable selected-section title/band-kind/object-code metadata while retaining label identity.
- E3 designer/tooling extended report object-kind note — Studio host report/label layout JSON now has regression coverage proving rectangle, picture, and variable FRX/LBX layout objects surface as distinct live, unplaced, deleted, and stable selected-object `objectKind` values while retaining label identity.
- E3 designer/tooling unsupported OBJTYPE code note — Studio host report/label layout JSON now has regression coverage proving structurally valid but unsupported OBJTYPE values keep inspection non-failing, avoid fabricating root settings, sections, layout objects, preview bounds, or selected report metadata, and retain label identity.
- E3 designer/tooling missing OBJTYPE schema with OBJCODE note — Studio host report/label layout JSON now has regression coverage proving FRX/LBX rows with OBJCODE present but no OBJTYPE field keep inspection non-failing, avoid inferring root settings, sections, layout objects, or preview bounds from OBJCODE-only rows, degrade explicit record selection to no selected report metadata, and retain label identity.
- E3 designer/tooling unknown band-code JSON fallback note — Studio host report/label layout JSON now has regression coverage proving structurally valid FRX/LBX band rows with unknown OBJCODE values keep inspection non-failing, serialize live/deleted sections as `Other Band` / `bandKind: "other"`, preserve section geometry and preview/deleted-preview bounds, populate selected-section metadata, and retain label identity.
- E3 designer/tooling missing root EXPR schema note — Studio host report/label layout JSON now has regression coverage proving FRX/LBX root settings rows without an EXPR column keep inspection non-failing, preserve direct ORIENTATION/PAPERSIZE/TOPMARGIN live and deleted setting metadata, keep page setup summaries available from direct fields, populate selected-settings metadata, and retain label identity.
- E3 designer/tooling missing section EXPR schema note — Studio host report/label layout JSON now has regression coverage proving FRX/LBX section rows without an EXPR column keep inspection non-failing, preserve live/deleted band title/kind metadata and geometry, serialize empty expressions with null field provenance, populate selected-section metadata, and retain label identity.
- E3 designer/tooling fractional layout geometry note — Studio host report/label layout JSON now has regression coverage proving fractional section VPOS/HEIGHT and fractional live/deleted object HPOS/VPOS/WIDTH/HEIGHT values use integer portions consistently for selected-object and preview/deleted-preview geometry while preserving counts and label identity.
- E3 designer/tooling missing layout geometry schema note — Studio host report/label layout JSON now has regression coverage proving FRX/LBX section/object rows without HPOS/VPOS/WIDTH/HEIGHT columns keep inspection non-failing, default selected-object and preview/deleted-preview geometry to zero-origin zero-size bounds, serialize selected-object geometry field provenance as null, and retain label identity.
- E3 designer/tooling missing classification schema note — Studio host report/label layout JSON now has regression coverage proving FRX/LBX rows without OBJTYPE/OBJCODE columns keep inspection non-failing, avoid inferring root settings, sections, layout objects, or preview bounds from unrelated rows, degrade explicit record selection to no selected report metadata, and retain label identity.
- E3 designer/tooling blank stable report-selection JSON guard note — Studio host report/label stable selector resolution now has regression coverage proving whitespace-only `--unique-id` requests are treated as no stable selector, do not match blank stored FRX/LBX `UNIQUEID` fields, keep inspection non-failing, expose explicit no-selection metadata, and retain label identity.
- E3 designer/tooling missing stable report-selection schema note — Studio host report/label stable selector resolution now has regression coverage proving FRX/LBX tables without a `UNIQUEID` field keep `--unique-id` inspection non-failing, expose explicit no-selection metadata, preserve live section/settings/object counts, and retain label identity without arbitrary record selection.
- E3 designer/tooling deep explicit report-record selection note — Studio host report/label `--record` selection now parses through the requested record instead of stopping at the default preview window, with regression coverage proving deep FRX/LBX layout object, section, and root settings records can populate selected JSON by explicit record index while unrelated selected surfaces stay false/null.
- E3 designer/tooling deep stable report-selection JSON note — Studio host report/label stable `--unique-id` selection now loads the full FRX/LBX table for selector resolution instead of only the default preview window, with regression coverage proving deep layout object, section, and root settings rows beyond the first eight records can still populate the matching selected-object, selected-section, or selected-settings JSON while unrelated selected surfaces stay false/null.
- E3 designer/tooling normalized stable report-selection JSON note — Studio host report/label stable selector resolution now has regression coverage proving padded stored FRX/LBX `UNIQUEID` values can be selected by trimmed case-varied `--unique-id` requests across layout object, section, and root settings rows, with the selected object/section/settings JSON populated and unrelated selected surfaces kept false/null.
- E3 designer/tooling record/stable selector precedence note — Studio host report/label JSON now has regression coverage proving explicit FRX/LBX `--record` selections remain authoritative when a conflicting `--unique-id` is also supplied, so record-selected section/settings rows stay selected and the conflicting selected-object surfaces remain false/null.
- E3 designer/tooling missing stable report-selection JSON note — Studio host JSON now has regression coverage proving missing FRX/LBX stable unique-id selectors keep report/label inspection non-failing, expose explicit no-selection metadata, clear selected object/section/settings/containing-section JSON to false/null, preserve live/deleted layout counts, and retain label identity without leaking stale selection data.
- E3 designer/tooling stable-selector deleted summary report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside summary-section geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector summary report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside summary sections can be selected by stable unique id with selected-object availability/kind, summary containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted column-header report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside column-header geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted column-footer report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside column-footer geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector group-header report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside group-header sections can be selected by stable unique id with selected-object availability/kind, group-header containing-section metadata, expression provenance, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector group-footer report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside group-footer sections can be selected by stable unique id with selected-object availability/kind, group-footer containing-section metadata, expression provenance, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted group-header report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside group-header geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted group-footer report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside group-footer geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector summary report-section selection note — Studio host JSON now has regression coverage proving FRX/LBX summary sections can be selected by stable unique id with selected-section availability/kind, summary band metadata, sibling detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted summary report-section selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX summary sections can be selected by stable unique id with selected-section availability/kind, deleted summary band metadata, live sibling detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted page-footer report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside page-footer geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted title-band report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX label objects inside title-band geometry can be selected by stable unique id with selected-object availability/kind, deleted-object metadata and geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector column-footer report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside column-footer sections can be selected by stable unique id with selected-object availability/kind, column-footer containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector column-header report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside column-header sections can be selected by stable unique id with selected-object availability/kind, column-header containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector page-footer report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside page-footer sections can be selected by stable unique id with selected-object availability/kind, page-footer containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector title-band report-object selection note — Studio host JSON now has regression coverage proving FRX/LBX label objects inside title sections can be selected by stable unique id with selected-object availability/kind, title containing-section metadata, section-relative geometry, live/deleted section and object counts, null selected section/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted column-footer report-section selection note — Studio host JSON now has regression coverage proving deleted column-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted column-footer band metadata, live sibling column-header/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted column-header report-section selection note — Studio host JSON now has regression coverage proving deleted column-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted column-header band metadata, live sibling detail/column-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted page-footer report-section selection note — Studio host JSON now has regression coverage proving deleted page-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted page-footer band metadata, live sibling title/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted title report-section selection note — Studio host JSON now has regression coverage proving deleted title FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector column-footer report-section selection note — Studio host JSON now has regression coverage proving column-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, column-footer band metadata, live sibling column-header/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector column-header report-section selection note — Studio host JSON now has regression coverage proving column-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, column-header band metadata, live sibling detail/column-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector page-footer report-section selection note — Studio host JSON now has regression coverage proving page-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, page-footer band metadata, live sibling title/detail metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector title report-section selection note — Studio host JSON now has regression coverage proving title FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted orphaned detail report-object selection note — Studio host JSON now has regression coverage proving deleted detail FRX/LBX label objects whose containing detail section is also deleted can be selected by stable unique id with selected-object availability/kind, expression provenance, geometry, live/deleted section and object counts, null selected section/settings metadata, null containing-section metadata, sibling page-header metadata, and label identity preserved.
- E3 designer/tooling stable-selector orphaned detail report-object selection note — Studio host JSON now has regression coverage proving live detail FRX/LBX field objects whose containing detail section is deleted can be selected by stable unique id with selected-object availability/kind, expression provenance, geometry, live/unplaced/deleted object counts, null selected section/settings metadata, null containing-section metadata, sibling page-header metadata, and label identity preserved.
- E3 designer/tooling stable-selector orphaned page-header report-object selection note — Studio host JSON now has regression coverage proving live page-header FRX/LBX label objects whose containing page-header section is deleted can be selected by stable unique id with selected-object availability/kind, expression provenance, geometry, live/unplaced/deleted object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted page-header report-object selection note — Studio host JSON now has regression coverage proving deleted page-header FRX/LBX label objects can be selected by stable unique id with selected-object availability/kind, expression provenance, geometry, live/deleted/placed object counts, null selected section/settings metadata, null containing-section metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted group-footer report-section selection note — Studio host JSON now has regression coverage proving deleted group-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted group-footer band metadata, expression provenance, live/deleted section counts, sibling group-header/detail metadata, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted group-header report-section selection note — Studio host JSON now has regression coverage proving deleted group-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted group-header band metadata, expression provenance, live/deleted section counts, sibling detail/footer metadata, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector group-footer report-section selection note — Studio host JSON now has regression coverage proving live group-footer FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, group-footer band metadata, expression provenance, sibling group-header/detail section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector group-header report-section selection note — Studio host JSON now has regression coverage proving live group-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, group-header band metadata, expression provenance, sibling detail/footer section counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector deleted page-header report-section selection note — Studio host JSON now has regression coverage proving deleted page-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, deleted page-header band metadata, live/deleted section counts, unplaced object accounting for former section members, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector page-header report-section selection note — Studio host JSON now has regression coverage proving live page-header FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, page-header band metadata, section object membership, live/deleted section and object counts, null selected object/settings metadata, and label identity preserved.
- E3 designer/tooling stable-selector page-header report-object selection note — Studio host JSON now has regression coverage proving live page-header FRX/LBX layout objects can be selected by stable unique id with selected-object availability/kind, label expression/provenance, page-header containing-section metadata, section-relative geometry, label identity, and live/deleted object counts preserved.
- E3 designer/tooling stable-selector deleted unplaced report-object selection note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout objects can be moved outside section bounds and selected by stable unique id with selected-object availability/kind, live/deleted/unplaced object counts, deleted-unplaced accounting, null containing-section metadata, label identity, geometry, and provenance preserved.
- E3 designer/tooling stable-selector unplaced report-object selection note — Studio host JSON now has regression coverage proving live unplaced FRX/LBX layout objects can be selected by stable unique id with selected-object availability/kind, live/deleted/unplaced object counts, null containing-section metadata, label identity, geometry, and provenance preserved.
- E3 designer/tooling stable-selector report-object selection note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects and deleted FRX/LBX label objects can be selected by stable unique id with selected-object availability/kind, live/deleted object counts, live containing-section metadata, deleted-object null containing-section metadata, label identity, geometry, and provenance preserved.
- E3 designer/tooling stable-selector report-section selection note — Studio host JSON now has regression coverage proving live and deleted FRX/LBX sections can be selected by stable unique id with selected-section availability/kind, live/deleted section counts, unplaced object accounting, null settings/object selections, label identity, and section metadata preserved.
- E3 designer/tooling stable-selector report root-settings selection note — Studio host JSON now has regression coverage proving live and deleted root FRX/LBX settings can be selected by stable unique id with selected-settings availability/kind, live/deleted setting counts, page setup state, null section/object selections, and setting provenance preserved.
- E3 designer/tooling stable-selector deleted report root-settings restore note — Studio host JSON now has regression coverage proving deleted root FRX/LBX settings can be restored by stable unique id with the DBF deleted flag cleared, live setting/page setup summaries restored, deleted setting counts cleared, and selected-settings metadata rehydrated.
- E3 designer/tooling stable-selector report root-settings delete note — Studio host JSON now has regression coverage proving root FRX/LBX settings can be deleted by stable unique id with the DBF deleted flag set, live setting/page setup summaries cleared, and deleted plus selected deleted-settings metadata exposed.
- E3 designer/tooling missing root OBJCODE schema note — Studio host JSON now has regression coverage proving FRX/LBX root/settings rows with valid OBJTYPE but no OBJCODE field keep inspection non-failing, preserve EXPR-derived grid/column settings and direct page setup fields with provenance, populate selected-settings JSON for live/deleted explicit record selection, and retain label identity.
- E3 designer/tooling missing object OBJCODE schema note — Studio host JSON now has regression coverage proving FRX/LBX object rows with valid OBJTYPE but no OBJCODE field keep inspection non-failing, preserve OBJTYPE-derived field/label kinds, serialize default object-code metadata with null provenance, preserve unplaced live/deleted object accounting and selected-object JSON, and retain label identity.
- E3 designer/tooling missing section OBJCODE schema note — Studio host JSON now has regression coverage proving FRX/LBX section rows with OBJTYPE=9 but no OBJCODE field keep inspection non-failing, default to Title band metadata with null object-code/title/band-kind provenance, preserve geometry and selected-section JSON, and retain label identity.
- E3 designer/tooling missing section geometry schema note — Studio host JSON now has regression coverage proving FRX/LBX section rows without VPOS/HEIGHT fields keep inspection non-failing, preserve live/deleted band and expression metadata, default selected-section geometry to zero with null top/height field provenance, and retain label identity.
- E3 designer/tooling missing object title schema note — Studio host JSON now has regression coverage proving FRX/LBX layout object rows without NAME, EXPR, or UNIQUEID fields keep inspection non-failing, synthesize `Record N` titles with null title/expression provenance, preserve live/deleted object geometry, retain selected-object metadata, preserve live containing-section metadata, and keep deleted selected-object containing-section metadata false/null.
- E3 designer/tooling missing object EXPR schema note — Studio host JSON now has regression coverage proving FRX/LBX layout object rows without an EXPR column keep inspection non-failing, preserve live/deleted object metadata and geometry, fall back to UNIQUEID titles, serialize empty expressions with null field provenance, retain selected-object metadata, preserve live containing-section metadata, and keep deleted selected-object containing-section metadata false/null.
- E3 designer/tooling stable-selector report-section delete note — Studio host JSON now has regression coverage proving live FRX/LBX sections can be deleted by stable unique id with the DBF deleted flag set, live/deleted section counts refreshed, former section objects moved to unplaced membership, and selected deleted-section metadata exposed.
- E3 designer/tooling stable-selector deleted report-section restore note — Studio host JSON now has regression coverage proving deleted FRX/LBX sections can be restored by stable unique id with the DBF deleted flag cleared, live/deleted section counts refreshed, formerly unplaced section objects reattached, and selected-section metadata rehydrated.
- E3 designer/tooling stable-selector edited deleted report-layout unplaced restore note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout objects can be edited into out-of-section geometry and restored by stable unique id with the DBF deleted flag cleared, edited unplaced geometry preserved, live preview bounds/counts refreshed, and selected containing-section metadata unavailable/null.
- E3 designer/tooling stable-selector edited deleted report-layout restore note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout objects can be edited and restored by stable unique id with the DBF deleted flag cleared, edited geometry preserved, and live selected-object section metadata rehydrated.
- E3 designer/tooling stable-selector deleted report-layout top-position clear note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object VPOS fields can be cleared by stable unique id through generic property clears with the DBF deleted flag preserved and stale deleted-object/selected-object top/bottom metadata removed.
- E3 designer/tooling stable-selector deleted report-layout top-position update note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object VPOS fields can be updated by stable unique id through generic property edits with the DBF deleted flag preserved and deleted-object/selected-object top/bottom metadata refreshed.
- E3 designer/tooling stable-selector deleted report-layout height clear note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object HEIGHT fields can be cleared by stable unique id through generic property clears with the DBF deleted flag preserved and stale deleted-object/selected-object height/bottom metadata removed.
- E3 designer/tooling stable-selector deleted report-layout height update note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object HEIGHT fields can be updated by stable unique id through generic property edits with the DBF deleted flag preserved and deleted-object/selected-object height/bottom metadata refreshed.
- E3 designer/tooling stable-selector deleted report-layout left-position clear note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object HPOS fields can be cleared by stable unique id through generic property clears with the DBF deleted flag preserved and stale deleted-object/selected-object left/right metadata removed.
- E3 designer/tooling stable-selector deleted report-layout left-position update note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object HPOS fields can be updated by stable unique id through generic property edits with the DBF deleted flag preserved and deleted-object/selected-object left/right metadata refreshed.
- E3 designer/tooling stable-selector deleted report-layout width clear note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object WIDTH fields can be cleared by stable unique id through generic property clears with the DBF deleted flag preserved and stale deleted-object/selected-object width/right metadata removed.
- E3 designer/tooling stable-selector deleted report-layout width update note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout object WIDTH fields can be updated by stable unique id through generic property edits with the DBF deleted flag preserved and deleted-object/selected-object width/right metadata refreshed.
- E3 designer/tooling stable-selector report-layout top-position clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object VPOS fields can be cleared by stable unique id through generic property clears with blanked direct fields, refreshed selected-object top/bottom metadata, and restored containing-section metadata.
- E3 designer/tooling stable-selector report-layout top-position update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object VPOS fields can be updated by stable unique id through generic property edits with refreshed preview top/height bounds, selected-object top/bottom metadata, and placed/unplaced counts.
- E3 designer/tooling stable-selector report-layout height clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object HEIGHT fields can be cleared by stable unique id through generic property clears with blanked direct fields and selected-object height/bottom metadata while removing stale geometry.
- E3 designer/tooling stable-selector report-layout height update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object HEIGHT fields can be updated by stable unique id through generic property edits with refreshed preview bottom/height bounds and selected-object height/bottom metadata while preserving containing-section metadata.
- E3 designer/tooling stable-selector report-layout left-position clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object HPOS fields can be cleared by stable unique id through generic property clears with blanked direct fields, refreshed preview left/width bounds, and selected-object left/right-edge metadata while removing stale geometry.
- E3 designer/tooling stable-selector report-layout left-position update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object HPOS fields can be updated by stable unique id through generic property edits with refreshed preview left/width bounds and selected-object left/right-edge metadata while preserving containing-section metadata.
- E3 designer/tooling stable-selector report-layout width clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object WIDTH fields can be cleared by stable unique id through generic property clears with refreshed preview bounds and selected-object width/right-edge metadata while removing stale geometry.
- E3 designer/tooling stable-selector report-layout width update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object WIDTH fields can be updated by stable unique id through generic property edits with refreshed preview bounds and selected-object width/right-edge metadata while preserving containing-section metadata.
- E3 designer/tooling stable-selector report-layout unplacement-transition note — Studio host JSON now has regression coverage proving section-contained live FRX/LBX layout objects can move out of all live sections by stable unique id through generic VPOS edits with placed/unplaced counts refreshed and selected-object containing-section metadata cleared/null.
- E3 designer/tooling stable-selector report-layout placement-transition note — Studio host JSON now has regression coverage proving initially unplaced live FRX/LBX layout objects can move into live sections by stable unique id through generic VPOS edits with placed/unplaced counts refreshed and selected-object containing-section metadata pointing at the moved object.
- E3 designer/tooling live-edited unplaced report-layout object reorder note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited outside section bands can be physically reordered with edited HPOS/VPOS/HEIGHT fields preserved, selected reordered-object JSON refreshed, and selected containing-section availability/object JSON kept false/null instead of fabricating section membership.
- E3 designer/tooling stable-selector report-layout object font metadata clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object FONTFACE memos can be cleared by stable unique id without a record selector with stale font highlight metadata removed and refreshed selected-object highlight JSON pointing at the edited object with containing-section metadata.
- E3 designer/tooling stable-selector report-layout object font metadata update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object FONTFACE memos can be updated by stable unique id without a record selector with the font metadata persisted and refreshed selected-object highlight JSON pointing at the edited object with containing-section metadata.
- E3 designer/tooling stable-selector report-layout object expression clear note — Studio host JSON now has regression coverage proving live FRX/LBX layout object EXPR memos can be cleared by stable unique id without a record selector with the expression blanked and refreshed selected-object JSON pointing at the cleared object with containing-section metadata.
- E3 designer/tooling stable-selector report-layout object expression update note — Studio host JSON now has regression coverage proving live FRX/LBX layout object EXPR memos can be updated by stable unique id without a record selector with the edited expression persisted and refreshed selected-object JSON pointing at the updated object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object distribution note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be horizontally distributed by stable ids without a record selector with computed HPOS persisted, edited WIDTH/HEIGHT fields preserved, and refreshed selected-object JSON pointing at the distributed object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object nudge note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be nudged by stable ids without a record selector with delta-adjusted HPOS/VPOS persisted, edited WIDTH/HEIGHT fields preserved, and refreshed selected-object JSON pointing at the nudged object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object snap note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be snapped to grid by stable ids without a record selector with rounded HPOS/VPOS persisted, edited WIDTH/HEIGHT fields preserved, and refreshed selected-object JSON pointing at the snapped object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object resize note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be resized by stable ids without a record selector with edited HPOS preserved, anchor WIDTH/HEIGHT dimensions applied, and refreshed selected-object JSON pointing at the resized object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object align note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be aligned by stable ids without a record selector with edited HPOS/WIDTH/HEIGHT fields preserved and refreshed selected-object JSON pointing at the aligned object with containing-section metadata.
- E3 designer/tooling live-edited report-layout object reorder note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be physically reordered with edited HPOS/WIDTH/HEIGHT fields preserved and refreshed selected-object JSON pointing at the reordered object with containing-section metadata.
- E3 designer/tooling live-edited unplaced report-layout object rename note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited outside section bands can be renamed with edited HPOS/VPOS/HEIGHT fields preserved, selected renamed-object JSON refreshed, and selected containing-section availability/object JSON kept false/null instead of fabricating section membership.
- E3 designer/tooling live-edited report-layout object rename note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be renamed with edited HPOS/WIDTH/HEIGHT fields preserved and refreshed selected-object JSON pointing at the renamed identity with containing-section metadata.
- E3 designer/tooling live-edited unplaced report-layout object duplicate note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited outside section bands can be duplicated with edited HPOS/VPOS/HEIGHT fields preserved, selected duplicate JSON refreshed, and selected containing-section availability/object JSON kept false/null instead of fabricating section membership.
- E3 designer/tooling live-edited report-layout object duplicate note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can be duplicated with edited HPOS/WIDTH/HEIGHT fields preserved and refreshed selected-object JSON pointing at the duplicate with containing-section metadata.
- E3 designer/tooling live-edited unplaced deleted report-layout object restore note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited outside section bands, deleted, and restored come back as live unplaced objects with edited geometry preserved, preview/unplaced accounting refreshed, and selected containing-section metadata unavailable/null.
- E3 designer/tooling live-edited deleted report-layout object restore note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties, deleted, and restored preserve edited geometry fields and rehydrate selected live containing-section metadata with section-relative geometry.
- E3 designer/tooling edited unplaced report-layout object delete note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited into out-of-section geometry can then be deleted while preserving edited geometry fields in deleted preview bounds, deleted placed/unplaced accounting, `deletedObjects`, and `selectedReportObject` JSON without fabricating containing-section metadata.
- E3 designer/tooling edited report-layout object delete note — Studio host JSON now has regression coverage proving live FRX/LBX layout objects edited through host geometry properties can then be deleted while preserving edited geometry fields in `deletedObjects` and `selectedReportObject` JSON and keeping selected containing-section metadata unavailable/null.
- E3 designer/tooling edited deleted report-layout object unplaced restore note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout objects edited outside live section bands restore as live unplaced objects, preserve edited geometry, expand preview bounds, update unplaced accounting, and keep selected containing-section metadata unavailable/null.
- E3 designer/tooling edited deleted report-layout object restore note — Studio host JSON now has regression coverage proving deleted FRX/LBX layout objects can receive geometry edits while deleted, restore to live DBF state with edited geometry preserved, leave `deletedObjects`, and rehydrate selected live containing-section metadata with section object indexes/counts.
- E3 designer/tooling deleted report-layout object top-position clear note — Studio host `--clear-property --record <deleted-layout-object> --property-name VPOS --json` now has regression coverage proving FRX/LBX deleted layout-object top-position clears blank the direct field, refresh deleted-object and selected deleted-object top/bottom-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling deleted report-layout object top-position edit note — Studio host `--set-property --record <deleted-layout-object> --property-name VPOS --property-value <n> --json` now has regression coverage proving FRX/LBX deleted layout-object top-position edits persist, refresh deleted-object and selected deleted-object top/bottom-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling deleted report-layout object height clear note — Studio host `--clear-property --record <deleted-layout-object> --property-name HEIGHT --json` now has regression coverage proving FRX/LBX deleted layout-object height clears blank the direct field, refresh deleted-object and selected deleted-object height/bottom-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling deleted report-layout object height edit note — Studio host `--set-property --record <deleted-layout-object> --property-name HEIGHT --property-value <n> --json` now has regression coverage proving FRX/LBX deleted layout-object height edits persist, refresh deleted-object and selected deleted-object height/bottom-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling deleted report-layout object left-position clear note — Studio host `--clear-property --record <deleted-layout-object> --property-name HPOS --json` now has regression coverage proving FRX/LBX deleted layout-object left-position clears blank the direct field, refresh deleted-object and selected deleted-object left/right-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling deleted report-layout object left-position note — Studio host `--set-property --record <deleted-layout-object> --property-name HPOS --property-value <n> --json` now has regression coverage proving FRX/LBX deleted layout-object left-position edits persist, refresh deleted-object and selected deleted-object left/right-edge metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- E3 designer/tooling report-layout top preview-bounds note — Studio host `--set-property --record <object> --property-name VPOS --property-value <above-section-band> --json` now has regression coverage proving FRX/LBX vertical-position edits can move an object above all live section bands and refreshed `reportLayout` JSON updates selected-object top/bottom metadata, document preview top/height bounds, and unplaced selected-object metadata.
- E3 designer/tooling report-layout height preview-bounds note — Studio host `--set-property --record <object> --property-name HEIGHT --property-value <height> --json` now has regression coverage proving FRX/LBX height edits update selected-object height/bottom-edge metadata and document preview bottom/height bounds while preserving section membership and placed counts.
- E3 designer/tooling report-layout left preview-bounds note — Studio host `--set-property --record <object> --property-name HPOS --property-value <left> --json` now has regression coverage proving FRX/LBX horizontal-position edits update selected-object left/right-edge metadata and document preview left/width bounds while preserving section membership and placed counts.
- E3 designer/tooling report-layout width preview-bounds note — Studio host `--set-property --record <object> --property-name WIDTH --property-value <width> --json` now has regression coverage proving FRX/LBX width edits update selected-object width/right-edge metadata and document preview bounds while preserving section membership and placed counts.
- E3 designer/tooling report-layout unplacement-transition note — Studio host `--set-property --record <object> --property-name VPOS --property-value <outside-section-band> --json` now has regression coverage proving FRX/LBX position edits can move a section-contained object outside all live section bands and refreshed `reportLayout` JSON updates placed/unplaced counts, clears selected containing-section availability, and serializes unplaced selected-object metadata.
- E3 designer/tooling report-layout placement-transition note — Studio host `--set-property --record <object> --property-name VPOS --property-value <section-top> --json` now has regression coverage proving FRX/LBX position edits can move an initially unplaced object into a live section band and refreshed `reportLayout` JSON updates placed/unplaced counts, selected object containing-section metadata, section-relative coordinates, and section object counts.
- E3 designer/tooling report-layout object-font editing note — Studio host `--set-property --record <object> --property-name FONTFACE --property-value <font> --json` now has regression coverage proving FRX/LBX object font memo edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates selected-object highlight metadata while preserving object selection kind and containing-section metadata.
- E3 designer/tooling report-layout object-expression editing note — Studio host `--set-property --record <object> --property-name EXPR --property-value <expr> --json` now has regression coverage proving FRX/LBX object expression memo edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates selected-object expression, title, expression provenance, object selection kind, and containing-section metadata.
- E3 designer/tooling report-layout column-setup editing note — Studio host `--set-property --record 0 --property-name EXPR --property-value <column setup memo> --json` now has regression coverage proving FRX/LBX `COLS`/`COLWIDTH`/`COLSPACING` memo edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates column setup values plus setting provenance without fabricating page setup values.
- E3 designer/tooling report-layout settings-memo editing note — Studio host `--set-property --record 0 --property-name EXPR --property-value <settings memo> --json` now has regression coverage proving FRX/LBX settings memo edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates page setup values plus memo/field setting provenance.
- E3 designer/tooling report-layout section-top editing note — Studio host `--set-property --record <section> --property-name VPOS --property-value <n> --json` now has regression coverage proving FRX/LBX section top edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates section top, bottom edge, section-relative object coordinates, and membership.
- E3 designer/tooling report-layout section-height editing note — Studio host `--set-property --record <section> --property-name HEIGHT --property-value <n> --json` now has regression coverage proving FRX/LBX section height edits persist through the generic property-edit path and refreshed `reportLayout` JSON updates section height, bottom edge, section-height total, and object membership.
- E3 designer/tooling report-layout deleted-preview-bounds note — Studio host `--json` now exposes compact non-mutating report/label deleted preview bounds for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the bounds from deleted sections and deleted objects while preserving live preview bounds and detailed deleted payloads.
- E3 designer/tooling report-layout deleted-placement summary note — Studio host `--json` now exposes compact non-mutating report/label deleted placed/unplaced counts for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the split from deleted object geometry against live and deleted section bands while preserving detailed object payloads.
- E3 designer/tooling report-layout placed-object summary note — Studio host `--json` now exposes compact non-mutating report/label placed-object counts for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the summary from live objects assigned to sections while preserving detailed section/object/settings payloads.
- E3 designer/tooling report-layout section-height summary note — Studio host `--json` now exposes compact non-mutating report/label section height totals for live and deleted sections for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the totals from existing section snapshots while preserving detailed section/object/settings payloads.
- E3 designer/tooling report-layout section-kind summary note — Studio host `--json` now exposes compact non-mutating report/label section band-kind count buckets for live and deleted sections for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the summaries from existing section snapshots while preserving detailed section/object/settings payloads.
- E3 designer/tooling report-layout object-kind summary note — Studio host `--json` now exposes compact non-mutating report/label object-kind count buckets for live objects, live unplaced objects, and deleted objects for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the summaries from existing layout object snapshots while preserving detailed section/object/settings payloads.
- E3 designer/tooling report-layout page setup note — Studio host `--json` now exposes compact non-mutating report/label page setup summary fields for orientation, paper size, top/bottom margins, and vertical/horizontal grid spacing for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the summary from live settings while preserving the provenance-rich settings arrays.
- E3 designer/tooling report-layout column setup note — Studio host `--json` now exposes compact non-mutating report/label column setup summary fields for column count, column width, and column spacing for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the summary from live settings while preserving the provenance-rich settings arrays.
- E3 designer/tooling report-layout-preview-bounds note — Studio host `--json` now exposes non-mutating report/label layout preview bounds and live object counts for `.frx` and `.lbx` assets through `StudioReportLayoutSnapshot`, deriving the metrics from live sections plus live placed and unplaced layout objects while excluding deleted layout objects.
- E2 designer/tooling visual-object-duplicate-subtree note — Studio host `--visual-object-duplicate-subtree` JSON now exposes root-plus-descendant duplication through `duplicate_visual_object_subtree`, including copied-root metadata, copied counts, committed mutation state, undo availability state, explicit replacement identity maps, copied child parent rewrites, replacement collision errors, missing replacement-map errors, incomplete replacement identity errors, parser errors, and usage text coverage.
- E2 designer/tooling visual-object-reorder-batch note — Studio host `--visual-object-reorder-batch` JSON now exposes selected-object order mutations through `reorder_visual_objects`, including affected-object counts, committed mutation state, undo availability state, per-item placements, relative target selectors, no-write failure handling for later in-memory reorder failures, missing selected-object and missing target errors, self-relative and unsupported-placement errors, parser errors, and usage text coverage.
- E2 designer/tooling visual-object-rename-batch note — Studio host `--visual-object-rename-batch` JSON now exposes selected-object identity rename batches through `rename_visual_objects`, including affected-object counts, committed mutation state, undo availability state, per-item replacement identities, rollback/no-mutation failure handling for later rename failures, missing selected-object errors, empty replacement identity errors, identity-collision errors, parser errors, and usage text coverage.
- E2 designer/tooling visual-object-duplicate-batch note — Studio host `--visual-object-duplicate-batch` JSON now exposes selected-object duplicate batches through `duplicate_visual_objects`, including affected-object counts, committed mutation state, undo availability state, per-item replacement identities, rollback/no-mutation failure handling for later duplicate failures, missing selected-object errors, identity-collision errors, parser errors, and usage text coverage.
- E2 designer/tooling visual-object-update-batch note — Studio host `--visual-object-update-batch` JSON now exposes multi-object property update batches through `update_visual_object_batch`, including affected-object counts, undo availability, committed mutation state, nested per-object property lists, direct DBF-backed and memo-backed property writes, rollback/no-mutation failure handling for later-object and later-property failures, missing selected-object errors, empty per-object property-list errors, parser errors, and usage text coverage.
- E2 designer/tooling visual-property-update-batch note — Studio host `--visual-property-update-batch` JSON now exposes selected-object property update batches through `update_visual_object_properties`, including affected-object counts, undo availability, committed mutation state, direct DBF-backed and memo-backed property writes, rollback/no-mutation failure handling, missing selected-object errors, missing property-name errors, parser errors, alias dispatch, and usage text coverage.
- E2 designer/tooling visual-object-reparent-batch note — Studio host `--visual-object-reparent-batch` JSON now exposes selected-object reparent batches through `reparent_visual_objects`, including affected-item counts, undo availability, committed mutation state, per-item parent selectors, clear-parent items, rollback/no-mutation failure handling, missing selected-object and missing parent errors, self-parenting rejection, descendant-cycle rejection, parser errors, and usage text coverage.
- E2 designer/tooling selection-toolbox-dispatch-execution-catalog note — Studio host selected-context toolbox dispatch-execution catalog JSON now exposes non-executing toolbox execution-readiness planning with compact ready/blocked item-id summaries and detailed item/readiness metadata.
- E2 designer/tooling visual-property-reorder-batch note — Studio host `--visual-property-reorder-batch` JSON now exposes selected-object memo-backed property reorder batches through `reorder_visual_object_properties`, including affected-item counts, undo availability, committed mutation state, relative placements, value preservation, rollback/no-mutation failure handling, direct-field rejection, missing-relative and self-relative rejection, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-reorder note — Studio host `--visual-property-reorder` JSON now exposes selected-object memo-backed property reorders through `reorder_visual_object_property`, including affected-object counts, undo availability, committed mutation state, relative placements, value preservation, direct-field rejection, missing-relative and self-relative rejection, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-rename-batch note — Studio host `--visual-property-rename-batch` JSON now exposes selected-object memo-backed property rename batches through `rename_visual_object_properties`, including affected-item counts, undo availability, committed mutation state, value preservation, rollback/no-mutation failure handling, direct-field rejection, same-name rejection, target-collision handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-rename note — Studio host `--visual-property-rename` JSON now exposes selected-object memo-backed property renames through `rename_visual_object_property`, including affected-object counts, undo availability, committed mutation state, value preservation, direct-field rejection, same-name rejection, target-collision handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-move-batch note — Studio host `--visual-property-move-batch` JSON now exposes selected-object property move batches through `move_visual_object_properties`, including affected-item counts, undo availability, committed mutation state, source clearing, explicit replacement, self-move rejection, target-collision handling, rollback/no-mutation failure handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-move note — Studio host `--visual-property-move` JSON now exposes selected-object property moves through `move_visual_object_property`, including affected-object counts, undo availability, committed mutation state, source clearing, default target names, explicit replacement, self-move rejection, target-collision handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-copy-batch note — Studio host `--visual-property-copy-batch` JSON now exposes selected-object property copy batches through `copy_visual_object_properties`, including affected-item counts, undo availability, committed mutation state, source preservation, explicit replacement, target-collision handling, rollback/no-mutation failure handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-clear-batch note — Studio host `--visual-property-clear-batch` JSON now exposes selected-object property clear batches through `clear_visual_object_properties`, including affected-item counts, undo availability, committed mutation state, rollback/no-mutation failure handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-copy note — Studio host `--visual-property-copy` JSON now exposes selected-object property copies through `copy_visual_object_property`, including affected-object counts, undo availability, committed mutation state, default target names, source preservation, explicit replacement, target-collision handling, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-clear note — Studio host `--visual-property-clear` JSON now exposes selected-object property clears through `clear_visual_object_property`, including affected-object counts, undo availability, committed mutation state, direct-field clearing, memo-backed assignment removal, idempotent missing-property clear behavior, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-method-reorder-batch note — Studio host `--visual-method-reorder-batch` JSON now exposes selected-object method reorder batches through `reorder_visual_object_methods`, including affected-item counts, undo availability, committed mutation state, parser/editor errors, rollback/no-mutation failure coverage, multi-object item selectors, relative placements, and usage text coverage.
- E2 designer/tooling visual-method-move-batch note — Studio host `--visual-method-move-batch` JSON now exposes selected-object method move batches through `move_visual_object_methods`, including affected-item counts, undo availability, committed mutation state, parser/editor errors, rollback/no-mutation failure coverage, multi-object source/target item selectors, source deletion, target-collision handling, and usage text coverage.
- E2 designer/tooling visual-method-copy-batch note — Studio host `--visual-method-copy-batch` JSON now exposes selected-object method copy batches through `copy_visual_object_methods`, including affected-item counts, undo availability, committed mutation state, parser/editor errors, rollback/no-mutation failure coverage, multi-object source/target item selectors, target-collision handling, and usage text coverage.
- E2 designer/tooling visual-method-rename-batch note — Studio host `--visual-method-rename-batch` JSON now exposes selected-object method rename batches through `rename_visual_object_methods`, including affected-item counts, undo availability, committed mutation state, parser/editor errors, rollback/no-mutation failure coverage, multi-object item selectors, target-collision handling, and usage text coverage.
- E2 designer/tooling visual-method-delete-batch note — Studio host `--visual-method-delete-batch` JSON now exposes selected-object method delete batches through `delete_visual_object_methods`, including affected-item counts, undo availability, committed mutation state, parser/editor errors, rollback/no-mutation failure coverage, multi-object item selectors, and usage text coverage.
- E2 designer/tooling visual-method-reorder note — Studio host `--visual-method-reorder` JSON now exposes selected-object method reorder mutations through `reorder_visual_object_method`, including affected-object counts, undo availability, committed mutation state, `first`/`last`/`before`/`after` placements, parser/editor errors, no-mutation failure coverage, unrelated object preservation, and usage text coverage.
- E2 designer/tooling visual-method-move note — Studio host `--visual-method-move` JSON now exposes selected-object method move mutations through `move_visual_object_method`, including affected-object counts, undo availability, committed mutation state, target method renames, explicit target replacement, parser/editor errors, target-collision no-mutation coverage, source deletion, and usage text coverage.
- E2 designer/tooling visual-method-copy note — Studio host `--visual-method-copy` JSON now exposes selected-object method copy mutations through `copy_visual_object_method`, including affected-object counts, undo availability, committed mutation state, default target method names, explicit target replacement, parser/editor errors, target-collision no-mutation coverage, source preservation, and usage text coverage.
- E2 designer/tooling visual-method-rename note — Studio host `--visual-method-rename` JSON now exposes selected-object method rename mutations through `rename_visual_object_method`, including affected-object counts, undo availability, committed mutation state, parser/editor errors, target-collision no-mutation coverage, unrelated-method preservation, and usage text coverage.
- E2 designer/tooling visual-method-delete note — Studio host `--visual-method-delete` JSON now exposes selected-object method deletion mutations through `delete_visual_object_method`, including affected-object counts, undo availability, committed mutation state, parser/editor errors, no-mutation failure coverage, unrelated-method preservation, and usage text coverage.
- E2 designer/tooling visual-method-update note — Studio host `--visual-method-update` JSON now exposes selected-object method update/append mutations through `update_visual_object_method`, including affected-object counts, undo availability, committed mutation state, parser/editor errors, no-mutation failure coverage, and usage text coverage.
- E2 designer/tooling visual-method-query note — Studio host `--visual-method-query` JSON now exposes non-mutating selected-object method reads through `query_visual_object_method`, including selected-record metadata, record deleted state, method existence, method snapshot metadata when present, absent-method success, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-method-list note — Studio host `--visual-method-list` JSON now exposes non-mutating selected-object method listings through `list_visual_object_methods`, including selected-record metadata, record deleted state, method counts, declaration kind, source text, source line, memo-block metadata, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-object-ancestors note — Studio host `--visual-object-ancestors` JSON now exposes non-mutating selected-object ancestor listings through `list_visual_object_ancestors`, including selected-record metadata, ancestor counts, depth, dry-run/non-mutating state, outline snapshot metadata, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-object-descendants note — Studio host `--visual-object-descendants` JSON now exposes non-mutating selected-object descendant listings through `list_visual_object_descendants`, including parent record/name metadata, descendant counts, traversal depth, dry-run/non-mutating state, deleted descendant visibility, outline snapshot metadata, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-object-children note — Studio host `--visual-object-children` JSON now exposes non-mutating selected-object immediate-child listings through `list_visual_object_children`, including parent record/name metadata, child counts, dry-run/non-mutating state, deleted child visibility, outline snapshot metadata, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-object-list note — Studio host `--visual-object-list` JSON now exposes non-mutating visual object outline listings through `list_visual_objects`, including object count, dry-run/non-mutating state, live/deleted rows, fallback names, stable IDs, hierarchy/path/sibling/ancestor metadata, class/baseclass names, property/method counts, parsed captions, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-list note — Studio host `--visual-property-list` JSON now exposes non-mutating selected-object property-grid listings through `list_visual_object_properties`, including selected-record metadata, record deleted state, property counts, dry-run/non-mutating state, direct-field metadata, memo-backed source-line metadata, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-query note — Studio host `--visual-property-query` JSON now exposes non-mutating selected-object point reads through `query_visual_object_property`, including selected-record metadata, record deleted state, property existence, direct-field status, resolved property name, current value, missing-property success, parser/editor errors, and usage text coverage.
- E2 designer/tooling visual-property-filter note — Studio host `--visual-property-filter` JSON now exposes non-mutating selected-object property-grid filtering through `filter_visual_object_properties`, including record/deleted metadata, search text, filtered counts, dry-run/non-mutating state, direct-field metadata, memo-backed source-line metadata, empty successful results, parser/editor errors, and usage text coverage.
- E2 designer/tooling toolbox-palette-query note — Studio host `--toolbox-palette-query` JSON now exposes non-mutating toolbox descriptor filtering by toolbox context, optional search text, and optional category, including item counts, dry-run/non-mutating state, matching descriptors, empty successful results, parser errors, and usage text coverage.
- E2 designer/tooling selection-builder-dispatch-execution-catalog note — Studio host selected-context builder dispatch-execution catalog JSON now exposes non-executing builder execution-readiness planning with compact ready/blocked builder-id summaries and detailed launch/admission/dispatch readiness entries.
- E2 designer/tooling selection-builder-catalog-status-summary note — Studio host selected-context builder launch, invocation-admission, and dispatch catalog JSON now emits compact ready and blocked builder-id summaries with blocked errors beside detailed per-builder selected-context entries.
- E2 designer/tooling direct-launch-summary note — Studio host direct designer launch-surface JSON now emits compact ready and blocked selected-context summaries with blocked launch errors beside detailed aggregate action, builder, and toolbox launch-surface metadata.
- E2 designer/tooling direct-invocation-summary note — Studio host direct designer invocation-admission JSON now emits compact admission-clean and admission-blocked selected-context summaries with blocked invocation errors beside detailed aggregate action, builder, and toolbox invocation-admission metadata.
- E2 designer/tooling direct-dispatch-summary note — Studio host direct designer dispatch JSON now emits compact dispatch-clean and dispatch-blocked selected-context summaries with blocked dispatch errors beside detailed aggregate action, builder, and toolbox dispatch metadata.
- E2 designer/tooling direct-execution-summary note — Studio host direct designer execution JSON now emits compact execution-ready and execution-blocked selected-context summaries with blocked execution errors beside detailed aggregate child execution metadata.
- E2 designer/tooling direct-toolbox-launch-summary note — Studio host direct toolbox palette launch-plan JSON now emits compact launch-ready and launch-blocked selected-context summaries with blocked launch errors beside detailed selected-context toolbox item metadata.
- E2 designer/tooling direct-toolbox-batch-plan-summary note — Studio host direct toolbox create-batch plan JSON now emits compact plan-ready and plan-blocked item summaries beside detailed direct and selected-context batch create-plan metadata.
- E2 designer/tooling direct-toolbox-create-plan-summary note — Studio host direct toolbox create-plan JSON now emits compact plan-ready and plan-blocked item summaries beside detailed direct and selected-context single create-plan metadata.
- E2 designer/tooling note — aggregate designer dispatch execution catalog planning now reports explicit aggregate execution-admission readiness and per-context preflight errors through `plan_studio_designer_dispatch_execution_catalog` without launching child executors, builders, editors, toolbox processes, or UI, and Studio host exposes that non-mutating readiness through `--designer-dispatch-execution-catalog`; builder dispatch execution catalog planning reports explicit execution-admission readiness and per-builder preflight errors through `plan_studio_builder_dispatch_execution_catalog` without launching builders, Studio host exposes that non-mutating readiness through `--builder-dispatch-execution-catalog`, editor action dispatch execution catalog planning reports explicit execution-admission readiness and per-action preflight errors through `plan_studio_editor_action_dispatch_execution_catalog` without launching editors, Studio host exposes that non-mutating readiness through `--editor-action-dispatch-execution-catalog`, toolbox dispatch execution catalog planning reports explicit execution-admission readiness and per-item preflight errors through `plan_studio_toolbox_dispatch_execution_catalog` without launching toolbox processes, and Studio host exposes that non-mutating readiness through `--toolbox-dispatch-execution-catalog`; selection-context toolbox palette invocation admission catalog planning now resolves Studio selections to toolbox palettes through `plan_studio_toolbox_invocation_admission_catalog_for_selection`, `plan_studio_toolbox_dispatch_catalog_for_selection` composes that admission catalog with non-executing dispatch planning, `plan_visual_object_from_toolbox_selection` resolves selected contexts into non-mutating single-item object create plans, `create_visual_object_from_toolbox_selection` and `create_visual_objects_from_toolbox_selection` execute selected-context single and ordered batch create plans through the existing VFP object append paths, `plan_visual_object_create_dispatch_from_toolbox_selection` composes selected-context single-item create plans with non-executing dispatch planning, `plan_visual_objects_from_toolbox_selection` resolves selected contexts into non-mutating ordered batch object create plans, `plan_visual_object_batch_create_dispatch_from_toolbox_selection` composes selected-context ordered batch create plans with non-executing dispatch planning, `plan_visual_object_catalog_from_toolbox_selection` resolves selected contexts into non-mutating object create-plan catalogs, and Studio host exposes `--selection-toolbox-invocation-admission-catalog`, `--selection-toolbox-dispatch-catalog`, `--selection-toolbox-create`, `--selection-toolbox-create-batch`, and `--selection-toolbox-create-batch-dispatch-plan` selected-context JSON surfaces with deterministic unsupported-context rejection before any UI execution, dispatch execution, or unintended asset mutation
- E2 designer/tooling execution-failure note — Studio host aggregate designer execution JSON preserves child reporting metadata on process-failure paths by using the pre-execution dispatch plan as a reporting-only fallback for child identity, command token, dispatch arguments, and executed command strings while keeping `execute_studio_designer_dispatch` free of stale aggregate success metadata.
- E2 designer/tooling failure-coverage note — aggregate designer execution JSON coverage now explicitly locks the failed editor-action, builder/wizard, and toolbox process-launch paths so all three child categories preserve planned child metadata after non-zero child exits.
- E2 designer/tooling execution-count note — Studio host aggregate designer execution JSON now reports explicit editor-action, builder/wizard, and toolbox child execution entry counts beside aggregate execution/error totals so host consumers do not need to infer category totals from arrays or optional child objects.
- E2 designer/tooling error-count note — Studio host aggregate designer execution JSON now reports explicit editor-action, builder/wizard, and toolbox child error counts beside aggregate error totals so host consumers can distinguish child failure distribution without scanning child result arrays.
- E2 designer/tooling success-count note — Studio host aggregate designer execution JSON now reports explicit editor-action, builder/wizard, and toolbox child successful-execution counts beside aggregate execution totals so host consumers can distinguish partial-success distribution without scanning child result arrays.
- E2 designer/tooling failed-child-summary note — Studio host aggregate designer execution JSON now reports compact failed editor-action ids, failed builder ids, and toolbox failure state beside detailed child execution entries so host consumers can identify failed child categories without scanning every child result.
- E2 designer/tooling failed-child-error-summary note — Studio host aggregate designer execution JSON now reports compact failed editor-action errors, failed builder errors, and toolbox error text beside failed-child summaries so host consumers can diagnose failed child categories without scanning every child result.
- E2 designer/tooling child-exit-code note — Studio host execution JSON now normalizes `std::system(...)` wait statuses before reporting child `observedExitCode` values, so failed builder, editor-action, toolbox, and aggregate designer child launches report portable process exit codes instead of raw platform wait-status encodings.
- E2 designer/tooling child-launch-command note — Studio host aggregate designer execution JSON now emits per-child `launchCommand` values for editor-action, builder/wizard, and toolbox entries beside dispatch arguments, executed commands, and observed exit-code metadata so aggregate child rows are self-contained for external hosts.
- E2 designer/tooling child-target-metadata note — Studio host aggregate designer execution JSON now emits child-level target metadata for editor actions, builders/wizards, and toolbox execution entries, including selected asset/object identity and editor source-position details where applicable.
- E2 designer/tooling aggregate-toolbox-items note — Studio host aggregate designer execution JSON now emits toolbox child `items` descriptors beside toolbox target metadata and execution telemetry, so external hosts can render toolbox child results without joining back to separate toolbox catalog output.
- E2 designer/tooling child-descriptor-metadata note — Studio host aggregate designer execution JSON now emits editor-action label/kind and builder title/kind metadata on child rows, keeping aggregate execution child entries aligned with descriptor metadata needed by external designer hosts.
- E2 designer/tooling child-descriptor-detail note — Studio host aggregate designer execution JSON now emits editor-action descriptions and builder VFP9-equivalent/component/description metadata on child rows, reducing the need for external hosts to join aggregate execution output back to descriptor catalogs.
- E2 designer/tooling failed-child-command-token note — Studio host aggregate designer execution JSON now emits compact failed child command-token summaries for editor-action, builder/wizard, and toolbox failures beside failed ids/errors and detailed child rows.
- E2 designer/tooling failed-child-executed-command note — Studio host aggregate designer execution JSON now emits compact failed child executed-command summaries for editor-action, builder/wizard, and toolbox failures beside failed ids/tokens/errors and detailed child rows.
- E2 designer/tooling failed-child-exit-code note — Studio host aggregate designer execution JSON now emits compact numeric failed child exit-code summaries for editor-action, builder/wizard, and toolbox failures beside failed ids/tokens/commands/errors and detailed child rows.
- E2 designer/tooling dispatch-execution-catalog-readiness-summary note — Studio host designer dispatch execution catalog JSON now emits compact ready and blocked selection-context summaries with blocked execution errors beside detailed per-context readiness entries.
- E2 designer/tooling dispatch-catalog-status-summary note — Studio host designer dispatch catalog JSON now emits compact dispatch-clean and dispatch-blocked selection-context summaries with representative blocked dispatch errors beside detailed per-context dispatch entries.
- E2 designer/tooling invocation-admission-catalog-status-summary note — Studio host designer invocation admission catalog JSON now emits compact admission-clean and admission-blocked selection-context summaries with representative blocked admission errors beside detailed per-context invocation admission entries.
- E2 designer/tooling launch-surface-catalog-status-summary note — Studio host designer launch-surface catalog JSON now emits compact launch-ready and launch-blocked selection-context summaries with representative blocked launch errors beside detailed per-context launch-surface entries.
- E2 designer/tooling builder-launch-catalog-status-summary note — Studio host builder launch catalog JSON now emits compact launch-ready and launch-blocked builder-id summaries with blocked launch errors beside detailed per-builder launch entries.
- E2 designer/tooling builder-launch-plan-status-summary note — Studio host direct builder launch-plan JSON success output now emits compact launch-ready and empty launch-blocked builder-id summaries beside detailed builder launch-plan metadata.
- E2 designer/tooling builder-invocation-admission-status-summary note — Studio host direct builder invocation-admission JSON success output now emits compact admission-ready and empty admission-blocked builder-id summaries beside detailed builder invocation-admission metadata.
- E2 designer/tooling builder-invocation-admission-catalog-status-summary note — Studio host builder invocation-admission catalog JSON now emits compact admission-ready and admission-blocked builder-id summaries with blocked admission errors beside detailed per-builder invocation-admission entries.
- E2 designer/tooling builder-dispatch-status-summary note — Studio host direct builder dispatch JSON success output now emits compact dispatch-ready and empty dispatch-blocked builder-id summaries beside detailed builder dispatch metadata.
- E2 designer/tooling builder-dispatch-catalog-status-summary note — Studio host builder dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked builder-id summaries with blocked dispatch errors beside detailed per-builder dispatch entries.
- E2 designer/tooling builder-execution-readiness-summary note — Studio host direct builder execution JSON success output now emits compact execution-ready and empty execution-blocked builder-id summaries beside detailed builder execution metadata.
- E2 designer/tooling builder-dispatch-execution-catalog-readiness-summary note — Studio host builder dispatch execution catalog JSON now emits compact execution-ready and execution-blocked builder-id summaries with blocked execution errors beside detailed per-builder execution-readiness entries.
- E2 designer/tooling editor-action-launch-catalog-status-summary note — Studio host editor action launch catalog JSON now emits compact launch-ready and launch-blocked action-id summaries with blocked launch errors beside detailed per-action launch entries.
- E2 designer/tooling editor-action-launch-plan-status-summary note — Studio host direct editor action launch-plan JSON success output now emits compact launch-ready and empty launch-blocked action-id summaries beside detailed editor action launch-plan metadata.
- E2 designer/tooling editor-action-invocation-admission-status-summary note — Studio host direct editor action invocation-admission JSON success output now emits compact admission-ready and empty admission-blocked action-id summaries beside detailed editor action invocation-admission metadata.
- E2 designer/tooling editor-action-invocation-admission-catalog-status-summary note — Studio host editor action invocation-admission catalog JSON now emits compact admission-ready and admission-blocked action-id summaries with blocked admission errors beside detailed per-action invocation-admission entries.
- E2 designer/tooling editor-action-dispatch-status-summary note — Studio host direct editor action dispatch JSON success output now emits compact dispatch-ready and empty dispatch-blocked action-id summaries beside detailed editor action dispatch metadata.
- E2 designer/tooling editor-action-dispatch-catalog-status-summary note — Studio host editor action dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked action-id summaries with blocked dispatch errors beside detailed per-action dispatch entries.
- E2 designer/tooling editor-action-execution-readiness-summary note — Studio host direct editor action execution JSON success output now emits compact execution-ready and empty execution-blocked action-id summaries beside detailed editor action execution metadata.
- E2 designer/tooling editor-action-dispatch-execution-catalog-readiness-summary note — Studio host editor action dispatch execution catalog JSON now emits compact execution-ready and execution-blocked action-id summaries with blocked execution errors beside detailed per-action execution-readiness entries.
- E2 designer/tooling toolbox-palette-launch-catalog-status-summary note — Studio host toolbox palette launch catalog JSON now emits compact launch-ready and launch-blocked selection-context summaries with blocked launch errors beside detailed per-context toolbox palette launch entries.
- E2 designer/tooling toolbox-invocation-admission-status-summary note — Studio host toolbox invocation-admission JSON now emits compact admission-ready and empty admission-blocked toolbox item-id summaries beside detailed direct toolbox item and palette invocation-admission metadata.
- E2 designer/tooling toolbox-invocation-admission-catalog-status-summary note — Studio host toolbox invocation-admission catalog JSON now emits compact admission-ready and admission-blocked toolbox item-id summaries with blocked admission errors beside detailed toolbox item and palette invocation-admission metadata.
- E2 designer/tooling toolbox-dispatch-status-summary note — Studio host toolbox dispatch JSON now emits compact dispatch-ready and empty dispatch-blocked toolbox item-id summaries beside detailed direct toolbox dispatch metadata.
- E2 designer/tooling toolbox-dispatch-catalog-status-summary note — Studio host toolbox dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked toolbox item-id summaries with blocked dispatch errors beside detailed toolbox dispatch metadata.
- E2 designer/tooling toolbox-execution-readiness-summary note — Studio host toolbox execution JSON success output now emits compact execution-ready and empty execution-blocked toolbox item-id summaries beside detailed direct toolbox execution metadata.
- E2 designer/tooling toolbox-dispatch-execution-catalog-readiness-summary note — Studio host toolbox dispatch execution catalog JSON now emits compact execution-ready and execution-blocked toolbox item-id summaries with blocked execution errors beside detailed per-item dispatch-execution metadata.
- E2 designer/tooling selection-toolbox-dispatch-catalog-status-summary note — Studio host selection toolbox dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked toolbox item-id summaries with blocked dispatch errors beside detailed selected-context toolbox dispatch metadata.
- E2 designer/tooling selection-toolbox-invocation-admission-catalog-status-summary note — Studio host selection toolbox invocation-admission catalog JSON now emits compact admission-ready and admission-blocked toolbox item-id summaries with blocked admission errors beside detailed selected-context toolbox invocation-admission metadata.
- E2 designer/tooling toolbox-create-plan-catalog-status-summary note — Studio host toolbox create-plan catalog JSON now emits compact plan-ready and plan-blocked toolbox item-id summaries with blocked plan errors beside detailed toolbox object create-plan metadata.
- E2 designer/tooling selection-toolbox-create-plan-catalog-status-summary note — Studio host selection toolbox create-plan catalog JSON now emits compact plan-ready and plan-blocked toolbox item-id summaries with blocked plan errors beside detailed selected-context toolbox object create-plan metadata.
- E2 designer/tooling toolbox-create-dispatch-catalog-status-summary note — Studio host toolbox create-dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked toolbox item-id summaries with blocked dispatch errors beside detailed toolbox object create-dispatch metadata.
- E2 designer/tooling selection-toolbox-create-dispatch-catalog-status-summary note — Studio host selection toolbox create-dispatch catalog JSON now emits compact dispatch-ready and dispatch-blocked toolbox item-id summaries with blocked dispatch errors beside detailed selected-context toolbox object create-dispatch metadata.
- E2 designer/tooling toolbox-create-batch-plan-catalog-status-summary note — Studio host toolbox create-batch-plan catalog JSON now emits compact plan-ready and aggregate plan-blocked summaries beside detailed toolbox object batch create-plan metadata.
- E2 designer/tooling selection-toolbox-create-batch-plan-catalog-status-summary note — Studio host selection toolbox create-batch-plan catalog JSON now emits compact plan-ready and aggregate plan-blocked summaries beside detailed selected-context toolbox object batch create-plan metadata.
- E2 designer/tooling toolbox-create-batch-dispatch-catalog-status-summary note — Studio host toolbox create-batch-dispatch catalog JSON now emits compact dispatch-ready and aggregate dispatch-blocked summaries beside detailed toolbox object batch create-dispatch metadata.
- E2 designer/tooling toolbox-batch-create-dispatch-plan-status-summary note — Studio host non-catalog toolbox batch create-dispatch plan JSON now emits compact dispatch-ready and aggregate dispatch-blocked summaries beside existing base and selected-context toolbox object batch create-dispatch metadata.
- E2 designer/tooling toolbox-create-dispatch-plan-status-summary note — Studio host non-catalog toolbox create-dispatch plan JSON now emits compact dispatch-ready and aggregate dispatch-blocked summaries beside existing base and selected-context toolbox object create-dispatch metadata.
- E2 designer/tooling toolbox-batch-create-from-dispatch-result-status-summary note — Studio host dispatch-sourced toolbox batch-create JSON now emits compact created-object and create-error summaries beside existing toolbox object batch create-from-dispatch metadata.
- E2 designer/tooling toolbox-create-from-dispatch-result-status-summary note — Studio host dispatch-sourced toolbox create JSON now emits compact created-object and create-error summaries beside existing toolbox object create-from-dispatch metadata.
- E2 designer/tooling toolbox-batch-create-result-status-summary note — Studio host direct toolbox batch-create JSON now emits compact created-object and create-error summaries beside existing base and selected-context toolbox object batch-create metadata.
- E2 designer/tooling toolbox-create-result-status-summary note — Studio host direct toolbox create JSON now emits compact created-object and create-error summaries beside existing base and selected-context toolbox object create metadata.
- E2 designer/tooling selection-toolbox-create-batch-dispatch-catalog-status-summary note — Studio host selection toolbox create-batch-dispatch catalog JSON now emits compact dispatch-ready and aggregate dispatch-blocked summaries beside detailed selected-context toolbox object batch create-dispatch metadata.
- Runtime-host packaging/security notes — security-enabled package startup now preserves recorded extension-payload relative-path fidelity during digest verification, so same-named files at the package root cannot satisfy nested payload hashes outside the recorded package location
- Native-wrapper scaffold notes — generated DLL/FLL native-wrapper scaffolds now derive sibling `app.cfmanifest` and `copperfin_runtime_host(.exe)` paths from the loaded module location, emit shared `CopperfinRuntimeBridgeDescriptor`, `CopperfinRuntimeBridgeInvocation`, `CopperfinRuntimeBridgeParameter`, `CopperfinRuntimeBridgeCall`, `CopperfinRuntimeBridgeReturn`, `CopperfinRuntimeBridgeResult`, `CopperfinRuntimeBridgeEnvironmentVariable`, `CopperfinRuntimeBridgeLaunchPlan`, `CopperfinRuntimeBridgeObservationPlan`, `CopperfinRuntimeBridgeExecutionPlan`, `CopperfinRuntimeBridgeTransportPlan`, `CopperfinRuntimeBridgeSerializationPlan`, `CopperfinRuntimeBridgeDispatchPlan`, `CopperfinRuntimeBridgeDispatchExecution`, `CopperfinRuntimeBridgeProcessLaunch`, `CopperfinRuntimeBridgeHostFailureEvaluation`, `CopperfinRuntimeBridgeMissingResponseEvaluation`, `CopperfinRuntimeBridgeResponseValidationEvaluation`, `CopperfinRuntimeBridgePayloadPlan`, `CopperfinRuntimeBridgeInterpretationPlan`, `CopperfinRuntimeBridgeFailurePolicyPlan`, `CopperfinRuntimeBridgeResponseValidationPlan`, `CopperfinRuntimeBridgeRequestArtifact`, `CopperfinRuntimeBridgeRequestWritePlan`, `CopperfinRuntimeBridgeResponseReadPlan`, `CopperfinRuntimeBridgeResponseArtifact`, `CopperfinRuntimeBridgeResponseParsePlan`, `CopperfinRuntimeBridgeResponseParseAdmission`, `CopperfinRuntimeBridgeParsedResponse`, `CopperfinRuntimeBridgeInterpretedResultPlan`, `CopperfinRuntimeBridgeInterpretedResultAdmission`, `CopperfinRuntimeBridgeInterpretedResult`, `CopperfinRuntimeBridgeNativeReturnPlan`, `CopperfinRuntimeBridgeNativeReturnAdmission`, `CopperfinRuntimeBridgeNativeReturn`, `CopperfinRuntimeBridgeOutcomeSelectionPlan`, `CopperfinRuntimeBridgeOutcomeSelectionAdmission`, `CopperfinRuntimeBridgeOutcomeSelection`, `CopperfinRuntimeBridgeReturnMaterializationPlan`, `CopperfinRuntimeBridgeReturnMaterializationAdmission`, `CopperfinRuntimeBridgeReturnMaterialization`, `CopperfinRuntimeBridgeReturnEmissionPlan`, `CopperfinRuntimeBridgeReturnEmissionAdmission`, `CopperfinRuntimeBridgeReturnEmission`, `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, `CopperfinRuntimeBridgeFinalReturnAdoptionAdmission`, `CopperfinRuntimeBridgeFinalReturnAdoption`, `CopperfinRuntimeBridgeReturnActivationPlan`, `CopperfinRuntimeBridgeReturnActivationAdmission`, `CopperfinRuntimeBridgeReturnActivation`, `CopperfinRuntimeBridgeStubReturnPlan`, `CopperfinRuntimeBridgeStubReturnAdmission`, `CopperfinRuntimeBridgeStubReturn`, `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, `CopperfinRuntimeBridgePlaceholderReturnValueAdmission`, `CopperfinRuntimeBridgePlaceholderReturnValue`, `CopperfinRuntimeBridgePlaceholderReturnIntAdmission`, and placeholder-return-int helper surfaces that capture exported routine/source/parameter metadata plus explicit future runtime-host argv, argument-binding, return-binding, working-directory, environment, stdout/stderr log-path, expected-exit-code, executable-path, output-capture, request/response artifact, request/response encoding, final dispatch-argv, payload-shape, return-interpretation, host-failure-policy, response-validation, request-artifact, request-write, response-read, response-artifact, response-parse, response-parse admission, parsed-response, interpreted-result, interpreted-result admission, native-return, native-return admission, outcome-selection, outcome-selection admission, return-materialization, return-materialization admission, return-emission, return-emission admission, final-return-adoption, final-return-adoption admission, return-activation, return-activation admission, stub-return, stub-return admission, placeholder-return-value, placeholder-return-value admission, placeholder-return-int, and placeholder-return-int admission contracts over those package-local bridge paths; the serialization layer now emits the current request/response media-type contracts plus the current schema-version contract through shared helpers instead of repeating those raw inline tokens, the dispatch layer now also emits the current request-path, response-path, request-media-type, response-media-type, and schema-version argv flag contracts through shared helpers instead of repeating those raw inline tokens, the dispatch execution layer now also emits a shared `copperfin_runtime_bridge_execute_dispatch(...)` helper plus a narrow dispatch-execution surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and expected-exit-code contracts flow through one scaffold-only helper before payload construction instead of remaining implicit inside the nested dispatch plan, the process-launch layer now also emits a shared `copperfin_runtime_bridge_launch_process(...)` helper plus a narrow process-launch surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and deterministic launch outcome contracts flow through one scaffold-only helper before payload construction instead of remaining implicit around the dispatch-execution seam, the host-failure evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_host_failure(...)` helper plus a narrow host-failure evaluation surface so staged process-launch output and failure-policy contracts flow through one scaffold-only helper before response validation instead of remaining implicit around the failure-policy seam, the missing-response evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_missing_response(...)` helper plus a narrow missing-response evaluation surface so staged host-failure output and response-read policy contracts flow through one scaffold-only helper before response artifact handling instead of remaining implicit around the response-read seam, the response-validation evaluation layer now also emits a shared `copperfin_runtime_bridge_evaluate_response_validation(...)` helper plus a narrow response-validation evaluation surface so staged missing-response output and response-validation contracts flow through one scaffold-only helper before response artifact handling instead of remaining implicit around the response-validation seam, the response-parse admission layer now also emits a shared `copperfin_runtime_bridge_admit_response_parse(...)` helper plus a narrow response-parse admission surface so staged response-validation output and response-parse contracts flow through one scaffold-only helper before interpreted-result planning instead of remaining implicit around the response-parse admission seam, the interpreted-result admission layer now also emits a shared `copperfin_runtime_bridge_admit_interpreted_result(...)` helper plus a narrow interpreted-result admission surface so staged response-parse admission output and interpreted-result contracts flow through one scaffold-only helper before native-return planning instead of remaining implicit around the interpreted-result admission seam, the native-return admission layer now also emits a shared `copperfin_runtime_bridge_admit_native_return(...)` helper plus a narrow native-return admission surface so staged interpreted-result admission output and native-return contracts flow through one scaffold-only helper before outcome-selection planning instead of remaining implicit around the native-return admission seam, the outcome-selection admission layer now also emits a shared `copperfin_runtime_bridge_admit_outcome_selection(...)` helper plus a narrow outcome-selection admission surface so staged native-return admission output and outcome-selection contracts flow through one scaffold-only helper before return-materialization planning instead of remaining implicit around the outcome-selection admission seam, the return-materialization admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_materialization(...)` helper plus a narrow return-materialization admission surface so staged outcome-selection admission output and return-materialization contracts flow through one scaffold-only helper before return-emission planning instead of remaining implicit around the return-materialization admission seam, the return-emission admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_emission(...)` helper plus a narrow return-emission admission surface so staged return-materialization admission output and return-emission contracts flow through one scaffold-only helper before final-return-adoption planning instead of remaining implicit around the return-emission admission seam, the final-return-adoption admission layer now also emits a shared `copperfin_runtime_bridge_admit_final_return_adoption(...)` helper plus a narrow final-return-adoption admission surface so staged return-emission admission output and final-return-adoption contracts flow through one scaffold-only helper before return-activation planning instead of remaining implicit around the final-return-adoption admission seam, the return-activation admission layer now also emits a shared `copperfin_runtime_bridge_admit_return_activation(...)` helper plus a narrow return-activation admission surface so staged final-return-adoption admission output and return-activation contracts flow through one scaffold-only helper before stub-return planning instead of remaining implicit around the return-activation admission seam, the stub-return admission layer now also emits a shared `copperfin_runtime_bridge_admit_stub_return(...)` helper plus a narrow stub-return admission surface so staged return-activation admission output and stub-return contracts flow through one scaffold-only helper before placeholder-return-value planning instead of remaining implicit around the stub-return admission seam, the placeholder-return-value admission layer now also emits a shared `copperfin_runtime_bridge_admit_placeholder_return_value(...)` helper plus a narrow placeholder-return-value admission surface so staged stub-return admission output and placeholder-return-value contracts flow through one scaffold-only helper before placeholder-return-int routing instead of remaining implicit around the placeholder-return-value admission seam, the placeholder-return-int admission layer now also emits a shared `copperfin_runtime_bridge_admit_placeholder_return_int(...)` helper plus a narrow placeholder-return-int admission surface so staged placeholder-return-value admission output and placeholder-return-int contracts flow through one scaffold-only helper before final stub emission instead of remaining implicit around the placeholder-return-int admission seam, the request-write, response-read, and response-parse layers now also emit the current write-mode, read-mode, and parser-kind policy contracts through shared helpers instead of repeating those raw inline tokens, the request-write layer now also emits a shared `copperfin_runtime_bridge_execute_write_request(...)` helper so staged request-document writes run through one executable helper path that creates the parent directory when required and writes `plan.request_artifact.request_document` at the planned request artifact path instead of leaving request-write execution implicit, the response-read layer now also emits a shared `copperfin_runtime_bridge_execute_read_response(...)` helper so staged response-document reads run through one executable helper path that falls back to the shared empty-response contract when the staged artifact is missing or unreadable and otherwise streams the response payload from `plan.source_path` instead of leaving response-read execution implicit, the response-parse layer now also emits a shared `copperfin_runtime_bridge_execute_parse_response(...)` helper plus a narrow parsed-response surface so staged response documents run through one scaffold-only field-extraction path for the planned `status`, `return_value`, and `diagnostics` fields instead of leaving response-parse execution implicit, the interpreted-result layer now also emits a shared `copperfin_runtime_bridge_execute_interpreted_result(...)` helper plus a narrow interpreted-result surface so parsed response status can flow through one scaffold-only success/fallback return-value selection path before native-return materialization instead of leaving interpreted-result execution implicit, the native-return layer now also emits a shared `copperfin_runtime_bridge_execute_native_return(...)` helper plus a narrow native-return surface so interpreted success/fallback selection can flow through one scaffold-only native return-value/int-value selection path before outcome selection instead of leaving native-return execution implicit, the outcome-selection layer now also emits a shared `copperfin_runtime_bridge_execute_outcome_selection(...)` helper plus a narrow outcome-selection surface so native-return success/fallback selection can flow through one scaffold-only selected-condition path before return materialization instead of leaving outcome-selection execution implicit, the return-materialization layer now also emits a shared `copperfin_runtime_bridge_execute_return_materialization(...)` helper plus a narrow return-materialization surface so outcome-selection metadata can flow through one scaffold-only success/fallback native return-statement materialization path before return emission instead of leaving return-materialization execution implicit, the return-emission layer now also emits a shared `copperfin_runtime_bridge_execute_return_emission(...)` helper plus a narrow return-emission surface so return-materialization metadata can flow through one scaffold-only emitted-branch-block path before final-return adoption instead of leaving return-emission execution implicit, the final-return-adoption layer now also emits a shared `copperfin_runtime_bridge_execute_final_return_adoption(...)` helper plus a narrow final-return-adoption surface so return-emission metadata can flow through one scaffold-only adopted-return-block path before return activation instead of leaving final-return-adoption execution implicit, the return-activation layer now also emits a shared `copperfin_runtime_bridge_execute_return_activation(...)` helper plus a narrow return-activation surface so final-return-adoption metadata can flow through one scaffold-only active/deferred return-selection path before stub-return handling instead of leaving return-activation execution implicit, the stub-return layer now also emits a shared `copperfin_runtime_bridge_execute_stub_return(...)` helper plus a narrow stub-return surface so return-activation metadata can flow through one scaffold-only placeholder/deferred stub-return path before placeholder-return-value handling instead of leaving stub-return execution implicit, the placeholder-return-value layer now also emits a shared `copperfin_runtime_bridge_execute_placeholder_return_value(...)` helper plus a narrow placeholder-return-value surface so stub-return metadata can flow through one scaffold-only placeholder-return contract before final DLL/FLL stub emission instead of leaving placeholder-return-value execution implicit, the final stub-emission layer now also emits a shared `copperfin_runtime_bridge_execute_placeholder_return_int(...)` helper so final placeholder integer selection flows through one scaffold-only helper before DLL direct return or FLL `_RetInt(...)` surface application instead of staying inline inside the output-specific stub emitters, the shared parsed-int helper layer now also emits the current default integer sentinel through a shared helper instead of repeating the raw inline `-1` token inside the parser, the response-artifact layer now also emits the current empty response-document token through a shared helper instead of repeating the raw inline empty-string literal, the payload layer now emits the current request/response shape contracts plus the current request/response field-name contracts through shared helpers instead of repeating those raw inline tokens, the payload and interpretation layers now emit the current response `status`, `return_value`, and `diagnostics` field-name contracts through shared helpers instead of repeating those raw inline tokens across multiple builders, the failure-policy layer now records non-zero-exit handling, missing-response handling, a deterministic `runtime_host_failure` diagnostics fallback, and the placeholder return fallback value for each stub, and it now emits that diagnostics fallback through a shared helper instead of repeating the raw token inline, the response-validation layer records the expected response media type, schema version, required response fields, and the success-status token expected by the wrapper scaffold before it would accept a host response, and it now emits that success-status token through a shared helper instead of repeating the raw token inline, the request-artifact layer records the serialized request document that would be written to the planned request-path artifact, the request-write layer records the concrete target request path plus overwrite policy that would be used before launch, the response-read layer records the concrete staged response path plus text-read policy that would be used after launch, the response-artifact layer records the raw staged response document payload before any parsing or interpretation, the response-parse layer records the parser kind plus status/value/diagnostics field extraction contract that would be applied to that raw document, the interpreted-result layer records the combined success-status, wrapper-return-surface, and fallback-return contract that would drive the eventual native return path, the native-return layer now also records typed success and fallback integer values alongside the success/fallback native value representations plus final DLL/FLL return surface that would eventually materialize from that interpreted result, reusing `copperfin_parse_runtime_bridge_int_value_representation(...)` to derive those typed values from the existing success/fallback representation metadata instead of hardcoding them again, the return-materialization layer now also emits a shared native-`int` return-surface helper so the current plain-`int` branch no longer repeats that raw inline token, a shared native-`int` placeholder-signature helper so typed `_RetInt(int)`-style matching no longer repeats the raw inline `"(int)"` token, and a shared native return-statement framing helper so the plain-`int`, typed placeholder, and representation fallback branches now all reuse one `return ...;` contract instead of repeating the raw framing tokens before the function falls back to representation-based materialization for any non-`int` surfaces, the outcome-selection layer records the explicit success/fallback branch conditions plus diagnostics field that would govern which native return path is chosen, the return-materialization layer now routes the current `int` / `_RetInt(int)` success/fallback statements through those parsed typed integer values while preserving the existing representation-based fallback path for any non-`int` surfaces, the return-emission layer now records an explicit fallback else-branch statement and composes the emitted return block from the prebuilt success/fallback branch statements instead of re-materializing the fallback return inline, the final-return-adoption layer records how that emitted return block would replace the current placeholder native return in the generated wrapper stub, the return-activation layer now also seeds the future `active_return_block` from that adopted return block while keeping activation disabled so the scaffold still emits the placeholder return today, the stub-return layer now routes its inactive `deferred_return_block` through `return_activation_plan.active_return_block` instead of reaching back to final-adoption metadata, explicitly carries the placeholder fallback integer/value metadata needed by the current helper path, flattens the upstream `activation_mode` / `adoption_mode` contracts for downstream consumption, derives explicit placeholder-helper active/replacement policy booleans from that upstream metadata, and now also emits shared placeholder return-binding and placeholder return-statement helpers so exported stubs, failure-policy fallback metadata, and final-return adoption all consume one shared placeholder return contract instead of repeating raw inline `-1` binding and return-statement literals, while the placeholder-return-value layer carries those activation/adoption mode contracts plus the derived helper-policy booleans alongside the deferred return block, placeholder-emission flag, emitted placeholder return statement, and fallback value metadata so the current helper consumes the full plan-level activation/deferred/emitted/helper-policy contract instead of reconstructing the same policy from string comparisons in the helper body, and the host-toolchain compile check matches the emitted CMake visibility policy so wrapper build checks track only the deliberate callable/data export surface
- Native-wrapper stub-emission admission notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_admit_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionAdmission` surface so staged placeholder-return-int admission output and final stub-emission contracts flow through one scaffold-only helper before output-specific stub emission instead of remaining implicit around the final stub-emission seam
- Native-wrapper stub-emission execution notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_execute_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmission` surface so staged placeholder-return-value execution and placeholder-return-int execution flow through one scaffold-only helper before DLL/FLL output-specific stub emission instead of remaining implicit across the final stub-emission execution seam
- Native-wrapper stub-emission return-surface notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionReturnSurface` surface so shared stub-emission execution output flows through one scaffold-only helper before DLL/FLL output-specific stub return application instead of remaining implicit across the final stub-emission return-surface seam
- Native-wrapper stub-emission output-application notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_apply_stub_emission_output(...)` helper so shared stub-emission return-surface output flows through one scaffold-only helper before the final DLL/FLL return adapter is applied instead of remaining implicit inside the output-specific stub emitters
- Native-wrapper stub-emission emitter notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `copperfin_runtime_bridge_emit_stub_return_shared(...)` helper so the formerly duplicated DLL/FLL `copperfin_runtime_bridge_emit_stub_return(...)` bodies now flow through one scaffold-only emitter helper before output-specific wrapper forwarding instead of remaining duplicated across the two output-specific emitters
- Native-wrapper stub-emission wrapper notes — generated DLL/FLL native-wrapper scaffolds now also emit a shared `CopperfinRuntimeBridgeStubEmissionWrapper` surface plus `copperfin_runtime_bridge_build_stub_emission_wrapper(...)` so the remaining output-specific `copperfin_runtime_bridge_emit_stub_return(...)` bodies now only construct one wrapper contract before handing off to the shared stub-emission emitter helper instead of forwarding raw return-surface and adapter inputs directly
- Native-wrapper stub-emission wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the placeholder-return-value plan and then route placeholder returns straight through `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping separate output-specific stub-return wrapper helpers
- Native-wrapper stub-return wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeStubReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the stub-return plan and then build placeholder-return-value plans straight from the enriched stub-return plan instead of passing the wrapper separately at the placeholder-plan seam
- Native-wrapper return-activation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnActivationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-activation plan and then build stub-return plans straight from the enriched activation plan instead of passing the wrapper separately at the stub-return-plan seam
- Native-wrapper final-return-adoption wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the final-return-adoption plan and then build return-activation plans straight from the enriched final-return-adoption plan instead of passing the wrapper separately at the return-activation-plan seam
- Native-wrapper return-emission wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnEmissionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-emission plan and then build final-return-adoption plans straight from the enriched return-emission plan instead of passing the wrapper separately at the final-return-adoption-plan seam
- Native-wrapper return-materialization wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnMaterializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-materialization plan and then build return-emission plans straight from the enriched return-materialization plan instead of passing the wrapper separately at the return-emission-plan seam
- Native-wrapper outcome-selection wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeOutcomeSelectionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the outcome-selection plan and then build return-materialization plans straight from the enriched outcome-selection plan instead of passing the wrapper separately at the return-materialization-plan seam
- Native-wrapper native-return wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeNativeReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the native-return plan and then build outcome-selection plans straight from the enriched native-return plan instead of passing the wrapper separately at the outcome-selection-plan seam
- Native-wrapper interpreted-result wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretedResultPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpreted-result plan and then build native-return plans straight from the enriched interpreted-result plan instead of passing the wrapper separately at the native-return-plan seam
- Native-wrapper response-parse wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseParsePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-parse plan and then build interpreted-result plans straight from the enriched response-parse plan instead of passing the wrapper separately at the interpreted-result-plan seam
- Native-wrapper response-artifact wrapper notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the response artifact and then build response-parse plans straight from the enriched response artifact instead of passing the wrapper separately at the response-parse-plan seam
- Native-wrapper response-read wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseReadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-read plan and then build response artifacts straight from the enriched response-read plan instead of passing the wrapper separately at the response-artifact seam
- Native-wrapper request-write wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestWritePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the request-write plan and then build response-read plans straight from the enriched request-write plan instead of passing the wrapper separately at the response-read-plan seam
- Native-wrapper request-artifact wrapper notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the request artifact and then build request-write plans straight from the enriched request artifact instead of passing the wrapper separately at the request-write-plan seam
- Native-wrapper bridge response-contract expectation notes — generated DLL/FLL native-wrapper request JSON now carries expected response media type plus a `response_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::response_fields`, so runtime-host bridge consumers can produce and validate the matching response envelope from the same wrapper request document
- Native-wrapper response-document validation-routing notes — generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_response_validation(...)`, and `CopperfinRuntimeBridgeResponseValidationEvaluation` records response-document availability from the response body so response-contract enforcement has the actual bridge payload at the validation seam
- Native-wrapper required response-field validation notes — generated DLL/FLL native-wrapper response validation now checks `response_document` for every field declared by `CopperfinRuntimeBridgeResponseValidationPlan::required_response_fields` and routes to the existing fallback return path when any required response field is absent
- Native-wrapper response media-type validation notes — generated DLL/FLL native-wrapper response validation now extracts the response document's `response_media_type` field and compares it with `CopperfinRuntimeBridgeResponseValidationPlan::expected_response_media_type`, routing to the existing fallback return path when the field is absent or mismatched
- Native-wrapper response schema-version validation notes — generated DLL/FLL native-wrapper response-field contracts now include `schema_version`, and response validation compares the response document's schema-version field with `CopperfinRuntimeBridgeResponseValidationPlan::expected_schema_version`, routing to the existing fallback return path when the field is absent or mismatched
- Native-wrapper runtime-host dispatch execution notes — generated DLL/FLL native-wrapper launch helpers now quote and build a runtime-host command line from `CopperfinRuntimeBridgeDispatchExecution`, execute it through `std::system(...)`, capture the observed exit code, and mark launch success only when that exit code matches the expected exit code instead of returning deterministic launch failure
- Native-wrapper launch-environment dispatch notes — generated DLL/FLL native-wrapper dispatch execution now carries `CopperfinRuntimeBridgeLaunchPlan::environment` into command construction, and runtime-host command lines now apply those environment assignments before launching the host
- Native-wrapper missing-response document-evaluation notes — generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_missing_response(...)`, and missing-response policy treats the response as missing only when an existing response is required and the read document is empty
- Native-wrapper bridge request-field serialization notes — generated DLL/FLL native-wrapper request JSON now emits a `request_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::request_fields`, so runtime-host bridge consumers can inspect the exact request contract carried by the wrapper request document
- Native-wrapper bridge request-field contract notes — generated DLL/FLL native-wrapper `CopperfinRuntimeBridgePayloadPlan::request_fields` now declares the same descriptor metadata fields serialized by the bridge request document, so the request-field contract stays synchronized with routine kind, source provenance, parameter declaration style, parameter-name, schema-version, export, arity, media-type, and parameter payload fields
- Studio builder/wizard registry notes — Studio now exposes a context-aware VFP-compatible builder and wizard registry for form, class, control, report, label, project, and data-environment designer surfaces, preserving `builder.app`, `ReportBuilder.app`, and `Wizards` equivalence metadata while giving modern designer shells stable Copperfin entry-point tokens and per-context launch, invocation-admission, dispatch, and injected execution seams to filter and run relevant actions by context; dispatch catalogs now route through the shared admission catalog seam, `copperfin_studio_host --builder-launch-catalog` plus `--builder-invocation-admission-catalog` expose the non-executing launch/admission catalogs as JSON without opening UI, spawning builders, or mutating assets, and `copperfin_studio_host --builder-execute` exposes explicitly admitted builder/wizard dispatch execution with launch-command, executed-command, observed-exit-code, and no-stale-metadata failure reporting.
- Studio toolbox palette notes — Studio now exposes a VFP-compatible toolbox palette registry for common form, class, container, and report designer controls, including VFP class/baseclass metadata, default object-name prefixes, container flags, context filtering, and non-mutating launch catalogs across known Studio selection contexts so modern shells can drive toolbox creation without hardcoded UI-only control lists; toolbox dispatch now also has an injected execution seam with explicit admission, item metadata validation, launch observation, executed-state, and no-stale-metadata failure reporting; `copperfin_studio_host --toolbox-palette-launch-catalog` exposes the launch catalog as JSON without opening UI, spawning processes, creating controls, or mutating assets, and `copperfin_studio_host --toolbox-execute` exposes explicitly admitted toolbox dispatch execution with launch-command, executed-command, observed-exit-code, item metadata, and failure reporting.
- Studio context-editor action notes — Studio now exposes selection-context action metadata, non-executing launch catalogs, invocation-admission catalogs, dispatch catalogs, aggregate designer catalogs, toolbox palette admission catalogs, and toolbox dispatch catalogs for property-grid, method-editor, expression-editor, builder, toolbox, data-environment, and project-navigation surfaces so designer hosts can choose relevant editor commands for visual objects, report expressions, project items, and data-environment selections without hardcoded UI conditionals; editor-action dispatch now also has an injected execution seam with explicit admission, launch observation, executed-state, and no-stale-metadata failure reporting; `copperfin_studio_host --editor-action-launch-catalog`, `--editor-action-invocation-admission-catalog`, `--editor-action-execute`, and `--toolbox-invocation-admission-catalog` expose selected catalogs or admitted execution results as JSON.
- Studio designer-context aggregation notes — Studio now exposes a single active-selection context query that combines the context-aware editor action registry, builder/wizard registry, and toolbox palette registry so hosts can populate designer panes and commands from one filtered model for visual objects, report expressions, project items, and data-environment selections; aggregate designer dispatch now also has an execution seam that composes editor-action, builder, and toolbox execution with strict aggregate preflight so invalid aggregate inputs cannot launch partial child executions, `copperfin_studio_host --designer-execute` exposes that admitted aggregate execution path as JSON with child identity metadata, child dispatch argument arrays, child execution-state metadata, child process summaries, and exact child executed-command strings, and both `copperfin_studio_host --designer-dispatch-catalog` and `--designer-dispatch-execution-catalog` preserve nested child dispatch command tokens and dispatch arguments for non-mutating aggregate inspection.
- Studio selection-context builder launch/admission/dispatch notes — Studio now exposes non-executing builder/wizard launch, invocation-admission, and dispatch catalogs for one selected Studio context, so visual-object selections can preserve mixed form/control builder metadata and menu/data-environment selections can expose their context-specific builders without opening UI, spawning builders, or mutating assets; `copperfin_studio_host --selection-builder-launch-catalog`, `--selection-builder-invocation-admission-catalog`, and `--selection-builder-dispatch-catalog` expose the selection-scoped catalogs as JSON.
- Studio opened-document designer-context notes — opened Studio document models now carry default designer-context aggregates derived from asset kind, so form/class, report/label, project, and table/database-container assets expose relevant editor actions, builder/wizard entries, and toolbox entries directly from `open_document`.
- Studio host designer-context JSON notes — `copperfin_studio_host --json` now serializes opened-document designer contexts with stable selection-context tokens, editor action descriptors, builder/wizard descriptors, and toolbox item descriptors so external Studio and Visual Studio shells can consume the same context-aware model as in-process hosts.
- Studio host report-layout provenance JSON notes — `copperfin_studio_host --json` now serializes report/label layout provenance from `StudioReportLayoutSnapshot`, including document-title provenance, live/deleted setting/section/object counts, deleted collections, section/object raw codes, field ordinals, memo block numbers, memo-line indexes, highlight provenance, and unplaced-object counts for external report and label designers.
- Studio host selected report-section JSON notes — `copperfin_studio_host --json --record <section-record>` now exposes `selectedReportSectionAvailable` plus a provenance-rich `selectedReportSection` when the active report/label selection is a band record, and reports false/null for non-section selections.
- Studio host selected report-object JSON notes — `copperfin_studio_host --json --record <layout-object-record>` now exposes `selectedReportObjectAvailable` plus a provenance-rich `selectedReportObject` when the active report/label selection is a layout object record, and reports false/null for non-object selections.
- Studio host selected report-object section JSON notes — `copperfin_studio_host --json --record <layout-object-record>` now exposes `selectedReportObjectSectionAvailable` plus a provenance-rich `selectedReportObjectSection` for section-contained report/label layout objects, and reports false/null for unplaced, deleted, non-object, or unavailable selections.
- Studio host selected report-settings JSON notes — `copperfin_studio_host --json --record <root-settings-record>` now exposes `selectedReportSettingsAvailable` plus provenance-rich `selectedReportSettings` for report/label root settings, and reports false/null for non-settings selections.
- Studio host report-selection kind JSON notes — `copperfin_studio_host --json` now exposes `selectedReportSelectionAvailable` and `selectedReportSelectionKind` (`settings`, `section`, `object`, or `none`) so report/label designer hosts can branch on the active selection without probing each selected report payload.
- Studio host report printer-identity summary JSON notes — `copperfin_studio_host --json` now exposes additive `driverAvailable`/`driver`, `deviceAvailable`/`device`, and `outputAvailable`/`output` fields in the shared report-layout summary for FRX/LBX root settings, so hosts can consume printer identity without re-scanning raw settings arrays.
- Studio report object containing-section metadata notes — report/label layout objects now carry containing section id, containing section record index, and section-relative top coordinates in the core report layout model and Studio host JSON, with explicit null/missing metadata for unplaced or deleted objects.
- Studio report object section-order metadata notes — report/label layout objects now carry zero-based object order and object count within their containing section in the core report layout model and Studio host JSON, with explicit null/missing order metadata for unplaced or deleted objects.
- Studio report section order metadata notes — report/label sections now carry zero-based section order and live section count in the core report layout model, full Studio host report-layout JSON, and selected-section JSON, with explicit null/missing order metadata for deleted sections.
- Studio report layout bottom-edge metadata notes — report/label sections and layout objects now carry explicit bottom-edge coordinates in the core report layout model and Studio host JSON, including section-relative object bottom coordinates for section-contained objects while unplaced or deleted objects avoid fabricating relative section bottoms.
- Studio report layout right-edge metadata notes — report/label layout objects now carry explicit right-edge coordinates in the core report layout model and Studio host JSON so designer clients can consume full horizontal object spans without recomputing from left and width fields.
- Studio report layout nudge mutation notes — Studio host `--nudge-object` now has regression coverage proving stable-selector mutation of FRX report layout object `HPOS`/`VPOS` fields and refreshed report-layout JSON recomputation of absolute, edge, section-relative, and containing-section geometry after the mutation.
- Studio report layout alignment mutation notes — Studio host `--align-object --alignment-mode left` now has regression coverage proving stable-selector alignment of FRX report layout object `HPOS` fields and refreshed report-layout JSON recomputation of horizontal geometry after the mutation while preserving vertical and containing-section metadata.
- Studio report layout resize mutation notes — Studio host `--resize-object --resize-mode size` now has regression coverage proving stable-selector resizing of FRX report layout object `WIDTH`/`HEIGHT` fields and refreshed report-layout JSON recomputation of object spans after the mutation while preserving position and containing-section metadata.
- Studio report layout snap-to-grid mutation notes — Studio host `--snap-object --snap-mode both` now has regression coverage proving stable-selector grid snapping of FRX report layout object `HPOS`/`VPOS` fields and refreshed report-layout JSON recomputation of absolute, edge, section-relative, and containing-section geometry after the mutation.
- Studio report layout delete-state mutation notes — Studio host `--delete-object` now has regression coverage proving stable-selector deletion of FRX report layout object records and refreshed report-layout JSON movement from live section membership into deleted report objects while preserving selected-object metadata and false/null containing-section metadata.
- Studio report layout restore mutation notes — Studio host `--restore-object` now has regression coverage proving stable-selector restoration of deleted FRX report layout object records and refreshed report-layout JSON movement back into live section membership with selected-object and selected containing-section metadata available again.
- Studio deleted report layout object expression edit notes — Studio host `--set-property --record <deleted-layout-object-record> --property-name EXPR` now has regression coverage proving deleted FRX/LBX layout-object expression memo edits persist, refresh deleted-object and selected deleted-object expression metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio deleted report layout object expression clear notes — Studio host `--clear-property --record <deleted-layout-object-record> --property-name EXPR` now has regression coverage proving deleted FRX/LBX layout-object expression clears blank the memo property, refresh deleted-object and selected deleted-object expression metadata with fallback titles, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio deleted report layout object font edit notes — Studio host `--set-property --record <deleted-layout-object-record> --property-name FONTFACE` now has regression coverage proving deleted FRX/LBX layout-object font memo edits persist, refresh deleted-object and selected deleted-object highlight metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio deleted report layout object font clear notes — Studio host `--clear-property --record <deleted-layout-object-record> --property-name FONTFACE` now has regression coverage proving deleted FRX/LBX layout-object font clears remove seeded font highlights, refresh deleted-object and selected deleted-object highlight metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio deleted report layout object width edit notes — Studio host `--set-property --record <deleted-layout-object-record> --property-name WIDTH` now has regression coverage proving deleted FRX/LBX layout-object width edits persist, refresh deleted-object and selected deleted-object right-edge geometry, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio deleted report layout object width clear notes — Studio host `--clear-property --record <deleted-layout-object-record> --property-name WIDTH` now has regression coverage proving deleted FRX/LBX layout-object width clears recompute right-edge geometry, refresh deleted-object and selected deleted-object geometry metadata, and keep containing-section metadata unavailable/null instead of fabricating live section membership.
- Studio report layout distribution mutation notes — Studio host `--distribute-object --distribution-mode horizontal` now has regression coverage proving stable-selector redistribution of FRX report layout object `HPOS` fields and refreshed report-layout JSON recomputation of object coordinates while preserving containing-section and section object order/count metadata.
- Studio report layout reorder mutation notes — Studio host `--reorder-object --placement before` now has regression coverage proving stable-selector reorder of FRX report layout object records and refreshed report-layout JSON recomputation of section object indexes for tied-geometry objects.
- Studio report layout duplicate mutation notes — Studio host `--duplicate-object` now has regression coverage proving stable-selector duplication of FRX report layout object records, replacement `UNIQUEID` persistence, and refreshed report-layout JSON membership/index/count metadata for the appended copy.
- Studio report layout rename identity mutation notes — Studio host `--rename-object` now has regression coverage proving stable-selector replacement of FRX report layout object `UNIQUEID` values while preserving record order and refreshed live section membership/index/count metadata.
- Studio report section delete-state mutation notes — Studio host `--delete-object --record <section-record>` now has regression coverage proving FRX section-row delete flags persist and refreshed report-layout JSON moves the section to deleted-section metadata while leaving former section objects unplaced instead of fabricating containing sections.
- Studio report section restore mutation notes — Studio host `--restore-object --record <section-record>` now has regression coverage proving deleted FRX section rows can be restored, refreshed report-layout JSON returns them to live section metadata, and former section objects regain containing-section membership.
- Studio report settings delete-state mutation notes — Studio host `--delete-object --record <settings-record>` now has regression coverage proving FRX root settings delete flags persist and refreshed report-layout JSON moves root settings to deleted-settings metadata without disturbing section/object metadata.
- Studio report settings restore mutation notes — Studio host `--restore-object --record <settings-record>` now has regression coverage proving deleted FRX root settings rows can be restored and refreshed report-layout JSON returns root settings to live settings metadata without disturbing section/object metadata.
- Studio deleted report settings selection notes — Studio host `--json --record <settings-record>` now has regression coverage proving DBF-deleted root settings rows still classify as report settings selections and expose selected-settings provenance while live/deleted settings counts remain distinct.
- Studio deleted report section selection notes — Studio host `--json --record <section-record>` now has regression coverage proving DBF-deleted section rows still classify as report section selections and expose selected-section metadata while live/deleted section counts remain distinct.
- Studio deleted report object selection notes — Studio host `--json --record <layout-object-record>` now has regression coverage proving DBF-deleted report layout objects still classify as report object selections, expose deleted selected-object metadata, and keep containing-section metadata explicitly unavailable.
- Studio unplaced report object selection notes — Studio host `--json --record <layout-object-record>` now has regression coverage proving live unplaced report layout objects classify as report object selections, expose selected-object metadata, and keep containing-section metadata explicitly unavailable.
- Studio label layout parity notes — Studio host `.lbx` label assets now have regression coverage proving `reportLayout.isLabel`, selected label object metadata, selected label section metadata, live settings, live sections, and containing-section metadata use the same JSON contract as `.frx` report assets.
- Studio label layout nudge mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--nudge-object` mutates stable-selector label layout object `HPOS`/`VPOS` fields and refreshed label report-layout JSON recomputes object geometry while preserving `isLabel`.
- Studio label layout alignment mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--align-object --alignment-mode left` mutates stable-selector label layout object `HPOS` fields and refreshed label report-layout JSON recomputes horizontal geometry while preserving `isLabel`.
- Studio label layout resize mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--resize-object --resize-mode size` mutates stable-selector label layout object `WIDTH`/`HEIGHT` fields and refreshed label report-layout JSON recomputes span and edge geometry while preserving `isLabel`.
- Studio label layout snap-to-grid mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--snap-object --snap-mode both` mutates stable-selector label layout object `HPOS`/`VPOS` fields to grid-aligned values and refreshed label report-layout JSON recomputes geometry while preserving `isLabel`.
- Studio label layout delete-state mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--delete-object` marks stable-selector label layout objects deleted, moves them into deleted-object metadata, keeps selected deleted-object JSON available, and preserves `isLabel`.
- Studio label layout restore mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--restore-object` clears stable-selector label layout object delete flags, restores live section membership and selected containing-section metadata, and preserves `isLabel`.
- Studio label layout distribution mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--distribute-object --distribution-mode horizontal` redistributes stable-selector label layout object `HPOS` values, preserves vertical geometry and size, refreshes section order/count metadata, and preserves `isLabel`.
- Studio label layout reorder mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--reorder-object --placement before` persists stable-selector physical object order changes, refreshes section object index/count metadata, and preserves `isLabel`.
- Studio label layout duplicate mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--duplicate-object --new-unique-id` appends a copied label layout object with the requested replacement `UNIQUEID`, refreshes live section membership/index/count metadata, and preserves `isLabel`.
- Studio label layout rename identity mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--rename-object --new-unique-id` replaces stable-selector label layout object `UNIQUEID` values while preserving object count/order, refreshing live section membership metadata, and preserving `isLabel`.
- Studio label section delete-state mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--delete-object --record <section-record>` persists section delete flags, moves label sections into deleted-section metadata, preserves selected deleted-section JSON, leaves former section objects unplaced without fabricated containing-section metadata, and preserves `isLabel`.
- Studio label section restore mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--restore-object --record <section-record>` clears section delete flags, restores live section counts, clears deleted-section counts, returns former unplaced objects to section membership, restores containing-section metadata, and preserves `isLabel`.
- Studio label settings delete-state mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--delete-object --record <settings-record>` persists root/settings delete flags, moves root settings into deleted-setting metadata, preserves selected deleted-settings JSON, preserves section/object metadata, and preserves `isLabel`.
- Studio label settings restore mutation notes — Studio host `.lbx` label assets now have regression coverage proving `--restore-object --record <settings-record>` clears root/settings delete flags, restores live settings counts, clears deleted-setting counts, preserves selected settings JSON, preserves section/object metadata, and preserves `isLabel`.
- Studio selected label settings JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <settings-record>` exposes shared `selectedReportSettings` JSON, selected settings kind/availability, memo-line and direct setting provenance, section metadata, layout-object metadata, and `isLabel`.
- Studio selected deleted label settings JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-settings-record>` exposes shared `selectedReportSettings` JSON, selected settings kind/availability, deleted-setting counts/provenance, section metadata, deleted-object metadata, and `isLabel`.
- Studio selected deleted label section JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-section-record>` exposes shared selected-section JSON, selected section kind/availability, deleted-section counts/provenance, unplaced former section objects without fabricated containing-section metadata, and `isLabel`.
- Studio selected deleted label object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-object-record>` exposes shared selected-object JSON, selected object kind/availability, deleted-object counts, explicit null selected containing-section metadata, and `isLabel`.
- Studio selected unplaced label object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <unplaced-object-record>` exposes shared selected-object JSON, selected object kind/availability, unplaced-object counts, explicit null selected containing-section metadata, and `isLabel`.
- Studio unselected label layout summary JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json` without `--record` exposes shared report-layout summary JSON, label identity, live setting/section counts, deleted/unplaced object counts, setting provenance, and explicit `selectedReportSelectionKind: none` no-selection metadata.
- Studio selected label section no-object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <section-record>` exposes shared selected-section JSON while keeping selected-object and containing-object-section metadata explicitly unavailable/null.
- Studio selected deleted label section no-object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-section-record>` exposes shared deleted selected-section JSON while keeping selected-object and containing-object-section metadata explicitly unavailable/null.
- Studio selected label settings no-section/object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <settings-record>` exposes shared selected-settings JSON while keeping selected-section, selected-object, and containing-object-section metadata explicitly unavailable/null.
- Studio selected deleted label settings no-section/object JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-settings-record>` exposes shared deleted selected-settings JSON while keeping selected-section, selected-object, and containing-object-section metadata explicitly unavailable/null.
- Studio selected label object no-section/settings JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <object-record>` exposes shared selected-object JSON and containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected deleted label object no-section/settings JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <deleted-object-record>` exposes shared selected deleted-object JSON and null containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected unplaced label object no-section/settings JSON notes — Studio host `.lbx` label assets now have regression coverage proving `--json --record <unplaced-object-record>` exposes shared selected unplaced-object JSON and null containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected report object no-section/settings JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <object-record>` exposes shared selected-object JSON and containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected deleted report object no-section/settings JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <deleted-object-record>` exposes shared selected deleted-object JSON and null containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected unplaced report object no-section/settings JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <unplaced-object-record>` exposes shared selected unplaced-object JSON and null containing-object-section metadata while keeping selected-section and selected-settings metadata explicitly unavailable/null.
- Studio selected report section no-object/settings JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <section-record>` exposes shared selected-section JSON while keeping selected-object, containing-object-section, and selected-settings metadata explicitly unavailable/null.
- Studio selected deleted report section no-object/settings JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <deleted-section-record>` exposes shared deleted selected-section JSON while keeping selected-object, containing-object-section, and selected-settings metadata explicitly unavailable/null.
- Studio selected report settings no-section/object JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <settings-record>` exposes shared selected-settings JSON while keeping selected-section, selected-object, and containing-object-section metadata explicitly unavailable/null.
- Studio selected deleted report settings no-section/object JSON notes — Studio host `.frx` report assets now have regression coverage proving `--json --record <deleted-settings-record>` exposes shared deleted selected-settings JSON while keeping selected-section, selected-object, and containing-object-section metadata explicitly unavailable/null.
- Studio host selection-context override notes — Studio launch requests now accept repeatable `--selection-context` tokens so external hosts can request active visual-object, visual-method, report-expression, project-item, or data-environment context surfaces explicitly instead of relying only on asset-kind defaults.
- Studio host method-symbol inference notes — form/class launches with method-like symbols such as `cmdSave.Click` now infer the visual-method designer context automatically when no explicit selection-context override is present, aligning default context-aware editor output with Visual Studio method navigation.
- Studio launch-selection metadata notes — opened Studio documents and Studio host JSON now preserve the requested selection symbol, line, column, and record index so designer hosts can correlate active context output with the Visual Studio navigation target.
- Studio/Visual Studio launch-contract error notes — `test_studio_host` now covers malformed and missing numeric selection values for VS launch-contract `--record`, `--line`, and `--column` arguments, proving those parser paths reject invalid navigation metadata with explicit errors.
- Studio DataEnvironment symbol inference notes — form/class launches targeting DataEnvironment symbols such as `Dataenvironment.OpenTables` now infer the data-environment designer context before generic visual-method fallback, keeping data-environment actions and builders visible for those navigation targets.
- Studio designer context count notes — designer-context results and Studio host JSON now expose editor-action, builder, and toolbox-item counts derived from the filtered context descriptor vectors.
- Studio visual context builder notes — visual-object designer contexts now expose the VFP-compatible form builder alongside existing control/grid builders, while visual-method contexts keep the narrower control-builder set.
- Studio label context notes — LBX/LBT documents now use an explicit `label_expression` designer context with shared expression editing, report-safe toolbox items, and label-wizard builder metadata instead of inheriting report-builder metadata.
- Studio class context notes — VCX/VCT documents now use an explicit `class_designer` designer context with property-grid, method-editor, class-safe toolbox items, and class-builder metadata instead of inheriting form/control builder metadata from the generic visual-object context.
- Studio menu context notes — MNX/MNT documents now use an explicit `menu_item` designer context with property-grid actions, a dedicated menu command/detail editor action (`edit-menu-command` / `studio.menu_command_editor.open`), and menu-designer builder metadata, while reporting zero toolbox items and excluding unrelated form/report builder metadata.
- Studio container context notes — Studio launch requests can now target an explicit `container_object` designer context that exposes property-grid, method-editor, control/grid builder metadata, and container-safe toolbox items without inheriting top-level form/class/report/menu/project/data-environment builders.
- Studio selected-container inference notes — form/class launches with a selected Container, PageFrame, Page, Grid, or Column record now infer the `container_object` designer context when no explicit selection-context override is present, keeping nested container editing on container-safe builders and toolbox entries.
- Studio report/label DataEnvironment inference notes — DataEnvironment symbols and selected DataEnvironment records now infer the `data_environment` designer context for report and label assets as well as form/class assets, while expression-context defaults remain unchanged for ordinary FRX/LBX launches.
- Studio toolbox descriptor creation notes — Studio now resolves toolbox palette descriptors into rollback-safe VFP visual-object create requests through `create_visual_object_from_toolbox_item`, including default object-name generation, descriptor class/baseclass mapping, optional identity/parent metadata, caller-provided field values, and no-mutation failure for unknown toolbox item ids.
- Studio selection-context toolbox creation execution notes — Studio now resolves selected-context single and ordered batch toolbox creates through `create_visual_object_from_toolbox_selection` and `create_visual_objects_from_toolbox_selection`, preserving launch metadata, generated-name reservation, explicit per-item identity/parent/field values, record indexes, created-object metadata, and no-mutation failure behavior for unsupported selections or context-incompatible toolbox items.
- Studio toolbox dispatch-sourced creation execution notes — Studio now executes admitted toolbox palette dispatch single-item and ordered batch creates through `create_visual_object_from_toolbox_dispatch` and `create_visual_objects_from_toolbox_dispatch`, preserving dispatch context, selected-parent defaults, generated or explicit identity metadata, lower-level create-result metadata, and no-mutation failure behavior for non-admitted, unavailable, stale, duplicate, or lower-layer invalid requests.
- Studio host dispatch-sourced creation execution JSON notes — `copperfin_studio_host` now exposes `--toolbox-create-from-dispatch` and `--toolbox-create-batch-from-dispatch`, routing admitted toolbox palette dispatches into single-object or ordered batch toolbox creation while emitting dispatch-sourced create-plan or batch-plan metadata, lower-level create-result metadata, created-object metadata, dry-run state, mutation state, parser errors, and clean no-stale-metadata failure output.
- Studio host toolbox-create JSON notes — `copperfin_studio_host` now exposes toolbox descriptor creation through `--toolbox-create`, `--selection-toolbox-create`, `--toolbox-create-batch`, and `--selection-toolbox-create-batch` plus optional identity, parent, and repeatable field-value arguments, returning deterministic JSON result metadata for external designer shells.
- Studio toolbox creation context-filter notes — toolbox descriptor creation now accepts an optional toolbox context and rejects context-incompatible controls, such as form-only TextBox creation in report contexts, without mutating the asset; the Studio host exposes this through `--toolbox-context`.
- Studio toolbox creation planning notes — Studio now exposes non-mutating single and batch toolbox creation planners that validate descriptor metadata, toolbox context compatibility, generated or explicit object names, identity collisions, parent metadata, and caller field values before any asset write.
- Studio toolbox create-from-dispatch planning notes — Studio now composes admitted toolbox palette dispatches with non-mutating single and ordered batch creation planners, preserving dispatch context, selected-parent defaults, generated-name reservation, per-item overrides, and no-mutation failure behavior before any asset write.
- Studio host toolbox creation planning JSON notes — `copperfin_studio_host` now exposes `--toolbox-create-plan`, `--selection-toolbox-create-plan`, `--toolbox-create-batch-plan`, `--selection-toolbox-create-batch-plan`, `--toolbox-create-from-dispatch-plan`, `--toolbox-create-batch-from-dispatch-plan`, `--toolbox-create-plan-catalog`, `--selection-toolbox-create-plan-catalog`, `--toolbox-create-batch-plan-catalog`, and `--selection-toolbox-create-batch-plan-catalog` so external designer shells can preview single-item, selected-context single-item, ordered batch, selected-context ordered batch, dispatch-sourced single-item, dispatch-sourced batch, per-context single-item catalog, selected-context single-item catalog, per-context batch catalog, and selected-context batch catalog creates without mutating visual assets; execution host JSON now includes `--toolbox-create-from-dispatch` and `--toolbox-create-batch-from-dispatch` for admitted dispatch-sourced creation.
- Studio toolbox creation dispatch planning notes — Studio now exposes non-executing dispatch planners for single, batch, dispatch-sourced single-item, dispatch-sourced ordered batch, single-item catalog, selected-context single-item catalog, context-wide batch catalog, and selected-context batch catalog toolbox creates, with explicit create-operation admission required before dispatch arguments claim mutation intent.
- Studio host toolbox creation dispatch JSON notes — `copperfin_studio_host` now exposes `--toolbox-create-dispatch-plan`, `--selection-toolbox-create-dispatch-plan`, `--toolbox-create-dispatch-from-dispatch-plan`, `--toolbox-create-batch-dispatch-plan`, `--toolbox-create-batch-dispatch-from-dispatch-plan`, `--toolbox-create-dispatch-catalog`, `--selection-toolbox-create-dispatch-catalog`, `--toolbox-create-batch-dispatch-catalog`, and `--selection-toolbox-create-batch-dispatch-catalog`, emitting deterministic create-command argument contracts while keeping `executed` false and leaving visual assets unchanged.
- Studio host stable-selector property edit notes — `copperfin_studio_host --set-property` now accepts `--object-name` and `--unique-id` selectors, so external designer hosts can edit selected controls by stable VFP object identity instead of depending only on DBF record indexes.
- Studio host clear-property command notes — `copperfin_studio_host --clear-property` now exposes rollback-safe visual property clearing by record index, object name, or unique id, with ambiguous set/clear requests rejected before mutation.
- Studio host rename-property command notes — `copperfin_studio_host --rename-property` now exposes rollback-safe memo-backed visual property renaming by record index, object name, or unique id, with `--new-property-name` validation and ambiguous property command rejection before mutation.
- Studio host delete-object command notes — `copperfin_studio_host --delete-object` now exposes rollback-safe selected-object deletion by record index, object name, or unique id, with delete/property command ambiguity rejected before mutation.
- Studio host restore-object command notes — `copperfin_studio_host --restore-object` now exposes rollback-safe selected-object restoration by record index, object name, or unique id, with delete/restore/property command ambiguity rejected before mutation.
- Studio host duplicate-object command notes — `copperfin_studio_host --duplicate-object` now exposes rollback-safe selected-object duplication by record index, object name, or unique id, accepts replacement object/name/unique-id fields, and rejects duplicate/delete/restore/property command ambiguity before mutation.
- Studio host rename-object command notes — `copperfin_studio_host --rename-object` now exposes rollback-safe selected-object identity renaming by record index, object name, or unique id, accepts replacement object/name/unique-id fields, and rejects empty identity plus rename-object/delete/restore/duplicate/property command ambiguity before mutation.
- Studio host reparent-object command notes — `copperfin_studio_host --reparent-object` now exposes rollback-safe selected-object reparenting by record index, object name, or unique id, accepts parent object-name/unique-id selectors plus `--clear-parent`, and rejects missing parent selectors plus reparent-object/delete/restore/duplicate/rename-object/property command ambiguity before mutation.
- Studio host reorder-object command notes — `copperfin_studio_host --reorder-object` now exposes rollback-safe selected-object reordering by record index, object name, or unique id, accepts front/back/before/after placement with relative target object-name/unique-id selectors, and rejects missing placement plus reorder-object/delete/restore/duplicate/rename-object/reparent-object/property command ambiguity before mutation.
- Studio host group-object command notes — `copperfin_studio_host --group-object` now exposes rollback-safe selected-child grouping through repeated `--group-child-object-name` / `--group-child-unique-id` selectors and repeated `--field-value name=value` group-container assignments, and rejects group-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/ungroup-object/property command ambiguity before mutation.
- Studio host align-object command notes — `copperfin_studio_host --align-object` now exposes rollback-safe selected-object alignment through stable `--anchor-object-name` / `--anchor-unique-id` selectors, repeated `--align-target-object-name` / `--align-target-unique-id` selectors, and `--alignment-mode left|right|top|bottom|horizontal-center|vertical-center`, and rejects align-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/ungroup-object/property command ambiguity before mutation.
- Studio host resize-object command notes — `copperfin_studio_host --resize-object` now exposes rollback-safe selected-object resize-to-anchor behavior through stable `--anchor-object-name` / `--anchor-unique-id` selectors, repeated `--resize-target-object-name` / `--resize-target-unique-id` selectors, and `--resize-mode width|height|size`, and rejects resize-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/ungroup-object/property command ambiguity before mutation.
- Studio host distribute-object command notes — `copperfin_studio_host --distribute-object` now exposes rollback-safe selected-object distribution through repeated `--distribute-target-object-name` / `--distribute-target-unique-id` selectors and `--distribution-mode horizontal|vertical`, and rejects distribute-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/ungroup-object/property command ambiguity before mutation.
- Studio host snap-object command notes — `copperfin_studio_host --snap-object` now exposes rollback-safe selected-object snap-to-grid behavior through repeated `--snap-target-object-name` / `--snap-target-unique-id` selectors, `--snap-mode horizontal|vertical|both`, `--grid-width`, and `--grid-height`, and rejects snap-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/ungroup-object/property command ambiguity before mutation.
- Studio host nudge-object command notes — `copperfin_studio_host --nudge-object` now exposes rollback-safe selected-object nudging through repeated `--nudge-target-object-name` / `--nudge-target-unique-id` selectors, `--nudge-mode horizontal|vertical|both`, `--delta-hpos`, and `--delta-vpos`, and rejects nudge-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/ungroup-object/property command ambiguity before mutation.
- Studio host tab-order-object command notes — `copperfin_studio_host --tab-order-object` now exposes rollback-safe selected-object TABINDEX sequencing through repeated `--tab-order-target-object-name` / `--tab-order-target-unique-id` selectors and optional `--starting-tab-index`, and rejects tab-order-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/ungroup-object/property command ambiguity before mutation.
- Studio host tab-stop-object command notes — `copperfin_studio_host --tab-stop-object` now exposes rollback-safe selected-object TABSTOP assignment through repeated `--tab-stop-target-object-name` / `--tab-stop-target-unique-id` selectors and `--tab-stop true|false`, and rejects tab-stop-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/ungroup-object/property command ambiguity before mutation.
- Studio host visibility-object command notes — `copperfin_studio_host --visibility-object` now exposes rollback-safe selected-object VISIBLE assignment through repeated `--visibility-target-object-name` / `--visibility-target-unique-id` selectors and `--visible true|false`, and rejects visibility-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/ungroup-object/property command ambiguity before mutation.
- Studio host enabled-object command notes — `copperfin_studio_host --enabled-object` now exposes rollback-safe selected-object ENABLED assignment through repeated `--enabled-target-object-name` / `--enabled-target-unique-id` selectors and `--enabled true|false`, and rejects enabled-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/ungroup-object/property command ambiguity before mutation.
- Studio host read-only-object command notes — `copperfin_studio_host --read-only-object` now exposes rollback-safe selected-object READONLY assignment through repeated `--read-only-target-object-name` / `--read-only-target-unique-id` selectors and `--object-read-only true|false`, and rejects read-only-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/ungroup-object/property command ambiguity before mutation.
- Studio host locked-object command notes — `copperfin_studio_host --locked-object` now exposes rollback-safe selected-object LOCKED assignment through repeated `--locked-target-object-name` / `--locked-target-unique-id` selectors and `--locked true|false`, and rejects locked-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/ungroup-object/property command ambiguity before mutation.
- Studio host caption-object command notes — `copperfin_studio_host --caption-object` now exposes rollback-safe selected-object CAPTION assignment through repeated `--caption-target-object-name` / `--caption-target-unique-id` selectors and `--caption <value>`, including space-containing argv values, and rejects caption-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/ungroup-object/property command ambiguity before mutation.
- Studio host tooltip-text-object command notes — `copperfin_studio_host --tooltip-text-object` now exposes rollback-safe selected-object TOOLTIPTEXT assignment through repeated `--tooltip-text-target-object-name` / `--tooltip-text-target-unique-id` selectors and `--tooltip-text <value>`, including space-containing argv values, and rejects tooltip-text-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/ungroup-object/property command ambiguity before mutation.
- Studio host status-bar-text-object command notes — `copperfin_studio_host --status-bar-text-object` now exposes rollback-safe selected-object STATUSBARTEXT assignment through repeated `--status-bar-text-target-object-name` / `--status-bar-text-target-unique-id` selectors and `--status-bar-text <value>`, including space-containing argv values, and rejects status-bar-text-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/ungroup-object/property command ambiguity before mutation.
- Studio host control-source-object command notes — `copperfin_studio_host --control-source-object` now exposes rollback-safe selected-object CONTROLSOURCE assignment through repeated `--control-source-target-object-name` / `--control-source-target-unique-id` selectors and `--control-source <value>`, including space-containing argv values, and rejects control-source-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/ungroup-object/property command ambiguity before mutation.
- Studio host current-control-object command notes — `copperfin_studio_host --current-control-object` now exposes rollback-safe selected-object CURRENTCONTROL assignment through repeated `--current-control-target-object-name` / `--current-control-target-unique-id` selectors and `--current-control <value>`, including space-containing argv values, and rejects current-control-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/ungroup-object/property command ambiguity before mutation.
- Studio host closable-object command notes — `copperfin_studio_host --closable-object` now exposes rollback-safe selected-object CLOSABLE assignment through repeated `--closable-target-object-name` / `--closable-target-unique-id` selectors and `--closable true|false`, and rejects closable-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/ungroup-object/property command ambiguity before mutation.
- Studio host control-box-object command notes — `copperfin_studio_host --control-box-object` now exposes rollback-safe selected-object CONTROLBOX assignment through repeated `--control-box-target-object-name` / `--control-box-target-unique-id` selectors and `--control-box true|false`, and rejects control-box-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/closable-object/ungroup-object/property command ambiguity before mutation.
- Studio host allow-output-object command notes — `copperfin_studio_host --allow-output-object` now exposes rollback-safe selected-object ALLOWOUTPUT assignment through repeated `--allow-output-target-object-name` / `--allow-output-target-unique-id` selectors and `--allow-output true|false`, and rejects allow-output-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/current-control-object/closable-object/control-box-object/ungroup-object/property command ambiguity before mutation.
- Studio host input-mask-object command notes — `copperfin_studio_host --input-mask-object` now exposes rollback-safe selected-object INPUTMASK assignment through repeated `--input-mask-target-object-name` / `--input-mask-target-unique-id` selectors and `--input-mask <value>`, including space-containing argv values, and rejects input-mask-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/ungroup-object/property command ambiguity before mutation.
- Studio host format-object command notes — `copperfin_studio_host --format-object` now exposes rollback-safe selected-object FORMAT assignment through repeated `--format-target-object-name` / `--format-target-unique-id` selectors and `--format <value>`, including space-containing argv values, and rejects format-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/ungroup-object/property command ambiguity before mutation.
- Studio host row-source-object command notes — `copperfin_studio_host --row-source-object` now exposes rollback-safe selected-object ROWSOURCE assignment through repeated `--row-source-target-object-name` / `--row-source-target-unique-id` selectors and `--row-source <value>`, including space-containing argv values, and rejects row-source-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/ungroup-object/property command ambiguity before mutation.
- Studio host row-source-type-object command notes — `copperfin_studio_host --row-source-type-object` now exposes rollback-safe selected-object ROWSOURCETYPE assignment through repeated `--row-source-type-target-object-name` / `--row-source-type-target-unique-id` selectors and `--row-source-type <n>`, rejects negative values, and rejects row-source-type-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/ungroup-object/property command ambiguity before mutation.
- Studio host bound-column-object command notes — `copperfin_studio_host --bound-column-object` now exposes rollback-safe selected-object BOUNDCOLUMN assignment through repeated `--bound-column-target-object-name` / `--bound-column-target-unique-id` selectors and `--bound-column <n>`, rejects negative values, and rejects bound-column-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/ungroup-object/property command ambiguity before mutation.
- Studio host column-count-object command notes — `copperfin_studio_host --column-count-object` now exposes rollback-safe selected-object COLUMNCOUNT assignment through repeated `--column-count-target-object-name` / `--column-count-target-unique-id` selectors and `--column-count <n>`, rejects negative values, and rejects column-count-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/ungroup-object/property command ambiguity before mutation.
- Studio host style-object command notes — `copperfin_studio_host --style-object` now exposes rollback-safe selected-object STYLE assignment through repeated `--style-target-object-name` / `--style-target-unique-id` selectors and `--style <n>`, rejects negative values, and rejects style-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/ungroup-object/property command ambiguity before mutation.
- Studio host list-index-object command notes — `copperfin_studio_host --list-index-object` now exposes rollback-safe selected-object LISTINDEX assignment through repeated `--list-index-target-object-name` / `--list-index-target-unique-id` selectors and `--list-index <n>`, rejects negative values, and rejects list-index-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/ungroup-object/property command ambiguity before mutation.
- Studio host left-column-object command notes — `copperfin_studio_host --left-column-object` now exposes rollback-safe selected-object LEFTCOLUMN assignment through repeated `--left-column-target-object-name` / `--left-column-target-unique-id` selectors and `--left-column <n>`, rejects negative values, and rejects left-column-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/ungroup-object/property command ambiguity before mutation.
- Studio host display-value-object command notes — `copperfin_studio_host --display-value-object` now exposes rollback-safe selected-object DISPLAYVALUE assignment through repeated `--display-value-target-object-name` / `--display-value-target-unique-id` selectors and `--display-value <value>`, including quoted text passed as one argv value, and rejects display-value-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/ungroup-object/property command ambiguity before mutation.
- Studio host selected-back-color-object command notes — `copperfin_studio_host --selected-back-color-object` now exposes rollback-safe selected-object SELECTEDBACKCOLOR assignment through repeated `--selected-back-color-target-object-name` / `--selected-back-color-target-unique-id` selectors and `--selected-back-color <n>`, rejects negative values, and rejects selected-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/ungroup-object/property command ambiguity before mutation.
- Studio host selected-fore-color-object command notes — `copperfin_studio_host --selected-fore-color-object` now exposes rollback-safe selected-object SELECTEDFORECOLOR assignment through repeated `--selected-fore-color-target-object-name` / `--selected-fore-color-target-unique-id` selectors and `--selected-fore-color <n>`, rejects negative values, and rejects selected-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host selected-item-back-color-object command notes — `copperfin_studio_host --selected-item-back-color-object` now exposes rollback-safe selected-object SELECTEDITEMBACKCOLOR assignment through repeated `--selected-item-back-color-target-object-name` / `--selected-item-back-color-target-unique-id` selectors and `--selected-item-back-color <n>`, rejects negative values, and rejects selected-item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host selected-item-fore-color-object command notes — `copperfin_studio_host --selected-item-fore-color-object` now exposes rollback-safe selected-object SELECTEDITEMFORECOLOR assignment through repeated `--selected-item-fore-color-target-object-name` / `--selected-item-fore-color-target-unique-id` selectors and `--selected-item-fore-color <n>`, rejects negative values, and rejects selected-item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host disabled-item-back-color-object command notes — `copperfin_studio_host --disabled-item-back-color-object` now exposes rollback-safe selected-object DISABLEDITEMBACKCOLOR assignment through repeated `--disabled-item-back-color-target-object-name` / `--disabled-item-back-color-target-unique-id` selectors and `--disabled-item-back-color <n>`, rejects negative values, and rejects disabled-item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host disabled-item-fore-color-object command notes — `copperfin_studio_host --disabled-item-fore-color-object` now exposes rollback-safe selected-object DISABLEDITEMFORECOLOR assignment through repeated `--disabled-item-fore-color-target-object-name` / `--disabled-item-fore-color-target-unique-id` selectors and `--disabled-item-fore-color <n>`, rejects negative values, and rejects disabled-item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host item-back-color-object command notes — `copperfin_studio_host --item-back-color-object` now exposes rollback-safe selected-object ITEMBACKCOLOR assignment through repeated `--item-back-color-target-object-name` / `--item-back-color-target-unique-id` selectors and `--item-back-color <n>`, rejects negative values, and rejects item-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host item-fore-color-object command notes — `copperfin_studio_host --item-fore-color-object` now exposes rollback-safe selected-object ITEMFORECOLOR assignment through repeated `--item-fore-color-target-object-name` / `--item-fore-color-target-unique-id` selectors and `--item-fore-color <n>`, rejects negative values, and rejects item-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host highlight-back-color-object command notes — `copperfin_studio_host --highlight-back-color-object` now exposes rollback-safe selected-object HIGHLIGHTBACKCOLOR assignment through repeated `--highlight-back-color-target-object-name` / `--highlight-back-color-target-unique-id` selectors and `--highlight-back-color <n>`, rejects negative values, and rejects highlight-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host highlight-fore-color-object command notes — `copperfin_studio_host --highlight-fore-color-object` now exposes rollback-safe selected-object HIGHLIGHTFORECOLOR assignment through repeated `--highlight-fore-color-target-object-name` / `--highlight-fore-color-target-unique-id` selectors and `--highlight-fore-color <n>`, rejects negative values, and rejects highlight-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host back-color-object command notes — `copperfin_studio_host --back-color-object` now exposes rollback-safe selected-object BACKCOLOR assignment through repeated `--back-color-target-object-name` / `--back-color-target-unique-id` selectors and `--back-color <n>`, rejects negative values, and rejects back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host fore-color-object command notes — `copperfin_studio_host --fore-color-object` now exposes rollback-safe selected-object FORECOLOR assignment through repeated `--fore-color-target-object-name` / `--fore-color-target-unique-id` selectors and `--fore-color <n>`, rejects negative values, and rejects fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host disabled-back-color-object command notes — `copperfin_studio_host --disabled-back-color-object` now exposes rollback-safe selected-object DISABLEDBACKCOLOR assignment through repeated `--disabled-back-color-target-object-name` / `--disabled-back-color-target-unique-id` selectors and `--disabled-back-color <n>`, rejects negative values, and rejects disabled-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host disabled-fore-color-object command notes — `copperfin_studio_host --disabled-fore-color-object` now exposes rollback-safe selected-object DISABLEDFORECOLOR assignment through repeated `--disabled-fore-color-target-object-name` / `--disabled-fore-color-target-unique-id` selectors and `--disabled-fore-color <n>`, rejects negative values, and rejects disabled-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host dynamic-back-color-object command notes — `copperfin_studio_host --dynamic-back-color-object` now exposes rollback-safe selected-object DYNAMICBACKCOLOR assignment through repeated `--dynamic-back-color-target-object-name` / `--dynamic-back-color-target-unique-id` selectors and `--dynamic-back-color <expr>`, preserves raw expression text without integer parsing or quoted-string serialization, and rejects dynamic-back-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host dynamic-fore-color-object command notes — `copperfin_studio_host --dynamic-fore-color-object` now exposes rollback-safe selected-object DYNAMICFORECOLOR assignment through repeated `--dynamic-fore-color-target-object-name` / `--dynamic-fore-color-target-unique-id` selectors and `--dynamic-fore-color <expr>`, preserves raw expression text without integer parsing or quoted-string serialization, and rejects dynamic-fore-color-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/group-object/align-object/resize-object/distribute-object/snap-object/nudge-object/tab-order-object/tab-stop-object/visibility-object/enabled-object/read-only-object/locked-object/caption-object/tooltip-text-object/status-bar-text-object/control-source-object/input-mask-object/format-object/row-source-object/row-source-type-object/bound-column-object/column-count-object/style-object/list-index-object/left-column-object/display-value-object/selected-back-color-object/selected-fore-color-object/selected-item-back-color-object/selected-item-fore-color-object/disabled-item-back-color-object/disabled-item-fore-color-object/item-back-color-object/item-fore-color-object/highlight-back-color-object/highlight-fore-color-object/back-color-object/fore-color-object/disabled-back-color-object/disabled-fore-color-object/dynamic-back-color-object/ungroup-object/property command ambiguity before mutation.
- Studio host ungroup-object command notes — `copperfin_studio_host --ungroup-object` now exposes rollback-safe selected-container ungrouping by record index, object name, or unique id, and rejects ungroup-object/delete/restore/duplicate/rename-object/reparent-object/reorder-object/property command ambiguity before mutation.
- Visual object undo affected-count notes — visual property undo now surfaces an affected-object count through generic edit results, with missing-history failures reporting zero affected objects.
- Visual object base property-edit affected-count notes — base single and multi-property visual object edit APIs now surface affected-object counts through generic edit results, with failed requests reporting zero affected objects.
- Visual object structural-command affected-count notes — visual object reparent, rename, reorder, deleted-state, and subtree deleted-state commands now surface affected-object counts through generic edit results for designer tree operations, with failed requests reporting zero affected objects.
- Visual object property-command affected-count notes — visual property clear, copy, move, rename, and reorder commands now surface affected-object counts through generic edit results for single and batch property-grid operations, with failed requests reporting zero affected objects.
- Visual object property-grid search notes — `filter_visual_object_properties` now provides non-mutating selected-object property-grid filtering over existing property-list results, with case-insensitive matching across direct and memo-backed property names, values, backing metadata, and source-line metadata while preserving selector resolution and no-undo behavior.
- Visual object method-command affected-count notes — visual method update, delete, rename, copy, move, and reorder commands now surface affected-object counts through generic edit results for single and batch code-pane operations, with failed requests reporting zero affected objects.
- Visual object helper-backed property affected-count notes — shared logical and non-negative numeric designer property command coverage now proves generic edit-result affected-object counts, with failed empty-selection requests reporting zero affected objects.
- Visual object text-property affected-count notes — additional text designer property commands now surface affected-object counts through generic edit results, with failed text property requests reporting zero affected objects.
- Visual object scalar-property affected-count notes — representative logical and text scalar designer property commands now surface affected-object counts through generic edit results, with failed scalar property requests reporting zero affected objects.
- Visual object tab-command affected-count notes — tab-order and tab-stop designer commands now surface affected-object counts through generic edit results, with failed tab command requests reporting zero affected objects.
- Visual object geometry-command affected-count notes — alignment, resize, distribution, snap-to-grid, and nudge operations now surface affected-object counts through generic edit results, with failed geometry commands reporting zero affected objects.
- Visual object batch-edit affected-count notes — rollback-safe batch visual object edits now report the number of edited objects through generic edit results, while failed or empty batch edit requests report zero affected objects.
- Visual object ungroup-result container identity notes — ungrouping now reports the selected container's resolved object name, unique id, and parent name alongside target-parent and child-count metadata, while failed ungroup requests return empty container identity metadata.
- Visual object group-result container identity notes — grouping now reports the created container's resolved object name, unique id, and parent name while preserving grouped-child-count reporting, and failed grouping requests return empty container identity metadata.
- Visual object subtree-duplicate-result root identity notes — selected-object subtree duplication now reports the appended copied root's resolved object name, unique id, and parent name while preserving copied-count reporting, and failed subtree-duplicate requests return empty root identity metadata.
- Visual object batch-duplicate-result identity notes — batch selected-object duplication now reports each appended duplicate's record index, resolved object name, unique id, and parent name in append order, while failed batch-duplicate requests return empty identity metadata.
- Visual object duplicate-result identity notes — selected-object duplication now reports the appended duplicate's resolved object name, unique id, and parent name directly from the duplicated visual asset record, while failed duplicate requests return empty identity metadata.
- Visual object batch-create-result identity notes — batch toolbox create operations now report each appended object's record index, resolved object name, unique id, and parent name in append order, while failed batch-create requests return empty identity metadata.
- Visual object create-result identity notes — toolbox create operations now report the appended object's resolved object name, unique id, and parent name directly from the created visual asset record, while failed create requests return empty identity metadata.
- Studio selected-record DataEnvironment inference notes — form/class launches with `--record` targeting parsed DataEnvironment object metadata now infer the data-environment designer context when no explicit override is present, while non-DataEnvironment selected records keep visual-object defaults.
- Studio selected-object JSON notes — Studio host JSON now emits a compact `selectedObject` summary for explicit record selections that match parsed designer objects, including direct selected-object property snapshots with stable `name`, `type`, `isNull`, and `value` fields, giving external hosts direct access to selected record/object identity and property-grid data without scanning the full object array.
- Studio selected-object raw-code fallback notes — Studio host JSON now has regression coverage proving malformed, oversized, and dot-leading visual object `OBJTYPE`/`OBJCODE` source values keep document inspection non-failing, default parsed `objectTypeCode`/`objectCode` to zero, and remain available as raw selected-object and object-list property metadata for designer diagnostics.
- Studio object-list JSON notes — Studio host JSON now emits parsed designer-object identity fields directly on each `document.objects[]` entry, so object browsers can consume `objectName`, `uniqueId`, `parentName`, `className`, and `baseclassName` without reparsing the property list.
- Studio object hierarchy JSON notes — Studio object snapshots now derive direct child counts from parsed object-name/parent-name relationships, and Studio host JSON emits `childCount` for selected-object summaries and full object-list entries so designer tree panes can show hierarchy affordances without rescanning all objects.
- Studio object parent-link JSON notes — Studio object snapshots now resolve nullable parent record links from parsed object-name/parent-name relationships, and Studio host JSON emits `parentRecordIndex` for selected-object summaries and full object-list entries so designer tree panes can attach child nodes without resolving parent names themselves.
- Studio object-path JSON notes — Studio object snapshots now derive dot-separated object paths from parsed object names and resolved parent record links, and Studio host JSON emits `objectPath` for selected-object summaries and full object-list entries so designer tree/search panes can identify nested objects without rebuilding parent chains.
- Studio object-code metadata JSON notes — Studio host JSON now emits raw parsed object code metadata (`objectTypeCode`, `objectCode`, and `platform`) for selected-object summaries and full object-list entries so designer panes can distinguish object families without scanning DBF property rows.
- Studio selected-object deletion-state JSON notes — Studio host JSON now emits the parsed `deleted` boolean directly on non-null selected-object summaries, matching the full object-list contract so hosts can render selected deleted records without consulting `document.objects[]`.
- Studio property provenance JSON notes — Studio host JSON now emits property provenance (`fieldIndex`, `memoBlockNumber`, `derivedFromPropertyBlob`, and nullable `sourceLineIndex`) for selected-object and full object-list properties so property grids can locate the backing DBF field or serialized property source without reparsing asset records.
- Studio object property-count JSON notes — Studio host JSON now emits direct `propertyCount` values for selected-object summaries and full object-list entries so designer/property-grid panes can size or lazy-load rows without scanning the emitted property arrays first.
- Studio document object-count JSON notes — Studio host JSON now emits document-level `objectCount` and `deletedObjectCount` values derived from the emitted object snapshot vector so designer panes can size object browsers without scanning `document.objects[]` first.
- Studio document root-object-count JSON notes — Studio host JSON now emits document-level `rootObjectCount` from parsed object snapshots with no resolved parent record link so designer tree panes can size top-level nodes without scanning `document.objects[]` first.
- Studio selected-object availability JSON notes — Studio host JSON now emits document-level `selectedObjectAvailable`, set only when an explicit selected record resolves to a parsed object snapshot, so hosts can distinguish unmatched selected records from real selected objects without relying only on `selectedObject: null`.
- Studio object child-record-link JSON notes — Studio object snapshots now carry direct child record-index arrays, and Studio host JSON emits `childRecordIndexes` for selected-object summaries and full object-list entries so designer tree panes can expand nodes without scanning all objects for matching parent links.
- Studio document root-record-link JSON notes — Studio host JSON now emits document-level `rootRecordIndexes` from parsed object snapshots with no resolved parent record link so designer tree panes can seed top-level nodes without scanning `document.objects[]` first.
- Studio document leaf-record-link JSON notes — Studio host JSON now emits document-level `leafObjectCount` and `leafRecordIndexes` from parsed object snapshots with no child record links so designer tree panes can identify terminal nodes without scanning `document.objects[]` first.
- Studio object tree-depth JSON notes — Studio object snapshots now derive hierarchy depth from resolved parent record links, and Studio host JSON emits per-object `objectDepth` plus document-level `maxObjectDepth` so designer tree panes can indent and virtualize hierarchy rows without rebuilding parent chains.
- Studio object sibling-order JSON notes — Studio object snapshots now derive sibling index/count metadata from resolved parent record links, and Studio host JSON emits per-object `siblingIndex` and `siblingCount` so designer tree panes can navigate stable object ordering without regrouping client-side.
- Studio object ancestor-link JSON notes — Studio object snapshots now derive root-to-parent ancestor record links from resolved parent chains, and Studio host JSON emits per-object `ancestorRecordIndexes` so designer tree panes and context-aware editors can expand parent chains without reparsing paths or walking links client-side.
- Visual editor outline hierarchy notes — `VisualObjectSnapshot` now exposes resolved parent-record availability/index plus direct child counts across object, child, descendant, and ancestor listing APIs so designer object browsers can render hierarchy affordances without issuing extra per-row hierarchy calls.
- Visual editor outline path/order notes — `VisualObjectSnapshot` now exposes object path, hierarchy depth, ancestor record links, sibling index, and sibling count across object, child, descendant, and ancestor listing APIs so designer object browsers can render stable tree rows from one listing call.
- Visual editor outline count notes — `VisualObjectSnapshot` now exposes property and method counts, derived through the existing property and method parsers, so property grids and method browsers can size affordances from outline rows before loading per-object detail panes.
- Visual object grouping result notes — `VisualObjectGroupResult` now reports the number of selected objects reparented into the new container, while failed grouping paths continue to return zero and preserve rollback behavior.
- Visual object ungrouping result notes — `VisualObjectUngroupResult` now reports the target parent name and resolved parent-record metadata after successful ungroup operations, while root-level and failed ungroup paths report empty/unavailable parent metadata.
- Native-wrapper bridge-request descriptor metadata notes — generated DLL/FLL native-wrapper request JSON now serializes routine kind, source provenance, parameter declaration style, parameter-name list, and schema version beside the existing export name, arity, request media type, and parameter value payloads, so runtime-host bridge requests carry the same descriptor metadata already present in wrapper argv/env scaffolding
- Native-wrapper exported-argument consumption notes — generated DLL/FLL native-wrapper stubs now consume DLL integer parameters and FLL `ParamBlk* parm` through `copperfin_build_runtime_bridge_call(...)` parameter payloads without emitting false `(void)` unused markers for those exported arguments
- Native-wrapper request-write execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_write_request(request_write_plan)` immediately after constructing the request-write plan and pass the execution result into response-read planning, so request documents are materialized through the shared helper while failed request writes force deterministic empty-response fallback behavior
- Native-wrapper request-write execution consumption notes — generated DLL/FLL native-wrapper stubs now consume `request_write_execution` through `copperfin_build_runtime_bridge_response_read_plan(...)`; the response-read plan now carries `request_write_succeeded` and returns the shared empty response document when request writing fails
- Native-wrapper response-read execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_read_response(response_read_plan)` immediately after constructing the response-read plan and pass the resulting response document into `CopperfinRuntimeBridgeResponseArtifact` instead of seeding response artifacts from the empty-response fallback
- Native-wrapper dispatch-execution consumption notes — generated DLL/FLL native-wrapper stubs now consume `dispatch_execution` through `copperfin_runtime_bridge_launch_process(...)` without emitting a false `(void)dispatch_execution` unused marker
- Native-wrapper process-launch consumption notes — generated DLL/FLL native-wrapper stubs now consume `process_launch` through `copperfin_runtime_bridge_evaluate_host_failure(...)` without emitting a false `(void)process_launch` unused marker
- Native-wrapper host-failure evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `host_failure` through `copperfin_runtime_bridge_evaluate_missing_response(...)` without emitting a false `(void)host_failure` unused marker
- Native-wrapper missing-response evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `missing_response` through `copperfin_runtime_bridge_evaluate_response_validation(...)` without emitting a false `(void)missing_response` unused marker
- Native-wrapper response-validation evaluation-consumption notes — generated DLL/FLL native-wrapper stubs now consume `response_validation_evaluation` through `copperfin_runtime_bridge_admit_response_parse(...)` without emitting a false `(void)response_validation_evaluation` unused marker
- Native-wrapper response-parse execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_parse_response(response_parse_plan)` after response-parse admission and pass the parsed response into `CopperfinRuntimeBridgeInterpretedResultPlan` so parsed status, return-value, and diagnostics values flow forward through interpretation instead of parse execution staying hidden inside the interpreted-result helper
- Native-wrapper response-parse admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `response_parse_admission` through `copperfin_runtime_bridge_admit_interpreted_result(...)` without emitting a false `(void)response_parse_admission` unused marker
- Native-wrapper interpreted-result execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_interpreted_result(interpreted_result_plan)` after interpreted-result admission and pass the interpreted success/fallback result into `CopperfinRuntimeBridgeNativeReturnPlan` so native-return planning consumes explicit interpretation output instead of invoking interpretation internally
- Native-wrapper interpreted-result admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `interpreted_result_admission` through `copperfin_runtime_bridge_admit_native_return(...)` without emitting a false `(void)interpreted_result_admission` unused marker
- Native-wrapper native-return execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_native_return(native_return_plan)` after native-return admission and pass the selected native return metadata into `CopperfinRuntimeBridgeOutcomeSelectionPlan` so outcome selection consumes explicit native-return output instead of invoking native-return execution internally
- Native-wrapper native-return admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `native_return_admission` through `copperfin_runtime_bridge_admit_outcome_selection(...)` without emitting a false `(void)native_return_admission` unused marker
- Native-wrapper outcome-selection execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_outcome_selection(outcome_selection_plan)` after outcome-selection admission and pass the selected condition/diagnostics/native return metadata into `CopperfinRuntimeBridgeReturnMaterializationPlan` so return materialization consumes explicit outcome-selection output instead of invoking outcome-selection execution internally
- Native-wrapper outcome-selection admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `outcome_selection_admission` through `copperfin_runtime_bridge_admit_return_materialization(...)` without emitting a false `(void)outcome_selection_admission` unused marker
- Native-wrapper return-materialization execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_materialization(return_materialization_plan)` after return-materialization admission and pass the materialized return branch metadata into `CopperfinRuntimeBridgeReturnEmissionPlan` so return emission consumes explicit materialized-return output instead of invoking return-materialization execution internally
- Native-wrapper return-materialization admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_materialization_admission` through `copperfin_runtime_bridge_admit_return_emission(...)` without emitting a false `(void)return_materialization_admission` unused marker
- Native-wrapper return-emission execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_emission(return_emission_plan)` after return-emission admission and pass the emitted return block metadata into `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` so final-return adoption consumes explicit return-emission output instead of invoking return-emission execution internally
- Native-wrapper return-emission admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_emission_admission` through `copperfin_runtime_bridge_admit_final_return_adoption(...)` without emitting a false `(void)return_emission_admission` unused marker
- Native-wrapper final-return-adoption execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_final_return_adoption(final_return_adoption_plan)` after final-return-adoption admission and pass the adopted return metadata into `CopperfinRuntimeBridgeReturnActivationPlan` so return activation consumes explicit final-return-adoption output instead of invoking final-return-adoption execution internally
- Native-wrapper final-return-adoption admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `final_return_adoption_admission` through `copperfin_runtime_bridge_admit_return_activation(...)` without emitting a false `(void)final_return_adoption_admission` unused marker
- Native-wrapper return-activation execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_activation(return_activation_plan)` after return-activation admission and pass the activated return metadata into `CopperfinRuntimeBridgeStubReturnPlan` so stub-return handling consumes explicit return-activation output instead of invoking return-activation execution internally
- Native-wrapper return-activation admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `return_activation_admission` through `copperfin_runtime_bridge_admit_stub_return(...)` without emitting a false `(void)return_activation_admission` unused marker
- Native-wrapper stub-return execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_return(stub_return_plan)` after stub-return admission and pass the routed stub-return metadata into `CopperfinRuntimeBridgePlaceholderReturnValuePlan` so placeholder-return-value planning consumes explicit stub-return output instead of invoking stub-return execution internally
- Native-wrapper stub-return admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `stub_return_admission` through `copperfin_runtime_bridge_admit_placeholder_return_value(...)` without emitting a false `(void)stub_return_admission` unused marker
- Native-wrapper placeholder-return-value execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_value(placeholder_return_value_plan)` after placeholder-return-value admission and pass the placeholder return metadata into shared stub-emission execution so stub emission consumes explicit placeholder-return-value output instead of invoking placeholder-return-value execution internally
- Native-wrapper placeholder-return-value admission-consumption notes — generated DLL/FLL native-wrapper stubs now consume `placeholder_return_value_admission` through `copperfin_runtime_bridge_admit_placeholder_return_int(...)` without emitting a false `(void)placeholder_return_value_admission` unused marker
- Native-wrapper placeholder-return-int admission-driven execution notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_int_admission)` after placeholder-return-int admission and pass the explicit integer into stub emission, so placeholder-return-int output consumes the admitted int surface instead of re-reading placeholder-return-value metadata
- Native-wrapper stub-emission admission-to-execution notes — generated DLL/FLL native-wrapper stubs now pass the explicit placeholder-return integer into `copperfin_runtime_bridge_admit_stub_emission(placeholder_return_int_admission, placeholder_return_int)` and execute `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` from that admitted surface, so stub-emission execution consumes admission output instead of discarding it
- Native-wrapper stub-emission execution call-site notes — generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` after stub-emission admission and pass the emitted stub metadata into return-surface construction so stub-emission return handling consumes explicit stub-emission output instead of invoking stub-emission execution internally
- Native-wrapper stub-emission return-surface call-site notes — generated DLL/FLL native-wrapper stubs now build `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` after stub-emission execution and pass the explicit return-surface metadata into direct output application so final output handling consumes explicit return-surface output instead of constructing it internally
- Native-wrapper stub-emission output-application call-site notes — generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_apply_stub_emission_output(stub_emission_return_surface, ...)` after return-surface construction with the planned return adapter, so final DLL/FLL return application is visible at the generated wrapper call-site instead of hidden inside a shared stub-return emitter
- Native-wrapper descriptor wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDescriptor`, so exported DLL/FLL stubs now build that wrapper once before constructing the descriptor and then build invocations straight from the enriched descriptor instead of passing the wrapper separately at the invocation seam
- Native-wrapper invocation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInvocation`, so exported DLL/FLL stubs now build that wrapper once before constructing the invocation and then build calls straight from the enriched invocation instead of passing the wrapper separately at the call seam
- Native-wrapper call wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeCall`, so exported DLL/FLL stubs now build that wrapper once before constructing the call and then build results straight from the enriched call instead of passing the wrapper separately at the result seam
- Native-wrapper result wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResult`, so exported DLL/FLL stubs now build that wrapper once before constructing the result and then build launch plans straight from the enriched result instead of passing the wrapper separately at the launch seam
- Native-wrapper launch wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeLaunchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the launch plan and then build observation plans straight from the enriched launch plan instead of passing the wrapper separately at the observation seam
- Native-wrapper observation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeObservationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the observation plan and then build execution plans straight from the enriched observation plan instead of passing the wrapper separately at the execution seam
- Native-wrapper execution wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeExecutionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the execution plan and then build transport plans straight from the enriched execution plan instead of passing the wrapper separately at the transport seam
- Native-wrapper transport wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeTransportPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the transport plan and then build serialization plans straight from the enriched transport plan instead of passing the wrapper separately at the serialization seam
- Native-wrapper serialization wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeSerializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the serialization plan and then build dispatch plans straight from the enriched serialization plan instead of passing the wrapper separately at the dispatch seam
- Native-wrapper dispatch wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDispatchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the dispatch plan and then build payload plans straight from the enriched dispatch plan instead of passing the wrapper separately at the payload seam
- Native-wrapper payload wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePayloadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the payload plan and then build interpretation plans straight from the enriched payload plan instead of passing the wrapper separately at the interpretation seam
- Native-wrapper interpretation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpretation plan and then build failure-policy plans straight from the enriched interpretation plan instead of passing the wrapper separately at the failure-policy seam
- Native-wrapper failure-policy wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFailurePolicyPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the failure-policy plan and then build response-validation plans straight from the enriched failure-policy plan instead of passing the wrapper separately at the response-validation seam
- Native-wrapper response-validation wrapper-plan notes — generated DLL/FLL native-wrapper scaffolds now also carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseValidationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-validation plan and then build request artifacts straight from the enriched response-validation plan instead of passing the wrapper separately at the request-artifact seam
- Runtime-host AI/MCP policy notes — AI-assisted federation planning now requires the native `ai.mcp` role permission through `COPPERFIN_SECURITY_ROLE` (defaulting to `developer`), so optional AI planner requests do not bypass the existing security profile
- FLL registration notes — generated `FoxInfo` metadata now also carries pipe-separated parameter-name metadata (`tcMode`, `tnLeft|tnRight`, and so on) beside routine kind, source provenance, and arity in the honest wrapper scaffold
- Exported routine metadata notes — DLL/FLL callable contracts now also preserve whether each exported routine used `PARAMETERS` or `LPARAMETERS`, and the FLL `FoxInfo` scaffold carries that declaration-style metadata too
- FLL broad-manifest notes — the runtime/debug manifests now mirror the existing FLL per-function callable metadata, and the debug manifest now also records the generated `.def` / `.fll.api` artifact paths, compiler-contract digests, library-output feature flags, extension-payload digests, staged `asset=` inventory lines, `warning=` diagnostics, the `configuration=` contract, the `project_title=` contract, the `project_path=` contract, the `package_root=` contract, the `content_root=` contract, the `ast_manifest_path=` contract, the `ir_manifest_path=` contract, the `transpiled_csharp_path=` contract, the `security_enabled=` / `security_role=` / `security_mode=` / `audit_log_path=` contract, the `runtime_host_sha256=` / `security_roles=` contract, the `.NET` policy summary contract (`dotnet_enabled=`, `dotnet_story=`, allowlist/denylist summaries and items, parity-matrix summaries and items, and gateway decisions), the language/AI/extensibility summary contract (`language_integration_count=`, repeated `language_integration=` lines, `ai_feature_count=`, repeated `ai_feature=` lines, `extensibility_guardrail_count=`, repeated `extensibility_guardrail=` lines, and aggregate `language_integrations=` / `ai_features=` lines), the remaining runtime `feature_flag=` contract lines (launcher, host-compatibility, build-output, debug, and security flags), discovered routine export symbols, plus the requested/materialized primary-output state, so downstream tooling does not need to parse only the dedicated `.fll.api` contract to recover routine-level contract detail, artifact locations, integrity digests, capability flags, packaged-output digests, staged file inventory, warning diagnostics, build configuration, project identity, project location, package location, package content location, compiler-artifact locations, transpilation-artifact location, security metadata, integrity/security summary, extensibility-policy summary, language/AI summary, feature-rollout summary, export surface, or output-state transitions
- `DO`, `DO FORM`, `REPORT FORM`, `LABEL FORM` — command-target coverage now also includes true second-hop macro-expanded routine dispatch such as `DO &cProcDeepHolder`, focused call-binding coverage now also includes true second-hop macro-expanded `WITH` arguments such as `DO testparam WITH &cParamExprDeepHolder`, shared assignment-target coverage now includes true second-hop `&target = value` forms such as `&cTargetDeepHolder = 'MACROASSIGN'`, and focused expression coverage now also includes true second-hop macro-expanded array subscripts and function arguments such as `aData[&cRowExprDeepHolder]` and `LEN(&cFieldExprDeepHolder)`
- `CALL` — first-pass command-path parity now includes `CALL <target> WITH ...` argument binding and external `.prg` target resolution when local routines are not found, including BYREF `@var` write-back through existing call-reference plumbing
- `ACTIVATE POPUP`, `ACTIVATE MENU`, `RELEASE POPUP`, `RELEASE MENU`
- `RETURN`
- `IF/ELSE/ENDIF`
- `DO CASE/CASE/OTHERWISE/ENDCASE`
- `FOR/ENDFOR`
- `DO WHILE/ENDDO`
- `WITH/ENDWITH`
- `TRY/CATCH/FINALLY/ENDTRY`
- `LOOP`, `CONTINUE`, `EXIT`
- `SPAWN`, `AWAIT`, `YIELD`, `ENTER CRITICAL`, `EXIT CRITICAL` — first-pass task/coordination surface with spawned worker completion, cooperative yield, and named in-memory critical sections; the current critical-section policy is now centralized in `docs/25-engine-concurrency-policy.md`, explicitly requires ascending normalized section-name order for nested acquisitions, and rejects blocking wait surfaces while a critical section is held (`AWAIT`, positive-duration `SLEEP`, and lock-retry/backoff under contention, including explicit contested `RLOCK()`), emitting deterministic runtime diagnostics instead of deadlock-prone hangs
- `TEXT/ENDTEXT` with first-pass literal `TEXT TO ... [ADDITIVE] [NOSHOW]` capture, including `TEXT TO ... TEXTMERGE` interpolation coverage for nested `EVAL()` and `&macro` expressions inside merged `<<...>>` segments, including true second-hop command-path forms such as `<<EVAL(&cNameExprDeepHolder)>>` and `<<&cFieldExprDeepHolder>>`, plus custom-delimiter `TEXTMERGE()` recursion over stored expression text, macro-expanded delimiter arguments, and macro-expanded source-template text, including true second-hop source-template forms such as `TEXTMERGE(&cTemplateDeepHolder)` and true second-hop delimiter forms such as `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)`; first-pass `EXECSCRIPT()` return-expression coverage now includes direct `RETURN &macro` expansion and macro-expanded script-source text over current runtime state, including true second-hop source-text forms such as `EXECSCRIPT(&cExecScriptDeepHolder)` and true second-hop return-expression forms such as `EXECSCRIPT('RETURN &cExecReturnDeepHolder')`; the combined nested-surface harness now also includes second-hop holder flows across `EVAL()` and `EXECSCRIPT()` inputs; shared `EVAL()` / `EVALUATE()` runtime-state coverage now includes macro-expanded expression text such as `ALIAS()` and `SET('PATH')` plus nested macro-indirection through holder variables, including second-hop forms such as `EVAL(&cEvalExprDeepHolder)`, `EVALUATE(&cEvalExprDeepHolder)`, `EVALUATE(&cEvalPathExprDeepHolder)`, runtime-state `&macro` field-name resolution such as `&cFieldExprDeepHolder`, and direct true second-hop `&macro` expression indirection such as `&cMacroNameDeepHolder`; `LOOKUP()` helper coverage now includes nested macro-indirection for return/search expression text plus alias/tag designators on local and SQL cursors, including second-hop expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`, second-hop numeric return-expression forms such as `LOOKUP(&cAgeExprDeepHolder, &cSearchExpr, ...)`, second-hop local designator forms such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`, second-hop miss-path default typing for numeric `0` and logical `.F.` return expressions, second-hop SQL expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`, full all-argument second-hop SQL forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, &cAliasDeepHolder, &cTagDeepHolder)`, and second-hop SQL designator forms such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`; parameter-default coverage now also includes true second-hop macro defaults such as `LPARAMETERS nValue = &cDefaultExprDeepHolder`; macro dot-suffix coverage now includes nested-holder forms before suffix concatenation through second-hop cases such as `&cTypeDeepHolder.Name`, `&cFieldDeepHolder.`, and the literal fallback branch `&cStemDeepHolder.World`; direct `&macro` expression indirection coverage now also includes a true second-hop fallback-text branch via `&cFallbackExprDeepHolder`; embedded macro identifier coverage now includes nested-holder forms such as `m&cType2Holder.ID` and `m&cType2DeepHolder.ID`; and alias-qualified macro field access coverage now includes direct true second-hop alias reads such as `&cAliasDeepHolder..NAME` / `&cAliasDeepHolder..AGE`, the dynamic `AGE` second-hop form `&cAliasDeepHolder..&cAgeFieldHolder`, plus nested-holder alias and field-name variables through forms such as `&cAliasDeepHolder..&cNestedFieldHolder`
- `LOCATE`
- `SCAN/ENDSCAN`
- `CALCULATE`
- `COUNT`
- `SUM`
- `AVERAGE`
- `TOTAL`
- `READ EVENTS`, `CLEAR EVENTS`
- `REPLACE`
- `APPEND BLANK`
- `DELETE`, `RECALL` — current-record commands plus `ALL`, `NEXT <n>`, `RECORD <n>`, and `REST` physical record scopes composed with `FOR`, `WHILE`, `IN`, active filters, and the existing `SET DELETED` behavior for local and synthetic SQL/result cursors
- `DELETE FROM`
- `INSERT INTO`
- `PACK`, `ZAP`
- `SET FILTER TO/OFF` — cursor-scoped filter state for local and synthetic SQL/result cursors, including macro-expanded expression text through the shared `SET` helper and true second-hop holder forms such as `SET FILTER TO &cFilterExprDeepHolder IN sqlcust`; installing or clearing a filter preserves the current physical pointer, immediate field access, `RECNO()`, `BOF()`, `EOF()`, `FOUND()`, selected work area, and targeted non-selected work area until navigation/search evaluates the filter
- `SET FIELDS TO/ON/OFF` — first-pass session-scoped field visibility for runtime field lookup, including list readback through `SET('FIELDS')`; the shipped visibility lane now also honors `LIKE` / `EXCEPT` filters for both direct field resolution and consumers such as `BROWSE`, including true second-hop holder forms such as `SET FIELDS TO &cFieldsSpecDeepHolder`
- `SET ORDER TO TAG`
- `SEEK`
- `GO` / `GOTO`
- `SKIP`
- `USE IN <alias>` alias-target closure now also has focused true second-hop macro proof, including forms such as `USE IN &cAliasExprDeepHolder`
- `BROWSE` — first-pass headless/runtime browse event lane over local/runtime cursors; honors selected or `IN`-targeted cursor context, reuses session `SET FILTER` / `SET FIELDS` state by default, now including `LIKE` / `EXCEPT` field-visibility filters, accepts inline `FIELDS` and `FOR` clause capture, and emits `runtime.browse` metadata for later host/UI wiring
- `EDIT`, `CHANGE`, `INPUT`, `ACCEPT` — first-pass headless interactive command lane that emits runtime events (`runtime.edit`, `runtime.change`, `runtime.input`, `runtime.accept`) with captured prompt/target/field metadata for host-driven UI integration; `EDIT` / `CHANGE` now also surface effective selected-cursor/view metadata when a local cursor is active, including visible fields and active filter state, while `INPUT` / `ACCEPT` now reuse that same selected-cursor/view metadata and assign deterministic empty-string headless results for `TO <target>` variables
- `WAIT`, `KEYBOARD`, `DISPLAY`, `LIST`, `INPUT`, `ACCEPT`, `CALCULATE`, `COUNT`, `SUM`, `AVERAGE`, `TOTAL` — first-pass headless interaction/event lane that emits `runtime.wait`, `runtime.keyboard`, `runtime.display`, `runtime.list`, `runtime.input`, `runtime.accept`, and aggregate runtime events with captured mode/clause payloads for host integration; `WAIT` now captures resolved prompt/timeout/target state plus source expressions for common `WAIT [WINDOW] ... [TIMEOUT ...] [TO <target>] [NOWAIT] [NOCLEAR]` forms and assigns deterministic empty-string headless `TO` results, including true second-hop holder target forms such as `TO &cWaitTargetDeepHolder`; `INPUT` / `ACCEPT` now mirror that resolved prompt/target payload fidelity and deterministic empty-string `TO` behavior, including true second-hop holder target forms such as `TO &cInputTargetDeepHolder`; `KEYBOARD` now captures resolved key payloads plus source expressions and common `PLAIN` / `CLEAR` flags, `DISPLAY` / `LIST` now surface adjacent `STRUCTURE` / `STATUS` / `MEMORY` metadata in addition to the richer `RECORDS` lane, `DISPLAY` / `LIST RECORDS` now also retain `WHILE` clause payload detail and true second-hop `IN` target detail such as `IN &cAliasDeepHolder`, `DISPLAY` / `LIST STRUCTURE` now also has true second-hop `IN` target proof via forms such as `IN &cAliasDeepHolder`, and aggregate command events now also expose targeted cursor/work-area, visible fields, active filter, scope, `FOR` / `WHILE`, output-variable, and raw/resolved `IN` target detail for host/runtime readback, including true second-hop aggregate `TO` target forms such as `TO &cCountDeepHolder`, `TO &cSumDeepHolder`, and `TO &cCalcDeepHolder`
- `GETFILE`, `PUTFILE`, `GETDIR`, `INPUTBOX` — first-pass headless dialog-helper command lane; parser now captures both common clause-keyword forms and parenthesized/positional argument forms for prompt/title/default/filter/target metadata, and dispatcher emits `runtime.getfile`, `runtime.putfile`, `runtime.getdir`, and `runtime.inputbox` events while assigning deterministic empty-string first-pass results to `TO <target>` variables in headless mode
- `PUSH KEY` / `POP KEY`, `PUSH MENU` / `POP MENU`, `PUSH POPUP` / `POP POPUP` — first-pass session-scoped key/menu/popup stack semantics with deterministic push/pop depth behavior and runtime events (`runtime.push_*` / `runtime.pop_*`) including empty-pop signaling
- `SAVE TO`, `RESTORE FROM` — first-pass memvar persistence lane with `.mem` auto-extension behavior, `LIKE`/`EXCEPT` filtering, `ADDITIVE` merge support, robust escaped text serialization, and explicit empty-value markers for stable round-trip restore behavior; saved `.mem` payloads now also avoid persisting `PUBLIC` scope markers when the visible binding is actually a shadowing `LOCAL` or `PRIVATE` over an outer public name
- `RELEASE` — first-pass public/global release semantics now preserve public-pinned scalar variables and arrays across `RELEASE ALL`, `RELEASE ALL LIKE`, and `RELEASE ALL EXCEPT`, while named and non-public release paths continue to remove ordinary bindings; releasing current-frame `LOCAL` names now also reveals shadowed outer bindings instead of erasing them
- `SELECT`
- `USE`, `USE IN`, `USE AGAIN`
- `SET DATASESSION TO`
- `OPEN DATABASE`, `SET DATABASE TO`, `CLOSE DATABASE(S) [ALL]` — first-pass immutable DBC lifecycle parity now tracks open/current database identity per data session, preserves first-open `EXCLUSIVE` / `SHARED` plus `NOUPDATE` state, resolves omitted `.dbc` extensions through the default directory and `SET PATH`, and resolves DBC/DCT/DCX casing deterministically on case-sensitive hosts; `CLOSE DATABASE(S)` closes the current database, `CLOSE DATABASES ALL` closes databases in the current data session, and `CLOSE ALL` releases database state across data sessions
- `DBC()` / `DBUSED(cDatabaseName)` — first-pass current-database path and current-data-session open-database lookup; no undocumented `DATABASE()` alias is exposed
- `SET DEFAULT TO`
- `SET` — first-pass runtime-state surface now includes second-hop holder macro value coverage across ordinary string/numeric/logical options in addition to the earlier direct-variable paths; focused regressions now lock down representative forms such as `SET PATH TO &cPathTargetDeepHolder`, `SET MARK TO &cMarkTargetDeepHolder`, `SET DECIMALS TO &cDecimalsTargetDeepHolder`, `SET COLLATE TO &cCollateTargetDeepHolder`, `SET NULL TO &cNullTargetDeepHolder`, and `SET ANSI TO &cAnsiTargetDeepHolder`
- `CLOSE ALL` / `CLOSE TABLES` / `CLOSE DATABASES` — first-pass close semantics now cover open work-area cursors, session-scoped DBC lifecycle state, and non-DBF runtime handles for `ALL`/`DATABASES` scopes (SQL connection state, tracked OLE handles, and outstanding low-level `FOPEN` handles)
- `ON ERROR`
- `ON SHUTDOWN` — first-pass shutdown-handler semantics for `ON SHUTDOWN DO <routine> [WITH ...]` plus common inline compatibility forms including `ON SHUTDOWN CLEAR EVENTS` and close-style cleanup commands such as `ON SHUTDOWN CLOSE DATABASES ALL`; shutdown routines run before final `QUIT` completion and nested `QUIT` inside the shutdown routine is treated as the terminal quit rather than recursing indefinitely
- `PUBLIC` — first-pass public identity declaration for memory variables and arrays
- `LOCAL`
- `PRIVATE`
- `PARAMETERS` / `LPARAMETERS` — first-pass argument binding for `DO ... WITH ...`, including `@` by-reference write-back and default-value expressions when callers omit trailing arguments; default expressions now evaluate through the ordinary expression engine, so macro-expanded defaults such as `LPARAMETERS x = &cExpr` resolve consistently; routine declarations that carry inline signatures such as `PROCEDURE Foo(x)` and `FUNCTION Bar(x, y)` now also normalize the routine name and materialize an implicit `LPARAMETERS` declaration so inline parameter forms bind through the same stack-frugal call path as explicit `LPARAMETERS`
- `DIMENSION` / array-form `DECLARE` — first-pass runtime arrays with one- and two-dimensional sizing
- `DECLARE ... IN <dll>` — first-pass Windows native / managed declaration surface, including bare `@var` call arguments and native by-ref write-back for declared `STRING @` and numeric parameters when the callsite also uses `@`; mounted VFP9 documentation says omitting either marker passes by value, but Copperfin deliberately rejects a declared numeric `@` without a call-site `@` variable as localized Error 11 before native entry rather than reproducing the arbitrary-pointer crash hazard. Managed declarations currently target public static .NET Framework v4 methods named as `Namespace.Type.Method`, load the exact resolved assembly through `Assembly.LoadFrom`, retain CLR binder scalar coercion, resolve sibling dependencies, and preserve native-export precedence for mixed-mode images; managed objects/events/callbacks/by-reference write-back and non-Windows managed hosting remain outside this first-pass surface.
- `STORE` — evaluates the expression once and assigns through the same target semantics as ordinary assignment, including scalar variables, direct array elements, macro-expanded targets, `m.` memory variables, and OLE property targets
- `CREATE TABLE` — first-pass local DBF creation for field declarations (`C`/`N`/`F`/`B`/`L`/`D`/`I`/`Y`/`T`/`M`/`V`/`Q` plus common long-form names); nullable/default annotations are accepted by the parser, created/open cursors apply field defaults for omitted `INSERT INTO` fields, and `NOT NULL` fields are validated with append rollback on failure. Free-table physical field names are limited to 10 encoded bytes and are rejected through localized diagnostics before DBF/FPT publication.
- `CREATE CURSOR` — local temp-backed DBF cursor creation using the shared structural field-declaration parser and field-rule mapping; optional `NAME <alias>` declarations open under the explicit alias rather than combining the base cursor name and clause text. Resulting cursors participate in normal local-table mutation/schema flows (`APPEND BLANK`, `REPLACE`, `INSERT INTO`, `AFIELDS`, `FCOUNT`, `FSIZE`, `RECCOUNT`) instead of the old synthetic stub, and `DEFAULT` / `NOT NULL` metadata are enforced through the same open-cursor field-rule path used by `CREATE TABLE`
- `ALTER TABLE ... ADD/DROP/ALTER COLUMN` — first-pass local DBF schema rewrite that appends, removes, or rewrites one supported field while preserving row values and deleted flags where the resulting schema can store them; `ADD COLUMN ... DEFAULT ...` now also backfills existing rows with the declared default instead of leaving the new field blank
- structural rollback coverage now explicitly proves that `ALTER TABLE` rewrites and `PACK MEMO` compaction remain readable after `ROLLBACK`, so the structural lane is defended against transaction-unwind regressions as well as forward-mutation success
- `COPY TO` — full local DBF export via `create_dbf_table_file`; optional `FIELDS` clause filters columns, including single-field keyword-named selections such as `FIELDS TYPE` plus deeper `LIKE` / `EXCEPT` forms across the same copy-to/copy-to-array lane; optional `FOR` clause filters rows by visibility; first-pass `TYPE SDF` fixed-width text export plus VFP-style `TYPE CSV`, `TYPE TAB`, and `DELIMITED` text export, including CSV field-name headers, quoted character fields, tab-delimited rows, `WITH <enclosure>`, and `WITH CHARACTER <separator>` handling; first-pass `TYPE JSON` export emits a modern object-array JSON payload with typed numeric/logical values and string keys from field names; first-pass `TYPE XLS` export emits SpreadsheetML-style workbook XML with header-row and typed cell data; first-pass `TYPE DIF` export emits a conservative DIF-style text table with header-row emission and typed numeric/string cells; first-pass `TYPE SYLK` export emits a conservative SYLK-style text table with header-row emission and typed numeric/string cells; first-pass `COPY TO ARRAY <name>` populates a two-dimensional runtime array (rows=qualifying records, cols=fields) with optional `FIELDS` and `FOR` clauses, including macro-expanded array-name targets and true second-hop holder forms such as `&cMacroArrayDeepHolder`, `&cArrayNameDeepHolder`, and `&cTempArrayDeepHolder`
- selected synthetic SQL/result cursors now also materialize DBF output for plain `COPY TO` even when no backing local source path exists, deriving schema from remote field metadata (or remote rows) and preserving selected alias/record pointer state during export
- wildcard `FIELDS LIKE` / `FIELDS EXCEPT` coverage now explicitly reaches DBF `COPY TO` / `APPEND FROM` flows in addition to the earlier array-transfer and browse/name-object slices
- DBC metadata export now also has a first-pass whole-database JSON lane through the native asset-inspector API (`export_database_as_json`), including decoded binary `PROPERTIES` memo bags from `.dct` sidecars, case-insensitive same-base `DCT/DCX/DBF` resolution on case-sensitive filesystems, preference for the catalog `DATABASE` object name over the raw `.dbc` stem, and table-schema/table-row snapshots for resolvable local `TABLE` catalog entries
- `COPY STRUCTURE TO` — schema-only export (zero records); first-pass
- selected SQL/result cursors now also export schema-only DBF output through `COPY STRUCTURE TO`, including metadata-only remote cursors with zero rows. Logical metadata names longer than the free-table 10-byte physical limit are mapped deterministically and collision-safely for DBF output without changing the selected cursor's logical schema; already-valid physical names remain unchanged.
- `APPEND FROM` — full local DBF import via `parse_dbf_table_from_file`; inserts each non-deleted source record using `append_blank_record_to_file` + `replace_record_field_value`; when the source DBF is already open, its active `SET FILTER` expression selects import rows without changing the source cursor position; optional `FIELDS` clause, including single-field keyword-named selections such as `FIELDS TYPE` plus deeper `LIKE` / `EXCEPT` forms across the same append/import array lane; native DBF name matching continues to skip source-only fields but now aborts and atomically restores DBF plus cursor state when a matched destination-field write fails; first-pass `TYPE SDF` fixed-width text import plus first-pass `TYPE CSV`, `TYPE TAB`, and `DELIMITED` import with quoted-field parsing, tab-delimited rows, and matching CSV header skipping; first-pass `TYPE JSON` import reads the shipped object-array JSON lane and maps object keys to destination field names through the shared blank-row plus field-replacement path; first-pass `TYPE XLS` import reads the shipped SpreadsheetML-style workbook lane, skips a matching header row, and appends rows through the shared blank-row plus field-replacement path; first-pass `TYPE DIF` import reads the shipped DIF-style text lane, skips a matching header row, and appends rows through the same shared DBF mutation path; first-pass `TYPE SYLK` import reads the shipped SYLK-style text lane, skips a matching header row, and appends rows through that same shared DBF mutation path; failed file-based format imports now share DBF/cursor rollback through the common command-undo guard; first-pass `APPEND FROM ARRAY <name>` appends rows of a runtime array as new records (columns map positionally to destination fields with optional `FIELDS` filtering), including macro-expanded array-name sources, true second-hop holder forms such as `&cTempArrayDeepHolder` and `&cArrayNameDeepHolder`, typed date/datetime serialization instead of string-only writes, and command-atomic rollback with restored cursor state when any matched destination-field write fails
- selected synthetic SQL/result cursors now also support first-pass DBF `APPEND FROM` mutation in-memory (field-name mapping plus `FIELDS` filtering) instead of event-only stubbing, while unsupported non-DBF source types return deterministic runtime diagnostics
- selected synthetic SQL/result cursors now also apply `FOR` filtering during DBF `APPEND FROM` mutation, so only rows matching the clause are materialized into the target cursor; focused SQL cursor macro-filter coverage now also includes true second-hop `FOR &expr` forms across `LOCATE`, `DELETE`, and `RECALL`, such as `&cLocateExprDeepHolder` and `&cDeleteExprDeepHolder`
- selected synthetic SQL/result cursors now also support `APPEND FROM TYPE JSON` and `APPEND FROM TYPE CSV` / `TYPE DELIMITED` in-memory mutation: JSON sources map object keys to remote cursor field names, CSV/DELIMITED sources map positional values with optional CSV header-row skip; both branches honor `FIELDS` filtering and the `FOR` clause; previously both paths returned a "DBF source only" stub error
- `SCATTER MEMVAR` / `SCATTER TO <array>` / `SCATTER NAME <object>` — snapshot current record fields to `m.<fieldname>` variables, runtime arrays, or object properties; `FIELDS`, `BLANK`, and `MEMO` are honored with typed values, including runtime-facing `MM/DD/YYYY` and `MM/DD/YYYY HH:MM:SS` strings for `D`/`T` fields; the parser now also disambiguates single-field filters such as `FIELDS NAME` from the command's own `NAME` clause for both memvar and object-target forms, and deeper regression coverage now exercises `LIKE` / `EXCEPT` filters on object-target flows; macro-expanded array-name targets are supported, including direct true second-hop holder forms such as `SCATTER TO &cMacroRowNameDeepHolder`; `SCATTER NAME &cObjectName`-style macro-expanded object variable targets are also supported, including direct true second-hop holder forms such as `SCATTER NAME &cObjectNameDeepHolder`; nested object-property targets such as `SCATTER NAME oHolder.Row` / `GATHER NAME oHolder.Row` are supported, including macro-expanded nested path segments such as `SCATTER NAME &cHolder.&cChild` and direct true second-hop holder forms such as `SCATTER NAME &cHolderDeepHolder.&cChildDeepHolder`; scatter now creates missing nested child-object targets on demand instead of requiring them to already exist; non-`ADDITIVE` scatter on an existing object target now replaces that target object instead of mutating it in place, while `SCATTER NAME <object> ADDITIVE` reuses an existing object when present and merges field properties onto it; first-pass
- `GATHER MEMVAR` / `GATHER FROM <array>` — write `m.<fieldname>` variables or array elements back to current record via direct `replace_record_field_value` calls (type-faithful, no string-quote round-trip); `FIELDS` and `FOR` are honored, including single-field `FIELDS NAME` filters on both memvar and `GATHER NAME <object>` forms plus deeper `EXCEPT` coverage on object-target flows, date/datetime/blank runtime strings now restore consistently into DBF storage, and macro-expanded array-name sources are supported, including direct true second-hop holder forms such as `&cMacroRowNameDeepHolder`, while `GATHER NAME <object>` macro object targets now also have direct true second-hop proof via forms such as `&cObjectNameDeepHolder`; first-pass
- wildcard `FIELDS LIKE` / `FIELDS EXCEPT` coverage now also explicitly defends the MEMVAR scatter/gather path instead of only the NAME-object and array-transfer paths
- `SCATTER MEMVAR BLANK` (and other BLANK forms) now succeeds on zero-record tables by deriving typed blank values from the cursor schema instead of requiring a current record; `blank_value_for_field` now correctly returns an empty string for character ('C') and varchar ('V') fields, matching VFP behavior where `SCATTER BLANK` creates `''` for character-type memvars
- `UPDATE` — first-pass local/remote cursor command forms, including `UPDATE <alias> SET ...`, `UPDATE SET ...` against the selected cursor, and `UPDATE IN <alias> SET ...`; `WHERE`/`FOR` scope clauses reuse scoped record replacement semantics, including macro-expanded `FOR &cExpr` filters
- `DELETE FROM` / `INSERT INTO` — first-pass SQL-style local/remote cursor mutation forms; `DELETE FROM <target> WHERE/FOR <expr>` tombstones matching target rows and defaults to all rows when no condition is supplied, including macro-expanded `FOR &cExpr` filters and true second-hop local forms such as `FOR &cDeleteExprDeepHolder`; `INSERT INTO <target> [(fields)] VALUES (...)` appends through the shared blank-row plus field-replacement path, including expression-driven cursor targets, schema-order mapping when the field list is omitted, selected-cursor preservation, local DBF persistence, and synthetic SQL result cursor parity; `INSERT INTO <target> [(fields)] SELECT ...` now materializes through the same first-pass single/joined-cursor query planner used by native list-control `Requery()`, preserves selected/source cursor state, and rolls the complete append batch back if any target row fails
- `INSERT INTO` failure rollback — local and synthetic remote inserts now restore the previous record count when a field write fails after the blank-row append step, preventing a failed insert from leaving an extra blank row behind
- `PACK`, `PACK MEMO`, and `ZAP` — first-pass local DBF table-maintenance commands; `PACK` physically removes deleted records while preserving schema and kept-row order, `PACK MEMO` rewrites the memo sidecar from current memo values to compact stale memo blocks, and `ZAP` truncates the table to zero records while keeping it appendable; local cursors now honor first-pass exclusive-use guards from `SET EXCLUSIVE` / `USE ... SHARED|EXCLUSIVE`; synthetic remote/result cursors get matching in-memory behavior where applicable
- `SET REPROCESS`, `SET MULTILOCKS`, `RLOCK()`, `FLOCK()` / `LOCK()`, `ISRLOCKED()`, `ISFLOCKED()`, and `UNLOCK` — first-pass in-process lock-state tracking for open local/runtime cursors, including `SET()` readback, `UNLOCK ALL`, record-specific `UNLOCK RECORD <n>` release, `AUTOMATIC` default `REPROCESS` semantics, and bounded retry/timeout behavior across explicit lock attempts plus shipped local mutation paths; this establishes runtime-visible lock semantics but does not yet claim cross-process OS locking or transaction journaling
- `SET` runtime-state commands — first-pass normalized/evaluated handling for common state options including boolean options, `REPROCESS`, date/time formatting controls, `PATH`, `DECIMALS`, `COLLATE`, `MARK`, `POINT`, `SEPARATOR`, and `CURRENCY`; string/numeric RHS values can come from literals, variables, or macro-expanded expressions where supported, with `SET()` readback defaults for the shipped options
- `BEGIN TRANSACTION`, `END TRANSACTION`, `ROLLBACK`, and `TXNLEVEL()` — first-pass transaction nesting counters with per-data-session isolation, runtime event emission (`runtime.transaction.begin` / `runtime.transaction.end` / `runtime.transaction.rollback`), durable local DBF preimage journaling for shipped mutation paths, rollback cursor refresh/closure when replay removes a table, and startup replay of pending journals after interrupted transactions

Local DBF `APPEND BLANK` now also has a conservative opaque-field lane for otherwise readable unsupported record layouts: unknown/binary field bytes are zero-initialized instead of rejecting the whole append, which lets runtime mutation continue over mixed-layout tables while still avoiding overclaimed write-surface semantics for direct typed replacement on those field families.

Shared local DBF direct writes now also have a matching conservative opaque-field replacement lane for those same otherwise readable unsupported families: callers can write `NULL`, width-fitting raw text, or `0x`-prefixed hex payloads without inventing higher-level typed semantics for the underlying field family.

Command-level aggregate assignment now also includes first-pass `TO ARRAY` forms for `COUNT`, `SUM`, and `AVERAGE`, using one-dimensional runtime array assignment through the shared array helper path. Focused regression coverage validates scope-clause behavior (`ALL`/`REST`/`NEXT`/`RECORD`), `FOR`/`WHILE` filtering, `IN` alias targeting, `TO &macro` target resolution on the command aggregate lane including second-hop holder forms such as `&cCountHolder`, and malformed `TO ARRAY` diagnostics. `CALCULATE` now also preserves `WHILE` clause semantics on the same aggregate path. Aggregate expression parity also now includes omitted-expression forms: `COUNT()` returns visible-row counts, and no-expression `SUM()` / `AVG()` / `MIN()` / `MAX()` use the first numeric-compatible field (`N/F/B/I/Y`) with deterministic no-numeric-field fallbacks.

The `m.` variable namespace prefix is also now correctly handled as a memory-variable alias across assignment, lookup, expression evaluation, and declarations: `m.<name>`, `M.<name>`, and bare `<name>` share the same local/global runtime binding, and `m.<name>` no longer falls through to OLE property resolution.

The memory-variable lane now also restores saved outer bindings immediately when `RELEASE <name>` targets a currently shadowing `PRIVATE` variable, instead of waiting until the current frame unwinds.

`PRIVATE ALL [LIKE <skeleton> | EXCEPT <skeleton>]` now hides matching existing caller variables and arrays using VFP `?`/`*` wildcard matching, restores their scalar and array state on explicit release or frame unwind, and leaves nonmatching `LIKE` / matching `EXCEPT` bindings visible to the called routine.

Wildcard `RELEASE ALL LIKE ...` / `RELEASE ALL EXCEPT ...` handling now also covers array-only names rather than only scalar/global bindings, while continuing to preserve `PUBLIC` variables and arrays.

`CLEAR MEMORY` now also drops deferred `PRIVATE` saved bindings so cleared names do not reappear later when nested frames unwind.

`RELEASE ALL` now also clears current-frame `LOCAL` bindings instead of only global/array candidates, and later reassignment of those locals stays in local scope rather than leaking new globals.

`CLEAR MEMORY` now also clears current-frame `LOCAL` bindings while preserving local declaration routing for later in-scope reassignment.

The same memory-file lane now also has first-pass array/scope fidelity for `SAVE TO` / `RESTORE FROM`: saved `.mem` payloads now preserve runtime arrays with dimensions plus real visible-`PUBLIC` scope markers, additive restore now writes through visible `LOCAL` bindings instead of leaving restored values hidden behind stale locals, and non-additive restore now clears stale arrays and deferred `PRIVATE` shadow state before recreating the saved bindings.

That same memory-lifetime lane now also handles shadowed locals more faithfully: `RELEASE <name>` and `RELEASE ALL` remove current-frame `LOCAL` bindings without destroying the outer global/public value they were masking.

The indexed-search lane now also has a first-pass runtime collation step for plain string orders: when an active order does not already carry an explicit case-folding expression hint, non-`MACHINE` session collations such as `SET COLLATE TO GENERAL` now case-fold plain string seek comparisons instead of leaving `SET COLLATE` as metadata-only state.

That plain-string collation step is now explicitly defended on synthetic SQL/result cursors too: focused SQL seek coverage proves the same non-`MACHINE` session collation behavior on temporary `NAME`-style orders instead of leaving remote parity implied by the shared implementation.

The adjacent date/time runtime-state lane now also has stronger `SET()` readback fidelity: `SET('DATE')` defaults to `MDY` instead of falling through to `OFF`, and focused coverage now locks down data-session isolation/restoration for `DATE`, `CENTURY`, `MARK`, `HOURS`, and `SECONDS`.

The broader runtime-state lane now also has focused session-isolation/readback coverage for `COLLATE`, `NULL`, `ANSI`, `REPROCESS`, `MULTILOCKS`, and `EXCLUSIVE`, locking down their default values plus `SET DATASESSION` restoration behavior alongside the earlier shipped command semantics. The adjacent lock-contention slice now also proves that `REPROCESS` defaults to `AUTOMATIC`, that explicit numeric overrides bound retry counts deterministically, and that conflicting local lock/mutation attempts fail with timeout diagnostics instead of busy-spinning.

That same session-state lane now also explicitly defends formatting-state isolation/readback for `MEMOWIDTH`, `POINT`, `SEPARATOR`, and `CURRENCY`, plus adjacent calendar/week-state isolation/readback for `FDOW` and `FWEEK`. The shared `SET()` readback path also now preserves numeric/string values for these non-boolean settings instead of collapsing values like `1` to `ON` after session restoration.

Runtime arrays now have first-pass direct element reads and writes through both bracket and parenthesis syntax (`array[1]`, `array[1,2]`, `array(1)`, `array(1,2)`), including declared arrays and arrays resized through `ASIZE()`. Macro-expanded array identifiers now also work across adjacent helper and element-access paths such as `ALEN(&cArrayName)`, `ALINES(&cTargetName, ...)`, `ACOPY(&cSourceName, &cTargetName, ...)`, `&cArrayName[2,1]`, and `&cArrayName(1,2)`, and focused coverage now includes true second-hop forms such as `ALINES(&cLinesNameDeepHolder, ...)`, `ALEN(&cArrayNameDeepHolder)`, `AELEMENT(&cArrayNameDeepHolder, ...)`, `ACOPY(&cArrayNameDeepHolder, &cCopyNameDeepHolder, ...)`, `&cArrayNameDeepHolder[2,1]`, and `&cArrayNameDeepHolder(1,2)`. Shared assignment-target coverage now also includes true second-hop macro-expanded array-element targets such as `STORE 6 TO &cMacroTargetDeepHolder`. `parse_primary()` now also prefers shipped array-helper function calls over same-named runtime arrays when `(` follows an identifier, avoiding collisions such as an existing `aLines` array shadowing `ALINES(...)`. The array helper surface now also covers `ACOPY()` row-major copies over one- and two-dimensional arrays, `AELEMENT()` / `ASUBSCRIPT()` index conversion, `ASCAN()` start/count scans with column, case-insensitive, exactness, row-return, and first-pass predicate-expression search support, `ASORT()` start/count windows plus descending/case-insensitive/numeric-aware ordering and two-dimensional row sorting by the start element's column, and two-dimensional `ADEL()` / `AINS()` row/column shifting. Focused proof now also covers true second-hop target-array holder chains for `ALINES()` and `ACOPY()` via forms such as `&cLinesNameDeepHolder` and `&cCopyNameDeepHolder`. `ASCAN()` macro-expanded predicate fidelity and scan-metadata isolation are also now fully proven: variable-reference predicates, first-hop macro-expanded predicates, and second-hop holder-chain predicates all resolve correctly, and all four `_ASCAN*` metadata variables are restored on every exit path.

The current xAsset runtime surface behind those commands is now green at the first-pass runtime level: `DO FORM` is backed by generated `SCX/VCX` bootstrap sources that sequence `DataEnvironment` open/close hooks plus root-object lifecycle methods where present, `REPORT FORM` and `LABEL FORM` now both support preview/event-loop and `TO FILE` execution lanes, and menu assets now have executable startup/action/cleanup bootstrap modeling through the shared `MNX` xAsset path.

The current indexed-search slice covers `SET ORDER TO TAG`, first-pass `ASCENDING` / `DESCENDING` direction control on the local `SET ORDER` / `SEEK` path, grounded `UPPER(...)` seek-key normalization on that same search path, first-pass composite tag-expression evaluation for concatenated keys such as `UPPER(LAST+FIRST)`, first-pass substring-style tag evaluation for `LEFT(...)`, `RIGHT(...)`, and `SUBSTR(...)` expressions such as `UPPER(LEFT(NAME, 3))`, `UPPER(RIGHT(NAME, 3))`, and `UPPER(SUBSTR(NAME, 2, 3))`, first-pass grounded `FOR`-filtered order behavior for extracted `DELETED() = .F./.T.` hints, command/function `SEEK`, `INDEXSEEK()`, `ORDER()`, `TAG()`, `SET EXACT`, `SET NEAR`, function-order designator suffix parsing for `SEEK()` / `INDEXSEEK()` one-off `ASCENDING` / `DESCENDING` overrides, command-level one-off `SEEK ... TAG/ORDER` probes for local DBF-backed cursors, first-pass ad hoc local order expressions such as `SET ORDER TO UPPER(NAME)` and `SEEK(..., 'People', 'UPPER(NAME)')`, and descriptive stored CDX tag names such as `FULLNAME` now bind to their hinted tag-page-local expressions even when the tag name itself does not resemble the key expression. The runtime also preserves the actual inspected index-file identity for `ORDER(alias, 1)` plus `TAG(indexFile, ...)` across supported xBase index extensions instead of assuming `.cdx`.

Indexed candidate visibility now composes cursor-scoped `SET FILTER` and session-scoped `SET DELETED` on that same local and synthetic SQL `SEEK`/`SEEK()`/`INDEXSEEK()` path. Hidden or deleted duplicates are skipped without changing exact/prefix, ascending/descending, `SET NEAR`, status-function, or `INDEXSEEK()` pointer contracts; `SET DELETED OFF` remains independent from each order's own `FOR` expression.

Deferred filter evaluation now also matches the mounted VFP9 empty-result boundary behavior for nonempty local and synthetic SQL cursors. After `SET FILTER TO .F.`, `GO TOP` moves to physical EOF with `RECNO() = RECCOUNT() + 1`, `BOF() = .F.`, and `EOF() = .T.`; `GO BOTTOM` retains that record number and reports both `BOF()` and `EOF()` true. `FOUND()` is preserved, and character/numeric field reads at either boundary return their schema-typed blank values. Local schema descriptors are cached and refreshed on cursor open, transaction replay, and schema mutation so these boundary reads do not reread DBF contents.

That local indexed-search slice now also evaluates a broader grounded tag-expression family on the shipped `SEEK` path, including `LEFT(...)`, `RIGHT(...)`, `SUBSTR(...)`, `PADL(...)`, `PADR(...)`, and a constrained first-pass `STR(<numeric>[, <width>[, <decimals>]])` path captured from loaded tag metadata, including first-pass default-space `PADL/PADR` behavior. The seek path now also preserves significant search-key whitespace so padded tag expressions can match without trimming away meaningful leading/trailing spaces.

Grounded order `FOR` metadata is now consumed a little deeper on the same seek path instead of staying mostly descriptive. In addition to the earlier `DELETED()` filter support, local indexed seeks now honor first-pass top-level string and numeric comparison filters such as `NAME = 'BRAVO'` and `AGE >= 20`, including correct `SET NEAR` miss positioning against the surviving candidate set.

That same local temporary-order path now also has focused command-path `IN <alias|work area>` parity coverage for ad hoc expressions such as `UPPER(NAME)`, including selected-alias preservation and descending `SET NEAR` behavior on non-selected targeted cursors. Coverage now also explicitly defends plain-expression targeted orders (`SET ORDER TO NAME IN People`) under session collation changes, proving that `SET COLLATE TO GENERAL` case-folds `SEEK(..., 'People', 'NAME')` while preserving selected non-target alias/pointer state. Synthetic SQL/result cursors now also honor first-pass ad hoc `FOR` filters on that temporary-order lane, so probes such as `SEEK(..., 'sqlcust', "UPPER(NAME) FOR NAME = 'BRAVO'")` and command-path `SET ORDER TO UPPER(NAME) FOR ...` constrain the candidate set and `SET NEAR` miss positioning instead of treating the `FOR` tail as inert text.

That same ad hoc-order lane now also consumes grounded field-type metadata for plain numeric field expressions such as `SET ORDER TO AGE` and `SET ORDER TO AMOUNT`, so local and synthetic SQL `SET NEAR` probes choose the next numeric key instead of falling back to lexicographic string ordering for one-off numeric temporary orders.

That same local ad hoc-order lane now also has focused helper-path parity for `INDEXSEEK(.F./.T.)` over temporary expressions such as `UPPER(NAME)`, including descending `SET NEAR` miss positioning while preserving the controlling `ORDER()` state.

The expression/function layer currently has first-pass support for these VFP-facing built-ins:

- `COUNT()`, `SUM()`, `AVG()/AVERAGE()`, `MIN()`, `MAX()`
- first-pass `ALEN()` for runtime arrays created by `SCATTER TO <array>`, `AERROR(<array>)`, `DIMENSION` / array-form `DECLARE`, and `ASIZE()`, including total element count and dimension queries
- deeper first-pass `AERROR(<array>)` with VFP-aligned seven-column rows for normal runtime errors: code, message, mixed-case error parameter, faulting work area, fault line, fault procedure, and failing statement text; SQL/ODBC-like `1526` rows now preserve captured handle/detail plus provider-or-operation context and command/target payload when the runtime has them, and OLE/automation-like `1429` rows now preserve captured member-path detail, source object identifiers, and failing action text instead of leaving trailing columns as placeholders when Copperfin already knows that payload
- first-pass runtime array helpers over Copperfin runtime arrays: `ACOPY()`, `ADIR()`, `AELEMENT()`, `AFIELDS()` over local and synthetic SQL cursor schemas, `ALINES()`, `ASIZE()`, `ASCAN()`, `ADEL()`, `AINS()`, `ASORT()`, `ASUBSCRIPT()`, and `AUSED()`; the focused array regression target now exercises predicate-style `ASCAN()` searches, including unquoted block-style predicate arguments, common two-dimensional `ACOPY()` row/column workflows, and declared-array/metadata helper coverage separately from the legacy catch-all PRG-engine test
- `EVAL()` and deeper first-pass bounded-indirection `&macro` substitution in expression paths (identifier-chain expansion with deterministic recursion/cycle safety), now including alias-qualified field continuations such as `&cAlias..NAME`, macro-driven suffix forms such as `&cAlias..&cField`, and focused nested evaluation coverage where stored expression text feeds another `EVAL()` layer
- `SQLCONNECT()`, `SQLSTRINGCONNECT()`, `SQLEXEC()`, `SQLDISCONNECT()`, `SQLROWCOUNT()`, `SQLPREPARE()`, first-pass `SQLGETPROP()`, `SQLSETPROP()`, `SQLCANCEL()`, `SQLCOMMIT()`, `SQLROLLBACK()`, `SQLDATABASES()`, `SQLPRIMARYKEYS()`, `SQLFOREIGNKEYS()`, `SQLTABLES()`, and `SQLCOLUMNS()`
- `CREATEOBJECT()`, `GETOBJECT()` — first-pass runtime activation parity now includes caller-PRG-first native class lookup followed by the ordered PRGs opened through `SET PROCEDURE TO ... [ADDITIVE]` before Automation fallback, loaded-procedure base-class inheritance with per-object resolved-lineage retention across later procedure replacement/clear, explicit-PRG `NEWOBJECT()` isolation, object-property round-trip storage, seeded `Scripting.Dictionary` collection behavior (`Add` / `Exists` / `Item` / `Remove` / `RemoveAll` / `Count`), `NEWOBJECT()` library/server activation metadata capture, `GETOBJECT()` class/source targeting plus matching-attachment reuse instead of unconditional duplicate-handle registration, and trapped seeded-automation missing-member faults so `ON ERROR` / `AERROR()` and `TRY/CATCH/FINALLY` can contain `1429`-style property/method failures without poisoning later object use
- `DEFINE CLASS` array properties — class-body `DIMENSION` / array-form `DECLARE` statements now create arrays scoped to each native object instance, with member indexing, `ALEN()` / supported array mutation helpers, and `TYPE('oObject.aArray')` using the per-instance storage rather than the global array registry
- first-pass same-session native PRG event helpers: `BINDEVENT()`, `RAISEEVENT()`, and `UNBINDEVENTS()` now bind representative native-object events to native methods or top-level routines in the current runtime session, dispatch handlers through the existing stack-frugal iterative frame machine, support before/after and simple-call suppression flags on the current native-object lane, and deterministically prune bindings during explicit unbinds or released-object cleanup; `AEVENTS(array, oObject)` now also enumerate that same native binding table in a deterministic five-column runtime-array shape for representative source/handler inspection without clobbering existing or missing target arrays when the matching row count is zero, and `AEVENTS(array, 0)` now exposes delegate-scoped current-event metadata as a deterministic three-element array containing the active source object, normalized event name, and representative trigger type (`1` for `RAISEEVENT()`, `2` for direct method-call dispatch) while leaving existing or missing target arrays untouched outside an active native handler; representative same-session native property reads/writes now also participate in that same event lane for dotted property access, dotted assignment, `GETPEM()`, and `SETPEM()` across same-PRG accessors, external PRG-base accessors, top-level routine delegates, same-PRG handler-object delegates, and inherited external-base handler-object delegates; first-pass Windows-message bindings now preserve representative `BINDEVENT(hWnd | 0, nMessage, oHandler, cDelegate)` registrations with `AEVENTS(array, 1)` inspection, `UNBINDEVENTS(hWnd[, nMessage])` teardown, `AEVENTS(array, 0)` current-message metadata with system trigger type `0`, and focused host-dispatch delivery through the same stack-frugal frame machine while paused in `READ EVENTS`; and representative handle acquisition/translation on that lane now includes ordinary `_SCREEN.hWnd` / `_VFP.hWnd` reads, read-only native `Form` / `Toolbar` `hWnd` metadata through ordinary property access plus `GETPEM()` / `PEMSTATUS()`, deterministic `SYS(2326)` / `SYS(2327)` round-trips for those runtime windows and objects, declarative built-in visual/control child objects that now materialize as native runtime objects, representative `OleControl` / ActiveX host objects that now expose deterministic read-only `hWnd` metadata plus creation-time `OLEClass`, `DocumentFile`, and `OLETypeAllowed` inspection surfaces with coherent ordinary reads plus `GETPEM()` / `PEMSTATUS()` behavior, a stable read-only nested `Object` Automation surface derived from `OLEClass` for dotted contained-object property/method access, representative built-in `DoVerb()` activation success for default/edit/open flows plus canonicalized named-verb `DoVerb('edit'|'open')` activation on the same host lane, deterministic read-only `ObjectVerbsCount` plus zero-based `ObjectVerbs(index)` inspection for the representative supported named activation verbs `edit` and `open`, ordinary direct contained-object property/method routing on native OleControl hosts when the container itself does not own the referenced member, direct contained-object property reflection on that same host lane through `GETPEM()` / `PEMSTATUS()` that matches the explicit `.Object` path, direct contained-object property updates on that same host lane through `SETPEM()` that now match both ordinary dotted assignment and the explicit `.Object` path, representative direct contained-object `AMEMBERS()` enumeration on both the explicit `.Object` surface and the host fallback path for common properties/methods such as `Left`, `Visible`, and `Compose`, host-side `ADDPROPERTY()` / `REMOVEPROPERTY()` protection for those same representative direct contained-object member names so the synthesized contained Automation surface cannot be shadowed or erased accidentally, conflict-sensitive host-owned `Application` handling that stays distinct from `Object.Application` on the same OleControl lane, representative `AutoActivate` / `AutoVerbMenu` activation-policy defaults and programmatic updates plus writable `OLERequestPendingTimeout` / `OLEServerBusyTimeout` / `OLEServerBusyRaiseError` timeout-policy surfaces on the same host lane, representative explicit `.Object` reference assignment flows that preserve the transferred runtime object reference through both direct host reads and nested `.Object` reads on that same host lane, and Win Msg `BINDEVENT()` participation on the native fallback path, same-PRG native `DEFINE CLASS ... AS OleControl` objects that preserve class-defined `DocumentFile` / `OLETypeAllowed` values plus nested `Object` / `DoVerb()` / activation-policy / timeout-policy behavior on that same runtime lane, and intentional direct `.hWnd` missing-member faults on representative native controls that do not own a window handle while `GETPEM()` / `PEMSTATUS()` / `SETPEM()` stay coherent for the unsupported member
- representative application-window collection helpers: `_SCREEN.FormCount` / `_VFP.FormCount` now report the live same-session form-plus-toolbar collection size, and `_SCREEN.Forms` / `_VFP.Forms` now expose that same representative window stack through the shared hidden native `Collection` surface with coherent `.Count`, parenthesized and bracketed item selection, direct item-property reads such as `_SCREEN.Forms(1).Caption`, direct item-method invocation such as `_SCREEN.Forms(1).Show()` / `_VFP.Forms(1).SetRefTable(...)`, and `FOR EACH ... IN _SCREEN.Forms FOXOBJECT` traversal while preserving the shipped `_SCREEN.ActiveForm` / `_VFP.ActiveForm` alias lane
- reflection/runtime-surface object helpers: first-pass `COMPOBJ()`, `AMEMBERS()`, `ACLASS()`, `PEMSTATUS()`, `ADDPROPERTY()`, and `REMOVEPROPERTY()` with seeded `Scripting.Dictionary` member metadata for deterministic reflection behavior
- first-pass runtime-surface XML cursor bridge helpers: `CURSORTOXML()` and `XMLTOCURSOR()` over a deterministic Copperfin XML shape, including file-or-string transfer paths, row-count returns for imports, and `runtime.cursortoxml` / `runtime.xmltocursor` event emission with warning events on invalid inputs
- utility coverage already used by shipped tests and bootstrap paths, including occurrence-aware `AT()` / `RAT()`, first-pass case-insensitive `ATC()` / `RATC()`, line-oriented `ATLINE()` / `ATCLINE()` / `RATLINE()`, `LIKE()`, `INLIST()`, `PROPER()`, first-pass `STRCONV(..., 7/8)` casing modes, first-pass phonetic helpers (`SOUNDEX()` and `DIFFERENCE()`), first-pass memo-line helpers (`MEMLINES()` and `MLINE()` with session-scoped memo width, explicit width overrides, optional tab-width expansion, and opt-in LF hard-break flags), `CHRTRAN()` / first-pass `CHRTRANC()`, `STRTRAN()` with first-pass optional start-occurrence and replacement-count arguments, `SUBSTR()`, `ALLTRIM()`, `STREXTRACT()` with flag-aware delimiter handling, `STR()` with width/decimal formatting, `TRANSFORM()` numeric/picture formatting with first-pass `SET POINT`/`SET SEPARATOR`/`SET CURRENCY` support plus `@Z` blank-zero and `@B` trimmed-left-justified behavior, `PADL()` / `PADR()` / `PADC()` padding and overflow truncation, `GETWORDCOUNT()` / `GETWORDNUM()` with delimiter-set handling, `CHR()`, type/null helpers including first-pass expression-value `ISBLANK()`, real `.NULL.` literal parsing, and single-byte `ISLEADBYTE()` plus improved `TYPE('<expr>')` parenthesized-expression handling, cursor/schema helpers (`FIELD()` and `FSIZE()` over local DBF-backed and synthetic SQL cursors), numeric helpers (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`, `RAND()`, `RGB()`, and expression-list `MIN()` / `MAX()`), financial helpers (`FV()`, `PV()`, `PAYMENT()`), hex conversion (`HEX()`), runtime-surface helpers (`CAST()`, `BITAND()`, `BITOR()`, `BITXOR()`, `BITNOT()`, `BITCLEAR()`, `BITSET()`, `BITTEST()`, `BITLSHIFT()`, `BITRSHIFT()`, `BINTOC()`, `CTOBIN()`, first-pass host probes `HOME()`, `OS()`, `DISKSPACE()`, and `DRIVETYPE()` with portable backslash-aware relative probe resolution, first-pass `CURSORGETPROP()` / `CURSORSETPROP()`, lock-key probes such as `NUMLOCK()`, first-pass UUID generation `NEWID()`, first-pass code-page helpers `CPCURRENT()` / `CPCONVERT()` / `CPDBF()`, first-pass headless dialog stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()`, first-pass read-context stub `VARREAD()`, and context/introspection helpers `PCOUNT()`, `GETENV()`, and first-pass `PUTENV(<name>, <value>)`), date/time helpers (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, constructor-argument forms of `DATE()` / `DATETIME()`, `STOD()`, `TTOS()`, `CTOT()` including compact sortable datetime input, `DTOC(..., 1)`, `TTOC(..., 1)`, `DTOT()`, `TTOD()` including compact sortable datetime input, `HOUR()`, `MINUTE()`, `SEC()`, first-pass `SET DATE`/`SET CENTURY`/`SET MARK` formatting and parsing for common MDY/DMY/YMD orders and date separators, first-pass `SET HOURS`/`SET SECONDS` display formatting for time and datetime output, installed-VFP9-compatible Date/DateTime `+`/`-` arithmetic with numeric day/second offsets, same-flavor elapsed-unit subtraction, commutative addition, preserved D/T flavor, and localized invalid/mixed-operand faults, first-pass `WEEK()` with optional first-day and first-week mode arguments including mode 2/3 cross-year rollover plus `SET FDOW`/`SET FWEEK` defaults, `QUARTER()`, `EOMONTH()`, `DTOJ()` / `TTOJ()` / `JTOD()` / `JTOT()` with first-pass invalid-input guards), portable string-parsed path helpers (`FULLPATH()` with portable Windows/UNC/POSIX absolute preservation plus backslash-aware relative resolution, `JUSTPATH()`, `JUSTFNAME()`, `JUSTSTEM()`, `JUSTEXT()`, `JUSTDRIVE()`, `FORCEEXT()`, `FORCEPATH()`, `DEFAULTEXT()`, and first-pass `CURDIR()`), file metadata helpers (`FILE()` and `FILESIZE()` with `SET PATH` search handling plus portable backslash-aware relative probes), first-pass low-level file I/O helpers (`FOPEN`, `FCLOSE`, `FREAD`, `FWRITE`, `FGETS`, `FPUTS`, `FSEEK`, `FTELL`, `FEOF`, `FFLUSH`, `FCHSIZE`, `FILETOSTR`, `STRTOFILE` — handle table, portable relative path resolution, binary/text modes, POSIX/Win32 truncation), `SYS()` including first-pass deterministic `SYS(3)`, `SYS(7)`, `SYS(11)`, `SYS(13)`, `SYS(2003)`, `SYS(2004)`, `SYS(2020)`, and `SYS(2023)`, `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `VERSION()`, `ON()`, `SET()`, and `MESSAGEBOX()`, first-pass `TEXTMERGE()` with configurable `<<>>`-style delimiters and recursive evaluation support that now continues until the merged text stabilizes, first-pass `EXECSCRIPT()` RETURN-expression pass-through, first-pass runtime-surface XML cursor bridge helpers `CURSORTOXML()` / `XMLTOCURSOR()` with portable backslash-aware file targets, and first-pass `LOOKUP()` seek-and-evaluate over local alias/tag indexes plus synthetic SQL/result cursors through temporary field-order probes, including variable-driven alias/tag designators

That SQL slice now includes session-scoped synthetic cursor materialization that follows the current selected work-area flow for each data session when `SQLEXEC()` auto-opens a result cursor, plus first-pass connection-state helpers around that same per-session handle model: `SQLROWCOUNT()`, `SQLPREPARE()`, `SQLGETPROP()` / `SQLSETPROP()`, `SQLCANCEL()` / `SQLCOMMIT()` / `SQLROLLBACK()`, and first-pass catalog-style metadata helpers `SQLDATABASES()` / `SQLPRIMARYKEYS()` / `SQLFOREIGNKEYS()` / `SQLTABLES()` / `SQLCOLUMNS()`. The current connection-property pass now exposes deterministic `Connected`, `ConnectHandle`, `ConnectString`, `ConnectName`, `CurrentCatalog`, `LastCursorAlias`, `LastResultCount`, `Asynchronous`, `BatchMode`, `DispWarnings`, `DispLogin`, `Transactions`, `WaitTime`, and `PacketSize` behavior over the existing session-local handle state, while the metadata-cursor pass exposes deterministic synthetic database, table, primary-key, foreign-key, and column catalogs for top-level catalog enumeration plus `TABLE` / `VIEW` / `SYSTEM TABLE` probing, FoxPro-style and native-style `SQLCOLUMNS()` layouts, empty metadata cursors that still preserve schema, and strict cross-session handle rejection for the connection-state helpers. The shipped remote-cursor layer lets synthetic SQL rows participate in field lookup, targeted command-path filtering (`SET FILTER ... IN`) composed with targeted command-path navigation (`GO ... IN`, `SKIP ... IN`, `LOCATE ... IN`), command-path `SCAN ... IN` targeted iteration, aggregate built-ins, command-level `TOTAL` output aggregation, `CALCULATE`, in-memory `REPLACE` / `APPEND BLANK` / `DELETE` / `RECALL` mutation, non-selected-target `REPLACE ... IN` / `DELETE FOR ... IN` / `RECALL FOR ... IN` command-path behavior, `APPEND BLANK IN <alias|work area>` targeted append behavior with selected-alias/pointer isolation coverage, one-off `SEEK()` / `INDEXSEEK()` probes that use temporary order expressions over the in-memory result rows, first-pass normalization-aware temporary SQL order expressions such as `UPPER(NAME)` including combined `DESCENDING` suffix probes, and command-path `SET ORDER TO <expr>` / `SEEK` behavior over those same synthetic rows including non-selected-target `SET ORDER ... IN ... [ASCENDING|DESCENDING]` / `SEEK ... IN` parity, targeted backward/boundary navigation coverage (`GO BOTTOM IN`, `SKIP -1 IN`, targeted `BOF()` / `EOF()`), and post-mutation seeks against appended in-memory rows.

The current session/runtime-state slice also now keeps `SET DEFAULT TO` data-session-local, so `SET('DEFAULT')` and relative path resolution restore correctly after `SET DATASESSION` switches instead of leaking one session's default directory into another, and focused regression coverage now locks down the local-table counterpart to the shipped SQL selection-flow behavior so `SELECT 0`, `USE IN <selected-alias>`, and plain `USE` keep reusing each session's selected empty work area after session switches. `SET('FIELDS')` field-visibility readback and lookup restoration are now explicitly defended on that same data-session boundary too, so per-session `SET FIELDS TO ...` state does not leak across `SET DATASESSION` switches. Boolean runtime `SET` options used by search/filter semantics (`NEAR`, `EXACT`, `DELETED`) and runtime flags (`STRICTDATE`, `ENGINEBEHAVIOR`, `OPTIMIZE`, `TALK`, `SAFETY`, `ESCAPE`, `CENTURY`, `SECONDS`) now also normalize `SET ... TO` forms and logical token variants (`.T./.F.`, `1/0`, `true/false`, `yes/no`) to stable runtime state and `SET()` readback values across data-session switches, while `SET DATE TO`, `SET MARK TO`, first-pass `SET HOURS TO`, and first-pass `SET FDOW`/`SET FWEEK` state now feed the date/time helper parse/format path for common MDY/DMY/YMD orders, separators, time display, and week-number defaults.

`ON ERROR` now also has a richer first-pass compatibility lane: `ON ERROR DO <routine> WITH ...` can pass evaluated handler arguments, and handlers can inspect first-pass runtime fault metadata through `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `AERROR()`, `SYS(2018)`, and `ON('ERROR')` while execution resumes after the handler returns. Normal `AERROR()` rows now preserve the original faulting work area even if the handler changes selection before inspecting the array, handler execution now keeps those fault metadata helpers bound to the original fault even if the handler triggers and catches a secondary runtime error, and thrown-expression faults such as `LOG(-1)` / `ACOS(2)` now have explicit pause-state and repeated-fault coverage so line/statement/message metadata refresh correctly across multiple continues. Static PRG diagnostics are also now wired through the Studio document-open path so analyzer findings surface to editor-facing consumers instead of staying runtime-only.

`ON SHUTDOWN` now has a first-pass compatibility lane as well. `QUIT` checks for an active shutdown handler and, if configured, executes `ON SHUTDOWN DO <routine> [WITH ...]` before the final quit cleanup/unwind. Common inline compatibility forms are also recognized, including `ON SHUTDOWN CLEAR EVENTS` and close-style cleanup commands such as `CLOSE ALL`, `CLOSE TABLES`, and `CLOSE DATABASE(S) [ALL]`. `ON('SHUTDOWN')` now reports the active shutdown clause for runtime introspection. Shutdown handlers can still perform explicit cleanup such as `CLEAR EVENTS`, `CLOSE DATABASES ALL`, and even a nested `QUIT`; Copperfin treats that nested quit as the final terminal quit instead of recursively dispatching shutdown again.

The current PRG-engine slice now also supports first-pass `WITH/ENDWITH` member binding for leading-dot object access and first-pass `TRY/CATCH/FINALLY/ENDTRY` control flow, plus first-pass `DO ... WITH @var` reference semantics so `LPARAMETERS` assignments can write back into caller variables where needed for FoxPro-style procedure behavior. Error-pause stack metadata now also uses the actual faulting line for the top frame when nested routines fault instead of the post-incremented next statement. Inline `&&` comments now stay out of nested double-quoted and braced text during logical-line parsing, so macro-heavy expression text is less likely to be truncated before execution.

## Immediate Runtime Backlog Derived From The Official Reference

The official command inventory is much larger than the current runtime. The deepest next command groups should stay focused on data/runtime compatibility first:

### Active Issue Mapping For This Runtime Backlog

- command-surface and expression/runtime-state depth: #7, #8
- OLE/COM and automation compatibility: #10, #11
- runtime fault containment and diagnostics beyond the now-closed automation lane: #13, #14
- shared design-model and memo-heavy designer asset preservation: #22
- report/label designer fidelity and page/layout metadata editing: #24
- document/vector and AI planning policy: #30, #31

### Runtime Arrays And Expressions

- keep array compatibility work focused on official helper semantics and real FoxPro migration idioms; the current surface covers declared arrays, table/array transfer commands, metadata helpers, mutation helpers, sorting, scanning, and common row/column copy workflows, but it is still a first-pass runtime model rather than a complete VFP array engine
- `ASCAN()` macro-expanded predicate fidelity and metadata isolation are now closed: variable-reference predicates (`ASCAN(arr, cVarPred, ...)`, first-hop macro-expanded predicates (`ASCAN(arr, &cMacroPred, ...)`), and second-hop holder-chain predicates (`ASCAN(arr, &cPredDeepHolder, ...)`) all resolve to the correct block expression, and all four `_ASCAN*` metadata variables (`_ASCANVALUE`, `_ASCANINDEX`, `_ASCANROW`, `_ASCANCOLUMN`) are fully restored to their pre-scan state on every exit path including match, no-match, and early-return; focused regression coverage proves each of these paths in `test_ascan_macro_expanded_predicate_and_metadata_cleanup` (issue #391)
- continue moving new array regressions into `tests/test_prg_engine_arrays.cpp` / `test_prg_engine_arrays` instead of expanding the legacy `test_prg_engine.cpp` catch-all
- continue moving standalone expression/function regressions into `tests/test_prg_engine_functions.cpp` / `test_prg_engine_functions` instead of expanding the legacy `test_prg_engine.cpp` catch-all
- keep expression-function additions in reference-derived batches rather than one helper at a time; after shipping `FORCEEXT()`, `FORCEPATH()`, `CHRTRANC()`, adjacent date/time batches (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, `CTOT()`, `DTOT()`, `TTOD()`, `HOUR()`, `MINUTE()`, `SEC()`), the numeric trig/log helper batch (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`), the first-pass phonetic string helpers (`SOUNDEX()`, `DIFFERENCE()`), the mixed utility batch (`MIN()` / `MAX()` expression lists, `STRCONV(..., 7/8)`, `ISLEADBYTE()`), and the path/color/random batch (`DEFAULTEXT()`, `CURDIR()`, `RGB()`, `RAND()`), continue with adjacent expression helpers from the reference inventory in similarly sized groups
- keep expression-function additions in reference-derived batches rather than one helper at a time; after shipping `FORCEEXT()`, `FORCEPATH()`, `CHRTRANC()`, adjacent date/time batches (`DOW()`, `CDOW()`, `CMONTH()`, `GOMONTH()`, `SECONDS()`, `MDY()`, `CTOT()`, `DTOT()`, `TTOD()`, `HOUR()`, `MINUTE()`, `SEC()`), the numeric trig/log helper batch (`SIN()`, `COS()`, `TAN()`, `ASIN()`, `ACOS()`, `ATAN()`, `ATN2()`, `DTOR()`, `RTOD()`, `LOG10()`), the first-pass phonetic string helpers (`SOUNDEX()`, `DIFFERENCE()`), the mixed utility batch (`MIN()` / `MAX()` expression lists, `STRCONV(..., 7/8)`, `ISLEADBYTE()`), the path/color/random batch (`DEFAULTEXT()`, `CURDIR()`, `RGB()`, `RAND()`), and the financial/misc batch (`HEX()`, `FV()`, `PV()`, `PAYMENT()`, first-pass `TEXTMERGE()` with configurable delimiters, first-pass `EXECSCRIPT()` RETURN-pattern detection, first-pass `LOOKUP()` seek-and-evaluate, first-pass `NEWID()` UUID v4, `CPCURRENT()` / `CPCONVERT()` / `CPDBF()` code-page stubs, and headless stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()` / `VARREAD()`), continue with adjacent expression helpers from the reference inventory in similarly sized groups
- deepen `ISBLANK()` later when DBF field values preserve pristine blank-state metadata distinct from stored numeric zero or logical false; the current implementation covers expression-level empty/blank character values
- deepen `MLINE()` / `MEMLINES()` later with full compatibility flags and offset semantics beyond the current baseline; the current implementation now supports session-scoped `SET MEMOWIDTH` + `_MLINE`, explicit width overrides, optional tab-width expansion, CR hard breaks by default, optional LF hard-break mode via flag bit `1`, and optional start offsets

### Work Areas, Sessions, And Indexed Data

- finish the remaining alias/work-area edge cases across `SELECT`, `USE`, and nested data sessions after the first strict `USE ... IN <alias>` targeting pass, first-pass non-selected target preservation, plain `USE` current-work-area reuse, freed-work-area reuse plus side-effect-free `SELECT(0)` probing, stronger synthetic SQL cursor/session isolation, per-session SQL handle lifecycle cleanup, and the now-shipped session-local restoration coverage for `SET NEAR` and `SET DEFAULT TO`
- keep closing the remaining expression-driven work-area targeting edges after the shared designator-expression path now covers `SET FILTER ... IN`, `SELECT <expr>`, `USE ... IN <expr>`, and the shipped local `IN`-targeted data/search family (`SET ORDER`, `SEEK`, `LOCATE`, `SCAN`, `GO`, `SKIP`, `REPLACE`, `DELETE`, `RECALL`) for core local-cursor flows
- deepen order/search behavior beyond the current first-pass `SET ORDER TO TAG` / `SEEK` / `FOUND()` plus direction-control slice
- add adjacent data-navigation and data-search commands where VFP developers expect them to work together
- use the command inventory to pull the next search/index/data-session commands in families instead of one at a time

### Local Table Mutation And Query Flow

- continue beyond the new first-pass headless `BROWSE` lane toward the remaining interactive table-editing commands needed for real legacy business applications
- the shared DBF/FPT mutation path now covers shipped local-table write families used by current runtime flows: memo-backed pointer fields (`M`/`G`/`P`), fixed-width numeric/date families (`B`/`I`/`Y`/`T`), first-pass var-length families (`V`/`Q`), constrained `NULL` token mutation semantics, staged write safety for DBF/memo updates (including staged-artifact cleanup), physical `PACK` compaction, first-pass `PACK MEMO` sidecar rewrite/compaction, `ZAP` truncation, SQL-style insert rollback, first-pass `CREATE TABLE`, first-pass structural `ALTER TABLE`, and indexed-table plus unsupported-layout fail-fast guards until real structural/indexed storage fidelity exists
- extend the shipped filtering, browsing, locating, scanning, replacing, appending, SQL-style inserting/deleting, recalling, packing, zapping, first-pass structural `CREATE TABLE` / `ALTER TABLE`, aggregate-built-in, command-level `COUNT`/`SUM`/`AVERAGE` with first-pass scope/`WHILE`/`IN` plus first-pass `TO ARRAY`, first-pass `TOTAL` with `IN` targeting and local `N/F/I/Y` support, and `CALCULATE` families toward broader xBase parity
- deepen table-maintenance parity beyond the first-pass `PACK`/`ZAP`/`PACK MEMO` slice with real cross-process locking behavior, index rebuild/invalidated-order handling, and stronger diagnostics for unsafe structural layouts
- keep runtime semantics ahead of shell/designer work so the same engine can power both Visual Studio and the standalone IDE

### Native Control Flow

- Statement-form `TEXT ... ENDTEXT ... TEXTMERGE` applies one default interpolation pass, preserving delimiter-shaped merged values as literal data. Explicit recursion remains available through `TEXTMERGE(cExpression, .T.)`.
- extend the shipped `DO CASE/CASE/OTHERWISE/ENDCASE`, `DO WHILE/ENDDO`, `WITH/ENDWITH`, `TRY/CATCH/FINALLY`, `LOOP`/`CONTINUE`/`EXIT`, first-pass richer `ON ERROR`, and first-pass literal `TEXT/ENDTEXT` slice into the rest of the FoxPro control-flow surface
- keep tightening expression semantics around stored-expression evaluation, macro substitution, and runtime-state inspection beyond the first-pass `EVAL()` / `SET()` / `&macro` slice
- add the next adjacent control families beyond the shipped engine core as coherent batches instead of one syntax form at a time

### SQL Pass-Through And Remote Cursor Behavior

- add the broader SQL pass-through surface around the existing `SQLCONNECT` / `SQLEXEC` / `SQLDISCONNECT` slice
- bring remote cursor semantics closer to local cursor expectations where VFP does so
- use the general reference and local help corpus when Learn pages list a command but do not capture enough behavioral detail

### Automation And Compatibility Utilities

- keep host stability and debugger fault containment mandatory as the remaining Phase B runtime-safety work, now that the dedicated Phase A automation lane is closed

## How To Use The References

- Use the Learn language-reference index to decide what command/function families exist and to avoid missing major areas.
- Use the Learn general reference for file structures, table/index storage, and other low-level behavior.
- Use the generated local CHM command-topic index and topic manifest to jump from a command or `foxtools` function name to the installed VFP help page that describes its behavior.
- Use installed VFP 9 help and local source trees for product-era behavior and sample patterns.
- Use `FOXHELP.DBF` and its memo file as a fallback source for older semantics when Learn pages are missing, gated, or too shallow.
