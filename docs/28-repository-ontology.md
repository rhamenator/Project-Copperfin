# Repository Ontology

This is a descriptive ontology of Project Copperfin as the repository actually exists today: the concepts/entities present, how they relate, and where aspirational architecture docs (e.g. `docs/02-architecture.md`'s `copperfin-*` module list) diverge from what is actually built (the `cf_*` CMake targets below). Generated by inspecting `CMakeLists.txt`, `src/`, `include/`, `apps/`, `vsix/`, and `tests/` directly rather than restating planning docs.

Refreshed 2026-08-12 against `CMakeLists.txt`; the latest base-layer addition is
the portable file-version metadata boundary used by `AGETFILEVERSION()`. Corrected
2026-08-31: `cf_migration` (present in `CMakeLists.txt` since before the 2026-08-12
refresh) was missing from the library table and dependency graph; the test-suite
file/executable counts were also stale and are now re-derived directly from
`find tests -name "*.cpp" | wc -l` and `tests/CMakeLists.txt`'s registered
`add_executable` count rather than restated as an approximation.

## 1. Top-level entity classes

| Class | Instances | Where |
|---|---|---|
| Native static libraries | `cf_platform_support`, `cf_localization`, `cf_security`, `cf_licensing`, `cf_package_trust`, `cf_platform_profile`, `cf_sqlite_connector`, `cf_vfp_assets`, `cf_migration`, `cf_mcp_host`, `cf_design_model`, `cf_prg_analysis`, `cf_runtime_text`, `cf_xbase_runtime`, `cf_runtime_pipeline` | `CMakeLists.txt`, `src/*`, `include/copperfin/*` |
| Native executables ("hosts") | `copperfin_inspect`, `copperfin_mcp_host`, `copperfin_studio_host`, `copperfin_runtime_host`, `copperfin_build_host`, `copperfin_launcher_guard` (Windows-only) | `apps/*/main.cpp` |
| Managed (.NET) projects | `Copperfin.VisualStudio` (VSIX), `Copperfin.Studio` (standalone shell), `Copperfin.DesignerSmokeTests`, `Copperfin.LanguageServiceTests`, `Copperfin.PolyglotCandidate` (Native AOT external leaf sample) | `vsix/*`, `samples/polyglot-dotnet-candidate/*` |
| VFP-era asset formats (domain data) | `DBF/FPT`, `CDX/DCX/IDX/MDX`, `DBC`, `PJX/PJT`, `SCX/SCT`, `VCX/VCT`, `FRX/FRT`, `LBX/LBT`, `MNX/MNT`, `PRG/H/QPR/MPR/SPR` | parsed/edited across `cf_vfp_assets`, `cf_design_model`, `cf_xbase_runtime` |
| Test suites | 820 `.cpp` files under `tests/`, 227 registered CTest executables via `tests/CMakeLists.txt` | `tests/*` |
| Governance/process docs | `agents.md`, `agent-handoff.md`, `docs/01`…`docs/58`, `docs/safety/hazard-register.md` | repo root, `docs/` |

## 2. Native library dependency graph (ground truth, from `CMakeLists.txt`)

```
cf_platform_support                    (base: environment/path/executable-path/file-version helpers, no internal deps)
 └─ cf_localization                    (localization.cpp; PUBLIC-links cf_platform_support)
      ├─ cf_security                   (audit, auth, sha256, process hardening, secrets)
      ├─ cf_platform_profile           (database_model, extensibility_model, federation_execution, query_translator)
      ├─ cf_vfp_assets                 (DBF/CDX header+table readers, visual_asset_editor_*)
      └─ cf_runtime_text               (runtime-scoped localized text)
           └─ cf_prg_analysis          (prg_engine_static_analysis.cpp)

cf_licensing                            (base64, canonical payload serializer, ed25519 verify,
                                          license classifier/parser/status, vendored ed25519_ref)
                                         PRIVATE-links cf_platform_support for license-status
                                         environment access; no cf_localization link.
 └─ cf_package_trust                    (launcher_inventory_trust.cpp — PRIVATE-links cf_licensing;
                                          reuses the verify-only crypto primitive but owns its own
                                          envelope/key-registry/verification API; must not consume
                                          license parsing or license-status decisions)

cf_sqlite_connector = cf_platform_profile(public) + cf_security(private)
                      (read-only SQLite execution; raw Windows/POSIX SQLite ABI
                       selection remains private under src/platform)

cf_migration                            (base: src/migration/project_inventory.cpp, no internal deps;
                                          read-only project-inventory foundation for docs/03's migration-tool
                                          output contract -- only inventory.json is implemented so far)

cf_design_model  = cf_vfp_assets + cf_prg_analysis + cf_localization
                   (src/studio/*: designer_*, builder_*, toolbox_*, vs_launch_contract_*, report_layout, project_workspace, document_model)

cf_xbase_runtime = cf_prg_analysis + cf_runtime_text + cf_design_model(public) + cf_localization(private)
                   (src/runtime/prg_engine*.cpp + .inl, index_seek_optimizer, xasset_methods)

cf_runtime_pipeline = cf_design_model + cf_security + cf_platform_profile + cf_xbase_runtime + cf_licensing
                   (src/runtime/runtime_pipeline_*.cpp — packaging, manifests, C#/launcher emission)

cf_mcp_host = cf_platform_support + cf_vfp_assets
              (strict dual-era MCP messages + read-only DBF-header tool)
```

Executables consume this graph at the top:

```
copperfin_inspect        -> cf_vfp_assets, cf_security, cf_licensing, cf_localization
copperfin_mcp_host       -> cf_mcp_host, cf_security
copperfin_studio_host    -> cf_design_model, cf_security, cf_platform_profile, cf_licensing
copperfin_runtime_host   -> cf_xbase_runtime, cf_security, cf_platform_profile, cf_sqlite_connector, cf_licensing, cf_localization
copperfin_build_host     -> cf_runtime_pipeline, cf_localization
copperfin_launcher_guard -> cf_security, cf_localization, cf_platform_support, cf_package_trust   (Windows-only)
```

`copperfin_build_host` has a build-order dependency on `copperfin_launcher_guard`
(`add_dependencies`) — the build pipeline stages the compiled guard executable
into packages it produces, it does not link against it.

Note the inversion vs. the planning doc: `cf_design_model` (the "designer" concern) is a *dependency of* `cf_xbase_runtime` (the runtime engine), not the other way around — the runtime needs design-model types (e.g. extracted xAsset methods) to execute `SCX/VCX/MNX/FRX/LBX` startup assets.

Also note the root of the graph changed since this document's first pass:
`cf_localization` is no longer dependency-free — it now PUBLIC-links
`cf_platform_support`, so every library that transitively depends on
`cf_localization` (nearly everything) also picks up `cf_platform_support`.

## 3. Domain ontology — what each library models

### `cf_platform_support` (new base layer)

- `executable_path.cpp` — exposes a portable optional executable-search default
  and resolves the running executable's own path in a
  platform-portable way.
- `environment.cpp` and `path.cpp` — keep operating-system selection, native
  environment calls, synchronization, and path conversion/comparison behind
  platform-neutral public declarations. This is the lowest-level dependency
  in the native graph; `cf_localization` sits directly on top of it.
- `file_version.cpp` — owns Windows version-resource APIs and the portable PE
  resource-string fallback behind a standard-C++ metadata record consumed by
  the runtime's VFP-compatible `AGETFILEVERSION()` array contract.

### `cf_localization` / `cf_security` / `cf_platform_profile` (cross-cutting infrastructure)

- **Localization**: catalog-driven string lookup (`resources/locales/{en-US,es-419,pt-BR,qps-ploc}/strings.json`); each higher subsystem has its own `localized_text.{h,cpp}` shim over this base.
- **Security**: `authorization`, `audit_stream`, `external_process_policy`, `physical_path_containment`, `process_hardening`, `secret_provider`, `security_model`, `sha256` — an RBAC/audit/sandboxing layer (`docs/18-native-security-and-rbac.md`), independent of any one host. On Windows it links `bcrypt`, `crypt32`, `wintrust`, and `version`.
- **Platform profile**: `database_model`, `query_translator`, `federation_execution`, `extensibility_model` — the "database federation" concept (`docs/21-database-federation-and-query-translation.md`): deterministic relational query translation plus AI-assisted document/vector planning metadata, surfaced into both Studio and the runtime host.
- **SQLite connector**: bounded read-only execution of deterministic SQLite plans. Its public Copperfin header exposes only platform-neutral result/request types; the system SQLite/WinSQLite C ABI selection is a private implementation header.

### `cf_licensing` / `cf_package_trust` (new — crypto and package-trust layer)

- **Licensing**: `base64`, `canonical_payload_serializer`, `ed25519_verify`, `license_classifier`, `license_payload_parser`, `license_status`, plus a vendored `third_party/ed25519_ref` reference implementation (built with relaxed warning flags so the upstream source doesn't need local edits). Intentionally has no `cf_localization` dependency so it stays trivially unit-testable offline.
- **Package trust**: `launcher_inventory_trust.cpp` — implements the signed-envelope contract in `docs/29-package-trust-contract.md` (`app.cftrust` + `app.cftrust.sig`). Reuses `cf_licensing`'s verify-only Ed25519 primitive but owns a distinct envelope format, key registry, and verification API; must not consume license parsing or license-status decisions — the two concerns are deliberately kept separate per source comments in `CMakeLists.txt`.

### `cf_vfp_assets` (file-format fidelity + visual asset editing)

- **Binary readers**: `dbf_header`, `dbf_table`, `cdx_header`, `index_probe`, `asset_inspector` — the DBF/FPT/CDX/index fidelity layer (Phase A, closed per `docs/02`).
- **Visual asset editor**: `visual_asset_editor_*` files — a round-trip-safe editor for `SCX/VCX/FRX/LBX/MNX` object models: property blobs, method blobs, object lifecycle, undo, and typed property setters split by concern (`appearance`, `behavior`, `data`). This is the engine behind safe in-place edits to legacy VFP visual assets without lossy rewrites.

### `cf_mcp_host` (local read-only tool boundary)

- `mcp_host.cpp` implements current stateless and initialization-era MCP
  request handling over strict bounded JSON. Its single tool accepts exactly
  32 caller-supplied DBF-header bytes and reuses `cf_vfp_assets` parsing without
  adding caller-selected file, network, model-provider, or mutable-runtime
  access.

### `cf_design_model` (designer/studio object model — *not* UI)

- **Document/project model**: `document_model`, `project_workspace`, `product_subsystems` (the "list VFP9-equivalent subsystems" feature), `report_layout` (FRX/LBX section model).
- **Designer surfaces**: `designer_context`, `designer_dispatch`, `designer_launch_surfaces`, `designer_invocation_admission` — admission-checked dispatch of designer actions.
- **Builders/Toolbox**: `builder_registry`, `builder_dispatch`, `builder_invocation_admission`, `toolbox_palette`, `toolbox_creation`, `toolbox_dispatch`, `toolbox_invocation_admission` — modeled equivalents of VFP's Builders and Toolbox.
- **Editor actions**: `editor_action_dispatch`, `context_editor_actions`, `editor_action_invocation_admission`.
- **VS launch contract**: `vs_launch_contract*` (largest single cluster, ~30 files) — the JSON request/response protocol consumed by `Copperfin.VisualStudio` and `Copperfin.Studio` to drive the native designer surfaces (selection, property get/set by appearance/behavior/data, object lifecycle, layout actions), each paired with a `*_validate.cpp`.

### `cf_xbase_runtime` (the PRG execution engine)

- `prg_engine.cpp` + a large family of `.inl` partials (`_dispatch`, `_flow`, `_expression`, `_records`, `_cursor`, `_arrays`, `_variables`, `_session`, `_sql`, `_aggregate`, `_dll`, `_free_functions`, `_index_seek`) — a single logical engine split across translation units (see `scripts/split_prg_engine.py`, evidence this was mechanically decomposed from one large file).
- Function-family modules: `_command_helpers`, `_date_time_functions`, `_file_io_functions`, `_numeric_functions`, `_path_functions`, `_string_functions`, `_type_functions`, `_table_structure_helpers`, `_runtime_surface_functions`, `_runtime_config`, `_locale_code_page` — VFP built-in function coverage, organized to mirror `docs/22-vfp-language-reference-coverage.md`.
- `index_seek_optimizer.cpp` — Rushmore-style seek/optimization logic (see `tests/test_prg_engine_rushmore_optimization.cpp`).
- `xasset_methods.cpp` — extracts dispatchable runtime actions (method invocations, menu selections) from `SCX/VCX/FRX/LBX/MNX` objects, the bridge that lets `cf_design_model` assets actually run.
- `native_declared_library.h` is the portable native `DECLARE` loader/lifetime result contract; `native_declared_library.cpp` privately owns Windows module search, export resolution, PE classification, system-error formatting, and module release. `native_declared_call.h` is the portable typed invocation/result contract; `native_declared_call.cpp` and its private `win64_native_call.cpp` helper own Windows ABI storage, pointer formation, calling conventions, dispatch, return extraction, and by-reference copying.
- On Windows, additionally: `managed_declared_call.cpp`, `managed_pe_image.cpp`, `native_declared_library.cpp`, `native_declared_call.cpp`, and `win64_native_call.cpp` (links `oleaut32`) — the bounded native and .NET Framework `DECLARE` paths noted in `docs/24-system-uml.md`'s reading notes. `managed_declared_call.h` is a portable internal scalar contract; CLR/COM types, marshaling, and the `mscoree` linker directive remain in `managed_declared_call.cpp`. The superseded late-bound `IDispatch`/`EXCEPINFO` owner has been removed rather than retained as dormant interpreter infrastructure.
- `prg_engine_static_analysis.cpp` (in the separate `cf_prg_analysis` target) — analysis that doesn't need the full runtime, kept as a lighter dependency for `cf_design_model`.

### `cf_runtime_pipeline` (build/package/debug orchestration)

- `runtime_pipeline_file_io_and_classification`, `_text_and_identifier_utils` — staging/classifying project assets.
- `_ast_ir_manifest`, `_fxp_and_archive_manifest`, `_library_export_manifest` — manifest generation for packaged output.
- `_csharp_and_launcher`, `_launcher_artifact_inventory` — generates the `.NET` launcher stub and its signed-inventory metadata (`docs/29-package-trust-contract.md`).
- `_fd_file_io`, `_package_content_io`, `_manifest_pair`, `_manifest_pair_io` — package content and manifest-pair I/O.
- `_public_api` — the surface `copperfin_build_host` calls into.
- Links `cf_licensing` directly (in addition to the design/security/platform/runtime libraries) to consume the launcher-artifact trust primitives.

## 4. Executable roles (the native "hosts")

| Executable | Role | Key flags/behavior |
|---|---|---|
| `copperfin_inspect` | Low-level DBF/index inspector, no GUI | reads headers/records for diagnostics |
| `copperfin_mcp_host` | Local provider-independent MCP stdio host | one bounded read-only DBF-header tool; exits on stdin EOF |
| `copperfin_studio_host` | Native backend for visual designers; emits JSON snapshots | `--json`, `--from-vs`, `--list-subsystems`; produces `reportLayout` for FRX/LBX |
| `copperfin_build_host` | Native packaging/build pipeline for `PJX/PJT` | `build --project ... --output-dir ... --enable-security --emit-dotnet-launcher`; writes `app.cfmanifest`; depends on `copperfin_launcher_guard` being built first so it can stage it |
| `copperfin_runtime_host` | Runtime + debugger host for packaged apps | reads `app.cfmanifest`, executes `PRG` startup via `cf_xbase_runtime`, supports breakpoints/step-debug, `--debug-command select:<id>` / `invoke:<id>` |
| `copperfin_launcher_guard` (Windows-only) | Pre-launch trust guard staged alongside generated launchers | verifies `app.cftrust`/`app.cftrust.sig` sidecars via `cf_package_trust` before a managed apphost process starts; fail-closed when `COPPERFIN_ENFORCE_LAUNCHER_TRUST` is set, per `docs/29-package-trust-contract.md` |

These map onto pipeline stages: `studio_host` (design-time) → `build_host`
(package, staging `launcher_guard`) → `runtime_host` (execute/debug), with
`inspect` and `mcp_host` as standalone read-only diagnostic tools outside that
pipeline and `launcher_guard` as a Windows-only trust checkpoint that runs
before a packaged app's managed launcher.

## 5. Managed/.NET layer (`vsix/`)

- `Copperfin.VisualStudio` — VSIX package; hosts a "Copperfin Visual Designer" document shell for `PJX/SCX/VCX/FRX/LBX/MNX`, talks to `copperfin_studio_host`/`copperfin_runtime_host` over the JSON contract (`CopperfinStudioHostBridge`, `CopperfinRuntimeDebugClient`, `CopperfinProjectInsightClient`), plus a first-pass FoxPro language service (`FoxProCompletionSource`, `FoxProQuickInfoSource`, `FoxProSignatureHelpSource`) for `PRG/H/QPR/MPR/SPR`.
- `Copperfin.Studio` — standalone WinForms shell (`StudioMainForm`) reusing the same designer controls outside Visual Studio, now tabbed/multi-document.
- `Copperfin.DesignerSmokeTests` — WinForms smoke tests exercising real sample assets (`invoice.frx`, `cust.lbx`, `solution.pjx`) against the shared designer controls.
- `Copperfin.LanguageServiceTests` — tests for the FoxPro editor/language-service pieces.
- `samples/polyglot-dotnet-candidate/Copperfin.PolyglotCandidate` — a separate
  self-contained Native AOT C# leaf executable admitted by exact hash and
  invoked through the generic polyglot host. It is not linked into the VSIX,
  native libraries, or a general CLR host.

Design rule stated in `docs/02-architecture.md`: the VS extension should *orchestrate* designers, not be their only implementation — consistent with the native `cf_design_model` + JSON contract split actually built.

## 6. Planned-but-not-yet-real layer (from `docs/02-architecture.md`)

The architecture doc names a `copperfin-*` module list (`copperfin-core`, `-data`, `-connectors`, `-runtime`, `-designer`, `-reports`, `-migrator`, `-dotnet`, `-gateway`, `-shield`, `-cli`, `-vsix`) and suggested submodules (`cf-dbf`, `cf-fpt`, `cf-cdx`, `cf-dbc`, `cf-repair`, `cf-sqlite`, `cf-postgres`, `cf-sqlserver`, `cf-oracle`, `cf_clr-host`, `cf-com-bridge`, etc.). Those names remain a target-state taxonomy, not current structure. The real `cf_*` targets in §2 are a flatter, more organically grown set that partially covers this taxonomy (mostly `-data`, `-designer`, `-runtime`, and slices of `-dotnet`/`-connectors` via `cf_platform_profile`, and slices of `-shield` via `cf_security`/`cf_licensing`/`cf_package_trust`). `cf_migration` is now a deliberately narrow read-only inventory foundation; it is not yet the planned `copperfin-migrator` module. `-gateway` and `-cli` still have no dedicated code.

`copperfin-vsix` is the one entry on that aspirational list that is stale in
the *other* direction — see `docs/24-system-uml.md`'s staleness correction:
`vsix/Copperfin.VisualStudio` and `vsix/Copperfin.Studio` are real, shipping
managed projects today, not a target.

## 7. Process/governance ontology (non-code but load-bearing)

- **Issue hierarchy**: umbrella issues → parent/lane issues → prompt-sized child issues (only the last are execution units) — `agents.md`.
- **Execution status**: the ontology describes repository structure, not the active work queue. Current MVP workstreams and their completion gates live in `docs/05-roadmap.md`; issue-linked evidence lives in `agent-handoff.md` and progress documents.
- **Requirements Recovery**: a continuous, load-bearing DO-178C-inspired
  development-assurance practice adapted to this general-purpose platform (not
  a certification or compliance claim). Bidirectional requirement ↔
  architecture/code ↔ test/result links are grounded only in real installed
  VFP9 behavior, shipped Microsoft/VFP documentation, explicit owner product
  policy, identified parent requirements/hazards, or registered known-bug/crash
  exceptions—never existing Copperfin code or decompiled binaries. The durable
  matrix is `docs/32-recovered-requirements-traceability.md`.
- **Safety documentation traceability**: `DQ-*`/`DV-*`/`HZ-*` identifiers link doc changes to `docs/safety/hazard-register.md` when operator-facing/procedural docs change.
- **Handoff contract**: `agent-handoff.md` (compact, current-state) vs. `remaining-work.md` (deprecated, do not use as a planning source) vs. `issues.txt` (local snapshot only, never authoritative over live GitHub issue state).

## 8. Cross-cutting relationships worth noting

- Every native library now depends on `cf_localization` (and therefore
  transitively on `cf_platform_support`) except `cf_licensing`,
  `cf_package_trust`, and `cf_platform_support` itself. `cf_licensing`
  privately links `cf_platform_support` for environment access but remains
  outside the localization catalog.
- `cf_security` and `cf_platform_profile` are siblings consumed identically by both `copperfin_studio_host` and `copperfin_runtime_host`, but `copperfin_studio_host` does *not* link `cf_xbase_runtime` directly — design-time JSON snapshot generation doesn't need the execution engine, only `cf_design_model`.
- `cf_package_trust` and `cf_licensing` are siblings with a deliberately narrow relationship: package trust reuses only the licensing library's verify-only crypto primitive, never its parsing or status-decision logic, and neither one links `cf_localization` — they sit outside the localization-catalog requirement that governs the rest of the graph.
- The VFP asset type taxonomy (§1) is the spine the whole ontology hangs off: each format has a reader in `cf_vfp_assets`, an editable object model in `cf_design_model`, and (for `SCX/VCX/MNX/FRX/LBX/PRG`) an executable surface in `cf_xbase_runtime`.
