# Agent Handoff

Canonical Copperfin agent handoff brief.

This file should keep the repo pointed at the dependency graph instead of wandering into nearby but low-value slices. Treat `remaining-work.md`, `docs/22-vfp-language-reference-coverage.md`, and `docs/23-phase-a-dependency-breakdown.md` as the source of truth for what matters next.

Resume Project-Copperfin from the current repo state and continue with the highest-value unfinished implementation slice on the critical path. Do not re-plan the whole project unless the dependency graph or issue set has materially changed.

Important:

- Start by re-reading:
  - `remaining-work.md`
  - `docs/22-vfp-language-reference-coverage.md`
  - `docs/23-phase-a-dependency-breakdown.md`
  - `agents.md`
  - `docs/safety/triage-rubric.md` (feedback triage rubric)
  - `docs/safety/test-coverage-gap-analysis.md` (edge case gap analysis; consult when adding tests)
  - the runtime/data files directly relevant to the slice you choose
- Stay implementation-first. Do not stop at analysis.
- Prioritize the current active queue in `docs/23-phase-a-dependency-breakdown.md`; the Phase A runtime/data-engine gate is closed.
- Keep changes minimal and focused.
- Fix mistakes you encounter in your changed area.
- Treat the issue tracker at slice granularity: before coding, choose one open prompt-sized slice issue or create one under the active lane issue if none fits.
- Validate the slice before stopping.
- Update `remaining-work.md`, `docs/22-vfp-language-reference-coverage.md`, and this file to match shipped behavior.
- Do not choose work by local adjacency alone. Prefer blocker slices with the highest downstream fan-out.
- If release preparation is in scope, run `scripts/validate-safety-traceability.ps1` (or the `Safety Traceability Gate` workflow) against the release issue list before tagging and retain the generated report.

Current priority order:

Current E3 update: latest implemented slice is `#1678`, proving deleted title FRX/LBX sections can be selected by stable unique id without a record selector while selected-section availability/kind, deleted title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity are preserved. `#1677` is the immediately preceding stable-selector column-footer section selection slice, and `#1546` was closed not planned after validation showed direct-over-duplicate-EXPR summary precedence would regress existing memo-edit behavior. Parent `#24` remains open; create or pick the next prompt-sized E3 child before more report/label implementation work unless the live issue tree shows a higher-weight blocker.

E2 evidence-audit update: `#1078`-`#1086`, `#1087`-`#1101`, `#1102`-`#1202`, `#1203`-`#1447` were closed through 2026-06-21 with issue-specific evidence after focused validation passed for `test_studio_host`, `test_studio_host_json`, `test_context_editor_actions`, `test_designer_context`, `test_toolbox_palette`, `test_toolbox_creation`, `test_builder_registry`, and `test_visual_asset_editor`. Parent `#23` remains open; continue closing completed children only with specific evidence.

Snapshot alignment update: `issues.txt` now matches the 41-row live open GitHub export as of 2026-06-21. Direct checks confirmed stale local rows `#7`, `#8`, `#13`-`#22`, `#38`-`#43`, `#92`-`#94`, `#97`-`#101`, `#150`-`#193`, and `#200`-`#203` are closed, while omitted rows `#274`, `#277`, `#279`, and `#280` are open.

CI update: latest pre-`#1678` polled pushed head `8fa43484` records `#1677` stable-selector column-footer section selection coverage. At last poll, Native CMake Validation, Build Standalone Installers, Build Visual Studio VSIX, and Security Supply Chain Gate were all queued for that head; no new completed failure was visible in the latest `main` run list. Repoll after the next push before drawing hosted-CI conclusions.

CI update: latest polled pushed head `3c999eb6` records closed `#1300`-`#1303`; `#1304` was already closed live, making `#1203`-`#1304` contiguous. This handoff also records closed `#1305`, `#1307`, `#1309`, and `#1311`; together with already-closed `#1306`, `#1308`, `#1310`, `#1312`, and `#1313`, the `#1203`-`#1313` evidence-audit range is now contiguous. At last poll, `3c999eb6` Security Supply Chain Gate, Build Standalone Installers, Build Visual Studio VSIX, and Native CMake Validation were still queued. The prior `2049dfb5` Security Supply Chain Gate failed during SBOM generation because `anchore/sbom-action` received HTTP 500 while downloading Syft `v1.42.3` from GitHub releases; the Trivy vulnerability gate did not run. Repoll newer runs before making final CI conclusions.

CI update: latest polled pushed head `bc8109a8` records closed `#1098`; Native CMake Validation, Build Standalone Installers, Build Visual Studio VSIX, and Security Supply Chain Gate were still queued for that head. Earlier `53ccf3bf`, `a152182d`, and `587c07c7` Security Supply Chain Gate runs completed successfully while their native/installer/VSIX peers remained queued. Repoll after the next push before making final CI conclusions.

CI update: hosted Native CMake runs through `ba0cc148` repeated `test_toolbox_creation` failures in #1257/#1296/#1255/#1298. Current source now asserts the actual form toolbox palette order in context-wide batch plan/dispatch catalogs: `label` owns the first append target (`before_count`), `textbox` follows label, and `commandbutton` follows textbox. Local targeted rebuild/relink of `test_toolbox_creation` passed; hosted Native CMake remains authoritative after push.

CI update: a native toolbox-creation planner assertion patch is now applied after hosted Linux native logs from an older pre-fix commit showed first-target reservation checks failing. Ordered explicit batch tests still expect their first planned append target to equal `before_count` and subsequent targets to increase, while context-wide catalog tests now follow toolbox palette order. Local validation passed with syntax-only compilation, the test object target, and `git diff --check`; hosted Native CMake Validation remains authoritative.

CI update: a native macOS toolbox-creation fixture-width patch is now applied after hosted `test_toolbox_creation` failed in report-safe create and batch/dispatch create assertions. The synthetic fixture's `UNIQUEID` field is now `C(64)` instead of `C(32)`, matching the GUID-style identifiers those tests write and assert. Local validation is lighter here because a full target rebuild re-enters the known slow `vs_launch_contract.cpp` compile path; hosted Native CMake Validation remains authoritative.

CI update: a native macOS Studio host execution-test portability patch is now applied after hosted `test_studio_host_json` failed on hard-coded `/bin/true` and `/bin/false` launch commands. The test now uses `/usr/bin/true` and `/usr/bin/false` on macOS, keeps existing Linux paths elsewhere, and validates exact JSON launch/executed command fragments against the selected platform path. Local validation passed with `c++ -std=c++20 -Iinclude -fsyntax-only tests/test_studio_host_json.cpp` and `ctest --test-dir build --output-on-failure -R "test_studio_host_json"`. Re-check hosted Native CMake Validation after push; if `test_toolbox_creation` still fails on macOS, triage it separately instead of assuming this portability fix covers it.

CI/buildability note: focused cleanup has been applied after the latest GitHub `main` runs failed in Native CMake Validation, Build Standalone Installers, and Build Visual Studio VSIX. `CMakeLists.txt` now probes `-fzero-init-padding-bits=standard` before applying it to `src/studio/vs_launch_contract.cpp`, VSIX is green on the latest checked head after the `net472` source compatibility fixes, Linux installers are green, and a follow-up installer portability patch now fixes the Windows MSVC `getenv` warning-as-error path plus macOS libc++ `std::from_chars(double)` compile blocker. Subsequent native macOS harness patches fix literal runtime-host path matching, trailing-`TMPDIR` separator normalization, macOS `nm -gU` export inspection with residual underscore filtering, the remaining dotnet compile-check child descriptor issue, and the FLL `_FoxTable` registration-symbol normalization path where Mach-O reports it as `__FoxTable`. Local validation is partial: CMake reconfiguration, `cf_vfp_assets`, GCC/Clang syntax-only checks, `test_runtime_host_binding`, and syntax-only checks for the touched native-test sources passed, but hosted Windows/macOS reruns are still the authoritative validation. After this commit lands, check hosted CI before assuming the remaining red workflows are clear.

Native-test note: `test_runtime_pipeline` has a harness fix for hosted CTest/dotnet capture. The C# transpilation compile-check child now opens its dotnet build log before redirecting stdout/stderr, so a log-open failure no longer leaves dotnet with bad file descriptors. Local focused validation passed with `ctest --test-dir build --output-on-failure -R "test_runtime_pipeline"`.

E2 closure cleanup note: `#1304`, `#1306`, `#1308`, `#1310`, `#1312`, and `#1313` were closed in GitHub on 2026-06-20 after evidence-backed local verification confirmed their shipped `test_toolbox_creation` coverage and repo docs. Parent `#23` remains open; do not orphan or close remaining open children by implication.

Additional E2 closure note: `#1316`, `#1318`, `#1320`, and `#1322` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped toolbox palette launch catalog, builder dispatch execution, editor-action dispatch execution, and toolbox dispatch execution coverage. Parent `#23` remains open; `#1324`, `#1326`, `#1330`, and other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1324`, `#1326`, and `#1330` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped aggregate designer dispatch execution and builder/toolbox dispatch execution catalog coverage. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1314`, `#1315`, `#1317`, `#1319`, `#1321`, and `#1323` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_studio_host_json`, `test_toolbox_creation`, `test_toolbox_palette`, `test_builder_registry`, and `test_context_editor_actions` validation confirmed shipped dispatch-sourced toolbox object creation/batch creation host JSON, toolbox palette launch catalog host JSON, and builder/editor-action/toolbox dispatch execution host JSON. Error management coverage includes parser failures, admission failures without launch, nonzero process reporting, unavailable dispatch items, duplicate identities, stale metadata suppression, and no partial mutation on rejected create paths. Together with already-closed `#1316`, `#1318`, `#1320`, `#1322`, and `#1324`, the `#1203`-`#1324` evidence-audit range is now contiguous. Parent `#23` remains open; `#1325`, `#1327`, `#1328`, `#1329`, `#1331`, and other open E2 children still need live-state review before closure.

Additional E2 closure note: `#1325`, `#1327`, `#1328`, `#1329`, and `#1331` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_studio_host_json`, `test_builder_registry`, `test_context_editor_actions`, `test_toolbox_palette`, and `test_designer_context` validation confirmed shipped aggregate designer dispatch execution host JSON, builder dispatch execution catalog host JSON, editor-action dispatch execution catalog planning/host JSON, and toolbox dispatch execution catalog host JSON. Error management coverage includes parser failures, explicit execution-admission failures, dry-run dispatch readiness failures, no process launch for readiness catalogs, nonzero child execution summaries, stale metadata suppression, and no asset mutation. Together with already-closed `#1326` and `#1330`, the `#1203`-`#1331` evidence-audit range is now contiguous. Parent `#23` remains open; `#1332`, `#1333`, and other open E2 children still need live-state review before closure.

Additional E2 closure note: `#1332` and `#1333` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_designer_context`, and `test_studio_host_json` validation confirmed shipped aggregate designer dispatch execution catalog planning and Studio host JSON exposure. Error management coverage includes explicit aggregate execution-admission failures, dry-run/no-admitted-dispatch failures, dispatch-plan error propagation, invalid execution-admission parser errors, non-mutating readiness state, and no child executor/process/UI launch. The `#1203`-`#1333` evidence-audit range became contiguous; the following note records the subsequent `#1334`-`#1339` closure batch.

Additional E2 closure note: `#1334`-`#1339` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped aggregate designer dispatch/execution catalog argument preservation plus execution child command, identity, dispatch-argument, and state JSON. Error management coverage includes non-mutating dispatch/execution catalog inspection, no child executor/process/UI launch for catalog paths, preserved no-stale failure behavior, explicit child execution state, and failed-child process status reporting. The `#1203`-`#1339` evidence-audit range became contiguous; the following note records the subsequent `#1340`-`#1346` closure batch.

Additional E2 closure note: `#1340`-`#1346` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped failed aggregate designer execution child metadata, failed editor/toolbox metadata coverage, child execution/error/success counts, failed-child summaries, and failed-child error summaries. Error management coverage includes reporting-only fallback metadata on child process failure, category-specific error/success counts, failed-child ids/errors, no stale aggregate success metadata, and preserved successful sibling child state. The `#1203`-`#1346` evidence-audit range became contiguous; the following note records the subsequent `#1347`-`#1355` closure batch.

Additional E2 closure note: `#1347`-`#1355` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped observed process exit-code normalization, aggregate child launch/target/descriptor metadata, toolbox item descriptors, and failed-child command-token/executed-command/exit-code summaries. Error management coverage includes normalized nonzero process exits, preserved child launch metadata on failure, reporting-only child metadata fallback, failed-child token/command/exit summaries, and no execution behavior changes. The `#1203`-`#1355` evidence-audit range became contiguous; the following note records the subsequent `#1356`-`#1360` closure batch.

Additional E2 closure note: `#1356`-`#1360` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped designer dispatch-execution, dispatch, invocation-admission, launch-surface catalog context summaries plus builder launch catalog status summaries. Error management coverage includes ready/blocked context and builder summaries, representative blocked errors for unsupported toolbox and admission failures, dry-run/non-mutating catalog behavior, and no editor/builder/toolbox process or UI execution. The `#1203`-`#1360` evidence-audit range became contiguous; the following note records the subsequent `#1361`-`#1367` closure batch.

Additional E2 closure note: `#1361`-`#1367` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped builder invocation-admission, builder dispatch, builder dispatch-execution, editor-action launch, editor-action invocation-admission, editor-action dispatch, and editor-action dispatch-execution catalog status/readiness summaries. Error management coverage includes ready/blocked ids, representative admission/dispatch/execution blocked errors, dry-run/non-mutating catalog behavior, and no editor/builder/toolbox process or UI execution. The `#1203`-`#1367` evidence-audit range became contiguous; the following note records the subsequent `#1368`-`#1373` closure batch.

Additional E2 closure note: `#1368`-`#1373` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox palette launch catalog, toolbox invocation-admission catalog, toolbox dispatch catalog, toolbox dispatch-execution catalog, selection toolbox dispatch catalog, and selection toolbox invocation-admission catalog status/readiness summaries. Error management coverage includes unsupported toolbox contexts, ready/blocked item summaries, admission/dispatch/execution blocked errors, dry-run/non-mutating catalog behavior, and no toolbox/editor/builder process or UI execution. The `#1203`-`#1373` evidence-audit range became contiguous; the following note records the subsequent `#1374`-`#1378` closure batch.

Additional E2 closure note: `#1374`-`#1378` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox create-plan catalog, selection toolbox create-plan catalog, toolbox create-dispatch catalog, selection toolbox create-dispatch catalog, and toolbox create-batch-plan catalog status summaries. Error management coverage includes parser rejections, non-admitted create-dispatch blockers, stale-argument suppression, aggregate batch-plan errors, dry-run/non-mutating catalog behavior, and no asset mutation. The `#1203`-`#1378` evidence-audit range became contiguous; the following note records the subsequent `#1379`-`#1381` closure batch.

Additional E2 closure note: `#1379`-`#1381` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped selection toolbox create-batch-plan catalog, toolbox create-batch-dispatch catalog, and selection toolbox create-batch-dispatch catalog status summaries. Error management coverage includes unsupported-selection planner errors, aggregate plan/dispatch blocked errors without fabricated item rows, null dispatch payloads on rejected dispatches, stale-argument suppression, dry-run/non-mutating catalog behavior, and no asset mutation. The `#1203`-`#1381` evidence-audit range became contiguous; the following note records the subsequent `#1382`-`#1385` closure batch.

Additional E2 closure note: `#1382`-`#1385` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox create, toolbox batch-create, toolbox create-from-dispatch, and toolbox batch create-from-dispatch result summaries. Error management coverage includes unavailable toolbox items, non-admitted dispatches, duplicate identity rejection, lower-layer invalid-field failures, stale result suppression, and no mutation or partial mutation on rejected create paths. The `#1203`-`#1385` evidence-audit range became contiguous; the following note records the subsequent `#1386`-`#1387` closure batch.

Additional E2 closure note: `#1386`-`#1387` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox create-dispatch plan and toolbox batch create-dispatch plan summaries. Error management coverage includes non-admitted create dispatches, unavailable toolbox items, invalid batch plans, invalid admission tokens, orphan batch-item options, stale dispatch-argument suppression, non-executing dispatch planning, and no asset mutation. The `#1203`-`#1387` evidence-audit range became contiguous; the following note records the subsequent `#1388`-`#1390` closure batch.

Additional E2 closure note: `#1388`-`#1390` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox invocation-admission, toolbox dispatch, and toolbox execution summaries. Error management coverage includes unsupported toolbox contexts, invalid admission booleans, invalid records, missing contexts, dry-run/unadmitted dispatch rejection, missing launch-command parser failures, unadmitted execution rejection, nonzero toolbox process exit reporting, non-executing dispatch planning, and non-mutating execution summaries. The `#1203`-`#1390` evidence-audit range became contiguous; the following note records the subsequent `#1391`-`#1394` closure batch.

Additional E2 closure note: `#1391`-`#1394` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped builder launch-plan, builder invocation-admission, builder dispatch, and builder execution summaries. Error management coverage includes invalid context tokens, ambiguous builder/selection context requests, unavailable or wrong-context builders, dry-run and unadmitted dispatch/execution rejection, missing launch-command parser failures, nonzero child process exit reporting, non-executing dispatch planning, and non-mutating summaries. The `#1203`-`#1394` evidence-audit range became contiguous; the following note records the subsequent `#1395`-`#1398` closure batch.

Additional E2 closure note: `#1395`-`#1398` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped editor-action launch-plan, invocation-admission, dispatch, and execution summaries. Error management coverage includes wrong-context and unavailable actions, unknown or missing selection contexts, missing action ids, invalid boolean/numeric values, dry-run and unadmitted dispatch/execution rejection, missing launch-command parser failures, nonzero child process exit reporting, non-executing dispatch planning, and non-mutating summaries. The `#1203`-`#1398` evidence-audit range became contiguous; the following note records the subsequent `#1399`-`#1402` closure batch.

Additional E2 closure note: `#1399`-`#1402` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped designer launch-surface, invocation-admission, dispatch, and execution selected-context summaries. Error management coverage includes unknown or missing selection contexts, invalid admission booleans, unsupported toolbox contexts surfaced as blocked selected-context summaries, dry-run/unadmitted child dispatch errors, failed child process diagnostics, non-executing dispatch planning, and non-mutating summaries. The `#1203`-`#1402` evidence-audit range became contiguous; the following note records the subsequent `#1403`-`#1405` closure batch.

Additional E2 closure note: `#1403`-`#1405` were closed with evidence on 2026-06-21 after fresh `git diff --check` and `test_studio_host_json` validation confirmed shipped toolbox palette launch-plan, toolbox create-batch plan, and toolbox create-plan summaries. Error management coverage includes unsupported toolbox selections, unknown or missing selection contexts, invalid toolbox context tokens, unknown toolbox items, unavailable selected-context items, blocked batch-plan diagnostics without fabricated item ids, dry-run/non-mutating planning, and no asset mutation on rejected planning paths. Together with already-closed `#1406`-`#1447`, the `#1203`-`#1447` evidence-audit range is now contiguous. Parent `#23` remains open; only older E2 parent/roadmap rows remain open in the local issue snapshot.

Additional E2 closure note: `#1241`, `#1243`, `#1245`, `#1247`, and `#1249` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped toolbox object planning, catalog, batch planning, batch creation, and create-dispatch planning coverage. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1242`, `#1244`, `#1246`, and `#1248` were closed with evidence on 2026-06-21 after fresh `test_toolbox_creation` and `test_studio_host_json` validation confirmed shipped toolbox object creation plan/catalog host JSON, toolbox object batch creation planning host JSON, toolbox object batch creation host JSON, parser/runtime error management, and no-mutation/no-partial-mutation behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1250`, `#1251`, and `#1252` were closed with evidence on 2026-06-21 after fresh `test_toolbox_creation` and `test_studio_host_json` validation confirmed shipped toolbox object create dispatch planning host JSON, toolbox object batch create dispatch planning, toolbox object batch create dispatch planning host JSON, and no-stale-argument/no-mutation failure behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1253`, `#1254`, `#1255`, and `#1256` were closed with evidence on 2026-06-21 after fresh `test_toolbox_creation` and `test_studio_host_json` validation confirmed shipped toolbox object create dispatch catalog planning/host JSON, toolbox object batch create dispatch catalog planning/host JSON, parser/planner error management, and no-stale-argument/no-mutation behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1257`, `#1258`, and `#1259` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_creation`, and `test_studio_host_json` validation confirmed shipped toolbox object batch create plan catalog planning/host JSON plus coverage documentation for the non-mutating toolbox creation planning/dispatch surfaces. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1260`, `#1261`, `#1262`, and `#1263` were closed with evidence on 2026-06-21 after fresh `test_toolbox_creation`, `test_toolbox_palette`, and `test_studio_host_json` validation confirmed shipped toolbox object creation and batch planning from admitted palette dispatch plus host JSON exposure. The covered error management includes dry-run/non-admitted dispatch rejection, unavailable toolbox item ids, missing/inconsistent dispatch metadata, empty batches, orphan item options, invalid admission tokens, no-stale-plan behavior, and no-mutation behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1264`, `#1265`, `#1266`, and `#1267` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_creation`, `test_toolbox_palette`, and `test_studio_host_json` validation confirmed shipped toolbox object create/batch dispatch planning from admitted palette dispatch plus host JSON exposure. The covered error management includes dry-run/non-admitted palette dispatch rejection, non-admitted create operations, unavailable dispatch items, empty batches, orphan item options, invalid identities/tokens, no-stale-dispatch-argument behavior, and no-mutation behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1268`, `#1269`, and `#1270` are closed as of 2026-06-21 after live-state review and fresh `git diff --check`, `test_builder_registry`, and `test_studio_host_json` validation confirmed shipped builder launch catalog planning, builder launch catalog host JSON, and builder invocation-admission catalog planning. Error management coverage includes empty builder contexts, missing/unknown host contexts, invalid record indexes, invocation-only/unknown options rejected before planning, dry-run/non-mutating behavior, and no builder UI/process execution or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1271`-`#1276` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_builder_registry`, `test_designer_context`, and `test_studio_host_json` validation confirmed shipped builder invocation-admission catalog host JSON, builder dispatch catalogs routed through admission catalogs, selection-context builder invocation-admission catalogs, selection-context builder invocation-admission host JSON, selection-context builder dispatch catalogs, and selection-context builder dispatch host JSON. Error management coverage includes missing/unknown contexts, invalid records, invalid booleans, unknown options, empty selections, dry-run/non-admitted dispatch rejection, stale-entry prevention, and no builder UI/process execution or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1277`-`#1282` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_designer_context`, `test_context_editor_actions`, and `test_studio_host_json` validation confirmed shipped selection-context builder launch catalogs plus editor-action launch and invocation-admission catalogs, including host JSON exposure. Error management coverage includes empty selections/action contexts, missing/unknown contexts, invalid records/line/column values, invalid booleans, unknown options, stale-entry prevention, dry-run/non-mutating behavior, and no editor/builder UI, process execution, or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1283`-`#1289` are closed as of 2026-06-21 after live-state review plus fresh `git diff --check`, `test_toolbox_palette`, `test_context_editor_actions`, `test_designer_context`, and `test_studio_host_json` validation confirmed shipped editor/designer/toolbox dispatch catalog routing through invocation-admission catalogs, toolbox invocation-admission catalog planning/host JSON, and selection-context toolbox invocation-admission planning/host JSON. Error management coverage includes empty metadata, missing/unknown contexts, invalid records, invalid admission booleans, unsupported selections, stale-entry prevention, dry-run/non-mutating behavior, and no dispatch/UI/process execution or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1290`-`#1293` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_palette`, `test_toolbox_creation`, and `test_studio_host_json` validation confirmed shipped selection-context toolbox dispatch catalogs and selection-context toolbox object create-plan catalogs plus host JSON exposure. Error management coverage includes dry-run dispatch rejection, unsupported selections, missing/unknown contexts, invalid records, malformed field values, unknown options, missing paths, dry-run/non-mutating behavior, and no UI/process execution, control creation, dispatch execution, or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1294`-`#1299` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_creation`, and `test_studio_host_json` validation confirmed shipped selection-context toolbox object create-dispatch catalogs, batch create-plan catalogs, and batch create-dispatch catalogs plus host JSON exposure. Error management coverage includes unsupported selections, non-admitted create-operation errors without stale arguments, missing/unknown contexts, malformed field values, invalid booleans, unknown options, missing paths, dry-run/non-mutating behavior, and no dispatch/UI/process execution, control creation, or asset mutation. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1300`-`#1303` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_creation`, and `test_studio_host_json` validation confirmed shipped selection-context toolbox object create planning/create-dispatch planning plus host JSON exposure. Error management coverage includes unsupported selections, unavailable selected-context toolbox items, missing paths, missing item ids, missing/unknown contexts, malformed field values, invalid booleans, unknown options, non-admitted create operations, stale-plan/stale-argument suppression, no dispatch/UI/process execution, and no asset mutation. `#1304`, `#1306`, `#1308`, `#1310`, `#1312`, and `#1313` are also closed live from earlier evidence-backed work and were removed from the local issue snapshot. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1305`, `#1307`, `#1309`, and `#1311` were closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_toolbox_creation`, and `test_studio_host_json` validation confirmed shipped selection-context toolbox object batch create-plan host JSON, batch create-dispatch-plan host JSON, single create execution host JSON, and batch create execution host JSON. Error management coverage includes missing paths, missing item values, missing/unknown selections, empty item lists, orphan per-item options, malformed field values, invalid booleans, unknown options, non-admitted create operations, unavailable selected-context items, unsupported selections, stale-plan/stale-argument/stale-identity suppression, expected mutation only on accepted execution commands, and no mutation on rejected commands. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1098` was closed with evidence on 2026-06-21 after fresh `git diff --check`, `test_studio_host`, and `test_studio_host_json` validation confirmed shipped host `--picture-object` parsing, stable target selector routing through `set_visual_object_picture`, successful PICTURE mutation, parser/editor failure rejection, duplicate/missing target rejection, ambiguity rejection, and rollback/no-mutation behavior. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1203`, `#1205`, `#1211`, `#1213`, and `#1215` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped Studio builder launch planning, selection-context builder planning, combined designer launch-surface planning, launch-surface catalog coverage, and builder invocation admission planning. `#1212` and `#1214` were closed with evidence on 2026-06-21. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1215`, `#1217`, `#1219`, `#1221`, and `#1223` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped builder, editor-action, toolbox, aggregate designer, and catalog invocation-admission planning coverage. `#1216` and `#1218` were closed with evidence on 2026-06-21. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1225`, `#1227`, `#1229`, and `#1231` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped editor-action and builder invocation dispatch planning plus dispatch catalog coverage. `#1226`, `#1228`, `#1230`, and `#1232` were closed with evidence on 2026-06-21. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1233`, `#1235`, `#1237`, `#1268`, and `#1270` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped toolbox dispatch, aggregate designer dispatch, builder launch catalog, and builder invocation-admission catalog coverage. `#1234` and `#1236` were closed with evidence on 2026-06-21 after fresh `test_toolbox_palette` and `test_studio_host_json` validation confirmed host JSON exposure plus parser/planner error management. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1238`, `#1239`, and `#1240` were closed with evidence on 2026-06-21 after fresh `test_designer_context` and `test_studio_host_json` validation confirmed shipped aggregate designer dispatch host JSON, aggregate dispatch catalog planning, aggregate dispatch catalog host JSON, and parser/planner error management. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Additional E2 closure note: `#1283`, `#1284`, and `#1287` were closed in GitHub on 2026-06-20 after existing implementation comments plus fresh focused validation confirmed shipped editor-action, designer, and toolbox dispatch-catalog routing through shared invocation-admission catalog seams. Parent `#23` remains open; other open E2 children still need live-state review before any closure.

Current E3 update: latest implemented slice is `#1678`, proving deleted title FRX/LBX sections can be selected by stable unique id with selected-section JSON, deleted title band metadata, live sibling detail/page-footer metadata, live/deleted section counts, null selected object/settings metadata, and label identity preserved. `#1677` is the immediately preceding stable-selector column-footer section selection slice; `#1546` was closed not planned after validation showed direct-over-duplicate-EXPR summary precedence would regress existing memo-edit behavior. It follows the completed local E2 host-exposure sweep through `#1451`. Child issues `#1452`-`#1678` have been dispositioned with evidence-backed approval; parent `#24` remains open.

**Phase A critical path is COMPLETE as of 2026-06-13.** All A3 lane issues (#7, #8, #92-#101) are closed.

**D1/#19 and E1/#22 are COMPLETE as of 2026-06-16.** The live blocker path has moved through E2/#23 into E3/#24. Latest implemented slice: `#1678`, proving deleted title FRX/LBX sections can be selected by stable unique id with selected-section metadata preserved.

Next: continue E3/#24 by creating the next prompt-sized report/label designer fidelity child before coding unless the live issue tree shows a higher-weight blocker. A broader visual-asset editor API scan found remaining unwrapped batch APIs after `#1447`; `#1448` duplicate batches, `#1449` rename batches, `#1450` reorder batches, and `#1451` subtree duplication are now closed with evidence-backed approval. The `#1078`-`#1086`, `#1087`-`#1101`, `#1102`-`#1202`, and `#1203`-`#1447` E2 evidence-audit ranges are closed; only older E2 parent/roadmap rows remain open in the local issue snapshot and must not be orphaned. Do not reopen closed D1/#19, E1/#22, native-wrapper, transpilation, debugger, or E2 host-wrapper slices unless new evidence shows a regression.

Language-service queue update:

- 2026-06-16: **`#637` is now shipped.** Generated DLL/FLL native-wrapper dispatch execution now carries `CopperfinRuntimeBridgeLaunchPlan::environment` into command construction, and runtime-host command lines now apply those environment assignments before launching the host. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output carries launch environment entries through dispatch and command construction. `#637` is now shipped as the final visible prompt-sized `#40` child; re-audit `#40` before opening the next build-output child slice.
- 2026-06-16: **`#636` is now shipped.** Generated DLL/FLL native-wrapper launch helpers now quote and build a runtime-host command line from `CopperfinRuntimeBridgeDispatchExecution`, execute it through `std::system(...)`, capture the observed exit code, and mark launch success only when that exit code matches the expected exit code. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output no longer reports deterministic process-launch failure. `#637` followed this as the next shipped `#40` child.
- 2026-06-16: **`#635` is now shipped.** Generated DLL/FLL native-wrapper response contracts now include `schema_version`, and response validation now compares the response document's schema-version field with `CopperfinRuntimeBridgeResponseValidationPlan::expected_schema_version`, falling back when the field is absent or mismatched. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares and enforces response schema version at the validation seam. `#636` followed this as the next shipped `#40` child.
- 2026-06-16: **`#634` is now shipped.** Generated DLL/FLL native-wrapper response validation now extracts the response document's `response_media_type` field and compares it with `CopperfinRuntimeBridgeResponseValidationPlan::expected_response_media_type`, falling back when the field is absent or mismatched. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output enforces response media type at the validation seam. `#635` followed this as the next shipped `#40` child.
- 2026-06-16: **`#633` is now shipped.** Generated DLL/FLL native-wrapper response validation now checks `response_document` for every field declared by `CopperfinRuntimeBridgeResponseValidationPlan::required_response_fields` and falls back when any required response field is absent. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output enforces required response fields at the validation seam. `#634` followed this as the next shipped `#40` child.
- 2026-06-16: **`#632` is now shipped.** Generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_missing_response(...)`, and missing-response policy now treats the response as missing only when an existing response is required and the read document is empty instead of treating the `require_existing_response` policy flag itself as a missing response. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output evaluates missing responses from the response document. `#633` followed this as the next shipped `#40` child.
- 2026-06-16: **`#631` is now shipped.** Generated DLL/FLL native-wrapper stubs now pass the executed `response_document` into `copperfin_runtime_bridge_evaluate_response_validation(...)`, and the validation evaluation surface records response-document availability from the response body so later response-contract enforcement can validate against the actual bridge payload. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output carries the response document into response validation. `#632` followed this as the next shipped `#40` child.
- 2026-06-16: **`#630` is now shipped.** Generated DLL/FLL native-wrapper request JSON now carries expected response media type plus the response-field contract list from `CopperfinRuntimeBridgePayloadPlan::response_fields`, so runtime-host consumers can produce and validate the matching response envelope from the same wrapper request document. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the expected-response helpers and serializes each response-field contract item. `#631` followed this as the next shipped `#40` child.
- 2026-06-16: **`#629` is now shipped.** Generated DLL/FLL native-wrapper request JSON now emits a `request_fields` array serialized from `CopperfinRuntimeBridgePayloadPlan::request_fields`, so runtime-host consumers can validate the exact request contract carried by the wrapper request document. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the request-fields helper and serializes each request-field contract item. `#630` followed this as the next shipped `#40` child.
- 2026-06-16: **`#628` is now shipped.** Generated DLL/FLL native-wrapper `CopperfinRuntimeBridgePayloadPlan::request_fields` now declares the descriptor metadata fields serialized by the bridge request document, including routine kind, source provenance, parameter declaration style, parameter names, and schema version. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output keeps the request-field contract synchronized with descriptor metadata serialization. `#629` followed this as the next shipped `#40` child.
- 2026-06-16: **`#627` is now shipped.** Generated DLL/FLL native-wrapper request JSON now serializes routine kind, source provenance, parameter declaration style, parameter-name list, and schema version beside the existing export name, arity, request media type, and parameter value payloads. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL generated output declares the new request-field helpers and routes descriptor metadata into the bridge request document. `#628` followed this as the next shipped `#40` child.
- 2026-06-16: **`#626` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit stale `(void)` discards for DLL integer parameters or FLL `ParamBlk* parm` now that those exported arguments feed `copperfin_build_runtime_bridge_call(...)` parameter payloads. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove DLL/FLL arguments are consumed through bridge call bindings and the false unused markers are absent. `#627` followed this as the next shipped `#40` child.
- 2026-06-16: **`#625` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)request_write_execution` discard now that `request_write_execution` feeds `copperfin_build_runtime_bridge_response_read_plan(...)`; the response-read plan now carries `request_write_succeeded` and falls back to the shared empty response document when request writing fails. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the write result is consumed and the false unused marker is absent. `#626` followed this as the next shipped `#40` child.
- 2026-06-16: **`#624` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)dispatch_execution` discard now that `dispatch_execution` feeds `copperfin_runtime_bridge_launch_process(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch result is consumed and the false unused marker is absent. `#625` followed this as the next shipped `#40` child.
- 2026-06-16: **`#623` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)process_launch` discard now that `process_launch` feeds `copperfin_runtime_bridge_evaluate_host_failure(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch result is consumed and the false unused marker is absent. `#624` followed this as the next shipped `#40` child.
- 2026-06-16: **`#622` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)host_failure` discard now that `host_failure` feeds `copperfin_runtime_bridge_evaluate_missing_response(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation is consumed and the false unused marker is absent. `#623` followed this as the next shipped `#40` child.
- 2026-06-16: **`#621` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)missing_response` discard now that `missing_response` feeds `copperfin_runtime_bridge_evaluate_response_validation(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation result is consumed and the false unused marker is absent. `#622` followed this as the next shipped `#40` child.
- 2026-06-16: **`#620` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)response_validation_evaluation` discard now that `response_validation_evaluation` feeds `copperfin_runtime_bridge_admit_response_parse(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the evaluation is consumed and the false unused marker is absent. `#621` followed this as the next shipped `#40` child.
- 2026-06-16: **`#619` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)response_parse_admission` discard now that `response_parse_admission` feeds `copperfin_runtime_bridge_admit_interpreted_result(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#620` followed this as the next shipped `#40` child.
- 2026-06-16: **`#618` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)interpreted_result_admission` discard now that `interpreted_result_admission` feeds `copperfin_runtime_bridge_admit_native_return(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#619` followed this as the next shipped `#40` child.
- 2026-06-16: **`#617` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)native_return_admission` discard now that `native_return_admission` feeds `copperfin_runtime_bridge_admit_outcome_selection(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#618` followed this as the next shipped `#40` child.
- 2026-06-16: **`#616` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)outcome_selection_admission` discard now that `outcome_selection_admission` feeds `copperfin_runtime_bridge_admit_return_materialization(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#617` followed this as the next shipped `#40` child.
- 2026-06-16: **`#615` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_materialization_admission` discard now that `return_materialization_admission` feeds `copperfin_runtime_bridge_admit_return_emission(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#616` followed this as the next shipped `#40` child.
- 2026-06-16: **`#614` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_emission_admission` discard now that `return_emission_admission` feeds `copperfin_runtime_bridge_admit_final_return_adoption(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#615` followed this as the next shipped `#40` child.
- 2026-06-16: **`#613` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)final_return_adoption_admission` discard now that `final_return_adoption_admission` feeds `copperfin_runtime_bridge_admit_return_activation(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#614` followed this as the next shipped `#40` child.
- 2026-06-16: **`#612` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)return_activation_admission` discard now that `return_activation_admission` feeds `copperfin_runtime_bridge_admit_stub_return(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#613` followed this as the next shipped `#40` child.
- 2026-06-16: **`#611` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)stub_return_admission` discard now that `stub_return_admission` feeds `copperfin_runtime_bridge_admit_placeholder_return_value(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#612` followed this as the next shipped `#40` child.
- 2026-06-16: **`#610` is now shipped.** Generated DLL/FLL native-wrapper stubs no longer emit the stale `(void)placeholder_return_value_admission` discard now that `placeholder_return_value_admission` feeds `copperfin_runtime_bridge_admit_placeholder_return_int(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admission is consumed and the false unused marker is absent. `#611` followed this as the next shipped `#40` child.
- 2026-06-16: **`#609` is now shipped.** Generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_int_admission)` and remove the stale `(void)placeholder_return_int_admission` discard, so the explicit placeholder-return integer is produced from the admitted int surface before flowing into stub-emission admission and execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the admitted-int executor route and absence of the old placeholder-value helper input. `#610` followed this as the next shipped `#40` child.
- 2026-06-16: **`#608` is now shipped.** Generated DLL/FLL native-wrapper stubs now pass the explicit placeholder-return integer into `copperfin_runtime_bridge_admit_stub_emission(placeholder_return_int_admission, placeholder_return_int)` and execute stub emission from `copperfin_runtime_bridge_execute_stub_emission(stub_emission_admission)` instead of discarding the admitted surface and reusing direct placeholder-return inputs. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct admission-to-execution route and absence of the old `(void)stub_emission_admission` discard. `#609` followed this as the next shipped `#40` child.
- 2026-06-16: **`#607` is now shipped.** Generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_execute_placeholder_return_int(placeholder_return_value)` after placeholder-return-int admission and pass the explicit integer into `copperfin_runtime_bridge_execute_stub_emission(placeholder_return_value, placeholder_return_int)` instead of keeping placeholder-return-int execution hidden inside stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct placeholder-return-int executor call-site and absence of the old hidden helper call. `#608` followed this as the next shipped `#40` child.
- 2026-06-16: **`#606` is now shipped.** Generated DLL/FLL native-wrapper stubs now call `copperfin_runtime_bridge_apply_stub_emission_output(stub_emission_return_surface, ...)` after return-surface construction with the planned return adapter instead of keeping output application hidden inside `copperfin_runtime_bridge_emit_stub_return_shared(...)`. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the direct output-application call-site and absence of the obsolete shared stub-return emitter. `#607` followed this as the next shipped `#40` child.
- 2026-06-16: **`#605` is now shipped.** Generated DLL/FLL native-wrapper stubs now build `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` after stub-emission execution and pass explicit return-surface metadata into `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping return-surface construction hidden inside the shared stub-return emitter. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-surface construction call-site and two-input shared stub-return emitter call. `#606` followed this as the next shipped `#40` child.
- 2026-06-16: **`#604` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_emission(placeholder_return_value_plan, placeholder_return_value)` after stub-emission admission and pass emitted stub metadata into `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping stub-emission execution hidden inside the shared stub-return emitter. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-emission execution call-site and two-input shared stub-return emitter call. `#604` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#603` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_placeholder_return_value(placeholder_return_value_plan)` after placeholder-return-value admission and pass placeholder return metadata into shared stub-emission execution instead of keeping placeholder-return-value execution hidden inside stub-emission execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-return-value execution call-site and two-input shared stub-emission helper call. `#603` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#602` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_stub_return(stub_return_plan)` after stub-return admission and pass routed stub-return metadata into `CopperfinRuntimeBridgePlaceholderReturnValuePlan` instead of keeping stub-return execution hidden inside placeholder-return-value planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return execution call-site and two-input placeholder-return-value plan construction. `#602` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#601` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_activation(return_activation_plan)` after return-activation admission and pass activated return metadata into `CopperfinRuntimeBridgeStubReturnPlan` instead of keeping return-activation execution hidden inside stub-return planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation execution call-site and two-input stub-return plan construction. `#601` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#600` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_final_return_adoption(final_return_adoption_plan)` after final-return-adoption admission and pass adopted return metadata into `CopperfinRuntimeBridgeReturnActivationPlan` instead of keeping final-return-adoption execution hidden inside return-activation planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption execution call-site and two-input return-activation plan construction. `#600` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#599` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_emission(return_emission_plan)` after return-emission admission and pass emitted return block metadata into `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` instead of keeping return-emission execution hidden inside final-return-adoption planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission execution call-site and three-input final-return-adoption plan construction. `#599` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#598` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_return_materialization(return_materialization_plan)` after return-materialization admission and pass materialized return branch metadata into `CopperfinRuntimeBridgeReturnEmissionPlan` instead of keeping return-materialization execution hidden inside return-emission planning/execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization execution call-site and two-input return-emission plan construction. `#598` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#597` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_outcome_selection(outcome_selection_plan)` after outcome-selection admission and pass selected condition/diagnostics/native return metadata into `CopperfinRuntimeBridgeReturnMaterializationPlan` instead of keeping outcome-selection execution hidden inside return-materialization execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection execution call-site and two-input return-materialization plan construction. `#597` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#596` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_native_return(native_return_plan)` after native-return admission and pass the resulting selected native return metadata into `CopperfinRuntimeBridgeOutcomeSelectionPlan` instead of keeping native-return execution hidden inside outcome-selection execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return execution call-site and two-input outcome-selection plan construction. `#596` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#595` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_interpreted_result(interpreted_result_plan)` after interpreted-result admission and pass the resulting interpreted success/fallback output into `CopperfinRuntimeBridgeNativeReturnPlan` instead of keeping interpretation execution hidden inside native-return execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result execution call-site and three-input native-return plan construction. `#595` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#594` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_parse_response(response_parse_plan)` after response-parse admission and pass the resulting parsed response into `CopperfinRuntimeBridgeInterpretedResultPlan` instead of keeping parse execution hidden inside interpreted-result execution. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse execution call-site and two-input interpreted-result plan construction. `#594` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#529` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_write_request(request_write_plan)` immediately after constructing the request-write plan and explicitly discard the scaffold-only execution result before continuing into response-read planning, so request documents are materialized through the shared helper while the stub still follows the placeholder-return path. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper declaration, request-document write body, request-write execution call-site, and explicit discard in DLL and FLL generated output. `#593` followed this as the next shipped `#40` child.
- 2026-06-16: **`#593` is now shipped.** Generated DLL/FLL native-wrapper stubs now execute `copperfin_runtime_bridge_execute_read_response(response_read_plan)` immediately after constructing the response-read plan and pass the resulting response document into `CopperfinRuntimeBridgeResponseArtifact` instead of seeding artifacts from the empty-response fallback. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read execution call-site and two-input response-artifact construction. `#593` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-16: **`#592` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDescriptor`, so exported DLL/FLL stubs now build that wrapper once before constructing the descriptor and then build invocations straight from the enriched descriptor instead of passing the wrapper separately at the invocation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the descriptor-plan wrapper contract path. `#529` is shipped; `#630` is now the final visible prompt-sized `#40` child before the next re-audit.
- 2026-06-15: **`#591` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInvocation`, so exported DLL/FLL stubs now build that wrapper once before constructing the invocation and then build calls straight from the enriched invocation instead of passing the wrapper separately at the call seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the invocation-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#592` for carrying that wrapper contract through descriptor plans.
- 2026-06-15: **`#590` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeCall`, so exported DLL/FLL stubs now build that wrapper once before constructing the call and then build results straight from the enriched call instead of passing the wrapper separately at the result seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the call-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#591` for carrying that wrapper contract through invocation plans.
- 2026-06-15: **`#589` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResult`, so exported DLL/FLL stubs now build that wrapper once before constructing the result and then build launch plans straight from the enriched result instead of passing the wrapper separately at the launch seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the result-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#590` for carrying that wrapper contract through call plans.
- 2026-06-15: **`#588` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeLaunchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the launch plan and then build observation plans straight from the enriched launch plan instead of passing the wrapper separately at the observation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#589` for carrying that wrapper contract through result plans.
- 2026-06-15: **`#587` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeObservationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the observation plan and then build execution plans straight from the enriched observation plan instead of passing the wrapper separately at the execution seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the observation-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#588` for carrying that wrapper contract through launch plans.
- 2026-06-15: **`#586` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeExecutionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the execution plan and then build transport plans straight from the enriched execution plan instead of passing the wrapper separately at the transport seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the execution-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#587` for carrying that wrapper contract through observation plans.
- 2026-06-15: **`#585` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeTransportPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the transport plan and then build serialization plans straight from the enriched transport plan instead of passing the wrapper separately at the serialization seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the transport-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#586` for carrying that wrapper contract through execution plans.
- 2026-06-15: **`#584` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeSerializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the serialization plan and then build dispatch plans straight from the enriched serialization plan instead of passing the wrapper separately at the dispatch seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the serialization-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#585` for carrying that wrapper contract through transport plans.
- 2026-06-15: **`#583` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeDispatchPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the dispatch plan and then build payload plans straight from the enriched dispatch plan instead of passing the wrapper separately at the payload seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#584` for carrying that wrapper contract through serialization plans.
- 2026-06-15: **`#582` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePayloadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the payload plan and then build interpretation plans straight from the enriched payload plan instead of passing the wrapper separately at the interpretation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the payload-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#583` for carrying that wrapper contract through dispatch plans.
- 2026-06-15: **`#581` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpretation plan and then build failure-policy plans straight from the enriched interpretation plan instead of passing the wrapper separately at the failure-policy seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpretation-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#582` for carrying that wrapper contract through payload plans.
- 2026-06-15: **`#580` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFailurePolicyPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the failure-policy plan and then build response-validation plans straight from the enriched failure-policy plan instead of passing the wrapper separately at the response-validation seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the failure-policy-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#581` for carrying that wrapper contract through interpretation plans.
- 2026-06-15: **`#579` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseValidationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-validation plan and then build request artifacts straight from the enriched response-validation plan instead of passing the wrapper separately at the request-artifact seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-validation-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#580` for carrying that wrapper contract through failure-policy plans.
- 2026-06-15: **`#578` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the request artifact and then build request-write plans straight from the enriched request artifact instead of passing the wrapper separately at the request-write-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-artifact wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#579` for carrying that wrapper contract through response-validation plans.
- 2026-06-15: **`#577` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeRequestWritePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the request-write plan and then build response-read plans straight from the enriched request-write plan instead of passing the wrapper separately at the response-read-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-write-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#578` for carrying that wrapper contract through request artifacts.
- 2026-06-15: **`#576` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseReadPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-read plan and then build response artifacts straight from the enriched response-read plan instead of passing the wrapper separately at the response-artifact seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#577` for carrying that wrapper contract through request-write plans.
- 2026-06-15: **`#575` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseArtifact`, so exported DLL/FLL stubs now build that wrapper once before constructing the response artifact and then build response-parse plans straight from the enriched response artifact instead of passing the wrapper separately at the response-parse-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-artifact wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#576` for carrying that wrapper contract through response-read plans.
- 2026-06-15: **`#574` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeResponseParsePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the response-parse plan and then build interpreted-result plans straight from the enriched response-parse plan instead of passing the wrapper separately at the interpreted-result-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#575` for carrying that wrapper contract through response artifacts.
- 2026-06-15: **`#573` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeInterpretedResultPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the interpreted-result plan and then build native-return plans straight from the enriched interpreted-result plan instead of passing the wrapper separately at the native-return-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#574` for carrying that wrapper contract through response-parse plans.
- 2026-06-15: **`#572` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeNativeReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the native-return plan and then build outcome-selection plans straight from the enriched native-return plan instead of passing the wrapper separately at the outcome-selection-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#573` for carrying that wrapper contract through interpreted-result plans.
- 2026-06-15: **`#571` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeOutcomeSelectionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the outcome-selection plan and then build return-materialization plans straight from the enriched outcome-selection plan instead of passing the wrapper separately at the return-materialization-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#572` for carrying that wrapper contract through native-return plans.
- 2026-06-15: **`#570` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnMaterializationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-materialization plan and then build return-emission plans straight from the enriched return-materialization plan instead of passing the wrapper separately at the return-emission-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#571` for carrying that wrapper contract through outcome-selection plans.
- 2026-06-15: **`#569` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnEmissionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-emission plan and then build final-return-adoption plans straight from the enriched return-emission plan instead of passing the wrapper separately at the final-return-adoption-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#570` for carrying that wrapper contract through return-materialization plans.
- 2026-06-15: **`#568` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeFinalReturnAdoptionPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the final-return-adoption plan and then build return-activation plans straight from the enriched final-return-adoption plan instead of passing the wrapper separately at the return-activation-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#569` for carrying that wrapper contract through return-emission plans.
- 2026-06-15: **`#567` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeReturnActivationPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the return-activation plan and then build stub-return plans straight from the enriched activation plan instead of passing the wrapper separately at the stub-return-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#568` for carrying that wrapper contract through final-return-adoption plans.
- 2026-06-15: **`#566` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgeStubReturnPlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the stub-return plan and then build placeholder-return-value plans straight from the enriched stub-return plan instead of passing the wrapper separately at the placeholder-plan seam. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-plan wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#567` for carrying that wrapper contract through return-activation plans.
- 2026-06-15: **`#565` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the stub-emission wrapper contract through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, so exported DLL/FLL stubs now build that wrapper once before constructing the placeholder-return-value plan and then route placeholder returns straight through `copperfin_runtime_bridge_emit_stub_return_shared(...)` instead of keeping separate output-specific stub-return wrapper helpers. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the plan-level wrapper contract path. The next open prompt-sized child in the `#40` branch is now `#566` for carrying that wrapper contract through stub-return plans.
- 2026-06-15: **`#564` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeStubEmissionWrapper` surface plus `copperfin_runtime_bridge_build_stub_emission_wrapper(...)` so the remaining output-specific `copperfin_runtime_bridge_emit_stub_return(...)` bodies now only construct one wrapper contract before handing off to the shared stub-emission emitter helper. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission wrapper helper path. The next open prompt-sized child in the `#40` branch is now `#565` for carrying that wrapper contract through placeholder-return-value plans.
- 2026-06-15: **`#563` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_emit_stub_return_shared(...)` helper so the formerly duplicated DLL/FLL `copperfin_runtime_bridge_emit_stub_return(...)` bodies now flow through one scaffold-only emitter helper before output-specific wrapper forwarding. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission emitter helper path. The next open prompt-sized child in the `#40` branch is now `#564` for the remaining shared stub-emission wrapper helper seam.
- 2026-06-15: **`#562` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_apply_stub_emission_output(...)` helper so shared stub-emission return-surface output flows through one scaffold-only helper before the final DLL/FLL return adapter is applied. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission output-application helper path. The next open prompt-sized child in the `#40` branch is now `#563` for the remaining shared stub-emission emitter helper seam.
- 2026-06-15: **`#561` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_build_stub_emission_return_surface(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionReturnSurface` surface so shared stub-emission execution output flows through one scaffold-only helper before DLL/FLL output-specific stub return application. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission return-surface helper path. The next open prompt-sized child in the `#40` branch is now `#562` for the remaining shared stub-emission output-application helper seam.
- 2026-06-15: **`#560` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmission` surface so staged placeholder-return-value execution and placeholder-return-int execution flow through one scaffold-only helper before DLL/FLL output-specific stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission execution helper path. The next open prompt-sized child in the `#40` branch is now `#561` for the remaining shared stub-emission return-surface helper seam.
- 2026-06-15: **`#559` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_stub_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeStubEmissionAdmission` surface so staged placeholder-return-int admission output and final stub-emission contracts flow through one scaffold-only helper before output-specific stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-emission admission helper path. The next open prompt-sized child in the `#40` branch is now `#560` for the remaining shared stub-emission execution helper seam.
- 2026-06-15: **`#558` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_placeholder_return_int(...)` helper plus a narrow `CopperfinRuntimeBridgePlaceholderReturnIntAdmission` surface so staged placeholder-return-value admission output and placeholder-return-int contracts flow through one scaffold-only helper before final stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-int admission helper path. The next open prompt-sized child in the `#40` branch is now `#559` for the remaining shared stub-emission admission helper seam.
- 2026-06-15: **`#557` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_placeholder_return_value(...)` helper plus a narrow `CopperfinRuntimeBridgePlaceholderReturnValueAdmission` surface so staged stub-return admission output and placeholder-return-value contracts flow through one scaffold-only helper before placeholder-return-int routing. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-value admission helper path. The next open prompt-sized child in the `#40` branch is now `#558` for the remaining shared placeholder-return-int admission helper seam.
- 2026-06-15: **`#556` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_stub_return(...)` helper plus a narrow `CopperfinRuntimeBridgeStubReturnAdmission` surface so staged return-activation admission output and stub-return contracts flow through one scaffold-only helper before placeholder-return-value planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-return admission helper path. The next open prompt-sized child in the `#40` branch is now `#557` for the remaining shared placeholder-return-value admission helper seam.
- 2026-06-15: **`#555` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_activation(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnActivationAdmission` surface so staged final-return-adoption admission output and return-activation contracts flow through one scaffold-only helper before stub-return planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-activation admission helper path. The next open prompt-sized child in the `#40` branch is now `#556` for the remaining shared stub-return admission helper seam.
- 2026-06-15: **`#554` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_final_return_adoption(...)` helper plus a narrow `CopperfinRuntimeBridgeFinalReturnAdoptionAdmission` surface so staged return-emission admission output and final-return-adoption contracts flow through one scaffold-only helper before return-activation planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared final-return-adoption admission helper path. The next open prompt-sized child in the `#40` branch is now `#555` for the remaining shared return-activation admission helper seam.
- 2026-06-15: **`#553` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnEmissionAdmission` surface so staged return-materialization admission output and return-emission contracts flow through one scaffold-only helper before final-return-adoption planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-emission admission helper path. The next open prompt-sized child in the `#40` branch is now `#554` for the remaining shared final-return-adoption admission helper seam.
- 2026-06-15: **`#552` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_return_materialization(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnMaterializationAdmission` surface so staged outcome-selection admission output and return-materialization contracts flow through one scaffold-only helper before return-emission planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-materialization admission helper path. The next open prompt-sized child in the `#40` branch is now `#553` for the remaining shared return-emission admission helper seam.
- 2026-06-15: **`#551` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_outcome_selection(...)` helper plus a narrow `CopperfinRuntimeBridgeOutcomeSelectionAdmission` surface so staged native-return admission output and outcome-selection contracts flow through one scaffold-only helper before return-materialization planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared outcome-selection admission helper path. The next open prompt-sized child in the `#40` branch is now `#552` for the remaining shared return-materialization admission helper seam.
- 2026-06-15: **`#550` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_native_return(...)` helper plus a narrow `CopperfinRuntimeBridgeNativeReturnAdmission` surface so staged interpreted-result admission output and native-return contracts flow through one scaffold-only helper before outcome-selection planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared native-return admission helper path. The next open prompt-sized child in the `#40` branch is now `#551` for the remaining shared outcome-selection admission helper seam.
- 2026-06-15: **`#549` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_interpreted_result(...)` helper plus a narrow `CopperfinRuntimeBridgeInterpretedResultAdmission` surface so staged response-parse admission output and interpreted-result contracts flow through one scaffold-only helper before native-return planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared interpreted-result admission helper path. The next open prompt-sized child in the `#40` branch is now `#550` for the remaining shared native-return admission helper seam.
- 2026-06-15: **`#548` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_admit_response_parse(...)` helper plus a narrow `CopperfinRuntimeBridgeResponseParseAdmission` surface so staged response-validation output and response-parse contracts flow through one scaffold-only helper before interpreted-result planning. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-parse admission helper path. The next open prompt-sized child in the `#40` branch is now `#549` for the remaining shared interpreted-result admission helper seam.
- 2026-06-15: **`#547` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_response_validation(...)` helper plus a narrow `CopperfinRuntimeBridgeResponseValidationEvaluation` surface so staged missing-response output and response-validation contracts flow through one scaffold-only helper before response artifact handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-validation evaluation helper path. The next open prompt-sized child in the `#40` branch is now `#548` for the remaining shared response-parse admission helper seam.
- 2026-06-15: **`#546` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_missing_response(...)` helper plus a narrow `CopperfinRuntimeBridgeMissingResponseEvaluation` surface so staged host-failure output and response-read policy contracts flow through one scaffold-only helper before response artifact handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared missing-response evaluation helper path. The next open prompt-sized child in the `#40` branch is now `#547` for the remaining shared response-validation evaluation helper seam.
- 2026-06-15: **`#545` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_evaluate_host_failure(...)` helper plus a narrow `CopperfinRuntimeBridgeHostFailureEvaluation` surface so staged process-launch output and failure-policy contracts flow through one scaffold-only helper before response validation. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared host-failure evaluation helper path. The next open prompt-sized child in the `#40` branch is now `#546` for the remaining shared missing-response evaluation helper seam.
- 2026-06-15: **`#544` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_launch_process(...)` helper plus a narrow `CopperfinRuntimeBridgeProcessLaunch` surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and deterministic launch outcome contracts flow through one scaffold-only helper before payload construction. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared process-launch helper path. The next open prompt-sized child in the `#40` branch is now `#545` for the remaining shared host-failure evaluation helper seam.
- 2026-06-15: **`#543` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_dispatch(...)` helper plus a narrow `CopperfinRuntimeBridgeDispatchExecution` surface so staged runtime-host executable path, argv, working-directory, log-path, capture-policy, and expected-exit-code contracts flow through one scaffold-only helper before payload construction. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared dispatch-execution helper path. The next open prompt-sized child in the `#40` branch is now `#544` for the remaining shared process-launch helper seam.
- 2026-06-15: **`#542` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_placeholder_return_int(...)` helper so final placeholder integer selection flows through one scaffold-only helper before DLL direct return or FLL `_RetInt(...)` surface application. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-int execution helper path. The next open prompt-sized child in the `#40` branch is now `#543` for the remaining shared dispatch-execution helper seam.
- 2026-06-15: **`#541` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_placeholder_return_value(...)` helper plus a narrow `CopperfinRuntimeBridgePlaceholderReturnValue` surface so stub-return metadata can flow through one scaffold-only placeholder-return contract before final DLL/FLL stub emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared placeholder-return-value execution helper path. The next open prompt-sized child in the `#40` branch is now `#542` for the remaining shared placeholder-return-int execution helper seam.
- 2026-06-15: **`#540` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_stub_return(...)` helper plus a narrow `CopperfinRuntimeBridgeStubReturn` surface so return-activation metadata can flow through one scaffold-only placeholder/deferred stub-return path before placeholder-return-value handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared stub-return execution helper path. The next open prompt-sized child in the `#40` branch is now `#541` for the remaining shared placeholder-return-value execution helper seam.
- 2026-06-15: **`#539` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_activation(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnActivation` surface so final-return-adoption metadata can flow through one scaffold-only active/deferred return-selection path before stub-return handling. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-activation execution helper path. The next open prompt-sized child in the `#40` branch is now `#540` for the remaining shared stub-return execution helper seam.
- 2026-06-15: **`#538` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_final_return_adoption(...)` helper plus a narrow `CopperfinRuntimeBridgeFinalReturnAdoption` surface so return-emission metadata can flow through one scaffold-only adopted-return-block path before return activation. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared final-return-adoption execution helper path. The next open prompt-sized child in the `#40` branch is now `#539` for the remaining shared return-activation execution helper seam.
- 2026-06-15: **`#537` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_emission(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnEmission` surface so return-materialization metadata can flow through one scaffold-only emitted-branch-block path before final-return adoption. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-emission execution helper path. The next open prompt-sized child in the `#40` branch is now `#538` for the remaining shared final-return-adoption execution helper seam.
- 2026-06-15: **`#536` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_return_materialization(...)` helper plus a narrow `CopperfinRuntimeBridgeReturnMaterialization` surface so outcome-selection metadata can flow through one scaffold-only success/fallback native return-statement materialization path before return emission. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared return-materialization execution helper path. The next open prompt-sized child in the `#40` branch is now `#537` for the remaining shared return-emission execution helper seam.
- 2026-06-15: **`#535` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_outcome_selection(...)` helper plus a narrow `CopperfinRuntimeBridgeOutcomeSelection` surface so native-return success/fallback selection can flow through one scaffold-only selected-condition path before return materialization. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared outcome-selection execution helper path. The next open prompt-sized child in the `#40` branch is now `#536` for the remaining shared return-materialization execution helper seam.
- 2026-06-15: **`#534` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_native_return(...)` helper plus a narrow `CopperfinRuntimeBridgeNativeReturn` surface so interpreted success/fallback selection can flow through one scaffold-only native return-value/int-value selection path before outcome selection. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared native-return execution helper path. The next open prompt-sized child in the `#40` branch is now `#535` for the remaining shared outcome-selection execution helper seam.
- 2026-06-15: **`#533` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_interpreted_result(...)` helper plus a narrow `CopperfinRuntimeBridgeInterpretedResult` surface so parsed response status can flow through one scaffold-only success/fallback return-value selection path before native-return materialization. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared interpreted-result execution helper path. The next open prompt-sized child in the `#40` branch is now `#534` for the remaining shared native-return execution helper seam.
- 2026-06-15: **`#532` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_parse_response(...)` helper plus a narrow `CopperfinRuntimeBridgeParsedResponse` surface so staged response documents run through one scaffold-only field-extraction path for the planned `status`, `return_value`, and `diagnostics` fields. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-parse execution helper path. The next open prompt-sized child in the `#40` branch is now `#533` for the remaining shared interpreted-result execution helper seam.
- 2026-06-15: **`#531` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_read_response(...)` helper that falls back to the shared empty-response contract when the staged response artifact is missing or unreadable and otherwise streams the staged response payload through one executable helper path. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared response-read execution helper path. The next open prompt-sized child in the `#40` branch is now `#532` for the remaining shared response-parse execution helper seam.
- 2026-06-15: **`#530` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_runtime_bridge_execute_write_request(...)` helper that creates the staged parent directory when required, writes `plan.request_artifact.request_document` through one executable helper path, and returns deterministic write success for the future wrapper-launch flow instead of leaving request-write execution implicit. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the shared request-write execution helper path. The next open prompt-sized child in the `#40` branch is now `#531` for the remaining shared response-artifact read execution helper seam.
- 2026-06-15: **`#528` is now shipped.** Generated FLL native-wrapper scaffolds now emit `copperfin_build_runtime_bridge_fll_int_return_surface()` just before the FLL-specific helper section and route both FLL stub call sites (placeholder return binding and interpretation plan wrapper_return_surface) through that helper instead of repeating the compound `"_RetInt(int)"` literal. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized FLL return-surface path. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#527` is now shipped.** The DLL generated stub no longer passes the bare `"int"` literal as the wrapper_return_surface argument to `copperfin_build_runtime_bridge_interpretation_plan`; it now calls the existing `copperfin_build_runtime_bridge_native_int_return_surface()` helper. Two existing assertions updated to check for the new helper-routed form. New DLL-stub-specific call-site assertion added in both test files. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#526` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route all 8 JSON field name string literals in `copperfin_build_runtime_bridge_request_document` through named shared helpers (4 new: `payload_shape_field_name`, `parameter_name_field_name`, `parameter_value_field_name`, `parameter_surface_field_name`; plus 4 existing helpers moved earlier) so every request document field name is now behind a named contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized field-name paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#525` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route four bare inline environment variable name literals through named shared helpers (`copperfin_runtime_bridge_library_export_env_var`, `routine_kind_env_var`, `source_path_env_var`, `parameter_count_env_var`) so `copperfin_build_runtime_bridge_launch_plan` consumes named env-var contracts instead of anonymous raw strings while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized env-var paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#524` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route eight bare inline flag string literals through named shared helpers (`copperfin_runtime_bridge_manifest_flag`, `library_export_flag`, `routine_kind_flag`, `source_path_flag`, `source_line_flag`, `parameter_declaration_flag`, `parameter_names_flag`, `parameter_count_flag`) so `copperfin_build_runtime_bridge_invocation` consumes named contracts instead of raw inline literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized flag paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#523` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route six inline policy-bearing literals through shared helpers for observation-plan log-file suffix and exit-code contracts, transport-plan artifact suffix contracts, and return-activation-plan activation-flag contract while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#522` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route six hardcoded `true` boolean literals through shared policy helpers (`capture_stdout`, `capture_stderr`, `fail_on_nonzero_exit`, `fail_on_missing_response`, `ensure_parent_directory`, `require_existing_response`) so all four plan-builder functions consume named policy contracts instead of anonymous `true` literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized boolean policy paths in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#521` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route the `"planned_activation_pending"` and `"replace_placeholder_return"` string literals through shared `copperfin_build_runtime_bridge_planned_activation_pending_mode` and `copperfin_build_runtime_bridge_replace_placeholder_return_mode` helpers, so all four usage sites (plan-builder construction and comparison sites in `copperfin_build_runtime_bridge_stub_return_plan`) consume the shared contracts instead of repeating the raw inline literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized mode-token paths in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-15: **`#520` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now route the typed placeholder return-expression assembly through a shared `copperfin_build_runtime_bridge_typed_native_return_expression` helper, so `copperfin_build_runtime_bridge_return_statement(...)` delegates the prefix-substr + `(` + int_value_representation + `)` construction to that helper instead of repeating the inline tokens while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized typed return-expression construction path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue — create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#519` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared native return-statement framing helper, so `copperfin_build_runtime_bridge_return_statement(...)` routes its plain-`int`, typed placeholder, and representation fallback branches through one shared `return ...;` contract instead of repeating the raw inline framing tokens while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native return-statement framing path in direct pipeline and build-host paths. The next open prompt-sized child in the `#40` branch is now `#520` for the remaining typed native return-expression construction helperization seam.
- 2026-06-14: **`#518` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared native-`int` placeholder-signature helper, so `copperfin_build_runtime_bridge_return_statement(...)` consumes that shared token contract instead of repeating the raw inline `"(int)"` literal when matching typed return surfaces while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native-int placeholder-signature path in direct pipeline and build-host paths. The next open prompt-sized child in the `#40` branch is now `#519` for the remaining native return-statement framing token helperization seam.
- 2026-06-14: **`#517` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared native-`int` return-surface helper, so `copperfin_build_runtime_bridge_return_statement(...)` consumes that shared token contract instead of repeating the raw inline `int` literal when materializing native return statements while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized native-int return-surface path in direct pipeline and build-host paths. The next open prompt-sized child in the `#40` branch is now `#518` for the remaining typed placeholder-signature token helperization seam.
- 2026-06-14: **`#516` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared empty response-document helper, so `CopperfinRuntimeBridgeResponseArtifact` consumes that shared token contract instead of repeating the raw inline empty-string literal while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized empty-document token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#515` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared parsed-int default helper, so `copperfin_parse_runtime_bridge_int_value_representation(...)` consumes that shared token contract instead of repeating the raw inline `-1` sentinel while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized parsed-int sentinel path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#514` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared success/fallback comparator helpers, so `CopperfinRuntimeBridgeOutcomeSelectionPlan` consumes those shared token contracts instead of repeating raw inline comparator literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized comparator token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#513` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared request-write, response-read, and response-parse policy helpers, so `CopperfinRuntimeBridgeRequestWritePlan`, `CopperfinRuntimeBridgeResponseReadPlan`, and `CopperfinRuntimeBridgeResponseParsePlan` consume those shared token contracts instead of repeating raw inline policy literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized IO-policy token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#512` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared request-path, response-path, request-media-type, response-media-type, and schema-version dispatch-argument helpers, so `CopperfinRuntimeBridgeDispatchPlan` consumes those shared token contracts instead of repeating raw inline argv flag literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized dispatch-flag token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#511` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared payload request/response field-name helpers, so `CopperfinRuntimeBridgePayloadPlan` consumes those shared token contracts instead of repeating raw inline field-name literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized payload field-name token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#510` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared serialization schema-version helper, so the serialization plan consumes that shared token contract instead of repeating the raw inline `v1` literal while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized schema-version token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#509` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared request/response serialization media-type helpers, so the serialization plan consumes those shared token contracts instead of repeating raw inline media-type literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized media-type token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#508` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared request/response payload-shape helpers, so the payload builder consumes those shared token contracts instead of repeating raw inline `bridge_request_v1` / `bridge_response_v1` literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized payload-shape token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#507` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared response-status, response-value, and response-diagnostics field-name helpers, so payload and interpretation builders consume those shared token contracts instead of repeating raw inline string literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized response-field token path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#506` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared failure-diagnostics and success-status token helpers, so failure-policy and response-validation builders consume those shared token contracts instead of repeating raw inline string literals while preserving the same scaffold-only behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized token-contract path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#505` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared placeholder return-binding and placeholder return-statement helpers, so exported stubs feed `CopperfinRuntimeBridgeResult`, failure-policy fallback metadata, and final-return adoption from one shared placeholder return contract instead of repeating raw inline `-1` binding and return-statement literals while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helperized placeholder return-contract path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#504` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now derive explicit placeholder-helper active/replacement policy booleans in `CopperfinRuntimeBridgeStubReturnPlan`, route those booleans through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and have the current placeholder-return helper consume them instead of reconstructing the same policy from `activation_mode` / `adoption_mode` string comparisons while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed helper-policy path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#503` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now flatten `activation_mode` and `adoption_mode` into `CopperfinRuntimeBridgeStubReturnPlan`, route those mode contracts through `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and have the current placeholder-return helper consume them alongside the emitted-return, deferred-return, and fallback-value metadata while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed activation/adoption-mode contract path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#502` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now record the deferred return block directly in `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and the current placeholder-return helper now consumes that plan-level deferred-return contract alongside the emitted-return and fallback-value metadata while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed deferred-return-contract path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#501` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now record the placeholder-emission flag and emitted placeholder return statement directly in `CopperfinRuntimeBridgePlaceholderReturnValuePlan`, and the current placeholder-return helper now consumes that plan-level emission contract in addition to the fallback int value while preserving the same disabled-activation placeholder behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the placeholder-plan-routed emission-contract path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#500` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now record placeholder fallback integer/value metadata directly in `CopperfinRuntimeBridgeStubReturnPlan`, and `CopperfinRuntimeBridgePlaceholderReturnValuePlan` now consumes that stub-return metadata instead of reaching back through the deep native-return-plan chain, while preserving the current placeholder-emission behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-routed placeholder-fallback metadata path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#499` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now derive the inactive `deferred_return_block` in `CopperfinRuntimeBridgeStubReturnPlan` from `return_activation_plan.active_return_block` instead of reaching back to final-adoption metadata, while still emitting the placeholder return because activation remains disabled. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the activation-metadata-routed deferred-return path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#498` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry the already-built adopted return block forward as the future `active_return_block` inside `CopperfinRuntimeBridgeReturnActivationPlan`, while keeping activation disabled so the scaffold still emits the placeholder return today. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the seeded active-return-block path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#497` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now record an explicit fallback else-branch statement inside `CopperfinRuntimeBridgeReturnEmissionPlan`, and the emitted return block now composes from the prebuilt success/fallback branch statements instead of re-materializing the fallback return inline, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the branch-composed return-emission path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#496` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now feed the already-parsed `success_int_value` / `fallback_int_value` through `copperfin_build_runtime_bridge_return_statement(...)` for the current `int` / `_RetInt(int)` return surfaces, while preserving the existing representation-based fallback path for any future non-`int` surfaces and keeping the scaffold-only deferred adopted-return behavior intact. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the typed return-materialization path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#495` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now reuse `copperfin_parse_runtime_bridge_int_value_representation(...)` to derive the typed success integer value in `CopperfinRuntimeBridgeNativeReturnPlan` from the existing success representation metadata instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parsed typed-success path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#494` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `copperfin_parse_runtime_bridge_int_value_representation(...)` helper, and `CopperfinRuntimeBridgeNativeReturnPlan` now derives its typed fallback integer value from the existing fallback representation metadata instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parsed typed-fallback path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#493` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now carry a typed fallback integer value in `CopperfinRuntimeBridgeNativeReturnPlan`, and the downstream placeholder-return-value plan now consumes that upstream typed fallback instead of hardcoding `-1`, while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the typed fallback propagation path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#492` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgePlaceholderReturnValuePlan` helper layered on top of the stub-return plan, so each exported stub routes its executable placeholder return through a plan-backed fallback integer value instead of a hardcoded raw literal while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the plan-backed placeholder return path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#491` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit an executable `copperfin_runtime_bridge_emit_stub_return(...)` helper layered on top of the stub-return plan, so each exported stub routes its current placeholder return through a shared helper instead of a raw literal while preserving the scaffold-only deferred adopted-return behavior. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper-routed placeholder return path in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#490` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeStubReturnPlan` helper layered on top of the return-activation plan, so each exported stub carries an explicit contract for which placeholder return statement it emits today versus which adopted return block remains deferred without pretending to execute the host, parse a live response, branch at runtime, or replace the placeholder native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the stub-return-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#489` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnActivationPlan` helper layered on top of the final-return-adoption plan, so each exported stub carries an explicit future contract for whether that adopted return block would become active in the wrapper body without pretending to execute the host, parse a live response, branch at runtime, or replace the placeholder native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-activation-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#488` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeFinalReturnAdoptionPlan` helper layered on top of the return-emission plan, so each exported stub carries an explicit future contract for replacing the current placeholder native return with the planned emitted success/fallback return block without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the final-return-adoption-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#487` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnEmissionPlan` helper layered on top of the return-materialization plan, so each exported stub carries an explicit future contract for the complete conditional success/fallback return block it would emit for DLL/FLL surfaces without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-emission-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#486` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeReturnMaterializationPlan` helper layered on top of the outcome-selection plan, so each exported stub carries an explicit future contract for the exact success/fallback native `return ...;` statements it would materialize for DLL/FLL surfaces without pretending to execute the host, parse a live response, branch at runtime, or substitute the final return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-materialization-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#485` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeOutcomeSelectionPlan` helper layered on top of the native-return plan, so each exported stub carries an explicit future contract for choosing the success vs fallback native return path from parsed status/diagnostics metadata without pretending to execute the host, parse a live response, or branch at runtime yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the outcome-selection-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#484` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeNativeReturnPlan` helper layered on top of the interpreted-result plan and existing return-binding metadata, so each exported stub carries an explicit future contract for mapping success and fallback result values onto the native DLL/FLL return surface without pretending to execute the host or materialize the final native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the native-return-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#483` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInterpretedResultPlan` helper layered on top of the response-parse plan, so each exported stub carries an explicit future contract for combining the parsed response status, wrapper return surface, and fallback return value into a single interpreted-result shape without pretending to execute the host or materialize the final native return yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpreted-result-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#482` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseParsePlan` helper layered on top of the response-artifact builder, so each exported stub carries an explicit future contract for extracting structured response fields from the raw staged response document without pretending to read the filesystem, parse JSON, or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-parse-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#481` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseArtifact` helper layered on top of the response-read plan, so each exported stub carries an explicit future contract for the raw staged response document that would be recovered after runtime-host launch without pretending to read the filesystem, parse the response, or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-artifact-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#480` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseReadPlan` helper layered on top of the request-write plan, so each exported stub carries an explicit future contract for where and how the staged response document would be read back after runtime-host launch without pretending to touch the filesystem, spawn the host, or parse the response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-read-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#479` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeRequestWritePlan` helper layered on top of the request-artifact builder, so each exported stub carries an explicit future contract for where and how the serialized request document would be written before runtime-host launch without pretending to touch the filesystem or spawn the host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-write-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#478` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeRequestArtifact` model plus request-document/request-artifact helpers layered on top of the response-validation plan, so each exported stub carries an explicit future contract for the serialized request document that would be written to the planned request-path artifact without pretending to spawn the host or consume a response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the request-artifact-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#477` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeResponseValidationPlan` helper layered on top of the host-failure policy plan, so each exported stub carries an explicit future contract for response media-type/schema validation, required response fields, and success-status expectations without pretending to execute the host or parse a real response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the response-validation-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#476` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeFailurePolicyPlan` helper layered on top of the result-interpretation plan, so each exported stub carries an explicit future contract for handling non-zero runtime-host exits and missing responses via deterministic diagnostics fallback (`runtime_host_failure`) plus placeholder return fallback wiring without pretending to execute the host or consume a real response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the failure-policy-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#475` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInterpretationPlan` helper layered on top of the payload plan, so each exported stub carries an explicit future contract for mapping runtime-host response fields onto the wrapper return surface without pretending to execute or deserialize the host response yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the interpretation-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#474` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgePayloadPlan` helper layered on top of the dispatch plan, so each exported stub carries an explicit future request/response envelope-shape contract (named payload shapes plus required fields) without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the payload-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#473` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeDispatchPlan` helper layered on top of the serialization plan, so each exported stub carries an explicit future runtime-host command-line contract for request/response artifact paths and encoding metadata without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the dispatch-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#472` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeSerializationPlan` helper layered on top of the transport plan, so each exported stub carries an explicit future request/response encoding contract (media types plus schema version) without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the serialization-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#471` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeTransportPlan` helper layered on top of the execution plan, so each exported stub carries an explicit future request/response artifact contract without pretending to serialize payloads or spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the transport-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#470` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeExecutionPlan` helper layered on top of the observation plan, so each exported stub carries an explicit future executable-path, argv, and output-capture contract without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the execution-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#469` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeObservationPlan` helper layered on top of the launch plan, so each exported stub carries explicit future stdout/stderr log paths plus an expected exit-code contract without pretending to spawn or observe the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the observation-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#468` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeEnvironmentVariable` / `CopperfinRuntimeBridgeLaunchPlan` helpers layered on top of the bridge result model, so each exported stub carries an explicit future working-directory and environment contract (`COPPERFIN_LIBRARY_EXPORT`, routine/source metadata, and parameter-count context) without pretending to spawn the runtime host yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the launch-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#467` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeReturn` / `CopperfinRuntimeBridgeResult` helpers layered on top of the bridge call model, so each exported stub carries an explicit future result-marshalling contract (`int` for DLL/OCX placeholder returns and `_RetInt(int)` for FLL placeholder returns) without pretending to execute or marshal the real runtime-host result yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the return-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#466` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit shared `CopperfinRuntimeBridgeParameter` / `CopperfinRuntimeBridgeCall` helpers layered on top of the invocation plan, so DLL stubs record their actual placeholder integer arguments and FLL stubs record the honest `ParamBlk*` call-surface pointer as a future binding contract without pretending to marshal or execute the runtime-host call yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the parameter-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#465` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeInvocation` helper layered on top of the bridge descriptors, producing an explicit future runtime-host argv contract (`--manifest`, `--library-export`, routine/source metadata, and parameter metadata) for each exported routine without pretending the wrapper executes that plan yet. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the invocation-plan-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#464` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now emit a shared `CopperfinRuntimeBridgeDescriptor` helper that captures the exported routine identity, source metadata, parameter metadata, and the package-local manifest/runtime-host paths derived from the loaded wrapper module, so the scaffold carries an explicit future runtime-dispatch contract instead of leaving that bridge shape implicit. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the descriptor-bearing wrapper source in direct pipeline and build-host paths. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#463` is now shipped.** Generated DLL/FLL native-wrapper scaffolds now derive sibling `app.cfmanifest` and `copperfin_runtime_host(.exe)` paths from the loaded module location, the host-toolchain compile check now matches the emitted CMake visibility policy, and native export-contract validation now ignores weak toolchain leakage so DLL/FLL wrapper builds are checked against only the deliberate exported callable surface. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the helper-bearing wrapper source, generated CMake metadata, compile/build validation, and export-contract preservation before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#462` is now shipped.** Runtime debug manifests now mirror the remaining `feature_flag=` lines from the broad runtime manifest for DLL/FLL package plans, including launcher, host-compatibility, build-output, debug, and security flags, so downstream debug tooling can recover the same feature-rollout contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest feature-flag contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#461` is now shipped.** Runtime debug manifests now mirror the library-output language-integration summary, AI-feature summary, extensibility-guardrail summary, and the aggregate `language_integrations=` / `ai_features=` counts from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same extensibility-summary contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest language/AI-summary contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#460` is now shipped.** Runtime debug manifests now mirror the library-output `.NET` policy summary fields (`dotnet_enabled=`, `dotnet_story=`, allowlist/denylist summary and item lines, parity-matrix summary and item lines, and gateway decision lines) from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same extensibility-policy contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest `.NET` policy-summary contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#459` is now shipped.** Runtime debug manifests now mirror the library-output `runtime_host_sha256=` and `security_roles=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same integrity/security-summary contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest security-summary contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#458` is now shipped.** Runtime debug manifests now mirror the library-output `security_enabled=`, `security_role=`, `security_mode=`, and `audit_log_path=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same security contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest security-metadata contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#454` has been corrected on `main`.** The current debug-manifest builder now actually emits the library-output `package_root=` field for DLL/FLL package plans, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now directly proves that contract before and after materialization. Continue taking the next prompt-sized child issue under `#40` instead of assuming earlier closure notes remain self-validating.
- 2026-06-14: **`#457` is now shipped.** Runtime debug manifests now mirror the library-output `transpiled_csharp_path=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same transpilation-artifact path contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest transpiled-C#-path contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#456` is now shipped.** Runtime debug manifests now mirror the library-output `ast_manifest_path=` and `ir_manifest_path=` fields from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same compiler-artifact path contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest AST/IR-path contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#455` is now shipped.** Runtime debug manifests now mirror the library-output `content_root=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same package-content contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest content-root contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#454` is now shipped.** Runtime debug manifests now mirror the library-output `package_root=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same package-location contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest package-root contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#453` is now shipped.** Runtime debug manifests now mirror the library-output `project_path=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same project-location contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest project-path contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#452` is now shipped.** Runtime debug manifests now mirror the library-output `project_title=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same project-identity contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest project-title contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#451` is now shipped.** Runtime debug manifests now mirror the library-output `configuration=` field from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same build-configuration contract without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest configuration contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#450` is now shipped.** Runtime debug manifests now mirror the existing `warning=` lines from the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can see no-export and other package-plan diagnostics without reparsing only the runtime manifest. Focused `test_runtime_pipeline` coverage now proves the library-output warning path for both DLL and FLL debug manifests. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#449` is now shipped.** Runtime debug manifests now mirror the staged `asset=` inventory lines already emitted by the broad runtime manifest for DLL/FLL package plans, so downstream debug tooling can recover the same wrapper/source/supporting-file inventory without reparsing only the runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest asset-inventory contract before and after materialization. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#448` is now shipped.** Runtime debug manifests now mirror `extension_payload=` digest lines, including the built DLL/FLL primary output payload after materialization, so downstream debug tooling can verify the same packaged-output digest surface already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest extension-payload contract. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#447` is now shipped.** Runtime debug manifests now mirror the library-output feature flags (`build.output.library_contract`, `build.output.native_library_wrapper`, and `build.output.fll_api_contract` for FLL builds), so downstream debug tooling sees the same build-contract capability markers already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest feature-flag contract. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#446` is now shipped.** Runtime debug manifests now mirror `compiler_contract=` digest lines, including the generated library contract artifacts for DLL/FLL outputs, so downstream debug tooling can verify the same contract-file integrity surface already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest compiler-contract digest contract. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#445` is now shipped.** Runtime debug manifests now mirror the discovered library `export_symbol=` contract for DLL/FLL outputs, so downstream debug tooling can recover the same exported-routine surface already exposed through the broader runtime manifest without reparsing only the `.def` / API artifacts. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest export contract. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#444` is now shipped.** Runtime debug manifests now record `primary_output_path` and `primary_output_materialized` for library outputs, so both the scaffolded pre-build state and the materialized build-host/runtime-pipeline state remain visible through the debug contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the DLL/FLL debug-manifest path/state contract. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#443` is now shipped.** Runtime debug manifests now record `module_definition_path` for DLL/FLL outputs and `fll_api_manifest_path` for FLL outputs, so downstream debug tooling can recover the same generated artifact-path contract already exposed through the broader runtime manifest. Focused `test_runtime_pipeline` coverage now proves both the DLL and FLL debug-manifest paths are emitted. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#442` is now shipped.** The broad runtime/debug manifests now mirror the existing FLL per-function contract metadata (`library_function_kind`, source provenance, parameter declaration style, parameter names, arity, and callable surface) instead of leaving that detail only in the dedicated `.fll.api` contract. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the FLL wrapper/build-host path surfaces that callable metadata through the broader manifests as well. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#441` is now shipped.** DLL/FLL exported-routine contracts now preserve whether each routine used `PARAMETERS` or `LPARAMETERS`, and the generated FLL `CopperfinFoxInfoRecord` scaffold now carries that declaration-style metadata beside routine kind, source provenance, parameter names, and arity. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove `InitLibrary` preserves `lparameters` while `AddNumbers` preserves `parameters` through the wrapper/build-host path. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#440` is now shipped.** The generated FLL `CopperfinFoxInfoRecord` scaffold now carries per-routine parameter-name metadata alongside routine kind, source path, source line, and arity, so the honest FLL registration surface preserves more callable-contract detail without claiming full VFP loader ABI parity. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove `InitLibrary` and `AddNumbers` carry the expected `tcMode` and `tnLeft|tnRight` metadata in the generated wrapper source. The `#40` branch has no open prompt-sized child queue again, so create the next native child issue under `#40` before taking more build-output work there.
- 2026-06-14: **`#193` is now shipped.** `copperfin_runtime_host` now requires the existing `ai.mcp` role permission before it will enter AI-assisted federation-planning mode, using `COPPERFIN_SECURITY_ROLE` with a default of `developer`, so optional AI planning no longer bypasses the native security profile. Focused `tests/test_runtime_host_debug_output.cpp` coverage now adds `test_runtime_host_rejects_ai_federation_planning_without_ai_permission()`, and both `test_runtime_host_debug_output` and `test_federation_execution` pass from `build/`. The `#190`-`#193` security queue is now exhausted, so choose the next prompt-sized slice from the remaining open blocker tree rather than continuing this security branch.
- 2026-06-14: **`#192` is now shipped.** `copperfin_runtime_host` now requires security-verified `extension_payload` entries to rebind through their recorded package-relative paths instead of falling back to a same-named file at the package root, so nested payload digests can no longer be satisfied by basename collisions outside the recorded location. Focused `tests/test_runtime_host_debug_output.cpp` coverage now adds `test_runtime_host_rejects_extension_payload_basename_fallback()`, and both `test_runtime_host_debug_output` and `test_runtime_host_binding` pass from `build/`. The next open slice in the `#190`-`#193` security queue is now `#193`.
- 2026-06-14: **`#191` is now shipped.** `copperfin_runtime_host` now reports the effective rebound audit sink through `security.audit_log_path:` during generated-app startup, so deployed packages can see which package-local audit log is active after manifest rebinding. The dedicated `test_runtime_host_binding` smoke now proves both that the rebound `security_audit.log` file is written under the deployed package root and that the runtime host reports that rebound path in stdout. The next open slice in the `#191`-`#193` security queue is now `#192`.
- 2026-06-14: **`#190` is now shipped.** `resolve_secret_reference()` now rejects malformed `env:` variable names containing whitespace, control characters, or `=`, so secret references fail deterministically as invalid input instead of falling through as ambiguous missing-variable lookups. Focused `tests/test_security_controls.cpp` coverage now adds `test_secret_provider_rejects_malformed_env_var_names()`, and `test_security_controls` passes from `build/`. The next open slice in the `#190`-`#193` security queue is now `#191`.
- 2026-06-14: **`#203` is now shipped.** Declared-DLL invocation now preserves bare caller `@var` references through expression parsing, native Windows `DECLARE` calls now keep non-by-ref string buffers alive across the call boundary, and native `STRING @` / numeric by-ref parameters now write back into Copperfin variables after the DLL returns when both the declaration and callsite are by-ref. Focused `tests/test_prg_engine_seek_index.cpp` coverage now adds `test_declared_dll_string_byref_argument_writeback()` to prove `DECLARE INTEGER lstrcpyA(STRING @, STRING)` mutates `@cBuffer` end to end. Upstream issue state now shows the `#200`-`#203` child queue is exhausted, so choose the next prompt-sized slice from the remaining open blocker tree rather than continuing that interop branch.
- 2026-06-14: **`#187` is now shipped.** Runtime federation planning now supports policy-driven AI planning integration: `FederationExecutionRequest` carries `enable_ai_assistance`, `require_ai_assistance`, and `policy_audit_enabled`; `build_federation_execution_plan()` now emits deterministic planning-metadata fields (`planning_mode`, `ai_assisted`, `deterministic_translation_succeeded`, `planning_policy_allows_ai`, `planning_policy_audit_enabled`) and the runtime host now exposes `--federation-planning-enable`, `--federation-planning-require`, and `--federation-planning-audit` for execution-policy control. `tests/test_federation_execution.cpp` now includes `test_plan_rejects_with_ai_policy_disabled`, `test_plan_allows_ai_fallback_metadata`, `test_plan_requires_ai_fallback_metadata`, and `test_plan_tracks_policy_audit_toggle`.
- 2026-06-14: **`#439` is now shipped.** The generated FLL `CopperfinFoxInfoRecord` scaffold now carries routine kind plus source-path/source-line fields instead of only name/entrypoint/arity, and the generated wrapper now populates that richer metadata for each exported routine. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the enriched `FoxInfo` table survives direct pipeline materialization and the build-host path without claiming full VFP loader ABI parity.
- 2026-06-14: **`#438` is now shipped.** DLL/FLL callable contracts now preserve the declaring PRG file path and source line for each exported routine, and the dedicated DLL/FLL manifests plus the broader runtime manifest now emit `function_source=` / `library_function_source=` lines for that provenance. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the source-provenance metadata survives the wrapper/build-host path end to end without claiming full source-level debugging parity.
- 2026-06-14: **`#437` is now shipped.** DLL/FLL callable contracts now preserve whether each exported PRG routine originated from `PROCEDURE` or `FUNCTION` instead of flattening everything into a generic function-shaped surface, and the dedicated DLL/FLL manifests plus the broader runtime manifest now emit `function_kind=` / `library_function_kind=` lines for that metadata. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the routine-kind metadata survives the wrapper/build-host path end to end without claiming full return-type parity.
- 2026-06-14: **`#436` is now shipped.** DLL/OCX wrapper stubs now preserve declared PRG `PARAMETERS` / `LPARAMETERS` names in their generated callable signatures instead of generic `argN` placeholders, and the dedicated DLL/FLL API manifests plus the broader runtime manifest now record those per-routine parameter-name lists through `function_parameters=` / `library_function_parameters=` lines. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove those names survive the wrapper/build-host path end to end without pretending full type-marshalling parity is already complete.
- 2026-06-14: **`#435` is now shipped.** DLL/OCX package plans now emit a standalone `.dll.api`-style contract artifact beside the generated wrapper assets, that contract records export names, VFP calling convention, routine arity, and placeholder call-surface details, and runtime/debug manifests plus build-host output now report its path explicitly. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the built DLL export surface stays synchronized with that dedicated contract instead of only the broader runtime manifest.
- 2026-06-14: **`#434` is now shipped.** Generated DLL/OCX wrappers now accept placeholder arguments matching discovered PRG `PARAMETERS` / `LPARAMETERS` arity instead of always using no-argument entrypoints, and runtime/build manifests now record per-routine `library_function_arity=` plus `library_function_call_surface=` contract lines beside the earlier VFP calling-convention marker. Focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the DLL wrapper/build-host path preserves that arity-shaped callable contract without pretending full type-marshalling parity is already complete.
- 2026-06-14: **`#433` is now shipped.** Generated DLL/OCX wrappers now emit an explicit `COPPERFIN_VFP_DLL_CALL` macro that maps to `__stdcall` on 32-bit Windows and a neutral fallback elsewhere, and non-FLL library routine stubs now use that contract directly. Runtime/build manifests now record `library_callable_convention=vfp_declare_default`, and focused `test_runtime_pipeline` plus `test_build_host_output` coverage now both prove the DLL wrapper/build-host path preserves that calling-convention contract without pretending full marshalling parity is already complete.
- 2026-06-14: **`#432` is now shipped.** Generated FLL wrappers now move their callable surface toward the FoxPro/VFP API model by emitting `ParamBlk*` routine stubs that route placeholder returns through `_RetInt`, while the emitted FLL API manifest now records the callable signature, default return helper, and per-function call-surface lines. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove that richer callable contract survives the wrapper/build-host path without pretending the full FoxPro/VFP runtime API is already implemented.
- 2026-06-14: **`#431` is now shipped.** Generated FLL wrappers now emit a minimal VFP-shaped registration contract: a `FoxInfo()` loader entrypoint returning an exported `_FoxTable` registration symbol backed by per-routine metadata, while the emitted `.def` and `.fll.api` contracts now declare that richer registration surface explicitly. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the wrapper/build-host path preserves the expanded FLL export contract end to end without pretending the full FoxPro/VFP runtime-loader ABI is already complete.
- 2026-06-14: **`#41` is now closed.** The dedicated child queue is exhausted after `#210`, `#211`, `#429`, and `#430`, covering FXP/APP output-kind planning, staged contract emission, honest APP archive materialization, and honest FXP contract-file materialization with focused pipeline plus build-host coverage.
- 2026-06-14: **`#430` is now shipped.** FXP packages now materialize a deterministic Copperfin logical-statement contract file at the requested `.fxp` output path, record it as the primary output plus an extension payload digest, and keep the token manifest as the source-of-truth contract without pretending the result is already a FoxPro/VFP-native FXP binary. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the emitted `.fxp` contract preserves the tokenized statement data end to end.
- 2026-06-14: **`#429` is now shipped.** APP packages now materialize a deterministic Copperfin content-archive file at the requested `.app` output path, record it as the primary output plus an extension payload digest, and keep the staged-content manifest as the source-of-truth contract without pretending the result is already a FoxPro/VFP-native APP binary. Focused `test_runtime_pipeline` and `test_build_host_output` coverage now both prove the emitted archive carries the staged startup/supporting asset payloads end to end.
- 2026-06-14: **`#428` is now shipped.** `copperfin_build_host` now invokes `build_runtime_package_primary_output(...)` for materialized DLL/FLL/OCX-style packages before reporting success, so the real build-host entrypoint now materializes the requested native library artifact, rewrites the runtime/debug manifests to the built output, and reports `primary_output_materialized=true`. The new process-level `test_build_host_output` smoke proves both DLL and FLL builds produce the requested primary output path and preserve their declared export contracts through the host path itself.
- 2026-06-14: **`#427` is now shipped.** The runtime pipeline now exposes `build_runtime_package_primary_output(...)` for materialized library-output packages, the helper drives the generated wrapper project into the requested primary output path, marks `primary_output_materialized=true`, records the built artifact as an extension payload, and rewrites the runtime/debug manifests to match, while `test_runtime_pipeline` now proves both DLL and FLL package builds preserve their declared export contracts through that pipeline helper.
- 2026-06-14: **`#426` is now shipped.** Library-output packages now emit turnkey `build_wrapper.sh` and `build_wrapper.ps1` scripts beside the generated wrapper source/CMake metadata, runtime/debug manifests now record those script paths, and `test_runtime_pipeline` now proves the scripts are emitted, describe the intended CMake invocation, and that the POSIX script can materialize the requested primary output path while preserving the existing export-contract checks.
- 2026-06-14: **`#425` is now shipped.** The emitted wrapper-local `CMakeLists.txt` now routes generated DLL/FLL builds to the package root via explicit output-directory properties, and `test_runtime_pipeline` now proves the generated-CMake path materializes the requested primary output path itself while preserving the filename-identity and export-contract checks, moving the honest library-output pipeline closer to a usable package layout without overstating loader compatibility.
- 2026-06-14: **`#424` is now shipped.** The emitted wrapper-local `CMakeLists.txt` now preserves the requested DLL/FLL artifact basename and suffix (`PREFIX ""`, requested `.dll` / `.fll` suffix), and `test_runtime_pipeline` now proves the generated-CMake path produces `LibraryDemo.dll` / `LibraryDemo.fll` while keeping the existing export-contract checks intact, moving the honest library-output pipeline one step closer to FoxPro/VFP-shaped deliverables without overstating loader compatibility.
- 2026-06-14: **`#423` is now shipped.** `test_runtime_pipeline` now inspects the shared-library artifacts produced through the emitted wrapper-local `CMakeLists.txt` and proves those generated-CMake DLL/FLL smoke artifacts export exactly the symbol sets declared by the generated `.def` / `.fll.api` contracts, closing the gap between wrapper-source validation and wrapper-project validation without overstating full FoxPro/VFP loader compatibility.
- 2026-06-14: **`#422` is now shipped.** `test_runtime_pipeline` now configures and builds the emitted wrapper-local `CMakeLists.txt` through `cmake -S` / `cmake --build` and proves the generated wrapper project produces the expected shared-library artifact under host CMake/toolchain support, so the build pipeline now validates the emitted wrapper build metadata itself rather than only compiling the wrapper source directly.
- 2026-06-14: **`#421` is now shipped.** FLL contracts now explicitly declare `FoxInfo` in both the generated `.def` and `.fll.api` outputs, and `test_runtime_pipeline` now proves the compiled DLL/FLL wrapper smoke artifacts export exactly the same symbol sets declared by the generated contract files, tightening the library-output pipeline around a single consistent export surface without overstating full FoxPro/VFP loader compatibility.
- 2026-06-14: **`#420` is now shipped.** `test_runtime_pipeline` now inspects host-built DLL/FLL wrapper smoke artifacts with `nm -D --defined-only` and proves the compiled stubs actually export the expected entrypoints (`InitLibrary`, `AddNumbers`, and `FoxInfo` for FLL), so the build pipeline now validates symbol-surface fidelity in addition to scaffold emission and compileability without overstating full FoxPro/VFP loader compatibility.
- 2026-06-14: **`#419` is now shipped.** `test_runtime_pipeline` now detects a host C++ toolchain and proves the generated DLL/FLL wrapper scaffolds compile as shared-library stubs, so the build pipeline now validates the emitted native wrapper sources are buildable without overstating them as FoxPro/VFP-compatible runtime bridges.
- 2026-06-14: **`#418` is now shipped.** Library-style runtime packages now emit an honest generated native-wrapper scaffold (`wrapper/<stem>_wrapper.cpp`) plus wrapper-local `CMakeLists.txt` build metadata alongside the existing `.def` / `.fll.api` contracts, and runtime/debug manifests now record those wrapper artifact paths plus a `build.output.native_library_wrapper` feature flag. Focused `test_runtime_pipeline` coverage now proves the DLL/FLL wrapper contract end to end without pretending the final FoxPro/VFP-compatible binary bridge already exists.
- 2026-06-14: **`#21` is now closed.** The dedicated D3 child queue is exhausted after `#166` and `#167`, and the shipped follow-on workflow slices `#206`, `#207`, `#278`, and `#403` now cover native entrypoint packaging, deployed-package rebinding, launcher routing/reporting, and argument-forwarding fidelity across the current workflow baseline.
- 2026-06-14: **`#20` is now closed.** The dedicated debugger child queue is exhausted after `#164`, `#165`, `#404`, `#405`, `#406`, `#407`, `#408`, `#411`, `#412`, `#413`, `#414`, `#415`, and `#416`, covering watch evaluation, locals/pause metadata, breakpoint management, richer stepping, cursor/runtime inspection, and xAsset-to-debugger linkage.
- 2026-06-14: **`#38` is now closed.** The dedicated round-trip child queue is exhausted after `#204`, `#205`, and `#417`, covering SCX/VCX, FRX/LBX/MNX, and PJX/PJT plus DBC/DCT reopenability and undo fidelity through focused visual-asset regression coverage.
- 2026-06-14: **`#417` is now shipped.** `visual_asset_editor` now infers `.dct` memo sidecars for `.dbc` assets alongside the existing `.pjt` project pairing, and focused `test_visual_asset_editor` coverage now proves representative direct-field plus memo-backed edits on synthetic `PJX/PJT` and `DBC/DCT` fixtures remain readable and fully undo back to their original serialized values.
- 2026-06-14: **`#42` is now closed.** The dedicated AST/IR child queue is exhausted after `#212` and `#213`, covering durable AST schema fidelity plus IR opcode/instruction mapping alongside selected runtime outputs, while the broader compiler/runtime contract now also records deterministic artifact digests through `#410`. Remaining `#19` work stays under `#40` and `#41`.
- 2026-06-14: **`#14` is now closed.** The dedicated debugger-metadata/diagnostics child queue is exhausted after `#144`, `#145`, `#152`, `#153`, `#402`, and `#409`, covering stack-frame/source-line fidelity, developer-facing diagnostic normalization, critical-section enforcement, and the centralized engine concurrency policy. Remaining broader debugger feature work continues under `#20`.
- 2026-06-14: **`#409` is now shipped.** Copperfin now has a centralized `docs/25-engine-concurrency-policy.md` defining named in-memory critical sections, ascending normalized lock ordering, and the "no blocking while holding a critical section" rule, while the runtime source now points directly at that policy beside the shared enforcement helper. Focused `test_prg_engine_table_mutation` coverage now proves an explicit contested `RLOCK()` fails fast under the policy without emitting retry-backoff events, and adjacent `test_prg_engine_control_flow` coverage still passes.
- 2026-06-14: **`#416` is now shipped.** `copperfin_runtime_host` now enriches ordinary pause-state breakpoint listings with `debug.breakpoint[n].xasset.action_id` and `debug.breakpoint[n].xasset.title` when an active breakpoint maps back to an xAsset action, so debugger clients can recover designer-facing breakpoint identity directly from pause output without a separate `break:list` round-trip. `test_runtime_host_debug_output` now proves that metadata appears during ordinary xAsset pause output with no explicit inventory command, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#415` is now shipped.** `copperfin_runtime_host` now enriches breakpoint inventory output with `debug.breakpoint[n].xasset.action_id` and `debug.breakpoint[n].xasset.title` when a listed breakpoint maps back to an xAsset action, reusing the same executable-model/bootstrap resolver already used for action-id breakpoint commands. `test_runtime_host_debug_output` now proves `break:list` round-trips xAsset action metadata for those breakpoints, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#414` is now shipped.** `copperfin_runtime_host` now supports `break:add-action:<action-id>` and `break:remove-action:<action-id>` during xAsset bootstrap debug sessions, reusing the existing executable-model action bindings plus generated bootstrap source to resolve action ids into real engine breakpoints without exposing bootstrap file/line details to debugger clients. `test_runtime_host_debug_output` now proves add/remove-by-action-id behavior end to end, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#413` is now shipped.** `copperfin_runtime_host` now supports `break:remove:<file:line>` during live debug sessions, while `PrgRuntimeSession` now exposes an engine-backed `remove_breakpoint()` path using the same normalized breakpoint identity as add/list. `test_runtime_host_debug_output` now proves removing one breakpoint leaves unrelated breakpoints active and still pauses on the surviving line, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#412` is now shipped.** `copperfin_runtime_host` now supports `break:add:<file:line>`, `break:clear`, and `break:list` during live debug sessions, while `PrgRuntimeSession` now exposes a deduplicated engine-backed breakpoint inventory through `list_breakpoints()`. `test_runtime_host_debug_output` now proves add/list/clear plus a managed-breakpoint pause without restarting the host, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#411` is now shipped.** `copperfin_runtime_host` now maps paused xAsset bootstrap frames back onto the executable-model action binding and prints `debug.xasset.action_id`, `debug.xasset.record_index`, `debug.xasset.kind`, and `debug.xasset.title` alongside the existing pause metadata, so debugger pauses inside generated SCX/VCX/MNX bootstrap routines can be mapped back to the originating designer-facing action. The new `test_runtime_host_debug_output` process-level smoke proves a breakpoint inside a nested SCX action reports that identity end to end, and adjacent `test_runtime_host_binding` / `test_xasset_methods` coverage still passes.
- 2026-06-14: **`#410` is now shipped.** Materialized runtime packages now record deterministic `compiler_contract=` digest lines for generated contract artifacts such as `.ast.json`, `.ir.json`, `.transpiled.cs`, and output-kind-specific contract files when present, so downstream tooling can verify that copied package contents still match the declared compiler/runtime contract. Focused `test_runtime_pipeline` coverage now proves the digest entries for generated AST/IR/C# artifacts end to end.
- 2026-06-14: **`#215` is now shipped.** The transpilation artifact now also emits compileable C# scaffolding for VCX/SCX-style class/object assets using the existing xAsset executable model, root-object lifecycle methods and nested object methods now surface as deterministic C# members with ordered `RunStartup()` / `RunShutdown()` wrappers, untranslated xAsset bodies stay honest via explicit manual-port exceptions, and focused `test_runtime_pipeline` coverage now proves both the emitted class/object scaffold and a direct `dotnet build` compile check for the generated artifact.
- 2026-06-14: **`#214` is now shipped.** Runtime packages now emit a target-specific `.transpiled.cs` artifact whenever `.NET`-oriented output is requested, the generated source now maps first-pass procedural PRG constructs (`LOCAL`, assignment, `DO`, `WAIT WINDOW`, and `RETURN`) into deterministic C# scaffolding with stable routine naming, the runtime manifest now records that artifact path plus a `build.output.csharp_transpilation` feature flag, and focused `test_runtime_pipeline` coverage now proves both the emitted source contract and a direct `dotnet build` compile check for the generated artifact.
- 2026-06-14: **`#213` is now shipped.** Runtime packages now emit a target-specific `.ir.json` artifact alongside the selected output, the IR schema records PRG-relative files, routines, stable statement opcode names, and mapped instruction operands/metadata derived from parsed statements, and the runtime manifest now records that IR artifact path as part of the shared compiler contract.
- 2026-06-14: **`#212` is now shipped.** Runtime packages now emit a target-specific `.ast.json` artifact alongside the selected output, the AST schema records project title, output kind, PRG-relative paths, routines, and logical statement line/text metadata, and the runtime manifest now records that AST artifact path as part of the compiler contract.
- 2026-06-14: **`#211` is now shipped.** Project workspaces now infer `.app` outputs as Visual FoxPro application archives, runtime package plans now emit a `.app.contents` contract only for APP targets, and materialized APP packages now record staged content membership plus startup fidelity while honestly recording that the primary APP archive is still not materialized.
- 2026-06-14: **`#210` is now shipped.** Project workspaces now infer `.fxp` outputs as Visual FoxPro tokenized programs, runtime package plans now emit a `.fxp.tokens` contract only for FXP targets, and materialized FXP packages now capture parsed PRG logical statements by scope/line/text while honestly recording that the primary FXP binary is still not materialized.
- 2026-06-14: **`#209` is now shipped.** Project workspaces now infer `.fll` outputs as Visual FoxPro libraries, runtime package plans now emit a `.fll.api` contract only for FLL targets, and materialized FLL packages now carry both the `.def` export map and a `SET LIBRARY TO` / `RELEASE LIBRARY`-oriented API manifest populated from discovered PRG `PROCEDURE` / `FUNCTION` names while honestly recording that the primary FLL binary is still not materialized.
- 2026-06-14: **`#208` is now shipped.** Project workspaces now infer library-style output kinds from non-`.exe` `OUTFILE` targets, runtime package plans now preserve the configured library filename instead of forcing `.exe`, and DLL/FLL/OCX outputs now switch into an explicit `foxpro_library_definition` packaging mode that emits a `.def` export map from discovered PRG `PROCEDURE` / `FUNCTION` names while honestly recording that the primary DLL binary is still not materialized.
- 2026-06-14: **`#207` is now shipped.** `copperfin_runtime_host` now rebinds stale manifest paths back onto the deployed package root for startup source, working directory, audit log, and executable-payload hash verification, so moved native packages no longer depend on build-time absolute paths or the caller's current working directory. The new `test_runtime_host_binding` CTest smoke proves a relocated security-enabled package with nested payloads still launches successfully from an unrelated working directory.
- 2026-06-14: **`#206` is now shipped.** Native-host packages now materialize a project-named executable entrypoint beside `app.cfmanifest` and `copperfin_runtime_host.exe`, the package manifest now records that entrypoint as a hashed executable payload, focused `test_runtime_pipeline` coverage proves the native-fallback package emits and records it, and a direct copied-entrypoint smoke run proves `copperfin_runtime_host` auto-discovers a sibling manifest when launched without `--manifest`.
- 2026-06-14: **`#205` is now shipped.** Focused `test_visual_asset_editor` coverage now proves synthetic `LBX/LBT` and `MNX/MNT` assets remain readable after paired direct-field plus memo-field edits, fully undo back to their original serialized values, and leave no lingering undo residue, extending the memo-backed asset round-trip contract across the remaining report/menu families beside the existing `FRX` regression.
- 2026-06-14: **`#204` is now shipped.** Focused `test_visual_asset_editor` coverage now proves a synthetic `VCX/VCT` class library remains readable after adding a new serialized property, preserves its pre-existing serialized properties, and cleanly undoes the added property without leaving undo residue, locking class-library round-trip safety down beside the existing `SCX` regression.
- 2026-06-14: **`#408` is now shipped.** `PrgRuntimeSession` now exposes watch-expression evaluation against the current paused frame/global/cursor context, `copperfin_runtime_host` now accepts `--debug-command watch:<expr>` without resuming execution, focused `test_prg_engine` coverage proves local/global/cursor-field watch evaluation plus deterministic empty-watch handling, and a direct runtime-host smoke invocation confirms `watch:nLocal` reports the paused worker-frame value.
- 2026-06-14: **`#278` is now shipped.** Runtime/debug manifests now record the effective launcher mode plus fallback reason, emit explicit feature-flag lines for rollout/host-compatibility/debug/security gates, and requested `.NET` launcher emission now falls back deterministically to the native runtime host when `.NET` output is unavailable. Focused `test_runtime_pipeline` coverage proves both the `.NET` route and native-fallback route, and `test_managed_compile` passes.
- 2026-06-14: **`#407` is now shipped.** Focused `test_prg_engine` coverage now proves `step_into`, `step_over`, and `step_out` preserve the selected work area, alias map, and open-cursor inspection state alongside the existing statement-text and frame-routing contract across a simple `DO worker` flow.
- 2026-06-14: **`#406` is now shipped.** Focused `test_prg_engine` coverage now proves a breakpoint pause preserves the selected work area, alias map, and open-cursor inspection state (work area, record position, record count, and source path) alongside the existing statement-text contract.
- 2026-06-14: **`#405` is now shipped.** Focused `test_prg_engine` coverage now proves `step_into`, `step_over`, and `step_out` each pause with `DebugPauseReason::step` and preserve the expected current/next statement text plus caller-vs-callee frame routing across a simple `DO worker` flow.
- 2026-06-14: **`#404` is now shipped.** Breakpoint pauses now have explicit regression coverage proving the debug state preserves the current statement text alongside the existing line/frame/locals contract. Focused `test_prg_engine` coverage locks both the ordinary in-routine breakpoint and the first-line breakpoint case down directly.
- 2026-06-14: **`#403` is now shipped.** The generated .NET launcher now preserves ordinary application arguments instead of dropping everything except `--debug`, while keeping the manifest injection and debug-flag normalization contract intact. Focused `test_runtime_pipeline` coverage locks that launcher source contract down directly.
- 2026-06-14: **`#402` is now shipped.** `ENTER CRITICAL` now enforces ascending normalized lock ordering for nested acquisitions, and shipped blocking wait surfaces now reject blocking while a critical section is held (`AWAIT`, positive-duration `SLEEP`, and lock-retry/backoff under contention). Focused `test_prg_engine_control_flow` and `test_prg_engine_table_mutation` coverage prove deterministic `runtime.critical.order_violation` / `runtime.critical.blocking_violation` diagnostics instead of deadlock-prone hangs.
- 2026-06-14: **`#271` is now shipped.** `SET('REPROCESS')` now defaults to `AUTOMATIC`, lock acquisition honors per-session retry budgets across explicit lock commands plus shipped local mutation paths, and contention now emits deterministic `runtime.lock_retry` / `runtime.lock_timeout` diagnostics instead of busy-spinning. Focused `test_prg_engine_table_mutation` and `test_prg_engine_work_areas` coverage prove cross-session contention, retry counts, timeout errors, and session isolation/restoration.
- 2026-06-14: **`#273` is now shipped.** The Visual Studio asset editor pane now routes `Undo` through edit-level text-box undo or host-backed command undo as appropriate, `copperfin_studio_host` now reports command-undo availability/labels and executes `--undo-mode command`, and the native CTest suite now includes `test_managed_compile` so `Copperfin.LanguageServiceTests`, `Copperfin.Studio`, and `Copperfin.DesignerSmokeTests` all compile in routine validation.
- 2026-06-14: **`#401` is now shipped.** `CopperfinProjectInsightClient` now records class-scoped methods under their fully qualified symbol name and maps qualified plus instance-style method calls back to that symbol for reference collection and rename preview.
- 2026-06-14: **`#400` is now shipped.** `FoxProIntelliSenseCatalog` now infers default work-area aliases from plain `USE <path>` opens and ingests literal `SQLEXEC(..., ..., "alias")` target cursors, so those source-derived cursor names surface in completion and description flows.
- 2026-06-14: **`#399` is now shipped.** `Copperfin.LanguageServiceTests` now proves a procedure defined in one project file resolves from a sibling project file and surfaces signature help through the shared project-root symbol index.
- 2026-06-14: **`#398` is now shipped.** `FoxProIntelliSenseCatalog` now follows `#INCLUDE` edges with cycle protection and resolves `#DEFINE` symbols from included headers even when the header lives outside the project root.
- 2026-06-14: **`#397` is now shipped.** `FoxProIntelliSenseCatalog` now falls back from instance-style tokens such as `oEditor.SaveOrder` to a unique project method with the same trailing name, while still refusing ambiguous duplicate matches across classes.
- 2026-06-14: **`#396` is now shipped.** `FoxProIntelliSenseCatalog` now surfaces project-defined class methods as `member` completions in dotted/member-access contexts and ranks them ahead of the generic fallback member catalog while preserving the originating class path in the description.
- 2026-06-14: **`#395` is now shipped.** `FoxProIntelliSenseCatalog` now indexes class-scoped `PROCEDURE` / `FUNCTION` members under their fully-qualified class path, so exact tokens such as `app.customer.editor.SaveOrder` resolve to the method definition and surface method-specific signature help.
- 2026-06-14: **`#183` is now shipped.** `FoxProIntelliSenseCatalog` now preserves higher-priority context-specific completion entries instead of overwriting them with later global symbol entries, so `SELECT`-context aliases rank ahead of unrelated procedures and keep their context-aware descriptions.
- 2026-06-14: **`#182` is now shipped.** `FoxProIntelliSenseCatalog` now ingests source-created cursor aliases from `CREATE CURSOR` and `SELECT ... INTO CURSOR`, so metadata-driven completion and token-description paths see those aliases instead of only `USE ... ALIAS` names.
- 2026-06-14: **`#181` is now shipped.** `CopperfinProjectInsightClient` now builds a first rename preview over the shared definition/reference index, normalizing dotted tokens such as `oToolbar.SaveOrder` and returning the defining declaration plus the collected call sites for that project symbol.
- 2026-06-14: **`#180` is now shipped.** `CopperfinProjectInsightClient` now performs a definition pass before collecting references, so project-defined procedures/functions surface reference-navigation entries from direct and dotted call sites such as `SaveOrder(...)` and `oToolbar.SaveOrder(...)`.
- 2026-06-14: **`#179` is now shipped.** `FoxProIntelliSenseCatalog` now derives project procedure/function signature help from nearby `LPARAMETERS` / `PARAMETERS` declarations and reuses it for dotted invocation fallback such as `oToolbar.SaveOrder`.
- 2026-06-14: **`#178` is now shipped.** `FoxProIntelliSenseCatalog` now resolves dotted/member tokens through the longest matching project-symbol prefix before trailing-member fallback, and `Copperfin.LanguageServiceTests` proves dotted class-prefix navigation plus quick-info description reuse.
- The current prompt-sized language-service child queue is closed (`#178`-`#183`, `#395`, `#396`, `#397`, `#398`, `#399`, `#400`, and `#401` shipped). Re-audit umbrella issues `#27`, `#28`, and `#29` against their broader remaining scope before opening more child slices.
- The current prompt-sized runtime-safety child queue is also closed (`#142`-`#145`, `#150`-`#153`, `#269`, `#270`, `#271`, `#272`, `#273`, and `#402` shipped). Create a new `#14` child issue before taking more safety/diagnostics work.

Current shipped highlights worth remembering:

- 2026-06-14: **`#21` is now closed.** Build/run/debug/deploy workflow tightening is now considered complete at the umbrella level: runtime package materialization, migration-report diagnostics, native entrypoint packaging, deployed-package rebinding, launcher routing, and launcher argument forwarding all have focused workflow coverage across the current baseline.
- 2026-06-14: **`#20` is now closed.** Debugger completion is now considered complete at the umbrella level: the runtime host exposes watch evaluation, live breakpoint management, xAsset-aware breakpoint identity, and richer step/breakpoint pause inspection with focused regression coverage across both engine and host paths.
- 2026-06-14: **`#38` is now closed.** IDE-safe round-trip coverage for modified FoxPro/VFP assets now spans the core memo-backed families the current editor path touches: screens/classes, reports/labels/menus, projects, and database containers all have focused reopenability plus undo-fidelity regression coverage.
- 2026-06-14: **`#417` is now closed.** Project and database-container round-trip coverage now extends the visual-asset editor beyond the earlier SCX/VCX/FRX/LBX/MNX families: synthetic `PJX/PJT` and `DBC/DCT` assets survive paired direct/memo edits, remain readable, and fully undo back to their original serialized state.
- 2026-06-14: **`#42` is now closed.** AST/IR emission is now considered complete at the family-issue level: selected runtime outputs get durable AST and IR artifacts with focused schema/opcode coverage, and the surrounding compiler/runtime contract now carries deterministic integrity data for those artifacts.
- 2026-06-14: **`#14` is now closed.** Debugger metadata, diagnostics, and developer-facing error work is now considered complete at the umbrella level; remaining debugger backlog belongs under `#20` rather than new `#14` slices unless the scope itself expands.
- 2026-06-14: **`#409` is now closed.** The engine now has a centralized critical-section policy source of truth: named in-memory sections use ascending normalized lock ordering, blocking wait/retry surfaces are forbidden while a section is held, and direct contested `RLOCK()` coverage now guards the explicit-lock fast-fail path beside the earlier `AWAIT`, `SLEEP`, and mutation-contention guards.
- 2026-06-14: **`#416` is now closed.** Ordinary pause-state breakpoint listings now carry designer-facing xAsset action ids and titles for active xAsset-backed breakpoints instead of requiring a separate inventory query, and the shared runtime-host smoke test now guards that pause-output metadata directly.
- 2026-06-14: **`#415` is now closed.** XAsset-backed breakpoint inventory now round-trips designer-facing action ids and titles instead of exposing only generated bootstrap file/line entries, reusing the same action/breakpoint resolver already shipped for action-id breakpoint commands, and the shared host smoke test now guards that inventory metadata directly.
- 2026-06-14: **`#414` is now closed.** The runtime host now lets debugger clients add and remove xAsset breakpoints by action id instead of leaking generated bootstrap file/line details, reusing the existing action-binding model plus generated bootstrap source to resolve real engine breakpoints, and the shared host smoke test now guards that mapping directly.
- 2026-06-14: **`#413` is now closed.** The runtime host now supports removing a single normalized breakpoint without clearing the full set, the engine owns that removal path through `remove_breakpoint()`, and the shared host smoke test now guards retention of unrelated breakpoints plus pause behavior on the surviving line.
- 2026-06-14: **`#412` is now closed.** The runtime host now has a live breakpoint-management surface (`break:add`, `break:clear`, `break:list`) backed by deduplicated engine breakpoint inventory rather than a host-only shadow list, and the shared host smoke test now guards both breakpoint management and pause behavior.
- 2026-06-14: **`#411` is now closed.** The runtime host now enriches xAsset breakpoint/event-loop pause output with the originating action id, record index, kind, and title by resolving paused bootstrap frames back through the existing executable model, and a dedicated host-process smoke test now guards that debugger-to-designer linkage directly.
- 2026-06-14: **`#410` is now closed.** The compiler/runtime contract now carries integrity data for generated build artifacts as well as their paths: materialized packages emit deterministic `compiler_contract=` digest lines for AST/IR/transpilation and output-kind-specific contract files, so downstream tooling can verify package copies without relying on file names alone.
- 2026-06-14: **`#215` is now closed.** The build pipeline’s C# transpilation contract now covers xAsset class/object structure as well as plain PRG flow: VCX/SCX assets emit compileable root-object and nested-object scaffolding derived from the existing xAsset executable model, lifecycle ordering is preserved through ordered `RunStartup()` / `RunShutdown()` wrappers, and untranslated method bodies remain explicit manual-port seams rather than pretending to be complete ports.
- 2026-06-14: **`#214` is now closed.** The build pipeline now has an explicit C# transpilation artifact contract: `.NET`-oriented package requests emit a target-specific `.transpiled.cs` file whose first-pass generator maps procedural PRG control flow into deterministic C# scaffolding with stable routine naming, the runtime manifest carries that artifact path plus a build-output feature flag, and focused runtime-pipeline coverage now includes a direct `dotnet build` proof that the emitted source stays syntactically valid.
- 2026-06-14: **`#213` is now closed.** The build pipeline now has an explicit IR emission contract: every packaged output gets a target-specific `.ir.json` artifact whose schema records PRG-relative files, routines, stable statement opcode names, and mapped statement operand metadata, and the runtime manifest carries that IR path for downstream consumers.
- 2026-06-14: **`#212` is now closed.** The build pipeline now has an explicit AST emission contract: every packaged output gets a target-specific `.ast.json` artifact whose schema records PRG-relative files, routines, and logical statement line/text fidelity, and the runtime manifest carries that AST path for downstream consumers.
- 2026-06-14: **`#211` is now closed.** APP package planning is now explicitly regression-guarded: `.app` outputs infer the right Visual FoxPro application-archive target kind, emit a dedicated `.app.contents` contract only for APP builds, and materialize staged content membership plus startup fidelity instead of pretending the compiled APP archive already exists.
- 2026-06-14: **`#210` is now closed.** FXP package planning is now explicitly regression-guarded: `.fxp` outputs infer the right Visual FoxPro tokenized-program target kind, emit a dedicated `.fxp.tokens` contract only for FXP builds, and materialize parsed PRG logical statements by scope/line/text instead of pretending the compiled FXP binary already exists.
- 2026-06-14: **`#209` is now closed.** FLL package planning is now explicitly regression-guarded: `.fll` outputs infer the right Visual FoxPro target kind, emit a dedicated `.fll.api` contract only for FLL builds, and materialize a `SET LIBRARY TO` / `RELEASE LIBRARY`-oriented API manifest populated from discovered PRG routine exports alongside the honest `.def` contract.
- 2026-06-14: **`#205` is now closed.** Memo-backed label and menu asset round-trip safety is now explicitly regression-guarded: synthetic `LBX/LBT` and `MNX/MNT` assets survive paired direct/memo edits, remain readable, and fully undo back to their original serialized state without leaving undo residue.
- 2026-06-14: **`#204` is now closed.** Class-library round-trip serialization is now explicitly regression-guarded: synthetic `VCX/VCT` assets survive added-property writes, preserve existing serialized blob content, and undo the addition cleanly without leaving the asset unreadable.
- 2026-06-14: **`#408` is now closed.** The debugger now has an explicit paused-session watch evaluation surface: watch expressions resolve against the live frame/global/cursor context without resuming execution, the runtime host exposes that through `watch:<expr>`, and focused PRG coverage plus a direct host smoke run guard the contract.
- 2026-06-14: **`#278` is now closed.** The runtime package contract now records requested-vs-effective launcher routing explicitly, including deterministic native-host fallback when `.NET` output is unavailable, with feature-flag diagnostics carried in the runtime/debug manifests and validated by focused pipeline plus managed compile coverage.
- 2026-06-14: **`#407` is now closed.** Step pauses now have explicit regression coverage for selected work-area and open-cursor inspection fidelity, including alias/work-area identity plus current record position across `step_into`, `step_over`, and `step_out`.
- 2026-06-14: **`#406` is now closed.** Breakpoint pauses now have explicit regression coverage for selected work-area and open-cursor inspection fidelity, including alias/work-area identity plus current record position at the paused statement.
- 2026-06-14: **`#405` is now closed.** Step pause-state routing is now explicitly regression-guarded: `step_into`, `step_over`, and `step_out` each preserve the expected current/next statement text and caller-vs-callee frame routing across a simple `DO worker` flow.
- 2026-06-14: **`#404` is now closed.** Breakpoint pauses now have explicit regression coverage proving the debug state preserves the current statement text alongside the existing line/frame/locals contract in both ordinary and first-line breakpoint cases.
- 2026-06-14: **`#403` is now closed.** Generated .NET launchers now preserve ordinary application arguments while still injecting the manifest path and normalizing debug flags, and focused `test_runtime_pipeline` coverage now proves the launcher source contract directly.
- 2026-06-14: **`#402` is now closed.** The runtime now has an explicit in-memory critical-section policy: nested acquisitions must follow ascending normalized section-name order, and blocking wait surfaces are rejected while a critical section is held. Focused `test_prg_engine_control_flow` and `test_prg_engine_table_mutation` coverage prove deterministic diagnostics for order violations and lock/wait blocking violations.
- 2026-06-14: **`#271` is now closed.** The lock/reprocess lane now defaults `SET('REPROCESS')` to `AUTOMATIC`, preserves data-session isolation of that state, and applies bounded retry/timeout behavior to explicit lock commands plus shipped local mutation paths. Focused `test_prg_engine_table_mutation` and `test_prg_engine_work_areas` coverage prove contention retries, timeout diagnostics, and non-busy-spin behavior.
- 2026-06-13: **`#7` and `#8` are now closed.** All active lane issues (#92-#101) complete. A3 (Command/Expression Surface) is done at ~98%.
- 2026-06-13: **`#100` is now closed.** Child slice #393 fixed `SCATTER BLANK` on zero-record tables (schema-fallback path) and `blank_value_for_field` for character/varchar fields. G12 (Field Projection/Data Transfer) complete.
- 2026-06-13: **`#101` is now closed.** All headless interaction commands (WAIT/KEYBOARD/INPUT/ACCEPT/GETFILE/PUTFILE/GETDIR/INPUTBOX) have macro-expanded prompt/target second-hop coverage. G14 (Headless Interaction) complete.
- 2026-06-13: **`#93` is now closed.** SQL result-cursor parity is fully proven across navigation, ordering, filtering, mutation, import, export, identity, session isolation, seek, and OLE actions (45+ tests in test_prg_engine_sql_cursors.cpp). G8 (Remote/Result-Cursor) complete.
- 2026-06-13: **`#94` is now closed.** Structural table-operation parity proven for CREATE TABLE, ALTER TABLE, PACK, PACK MEMO, ZAP, COPY TO (7 formats), APPEND FROM (7 formats), and journaling/rollback. G11 (Structural Table Ops) complete.
- 2026-06-13: **`#99` is now closed.** Child slice #392 implemented DISPLAY/LIST MEMORY LIKE/EXCEPT wildcard name-filter clause. G15 (Memory/Assignment Semantics) complete.
- 2026-06-13: **`#98` is now closed.** All child slices (#105, #106, #107) shipped; runtime-state normalization and SET() compatibility residuals proven complete.
- 2026-06-13: **`#92` is now closed.** All child slices (#102, #103, #104) shipped; G6 (Order/Seek/Collation) is proven for local DBF-backed and synthetic SQL/result cursors. `SET COLLATE`, `FOR`-filtered temporary orders, and grounded numeric-field seek metadata all confirmed.
- 2026-06-13: **`#97` is now closed.** All 45 child slices (#347–#391) shipped, completing G9 (Macro/Eval Core). Final child: `#391` — `ASCAN()` macro-expanded predicate fidelity and `_ASCAN*` metadata cleanup proof across variable-reference, first-hop macro, and second-hop holder paths.
- 2026-06-13: H3/#97 child issue `#390` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now drives the SQL/result-cursor `LOOKUP()` miss path through `&cReturnExprDeepHolder`, `&cMissingSearchDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`.
- 2026-06-13: H3/#97 child issue `#389` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now drives the `LOOKUP()` miss-default branch through deep return/search holders plus `&cAliasDeepHolder` and `&cTagDeepHolder`.
- 2026-06-13: H3/#97 child issue `#388` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now drives the local string-return `LOOKUP()` branch through `&cReturnExprDeepHolder`, `&cSearchExprDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`.
- 2026-06-13: H3/#97 child issue `#387` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now drives the numeric `LOOKUP()` branch through `&cAgeExprDeepHolder`, `&cSearchExprDeepHolder`, `&cAliasDeepHolder`, and `&cTagDeepHolder`.
- 2026-06-13: H3/#97 child issue `#386` is now shipped. Focused `test_prg_engine_control_flow` coverage now routes recursive command-path merged text through `&cRecursiveExprDeepHolder`, and the `TEXT TO ... TEXTMERGE` dispatcher now reapplies merged interpolation to a bounded fixed point.
- 2026-06-13: H3/#97 child issue `#385` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now uses `EVAL(EVAL(cEvalExprDeepHolder))`, and `evaluate_runtime_surface_function()` now preserves the last identifier in bounded direct-holder `EVAL()` chains before final evaluation.
- 2026-06-13: H3/#97 child issue `#384` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now drives nested `EXECSCRIPT()` body evaluation through `EVAL(&cScriptExprDeepHolder)` while the script source still resolves via `&cScriptDeepHolder`.
- 2026-06-13: H3/#97 child issue `#383` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now routes recursive custom-delimiter merged text through `&cRecursiveCustomExprDeepHolder`, and the recursive template itself now uses `EVAL(&cNameExprDeepHolder)`.
- 2026-06-13: H3/#97 child issue `#382` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now uses true second-hop holder chains for nested `EVAL()` and `&macro` interpolation inside the alternate-delimiter `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)` branch.
- 2026-06-13: H3/#97 child issue `#381` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now uses true second-hop holder chains for custom-delimiter `TEXTMERGE()` nested `EVAL()` and `&macro` interpolation, and `evaluate_runtime_surface_function()` now falls back to the last valid bare identifier when macro-expanded `EVAL()` input over-expands into final scalar text.
- 2026-06-13: H3/#97 child issue `#380` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now uses a true second-hop holder chain inside merged `TEXTMERGE()` eval content, verifying `EVAL(&cEvalExprDeepHolder)` inside the template.
- 2026-06-13: H3/#97 child issue `#379` is now shipped. Focused `test_prg_engine_arrays` coverage now uses a true second-hop holder chain for the `STORE ... TO &target` array-element assignment path, verifying `STORE 6 TO &cMacroTargetDeepHolder`.
- 2026-06-13: H3/#97 child issue `#378` is now shipped. Focused `test_prg_engine` coverage now uses a true second-hop holder chain for the `DO &macro` routine-target path, verifying `DO &cProcDeepHolder`.
- 2026-06-13: H3/#97 child issue `#377` is now shipped. Focused `test_prg_engine` coverage now uses true second-hop holder chains in the `macro_deep.prg` harness for row-expression, field-expression, assignment-target, and `DO ... WITH` argument paths, verifying `&cRowExprDeepHolder`, `&cFieldExprDeepHolder`, `&cTargetDeepHolder`, and `&cParamExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#376` is now shipped. Focused `test_prg_engine_table_mutation` coverage now uses true second-hop holder chains for the local SQL-style `DELETE FROM` / `UPDATE ... FOR &expr` paths, verifying `&cDeleteExprDeepHolder` and `&cUpdateExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#375` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now uses true second-hop holder chains for the SQL cursor `LOCATE` / `DELETE` / `RECALL FOR &expr` paths, verifying `&cLocateExprDeepHolder` and `&cDeleteExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#374` is now shipped. Focused `test_prg_engine_arrays` coverage now uses a true second-hop holder chain for the source-array branch in the array helper/access harness, verifying forms such as `ALEN(&cArrayNameDeepHolder)` and `&cArrayNameDeepHolder[2,1]`.
- 2026-06-13: H3/#97 child issue `#373` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop field-name holder chain for the alias-qualified dynamic `AGE` branch, verifying `&cAliasDeepHolder..&cAgeNestedFieldHolder`.
- 2026-06-13: H3/#97 child issue `#372` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for the direct macro self-reference branch, verifying `&cSelfRefDeepHolder`.
- 2026-06-13: H3/#97 child issue `#371` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for the direct macro fallback-text branch, verifying `&cFallbackExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#370` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for the first `COPY TO ARRAY` macro target path, verifying forms such as `&cMacroArrayDeepHolder`.
- 2026-06-13: H3/#97 child issue `#369` is now shipped. Focused `test_prg_engine_arrays` coverage now uses true second-hop holder chains for the `ALINES` target-array path and `ACOPY` target-array path, verifying forms such as `&cLinesNameDeepHolder` and `&cCopyNameDeepHolder`.
- 2026-06-13: H3/#97 child issue `#368` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for the first macro-expanded `SCATTER TO` / `GATHER FROM` round-trip, verifying `&cMacroRowNameDeepHolder` across scatter, gather, and adjacent macro array reads.
- 2026-06-13: H3/#97 child issue `#367` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for the alias-qualified dynamic `AGE` branch, verifying `&cAliasDeepHolder..&cAgeFieldHolder`.
- 2026-06-13: H3/#97 child issue `#366` is now shipped. Focused `test_prg_engine_functions` coverage now uses true second-hop holder chains for direct alias-qualified field reads, verifying `&cAliasDeepHolder..NAME` and `&cAliasDeepHolder..AGE`.
- 2026-06-13: H3/#97 child issue `#365` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for the literal `&stem.suffix` fallback branch, verifying `&cStemDeepHolder.World`.
- 2026-06-13: H3/#97 child issue `#364` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for `LPARAMETERS nValue = &cDefaultExprDeepHolder`, verifying default-expression binding through real intermediate holders.
- 2026-06-13: H3/#97 child issue `#363` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now verifies the full SQL `LOOKUP()` all-argument path through true second-hop holder chains, using `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, &cAliasDeepHolder, &cTagDeepHolder)`.
- 2026-06-13: H3/#97 child issue `#362` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now uses a true second-hop holder chain for the numeric `LOOKUP()` return-expression path, verifying `LOOKUP(&cAgeExprDeepHolder, &cSearchExpr, cAlias, cTag)`.
- 2026-06-13: H3/#97 child issue `#361` is now shipped. Focused `test_prg_engine_data_io` coverage now reuses the existing true second-hop holder chain on both sides of the round-trip array harness, verifying `COPY TO ARRAY &cTempArrayDeepHolder` alongside `APPEND FROM ARRAY &cTempArrayDeepHolder`.
- 2026-06-13: H3/#97 child issue `#360` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for temporal `COPY TO ARRAY &macro` / `APPEND FROM ARRAY &macro` flows, verifying `&cArrayNameDeepHolder` on the typed date/datetime round-trip path.
- 2026-06-13: H3/#97 child issue `#359` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for `SCATTER ... NAME &macro` / `GATHER NAME &macro` object targets, verifying forms such as `&cObjectNameDeepHolder` on the existing additive object-target path.
- 2026-06-13: H3/#97 child issue `#358` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for `DISPLAY STRUCTURE IN &alias` / `LIST STRUCTURE IN &alias`, verifying raw/resolved target detail through `&cAliasDeepHolder`.
- 2026-06-13: H3/#97 child issue `#357` is now shipped. Focused `test_prg_engine` coverage now uses true second-hop holder chains for macro-expanded array subscripts and function arguments in `macro_deep.prg`, verifying `aData[&cRowExprDeepHolder]` and `LEN(&cFieldExprDeepHolder)`.
- 2026-06-13: H3/#97 child issue `#356` is now shipped. Focused `test_prg_engine` coverage now uses a true second-hop holder chain for macro assignment targets, verifying `&cTargetDeepHolder = 'MACROASSIGN'` through the shared assignment-target resolver.
- 2026-06-13: H3/#97 child issue `#355` is now shipped. Focused `test_prg_engine` coverage now uses a true second-hop holder chain for `DO ... WITH &macro` argument binding, verifying `DO testparam WITH &cParamExprDeepHolder` through the shared call-argument path.
- 2026-06-13: H3/#97 child issue `#354` is now shipped. Focused `test_prg_engine` coverage now uses a true second-hop holder chain for `DO &macro` routine targets, verifying `DO &cProcDeepHolder` through the shared command-target dispatch path.
- 2026-06-13: H3/#97 child issue `#353` is now shipped. Focused `test_prg_engine` coverage now uses a true second-hop holder chain for `USE IN &alias` closure, verifying `USE IN &cAliasExprDeepHolder` through the shared alias-target runtime-state path.
- 2026-06-13: H3/#97 child issue `#352` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain on the alias side in the deepest alias-qualified macro field-access and `LOCATE FOR` cases, verifying forms such as `&cAliasDeepHolder..&cNestedFieldHolder`.
- 2026-06-13: H3/#97 child issue `#351` is now shipped. Focused `test_prg_engine_table_mutation` coverage now uses true second-hop holder chains for local SQL-style `DELETE FROM` and `UPDATE ... FOR &expr` flows, verifying forms such as `&cDeleteExprDeepHolder` and `&cUpdateExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#350` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now uses true second-hop holder chains for SQL cursor `LOCATE` / `DELETE` / `RECALL FOR &expr` flows, verifying forms such as `&cLocateExprDeepHolder` and `&cDeleteExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#349` is now shipped. Focused `test_prg_engine_arrays` coverage now uses a true second-hop holder chain for `STORE ... TO &target` array-element assignment, verifying `STORE 6 TO &cMacroTargetDeepHolder` through the shared assignment-target path.
- 2026-06-13: H3/#97 child issue `#348` is now shipped. `parse_primary()` now prefers shipped array-helper function calls over same-named runtime arrays when `(` follows an identifier, and focused `test_prg_engine_arrays` coverage now verifies true second-hop macro-expanded array helper/access forms across `ALINES`, `ALEN`, `AELEMENT`, `ACOPY`, and `&array[...]` / `&array(...)`.
- 2026-06-13: H3/#97 child issue `#347` is now shipped. Focused `test_prg_engine_data_io` coverage now uses a true second-hop holder chain for `SCATTER TO` / `GATHER FROM` array-name macros, verifying `&cMacroRowNameDeepHolder` through a real intermediate holder.
- 2026-06-13: H3/#97 child issue `#346` is now shipped. Focused `test_prg_engine_data_io` coverage now uses true second-hop holder chains for both macro-expanded nested-property segments in `SCATTER NAME` / `GATHER NAME`, verifying forms such as `&cHolderDeepHolder.&cChildDeepHolder`.
- 2026-06-13: H3/#97 child issue `#345` is now shipped. Focused `test_prg_engine_functions` coverage now uses a true second-hop holder chain for direct `&macro` expression indirection.
- 2026-06-13: H3/#97 child issue `#344` is now shipped. Focused `test_prg_engine_control_flow` coverage now uses true second-hop holder chains for command-path `TEXT TO ... TEXTMERGE` nested `EVAL()` and `&macro` interpolation.
- 2026-06-13: H3/#97 child issue `#343` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now uses true second-hop holder chains for `TEXTMERGE()` and `EXECSCRIPT()` macro surfaces, including source-template, delimiter, `RETURN &macro`, and script-source forms.
- 2026-06-13: H3/#97 child issue `#342` is now shipped. Focused `test_prg_engine_data_io` coverage now uses true second-hop holder chains for command array-name targets, verifying forms such as `&cMacroArrayDeepHolder` and `&cTempArrayDeepHolder` across `COPY TO ARRAY` and `APPEND FROM ARRAY`.
- 2026-06-13: H3/#97 child issue `#341` is now shipped. Focused `test_prg_engine_control_flow` coverage now uses true second-hop holder chains for aggregate `TO` targets, verifying forms such as `&cCountDeepHolder`, `&cSumDeepHolder`, and `&cCalcDeepHolder`.
- 2026-06-13: H3/#97 child issue `#340` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now uses true second-hop holder chains for SQL cursor `SET FIELDS TO` / `SET FILTER TO` text macros, verifying forms such as `&cFieldsSpecDeepHolder` and `&cFilterExprDeepHolder`.
- 2026-06-13: H3/#97 child issue `#339` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now uses a true second-hop holder chain for runtime-state path text, verifying `EVALUATE(&cEvalPathExprDeepHolder)` instead of a first-hop-only holder mislabeled as second-hop.
- 2026-06-13: H3/#97 child issue `#338` is now shipped. Focused `test_prg_engine_data_io` coverage now uses true second-hop holder chains for headless command target-detail proof across `INPUT` / `ACCEPT`, `WAIT WINDOW`, and `DISPLAY` / `LIST`, verifying raw/resolved detail against forms such as `&cInputTargetDeepHolder`, `&cWaitTargetDeepHolder`, and `&cAliasDeepHolder`.
- 2026-06-13: H3/#97 child issue `#337` is now shipped. `evaluate_set_string_value()` now reuses bounded raw macro expansion before expression evaluation, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop holder value macros across ordinary `SET` runtime-state options such as `PATH`, `MARK`, `DECIMALS`, `COLLATE`, `NULL`, and `ANSI`.
- 2026-06-13: H3/#97 child issue `#336` is now shipped. `expand_set_text_macro()` now chases bounded bare-identifier holder hops after the initial `&macro` expansion, and focused `test_prg_engine_sql_cursors` coverage now locks down second-hop `SET FIELDS TO &...` visible-field metadata plus second-hop `SET FILTER TO &... IN sqlcust` positioning and field lookup parity.
- 2026-06-13: H3/#97 child issue `#335` is now shipped. `resolve_runtime_target_identifier()` now stops before scalar values when expanding `&target`, preserving assignment identity across cases such as `PUBLIC shared` plus `RELEASE ALL`, and focused `test_prg_engine_control_flow` coverage now locks down second-hop aggregate `TO` targets such as `&cCountHolder`, `&cSumHolder`, and `&cCalcHolder`.
- 2026-06-13: H3/#97 child issue `#334` is now shipped. `resolve_runtime_target_identifier()` now chases bounded holder hops for `TO &target` assignment/detail paths, and focused `test_prg_engine_data_io` coverage now locks down second-hop target detail for `INPUT`, `ACCEPT`, and `WAIT WINDOW` plus second-hop `IN` target detail for `DISPLAY` / `LIST`.
- 2026-06-13: H3/#97 child issue `#333` is now shipped. Focused `test_prg_engine_data_io` coverage now locks down second-hop nested-holder command array-name resolution for `SCATTER TO` / `GATHER FROM` forms such as `&cMacroRowNameDeepHolder`.
- 2026-06-13: H3/#97 child issue `#332` is now shipped. `parse_command_object_target_path()` now chases bounded string-holder hops without consuming runtime object-reference strings, and focused `test_prg_engine_data_io` coverage now locks down second-hop nested-holder `SCATTER NAME` / `GATHER NAME` object targets such as `&cHolderDeepHolder.&cChildDeepHolder`.
- 2026-06-13: H3/#97 child issue `#331` is now shipped. Focused `test_prg_engine_data_io` coverage now locks down second-hop nested-holder command array-name resolution for `COPY TO ARRAY` and `APPEND FROM ARRAY` forms such as `&cMacroArrayHolder` and `&cTempArrayHolder`.
- 2026-06-13: H3/#97 child issue `#330` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder `EVALUATE()` runtime-state path text such as `EVALUATE(&cEvalPathExprHolder)` over `SET('PATH')`.
- 2026-06-13: H3/#97 child issue `#329` is now shipped. Focused `test_prg_engine_functions` coverage now locks down second-hop nested-holder direct `&macro` expression indirection such as `&cMacroNameDeepHolder`.
- 2026-06-13: H3/#97 child issue `#328` is now shipped. Focused `test_prg_engine` coverage now locks down second-hop nested-holder runtime-state `&macro` field-name resolution such as `&cFieldExprDeepHolder`.
- 2026-06-11: H3/#97 child issue `#327` is now shipped. `LOOKUP()` now derives miss defaults from fully-resolved macro return-expression text without over-rewriting non-macro expressions, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop holder miss paths for numeric `0` and logical `.F.` defaults.
- 2026-06-11: H3/#97 child issue `#326` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder local `LOOKUP()` alias/tag designators such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#325` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down second-hop nested-holder combined nested-surface flows across `EVAL()` and `EXECSCRIPT()`.
- 2026-06-11: H3/#97 child issue `#324` is now shipped. Focused `test_prg_engine_control_flow` coverage now locks down second-hop nested-holder command-path `TEXT TO ... TEXTMERGE` interpolation such as `<<EVAL(&cNameExprDeepHolder)>>` and `<<&cFieldExprDeepHolder>>`.
- 2026-06-11: H3/#97 child issue `#323` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down second-hop nested-holder `EXECSCRIPT()` return-expression forms such as `EXECSCRIPT('RETURN &cExecReturnDeepHolder')`.
- 2026-06-11: H3/#97 child issue `#322` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down second-hop nested-holder `TEXTMERGE()` delimiter forms such as `TEXTMERGE(..., &cLeftDelimDeepHolder, &cRightDelimDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#321` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down second-hop nested-holder `TEXTMERGE()` source-template forms such as `TEXTMERGE(&cTemplateDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#320` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down second-hop nested-holder `EXECSCRIPT()` source-text forms such as `EXECSCRIPT(&cExecScriptDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#319` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder SQL `LOOKUP()` expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`.
- 2026-06-11: H3/#97 child issue `#318` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder SQL `LOOKUP()` alias/tag designators such as `LOOKUP(..., &cAliasDeepHolder, &cTagDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#317` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder `LOOKUP()` expression forms such as `LOOKUP(&cReturnExprDeepHolder, &cSearchExprDeepHolder, ...)`.
- 2026-06-11: H3/#97 child issue `#316` is now shipped. Focused `test_prg_engine` and `test_prg_engine_runtime_surface_functions` coverage now locks down second-hop nested-holder `EVAL()` / `EVALUATE()` forms such as `EVAL(&cEvalExprDeepHolder)` and `EVALUATE(&cEvalExprDeepHolder)`.
- 2026-06-11: H3/#97 child issue `#315` is now shipped. Focused `test_prg_engine_functions` coverage now locks down second-hop nested-holder `&stem.suffix`, trailing-dot, and adjacent embedded-identifier forms such as `&cTypeDeepHolder.Name`, `&cFieldDeepHolder.`, and `m&cType2DeepHolder.ID`.
- 2026-06-11: H3/#97 child issue `#314` is now shipped. Focused `test_prg_engine_functions` coverage now locks down second-hop nested-holder alias-qualified forms such as `&cAliasHolder..&cNestedFieldHolder` across direct reads and `LOCATE FOR` evaluation.
- 2026-06-11: H3/#97 child issue `#313` is now shipped. `parse_identifier()` now recursively expands embedded macro holders before concatenating identifier fragments, and focused `test_prg_engine_functions` coverage now locks down nested-holder embedded macro identifiers such as `m&cType2Holder.ID`.
- 2026-06-11: H3/#97 child issue `#312` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down nested-holder SQL alias/tag designators for `LOOKUP()`.
- 2026-06-11: H3/#97 child issue `#311` is now shipped. Focused `test_prg_engine_functions` coverage now locks down nested-holder alias-qualified forms such as `&cAliasHolder..NAME` and `&cAliasHolder..&cNameFieldHolder`.
- 2026-06-11: H3/#97 child issue `#310` is now shipped. `parse_macro_reference()` now recursively expands the macro stem before suffix concatenation, and focused `test_prg_engine_functions` coverage now locks down nested-holder dot-suffix forms such as `&cTypeHolder.Name` and `&cFieldHolder.`.
- 2026-06-10: H3/#97 child issue `#309` is now shipped. Nested macro-expanded `LOOKUP()` alias/tag designators now chase memory-variable holder indirection without leaking into generic field/alias resolution, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down that path.
- 2026-06-10: H3/#97 child issue `#308` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down nested macro-indirection for `LOOKUP()` return and search expression text.
- 2026-06-10: H3/#97 child issue `#307` is now shipped. Focused `test_prg_engine` coverage now locks down nested macro-indirection for `EVAL(&cExprHolder)` over runtime-state expression text such as `ALIAS()`.
- 2026-06-10: H3/#97 child issue `#306` is now shipped. `EVAL()` / `EVALUATE()` now chase simple macro-expanded bare-identifier hops before evaluating the final stored expression text, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down nested macro-indirection for `EVALUATE(&cExprHolder)`.
- 2026-06-10: H3/#97 child issue `#305` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down `EXECSCRIPT(&cScriptExpr)` so macro-expanded script text still reaches first-pass `RETURN` evaluation against the current runtime state.
- 2026-06-10: H3/#97 child issue `#304` is now shipped. `parse_macro_reference()` now falls back to raw expanded string text when a macro-expanded value is not itself a valid expression, and focused `test_prg_engine_string_math_functions` coverage now locks down `TEXTMERGE(&cTemplateExpr)` so macro-expanded source-template text still merges against the current runtime state.
- 2026-06-10: H3/#97 child issue `#303` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down `EVALUATE(&cExpr)` for runtime-state expression text such as `SET('PATH')`.
- 2026-06-10: H3/#97 child issue `#302` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down `EXECSCRIPT('RETURN &cExpr')` so macro-expanded return-expression text still evaluates against the current runtime state.
- 2026-06-10: H3/#97 child issue `#301` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down `TEXTMERGE(..., &cLeft, &cRight)` so macro-expanded delimiter arguments preserve nested `EVAL()` and `&macro` interpolation inside the merged text.
- 2026-06-10: H3/#97 child issue `#300` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down custom-delimiter `TEXTMERGE()` parity so `{|...|}` merges preserve nested `EVAL()` and `&macro` interpolation, including recursive re-entry over stored expression text.
- 2026-06-10: H3/#97 child issue `#299` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down `EVALUATE(&cExpr)` so the synonym preserves macro-expanded expression text through the shared eval helper.
- 2026-06-10: H3/#97 child issue `#298` is now shipped. `EVAL()` now preserves simple `&macro` variable text before evaluating the resulting expression, and focused `test_prg_engine` coverage now locks down `EVAL(&cExpr)` for runtime-state expression text such as `ALIAS()`.
- 2026-06-10: H3/#97 child issue `#297` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down `LOOKUP()` miss-path typing when the return expression arrives through macro expansion, including numeric `0` and `.F.` fallback behavior.
- 2026-06-10: H3/#97 child issue `#296` is now shipped. `LOOKUP()` now prefers simple `&macro` variable text for alias/tag designators before falling back to full expression evaluation, and focused `test_prg_engine_runtime_surface_functions` coverage now locks down macro-expanded return/search/alias/tag arguments on synthetic SQL cursors.
- 2026-06-10: H3/#97 child issue `#295` is now shipped. Focused `test_prg_engine_runtime_surface_functions` coverage now locks down `LOOKUP()` when return and search expressions are supplied through macro-expanded expression text, while preserving the selected non-target alias context.
- 2026-06-10: H3/#97 child issue `#294` is now shipped. Focused `test_prg_engine_control_flow` coverage now locks down command-path `TEXT TO ... TEXTMERGE` interpolation when merged segments contain nested `EVAL()` and `&macro` expressions.
- 2026-06-10: H3/#97 child issue `#293` is now shipped. Focused `test_prg_engine_string_math_functions` coverage now locks down nested expression-surface expansion across `EVAL()`, `TEXTMERGE()`, and macro-expanded `EXECSCRIPT()` return flows without broadening the current first-pass script runtime.
- 2026-06-10: H3/#92 child issue `#292` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down representative SQL default `PADR(...)` and decimal `STR(..., width, decimals)` temporary-order seek parity over synthetic SQL rows.
- 2026-06-10: H3/#92 child issue `#291` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down representative SQL default-padding and default-width `STR()` temporary-order seek parity, including `PADL(name, n)` and `STR(amount)` key paths over synthetic SQL rows.
- 2026-06-10: H3/#92 child issue `#290` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down representative SQL `RIGHT(...)` and `STR(...)` temporary-order seek parity, including suffix extraction and numeric string-format key paths over synthetic SQL rows.
- 2026-06-10: H3/#92 child issue `#289` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down representative SQL command-path derived temporary orders: `SET ORDER TO UPPER(LEFT(...))` and `SET ORDER TO UPPER(PADR(...))` followed by command `SEEK` position on the expected synthetic SQL rows.
- 2026-06-10: H3/#92 child issue `#288` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down representative derived-string SQL temporary-order parity: one-off `LEFT(...)`, `SUBSTR(...)`, and `PADL(...)` seek expressions resolve and position on the expected synthetic SQL rows.
- 2026-06-10: H3/#92 child issue `#287` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down SQL/result-cursor `SET NEAR` session scoping: missed seeks with `SET NEAR ON` near-position within one data session, a fresh second session retains default EOF behavior, and restoring the first session restores its near-positioning behavior.
- 2026-06-10: H3/#92 child issue `#286` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down SQL/result-cursor `SET EXACT` seek parity: prefix seeks over temporary `NAME` orders succeed with `SET EXACT OFF`, fail with `SET EXACT ON`, set EOF on the failed exact seek, and remain isolated per data session.
- 2026-06-10: H3/#92 child issue `#285` is now shipped. Focused `test_prg_engine_sql_cursors` coverage now locks down targeted SQL/result-cursor plain-string collate parity: `SET ORDER TO NAME IN sqlcust` plus `SEEK('bravo', 'sqlcust', 'NAME')` respects `SET COLLATE TO GENERAL`, moves only the targeted SQL cursor to the case-folded match, and preserves the selected non-target alias/record pointer.
- 2026-05-12: D3/#166 build/run orchestration coverage deepened in `runtime_pipeline` + `test_runtime_pipeline`. Runtime-host input validation now executes before asset staging, and invalid host paths fail fast without partially staged package content.
- 2026-05-12: D2/#165 breakpoint/stepping coverage deepened in `prg_engine` + `test_prg_engine`. First executable-line breakpoints now trigger after stop-on-entry continue, with one-shot same-location breakpoint re-hit suppression preserved for immediate resume.
- 2026-05-12: D2/#164 watch/locals launch-path coverage deepened in `runtime_pipeline` + `test_runtime_pipeline`. Debug `source_roots` now normalize/deduplicate entries so overlapping source/content paths emit one stable root for evaluation lookups.
- 2026-05-12: D1/#163 semantic manifest/asset-contract coverage deepened in `runtime_pipeline` + `test_runtime_pipeline`. Runtime manifest `asset=` lines now emit a terminal `copied` state field (`true`/`false`) with focused staged-vs-excluded contract checks.
- 2026-05-12: C5/#162 startup-diagnostics coverage deepened in `test_runtime_pipeline`. Missing startup-record resolution is now explicitly regression-guarded to emit plan warnings and keep startup debug capabilities disabled.
- 2026-05-12: C4/#161 build-inclusion coverage deepened in `test_runtime_pipeline`. Startup assets are now explicitly regression-guarded to stage into runtime packages even when marked excluded in project entries.
- 2026-05-12: C4/#160 startup-resolution coverage deepened in `test_runtime_pipeline`. Debug startup capability checks now treat `.PRG` extensions case-insensitively for breakpoint/step-debug support.
- 2026-05-12: C3/#159 menu lifecycle coverage deepened in `test_xasset_methods`. Assertions now lock cleanup routine/line identity and single shutdown emission for menu executable models.
- 2026-05-12: C3/#158 menu dispatch coverage deepened in `test_xasset_methods`. Menu executable-model assertions now lock startup setup→activate ordering and item action-to-routine binding fidelity.
- 2026-05-12: C2/#157 label pipeline coverage deepened in `test_xasset_methods`. Label executable-model assertions now lock preview-startup-only behavior (no synthetic methods/actions/shutdown lines).
- 2026-05-12: C2/#156 report pipeline coverage deepened in `test_xasset_methods`. Report executable-model assertions now lock preview-startup-only behavior (no synthetic methods/actions/shutdown lines).
- 2026-05-12: C1/#155 object-graph fidelity coverage deepened in `test_xasset_methods`. Form executable-model assertions now lock root-object selection behavior (ignore comment/data-environment records) and nested action-path preservation.
- 2026-05-12: C1/#154 lifecycle-order coverage deepened in `test_xasset_methods`. Form/class executable-model assertions now lock startup/shutdown sequence ordering for `DataEnvironment` and root-object lifecycle routines.
- 2026-05-12: H3/#94 structural rollback coverage deepened in `test_prg_engine_table_structure`. New focused `PACK MEMO` transaction rollback assertions now lock down `RECNO()` position retention alongside memo sidecar restoration and disk readability.
- 2026-05-12: H3/#93 SQL/result-cursor coverage deepened in `test_prg_engine_sql_cursors`. New focused `APPEND FROM ... TYPE JSON FOR ...` assertions now lock down row-filtering behavior on selected SQL/result cursors.
- 2026-05-12: H3/#92 optimizer-support coverage deepened in `test_prg_engine_index_seek_optimization`. New focused tests now lock `IndexSeekPlan::ExecutionStrategy` enum stability plus default `IndexOrderCandidate` metadata initialization used by planning/diagnostic paths.
- 2026-05-12: H3/#100 field-transfer coverage deepened in `test_prg_engine_data_io`. New focused `SCATTER/GATHER FROM array` tests now lock down `FIELDS LIKE` wildcard selection plus `FIELDS EXCEPT` write filtering (`AGE` exclusion preserved).
- 2026-05-12: Help tracker sync completed in `docs/23-vfp-help-and-component-mining.md`. The document now maps recent shipped slices (`#93/#94/#99/#101`) to the remaining `#100` field-transfer help-mining target (`SCATTER/GATHER` field-selection and transfer filtering semantics).
- 2026-05-12: H3/#94 structural rollback fidelity deepened in `test_prg_engine_table_structure`. New focused `ALTER TABLE ... ROLLBACK` coverage now locks down open-cursor pointer stability (`RECNO()` preserved) alongside schema restoration/readability.
- 2026-05-12: H3/#93 selected SQL/result-cursor export parity deepened in `test_prg_engine_sql_cursors`. New focused `COPY TO ... TYPE JSON FIELDS ...` coverage now locks down selected alias/pointer preservation plus filtered JSON field materialization for selected SQL/result cursors.
- 2026-05-12: H3/#101 headless interaction fidelity deepened in `test_prg_engine_data_io`. New focused `INPUT`/`ACCEPT` `TO LOCAL` coverage now locks down local-scope target assignment with deterministic headless values and no post-return global leak.
- 2026-05-12: H3/#99 memory-binding semantics coverage deepened in `test_prg_engine_control_flow`. New focused `RELEASE ALL` + `PRIVATE` shadow test now locks down in-frame restoration of outer globals and post-return stability.
- 2026-05-12: H3/#98 runtime-state normalization coverage deepened in `test_prg_engine_runtime_surface_functions`. Added focused `SET()` + `SET DATASESSION` readback/isolation assertions for `FDOW`, `FWEEK`, `POINT`, `SEPARATOR`, and `CURRENCY`, including current `FWEEK` normalization/clamp behavior.
- 2026-05-12: H3/#153 developer-facing diagnostic normalization deepened in `test_prg_engine_control_flow`. New focused repeated-fault `ON ERROR` coverage now verifies normalized `AERROR()` and helper function diagnostics (`ERROR()`/`MESSAGE()`/`LINENO()`/`PROGRAM()`) refresh per-fault without stale carry-over.
- 2026-05-12: H3/#152 stack-frame diagnostics coverage deepened in `test_prg_engine_control_flow`. New focused repeated-fault coverage now proves intermediate caller frames refresh routine + line metadata per pause (resume-PC semantics) instead of leaking stale values from prior faults.
- 2026-05-12: H3/#151 repeated-continue stability deepened in `test_prg_engine_control_flow`. New focused coverage now exercises two thrown faults inside `SET DATASESSION TO 2` and confirms selected alias plus record position remain stable after each `CONTINUE` pause/resume cycle.
- 2026-05-12: H3/#150 fault-state containment advanced in `src/runtime/prg_engine.cpp`, `src/runtime/prg_engine_session.inl`, and `src/runtime/prg_engine_dispatch.inl`. Fault snapshots now retain data-session id, and post-fault recovery (`RETRY`, `RESUME`, and normal ON ERROR unwind) now restores captured data-session/cursor state before returning to user code. Focused `test_prg_engine_control_flow` coverage now locks down handler-side `SET DATASESSION` / `SELECT` drift containment.

- 2026-05-12: H3/#93 slice issue `#263` is now shipped. `COPY TO` over selected synthetic SQL/result cursors now materializes DBF export output instead of returning a runtime event stub when no local `source_path` exists. Remote cursor schema/field-width inference now scans all remote rows before DBF export, and focused `test_prg_engine_sql_cursors` coverage now asserts selected-alias/pointer preservation plus exported row/value parity.
- 2026-05-12: H3/#93 slice issue `#264` is now shipped. `APPEND FROM` over selected synthetic SQL/result cursors now performs first-pass DBF-row append into in-memory remote/result rows with destination field-name mapping and `FIELDS` filtering instead of event-only stubbing, and unsupported non-DBF source types now return deterministic diagnostics. Focused `test_prg_engine_sql_cursors` coverage now validates selected-cursor append behavior.
- 2026-05-12: H3/#93 slice issue `#265` is now shipped. `COPY STRUCTURE TO` over selected SQL/result cursors now resolves schema through the shared cursor descriptor path, so metadata-only remote cursors (zero rows, schema present) export schema-only DBF output instead of failing due to missing local-table context. Focused `test_prg_engine_sql_cursors` coverage now validates this metadata-cursor schema export path.
- 2026-05-12: H3/#93 slice issues `#267` and `#268` are now shipped. Selected SQL/result cursor `APPEND FROM TYPE JSON` now maps JSON object keys to remote cursor field names and materializes rows into `remote_records`, and `APPEND FROM TYPE CSV` / `TYPE DELIMITED` now maps positional delimited values with header-row skip for CSV. Both branches honor `FIELDS` filtering and the `FOR` clause. Previously both paths returned a "DBF source only" stub error. Focused `test_prg_engine_sql_cursors` coverage now locks down JSON and CSV/DELIMITED → remote cursor append parity. The `#93` child queue is now exhausted.
- 2026-05-12: H3/#93 slice issue `#266` is now shipped. Selected SQL/result cursor `APPEND FROM` now honors `FOR` clause filtering during DBF-row append, so non-matching rows are not materialized into the target remote/result cursor. Focused `test_prg_engine_sql_cursors` coverage now validates the new `APPEND FROM ... FOR ...` behavior.

- 2026-05-12: H3/#92 temporary-order collation parity advanced in `test_prg_engine_seek_index`. Focused coverage now proves that a plain targeted local temporary order (`SET ORDER TO NAME IN People`) honors `SET COLLATE TO GENERAL` case-folding for `SEEK(..., 'People', 'NAME')` while preserving selected non-target alias/pointer state.

- 2026-05-12: H3/#97, `#99`, `#94`, `#100`, and `#93` queue issues `#258`-`#262` are now shipped. Macro parsing now supports alias-qualified field indirection with macro suffixes (`&cAlias..&cField`) in expressions and command contexts, macro-expanded assignment targets now preserve `PRIVATE` / `PUBLIC` identity instead of collapsing to current values, structural rollback coverage now explicitly proves `ALTER TABLE` and `PACK MEMO` leave tables readable after rollback, wildcard `FIELDS LIKE` / `FIELDS EXCEPT` transfer coverage now reaches `SCATTER/GATHER MEMVAR` plus DBF `COPY TO` / `APPEND FROM`, and targeted SQL `APPEND BLANK IN <alias>` mutation now has focused selected-alias/pointer-isolation coverage beside the existing targeted `REPLACE/DELETE/RECALL` tests.

- 2026-05-12: Issue tree synchronization note: current open-export issue inventory tracks active-open slices through `#203`; treat references to higher-number exploratory queues as stale until they appear in the live open issue tree.

- 2026-05-12: Gap-03 failure-injection/rollback coverage advanced across slice issues `#266`, `#267`, and `#268`. `test_dbf_table` now verifies deterministic staged-write rollback for DBF and memo sidecar write paths (original-bytes preservation plus artifact cleanup), and `src/vfp/dbf_table.cpp` now exposes test-only failure checkpoints (`temp-open`, `before-backup`, `before-promote`) gated by environment variables.
- 2026-05-12: Additional DBF header guardrail coverage advanced across three focused slices, locking down rejection of zero record-length headers, undersized header lengths, and header-length claims beyond physical file size.

- 2026-05-12: Gap-driven DBF robustness coverage advanced across slice issues `#260`, `#261`, `#262`, `#263`, `#264`, and `#265`. `test_dbf_table` now covers currency boundary round-trips, NaN/INF double survivability, descriptor parsing bounded by header length, record-width/field-layout mismatch rejection during mutation, malformed memo sidecar placeholder diagnostics, and full 11-byte non-null-terminated field-name tolerance. Runtime parsing was hardened in `src/vfp/dbf_table.cpp` so descriptor scanning stops at declared `header_length`.

- 2026-05-11: Critical-path Phase A runtime diagnostics advanced across slice issues `#151`, `#152`, `#153`, `#257`, `#258`, and `#259`. Cursor record position is preserved across a multi-fault CONTINUE cycle (#151). Intermediate call-stack frame line numbers at a fault pause reflect the resume PC, not zero (#152). AERROR() columns are consistent with ERROR()/MESSAGE()/LINENO()/PROGRAM() inside an ON ERROR handler (#153). Division by zero now dispatches a runtime error instead of silently returning +Infinity — guardrail added to `src/runtime/prg_engine_expression.inl` (#257). Numeric field overflow via REPLACE is either diagnosed or safely bounded (#258). DBF files with inflated header record counts are rejected or clamped by the parser (#259, GAP-02).

- 2026-05-11: Gap-04/gap-09b coverage extended across slice issues `#239`, `#238`, and `#246`. `test_prg_engine_control_flow` now covers `GO TOP`/`GO BOTTOM` on empty tables (no crash, `BOF()`/`EOF()` both true). `test_prg_engine_data_io` covers COPY TO empty table producing a valid zero-record DBF, APPEND FROM that empty DBF completing cleanly, and AERROR after a SQL pass-through fault returning a non-zero error code and non-empty message.

- 2026-05-11: Gap-05 field-write edge coverage advanced across slice issues `#242`, `#243`, and `#244`. Focused `test_prg_engine_table_mutation` tests now lock down `REPLACE` truncation to character-field width, exact-width character round-trips, and memo-field empty-string replacement. Runtime behavior was adjusted so truncation is scoped to `REPLACE` paths only; `INSERT INTO` continues to fail/rollback on overflow-width values.
- 2026-05-11: Gap-04 empty-table behavior coverage advanced across slice issues `#237`, `#241`, and `#240`. Focused `test_prg_engine_control_flow` tests now lock down that empty-table `SCAN` bodies do not execute, aggregate helpers return deterministic zeros on empty tables, and `LOCATE FOR .T.` over empty tables leaves `FOUND()` false with `EOF()` true. The slice shipped as focused coverage hardening (no runtime engine code changes required).
- 2026-05-11: Gap-07 runtime guardrail boundary precision advanced across slice issues `#254`, `#250`, and `#253`. Focused `test_prg_engine_control_flow` coverage now proves three invariants: nested routine chains exactly at `MAX_CALL_DEPTH` succeed, one-over-limit chains fail with the expected guardrail error, and `config.fpw` custom `MAX_CALL_DEPTH` overrides enforce the same boundary behavior. The runtime call-depth guardrail check now treats the configured limit as inclusive (off-by-one fixed in `can_push_frame()`), which closes the boundary mismatch identified by the new tests.
- 2026-05-11: Gap-06 security-control edge coverage advanced across slice issues `#247`, `#255`, `#245`, `#248`, and `#252`. `test_security_controls` now verifies that `role_has_permission` returns `false` for unknown/empty roles and for empty/nonexistent permissions, that `resolve_secret_reference` returns non-ok for absent env vars, that corrupting the last audit-entry hash causes the next append to diverge (tamper detectability), and that `append_immutable_audit_event` returns non-ok when the log parent directory cannot be created.
- 2026-05-11: Gap-08 transaction and work-area coverage advanced across slice issues `#251` and `#249`. `test_prg_engine_table_mutation` now verifies that `ROLLBACK` after `INSERT INTO` restores the pre-transaction record count on disk. `test_prg_engine_work_areas` now verifies that `USE ... ALIAS Area2 AGAIN IN 0` opens the same file in a second alias and that field mutations via Area1 are immediately visible when reading via Area2 in the same data session.

- 2026-04-30: Two more critical-path slices shipped back-to-back. Issue `#92` now has a first-pass runtime collation step for plain string indexed seeks, so non-`MACHINE` session collations such as `SET COLLATE TO GENERAL` case-fold plain `NAME`-style order comparisons when the order does not already carry an explicit `UPPER/LOWER` expression hint. Issue `#98` also tightened date/time runtime-state readback: `SET('DATE')` now defaults to `MDY`, and focused coverage now locks down `SET DATASESSION` isolation/restoration for `DATE`, `CENTURY`, `MARK`, `HOURS`, and `SECONDS`. Focused `test_prg_engine_seek_index` and `test_prg_engine_date_time_functions` coverage pass.
- 2026-04-30: The same `#98` session-state lane now has another six focused readback/isolation slices locked down: `COLLATE`, `NULL`, `ANSI`, `REPROCESS`, `MULTILOCKS`, and `EXCLUSIVE`. Focused `test_prg_engine_runtime_surface_functions` and `test_prg_engine_table_mutation` coverage now verifies their default values plus `SET DATASESSION` restoration behavior, which narrows the remaining runtime-state backlog to smaller semantic gaps instead of broad untested state surface.
- 2026-04-30: Issue `#92` deepened again in the grounded-order filter path. The local seek runtime no longer treats most loaded `FOR` expressions as metadata-only; in addition to `DELETED()` filters it now honors first-pass top-level string and numeric comparisons such as `NAME = 'BRAVO'` and `AGE >= 20`, including `SET NEAR` behavior against the surviving candidate set. Focused `test_prg_engine_seek_index` coverage passes.
- 2026-04-30: Slice issue `#103` is now closed. Synthetic SQL/result-cursor plain-string `SEEK` now has focused parity coverage proving that `SET COLLATE TO GENERAL` case-folds temporary `NAME`-style searches on SQL cursors just as it already did on local DBF-backed cursors. Focused `test_prg_engine_sql_cursors` coverage passes.
- 2026-04-30: Slice issues `#105` and `#106` are now closed. Formatting-state session isolation is now explicitly defended for `MEMOWIDTH`, `POINT`, `SEPARATOR`, and `CURRENCY`, calendar/week-state isolation is now explicitly defended for `FDOW` and `FWEEK`, and the shared `SET()` readback path now preserves numeric/string option values instead of misreporting values like `FDOW=1` as `ON` after session restoration. Focused `test_prg_engine_string_math_functions` and `test_prg_engine_date_time_functions` coverage passes.
- 2026-04-30: Slice issues `#102`, `#104`, and `#107` are now closed. Synthetic SQL/result cursors now honor first-pass `FOR` filters on ad hoc temporary orders, including command-path `SET ORDER TO UPPER(NAME) FOR ...` and helper-path `SEEK(..., 'sqlcust', "UPPER(NAME) FOR ...")`, so filtered candidate sets and `SET NEAR` positioning now line up with the shipped local grounded-order behavior. That same ad hoc-order lane now also consumes grounded field-type metadata for plain numeric field orders such as `SET ORDER TO AGE` / `SET ORDER TO AMOUNT`, so local and SQL temporary numeric seeks no longer fall back to lexicographic near-ordering. The adjacent runtime-state lane now also has explicit `SET('FIELDS')` data-session isolation coverage alongside the already-shipped `PATH` / `DEFAULT` checks. Focused `test_prg_engine_sql_cursors`, `test_prg_engine_seek_index`, and `test_prg_engine_work_areas` coverage pass.
- 2026-04-30: Slice issues `#123` and `#124` are now closed. Macro-expanded identifier resolution now preserves alias-qualified field continuations such as `&cAlias..NAME`, and recursive `TEXTMERGE(..., .T.)` evaluation now keeps merging until the text stabilizes instead of stopping after one extra pass. Focused `test_prg_engine_functions` and `test_prg_engine_string_math_functions` coverage pass.
- 2026-04-30: Slice issues `#125` and `#126` are now closed. Releasing current-frame `LOCAL` names now reveals the outer global/public value they were masking instead of erasing it, and `.mem` persistence now avoids saving `PUBLIC` scope markers when the visible binding is actually a shadowing `LOCAL`/`PRIVATE` over an outer public. Focused `test_prg_engine_control_flow` and `test_prg_engine_data_io` coverage pass.
- 2026-04-30: Slice issues `#117` and `#118` are now closed. `ALTER TABLE ... ADD COLUMN ... DEFAULT ...` now backfills existing rows instead of leaving the new field blank, and rollback replay now prunes stale `ALTER TABLE`-introduced field rules from live local cursors so later inserts use the restored schema instead of trying to write phantom fields. Focused `test_prg_engine_table_structure` and `test_prg_engine_table_mutation` coverage pass.
- 2026-04-30: Slice issues `#127` and `#128` are now closed. `SCATTER NAME` / `GATHER NAME` now expand macro-driven nested object-path segments such as `&cHolder.&cChild`, and `APPEND FROM ARRAY` now serializes typed date/datetime values through the shared DBF field contract instead of treating every array element as an opaque string. Focused `test_prg_engine_data_io` coverage pass.
- 2026-04-30: Slice issues `#129`, `#130`, `#115`, and `#116` are now closed. `INPUT` / `ACCEPT` now emit resolved prompt and macro-target detail just like the deeper `WAIT` lane, `DISPLAY` / `LIST RECORDS` now preserve raw and resolved `IN` target payload detail, SQL/result cursors now honor macro-expanded `SET FIELDS TO &cSpec` visible-field metadata plus `SET FILTER TO &cExpr IN <alias>` filter flows, and SQL cursor `LOCATE` / `DELETE` / `RECALL FOR &cExpr` paths now evaluate macro-expanded boolean text instead of treating it as a truthy string. Focused `test_prg_engine_data_io` and `test_prg_engine_sql_cursors` coverage pass.
- 2026-04-30: Slice issues `#119` and `#120` are now closed. Aggregate command events now expose targeted cursor/view metadata, scope, clause, and target-detail readback instead of only raw command text, and focused helper coverage now proves `LOOKUP()` parity over synthetic SQL/result cursors without disturbing a separately selected local work area. Focused `test_prg_engine_control_flow` and `test_prg_engine_runtime_surface_functions` coverage pass.
- 2026-04-30: Slice issues `#121` and `#122` are now closed. DBC/container fidelity now resolves same-base `DCT`/`DCX` companions and exported `TABLE` `.dbf` assets case-insensitively on case-sensitive filesystems, and `export_database_as_json(...)` now prefers the catalog `DATABASE` object name over the raw `.dbc` stem while preserving decoded `PROPERTIES` memo bags and table-row snapshots. Focused `test_vfp_assets` coverage passes.
- 2026-04-30: Slice issues `#131` and `#132` are now closed. OLE object property assignment now round-trips through runtime object state instead of collapsing to placeholder `ole:` strings, and the seeded `Scripting.Dictionary` compatibility surface now supports first-pass `Add`, `Exists`, `Item`, `Remove`, `RemoveAll`, and live `Count` updates for deterministic collection-style behavior. Focused `test_prg_engine_runtime_surface_functions` coverage passes.
- 2026-04-30: Slice issues `#133` and `#134` are now closed. `NEWOBJECT()` now preserves library/server activation detail in runtime object state and `ole.newobject` events, while `GETOBJECT()` now resolves class-vs-source targeting more faithfully and reuses matching existing attachments instead of always registering duplicate handles for the same running class or same file/class pair. Focused `test_prg_engine_runtime_surface_functions` coverage passes.
- 2026-04-30: Slice issues `#135` and `#136` are now closed, and parent issue `#12` is closed with them. Unresolved OLE reads/invokes and missing seeded `Scripting.Dictionary` members now trap as `1429` automation faults instead of failing silently, so `ON ERROR` / `AERROR()` and `TRY/CATCH/FINALLY` can contain those failures while preserving later object/session usability. Focused `test_prg_engine_control_flow` coverage passes.
- 2026-04-30: Slice issues `#142` and `#143` are now closed. Thrown expression faults such as `LOG(-1)` and `ACOS(2)` now have explicit pause-state coverage proving that line/statement/message metadata refresh correctly across multiple continues and that continuing after one or more thrown-expression faults keeps the session alive. Focused `test_prg_engine_control_flow` coverage passes.
- 2026-04-30: Slice issues `#144` and `#145` are now closed. Error-pause debugger metadata now uses the actual faulting line for the top stack frame when nested routines fault instead of the post-incremented next statement, and focused coverage now locks down nested-routine stack-frame fidelity plus repeated nested-fault metadata refresh. Focused `test_prg_engine_control_flow` coverage passes.
- 2026-04-30: Slice issues `#146`, `#147`, `#148`, and `#149` are now closed, and parent lane `#95` is now closed with them. `COUNT` / `SUM` / `AVERAGE` plus `CALCULATE` now resolve `TO &macro` targets through the normal runtime target path, `CALCULATE` now preserves `WHILE` clause metadata/execution, `DISPLAY` / `LIST RECORDS` now preserve `WHILE` clause detail, `DISPLAY` / `LIST STRUCTURE` now honor `IN <alias|work area>` targeting with raw/resolved target detail, `.NULL.` now parses as a real expression literal instead of falling through to OLE-property lookup, and focused `LOOKUP()` coverage now locks down variable-driven alias/tag targeting. Focused `test_prg_engine_control_flow`, `test_prg_engine_runtime_surface_functions`, and `test_prg_engine_data_io` coverage pass.
- 2026-04-30: The `#99` memory-file lane deepened again. `SAVE TO` now preserves runtime arrays with dimensions and records `PUBLIC` scope markers instead of only serializing plain scalar globals; `RESTORE FROM` now recreates arrays and `PUBLIC` bindings, additive restore now writes through visible `LOCAL` bindings instead of hiding restored values behind stale locals, and non-additive restore now clears stale arrays plus deferred `PRIVATE` shadow state before rebuilding the saved memory set. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-30: Local DBF-backed indexed-search parity now accepts the same first-pass ad hoc order expressions that were already available on synthetic SQL cursors. Local `SET ORDER TO UPPER(NAME)`, `SEEK('bravo', 'People', 'UPPER(NAME)')`, and descending one-off local probes such as `SEEK('beta', 'People', 'UPPER(NAME) DESCENDING')` now derive normalization/collation hints and search through local table rows instead of rejecting non-tag expressions. Focused `test_prg_engine_seek_index` coverage passes.
- 2026-04-30: The adjacent headless command/event lane deepened substantially toward issue #7/#8 closure. `INPUT` / `ACCEPT` now reuse selected-cursor/view metadata and assign deterministic empty-string headless `TO <target>` results; `WAIT` now captures resolved prompt/timeout/target state plus source expressions for common `WAIT [WINDOW] ... [TIMEOUT ...] [TO <target>] [NOWAIT] [NOCLEAR]` forms; `KEYBOARD` now captures resolved key payloads plus source expressions and common `PLAIN` / `CLEAR` flags; `DISPLAY` / `LIST` `STRUCTURE` and `STATUS` forms now emit selected-cursor schema and current session/view metadata; and `DISPLAY` / `LIST` `MEMORY` now emit visible and shadowed public/private/local/global memory-variable summaries, scoped array detail, and first-pass object previews. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-30: Financial/misc expression-function batch shipped. The runtime now supports first-pass `HEX()` (uppercase hex without prefix), `FV()`, `PV()`, and `PAYMENT()` financial helpers in `prg_engine_numeric_functions.cpp`; first-pass `NEWID()` (UUID v4 via `mt19937_64`), `CPCURRENT()` / `CPCONVERT()` / `CPDBF()` code-page stubs, and headless dialog/context stubs `GETPICT()` / `GETCOLOR()` / `GETFONT()` / `VARREAD()` in `prg_engine_runtime_surface_functions.cpp`; and first-pass `TEXTMERGE()` with configurable delimiters and recursive evaluation support, first-pass `EXECSCRIPT()` RETURN-expression pass-through, and first-pass `LOOKUP()` seek-and-evaluate over local alias/tag indexes in `prg_engine_expression.inl`. Focused `test_prg_engine_string_math_functions` and `test_prg_engine_runtime_surface_functions` coverage passes.
- 2026-04-29: The next deeper `SCATTER/GATHER` and modern interchange slice shipped. `SCATTER NAME` without `ADDITIVE` now replaces an existing target object on nested paths instead of mutating it in place and leaking unrelated stale properties through the old object instance. In parallel, `COPY TO ... TYPE JSON` and `APPEND FROM ... TYPE JSON` now provide a first-pass modern object-array interchange lane with typed numeric/logical emission and import by matching field names through the shared append/replace DBF mutation path. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-29: The next adjacent interchange/object slice shipped. `COPY TO ... TYPE TAB` and `APPEND FROM ... TYPE TAB` now reuse the delimited-text substrate with tab separators as a real first-pass interchange lane instead of requiring callers to spell the broader `DELIMITED` form. `SCATTER NAME` / `GATHER NAME` also now create missing nested object targets such as `oHolder.Row` on demand during scatter, so nearby object-path runtime flows no longer require those child objects to be pre-seeded before they can participate. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-29: Another adjacent interchange/object slice shipped. `COPY TO ... TYPE SYLK` and `APPEND FROM ... TYPE SYLK` now have a real first-pass text interchange lane using a conservative SYLK-style serializer/parser with header-row emission and import header skipping, again reusing existing `FIELDS` filtering and the shared append/replace DBF mutation path. `SCATTER NAME` / `GATHER NAME` also now resolve nested object-property targets such as `oHolder.Row`, so runtime object scattering/gathering can operate on an object already stored inside another object property instead of only top-level variables. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-29: The next adjacent interchange/object slice shipped. `COPY TO ... TYPE DIF` and `APPEND FROM ... TYPE DIF` now have a real first-pass interchange lane using a conservative DIF-style text serializer/parser with header-row emission and import header skipping, flowing through the same `FIELDS` filtering and shared append/replace DBF mutation path as the other non-DBF formats. `SCATTER NAME` / `GATHER NAME` now also resolve macro-expanded object variable names such as `NAME &cObjectName`, so object-target command forms line up better with the macro-expanded array-name behavior already shipped nearby. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-29: Adjacent local-data parity slices shipped for opaque writes and first-pass spreadsheet interchange. Shared DBF direct writes now accept conservative opaque/binary payloads on otherwise readable unsupported field families: callers can clear with `NULL`, write raw text that fits the field width, or use `0x`-prefixed hex payloads, while `APPEND BLANK` still zero-initializes those field bytes. Runtime/local-command coverage also now includes a first-pass SpreadsheetML lane for `COPY TO ... TYPE XLS` and `APPEND FROM ... TYPE XLS`, reusing existing `FIELDS` filtering and shared append/replace mutation paths instead of adding an event-only stub. Focused `test_dbf_table`, `test_prg_engine_table_mutation`, and `test_prg_engine_data_io` coverage passes.
- 2026-04-29: Local DBF mutation closed an `APPEND BLANK` layout gap. Tables that include otherwise readable opaque/unsupported field families (for example synthetic `W` fields) no longer hard-fail `APPEND BLANK`; the append path now zero-initializes those field bytes conservatively and grows the table normally, which keeps runtime mutation flowing over mixed-layout tables without overclaiming direct typed write support for those field families. Focused `test_dbf_table` and `test_prg_engine_table_mutation` coverage passes.
- 2026-04-29: Issue #8 macro/runtime follow-up trio shipped. Logical-line preprocessing now keeps `&&` inside nested double-quoted, bracketed, and braced text instead of truncating those expressions as comments; `PARAMETERS` / `LPARAMETERS` now honor default-value expressions when callers omit trailing arguments, including macro-expanded defaults such as `LPARAMETERS x = &cExpr`; and focused regression coverage now locks down macro-expanded `FOR &cExpr` filters for SQL-style `DELETE FROM` and `UPDATE` command forms. `test_prg_engine_functions` and `test_prg_engine_table_mutation` pass.
- 2026-04-29: `SCATTER NAME` object-state parity gained additive reuse semantics. The parser now recognizes `ADDITIVE` on `SCATTER`, and `SCATTER NAME <object> ADDITIVE` reuses an existing runtime object when one is already bound instead of always allocating a fresh `Empty` object. Existing non-field properties are preserved while matching field properties are refreshed from the current record and missing field properties are added. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-27: Issue #7 parser and PUBLIC-release parity advanced. The shared top-level command keyword scanner now ignores keywords inside double-quoted strings, bracketed expressions, and braced blocks, and command CSV splitting keeps braced block commas together. `PUBLIC` declarations now track public identity, so `RELEASE ALL`, `RELEASE ALL LIKE`, and `RELEASE ALL EXCEPT` preserve public-pinned scalar variables and arrays while still releasing matching non-public bindings. Focused `test_prg_engine_control_flow` coverage passes.
- 2026-04-27: Issue #7/#8 runtime-state and command-surface chunk shipped. `UNLOCK RECORD <n> [IN <alias>]` now releases only the requested record lock while preserving table/file locks and other record locks. Common `SET` option handling now evaluates/normalizes literal, variable, and macro-expanded RHS values for `PATH`, `MARK`, `DECIMALS`, `COLLATE`, `NULL`, `ANSI`, and related date/numeric/string state. Macro-expanded array names now resolve consistently for `SCATTER TO`, `GATHER FROM`, `COPY TO ARRAY`, and `APPEND FROM ARRAY`. Focused `test_prg_engine_table_mutation`, `test_prg_engine_runtime_surface_functions`, and `test_prg_engine_data_io` coverage passes.
- 2026-04-27: Copilot follow-up audit fixed transaction and STORE correctness gaps. Durable local DBF transaction journaling now covers additional direct mutation paths (`PACK MEMO`, `CREATE TABLE`, `ALTER TABLE`, `APPEND FROM ARRAY`, `APPEND FROM` DBF/SDF/CSV/delimited, and `GATHER`), and rollback refresh closes local cursors whose backing table was removed by replay. `STORE` now uses the ordinary assignment-target path, so scalar, direct array-element, macro-expanded array-element, `m.` namespace, and OLE property targets stay consistent with normal assignment. Focused `test_prg_engine_table_mutation` and `test_prg_engine_arrays` coverage passes.
- 2026-04-27: Runtime diagnostics depth advanced with handler-stable fault snapshots. During `ON ERROR` handler execution, `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, `SYS(2018)`, and `AERROR()` now remain bound to the original fault even if the handler triggers and catches a secondary runtime error. The snapshot is released on handler return and on `RETRY` / `RESUME` unwinds. Focused `test_prg_engine_control_flow` coverage passes.
- 2026-04-27: Runtime array semantics gained macro-expanded identifier depth. Helper entry points such as `ALEN(&cArrayName)`, `ALINES(&cTargetName, ...)`, and `ACOPY(&cSourceName, &cTargetName, ...)` now resolve macro-expanded array identifiers through the existing bounded macro path, and direct macro-expanded element access now works for both bracket and paren syntax (`&cArrayName[2,1]`, `&cArrayName(1,2)`). Focused `test_prg_engine_arrays` coverage passes.
- 2026-04-27: `SCATTER` / `GATHER` date and datetime parity advanced. `SCATTER` now converts `D` and `T` fields into runtime-facing `MM/DD/YYYY` and `MM/DD/YYYY HH:MM:SS` strings, `SCATTER ... BLANK` now produces blank string values for those field types, and `GATHER MEMVAR` / `GATHER FROM <array>` now restore date/datetime/blank values back into DBF storage consistently. Focused `test_prg_engine_data_io` coverage passes.
- 2026-04-27: Command-level aggregate scope depth advanced for `COUNT`. The stale non-`ALL/FOR/TO` limitation path is gone; `COUNT REST`, `COUNT NEXT <n>`, `COUNT RECORD <n>`, and `COUNT WHILE <expr>` now run through the shared aggregate scope collector while preserving existing visibility/filter/deleted semantics and `TO ARRAY` assignment behavior. Focused `test_prg_engine_control_flow` and `test_prg_engine_aggregate_array_functions` coverage passes.
- 2026-04-27: CALL command-surface semantics gained adjacent depth. Parser support now recognizes `CALL <target> WITH ...` argument forms (instead of treating the whole body as an identifier), and dispatch now resolves external `.prg` targets for `CALL` similarly to `DO` when local routines are not found. Focused `test_prg_engine_control_flow` coverage now validates `CALL ... WITH` LPARAMETER binding and external `CALL <file> WITH @var` BYREF write-back behavior.
- 2026-04-27: TYPE()/TRANSFORM() semantics gained adjacent depth. `TYPE('<expr>')` now strips fully wrapping parentheses before classification/evaluation so resolvable parenthesized expressions and array-element expressions return concrete type codes while preserving existing bare-array `A` detection. `TRANSFORM()` now supports first-pass `@Z` (blank output for zero numeric values) and `@B` (left-justified trimmed output), while preserving existing `@!`, `@L`, and numeric decimal-picture behavior. Focused `test_prg_engine_functions` coverage validates new TYPE and TRANSFORM cases.
- 2026-04-27: Macro/eval semantics depth advanced with bounded indirect macro dereferencing in expression parsing. `&name` now follows identifier chains (e.g., `cMacroName -> cExpr -> 'x+1'`) before expression evaluation, while deterministic recursion protection stops self-referential/cyclic chains safely and preserves the existing fallback-to-string behavior for non-evaluable expansions. Focused `test_prg_engine_functions` coverage now validates direct macro evaluation, one-level indirect macro evaluation, cyclic/self-reference safety, and non-expression fallback behavior.
- local DBF-backed runtime flow now covers `GO/GOTO`, `SKIP`, `SEEK`, `LOCATE`, `SCAN`, `REPLACE`, `APPEND BLANK`, `DELETE`, `RECALL`, `SET FILTER TO/OFF`, aggregate built-ins, command-level `CALCULATE`, first-pass command-level `COUNT` / `SUM` / `AVERAGE`, first-pass `TOTAL`, and core work-area/data-session behavior including `SELECT`, `USE`, `USE IN`, `USE AGAIN`, `SET DATASESSION`, and expression-based cursor designators for `SET FILTER ... IN`, `SELECT <expr>`, and `USE ... IN <expr>`
- indexed runtime behavior now has first-pass `SET ORDER TO TAG`, `FOUND()`, `SET EXACT`, `SET NEAR`, command/function `SEEK`, `INDEXSEEK()`, `ORDER()`, and `TAG()` support plus conservative index metadata plumbing for `CDX/DCX/IDX/NDX/MDX`
- recent runtime-language work also includes `DO CASE`, `DO WHILE`, `LOOP` / `CONTINUE` / `EXIT`, literal `TEXT/ENDTEXT`, `EVAL()`, `SET()`, `&macro`, `PRIVATE`, `STORE`, `WITH/ENDWITH`, `TRY/CATCH/FINALLY/ENDTRY`, `DO ... WITH @var` reference semantics, and `ELSEIF` branches
- 2026-04-14: expanded PRG expression function coverage (~60 new functions) and new command execution surface. `evaluate_function()` now handles: `LEN`, `LEFT`, `RIGHT`, `UPPER`, `LOWER`, `LTRIM`/`TRIM`, `RTRIM`, `SPACE`, `REPLICATE`, `STRTRAN`, `STUFF`, `ASC`, `VAL`, `OCCURS`, `EMPTY`, `ISNULL`/`ISEMPTY`, `VARTYPE`, `TYPE`, `IIF`, `NVL`, `BETWEEN`, `INT`, `ABS`/`FABS`, `ROUND`, `MOD`, `SQRT`, `CEILING`/`FLOOR`, `EXP`, `LOG`, `PI`, `SIGN`, transcription date functions (`DATE`, `TIME`, `DATETIME`, `YEAR`, `MONTH`, `DAY`, `DTOS`, `CTOD`, `DTOC`, `TTOC`), array functions (`ALEN`, `ADEL`, `AINS`, `ASORT`, `ASCAN`, `ASIZE`), path functions (`GETENV`, `FULLPATH`, `JUSTFNAME`, `JUSTSTEM`, `JUSTEXT`, `ADDBS`), character-class functions (`ISDIGIT`, `ISALPHA`, `ISLOWER`, `ISUPPER`), and cursor functions (`CURSORSETPROP`/`CURSORGETPROP`). New commands added: `CLOSE ALL`/`CLOSE TABLES`/`CLOSE DATABASES`, `ERASE`/`DELETE FILE`, `COPY FILE ... TO`, `RENAME ... TO`, `?`/`??` print-to-event, `CREATE CURSOR`, `COPY TO`, `APPEND FROM`, `SCATTER MEMVAR`, `GATHER MEMVAR`. Committed as `75d5102`.
- 2026-04-16: `COPY TO` and `COPY STRUCTURE TO` are now fully implemented (local DBF export via `create_dbf_table_file` with optional `FIELDS`/`FOR` filtering; `COPY STRUCTURE TO` emits schema only, zero records). `APPEND FROM` is now fully implemented (parses source DBF via `parse_dbf_table_from_file`, inserts each non-deleted record via `append_blank_record_to_file` + `replace_record_field_value`). `GATHER MEMVAR` correctness was fixed: the old string-quoted back-expression round-trip was replaced with direct per-field `replace_record_field_value` calls that preserve type fidelity. A related bug was fixed in the assignment handler: dotted identifiers with `m.` prefix were being incorrectly dispatched to the OLE property path; `m.` is now recognized as the VFP memory-variable namespace and stored as a plain variable. Four focused regression tests added to `test_prg_engine`; all pass.
- 2026-04-16: `m.` memory-variable namespace parity is now tighter across assignment, lookup, expression evaluation, and declarations. `m.<name>`, `M.<name>`, and bare `<name>` share one local/global runtime binding, and prefixed references no longer fall through to OLE property resolution. Focused `test_prg_engine` coverage passes.
- 2026-04-16: `COPY TO ... TYPE SDF` and `APPEND FROM ... TYPE SDF` now have first-pass fixed-width text support using local DBF field descriptor widths, existing `FIELDS`/`FOR` export filtering, and the shared append/replace mutation path for import. Focused `test_prg_engine` coverage passes.
- 2026-04-16: `SCATTER` / `GATHER` now honor `FIELDS`, `BLANK`, typed memvar values, `GATHER ... FOR`, and first-pass one-dimensional arrays via `SCATTER TO <array>` / `GATHER FROM <array>`. `ALEN(<array>)`, `ALEN(<array>, 1/2)`, `array[1]`, and `array(1)` now work for runtime arrays created by this path. Focused `test_prg_engine` coverage passes. This is the intended substrate for the next `AERROR()` array slice.
- 2026-04-16: `AERROR(<array>)` now populates a first-pass structured one-row/seven-column runtime error array for normal runtime errors: code, message, mixed-case error parameter, selected work area, and empty trigger/reserved columns. `SYS(2018)` exposes the uppercase error parameter, while `MESSAGE()`, `PROGRAM()`, `LINENO()`, `ERROR()`, and `ON('ERROR')` retain the richer handler context. `UPDATE <alias> SET field = expr [, ...] WHERE/FOR <expr>` also shipped as a scoped mutation command over local/remote cursors while preserving current-record `REPLACE` semantics. Focused `test_prg_engine` coverage passes.
- 2026-04-16: Runtime arrays now support first-pass `ACOPY()`, `AELEMENT()`, `ASIZE()`, `ASCAN()`, `ADEL()`, `AINS()`, `ASORT()`, and `ASUBSCRIPT()`. `UPDATE` grammar now also covers `UPDATE SET ...` against the selected cursor and `UPDATE IN <alias> SET ...`. Focused `test_prg_engine` coverage passes.
- 2026-04-16: Runtime arrays now support first-pass `DIMENSION`, array-form `DECLARE`, bracket/paren element assignment (`array[1] = ...`, `array[1,2] = ...`, `array(1) = ...`, `array(1,2) = ...`), declared 1D/2D `ALEN()` queries, and 2D preservation across `ASIZE()` growth. The shared command CSV splitter now keeps commas inside `[]` dimensions together. Focused `test_prg_engine` coverage passes.
- 2026-04-16: Runtime arrays now also support first-pass `ALINES()`, `ADIR()`, and `AFIELDS()`. Diagnostics now emit VFP-style `AERROR()` rows for SQL/ODBC-like `1526` failures and OLE/automation-like `1429` failures in addition to normal seven-column runtime errors. Text import/export now covers `COPY TO` / `APPEND FROM` `TYPE CSV` and `DELIMITED` paths, including CSV field-name headers, quoted character fields, matching-header skip on import, and VFP-style `WITH <enclosure>` plus `WITH CHARACTER <separator>` options. Focused `test_prg_engine` coverage passes.
- 2026-04-16: `COPY TO ARRAY <array>` and `APPEND FROM ARRAY <array>` are now implemented. `COPY TO ARRAY` populates a two-dimensional runtime array (rows=qualifying records, cols=fields) with optional `FIELDS` and `FOR` filtering. `APPEND FROM ARRAY` appends each array row as a new record, mapping columns positionally to destination fields with optional `FIELDS` filtering. Two focused `test_prg_engine` regression tests (round-trip via source→array→dest DBF) pass.
- Architecture/language guidance from 2026-04-16: keep runtime/file-format hot paths in fast native code by default; C# is allowed where it materially helps higher-level UI/tooling surfaces. If C# is used for visual surfaces, create companion files that remain maintainable in Visual Studio 2026 visual editors. Avoid broad refactors just to change language stacks. Future transpilation targets beyond the current scope can be selected later based on customer demand.
- recent refactoring already extracted `prg_engine_helpers.{h,cpp}` and `prg_engine_command_helpers.{h,cpp}`, so prefer building on those seams instead of adding fresh monolith-only helpers unless there is a good reason
- 2026-04-17: FIELDS LIKE/EXCEPT wildcard field filtering is now fully implemented: `field_wildcard_match()` helper, sentinel-prefix approach (`__LIKE__`/`__EXCEPT__`) in the field filter clause parser, and `parse_field_filter_clause` rewritten to support VFP `*`/`?` wildcard patterns and `EXCEPT <pattern>` negation. Focused `test_prg_engine` coverage passes.
- 2026-04-17: ON ERROR / RETRY / RESUME fault-PC bookmarking is now fully implemented. `retry_statement` and `resume_statement` added to `StatementKind`. Fault PC fields on Impl (`fault_frame_file_path`, `fault_statement_index`, `fault_pc_valid`). `dispatch_error_handler` saves the fault PC; RETRY unwinds the stack and re-executes the faulting statement; RESUME advances past the fault. Focused `test_prg_engine` coverage passes.
- 2026-04-17: String function extensions now include `PADL()`/`PADR()`/`PADC()` (padding with optional char, PADC splits L/R), `CHRTRAN()` (char-by-char translate/delete), `GETWORDCOUNT()`/`GETWORDNUM()` (delimiter-based word split), and `STREXTRACT()` (nth-occurrence delimited extraction with case-insensitive flag). Focused `test_prg_engine` coverage passes.
- 2026-04-17: Linux-side validation is now available through repo-local `.codex-venv` CMake plus `scripts/validate-posix.sh`, which runs under `zsh`, `bash`, or `sh`; local generated artifacts are ignored. The Linux path exposed and fixed portability issues in Windows-only process includes, PowerShell test launching, path-separator-specific exporter assertions, DBF memo-sidecar directory reads, project-workspace Windows-path fallback handling, runtime-host canonical-name validation, and non-Windows SHA-256 hashing. The full native CTest suite builds and passes under the local Linux CMake/Ninja toolchain.
- 2026-04-17: The next adjacent expression-function utility batch is implemented: occurrence-aware `AT()` / `RAT()`, case-insensitive `ATC()` / `RATC()`, line-oriented `ATLINE()` / `ATCLINE()` / `RATLINE()`, wildcard `LIKE()`, `INLIST()`, and `PROPER()`. Focused coverage was added to `test_prg_engine`.
- 2026-04-17: DECLARE DLL / .fll / .NET interop is now fully implemented. `DeclaredDllFunction` struct (`alias`, `function_name`, `dll_path`, `return_type`, `param_types` as comma-separated string, `is_dotnet`, `hmodule`, `proc_address`, `dotnet_type_name`, `dotnet_method_name`) lives in `prg_engine_internal.h`. Windows DLL/CLR headers added (`<windows.h>`, `<metahost.h>`, `#pragma comment(lib,"mscoree.lib")`, `<comdef.h>`, `<oaidl.h>`). `declared_dll_functions` map on Impl. DECLARE DLL parser added to `prg_engine_parser.cpp`. `invoke_declared_dll_function` method fully implemented:
  - Native path: x64 typed function pointer dispatch (FnI_0..FnI_8, FnD_0..FnD_4) and x86 `__stdcall` dispatch (FnSI_0..FnSI_8); string args passed as `const char*` with UTF-8 buffers kept alive; return type `"c"`/`"string"` interprets return as `const char*`.
  - .NET path: IDispatch late-binding CLR host (no `#import mscorlib.tlb` — that conflicts with WinSDK headers). `CLRCreateInstance` → `ICLRMetaHost` → `GetRuntime("v4.0.30319")` → `ICorRuntimeHost::Start()` → `GetDefaultDomain` as `IDispatch`. Assembly loaded via AppDomain::Load (IDispatch call); type/method located via `GetType`/`GetMethod`; args passed as SAFEARRAY of VARIANTs; return VARIANT unwrapped to PrgValue.
  - `declared_dll_invoke_callback_` wired in ExpressionParser (arg 51, after `record_event_callback` arg 50 — order matters).
  - Build clean, all `test_prg_engine` tests pass (8.08 sec, 100%).
- 2026-04-17: `PACK`, `PACK MEMO`, and `ZAP` now have first-pass runtime support. Local DBF-backed `PACK` physically removes deleted records while preserving schema and kept-row order; `PACK MEMO` rewrites memo-backed tables from current memo values to compact stale sidecar blocks while preserving row count/deleted flags; `ZAP` truncates local tables to zero records while keeping them appendable. Synthetic remote/result cursors get matching in-memory behavior where applicable. Focused coverage verifies `RECCOUNT()`, readable DBF output, file-size shrinkage, events, append-after-`ZAP`, and memo-sidecar compaction.
- 2026-04-17: SQL-style mutation now includes first-pass `DELETE FROM <target> WHERE/FOR <expr>` and `INSERT INTO <target> [(fields)] VALUES (...)` over local DBF-backed and synthetic remote/result cursors. `DELETE FROM` has its own statement kind so conditionless SQL-style deletes target all rows instead of inheriting bare `DELETE` current-record semantics. `INSERT INTO` appends via the existing blank-row plus field-replacement path, supports expression-driven cursor targets, and maps omitted field lists by schema order. The command CSV splitter now protects commas inside single- and double-quoted tokens; expression-level double-quoted string semantics remain a separate language-compatibility slice. Focused `test_prg_engine` coverage passes.
- 2026-04-17: `INSERT INTO` failure handling now rolls back the appended blank row when a later field write fails. Local DBF-backed inserts truncate the table back to the pre-insert record count; synthetic remote/result cursors resize the in-memory row set back to its original count. Focused `test_prg_engine` coverage verifies oversized character insert rollback and DBF readability.
- 2026-04-17: `CREATE TABLE` now has first-pass local DBF support for simple field declarations (`C`/`CHAR`, `N`/`NUMERIC`, `F`/`FLOAT`, `L`/`LOGICAL`, `D`/`DATE`, `I`/`INTEGER`, `Y`/`CURRENCY`, `T`/`DATETIME`, and `M`/`MEMO`). The command creates a DBF through `create_dbf_table_file`, opens it as a local cursor, and immediately supports `INSERT INTO`, field lookup, and `RECCOUNT()`. Focused `test_prg_engine` coverage passes.
- 2026-04-17: `ALTER TABLE ... ADD COLUMN` now has first-pass local DBF support. The runtime appends one supported field by rewriting the DBF schema while preserving existing row values and deleted flags, then updates any open cursor that points at the same table path. `CREATE TABLE` declarations now also accept broader DBF write-surface type names (`DOUBLE`, `VARCHAR`, `VARBINARY`) plus `NULL` / `NOT NULL` / `DEFAULT` annotations as parser syntax. Direct SQL-style `INSERT INTO <sqlcursor>` and `DELETE FROM <sqlcursor> WHERE ...` are covered over synthetic `SQLEXEC()` result cursors with selected-alias preservation. Focused `test_prg_engine` coverage passes; `test_dbf_table` remains green.
- 2026-04-17: `ALTER TABLE ... DROP COLUMN` and `ALTER TABLE ... ALTER COLUMN` now share the local DBF schema rewrite path, and `DEFAULT` / `NOT NULL` annotations are enforced for created or altered open cursors during `INSERT INTO` with rollback on constraint failures. Structural field parsing moved to `src/runtime/prg_engine_table_structure_helpers.{h,cpp}`, and new structural regressions live in `tests/test_prg_engine_table_structure.cpp` / `test_prg_engine_table_structure` instead of growing the main PRG-engine test file. `test_dbf_table`, `test_prg_engine`, and `test_prg_engine_table_structure` pass.
- 2026-04-17: PRG-engine regression coverage has started an incremental split. Shared helpers live in `tests/prg_engine_test_support.{h,cpp}`; SQL result cursor parity moved to `tests/test_prg_engine_sql_cursors.cpp` / `test_prg_engine_sql_cursors`; local table mutation/update coverage moved to `tests/test_prg_engine_table_mutation.cpp` / `test_prg_engine_table_mutation`. The main `tests/test_prg_engine.cpp` is still the legacy catch-all, but it is down to roughly 7,460 lines. Keep new PRG runtime coverage in focused files instead of adding to the catch-all when a suitable target exists.
- 2026-04-18: GitHub validation is now active for hosted Linux/macOS/Windows native CMake + CTest, and manual-only Windows deep validation covers native tests plus VSIX/Studio/smoke-test builds. Keep that workflow dispatch-only; do not add scheduled runs unless explicitly asked. The most recent manual Windows deep validation after the array-sort/mutation slice was green.
- 2026-04-18: Runtime array parity advanced again. `ASCAN()` supports search columns, case-insensitive/exactness/row-return flags, bounded scans, and first-pass predicate-expression search under flag `16`; predicate search accepts a string expression over `_ASCANVALUE`, `_ASCANINDEX`, `_ASCANROW`, and `_ASCANCOLUMN`, plus a stringified `{|x| ...}` block-style form. `ASORT()` supports start/count windows, descending order, case-insensitive keys, numeric-aware ordering, and two-dimensional row sorting by the start element's column. `ADEL()`/`AINS()` support VFP-style row/column shifting in two-dimensional arrays. `ACOPY()` row-major behavior is now covered for common two-dimensional row and column-helper workflows.
- 2026-04-18: PRG-engine regression splitting continued with `tests/test_prg_engine_arrays.cpp` / `test_prg_engine_arrays`. New array coverage belongs there when it is not tightly coupled to a broader legacy catch-all scenario. Local validation on Linux passed through `ninja -C build test` with 18/18 CTest tests green.
- 2026-04-18: `ASCAN()` predicate search now accepts unquoted block-style arguments such as `ASCAN(a, {|x| x > 8}, -1, -1, -1, 16)` by preserving braced literals through the expression parser. Array declaration/metadata tests were moved out of `tests/test_prg_engine.cpp` into `tests/test_prg_engine_arrays.cpp`, and a new `tests/test_prg_engine_functions.cpp` / `test_prg_engine_functions` target now covers portable path expression helpers. `JUSTPATH()`, `JUSTFNAME()`, `JUSTSTEM()`, `JUSTEXT()`, and new `JUSTDRIVE()` string-parse Windows and POSIX path forms consistently across hosts.
- 2026-04-18: Function coverage continued moving out of the legacy PRG catch-all. `test_string_and_math_expression_functions()` and `test_type_and_null_expression_functions()` now live in `tests/test_prg_engine_functions.cpp`, and that target also covers `FORCEEXT()`, `FORCEPATH()`, and first-pass case-insensitive `CHRTRANC()` alongside the existing utility expression batch.
- 2026-04-18: Function coverage continued moving out of the legacy PRG catch-all. `test_string_and_math_expression_functions()` and `test_type_and_null_expression_functions()` now live in `tests/test_prg_engine_functions.cpp`, and that target also covers `FORCEEXT()`, `FORCEPATH()`, and first-pass case-insensitive `CHRTRANC()` alongside the existing utility expression batch.
- 2026-04-27: Fix SET('PATH') test expectations after Codex f700a50 corrected path readback to return empty string (VFP-accurate: VFP returns "" when no PATH configured, not "OFF"). Two assertions in `tests/test_prg_engine.cpp` updated to use `.empty()`. 29/29 green.
- 2026-04-27: Wave 5 shipped (47f66d3). Four parallel slices implemented and merged:
  - Macro dot-suffix form (`&stem.suffix`): `parse_macro_reference()` now reads suffix after dot terminator and appends to expanded stem; `parse_identifier()` handles embedded `m&cType.ID` compound form. Tests in `test_prg_engine_functions.cpp`.
  - Array fill functions (ASESSIONS, AFONT, APRINTERS, AGETFILEVERSION): populate helpers added in `prg_engine_variables.inl`; dispatch in `prg_engine_arrays.inl`; registered in expression.inl. ASESSIONS returns active session IDs; AFONT stubs font list; APRINTERS enumerates /dev/lp*; AGETFILEVERSION stubs version rows for existing files. Tests in `test_prg_engine_arrays.cpp`.
  - Runtime surface improvements (NEWOBJECT library arg, GETPEM, SETPEM, COMPOBJ null-fix): NEWOBJECT optional second arg stored in OLE state; GETPEM returns property/method/.NULL.; SETPEM sets property/stores method refs; COMPOBJ deepened null handling. Tests in `test_prg_engine_runtime_surface_functions.cpp`.
  - SCATTER/GATHER deepening (NAME, MEMO, 2D arrays): SCATTER NAME / GATHER NAME scatter fields to/from object properties; SCATTER MEMO includes M/G/W memo-type fields; 2D [n,2] arrays as name/value maps; row-1 scatter for wider 2D arrays. Parser extended. Tests in `test_prg_engine_data_io.cpp`. 29/29 CTest passing.

Workflow:

1. Inspect the current repo state in the relevant files only.
2. Identify the single best prompt-sized slice from the current active queue.
3. Implement it.
4. Add or update focused regression tests.
5. Run narrow validation relevant to the slice.

- Runtime/data slices on Windows: `Push-Location "E:\Project-Copperfin"; cmake --build build --config Release --target test_dbf_table test_prg_engine test_prg_engine_arrays test_prg_engine_functions test_prg_engine_string_math_functions test_prg_engine_runtime_surface_functions test_prg_engine_seek_index`
- Runtime/data slices on Linux/macOS with an existing CMake/Ninja build: `ninja -C build test_prg_engine_seek_index && ./build/test_prg_engine_seek_index`
- Asset/index slices: also build and run `test_vfp_assets` when touched

1. Update docs/backlog.
2. Summarize what changed, what passed, and the next likely critical-path slice.

Default direction if no stronger signal appears from the current files:

- Prefer the current active queue from `docs/23-phase-a-dependency-breakdown.md`.
- Current target family: E2/#23 designer interaction, builder, and context-aware editor fidelity.
- Do not reopen the closed Phase A runtime/data-engine, D1/#19, E1/#22, native-wrapper, transpilation, or debugger slices unless concrete regression evidence appears.
- Avoid broad roadmap work; create one prompt-sized child issue under the active lane before coding.

Future-enhancement tracking:

- Put non-blocking migration/interchange ideas under umbrella issue `#137` instead of mixing them into the parity critical path.
