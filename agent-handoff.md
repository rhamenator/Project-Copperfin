# Agent Handoff

Canonical Copperfin continuation brief. Keep this file compact; do not rebuild a shipped-slice ledger here.

## Active Directive

- Start from live GitHub issue state plus the current repo guidance in `agents.md` and `docs/23-phase-a-dependency-breakdown.md`.
- Use the issue hierarchy: umbrella issues are planning/tracking units, parent/lane issues group work, and prompt-sized child issues are execution units.
- Pick or create one open prompt-sized child issue before implementation. Do not execute directly from an umbrella or parent/lane issue when child slices exist or can be created.
- Current active implementation lane is E3/#24, report/label designer fidelity, unless live GitHub state shows a higher-weight blocker. Localization/#2348 is a standing architectural constraint for new user-facing text, including after the current hard-coded backlog reaches zero.
- Treat the current shared WinForms shell chrome as provisional, not target UX. This is durable product direction, not optional polish: future shell work should keep shared designer behavior but move host-specific chrome into native hosts, with VSIX UI aligned to current Visual Studio 202x conventions, the VSIX Command window implemented as a bottom tabbed tool window in the same family as Terminal/Output panes, the standalone host owning a modern dockable shell with dockable Command and terminal panes, and each host following its platform UI standard directly: Microsoft Windows conventions for Windows, Apple conventions for macOS, and Linux conventions consistent with how Linux users expect tools such as VS Code to behave. Keep new visible shell text localized while those host-shell upgrades land.
- Phase A, D1/#19, E1/#22, and old numbered runtime/planning sequences are historical/closed unless live regression evidence reopens them.
- Preserve deferred modernization and wishlist work as roadmap intent, not noise; do not close, ignore, or discard future-facing issues merely because they are not current blockers.
- Keep changes narrow, implementation-first, and validated. Update durable docs only when behavior or active guidance changes.

## Current State

- Last shipped E3 child: `#3170`, fresh-run VFPSource visual startup-path smoke coverage; the fresh-run `VFPSource.zip` startup-path smoke now directly opens representative `books.scx`, `envmgr.vcx`, and `handler_context.mnx` assets from the newly materialized extraction root through the shared asset editor, verifies the adjacent `SCT`, `VCT`, and `MNT` sidecars remain present, and proves form/class-library/menu startup flows surface family-specific guidance plus visible design content without changing machine-readable contracts or localized UI text.
- Last shipped localization child: `#2959`, the shared VSIX/standalone `CopperfinDesignerSelection` path now routes the remaining form/class-library, menu, project, and generic property-grid display labels through the existing localization catalogs for `en-US`, `es-419`, `pt-BR`, and `qps-ploc` instead of hard-coded English text, while focused designer smokes prove those visible labels localize without changing invariant property names or machine-readable update targets such as `PROMPT`.
- Current lane: E3/#24 remains the active implementation lane for report/label designer fidelity; Localization/#2348 is now a standing engineering constraint for all new user-facing text and still needs fresh audits only when live repo evidence shows remaining hard-coded prose.
- Next action: continue live GitHub triage under E3/#24 by selecting or creating the next prompt-sized child from the remaining report/label host/designer fidelity backlog; after `#3170`, `#3169`, `#3168`, `#3167`, `#3166`, `#3165`, `#3164`, `#3163`, `#3162`, `#3161`, `#3160`, `#3159`, `#3158`, `#3157`, `#3156`, `#3155`, `#3154`, `#3153`, `#3152`, `#3151`, `#3150`, `#3149`, `#3148`, `#3147`, `#3146`, `#3145`, `#3144`, `#3143`, `#3142`, `#3141`, `#3140`, `#3139`, `#3138`, `#3137`, `#3136`, `#3135`, `#3134`, `#3133`, `#3132`, `#3131`, `#3129`, `#3128`, `#3127`, `#3126`, `#3125`, `#3124`, `#3123`, `#3122`, `#3121`, `#3120`, `#3119`, `#3118`, `#3117`, `#3116`, `#3115`, `#3114`, `#3113`, `#3112`, `#3111`, `#3110`, `#3109`, `#3108`, `#3107`, `#3106`, `#3105`, `#3104`, `#3103`, `#3102`, `#3101`, `#3100`, `#3099`, `#3098`, `#3097`, `#3096`, `#3095`, `#3094`, `#3093`, `#3092`, `#3091`, `#3090`, `#3089`, `#3088`, `#3087`, `#3086`, `#3085`, `#3084`, `#3083`, `#3082`, `#3081`, `#3080`, `#3079`, `#3078`, `#3077`, `#3076`, `#3075`, `#3074`, `#3073`, `#3072`, `#3071`, `#3070`, `#3069`, `#3068`, `#3067`, `#3066`, `#3065`, `#3064`, `#3063`, `#3062`, `#3061`, `#3060`, `#3059`, and `#3058` and `#3057` closed the fresh-run VFPSource visual startup-path smoke seam, the fresh-run VFPSource label startup-path smoke seam, the fresh-run VFPSource startup-path smoke seam, the fresh-run VFPSource zip real-asset root discovery seam, the shared Windows fake Studio host batch-update smoke activation seam, the shared Windows fake Studio host label drag summary-refresh smoke activation seam, the shared Windows fake Studio host report drag summary-refresh smoke activation seam, the shared Windows fake Studio host unplaced-report-object summary-refresh smoke activation seam, the shared Windows fake Studio host unplaced-label-object summary-refresh smoke activation seam, the shared Windows fake Studio host unplaced-label-object smoke activation seam, the shared Windows fake Studio host unplaced-report-object smoke activation seam, the shared Windows fake Studio host deleted-label-object smoke activation seam, the shared Windows fake Studio host deleted-report-object smoke activation seam, the shared Windows fake Studio host report-object smoke activation seam, the shared Windows fake Studio host deleted-label-section smoke activation seam, the shared Windows fake Studio host label-section smoke activation seam, the shared Windows fake Studio host deleted-report-section smoke activation seam, the shared Windows fake Studio host deleted-settings smoke activation seam, the shared Windows fake Studio host report-smoke activation seam, the shared Windows fake Studio host launcher seam, the shared real missing column-spacing settings round-trip seam, the shared real missing column-width settings round-trip seam, the shared real missing column-count settings round-trip seam, the shared real color settings round-trip seam, the shared real paper-size settings round-trip seam, the shared real grid-horizontal settings round-trip seam, the shared real missing printer-device settings round-trip seam, the shared real missing output-path settings round-trip seam, the shared real missing TrueType-option settings round-trip seam, the shared real missing Y-resolution settings round-trip seam, the shared real missing print-quality settings round-trip seam, the shared real report page-header height-contained-object stability seam, the shared real report page-header contained-object top reflow seam, the shared real report page-header section-top round-trip seam, the shared real report page-header section-height round-trip seam, the shared real report page-footer section-height round-trip seam, the shared real label column-header section-height round-trip seam, the shared real label column-footer section-height round-trip seam, the shared real label page-footer section-height round-trip seam, the shared deleted label page-footer metadata seam, the shared real label page-footer metadata seam, the shared deleted label column-footer metadata seam, the shared real label column-footer metadata seam, the shared deleted label column-header metadata seam, the shared real label column-header metadata seam, the shared deleted real page-footer metadata seam, the shared real page-footer metadata seam, the shared deleted nested foxref group-footer metadata seam, the shared deleted outer foxref group-footer metadata seam, the shared outer real group-footer metadata seam, the shared nested real group-footer metadata seam, the shared deleted nested foxref group-header metadata seam, the shared deleted populated foxref group-header metadata seam, the shared nested real group-header metadata seam, the shared deleted populated real-asset title metadata seam, the shared populated real-asset group-header metadata seam, the shared deleted populated real-asset group-header metadata seam, the shared deleted populated real-asset detail metadata seam, the shared populated real-asset detail metadata seam, the shared deleted real-asset page-header metadata seam, the shared real-asset page-header metadata seam, the shared real-asset report-detail metadata seam, the shared real-asset report-title metadata seam, the shared deleted real-asset report-title metadata seam, the shared deleted real-asset label-detail metadata seam, the shared real-asset label-detail metadata seam, the shared deleted real-asset grouped-header metadata seam, the shared deleted real-asset blank-footer metadata seam, the shared real-asset blank-footer metadata seam, the stable-selected group header/footer section seam, the stable-selected nested grouping partner-metadata seam, the TAG sort-setting unsupported `EXPR` memo preservation seam, the stable settings `EXPR` memo replacement unsupported-line parsing seam, the record-selected settings `EXPR` memo replacement unsupported-line parsing seam, the record-selected settings delete/restore unsupported `EXPR` preservation seam, the stable settings delete/restore unsupported `EXPR` preservation seam, the stable unsupported `EXPR` preservation evidence seam, the deleted unsupported `EXPR` preservation evidence seam, the shared deleted-settings missing gridh seam, the shared deleted-settings missing gridv seam, the shared deleted-settings missing papersize seam, the shared deleted-settings missing rightmargin seam, the shared deleted-settings missing leftmargin seam, the shared deleted-settings missing paperwidth seam, the shared deleted-settings missing paperlength seam, the shared deleted-settings missing orientation seam, the shared deleted-settings missing cols seam, the shared deleted-settings missing colwidth seam, the shared deleted-settings missing colspacing seam, the shared deleted-settings missing color seam, the shared deleted-settings missing copies seam, the shared deleted-settings missing collate seam, the shared deleted-settings missing ascii seam, the shared deleted-settings missing ttoption seam, the shared deleted-settings missing y-resolution seam, the shared deleted-settings missing print-quality seam, the shared deleted-settings missing printer-output seam, the shared deleted-settings missing printer-device seam, the shared deleted-settings missing printer-driver seam, the shared deleted-settings missing EXPR-backed default-source seam, the shared deleted-settings missing sort-setting seam, the shared deleted-settings missing top/bottom margin add seam, the shared missing direct-field top/bottom report-margin seam, the shared missing direct-field `TAG` root-setting seam, the shared missing EXPR-backed root-setting parity seam, the unsupported root-settings EXPR-line preservation seam, the absent memo-backed printer-setting add/remove seam, the memo-backed real root-settings write seam, and the grouped real-section key-expression, grouped multi-object section-height, real section-sibling, grouped section-top, and non-top section continuity seams on top of `#3056` label axis-specific snap, `#3055` vertical snap, `#3054` horizontal snap, `#3053` vertical distribution, `#3052` top-alignment, `#3049` resize-to-anchor height, `#3048` resize-to-anchor width, `#3047` resize-to-anchor size, `#3046` snap-to-grid, `#3045` horizontal distribution, `#3044` left-alignment, `#3043` post-nudge property-grid geometry refresh, and `#3042` real-asset report/label nudge continuity coverage, treat the shared live layout-action path as covered for those command families unless fresh evidence shows a broader section/object authoring gap; local Linux evidence still does not include the VFP9 sample tree because neither `COPPERFIN_VFP9_ROOT` nor a local install path resolved `invoice.frx`/`cust.lbx` here, but fresh-run zip-backed VFPSource startup-path coverage now proves project/program/form/class-library/menu/report/label flows from `~/Downloads/VFPSource.zip`, so the next worthwhile seam is resuming the VFP9 `invoice.frx`/`cust.lbx` real-asset metadata path when that sample tree is available or continuing into the next open live E3 child if GitHub triage shows a tighter report/label fidelity gap.
- Validation note: on Linux, the full `mono vsix/Copperfin.DesignerSmokeTests/bin/Release/net472/Copperfin.DesignerSmokeTests.exe` run is still not a clean release gate because the unmanaged Mono/libgdiplus tail remains unstable and the suite lacks targeted filtering; this slice used `git diff --check`, `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -c Release`, `dotnet build /tmp/copperfin-label-probe/Probe.csproj -c Release`, and a focused temporary reflection runner that invoked `SmokeAssetEditorReportSectionPropertyGridHostUpdate`, `SmokeAssetEditorReportSettingsPropertyGridHostUpdate`, `SmokeAssetEditorDeletedReportSettingsPropertyGridHostUpdate`, `SmokeAssetEditorDeletedReportSectionPropertyGridHostUpdate`, `SmokeAssetEditorLabelSectionPropertyGridHostUpdate`, `SmokeAssetEditorDeletedLabelSectionPropertyGridHostUpdate`, `SmokeAssetEditorReportObjectPropertyGridHostUpdate`, `SmokeAssetEditorDeletedReportObjectPropertyGridHostUpdate`, `SmokeAssetEditorDeletedLabelObjectPropertyGridHostUpdate`, `SmokeAssetEditorUnplacedLabelObjectPropertyGridHostUpdate`, `SmokeAssetEditorUnplacedLabelObjectHostUpdateRefreshesShellSummary`, `SmokeAssetEditorUnplacedReportObjectPropertyGridHostUpdate`, `SmokeAssetEditorUnplacedReportObjectHostUpdateRefreshesShellSummary`, `SmokeAssetEditorReportDragRefreshesShellSummary`, `SmokeAssetEditorLabelDragRefreshesShellSummary`, and `SmokeAssetEditorReportDragUsesBatchStudioHostUpdate` directly, then invoked `SmokeFreshRunVfpSourceStartupPaths` through `WithResolvedRealAssetToolchain`, while also inspecting the Windows batch-wrapper branch from `CreateFakeStudioHostScriptPath` plus `CreateFakeStudioHostScript`, proving all sixteen active section/settings/object/drag/batch fake-host smokes pass on Linux, the live report, deleted report, live label, and deleted label section paths emit invariant host `VPOS`, the live report, deleted report, deleted label, unplaced label, and unplaced report object paths emit invariant host `HPOS`, both unplaced object summary-refresh paths now refresh shell preview-bounds text from the returned snapshot while preserving object-rooted continuity, both drag-summary paths refresh shell preview-bounds text from the returned batch-update snapshot while preserving host-specific identity, object selection, and containing-section continuity, the standalone batch-update path preserves section/object selection continuity with invariant `--visual-object-update-batch` host arguments and without regressing to `--set-property`, the settings path emits invariant host `COLWIDTH`, the deleted-settings path emits invariant host `COLSPACING`, the fresh-run `VFPSource.zip` startup-path smoke now deletes any prior extraction root, materializes the zip-backed `VFPSource` root during enumeration, resolves `tasklist.PJX`, `main.prg`, `books.scx`, `envmgr.vcx`, `handler_context.mnx`, `by_author.FRX`, and `STYLELBL.LBX` from that fresh root, verifies the adjacent `SCT`, `VCT`, `MNT`, `LBT`, and other sidecars remain present, and proves the shared project editor, program editor, standalone Studio, direct shared form/class-library/menu editor startup flows, and direct shared label-editor startup flow all succeed from those paths; the generated Windows fake-host wrapper still ends in `.cmd`, includes the batch logging/output flow, and the full focused probe finishes with `failures=0 batch_wrapper=ok`.
- E2/#23 remains open mostly for evidence-audit/closure cleanup. Do not open more E2 wrapper work unless new APIs or fresh regression evidence show a real implementation gap.
- Localization/release-readiness slices under `#2348`/`#113` have shipped through `#2352` and `#1856`; continue them only if live issue state makes them higher priority than E3/#24.
- Shell/UI backlog guidance is now captured in `#3050` (platform UI standards for native hosts), `#3051` (dockable Command and terminal surfaces, including VSIX bottom-tabbed Command window behavior), and `#2998` (future curated standalone extension support without loading the Copperfin VSIX). Reuse those issues rather than opening duplicate UI-direction backlog items unless new scope appears beyond the existing host-shell, docking, terminal, extension-compatibility, or localization-ready shell-text boundaries.

## Required Reading

Read only what is needed for the chosen slice:

- `agents.md` for operating rules and safety traceability.
- `docs/23-phase-a-dependency-breakdown.md` for current lane status and historical dependency evidence.
- `docs/28-repository-ontology.md` when the slice needs current repo-structure grounding instead of aspirational architecture summaries.
- `docs/22-vfp-language-reference-coverage.md` when the slice touches VFP/runtime language coverage.
- `docs/safety/triage-rubric.md` for feedback triage.
- `docs/safety/test-coverage-gap-analysis.md` when adding tests.
- Runtime, designer, or host files directly relevant to the selected child issue.

`remaining-work.md` is intentionally deprecated as an active planning source. `issues.txt` is a dated local snapshot, not planning authority; verify live GitHub state before choosing work.

## Validation And Updates

- Run focused tests for the selected slice plus `git diff --check`.
- For safety-relevant documentation, keep `DQ-*`, `DV-*`, and `HZ-*` traceability linked to `docs/safety/hazard-register.md`.
- Before a release tag, run `scripts/validate-safety-traceability.ps1` or the Safety Traceability Gate workflow against the intended release issue set and archive the report.
- Update `agent-handoff.md` only when the last shipped slice, current lane, or next action changes.
- Update `CHANGELOG.md` for lasting repo changes or material tracked-documentation changes.
