# Agent Handoff

Canonical Copperfin continuation brief. Keep this file compact; do not rebuild a shipped-slice ledger here.

## Active Directive

- Start from live GitHub issue state plus the current repo guidance in `agents.md` and `docs/23-phase-a-dependency-breakdown.md`.
- Use the issue hierarchy: umbrella issues are planning/tracking units, parent/lane issues group work, and prompt-sized child issues are execution units.
- Pick or create one open prompt-sized child issue before implementation. Do not execute directly from an umbrella or parent/lane issue when child slices exist or can be created.
- Current active implementation lane is runtime/designer/hardening slice selection across `#3217`, E3 `#24`, `#3214`, and `#110`, starting from live GitHub child state. Localization/#2348 is a standing architectural constraint for new user-facing text, including after the current hard-coded backlog reaches zero. Keep runtime work focused on prompt-sized common-in-the-wild VFP gaps rather than reopening broad umbrella work, and keep hardening work focused on low-risk duplicated-helper removal or concrete cross-platform defects.
- For product code, prefer `copperfin::platform::read_environment_variable(...)` / `read_environment_variable_or_empty(...)` over fresh ad hoc `std::getenv` / `_dupenv_s` helpers. Keep any unavoidable direct environment access narrow and justified.
- Treat the current shared WinForms shell chrome as provisional, not target UX. This is durable product direction, not optional polish: future shell work should keep shared designer behavior but move host-specific chrome into native hosts, with VSIX UI aligned to current Visual Studio 202x conventions, the VSIX Command window implemented by default as a bottom tabbed tool window in the same family as Terminal/Output panes, the standalone host owning a modern dockable shell with dockable Command and terminal panes, and Windows standalone work aiming for contemporary Visual Studio-grade polish without forcing a literal clone onto macOS or Linux. Each host should follow its platform UI standard directly: Microsoft Windows conventions for Windows, Apple conventions for macOS, and Linux conventions consistent with how Linux users expect tools such as VS Code to behave. Keep new visible shell text localized while those host-shell upgrades land.
- Preserve stack-frugal PRG execution as a non-negotiable runtime invariant. New routine, class, reflection, packaging, or debug work must continue using the heap-backed iterative frame machine and must not reintroduce recursive host-stack growth or VFP.exe-style stack exhaustion paths for common PRG code.
- Phase A, D1/#19, E1/#22, and old numbered runtime/planning sequences are historical/closed unless live regression evidence reopens them.
- Preserve deferred modernization and wishlist work as roadmap intent, not noise; do not close, ignore, or discard future-facing issues merely because they are not current blockers.
- Keep changes narrow, implementation-first, and validated. Update durable docs only when behavior or active guidance changes.

## Current State

- Last shipped slice: `#3690` shipped on the `#3217` lane by teaching `TRY` state to unwind leaked `WITH` bindings before entering `CATCH` or `FINALLY`. The runtime now records the active `WITH` stack depth at `TRY` entry and trims back to that lexical depth when a fault transfers control out of an interrupted `WITH...ENDWITH` block, so stale inner `__with_*` locals and object bindings no longer leak into handler code. Focused `test_prg_engine_control_flow` coverage now proves a fault inside an inner `WITH` caught by an outer `TRY/CATCH` resolves leading-dot member access against the still-active outer `WITH` target instead of the faulted inner one, while preserving the original runtime error metadata.
- Last shipped localization child: `#3172`, `FoxProIntelliSenseCatalog` now routes built-in Quick Info, completion metadata, signature-help documentation, and source-derived project symbol prose through the shared localization catalogs for `en-US`, `es-419`, `pt-BR`, and `qps-ploc` instead of hard-coded English strings, while preserving invariant token names, symbol kinds, and signature content.
- Current lane: with `#3690` shipped, choose the next concrete child from live GitHub state across the native OOP/runtime parity `#3217`, E3 `#24` report/label lane, and remaining hardening parents (`#3214`, plus any newly filed `#110` follow-ons), while keeping `#2348` active as a standing constraint for new user-facing text and locale flow. The `#3217` lane remains the parent for residual native PRG/runtime gaps, and current evidence now includes mounted VFP9 sample-driven `AddItem()` population, local-help/FFC-backed `ListCount` / writable `List(...)` / `RemoveItem()` row-consumption coverage, extracted-FFC-backed representative multicolumn `AddItem(..., nIndex, nColumn)` follow-through plus multicolumn `AddListItem()` / `NewItemId` / writable `List(nIndex, nColumn)` coverage, local-help/FFC-backed `ListItemID` selection coherence evidenced by `_movers.vc2`, local-help/FFC-backed `Selected(nIndex)` listbox selection coherence evidenced by `_controls.vc2`, local-help-backed representative `ListBox.MultiSelect` selection-mode coherence also evidenced by `_controls.vc2`, local-help-backed representative `Sorted` property plus sorted `AddItem()` / `AddListItem()` / first-column `List(...)` row-order maintenance, first-pass native `ComboBox` / `ListBox` `Value` selection parity driven by `BoundColumn`, first-pass native `BoundTo` parity plus builtin `ListBox.BoundColumn` reflection, first-pass native `ControlSource` parity for `ListBox`, first-pass `BoundTo = .F.` source-type selection semantics for bound list controls, first-pass native `ControlSource` write-back for direct selection and `Requery()` flows, first-pass native `Requery()` parity for value-list, array, alias, and representative single-cursor fields row sources, local-help-backed `SelectedID(nItemID)` selection coherence, local-help-backed `RemoveListItem(nItemID)` removal coherence, local-help-backed `NewIndex` insertion/removal coherence evidenced by extracted `VFPSource/builders/wbgrid.prg`, representative writable `List(NewIndex, nColumn)` row-churn coherence after insertion, shift, reselection, and reinsertion for both plain and derived native list controls, the already-shipped expression-level routine-call path from `#3216`, the newly shipped high-severity inherited self-call virtual-dispatch fix from `#3681`, the newly shipped `ASIZE(array, nRows)` 2-D shape-preservation fix from `#3677`, the newly shipped numeric/date SEEK exact-match fix from `#3672`, the newly shipped trim-family control-whitespace fix from `#3680`, the newly shipped explicit-missing-tag `SET ORDER` fault fix from `#3696`, the newly shipped slash-delimited `SET DATE` parsing fix from `#3695`, the newly shipped hidden-all `GO TOP` / `GO BOTTOM` BOF/EOF fix from `#3689`, the newly shipped PADL-index truncation fix from `#3679`, the newly shipped local plain-string CDX tag discovery fix from `#3699`, the newly shipped `LOCATE ... CONTINUE` parity fix from `#3694`, the newly shipped multi-field `REPLACE` original-value fix from `#3693`, the newly shipped `SET EPOCH` rollover fix from `#3698`, the newly shipped positional `GATHER FROM <array>` memo-skip fix from `#3691`, the newly shipped scoped `RECALL FOR` hidden-row fix from `#3682`, and the newly shipped string-aware `BETWEEN()` crash fix from `#3697`, plus the newly shipped leaked-`WITH` TRY/CATCH unwind fix from `#3690`, along with the recent native collection default-item/member-chain/subscript slices, the `_SCREEN/_VFP` forms-collection coverage, the representative `PageFrame.Pages` / `PageCount` collection/count parity for live `Page` children, the post-selection method-invocation plus inline-routine-signature slice, the recent tolerant aggregate/macro hardening follow-up from `#3637`, and the first-pass `#INCLUDE` / `#DEFINE` / conditional-preprocessor-selection work evidenced by mounted VFP9/FFC sources such as `ffc/Autgraph.h`, `ffc/_frxcursor.h`, `VFPSource/beautify/beautify.prg`, `genmenu.prg`, `vfpxtab.prg`, `samples/solution/forms/graphics/fdproc.prg`, `_controls.vc2`, `_table.vc2`, `_internet.vc2`, `_ws3utils.vc2`, and `_movers.vc2`. The E3 lane now has healthy mounted-media evidence across root settings, root sort settings, direct object fields, real object preview-bounds refresh, real object rename round-trip behavior, real object duplicate round-trip behavior, real object reorder round-trip behavior, real object nudge/undo lifecycle behavior, real object align/undo lifecycle behavior, real object snap/undo lifecycle behavior, real object deleted-preview lifecycle behavior, memo-backed object fields, object-font update and clear behavior, object delete/restore lifecycle behavior, section geometry mutation, real section deleted-preview lifecycle behavior, section delete/restore lifecycle behavior, and grouping: `test_studio_host_real_sample_round_trip`, `test_studio_host_real_sample_sort_settings_round_trip`, `test_studio_host_real_sample_object_round_trip`, `test_studio_host_real_sample_object_preview_bounds_round_trip`, `test_studio_host_real_sample_object_rename_round_trip`, `test_studio_host_real_sample_object_duplicate_round_trip`, `test_studio_host_real_sample_object_reorder_round_trip`, `test_studio_host_real_sample_object_nudge_round_trip`, `test_studio_host_real_sample_object_align_round_trip`, `test_studio_host_real_sample_object_snap_round_trip`, `test_studio_host_real_sample_object_deleted_preview_round_trip`, `test_studio_host_real_sample_object_memo_round_trip`, `test_studio_host_real_sample_object_font_round_trip`, `test_studio_host_real_sample_object_font_clear_round_trip`, `test_studio_host_real_sample_object_delete_restore_round_trip`, `test_studio_host_real_sample_section_round_trip`, `test_studio_host_real_sample_section_deleted_preview_round_trip`, `test_studio_host_real_sample_section_delete_restore_round_trip`, `test_studio_host_real_sample_grouping_round_trip`, and `test_studio_host_real_sample_grouping_deleted_preview_round_trip` now pass against mounted or extracted VFP9 report assets. Real rename and duplicate coverage both require short replacement `UNIQUEID` values because the shipped samples reject oversized replacements at write-back time with `Character value is too large for the target field.` Real reorder coverage now also makes one mounted-sample quirk explicit: physical row order changes and both sidecars mutate, but the reopened section-relative object-count/index metadata stays stable on these shipped samples rather than mirroring the raw record jump. The `#110` lane has now closed the launcher `app.cfdebug` follow-on together with the earlier runtime-host temp-bootstrap leak/clobbering child plus the launcher `/locale` forwarding and launcher-contract rewrite follow-ons, so the next slice should come from residual runtime parity, E3 report/label fidelity, or a newly surfaced pipeline child rather than continuing to mine stale hardening assumptions. The `#3214` lane now has the shipped runtime-host env-wrapper child, the shipped security/DBF direct-env cleanup child, both shipped Studio-host report/label selection batches, the shipped stable-settings cluster, the shipped record-settings cluster, the shipped margin/orientation settings cluster, the shipped selected-settings pair, the shipped settings restore/delete/memo family, the shipped section deleted-state/section selection family, the shipped deleted-state family, the shipped object batch family, the shipped selected object/section family, the shipped deleted object batch family, the shipped selected-column header/footer family, the shipped selected-group header/footer family, the shipped remaining selected-page family, the shipped real-sample round-trip family, the shipped deleted-property rejection pair, the shipped deleted-property clear pair, the shipped deleted-property copy pair, the shipped deleted-property move pair, the shipped deleted-update batch family, the shipped deleted subtree duplicate trio, the shipped subtree duplicate trio, the shipped deleted object batch section trio, the shipped selected title trio, the shipped selected summary object pair, the shipped selected nested-group section pair, the shipped remaining selected-asset singleton trio, the shipped remaining column-setting cluster, the shipped update-batch pair, the shipped report-section pair, and the shipped runtime pipeline support header swap. The obvious direct test env-mutation cluster in the current evidence set is exhausted; managed temp-artifact hardening for replay manifests and temp build roots is now also closed, so pick the next child on product value rather than stale helper debt.
- Next action: pick or create the next prompt-sized child from live GitHub state, preferring the highest-weight concrete `#3217`, E3 `#24`, or `#3214` blocker by current product value rather than stale assumptions about missing runtime basics. On the `#3217` lane, the inherited self-call dispatch gap, `ASIZE(array, nRows)` 2-D preservation gap, numeric/date `SEEK` exact-match gap, trim-family control-whitespace gap, explicit-missing-tag `SET ORDER` gap, slash-delimited `SET DATE` parsing gap, hidden-all `GO TOP` / `GO BOTTOM` BOF/EOF gap, PADL-index truncation gap, local plain-string CDX tag-discovery gap, `LOCATE ... CONTINUE` parity gap, multi-field `REPLACE` original-value gap, the set-aware two-digit-year `SET EPOCH` gap, the positional `GATHER FROM <array>` memo-misalignment gap, the scoped `RECALL FOR` hidden-row gap, the non-numeric-string `BETWEEN()` crash gap, and the leaked-`WITH` TRY/CATCH unwind gap are now closed, so the next adjacent runtime candidates are the next common-in-the-wild child that live GitHub state surfaces under `#3670` / `#3217`. The list-control write-back/refresh edge cases outside the shipped direct path (for example `SETPem()` or row-churn method side effects) and the SQL/query row-source `Requery()` gap after the shipped value-list, array, alias, fields, selected-value, `BoundTo`, `ControlSource`, and direct write-back work also remain valid `#3217` candidates. On the E3 `#24` lane, continue selecting mounted real-sample report/label round-trip gaps that still lack evidence now that align/nudge/snap coverage is in place. On the `#110` lane, launcher-based implicit debug-manifest routing is now closed, so only pick fresh pipeline children when live issue state shows another concrete package/runtime contract gap rather than relitigating the shipped `app.cfdebug` path. Keep the native runtime path focused on common VFP behavior without regressing the deterministic `hWnd` / `SYS(2326)` / `SYS(2327)` bridge, shipped OLE metadata, explicit `.Object`, direct contained-object routing, conflict-sensitive `Application` / `Quit` handling, `DoVerb()` numeric and named-verb flows, `ObjectVerbs` inspection surfaces, activation-policy surfaces, timeout-policy surfaces, the built-in child-object graph, the intentional unsupported-`hWnd` path for ordinary native controls, the stack-frugal frame machine, or localization constraints for any new diagnostics. Mounted VFP9 media at `/run/media/rich/VFPPROD1`, extracted help under `/tmp/vfp9help`, and the extracted FFC/VFP source trees under `/tmp/ffc-master` and `/tmp/vfpsource` remain available for primary-source runtime and report/designer evidence while choosing that next child.
- Durable runtime note: the trim-family baseline is intentionally space-only. `ALLTRIM()` now trims only `CHR(32)` in the ordinary runtime path, and the shared index-expression evaluator now applies the same space-only rule for `ALLTRIM` / `LTRIM` / `RTRIM` instead of using `std::isspace`; tabs, CR, LF, and other control whitespace remain significant unless a caller explicitly removes them by some other means.
- Durable runtime note: `CONTINUE` now follows VFP's locate-resume semantics again; it is no longer accepted as a loop-control synonym. Use `LOOP` for `FOR` / `DO WHILE` / `SCAN` continuation, and use `CONTINUE` only to resume the active `LOCATE FOR` search on the current work area.
- Durable runtime note: the shipped native list-control baseline now includes single-column `AddItem()`, representative multicolumn `AddItem(..., nIndex, nColumn)` follow-through, read-only `ListCount`, representative writable `List(nIndex)` / `List(nIndex, 1)` plus representative writable multicolumn `List(nIndex, nColumn)` and `List(NewIndex, nColumn)` row-churn follow-through, `RemoveItem()`, representative `RemoveListItem(nItemID)`, read-only `NewIndex`, read-only `NewItemId`, representative multicolumn `AddListItem()`, writable `ListItemID` selection coherence, representative `ListBox.Selected(nIndex)` row-selection coherence, representative `ListBox.MultiSelect` selection-mode coherence, representative `SelectedID(nItemID)` item-id selection coherence, representative `Sorted` row-order maintenance, first-pass selected `Value` coherence driven by `BoundColumn`, first-pass builtin `BoundTo` plus builtin `ListBox.BoundColumn` reflection, first-pass builtin `ListBox.ControlSource` reflection, first-pass `BoundTo = .F.` source-type selection semantics keyed from the bound `ControlSource` value kind when available, first-pass direct-selection and `Requery()` write-back through bound memory-variable and cursor-field `ControlSource` targets, and first-pass `Requery()` refresh for value-list, array, alias, and representative single-cursor fields row sources over the same-session runtime row state for native `ComboBox`/`ListBox` controls. It is still intentionally first-pass: broader list-control binding refresh parity and non-direct write-back edge cases are not claimed complete, so this remains a focused common-in-the-wild runtime baseline rather than a blanket claim of every VFP list-control edge case.
- Durable runtime note: ordinary native PRG self-calls now late-bind correctly from inherited method bodies. `THIS.Method()` and bare self-calls made on the live current object resolve from the object's most-derived runtime class, including across external-base PRG inheritance, while `DODEFAULT()` remains the explicit base-relative dispatch path that starts above the currently executing method's defining class.
- Durable runtime note: VFP help explicitly documents the `OLERequestPendingTimeout = 5000` and `OLEServerBusyRaiseError = .F.` defaults, but the `OLEServerBusyTimeout` topic documents behavior without an explicit numeric default. Copperfin currently seeds a representative `OLEServerBusyTimeout = 5000` value on native OleControl hosts to keep timeout-policy inspection/programmatic-control paths deterministic until a stricter VFP-default citation is available.
- Validation note: on Linux, the full `mono vsix/Copperfin.DesignerSmokeTests/bin/Release/net472/Copperfin.DesignerSmokeTests.exe` run is still not a clean release gate because the unmanaged Mono/libgdiplus tail remains unstable and the suite lacks targeted filtering; the latest E3 live-nudge preview-metadata hardening slice used `git diff --check`, `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -c Release`, and a focused repo-local net472/Mono probe that invoked `SmokeAssetEditorNudgeCommandWithRealAsset` for the real `by_author.FRX` nudge case under the resolved local toolchain, proving the shared editor now preserves expected live preview metadata after nudge/reload and after full undo-driven restore while avoiding stale-host false positives from older local `build` binaries. Earlier focused validation remains as described below for the shared designer/runtime workflow families.
- Validation note: the aggregate `test_studio_host` target is buildable again after `#3507` (`157da636`), so narrow Studio-host slices can use either that aggregate target or the split host executables as appropriate instead of assuming the old duplicate-`main` / unresolved-helper link failure is still present.
- Repository licensing changed: prior versions were GPL-3.0-only; new versions are dual-licensed (free individual/non-commercial use plus a paid Commercial License). See `LICENSE.md`, `MIGRATION_NOTICE.md`, and `LEGAL_FAQ.md`. A license-status mechanism (`cf_licensing`, `--license-status`, `app.cfmanifest`/`app.cfdebug` stamping, Studio JSON `licenseProfile`) was added in commits `d94b31f7`/`21177bd9`; it is display/audit-only and never gates functionality.
- `#3211` tracks two pre-existing bugs discovered while adding the licensing work above (unrelated to licensing itself, both confirmed pre-existing via A/B revert): the Windows `/sdl` test-helper `getenv` failure is now addressed in the touched core tests via a shared Windows-safe environment helper, and Windows builds now reserve a 16 MiB stack for `copperfin_studio_host` through target link settings. Root cause (see issue comments) and the 16 MiB fix are both independently verified: Debug and Release `copperfin_studio_host.exe` now both handle `--json` against a trivial `.scx` and the full `solution.pjx` sample with exit code 0, and `test_licensing_status` (22/22) still passes after Codex's `test_environment_support.h` refactor. The underlying oversized Studio host entrypoint (`apps/copperfin_studio_host/main.cpp`, ~4175 lines, ~150+ inlined property-setter blocks not yet split into `try_handle_*` helpers) still merits a later structural refactor as the real fix; the stack reservation is a verified-effective mitigation, not that refactor.
- PRG-level call-stack frugality (a founding requirement) was investigated in depth per user request: `DO`/`CALL`/`DO FORM` are confirmed genuinely frugal -- an explicit heap-backed frame stack inside an iterative `run()` loop (`src/runtime/prg_engine.cpp`), with a tested, configurable `max_call_depth` guardrail (default 1024, `prg_engine_session.inl:1652`) that raises a catchable error before any native stack risk. `#3216` is now covered through that same stack-frugal frame machine rather than a recursive side path, and `#3217` has been refreshed from a stale missing-feature report into the active parent lane for remaining native-OOP/runtime residuals. Treat that lane as common-in-the-wild VFP runtime-parity work, not a fringe wishlist item.
- Additional queued hardening/backlog bug still open from Linux review: `#3214` (continue hardening environment-variable access patterns across product and test code beyond the core suites already switched to a shared Windows-safe helper in the latest hardening slice).
- E2/#23 remains open mostly for evidence-audit/closure cleanup. Do not open more E2 wrapper work unless new APIs or fresh regression evidence show a real implementation gap.
- Localization/release-readiness slices under `#2348`/`#113` have shipped through `#2352` and `#1856`; continue them only if live issue state makes them higher priority than E3/#24.
- Shell/UI backlog guidance is now captured in `#3050` (platform UI standards for native hosts), `#3051` (dockable Command and terminal surfaces, including the default bottom-tabbed VSIX Command window behavior), and `#2998` (future curated standalone extension support without loading the Copperfin VSIX). Those three issues already cover the current durable UI direction; reuse them rather than opening duplicate backlog items unless new scope appears beyond the existing host-shell, docking, terminal, extension-compatibility, or localization-ready shell-text boundaries.

## Required Reading

Read only what is needed for the chosen slice:

- `agents.md` for operating rules and safety traceability.
- `docs/23-phase-a-dependency-breakdown.md` for current lane status and historical dependency evidence.
- `docs/28-repository-ontology.md` when the slice needs current repo-structure grounding instead of aspirational architecture summaries.
- `docs/22-vfp-language-reference-coverage.md` when the slice touches VFP/runtime language coverage.
- `docs/safety/triage-rubric.md` for feedback triage.
- `docs/safety/test-coverage-gap-analysis.md` when adding tests.
- Runtime, designer, or host files directly relevant to the selected child issue.

`remaining-work.md` is intentionally deprecated as an active planning source. `issues.txt` is a dated local snapshot, not planning authority; verify live GitHub state before choosing work.

## Validation And Updates

- Run focused tests for the selected slice plus `git diff --check`.
- For safety-relevant documentation, keep `DQ-*`, `DV-*`, and `HZ-*` traceability linked to `docs/safety/hazard-register.md`.
- Before a release tag, run `scripts/validate-safety-traceability.ps1` or the Safety Traceability Gate workflow against the intended release issue set and archive the report.
- Update `agent-handoff.md` only when the last shipped slice, current lane, or next action changes.
- Update `CHANGELOG.md` for lasting repo changes or material tracked-documentation changes.
