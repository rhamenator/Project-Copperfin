# Agent Handoff

Canonical Copperfin continuation brief. Keep this file compact; do not rebuild a shipped-slice ledger here.

## Active Directive

- Start from live GitHub issue state plus the current repo guidance in `agents.md` and `docs/23-phase-a-dependency-breakdown.md`.
- Use the issue hierarchy: umbrella issues are planning/tracking units, parent/lane issues group work, and prompt-sized child issues are execution units.
- Pick or create one open prompt-sized child issue before implementation. Do not execute directly from an umbrella or parent/lane issue when child slices exist or can be created.
- Current active implementation lane is runtime/designer/hardening slice selection across `#3217`, E3 `#24`, and `#110`, starting from live GitHub child state. Localization/#2348 is a standing architectural constraint for new user-facing text, including after the current hard-coded backlog reaches zero. Keep runtime work focused on prompt-sized common-in-the-wild VFP gaps rather than reopening broad umbrella work, and keep hardening work focused on low-risk duplicated-helper removal or concrete cross-platform defects.
- Keep `en-US`, `es-419`, and `pt-BR` as the current production-locale priorities; `qps-ploc` is test-only. Preserve the catalog/runtime architecture for a later first expansion wave of left-to-right alphabetic locales such as `en-GB`, French, German, and Russian, without adding separate language binaries or localizing machine contracts. Catalog parity or machine translation is not production acceptance: each regional locale needs recorded review by a native speaker or qualified cultural-language reviewer for accuracy, intent, tone, terminology, formality, euphemism/directness, regional usage, and offensive or awkward wording. Prefer natural regional language over literal translation, and permit a more specific regional catalog when a broad locale such as `es-419` cannot express a message naturally across its audience. Treat scripts needing bidirectional layout, shaping, vertical text, materially different input, or other distinct typography behavior as a later readiness lane with explicit font, layout, input, collation, casing, accelerator, packaging, and test evidence.
- For product code, prefer `copperfin::platform::read_environment_variable(...)` / `read_environment_variable_or_empty(...)` over fresh ad hoc `std::getenv` / `_dupenv_s` helpers. Filesystem-valued variables such as `COPPERFIN_LOCALE_DIR` must instead use `read_environment_path(...)`, `write_environment_path(...)`, and `clear_environment_path(...)` so Windows retains native Unicode paths. Keep any unavoidable direct environment access narrow and justified.
- Windows runtime-host debug fixture slice `#4048` is complete. Preserve capture-side CRLF normalization for invariant multiline assertions, JSON escaping for native bridge-request paths, and `ScopedEnvironmentPath` for filesystem-valued Unicode locale roots; do not regress those paths to lossy `std::filesystem::path::string()` environment conversion on Windows. The focused audit-containment workflow must continue building and running `test_runtime_host_debug_output`, and per-case/subcase progress markers should remain actionable without changing product output.
- Treat the current shared WinForms shell chrome as provisional, not target UX. This is durable product direction, not optional polish: future shell work should keep shared designer behavior but move host-specific chrome into native hosts, with VSIX UI aligned to current Visual Studio 202x conventions, the VSIX Command window implemented by default as a bottom tabbed tool window in the same family as Terminal/Output panes, the standalone host owning a modern dockable shell with dockable Command and terminal panes, and Windows standalone work aiming for contemporary Visual Studio-grade polish without forcing a literal clone onto macOS or Linux. Each host should follow its platform UI standard directly: Microsoft Windows conventions for Windows, Apple conventions for macOS, and Linux conventions consistent with how Linux users expect tools such as VS Code to behave. Keep new visible shell text localized while those host-shell upgrades land.
- Preserve stack-frugal PRG execution as a non-negotiable runtime invariant. New routine, class, reflection, packaging, or debug work must continue using the heap-backed iterative frame machine and must not reintroduce recursive host-stack growth or VFP.exe-style stack exhaustion paths for common PRG code.
- Phase A, D1/#19, E1/#22, and old numbered runtime/planning sequences are historical/closed unless live regression evidence reopens them.
- Preserve deferred modernization and wishlist work as roadmap intent, not noise; do not close, ignore, or discard future-facing issues merely because they are not current blockers.
- Keep changes narrow, implementation-first, and validated. Update durable docs only when behavior or active guidance changes.

## Current State

- Shipped installer artifact publication slice `#4044`: each Windows, macOS, and Linux package job creates a fresh `build/package` directory, runs its complete CPack generator set, removes CPack's internal `_CPack_Packages` staging tree, and verifies the exact current nonempty package set before upload. Uploads use exact package paths with `if-no-files-found: error`, so stale workspace-root files cannot satisfy publication. Local workflow-contract CTest and positive CPack-verifier coverage pass; hosted package jobs remain required release evidence. Preserve the invariant package names and formats and keep `tests/run_cpack_artifact_contract_check.cmake` fail-closed for missing, extra, empty, directory, or symlink artifacts. Issue `#4044` is closed.

- Shipped GitHub Actions supply-chain slice `#4042`: every external action in workflows and the shared native-validation composite action is pinned to a reviewed full commit SHA with a retained release comment; local reusable actions remain relative references. All workflows now expose explicit read-only token permissions, and `test_github_actions_contract` scans the complete `.github` action surface for mutable refs, malformed external names, missing release comments, missing permission blocks, and write-capable scopes. The five workflow-contract tests pass locally; hosted representative workflow runs remain required after dependency changes. Keep the documented dependency-update procedure in `docs/26-localization-and-release-readiness.md`. Issue `#4042` is closed.

- Shipped invocation-argument parser hardening slice `#3968`: the shared PRG function/method parser now requires a comma after every non-final regular or `@`-reference argument, so adjacent primary tokens cannot silently become extra arguments. Preserve operator-separated expressions, valid comma-separated calls, existing empty/trailing argument semantics, and the catalog-routed `Runtime.Prg.Expression.Error.ExpectedFunctionArgument` diagnostic. Regression coverage spans ordinary functions, by-reference calls, and object methods; `test_prg_engine_control_flow` passes in 92.28 seconds. Issue `#3968` is the current closure slice.
- Shipped SQL aggregate projection slice `#3972`: direct no-`GROUP BY` `SELECT COUNT/SUM/AVG/MIN/MAX` projections now materialize exactly one summary row, pass the query `WHERE` predicate into the existing visibility-aware aggregate scan, and treat `COUNT(*)` as a record count instead of sending `*` into the scalar expression parser. Preserve the explicit boundary: grouped and multi-table aggregate semantics remain separate work; do not broaden this compact query-plan representation without a prompt-sized child issue. Runtime-surface Requery coverage passes filtered and empty local-table cases. No visible text or machine contract changed.
- Shipped SQL JOIN projection slice `#3969`: direct `SELECT *` expansion appends the current joined cursor's fields after the primary cursor's fields, including the synthetic blank joined record used for unmatched `LEFT JOIN` rows. Preserve primary-then-joined positional ordering and the explicit boundary that three-table join chains remain separate architecture work. The real list-control SQL Requery regression covers inner and left joins; no visible text or machine contract changed.
- Shipped SQL JOIN-chain hardening slice `#4018`: `parse_query_plan` rejects a second top-level `JOIN` in the first join's `ON` tail before that unsupported text can reach expression evaluation. Preserve the current one-join query-plan boundary; two-table inner/left/right behavior remains supported, list controls use their existing empty-result fallback for rejected row sources, and `INSERT INTO ... SELECT` receives the existing localized invalid-query diagnostic. Three-table rejection and adjacent control-flow/requery coverage pass. Do not claim multi-table join support until the query plan becomes structurally plural.
- Shipped SQL TOP projection slice `#3966`: `QueryPlan::top_count` parses `TOP n` alongside `DISTINCT`, and row materialization truncates only after existing stable ORDER BY sorting and DISTINCT deduplication. Preserve `TOP 0`, ordered result identity, and the explicit first-pass boundary: `TOP PERCENT` and broader SQL pagination remain separate work. Focused list-control Requery coverage plus adjacent control-flow tests pass.
- Shipped SQL subquery hardening slice `#3967`: `parse_query_plan` rejects a top-level `WHERE ... IN (SELECT ...)` predicate before the generic expression parser can treat `IN(...)` as an unknown truthy function and disable filtering. Preserve the explicit boundary: SQL subquery execution is not implemented; list controls use their existing empty-result fallback for rejected row sources and `INSERT INTO ... SELECT` retains the localized invalid-query diagnostic. Focused subquery rejection plus adjacent control-flow/requery coverage pass.
- Shipped SQL grouping slice `#3960`: single-source query plans now recognize `GROUP BY` and `HAVING`, collect physical record numbers after `WHERE`/visibility filtering, build scalar and composite groups, evaluate direct aggregate projections per group, substitute projection aliases and direct aggregate calls in HAVING, and retain existing DISTINCT/ORDER BY/TOP post-processing. Preserve the explicit boundary: grouped multi-table queries are rejected while the join plan remains singular; do not claim general SQL grouping or subquery support. Focused list-control grouping coverage plus adjacent control-flow/requery tests pass.
- Shipped SQL date-ordering slice `#3931`: query materialization now sends paired DATE/DATETIME `ORDER BY` keys through `compare_date_time_values`, using the current data-session `SET` state only to parse display text when a timeline scalar is unavailable. Preserve stable multi-key ASC/DESC sorting, mixed DATE/DATETIME chronological behavior, empty-value handling, and presentation independence; numeric and plain-character keys retain their existing paths. The list-control Requery regression uses real DBF `D`/`T` fields under changing `SET DATE`, `MARK`, `HOURS`, and `SECONDS`. Expression-level `MIN()`/`MAX()` remains separate issue `#3930`.
- Shipped date aggregate slice `#3930`: expression-level `MIN()`/`MAX()` now reuse `compare_date_time_values` when both operands carry DATE/DATETIME flavor, preserving candidate type and treating DATE as midnight for mixed comparisons. Cross-year, within-day, mixed-value, and changed date-presentation regressions pass; ordinary numeric and plain-character comparisons remain on their existing branches. Keep invalid date/time comparator fallback behavior unchanged, and keep aggregate `MIN()`/`MAX()` over cursor fields as a separate existing aggregate path.
- Shipped DATE/DATETIME equality slice `#3923`: `values_equal` now consults `compare_date_time_values` for typed date/time pairs before applying `SET EXACT` character semantics. Preserve exact same-instant behavior in both operand orders, including DATE-as-midnight, and keep plain-character prefix matching plus numeric equality unchanged. Focused date/time, control-flow, runtime-surface, and string/math suites pass.
- Shipped date/time empty-value slice `#3916`: every implemented Date/DateTime producer now returns a flavored empty value on invalid/empty input, including constructors, `MDY`/`DMY`, `STOD`/`CTOD`, `GOMONTH`/`EOMONTH`, `CTOT`/`DTOT`/`TTOD`, and the split `JTOT`/`JTOD` path. Preserve `JTOT` as a DateTime-at-midnight producer, `JTOD` as Date, and all character-returning formatter contracts. Keep invalid empty values compatible with `VARTYPE`, `TYPE`, `EMPTY`, typed comparison, and typed arithmetic; the `{}` empty-date literal parser remains separate work.
- Shipped cursor-boundary slice `#3948`: `move_cursor_to` keeps `recno=1` for empty cursors and targets at or before BOF, while the EOF branch remains one-past-last. Preserve the asymmetry explicitly: `BOF()` is true with `RECNO()` 1, and `EOF()` is true with `RECNO()` `RECCOUNT()+1`; work-area, SQL-cursor, and empty-table navigation tests cover both boundaries.
- Shipped STRTRAN flag slice `#3950`: the sixth `nFlags` argument uses bit 1 for case-insensitive search, while flag 0 retains case-sensitive default behavior. Search positions and non-overlapping occurrence counts stay in source coordinates, and unmatched source text preserves its original casing. Keep explicit negative `nCount` follow-up #4100 separate; do not fold that count semantic into the flag change.
- Shipped transaction-state slice `#3949`: `END TRANSACTION` and `ROLLBACK` now check the current data-session transaction level before changing journals or emitting transaction events. At level zero they route `Runtime.Prg.Transaction.Error.NoActiveTransaction`, record the statement fault, and leave a resumable runtime error. Keep the diagnostic in all four locale catalogs and preserve balanced nested transaction behavior.
- Shipped STRTRAN count slice `#4100`: negative explicit `nCount` values map to the unlimited replacement sentinel, while zero and positive values retain bounded behavior. Preserve the sixth-argument `nFlags` case-insensitive path and the existing case-sensitive default; focused string/math and control-flow coverage passes.

- Shipped package publication transaction slice `#4053`: generated-launcher and native-library materialization now leave the owned package backup and an explicit `phase=awaiting_primary_output` marker until external publication, exact launcher-sidecar or native-wrapper admission, and atomic `app.cfmanifest`/`app.cfdebug` finalization all succeed. `copperfin_build_host` calls the runtime abort API when `dotnet publish` or native primary-output generation fails; finalization failures roll back the fresh package, while a first-ever failed build removes the incomplete package and a later build can retry cleanly. Repeated finalization after a committed package remains idempotent. Preserve the deferred marker as an on-disk ownership contract, keep logical package paths in returned plans and manifests, and never expose the transaction phase in machine manifest/debug fields. Deterministic runtime-pipeline regressions establish a known-good package, exercise second-build finalization failure and publish abort, verify old manifest/debug bytes plus transaction cleanup, cover library rematerialization, and complete later successful replacement. Local GCC and Clang runtime-pipeline suites plus build-host compilation pass; Windows deep validation of real `dotnet publish` remains pending. Issue `#4053` is the active closure slice.

- Shipped localization QA slice `#4082` in `e29467ba`: `en-US`, `es-419`, `pt-BR`, and `qps-ploc` catalogs now have enforced key parity, nonblank values, and exact placeholder name/multiplicity parity in `test_localization`; every pseudo-locale key is exercised with marker replacement values to prove decorated expansion preserves those values. Corrected 13 existing launch-diagnostic and visual-asset identity entries that dropped or renamed placeholders. Local GCC/Clang catalog plus adjacent Studio/visual-asset tests pass, and hosted Windows run `29605304500` passes all 7/7 focused environment/executable-path tests including MSVC `test_localization`; issue `#4082` is closed. `es-419` and `pt-BR` remain placeholder/review-pending language packs: do not call technical parity or machine translation production acceptance, and require qualified human review for tone, terminology, regional usage, formality, euphemism/directness, and awkward/offensive wording.
- Shipped SCAN predicate continuation slice `#4095` in `dcac1e99`. Expression-bearing `SCAN FOR`, `SCAN WHILE`, and active cursor-filter searches use heap-backed cursor-search state only when an expression resolves to an active unqualified user routine, preserving Rushmore/index-seek and remote SQL cursor paths otherwise. Candidate recno, work area, visibility order, filter/WHILE/FOR stage, scan loop state, `SCAN REST` scope, EOF/FOUND boundary state, `LOOP`, `EXIT`, nested routine calls, `RESUME`, exactly-once side effects, recursive-predicate max-call-depth failure, and low-stack behavior are covered. The first hosted Windows run `29557861702` exposed the pre-correction Rushmore and SQL regressions; GCC/Clang and ASan/UBSan focused suites plus hosted Windows run `29561179670` pass, with Windows green at 283/283 native tests. The full POSIX validation rebuild passed CTest entries 1-24 before its 10-minute wrapper timed out while starting entry 25's long Studio-host suite; do not call that a full POSIX pass. Keep the exact Windows evidence and regression rationale linked from issue `#4095`.
- Shipped Currency value slice `#4070` in `f17978c9`: add `PrgValueKind::currency` with signed 64-bit fixed-point units scaled by 10,000, and make `VAL()` return it when the trimmed source begins with `$`. Preserve four-place rounding, negative values, exact same-kind arithmetic/comparison, `VARTYPE()`/`TYPE()` code `Y`, zero/boolean conversion, list/date/SQL numeric boundaries, debug formatting, and `Y` memvar/expression serialization. Do not use a floating-point payload as the authoritative value, add user-facing text without catalogs, or change machine-contract field names. GCC/Clang focused builds and the data I/O, runtime-surface, and string/math tests pass. The package audit queued #4096 (post-commit status agreement), #4097 (Windows package-root identity casing), #4098 (Windows manifest-pair identity casing), and #4099 (required-asset existence refresh) for separate hardening slices; do not mix them into this issue.
- Shipped package-root parent identity slice `#4063` in `b646ef19`: pin the admitted output parent for the complete transaction. Windows holds a directory handle without delete sharing and verifies volume/file identity; POSIX retains the parent descriptor, routes package filesystem work through its stable fd path, and restores/removes transaction leaves through descriptor-relative operations after a parent rebind. Keep logical package paths in returned plans and manifest/debug contents while using the pinned filesystem paths only for writes and cleanup. The rematerialization regression first creates a known-good package, then moves the POSIX parent and installs an external symlink during the transaction; the external tree remains untouched and the known-good package is restored. Windows proves the handle blocks the parent move. GCC/Clang runtime-pipeline tests and exact hosted Windows validation `29569966017` at final head `2301f9bd` pass; issue `#4063` is closed.
- Shipped package-content write-hardening slice `#4066` in `e2e016f0`: pin the admitted content directory for each runtime package transaction. POSIX primary/companion writes walk pinned directory descriptors with `openat(..., O_NOFOLLOW)`, write unique temporary files, `fsync`, and atomically `renameat`; Windows holds the direct destination parent without delete sharing and uses a `CREATE_NEW` temporary followed by `MoveFileExW`. Existing symlink/reparse and multiply-linked destination checks remain fail-closed, while interrupted writes leave external bytes untouched and package rollback restores the last known-good tree. Deterministic tests cover content-root rebinding and hard-link leaf rejection. GCC/Clang runtime-pipeline suites and exact hosted Windows validation `29572339418` at head `3c200beb` pass; issue `#4066` is closed.
- Shipped materialization status slice `#4096` in `d57dce1c` keeps a post-commit manifest-pair refresh failure warning-only after the new package is live, rather than returning `ok=false` after rollback has become impossible. The warning uses catalog key `Runtime.Package.Warning.ManifestPairRewriteFailed` in all four current catalogs, preserves invariant package-plan/manifest/debug/status fields, and records the underlying pair-publish detail. A deterministic cleanup-warning plus promotion-fault regression proves `ok=true`, a live package, warning evidence, and no transaction artifacts under GCC and Clang. The on-disk pair may remain at the prior generation and must be treated as stale until a later successful refresh. Exact hosted Windows validation `29575181855` at synchronized head `87081c49` passes; issue `#4096` is closed.
- Shipped required-asset freshness slice `#4099` in `cec73228`: rematerialization refreshes every planned asset's live source existence immediately before required-startup admission and copy. A required startup source restored after planning can be staged through the same plan without weakening source-resolution, optional-asset, rollback, or machine contracts. GCC and Clang runtime-pipeline tests cover absent-at-plan/restored-before-materialization behavior and verify restored bytes. Exact hosted Windows validation `29577742606` at synchronized head `742d0bf3` passes; issue `#4099` is closed.
- Shipped Windows package-root identity slice `#4097` in `0f1d08ea` with the assertion correction in `7d266b9b`: the package transaction marker ownership identity and per-root mutex use a canonical, case-folded Windows path while filesystem I/O and returned logical package paths preserve their caller spelling. Legacy raw-cased transaction markers are accepted only when their recorded path resolves to the same package identity. A Windows-only interrupted-recovery regression uses a casing-only package alias; GCC and Clang runtime-pipeline tests and exact hosted Windows validation `29583147926` at synchronized head `7d266b9b` pass; issue `#4097` is closed.
- Shipped manifest-pair identity slice `#4098` in `db676b5e`, with Windows API compatibility correction `765f6333`, explicit regression include `716e3393`, concurrent-lock correction `bc1b66bb`, diagnostics `b5882906`, and complete alias-plan fixture correction `d84f05dc`: manifest-pair mutex/journal hashing reuses the shared canonical, case-folded Windows path identity for the package root and both manifest destinations, while logical manifest paths remain caller-spelled. Windows regressions cover casing-only identity equality, alias lock collision, and stale-journal recovery through a complete alias plan; GCC and Clang runtime-pipeline tests pass. Hosted Windows validation `29602198425` at final head `d84f05dc` passes all 283/283 native tests; issue `#4098` is closed.
- Shipped package-root transaction serialization slice `#4064` in `9e7754af`: acquire a nonblocking per-package-root lock before stale recovery or package mutation, using a Local named mutex on Windows and an atomically published Copperfin-owned `.copperfin-lock` plus `flock` on POSIX. Normalize the lock identity through absolute/weakly-canonical path resolution where available, preserve fail-closed caller-owned lock collisions, and keep the lock sidecar stable across process exit so later materialization can reacquire it. Same-process, cross-process, collision, and later-process recovery regressions pass under GCC and Clang. The lock sidecar is implementation metadata; deeper Windows package-root and manifest-pair identity casing remain owned by #4097 and #4098.
- Shipped loop-expression continuation slice `#4094`: direct `DO WHILE`, `FOR`, and `FOR EACH` predicates, numeric bounds, and collection expressions use heap-backed checkpoints. Preserve exactly-once evaluation, nested loop state, same-frame and nested `RESUME`/`RETRY`, TRY/CATCH/FINALLY, `ON ERROR`, debugger breakpoints, statement budgets, low-stack execution, direct `CANCEL`, and `QUIT`; do not clear a loop checkpoint while an `ON ERROR` handler still needs it, and do not reintroduce synchronous expression evaluation. Focused GCC/Clang, ASan/UBSan, 1 MiB-stack, expanded loop regression coverage, and exact hosted Windows run `29553500669` pass. Local full POSIX validation is environment-blocked for 38 real-sample report tests because the mounted VFP9 ISO path is root-only. `SCAN FOR/WHILE` visibility-filter continuation is queued separately as `#4095` because cursor-search state needs its own checkpoint design.

- Shipped DO CASE predicate continuation slice `#4093`: CASE predicates use the same heap-backed source-span checkpoints as `RETURN`, assignments, and IF/ELSEIF. Preserve left-to-right evaluation, nested and SCAN-embedded CASE state, false-to-next-clause/OTHERWISE routing, exactly-once side effects, references, arrays, `SET UDFPARMS`, TRY/CATCH/FINALLY, `ON ERROR`, debugger events, breakpoints, exact statement budgets, cancellation, direct `CANCEL`, `QUIT`, `RESUME`, and `RETRY`; do not reintroduce recursive host-stack growth. TRY entry records CASE depth and all TRY exits unwind CASE contexts created inside the TRY. An `ON ERROR` handler's `RETURN` value must never become the value of the expression that faulted. Focused GCC/Clang, ASan/UBSan, 1 MiB-stack, complete 282-test POSIX validation, and hosted Windows run `29548368685` pass. Loop predicates/bounds, standalone expressions, STORE, command arguments, object/member expressions, and other contexts remain under `#4068`.
- Shipped assignment-expression continuation slice `#4091`: assignment right-hand sides use the same heap-backed source-span checkpoints as `RETURN`; do not add a second evaluator or reintroduce synchronous user-routine recursion. Preserve exactly-once reads, built-ins, arguments, references, nested routines, side effects, and deferred writes through `assign_runtime_target_value()` for memory, array, field, and object targets. Resumption must not recount the statement or replay its breakpoint/execute event, and step-over/out must finish the suspended assignment before advancing. Fault, async cancellation, direct `CANCEL`, and confirmed `QUIT` paths must abandon pending continuations before control transfer. The 2,048-frame guardrail passes with a 1 MiB Linux process stack. Conditions, loop predicates/bounds, standalone expressions, STORE, command arguments, and other contexts remain under `#4068`.
- Shipped IF/ELSEIF predicate continuation slice `#4092`: conditional predicates use the same heap-backed source-span checkpoints as `RETURN` and assignment RHS evaluation. Preserve left-to-right and short-circuit behavior, nested and argument-nested calls, IF/ELSEIF/ELSE/no-match routing, references, arrays, `SET UDFPARMS`, TRY/CATCH/FINALLY, `ON ERROR`, debugger events/breakpoints, exact statement budgets, cancellation, and direct `CANCEL`/confirmed `QUIT`; do not replay completed operands, branch statements, or source metadata and do not increase process stack reserves. Focused GCC/Clang, ASan/UBSan, 1 MiB-stack, and complete 282-test POSIX validation pass; hosted Windows evidence is required before issue closure. DO CASE, loop predicates/bounds, standalone expressions, STORE, command arguments, object/member expressions, and other contexts remain under `#4068`.
- Shipped localization-root discovery slice `#4088`: automatic discovery accepts a candidate only when `en-US/strings.json` is a readable, valid regular file. Continue past empty, file-shaped, incomplete, unreadable, malformed, or directory-shaped decoys while preserving the established executable-relative, installed-share, PATH-launched, working-directory, and ancestor order. A nonblank explicit `COPPERFIN_LOCALE_DIR` remains caller-owned and authoritative even when it does not satisfy the discovery contract; do not silently replace it with another root. Focused GCC/Clang localization and platform-environment coverage includes Unicode paths and precedence without changing fallback, keys, visible text, or machine contracts.
- Shipped native procedure-class lookup slice `#4089`: unqualified `CREATEOBJECT()` and bare `NEWOBJECT()` search the caller PRG first, then the ordered PRGs opened by `SET PROCEDURE TO ... [ADDITIVE]`, before generic Automation fallback. Non-additive replacement and bare clear affect future lookup; additive duplicates retain first-opened precedence. Explicit `NEWOBJECT(..., library.prg)` remains isolated to its named PRG. Unsourced base classes may resolve from the loaded procedure list, but every created object snapshots the resolved base-to-derived PRG/class identities so later procedure replacement or clear cannot break inherited methods, accessors, reflection/edit paths, or `DODEFAULT()`. Remove that snapshot on every rejection, subtree erase, and release path. Preserve the caller-before-procedure order, stack-frugal method frames, visible text, and machine contracts.
- Shipped native test resource-discovery slice `#4085`: CTest supplies `${PROJECT_SOURCE_DIR}/resources/locales` to every source-tree test that does not already own `COPPERFIN_LOCALE_DIR`. Shared Studio subprocess tests restore this explicit fixture root after default-locale scopes clear the environment; this is test harness behavior, not product fallback. Tests whose purpose is executable-relative discovery must clear the override and deploy their own temporary installed layout, as the Build Host PATH probe now does. The focused Windows environment workflow builds under `runner.temp` and runs localization, Build Host, Runtime Host, executable-path, platform-environment, licensing, and shared Studio-host coverage. Preserve native-width/UTF-8 path conversion for filesystem environment variables and use `PROJECT_SOURCE_DIR` rather than an outer `CMAKE_SOURCE_DIR` for Copperfin-owned fixtures. Exact non-adjacent focused coverage plus `test_runtime_pipeline` pass; complete POSIX validation passes 281/281 with only the expected launcher skip. Product discovery and installed package layout remain unchanged; `#4088` separately owns rejecting invalid decoy roots during product auto-discovery.
- Shipped native CTest isolation child `#4087`: `tests/CopperfinTestIsolation.cmake` classifies every registered test fail-closed and writes `native-test-isolation.tsv`; the CMake-3.20-policy contract rejects missing axes, schedule/property disagreement, invalid resource-lock state, duplicate identities, stale inventory source hashes, and any `audit=pending` row. Current Linux configuration has 281 tests: 273 parallel-eligible and eight serial. The first local `--parallel 2` trial exposed a shared Studio fixture-root collision between distinct specialized targets; 42 affected Studio tests now share `copperfin-studio-host-shared-fixtures`, while the duplicate code-page identities share `copperfin-visual-asset-code-page-root`. The corrected local run passes 281/281 in 126.01 seconds versus 186.25 seconds serial. Three hosted Windows runs at `84c8c2b3` pass 282/282 in 342.27, 350.47, and 313.44 seconds versus the 449.70-second serial baseline, a 25.42% mean reduction. Exact ordinary Windows run `29515373596` passes 282/282 in 319.59 seconds and exact Linux policy-correction run `29518122094` passes 281/281. Ordinary Windows CI, manual deep validation, and `validate-windows.ps1` use an explicit two-job CTest cap. Keep Linux and macOS serial until each has independent evidence; never replace the cap with bare parallel execution.
- Shipped standalone Studio document-identity slice `#3954`: normalize relative/absolute aliases to full paths, retain Windows case-insensitive comparison, use exact comparison on POSIX, and canonicalize each existing POSIX path component to its stored spelling. This keeps real case-only files distinct on case-sensitive volumes while deduplicating aliases on case-insensitive macOS/POSIX volumes; nonexistent case mismatches remain unbound. Each tab and embedded editor retain the same exact canonical path. Preserve this filesystem-aware policy rather than replacing it with an OS-only comparer. Cross-platform managed tests, `net472` Studio/designer compilation, exact-binding smoke assertions, and complete 279-test POSIX validation pass; visible and machine contracts are unchanged.
- Shipped cross-platform managed host-discovery slice `#3889`: when host environment variables are unset, automatic discovery prefers extensionless names on POSIX and `.exe` on Windows, and automatic POSIX candidates must pass `access(..., X_OK)`. Existing explicitly configured files remain exact caller-owned paths even when automatic policy would reject them; a configured missing path fails closed instead of silently falling through. Fire-and-forget Studio launch catches startup exceptions and returns `false` to existing localized callers; the captured runner retains localized diagnostic detail. Preserve invariant host stems and environment names. Simulated Windows/POSIX ordering, real Linux execute-bit, configured-path, layout, startup-failure, `net10.0`, and full managed compile coverage pass.
- Shipped BCP 47 localization path slice `#3894`: canonicalize language subtags to lowercase, script subtags to title case, alphabetic regions to uppercase, and variants/extensions/private-use subtags to lowercase with locale-independent ASCII operations. Preserve right-truncated script parents (`zh-Hant-TW` -> `zh-Hant` -> `zh` -> `en-US`), removal of orphan extension singletons, `es-419` immediately before bare Spanish, and exact `qps-ploc` spelling. Canonically cased script-only and script-plus-region catalog directories load on case-sensitive hosts. Focused GCC/Clang, GCC ASan/UBSan, and complete 279-test POSIX validation pass; catalog text, parser tokens, and machine contracts are unchanged.
- Shipped Windows Studio test-transport slices `#4080`/`#4081` and parent `#4076`: ordinary Studio-host fixtures launch the host directly with an argument vector, native-width Windows paths, explicit working directory, binary stdout/stderr capture, and exact exit status. Keep raw capture byte-exact; apply `normalize_captured_process_line_endings()` only at Studio JSON observation boundaries. Final `2eb6fb90` passes affected GCC/Clang matrices, focused sanitizer execution, complete 279-test POSIX validation, and hosted Windows run `29461144155` with all 280 native tests green, including the eight formerly failing multiline fixtures.
- Shipped localization file-boundary slice `#3893`: `load_catalog_file()` accepts exactly one UTF-8 BOM only at byte zero before invoking the strict JSON parser. Preserve direct-parser rejection plus double, displaced, and trailing BOM rejection; retain UTF-8 values, locale fallback, keys, placeholders, and machine contracts. Focused GCC/Clang, ASan/UBSan from the repository root, and complete 279-test POSIX validation pass.
- Shipped Windows runtime-package resolver slice `#4079` and parent `#4077`: `resolve_existing_path_casefold()` admits a component only after exact and unique-casefold lookup fail and the host filesystem proves that exact token resolves, supporting NTFS 8.3 ancestor aliases without weakening ambiguity or missing-path rejection. Final `e5d632ac` assertions independently preserve nested xAsset companion directory-entry spelling, native UNC normalization, escaped debug provenance, and valid absolute debug manifests. Hosted MSVC `test_project_workspace` and `test_runtime_pipeline` pass with all PRG/SCX/VCX/FRX/LBX/MNX, DBF/DBC, writable-data, C# output, and rematerialization coverage intact.
- Shipped VFP workspace-path slice `#4078`: after absolute VFP paths receive the established project-relative or deterministic-leaf treatment, return already-relative PJX text unchanged instead of passing it through host `std::filesystem` separator normalization. Preserve relative backslash, forward-slash, mixed-separator, dot-segment, live, and deleted metadata as document identities; perform host path conversion only at explicit filesystem I/O boundaries. Focused GCC/Clang and ASan/UBSan, complete POSIX validation, and hosted MSVC `test_project_workspace` pass. Older duplicate `#4049` is satisfied by this slice. No visible text or machine contract changed.
- Shipped PRG fixture-containment correction `#4075`: `test_runtime_faults_preserve_state_and_allow_retry()` must materialize its PRG, cursor, and command-undo state beneath a process-owned absolute OS-temp root, scope the runtime session before cleanup, and remove that root after the caught-fault/RETRY case. Do not restore the deleted repository-root `runtime_fault_test.prg`, pass a repository/build CWD as this fixture's temp owner, or hide generated `runtime-temp/` with `.gitignore`. The control-flow executable records whether its working-directory `runtime-temp/` existed at entry and fails a clean run if any fixture creates it. Standalone GCC/Clang, ASan/UBSan, clean build-tree CTest, complete POSIX validation, hosted Linux/macOS, and the hosted Windows MSVC control-flow target leave no repository or owned OS-temp residue; preserve this for success, caught faults, cancellation, and failure paths without changing production temp semantics.
- Shipped return-expression UDF continuation slices `#4074`/`#4090`: after ordinary array, built-in, native, and declared-DLL precedence selects a user routine, every invocation reached while evaluating `RETURN` pushes the callee onto the heap-backed frame deque instead of re-entering the synchronous expression runner. Per-frame checkpoints retain completed primary values and user-routine results by source span, preserving left-to-right values and exactly-once variable reads, arguments, built-ins, side effects, and nested calls across resumption. Keep parser-owned reference classification and expression-depth scoping so `SET UDFPARMS`, explicit scalar/array references, bare arrays, and dispatch precedence remain exact. Resume through the ordinary TRY/CATCH/FINALLY and `ON ERROR` boundary; do not recount the suspended statement, replay its breakpoint or execute event, or advance debugger/cancellation source metadata past the original `RETURN`. The 2,048-frame guardrail passes GCC/Clang, ASan/UBSan, and a 1 MiB Linux process stack. Parent `#4068` now owns the remaining non-`RETURN` expression contexts, including assignments, conditions, loop predicates, command arguments, and object/member expressions; do not substitute a larger process stack.
- Shipped macOS native-link portability slice `#4073`: `src/vfp/dbf_text_encoding.cpp` makes `cf_vfp_assets` the sole non-Windows owner of `Iconv::Iconv`. Keep that dependency on the owning static-library target so CMake propagates the required Apple link flags to direct and transitive consumers. Do not move it back to `cf_xbase_runtime` or add per-executable/test workarounds. Focused GCC/Clang consumers and DBF/runtime tests pass; hosted macOS native validation is the required Apple evidence.
- Shipped free-table schema hardening slice `#4034`: raw/free DBF physical field names may contain at most 10 encoded bytes. Validate before DBF/FPT staging or replacement and keep the localized diagnostic separate from invariant parser, host, and machine contracts. Continue parsing legacy 11-byte descriptors tolerantly, but never emit them. DBC, SQL metadata, report, and editor models may retain longer logical names only when their owning boundary supplies an explicit collision-safe logical-to-physical mapping; do not weaken the writer or apply naive DBC truncation. `COPY TO` / `COPY STRUCTURE TO` preserves valid names and maps only overlength SQL cursor fields. Focused GCC/Clang and complete GCC coverage pass; Windows remains hosted validation evidence.
- Shipped runtime exception-lifecycle slice `#4020`: a try already handling a `CATCH` remains eligible to dispatch its not-yet-entered `FINALLY`, while a try whose `FINALLY` is active is retired before the replacement fault propagates outward. Preserve exactly-once cleanup and do not let an active try rematch its own CATCH. Both ordinary and expression-invoked parent-frame walkers recognize pending cleanup; internal propagated faults preserve the innermost replacement message/source, and new generic host faults clear stale caught-object identity without discarding fresh OLE/SQL detail. Complete GCC/Clang control-flow coverage passes; no visible or machine contract changed.
- Shipped runtime control-flow hardening slice `#4019`: every `FOR`, `FOR EACH`, `SCAN`, and `DO WHILE` state captures the active `DO CASE` depth at entry. `LOOP` and `EXIT` restore that exact depth alongside `WITH` cleanup, preventing per-iteration stale case contexts while preserving cases enclosing the loop. Keep normal `ENDCASE` handling unchanged and capture depth at every future loop-state creation path. Focused all-loop, enclosing-case, and 10,000-iteration coverage passes under GCC/Clang; no visible or machine contract changed.
- Shipped runtime BROWSE-clause fidelity slice `#4032`: `NOWAIT` is now a top-level parser boundary for BROWSE `FIELDS`, `IN`, and `FOR` extraction. Preserve the complete requested field list, target cursor, and clean filter metadata across clause orderings while keeping the invariant `runtime.browse` event identity and raw clause summary. Focused GCC/Clang data-I/O coverage exercises each boundary; no visible or machine contract changed.
- Shipped runtime error-handler fidelity slice `#4021`: bare `ON ERROR` now parses as an empty handler assignment and restores default uncaught-error propagation, matching shipped VFP9 documentation. Preserve nonempty `ON ERROR <command>` binding, one-handler-at-a-time semantics, fault metadata, and TRY/CATCH priority. Focused GCC/Clang coverage proves the cleared handler does not intercept a later fault; no visible or machine contract changed.
- Shipped runtime parameter-fidelity slice `#3951`: unsupplied `PARAMETERS`, `LPARAMETERS`, inline-signature, and object-method formals now initialize to logical `.F.` instead of unknown values, as VFP9 documentation requires. Preserve explicit parameter defaults, caller argument binding and references, and actual-argument `PCOUNT()` semantics. Focused GCC/Clang routine-call coverage exercises every declaration path with no visible or machine contract change.
- Previous runtime declaration-fidelity slice `#3900`: bare `LOCAL` and `PUBLIC` scalar declarations now initialize to logical `.F.` instead of unknown values, as VFP9 documentation requires. Preserve public lifetime and existing scoped-array initialization; focused GCC/Clang core runtime coverage passes with no visible or machine contract change.
- Shipped runtime array-scope slice `#4071`: `PUBLIC ARRAY`, `PRIVATE ARRAY`, and `LOCAL ARRAY` now parse as real arrays. Keep public arrays global, private arrays on the existing hide/restore path, and local arrays frame-owned, initialized to `.F.`, invisible through bare names to caller/callee frames, and removable by `RELEASE` or frame exit. Preserve ordinary DIMENSION/DECLARE behavior, by-reference array binding, native-object arrays, PRIVATE ALL, RELEASE ALL, and CLEAR MEMORY; focused GCC/Clang control-flow coverage passes with no visible or machine contract change.
- Shipped runtime string-conversion slice `#3865`: `VAL()` now consumes a VFP decimal/exponent prefix rather than the full C++ floating-point grammar. Preserve leading plus/minus and scientific notation, which shipped VFP9 help explicitly permits, while rejecting `nan`, `inf`, and hexadecimal forms. The missing Currency `PrgValue` representation remains a separate fidelity issue; focused GCC/Clang string/math tests pass with no visible or machine contract change.
- Shipped runtime array-fidelity slice `#3956`: `ASCAN()` now compares two live runtime-object references by exact identity independent of `SET EXACT`, and treats a null operand as matching only another null. Preserve ordinary character comparison, including its existing `SET EXACT` and flag behavior, plus numeric/boolean matching and predicate scan semantics. Focused GCC/Clang array tests pass; no visible or machine contract changed.
- Shipped runtime array-fidelity slice `#3898`: `AELEMENT(aArray, n)` now treats its one subscript as the one-based linear element index for both one- and multi-dimensional arrays, returning `0` only outside the allocated array range. Preserve the explicit `AELEMENT(aArray, nRow, nColumn)` path through `array_linear_index()` and the related `ASUBSCRIPT()` contract. Focused GCC/Clang array tests pass; no visible or machine contract changed.
- Shipped runtime array-fidelity slice `#3868` maps VFP `ASORT()`'s fourth `nSortOrder` directly: `0`/`1` ascending case-sensitive, `2` descending case-sensitive, `3` ascending case-insensitive, and `4` descending case-insensitive. Do not reintroduce a fifth pseudo-flag argument. Preserve partial-window and 2D row-association behavior. GCC/Clang array and data-I/O regressions cover the corrected values.
- Shipped runtime array-fidelity slice `#3952` makes `ASCAN(aArray, eExpr, nStartElement, nElementsSearched, nSearchColumn, nFlags)` interpret start/count against the selected column's one-dimensional row sequence. Preserve ordinary flattened-array scanning, column bounds checks, row-return flag behavior, predicate metadata binding/restoration, and no-match `0` behavior. Focused GCC/Clang coverage must retain the column-relative false-positive exclusion and in-window linear/row result cases.
- Shipped runtime-debug launch-contract slice `#3955` routes managed runtime replay through `CopperfinStudioHostBridge.CreateProcessStartInfo`, the same wrapper-aware normalizer as managed Build/Run. Windows `.cmd`/`.bat` runtime hosts must keep `COMSPEC /d /c` wrapping, while `.exe`, extensionless, macOS, and Linux hosts remain direct. Preserve manifest/debug-command quoting, locale environment names, contained startup diagnostics through `AssetEditor.Dialog.RuntimeHostCouldNotStartWithMessage`, and invariant debug/host contracts. The linked managed smoke covers direct, wrapper, missing, and unlaunchable host paths; full VSIX/Windows execution remains the platform validation lane.
- Shipped debugger path-identity slice `#3884` uses a single platform-aware comparison for breakpoint matching, duplicate suppression, removal, and the resume-skip location. Windows uses ordinal case-insensitive lexical path identity; POSIX retains normalized case-sensitive identity. Preserve the exact stored/debug-event paths and all event identities. Focused coverage must retain casing-variant hit, duplicate, and removal assertions under both platform branches.
- Shipped focused-workflow trigger-contract slice `#4045` gives the Windows environment/path, POSIX executable-path, and Windows DECLARE ABI suites component-level input coverage plus a shared checked CMake contract. Keep the workflows focused on their current target inventories and bounded `--parallel 2` builds, but retain `push`/`pull_request` coverage for direct production, header, fixture, locale, and CMake inputs so future source additions cannot suppress validation. The contract runs directly in each focused workflow and under CTest.
- Shipped supply-chain artifact slice `#4046` makes `actions/upload-artifact` the only SBOM publisher for the stable `cyclonedx-sbom` / `sbom.cdx.json` artifact. Anchore must retain `upload-artifact: "false"` and `upload-release-assets: "false"`; the explicit uploader must keep repository-default `retention-days: 0` and fail on a missing output. The workflow runs its checked CMake contract before generation, and its touched actions remain pinned with release-tag comments under read-only token permissions. Do not add another SBOM owner without deliberately changing the contract and hosted artifact evidence.
- Shipped safety-traceability dispatch slice `#4043` keeps workflow-dispatch `issue_numbers` and boolean settings outside the PowerShell command text: bind them in `env` and pass `$env:` values to the validator. Keep the script's two-pass issue-list handling, which validates every token before any GitHub API request. The safety workflow runs its own CMake contract, and that contract must retain quote, semicolon, dollar-sign, newline, and comment-marker probes through a real PowerShell invocation with a fake token; malformed values must fail as nonnumeric without an API call. Preserve report artifact name/path and `contents: read` / `issues: read` permissions.
- Shipped data-import fidelity slice `#4031` makes local DBF `APPEND FROM` honor an active `SET FILTER` on an already-open source cursor. Preserve source filtering through the established record-visibility helper, physical-deleted-row handling, and source cursor position while the destination mutates; an unopened source must retain direct DBF import behavior. Focused native data-I/O coverage passes; no visible text or machine contract changed.
- Shipped text-merge fidelity slice `#4022` makes statement-form `TEXT ... ENDTEXT ... TEXTMERGE` single-pass by default. Preserve delimiter-shaped text resulting from a merge expression literally; only the explicit `TEXTMERGE(..., .T.)` function may recurse. Default and custom delimiter regression coverage passes under GCC/Clang; no visible text or machine contract changed.
- Shipped variable-scope parity slice `#3974` implements `PRIVATE ALL [LIKE <skeleton> | EXCEPT <skeleton>]` using the existing case-insensitive `?`/`*` wildcard semantics. Preserve ordinary named `PRIVATE` behavior, hide only existing caller-scope variables and arrays for the ALL form, and restore both scalar and array state on explicit release or frame unwind. Do not treat `ALL`, `LIKE`, or `EXCEPT` as variable names. Focused GCC/Clang control-flow coverage passes; no visible text or machine contract changed.
- Shipped native class-array parity slice `#3975` materializes class-body `DIMENSION` / array-form `DECLARE` properties as arrays owned by each native object handle, rather than by the global array registry. Preserve per-instance isolation; member indexing, `ALEN()` and mutation helpers, and `TYPE('oObject.aArray')` must resolve the member array while ordinary global/frame arrays remain unchanged. Clear member-array storage when native-object construction is rejected or an object tree is released. Focused GCC/Clang runtime-surface coverage passes; no visible text or machine contract changed.
- Shipped record-scope parity slice `#4030` routes explicit `DELETE` / `RECALL` `ALL`, `NEXT <n>`, `RECORD <n>`, and `REST` scopes through the shared aggregate physical-range collector. Preserve bare-current-record behavior when no scope or predicate is present, the existing `FOR`/`WHILE`/`IN` semantics, cursor-position restoration during selection, filter visibility, and the existing `SET DELETED` distinction: DELETE honors it while RECALL must reach deleted records. Local DBF and synthetic SQL/result cursors use the same scope selection. Focused GCC/Clang table-mutation coverage passes; no visible text or machine contract changed.
- Shipped runtime cursor-alias slice `#4033` parses `CREATE CURSOR <name> NAME <alias> (<fields>)` into separate base-name and alias fields, then opens the temp-backed DBF under the explicit alias. Preserve ordinary `CREATE CURSOR <name> (<fields>)` behavior and ensure the `NAME` clause remains limited to the declaration prefix before the field list, so a field named `NAME` is not mistaken for an alias clause. The named alias must remain selected and usable by `SELECT` and normal table operations; never register the prior combined multi-word identifier. Focused GCC/Clang table-structure coverage passes; no visible text or machine contract changed.
- Shipped runtime package rematerialization slice `#4055` resets all materialization/finalization-derived state before a fresh package build: asset staging/copy/hash state, primary-output status, runtime-host hash, compiler/extension/writable digests, launcher inventory, and generated warnings. Preserve the internal planning-warning prefix so only genuine planning diagnostics survive a returned-plan rerun; restored or newly excluded optional assets must not retain stale warning, stage, copy, or digest state. Focused GCC/Clang runtime-pipeline coverage passes; no visible text or machine contract changed.
- Shipped package output-identity slice `#4054` rejects a public `OUTFILE` basename that case-insensitively collides with a package-root runtime host for native/library output, or with a fixed generated-launcher sidecar (`Copperfin.GeneratedLauncher.dll`, `.deps.json`, `.runtimeconfig.json`, `.pdb`) for selected .NET launcher output. Keep the validation shared by planning, materialization, finalization, and wrapper builds. The configured public apphost identity remains legal, including `Copperfin.GeneratedLauncher.exe`; do not reserve generated prefixes wholesale. Archive/FXP output and the other platform's runtime-host filename remain legal because they do not occupy that selected mode's fixed artifact identity. The four-catalog error keeps all manifest/debug fields and other machine contracts invariant. Focused GCC/Clang runtime-pipeline coverage passes.
- Shipped runtime-pipeline fixture-isolation slice `#4067` installs a process-unique, short fixture namespace through `TMPDIR`, `TEMP`, and `TMP` before any runtime-pipeline test runs. Existing readable fixture leaf names remain unchanged beneath that owned root; cleanup may remove only that root. Preserve the startup ordering in `test_runtime_pipeline.cpp`, including probe-mode initialization. The synchronized child-process regression must continue to prove two concurrently launched copies get distinct roots, retain separate markers, and remove only their own roots. Concurrent finalized GCC/Clang runtime-pipeline executions pass; run Windows/MSVC coverage through the normal validation workflow rather than restoring the old serial-only restriction.
- Shipped runtime string-function parity slice `#3924` makes only the VFP `LIKE()` function use literal-case wildcard matching. Preserve `?`/`*` behavior and its independence from `SET EXACT`; do not route `LIKE()` back through the case-insensitive command/file/SQL wildcard helper, which remains correct for those other surfaces. Focused GCC/Clang string/math coverage passes; no visible text or machine contract changed.
- Shipped runtime datetime evidence slice `#3883` adds focused `TTOC()` to `CTOT()` 12-hour round-trip coverage for midnight, morning, noon, and late PM values. The existing set-aware parser already handles AM/PM and converts the 12 AM/12 PM boundaries correctly; preserve that behavior along with 24-hour, date-order, and locale contracts. GCC/Clang date/time coverage passes; no visible text or machine contract changed.
- Shipped E3 character-field fidelity slice `#4062` makes direct FRX/LBX `C`-field edits use the core DBF right-trim-only storage rule. Preserve significant leading spaces, discard only padding at the right edge, reject fixed-width overflow without mutation, and keep undo restoration on the same serializer path. Focused GCC/Clang visual-asset editor coverage passes; no visible text, localization catalog, host JSON property, or machine contract changed.
- Shipped localization parser-hardening slice `#3908` rejects unescaped U+0000-U+001F bytes within catalog JSON strings and continues accepting legal `\b`, `\f`, `\n`, `\r`, `\t`, and Unicode escapes. Preserve fallback, catalog keys, placeholders, and Unicode/surrogate behavior; malformed catalog input receives the deterministic `Catalog.Json.InvalidControlCharacter` token. Focused GCC/Clang localization coverage passes; no user-facing text changed.
- Previous E3 fractional-geometry slice `#4061` centralizes report/label geometry conversion as finite bounded truncation toward zero. Layout placement and editor section movement must continue using that shared rule so fractional boundary objects follow the section that displays them through FRX/FRT and LBX/LBT edits/reopen. Focused GCC/Clang editor, layout, and localization coverage passes; no host JSON, visible text, or machine contract changed.
- Shipped E3 memo-settings slice `#4060` adds `TOPMARGIN` and `TAG` to the existing writable EXPR settings contract. Preserve their full memo lifecycle (absent/create, existing/update, clear, re-add) for FRX/FRT and LBX/LBT while retaining unrelated EXPR assignments. Focused GCC/Clang editor and localization coverage passes; no user-facing or machine contract changed.
- Shipped E3 fallback-settings slice `#4059` makes the shared report model and editor use the same `OBJTYPE == 1` settings-root classification. Preserve fallback root support even when `OBJCODE` is omitted: exposed memo-backed `GRIDV` now writes and reopens for FRX/FRT and LBX/LBT. Focused editor and report-layout coverage pass; no visible text, localization key, host JSON field, or machine identifier changed.
- Shipped native PRG Form lifecycle slice `#4069` dispatches `Load` for Form and FormSet native classes before child materialization and `Init`, preserving VFP9's FormSet `Load`, Form `Load`, contained-object `Init`, Form `Init`, FormSet `Init` ordering. `Load RETURN .F.` returns `.NULL.`, keeps caller execution going, removes the pending tree without `Destroy`, and skips `Init`; `THISFORM` and `THISFORMSET` retain their existing owner bindings. Preserve the iterative expression-invoked method-frame path; do not introduce recursive host evaluation or a larger process stack. Shipped VFP9 event-sequence help plus focused CREATEOBJECT/bare NEWOBJECT, FormSet ownership/order, cancellation, GCC/Clang localization/runtime-surface, and ASan/UBSan runtime-surface evidence support this contract. No visible text or localization/catalog change was needed, and existing schema keys/identities remain invariant. `#3869` remains the preceding `Init RETURN .F.` constructor contract.
- Shipped E3 text-encoding slice `#4058` decodes CP-marked FRX/LBX C/V and M values to UTF-8 at the shared designer boundary and encodes editor C/M writes back to the original table code page without substitution. Schema rewrites retain the source mark while preserving raw fields/memo payloads. Invalid UTF-8 and unrepresentable input fail explicitly; unmarked tables retain Copperfin's UTF-8 compatibility write mode. Preserve raw structural record/memo paths exactly, including opaque payloads and malformed-legacy sanitized display fallback. CP1252/CP1251 codec, FRX/FRT and LBX/LBT direct/memo round-trip, rejection, raw-byte, schema-rewrite, and Studio-host JSON tests pass under GCC and Clang; ASan/UBSan passes codec/editor paths, while its externally launched host child is not sanitizer evidence because the local runner injects an incompatible ASan runtime. Keep future user-facing diagnostics in all four catalogs and JSON field names, identifiers, enums, and machine fields invariant.
- Shipped Windows drive-relative package-containment slice `#4065` preserves VFP `X:tail` source semantics on Windows, freezes an existing source to its absolute plan-time identity before later reads or copies, treats that source form as unavailable on non-Windows hosts, and removes the drive designator from staged identity. Preserve the exact direct `package_root/content` root, structural and physical re-admission of every primary/companion write, relative plan spelling, unresolved-source decoy exclusion, and rejection of external roots, redirects, cross-device parents, hard links, ADS/device aliases, and unsafe optional-asset identities. Fresh materialization must reset copied/hash and extension/writable-payload state, and AST/IR must include only assets copied in that run. FRX/FRT same-drive and mapped different-drive LBX/LBT sentinels use short fixture-owned mapping/output segments so expanded physical paths do not conflate this oracle with legacy `MAX_PATH`; the source still forces Win32 full-path buffer growth. Focused GCC/Clang coverage, a production-symbol audit, complete 271/271 POSIX validation, and hosted Windows return to the known 24-target / 97-assertion baseline pass with no `#4065` failure; independent re-review is clean. `#4066` owns race-free pinned inner writes and `#4063` owns outer package-root rebinding.
- Shipped package-contract finalization slice `#4056` publishes `app.cfmanifest` and `app.cfdebug` through one recoverable pair transaction. Preserve direct fixed package-root leaves, pinned-directory/no-reparse/no-replace promotion, versioned hash-journal recovery, exact old-pair restoration, and the production/test-library separation for fault hooks. Every late cleanup-warning rewrite must use the same pair API. Focused GCC/Clang, production-symbol audit, complete 271-test POSIX validation, and independent reviews pass.
- Generated-launcher artifact-inventory slice `#4052` admits the configured public apphost, required stable `.dll` / `.deps.json` / `.runtimeconfig.json` sidecars, and optional `.pdb` only after direct physical containment and immutable-snapshot hashing. Preserve package-relative `launcher_artifact` provenance in both schema-3 manifests, keep it outside `extension_payload`, and do not imply that the post-launch runtime manifest protects pre-launch files; parent `#4041` owns the outer verifier. Supported build-host publication must continue disabling automatic MSBuild response files and inherited `Directory.Build.props` / `.targets` so ambient project settings cannot change the admitted publish set. Repeated materialization/finalization must clear stale launcher identities. Local focused GCC/Clang and complete 271-test Linux coverage pass; the real SDK/build-host oracle is Windows-only.
- Shipped project-relative PJX home-directory slice: `#4051` resolves relative `HOMEDIR` values from the PJX/document directory before directory validation and package/debug metadata generation. Preserve slash and backslash compatibility, unrelated-process-CWD isolation, and PJX-directory fallback when the relative target is missing. Absolute paths, current-drive-rooted paths, Windows drive-designator forms, and file-valued homes retain their prior behavior. The regression covers plan and materialized `app.cfdebug` working-directory/source-root identity and ordering; focused plus adjacent package/runtime/localization checks pass, and independent re-review is clean, without visible or machine-contract changes.
- Shipped portable `JUSTDRIVE()` slice: `#4050` returns complete ordinary UNC server/share roots when no descendant follows, for backslash and forward-slash spellings with or without trailing separators. Preserve the nonempty-server guard: three-leading-separator inputs remain malformed and return empty. Extended/device namespaces, drive letters, descendant UNC, POSIX, relative, and empty behavior remain unchanged. Focused and adjacent runtime targets pass 3/3; final independent review is clean after the malformed-input regression was added to coverage.
- Shipped runtime-pipeline locale-fixture slice: `#4040` gives `test_runtime_pipeline` one authoritative test-owned catalog root derived from `COPPERFIN_TEST_SOURCE_DIR/resources/locales`, uses it for direct catalog loads, and installs it through a suite-scoped `COPPERFIN_LOCALE_DIR` fixture so production-facing package diagnostics no longer depend on caller CWD or build-tree ancestry. Nested override and in-process refresh scopes still restore correctly. Fresh external GCC and Clang build trees pass the target with both locale variables absent, an invalid ambient locale-root run also passes, combined hosted runs `29320607972` / `29320607375` pass the full Linux/macOS inventories, and independent review is clean. Do not change production `resolve_catalog_root()` for this test-fixture concern.
- Shipped runtime-host logical startup-identity slice: `#4047` keeps canonical paths confined to physical containment, identity, digest verification, and reads while `startup.source`, PRG breakpoint inventories/locations/frames, and xAsset startup summaries retain the admitted package/deployment spelling. Only already verified immutable snapshots may be indexed under canonical, deployment-logical, or admitted-startup namespace aliases. Rootless absolute manifest spelling is retained only after strict admission; generated/package-root paths still rebind. A deployment symlink/junction may be the manifest-root trust boundary for missing audit output, but redirects below it remain rejected. Local 271/271 plus focused and adjacent gates pass; hosted focused run `29317621962` is green on Linux/macOS/Windows and full runs `29318838335` / `29318838448` are green on Linux/macOS. Preserve localized human prose and invariant debug/manifest/status/exit contracts; `#4048` separately owns the Windows broad fixture cascade.
- Shipped platform-owned native-validation slice: `#4037` replaces the aggregate OS matrix with independently triggerable Linux, macOS, and Windows workflows while preserving the exact required-check identities `Linux GCC`, `macOS Clang`, and `Windows MSVC`. Platform wrappers own triggers, runner, timeout, event-separated concurrency, permissions, and immutable checkout; `.github/actions/native-validation/action.yml` owns .NET setup, configure, bounded `--parallel 2` compilation, and full unfiltered CTest on the same runner. `Native Release Readiness` executes all three platform contracts and gates on all three without transporting CMake build trees. Preserve the focused Windows environment, VSIX, installer, deep-validation, and DECLARE workflows. Local configure/build and CTest pass 271/271 with one explicit non-Windows launcher skip; hosted commit `f0c28ac6` records Linux run `29311705379` green, macOS run `29311705374` completing with only `test_runtime_host_debug_output` red under new `#4047`, and Windows run `29311705356` completing all 272 tests with 24 product targets red under E3 `#24`, existing `#3936`/`#3937`, and new `#4048`/`#4049` ownership. Supply-chain run `29311705371` is green, workflow/contract validation passes, and independent reviews are clean. `#4042`-`#4046` retain separate action-pinning, dispatch-boundary, installer-artifact, focused-path, and SBOM ownership.
- Shipped generated .NET launcher output-name slice: `#4038` keeps `Copperfin.GeneratedLauncher` as the internal SDK assembly and sidecar identity, then makes the configured PJX `OUTFILE` authoritative by renaming only the published apphost to that exact legal filename. Do not feed project titles or configured output names into `AssemblyName`, and do not restore title-derived or legacy alternate executable fallbacks. Windows `dotnet publish` process startup uses `_wspawnv` with native-width arguments, a `dotnet.exe` basename `argv[0]`, and CRT-correct quoting so Program Files, spaced paths, embedded quotes, and trailing backslashes survive. The process fixture proves a metacharacter-heavy custom output runs through the actual build host, leaves no alternate executable, and preserves schema-3 manifests plus invariant status fields. Local GCC/Clang checks and the apphost-rename oracle pass; focused Windows run `29308629423` is green and independent reviews are clean. `#4041` owns the separate pre-execution trust contract for generated DLL/deps/runtimeconfig sidecars.
- Shipped generated .NET launcher process slice: `#4036` plus folded bug `#4039` now publish and execute a real Windows launcher against distinct release/debug manifests. Preserve `ProcessStartInfo.ArgumentList` forwarding, canonical `--debug` / `--locale` normalization for slash aliases, debug-manifest selection only when debug is requested and `app.cfdebug` exists, release fallback otherwise, and package-root working directory. Existing-but-unstartable runtime hosts must stay inside the localized launcher boundary with exit `5`, and generated console diagnostics must remain UTF-8 so pseudo-localized text survives redirected output. Keep `Program.cs` an explicit compile item because the generated project is nested beneath its parent publish directory. The process fixture covers spaces, embedded quotes, trailing separators, `pt-BR`, `qps-ploc`, and deterministic sharing-violation startup failure; unsupported platforms return CTest skip `77`. Local GCC/Clang contract coverage passes and focused Windows run `29305780015` is green; independent acceptance review is clean. Manifest schemas and invariant machine fields are unchanged.
- Shipped rootless runtime-manifest alias compatibility slice: `#4035` permits only native absolute `startup_source` to cross an equivalent physical package-root alias, such as macOS `/var` versus `/private/var` or Windows short versus long paths. Keep the exception in `resolve_startup_source`; do not broaden generic `bind_packaged_path`, rootless `content_root`, or rootless `working_directory`. The equivalent ancestor must be the package root itself, and every component beneath it remains subject to direct symlink/reparse, device-crossing, final canonical-containment, and identity checks. Preserve external/escape/basename rejection and generated manifest/debug schemas. The local GCC/Clang gates and hosted run `29299815927` pass binding, audit containment, and security controls on Linux/macOS/Windows; both independent reviews are clean. No visible or machine contract changed.
- Shipped database-lifecycle platform-casing regression slice: `#3880` makes component resolution return actual DBC/DCT/DCX directory-entry spelling on case-insensitive storage instead of accepting the probe spelling after `is_regular_file`. Preserve precedence and failure behavior: exact full filename, then existing exact-stem match, then one unique case-folded match; ambiguity remains rejected, while an already verified exact regular path is retained only if parent enumeration is unavailable. This keeps omitted `.dbc`, `DBC()` identity, exact verified companion bytes, immutable package admission, and missing-path diagnostics stable across APFS/NTFS without collapsing distinct case-sensitive POSIX paths. Tests must probe the actual fixture directory before creating case-variant siblings; POSIX does not imply case-sensitive storage. Exact extension-case precedence, non-listable-parent DBC/DCT/DCX fallback, verified-key collision, 2,000 repeated opens, sidecar behavior, and packaged runtime-host digest coverage pass under GCC/Clang. Hosted run `29296539629` is fully green on Linux and passes `test_prg_engine_database_lifecycle` on macOS and Windows; independent reviews are clean. No visible or machine contract changed.
- Shipped out-of-tree locale-helper hosted-regression correction: `#3997` compares filesystem identity only for invalid/absent override fallback roots, accepting canonical aliases such as macOS `/var` versus `/private/var` while retaining exact valid explicit-root and restoration contracts. Preserve exact caller `COPPERFIN_LOCALE` restoration across invalid and missing scopes and the POSIX unreadable-regular-catalog openability fixture. The symlinked-`TMPDIR` oracle and GCC/Clang focused plus out-of-tree adjacent matrices pass. Hosted native run `29293651007` records `test_platform_environment` passing on macOS in 0.19 seconds; focused Windows environment run `29293651050` is fully green. Production locale behavior, visible text, catalogs, and machine contracts are unchanged.
- Shipped runtime-host implicit-manifest regression correction: `#3669` confirms the host already selects adjacent `app.cfdebug` only for debug requests and adjacent `app.cfmanifest` for ordinary implicit launches. Preserve the production-like fixture topology: copied host and manifests at package root, release source under `content`, external debug source under a separate source root, serialized package/content roots, and distinct working directories. Canonical running-image aliases (`/var` versus `/private/var`, or Windows short versus long paths) made the old rootless absolute release source look external; a local alias oracle reproduced that failure with no `app.cfdebug` present. The test now corrupts `app.cfdebug` deliberately and directly proves ordinary startup still reports `ImplicitReleaseManifest` and `release_main.prg`. Local GCC focused coverage and the exact macOS/Windows assertions in hosted run `29290669521` pass; unrelated hosted failures remain. Follow-ups `#4035` and `#4036` have closed legacy rootless-manifest alias compatibility and generated .NET launcher execution respectively. Product code, localization, and machine contracts are unchanged.
- Shipped serialized DBF field-name collision slice: `#4028` validates the same trimmed first 11 bytes the raw writer serializes, with deterministic ASCII case folding, before any direct create or shared schema rewrite can emit duplicate physical identities. Preserve pre-write rejection, localized `Vfp.DbfTable.Error.TargetFieldExists`, original DBF/FPT bytes, and staged-artifact cleanup across direct create/add and PRG `CREATE TABLE`. Do not misstate this as exact VFP9 long-name parity: shipped help and a mounted oracle prove free-table names are limited to 10 bytes, while DBC tables retain up to 128-character logical names and assign distinct shortened physical names. `#4034` owns that separate free-table limit/DBC mapping work. Local GCC/Clang adjacent suites pass; hosted run `29286767564` is fully green on Linux and passes the affected macOS/Windows DBF, asset, locale, and table-structure targets. The remaining hosted failures are the already reopened `#3669`, `#3997`, `#3880`, and existing Windows Studio/workspace/runtime clusters. Final review is clean.
- Completed Windows validation performance slice `#4083` wraps hosted Windows configure, native compile/CTest, and managed compile/test phases with `scripts/measure-windows-validation.ps1`. Preserve schema-1 JSON fields, GitHub summary rows, full test inventories, and failure-path metric finalization/upload. Three-job native compilation was safe but provided only 1.39% median and 0.22% mean improvement over two jobs, so compilation remains capped at two. Guarded macro-independent and exact-variant CMake object targets reduce the GCC graph from 706 to 553 commands and hosted native compile by 8.01%; remaining cold-build time is dominated by unique large translation units and serialized links. Compiler-cache child `#4086` demonstrated an 88.51% warm improvement with safe invalidation probes but remains manual and non-authoritative because GitHub Actions cache contents are unsigned/unverified. CTest isolation child `#4087` classifies the complete inventory and adopts bounded Windows `--parallel 2` after three 282/282 hosted trials reduced mean CTest time by 25.42%. Keep unique macro variants separate, authoritative lanes cache-free, Windows build/test concurrency explicitly capped at two, POSIX builds capped at two, and POSIX CTest serial pending independent evidence.
- Shipped native-validation resource-hardening slice: `#4029` caps broad native, Windows deep, and focused DECLARE CMake builds at two concurrent jobs. Preserve the local override contracts: POSIX `COPPERFIN_BUILD_JOBS` must be a positive integer and Windows uses `-BuildJobs`; never restore bare `--parallel` in those workflows or drop targets/tests to reduce load. The contract test also locks README guidance and the broad matrix's 120-minute ceiling. Hosted run `29283027830` completes every build without OOM or shutdown: Linux is fully green in 19m29s, while macOS/Windows finish CTest and expose separate product failures. Focused run `29282894567` passes Win32/x64, and deep run `29282936159` completes the capped all-target build before the existing Windows CTest baseline stops later stages. Local syntax/configuration checks and independent review are clean.
- Shipped deferred-filter evaluation slice: `#4026` makes `SET FILTER` installation preserve the current physical record, immediate field access, status functions, and work-area selection until navigation/search evaluates the filter, with local DBF and synthetic SQL parity. Preserve the VFP9 empty-result boundary contract on nonempty cursors: `GO TOP` yields `RECNO() = RECCOUNT() + 1`, `BOF() = .F.`, `EOF() = .T.`, while `GO BOTTOM` retains that record number and sets both boundary flags; field reads return schema-typed blanks. Local field descriptors remain cached and refreshed on open, transaction replay, and `ALTER TABLE`, so boundary reads must not parse DBF contents. Exact invariant `runtime.filter` event details, targeted `IN` isolation, GCC 10/10, Clang 7/7, Windows run `29281173287`, and final reviews are green. Keep this path heap-backed and stack-frugal.
- Shipped session-deleted indexed-search slice: `#4027` makes command `SEEK`, `SEEK()`, and `INDEXSEEK(.F./.T.)` honor session-scoped `SET DELETED` for local DBF and synthetic SQL cursors. Preserve the shared candidate order: an order's independent `FOR` expression, then the session-deleted gate, then active `SET FILTER` evaluation. `SET DELETED OFF` must continue exposing deleted candidates unless the order itself excludes them. Preserve exact and explicit `SET EXACT OFF` prefix matching, deleted-first/live-later duplicate skipping, `SET NEAR ON/OFF`, ascending/descending orders, status functions, and both pointer-preserving and move-requested `INDEXSEEK()` misses; shipped VFP9 help says even `INDEXSEEK(..., .T.)` moves only on a match. GCC adjacent coverage passes 8/8, focused Clang coverage passes 4/4, hosted Windows run `29277891233` passes Win32/x64, and final reviews are clean.
- Shipped active-filter indexed-search slice: `#4023` makes command `SEEK`, `SEEK()`, and `INDEXSEEK()` honor cursor-scoped `SET FILTER` visibility for local DBF and synthetic SQL cursors. Preserve lazy lower-bound filter evaluation, hidden-duplicate skipping, exact/prefix and `SET NEAR` behavior, `INDEXSEEK(.F.)` pointer preservation, and targeted non-selected work-area expression context. Temporary explicit, implicit-first, and Rushmore search orders must remain invisible to filter `ORDER()`/`TAG()` and nested bare `SEEK()` expressions; same-cursor nested seeks must remain stack-frugal and must not erase the outer candidate after a miss. Filter faults must restore cursor/order and `SET FIELDS` state. Rushmore post-seek verification must retain `SET DELETED`, `LOCATE FOR`, and `WHILE` checks while skipping the already-evaluated active filter. Local GCC adjacent coverage passes 6/6, focused Clang evidence passes, hosted Windows run `29275599284` passes Win32/x64, and all review findings are addressed. Keep the active-filter behavior composed with the shipped session-deleted gate from `#4027` and deferred installation from `#4026`.
- Shipped bare-filter clearing slice: `#4024` sends `SET FILTER` option text through the shared terminal-`TO` parser, so bare `SET FILTER TO` clears the selected work-area filter instead of storing `TO`. Preserve bare `SET FILTER OFF`, `SET FILTER ... IN`, macro expansion, scoped visibility, and invariant `runtime.filter: OFF` behavior. The local-table regression exercises both clear forms and requires two `OFF` events while retaining GO/SKIP/LOCATE behavior; adjacent SQL/control-flow suites pass under GCC and Clang, hosted Windows run `29272615250` passes Win32/x64, and final re-review is clean. Microsoft-shipped VFP source contains this bare form in converter, transport, browser, and wizard code, so do not regress it to trailing-space-only parsing.
- Shipped build/inspect-host PATH-launched license-discovery slice: `#4025` passes each platform-resolved running image into default `license.cflicense` lookup, completing the deployed-image rule already used by the runtime host. Never restore raw basename `argv[0]`, because it rebinds license state and generated package metadata to caller CWD. Preserve explicit override before `COPPERFIN_LICENSE_PATH` before deployed-sibling precedence, signature validation, localized diagnostics, and invariant state/source/package fields. The dedicated process regression launches copied build and inspect hosts by basename through PATH/PATHEXT with conflicting deployed/caller licenses and checks physical deployed-file identity; the package regression proves a caller license cannot contaminate `app.cfdebug`. Local GCC/Clang tests, hosted executable-path run `29220995968`, Windows run `29220995976`, and final review are clean after explicit environment isolation.
- Shipped PATH-launched build-host sibling slice: `#4017` resolves the default runtime host beside the platform-resolved running build-host image, never a raw basename rebased onto caller CWD. Preserve precedence exactly as explicit `--runtime-host`, then `COPPERFIN_RUNTIME_HOST_PATH`, then deployed sibling fallback. The process package smoke launches by basename through PATH/PATHEXT from an unrelated CWD containing a decoy and requires exact staged bytes from the deployed sibling; adjacent cases pin exact environment and explicit override bytes. Keep platform-correct runtime-host names, digest/materialization behavior, localized diagnostics, and invariant package fields unchanged. Local GCC/Clang tests, hosted run `29219932391`, Windows run `29219932381`, and final review are clean after full precedence and byte-identity coverage.
- Shipped PATH-launched runtime license-discovery slice: `#4016` passes the platform-resolved running image into `load_license_status(...)`; never restore raw basename `argv[0]` for default sibling discovery, because that rebinds `license.cflicense` to caller CWD. Preserve license path precedence as explicit override, then `COPPERFIN_LICENSE_PATH`, then the resolved executable sibling. The process regression launches by basename through PATH/PATHEXT from an unrelated CWD with conflicting deployed/caller license files and requires invariant `status`/`state` plus physical equivalence to the deployed source path, including equivalent Windows long/8.3 spellings. Existing license unit coverage retains signature, diagnostics, free-state, environment, and explicit-override contracts. Local GCC/Clang tests, hosted run `29219932391`, Windows run `29219932381`, and final independent review are clean; visible text and machine fields are unchanged.
- Shipped runtime audit-containment slice: `#4015` treats an explicit manifest `audit_log_path` as an exact package-relative output before any policy-denial or other append. Reject external absolute paths, relative escapes, basename fallback, redirected/reparse components, cross-device/volume traversal, non-regular leaves, and leaves with multiple hard links; preserve localized containment prose, invariant status/event fields, and exit `8`, and never silently fall back after an invalid explicit path. Missing package-local directories/leaves remain admissible. Keep Windows exact-root containment normalized to the same `.` relative identity used by POSIX so a missing nested output can anchor at the package root. POSIX appends stay descriptor-anchored through `openat`, no-follow checks, directory locking, same-directory atomic replacement, and directory sync. Windows appends retain no-delete-sharing component handles, reject reparse points, serialize by canonical leaf with a named mutex, and use write-through same-directory replacement. Both implementations validate link identity and publish the complete verified chain atomically instead of appending in place. Keep the Windows runtime host on the established 16 MiB product stack reserve while preserving heap-backed, stack-frugal PRG execution. Final focused run `29220356029` passes binding, process containment, and concurrent-chain targets on Linux/macOS/Windows. Deep Windows run `29218886745` compiled all native targets and passed the dedicated target in 0.37 seconds; its remaining red target is the separately known broad runtime-host harness fast-fail. Final review found no blockers. Keep separately permitted external source/debug provenance in `app.cfdebug` unchanged.
- Shipped VSIX managed-process timeout slice: `#4014` terminates timed-out process trees before bounded output cancellation. Modern .NET uses `Process.Kill(true)`; `net472` Windows resolves `taskkill.exe /T /F` only from `Environment.SpecialFolder.System`; legacy POSIX snapshots `pid`/`ppid` relationships and kills deepest-first. Preserve the one-second root/helper/output waits and two-second POSIX discovery budget, which cap the legacy worst case at six seconds after timeout. Do not reintroduce unbounded `WaitForExit()` or pipe-event waits on timed invocations, multi-handle `WaitAll` on the Visual Studio STA thread, or relabel a successful root exit as timed out merely because a detached descendant retains output handles. Cross-platform and dedicated `net472` STA regressions cover child/grandchild cleanup, pre-timeout output, successful-root held pipes, large output, and elapsed bounds. Local managed/CTest gates and Windows VSIX run `29214290563` are green; final independent review is clean.
- Shipped executable-path discovery slice: `#4013` resolves the running image through `GetModuleFileNameW`, `/proc/self/exe`, or `_NSGetExecutablePath`, with one shared invocation fallback for localization and unsupported hosts. Preserve native-width Windows PATH/PATHEXT handling, exact untrimmed POSIX PATH entries, POSIX regular-file plus execute-permission checks, and empty-entry current-directory semantics. Runtime-host implicit `app.cfdebug`/`app.cfmanifest` selection and localization must consume the same OS-resolved image root; never rebase basename `argv[0]` onto caller CWD. Explicit manifest/debug arguments, debug preference, package-relative fields, localized prose, invariant status fields, and exit codes remain unchanged. Dedicated coverage proves extensionless Unicode Windows PATH fallback, non-executable/trailing-space POSIX fallback, debug and release manifest identities, deployed locale selection against a conflicting caller locale, and exit/status invariants. Focused run `29213179244` is green on GCC/Clang and Windows run `29213179261` is green; final independent review found no code defects. Keep related license and build-host sibling discovery work separate under `#4016`/`#4017`.
- Shipped POSIX environment synchronization slice: `#4010` serializes shared native `getenv`/`setenv`/`unsetenv` calls through one process-wide mutex and copies raw `getenv` values before releasing it. Preserve this as a helper-scoped boundary: unavoidable direct libc environment access must externally serialize with Copperfin helper calls. Windows keeps its existing duplicate-before-copy wide/narrow CRT behavior; POSIX keeps present-empty distinct from missing, while Windows empty assignment continues removing the variable. Because the helper is header-only, all native targets inherit `Threads::Threads`. The deterministic two-writer/four-reader long/empty/missing stress test passes under GCC, Clang, GCC TSAN, and focused Windows run `29210877088`; representative licensing/platform targets and runtime `GETENV()`/`PUTENV()` tests pass under both POSIX compilers. Final independent review is clean. Parallel PATH-launch mapping queued runtime-host license lookup `#4016` and build-host sibling-runtime lookup `#4017`.
- Shipped VSIX selected-item routing slice: `#4008` keeps Tools-menu Open In Copperfin Studio on invariant command ID `0x0100` with active-document-first selection and gives the Solution Explorer item-node command invariant ID `0x0101` with selected-item-first selection. Do not recombine those commands: item-node resolution checks project-item files in order, then the containing project path, then the active document; Tools retains the prior active-first fallback. Preserve the shared localized caption and invariant canonical names. The item-node group keeps Open/Build/Run/Debug at priorities `0x0100`/`0x0110`/`0x0120`/`0x0130`. Shared focused coverage spans PJX/PRG/SCX/VCX/FRX/LBX/MNX, missing/later selections, project fallback, and no-target behavior; `Copperfin.StudioTargetSelectionTests` is an explicit local and Windows gate in the VSIX workflow, deep validation, and `validate-windows.ps1`. Local focused/full managed suites and CTest pass, Windows run `29209907030` is green, and final independent review found no issues. The full managed Windows harness separately reproduces queued process-tree timeout defect `#4014`; do not weaken or skip that regression when fixing it.
- Shipped Windows locale-root portability slice: `#4005` routes the filesystem-valued `COPPERFIN_LOCALE_DIR` through native path helpers, using wide CRT environment access on Windows and preserving existing POSIX byte paths. Keep all production catalog-root reads and runtime-host bootstrap writes path-typed; never convert a Windows locale root through `.string()` or the generic narrow environment API. Shared test environment scopes special-case this filesystem variable through UTF-8/path conversion so legacy clear/restore fixtures remain exact. Coverage uses a real Cyrillic/CJK catalog directory, proves present and missing restoration, loads `en-US/strings.json`, and launches the runtime host against the Unicode root. Focused GCC/Clang matrices, final re-review, and focused Windows environment-path run `29208853253` are clean. Preserve `.github/workflows/windows-environment-validation.yml` as the authoritative wide-branch gate instead of depending on the unrelated broad runtime-host suite.
- Shipped VSIX command-caption localization slice: `#4012` gives Open in Studio, Build, Run, and Debug dedicated `VSIX.Command.*` keys shared by loaded managed commands, all four native JSON catalogs, and one multilingual VSCT. Preserve command GUIDs, numeric IDs, invariant canonical names, routing, and machine-readable fields. The VSSDK build emits neutral command resources plus parent-culture `es`/`pt` and `qps-ploc` satellites through explicit culture and manifest-resource identities. Source-level catalog/VSCT/MSBuild assertions are necessary but not sufficient: both Windows workflows and `scripts/validate-windows.ps1` must continue running `scripts/test-vsix-command-localization.ps1` against the built VSIX and requiring nonempty `Menus.ctmenu` plus the culture-specific `CTMENU` resource in every satellite. Local managed/localization gates, packaged Windows artifact inspection, and final independent review are clean.
- Shipped E3 final selector test-contract slice: `#4011` aligns record-index and stable-ID FRX/LBX detail-section deletion with the retained-live-object contract. Preserve exact live bounds `100,2600` through `150,2800` from the three retained live fields and deleted recovery bounds `0,2000` through `150,7000`; keep top-level live/deleted section counts anchored as one adjacent serialized pair. The focused selectors explicitly pin generated/stable section IDs, ID-field provenance, three live and zero deleted section objects, placed/unplaced totals, DBF deleted state, selection kind, containment, and FRX/LBX parity. Review findings were addressed; post-review 12-test GCC/Clang matrices and the complete original 44-target `#3874` inventory are green, with the tightened targets rerun under both compilers. Product behavior and machine contracts are unchanged. The audited `#3874` stale-expectation cluster is complete. Parallel localization review queued hard-coded VSIX command captions as `#4012`.
- Shipped E3 orphan-object selection test-contract slice: `#4009` aligns stable-ID FRX/LBX live/deleted object selection inside deleted sections with the retained-live-object contract. Preserve page-header live bounds `0,100` through `5200,8100` and deleted bounds `0,0` through `2700,2900`; preserve live and deleted detail-object selection live bounds `0,0` through `5200,8100` and deleted bounds `0,2000` through `5200,7000`. The live field at record `3` keeps detail live right/width at `5200` even when the selected sibling is deleted. Object/containing-section selection metadata, deleted state, counts, placement, containment, section-relative geometry, field/memo provenance, and FRX/LBX parity remain covered, with top-level section counts anchored as an adjacent pair. Twelve-test GCC/Clang matrices and final independent review are clean; product behavior and machine contracts are unchanged. A fresh audit proves only the record/stable section-delete selector targets remain under `#3874`. Parallel bug hunting queued POSIX environment synchronization defect `#4010` without changing this slice.
- Shipped E3 selected-section test-contract slice: `#4007` aligns stable-ID and record-selected FRX/LBX deleted detail/page-header selection with the retained-live-object contract. Preserve deleted-detail live bounds `100,2600` through `150,2800` and recovery bounds `0,2000` through `150,7000`; preserve deleted-page-header live bounds `0,100` through `5200,8100` and recovery bounds `0,0` through `2700,2900`. Selection availability/kind/id/provenance, null object/settings selections, containment, band summaries, and live-section controls remain covered. Keep top-level live/deleted section counts anchored as one adjacent serialized pair so nested `selectedReportSection.sectionCount` cannot satisfy them. Twelve-test GCC/Clang matrices plus post-review focused reruns and final re-review are clean; product behavior and machine contracts are unchanged. A fresh audit proves four `#3874` targets remain: two orphan-object selectors and two section-delete selectors. Parallel bug hunting queued VSIX selected-item precedence defect `#4008` without changing this slice.
- Shipped E3 deleted-state batch test-contract slice: `#4006` aligns stable-ID FRX/LBX settings+section and mixed settings+section+field delete batches with the shipped deleted-root summary and retained-live-object contracts. Preserve exact settings+section live bounds `0,100` through `5200,8100` and deleted bounds `0,0` through `2700,2900`; mixed field deletion narrows live bounds to `0,100` through `2700,8100` and expands deleted bounds to `0,0` through `5200,3050`. With `settingCount: 0`, keep effective deleted-root page setup at orientation `0`, paper size `1`, margins `10/20`, and grids `4/8`. DBF mutation/restore/rollback, counts, placement, selection, metadata order, and FRX/LBX parity remain covered. Twelve-test GCC/Clang matrices plus final independent review are clean; product behavior and machine contracts are unchanged. A fresh audit proves eight `#3874` targets remain: six selected-section/orphan targets and two section-delete selector targets.
- Shipped E3 detail-header/footer delete-preview test-contract slice: `#4004` aligns stable-ID FRX/LBX section delete/restore expectations with the shipped `#3840` retained-live-object contract. Preserve exact header-delete live bounds `0,50` through `1040,550` plus recovery bounds `0,0` through `800,300`, and footer-delete live bounds `0,0` through `1040,460` plus recovery bounds `0,300` through `1040,550`. DBF deleted state, selected-section identity, section/object containment, counts, undo metadata, restore behavior, and FRX/LBX parity remain covered. Twelve-test GCC/Clang matrices and final independent review are clean; product behavior and machine contracts are unchanged. A fresh audit proves 11 `#3874` targets remain: three deleted-state batch targets, six selected-section/orphan targets, and two section-delete selector targets. The batch cluster also contains residual deleted-root page-setup expectations in `test_studio_host_report_deleted_states` and `test_studio_host_settings_section_deleted_states`. Parallel bug hunting queued Windows Unicode locale-root defect `#4005` without changing this slice.
- Shipped E3 deleted-section geometry test-contract slice: `#4003` aligns record-index and stable-ID FRX/LBX deleted-section `HEIGHT`/`VPOS` update and clear expectations with the shipped `#3840` retained-live-object recovery-preview contract. Preserve live preview bounds at `100,2600` through `150,2800` for height mutations, translated to `100,3100` through `150,3300` for top updates and `100,600` through `150,800` for top clears. Exact deleted recovery bounds, section geometry/deleted state, selected-section identity, counts, object metadata, property persistence, and FRX/LBX parity remain covered. Identical 14-test GCC/Clang matrices pass and final independent review is clean; product behavior and machine contracts are unchanged. Twelve audited `#3874` failures remain across detail header/footer preview, deleted-state batches, selected-section/orphan previews, and section-delete selectors.
- Shipped E3 settings-delete test-contract slice: `#4002` extends deleted-root effective page-summary evidence to stable-ID and record-index FRX/LBX root-settings deletion. Preserve the exact supported summary (`orientation=0`, `paper=1`, margins `10/20`, grids `4/8`) and the unsupported/comment-line EXPR summary (`orientation=0`, direct top margin `10`, color `1`; paper/bottom/grids unavailable at zero), with every other page, column, and sort field unavailable/inert. Retain DBF deleted state, raw EXPR text, document/label identity, live/deleted count separation, complete top-level/selected provenance and values, report-selection availability/kind, null section/object selections, preview/section/object metadata, and FRX/LBX parity. Ten-test GCC/Clang matrices pass; product behavior and machine contracts are unchanged. All 13 remaining audited `#3874` failures are in the deleted-section preview-bounds family.
- Shipped E3 sort-setting reopen test-contract slice: `#4001` extends deleted-root effective page-summary evidence to stable-ID and record-index FRX/LBX TAG update/clear reopen paths, including unsupported/comment-line EXPR fixtures. Preserve page setup, orientation availability/code `1`, and paper-size availability/code `9` through one shared assertion path per selector while retaining live/deleted count separation, TAG field provenance, selected-settings identity, raw EXPR text, and FRX/LBX parity. Focused and eight-test adjacent GCC/Clang matrices pass; product behavior and machine contracts are unchanged. Fifteen failures remain from the audited `#3874` inventory, split between settings-delete summaries and deleted-section preview bounds.
- Shipped E3 settings-memo selector test-contract slice: `#4000` extends deleted-root effective page-summary evidence to stable-ID and record-index FRX/LBX EXPR update/clear paths. Preserve exact orientation, paper-size, top/bottom-margin, and vertical/horizontal-grid summaries for deleted roots while keeping `settingCount: 0`, deleted counts/provenance, selected-settings identity, section/object metadata, preview bounds, and unsupported EXPR lines. Clearing EXPR leaves the deleted direct `TOPMARGIN=10` field effective and resets every memo-derived availability/value. A fresh run found 19 failures remaining from the original 44-target `#3874` inventory before this slice; these two settings-memo targets are now green. Identical 18-test GCC/Clang matrices pass; product behavior and machine contracts are unchanged.
- Shipped E3 record-selected column-setting test-contract slice: `#3999` extends the deleted-root effective-summary evidence to record-index update/clear paths for FRX/LBX `COLS`, `COLWIDTH`, `COLSPACING`, and complete EXPR-backed column setup. Preserve effective aggregate and field availability/values together with `settingCount: 0`, deleted counts/provenance, and selected-settings identity. Clearing one direct field retains effective column setup from deleted siblings; clearing the complete EXPR bundle removes all column availability and resets every value to zero. The identical 10-test focused/adjacent GCC/Clang matrix passes; product behavior and machine contracts are unchanged.
- Shipped E3 record-selected page-setting test-contract slice: `#3998` extends the deleted-root effective-summary evidence from stable selectors to record-index update/clear/undo paths for FRX/LBX margins, grids, orientation, paper size, and unsupported-line-preserving EXPR edits. Preserve effective summary availability/values together with `settingCount: 0`, deleted counts/provenance, selected-settings identity, DBF deleted flags, and unsupported EXPR content. The root-delete diagnostics runner now enforces the same distinction. Identical 17-test GCC/Clang matrices pass; product behavior and machine contracts are unchanged.
- Shipped out-of-tree locale-helper slice: `#3997` requires `ScopedDefaultLocaleCatalogEnvironment` to preserve an existing `COPPERFIN_LOCALE_DIR` only when it contains an openable regular `en-US/strings.json` catalog. Missing, file-valued, incomplete, or inaccessible overrides fall back to a usable ancestor catalog root; caller locale/root present-or-missing state must restore exactly. Keep explicit roots distinct from discoverable fallback roots in regression fixtures so preservation cannot pass accidentally. Independent out-of-tree GCC/Clang environment, report-layout, and mounted-sample CTests pass with the repository root supplied explicitly.
- Shipped Studio-host test dependency slice: `#3996` makes all 209 native Studio-host process-test executables depend on the out-of-process `copperfin_studio_host` they launch. Preserve the single end-of-file CMake directory-target rule so new consistently named process tests inherit the edge automatically; the direct shared-model `test_studio_host` and self-only `test_studio_host_shell_command` targets remain explicitly excluded. `$<TARGET_FILE:...>` in `add_test(...)` supplies a path but not build order. GCC/Clang touch-and-build proofs relink the host from a narrow mounted-sample target. The out-of-tree locale helper defect found during Clang CTest validation is separate under `#3997`.
- Shipped E3 deleted-root summary test-contract slice: `#3994` keeps record lifecycle separate from effective report-layout summaries. If `settings` is empty, `deleted_settings` supplies page/column summary availability and values; this must not create live settings, change deleted counts/provenance, or alter selected deleted-root identity. If any live root exists, it is wholly authoritative and deleted settings do not backfill missing fields. Stable update/clear coverage spans FRX/LBX margins, grids, orientation, paper size, column count/width/spacing/full setup, and selected settings. Preserve the intended `#3840` recovery-preview contract too: live objects retained inside a deleted section contribute to both live preview bounds and that deleted section's recovery preview; the older contrary `#3684` synthetic expectation is stale. With the host explicitly rebuilt, all six mounted VFP9 sample/grouping/selected-section gates pass, including `cust.lbx` at `39374 x 11353`.
- Shipped native sidecar-consistency slice: `#3992` owns one exact-first, unique ASCII case-fold resolver below Studio for PJX/PJT, SCX/SCT, VCX/VCT, FRX/FRT, LBX/LBT, MNX/MNT, DBC/DCT, and memo-bearing DBF/FPT paths. Ordinary Studio opens, DBF parse/write, visual-editor recovery/mutation, asset inspection/database export, and runtime-host package/debug snapshot boundaries must reject ambiguity before relevant reads, recovery, or writes through the four-catalog `Vfp.Sidecar.Error.AmbiguousPath` diagnostic; never select one ambiguous sibling. Preserve the actual directory-entry spelling for one folded match and thread that selected path through inspection, preview parsing, document state, and the existing direct `sidecarPath` host JSON value. Exact filenames win. Ordinary non-memo DBFs must remain independent of stale FPT siblings. Keep the package resolver separate because it additionally owns component uniqueness, containment, and staged-path identity; the full runtime-pipeline regression remains the `#3905` guard. Six-family byte-preservation plus exact/unique/non-memo coverage and sequential GCC/Clang localization/Studio/DBF/inspector/editor/package matrices pass.
- Shipped asset-inspector warning cleanup slice: `#3988` removes the unused local `read_le_u16(...)` and explicitly initializes every `DatabaseExportResult` member on all missing/read/header/success returns. Preserve empty `json` on errors, empty `error` on success, existing localized messages, generated JSON, and public result layout. Missing, empty, malformed, and valid export coverage passes under warning-clean GCC/Clang builds plus independent review.
- Shipped VSIX batch-provenance slice: `#3985` requires every managed Studio-host bridge builder to include exactly one leading `--from-vs`, including `BuildPropertyBatchUpdateArguments(...)` and `BuildDeletedStatesArguments(...)`. Preserve existing argument order, quote doubling, boolean tokens, JSON fields, and localized diagnostics. The specialized native visual-object update-batch parser accepts the global flag and adds the established `launchedFromVisualStudio` identity only to that response family through the optional printer path; VSIX calls report true and standalone calls false. Deleted-state batches retain the existing document JSON provenance path. Managed plus GCC/Clang focused native coverage and full independent GCC JSON review pass; Windows extension-boundary launching remains a hosted validation responsibility.
- Shipped VSIX `SELECT` IntelliSense localization slice: `#3993` provides distinct `es-419` and `pt-BR` descriptions behind `LanguageService.IntelliSense.Keyword.Select`; English and generated `qps-ploc` behavior remain authoritative for their locales. Completion and Quick Info must continue consuming that shared key while `SELECT`, the `keyword` kind, and all machine identities remain invariant. The managed catalog gate now checks key parity and sorted placeholder-multiset parity across every supported locale, including duplicate placeholders and escaped `{{` literals. The Linux `net10.0` harness and final independent review pass; Windows `net472` packaging remains a hosted validation responsibility.
- Shipped runtime working-directory hardening slice: `#3989` accepts only directories for workspace `home_directory` and manifest `working_directory` values. Package planning falls back to the PJX directory before publishing debug roots or `app.cfdebug`. Runtime-host debug mode preserves genuine external directories through the no-basename `allow_external_fidelity` binding mode, while missing, file-valued, and unstatable roots fall back to package `content` through non-throwing probes for both effective session setup and legacy `startup_item` resolution. Keep directory symlink/junction behavior unchanged. Package/materialization plus real-host coverage includes file-valued homes, file-valued debug roots, missing absolute roots with same-basename directory decoys, an inaccessible POSIX root, and unusable startup roots; GCC/Clang pipeline, host-binding, and runtime-host suites pass.
- Shipped absolute project-item provenance slice: `#3991` never applies basename-only or `entry.name` fallback after a POSIX, Windows-drive, or UNC-style absolute `relative_path` remains unresolved. Preserve valid existing absolute sources, the early base-directory return for relative inputs, and the separate documented parent-tail plus entry-name recovery paths. On POSIX, keep foreign Windows-drive and UNC identities out of host filesystem existence and case-fold probes so CWD or host-root aliases cannot rebind them. Stale PRG, FRX/FRT, and LBX/LBT paths with complete project-root, adversarial drive-CWD, and UNC host-alias decoys must retain unresolved source/debug provenance, exclude decoy path/bytes from manifests, fail before package commit through the existing localized source diagnostic, and disable breakpoints. A valid native absolute source must stage its own bytes. Full sequential GCC/Clang runtime-pipeline suites pass. The separate VSIX `SELECT` localization gap is shipped as `#3993`.
- Shipped direct-sidecar document canonicalization slice: `#3990` resolves PJT/SCT/VCT/FRT/LBT/MNT inputs exact-first and then through one unique ASCII case-fold match to their PJX/SCX/VCX/FRX/LBX/MNX primaries before inspection or publication. Preserve actual primary/sidecar entry spelling, exact precedence, direct sidecar identity, family and launch metadata, and the invariant host JSON keys. The selected direct sidecar must be passed explicitly to both asset inspection and DBF preview parsing; never let either reader independently infer a different companion. Missing primaries plus ambiguous requested sidecars or primary siblings use the three four-catalog Studio diagnostics. All six families, both ambiguity directions, both exact-precedence directions, unique case-fold inputs/primaries, ordinary PJX/PJT, Windows-style paths, and selected payload versus corrupt inferred decoy pass under GCC/Clang; rebuilt-host GCC JSON evidence is green. Ordinary-primary/editor ambiguity consistency is shipped as `#3992`, and stale absolute package-item fallback is shipped as `#3991`.
- Shipped `FILE()` object-type parity slice: `#3986` rejects directory candidates while searching the default directory and `SET PATH`, and continues to a later non-directory file instead of letting a same-named folder terminate lookup. Keep `FILESIZE()` on the same directory-skipping resolver rule. Preserve existing handling for other non-directory entries until VFP9 evidence defines portable symlink or special-file semantics; do not overstate this as a regular-file-only contract. Direct/nested/absolute/empty/missing/`SET PATH`/mixed-separator and directory-shadow coverage pass under GCC/Clang and final independent review. The direct-`.PJT` self-sidecar defect is queued as `#3990` under `#109`.
- Shipped local DBF field-identifier parity slice: `#3984` resolves raw descriptor names through deterministic ASCII-only folding for direct writes plus local `REPLACE` and SQL-style `UPDATE`, including additive memo writes. Preserve descriptor bytes/spelling and the existing localized missing-field diagnostic. If multiple on-disk descriptors case-fold to the same requested name, fail closed before any DBF/FPT write rather than selecting the first descriptor. Runtime identifier collapsing must uppercase ASCII explicitly, preserve digits and bytes `>= 0x80`, and avoid locale-sensitive ctype so distinct high-byte field names remain distinct. Direct and PRG lower/upper/mixed-case, additive, `UPDATE`, unknown, descriptor-byte, altered-table ambiguity, high-byte distinction, and isolated `qps-ploc` coverage pass in a 10-test GCC/Clang matrix after independent review. The separate file-valued runtime working-directory defect is shipped as `#3989` under `#110`.
- Shipped asset-inspector memo-pointer hardening slice: `#3983` scans every readable record and all `M/G/P` pointer fields, validates each distinct block once, and preserves zero/shared valid references plus existing localized diagnostic codes. Keep descriptor discovery within `header_length`, recognize terminators only at 32-byte boundaries, and reject offset-zero, undersized, or record-crossing pointer reads so malformed descriptors never fabricate memo findings. Standalone and mixed M/G/P corruption, later field/record, shared/zero/clean, malformed descriptor, and valid internal-`0x0D` coverage pass under GCC/Clang and final independent review. Warning cleanup in the same translation unit is shipped as `#3988`.
- Shipped runtime-host debug-manifest trust slice: `#3987` grants external-source/debug verification bypass only when a validated `debug_manifest_version` document, `.cfdebug` path identity, and explicit `--debug` launch are all present. Missing any signal must retain ordinary containment, verified snapshots/overrides, and security digest enforcement. Reject mixed or duplicate version headers before security/source work through the four-catalog ambiguity diagnostic. Preserve generated `app.cfmanifest`/`app.cfdebug` version keys, supported versions, exit/status/debug fields, and genuine explicitly authorized external PRG/xAsset workflows. GCC/Clang host, localization, binding, and pipeline evidence plus final independent security/acceptance re-review pass.
- Shipped table-field dimension hardening slice: `#3980` parses field widths and decimal counts as complete nonnegative decimal tokens bounded to 255, rejects empty/trailing/malformed/out-of-range components, and fails a complete declaration list when any member is invalid. Preserve zero-to-default behavior and the 255 boundary, existing catalog keys, command tokens, event identities, and heap-backed execution. Direct boundaries plus `CREATE TABLE`, `CREATE CURSOR`, and both `ALTER TABLE` declaration paths pass under GCC/Clang and independent review.
- Shipped `XMLTOCURSOR()` cardinality hardening slice: `#3978` rejects every row whose `<Col>` count differs from the declared `<Field>` count at parse time and repeats the preflight at the loader boundary before alias lookup or mutation. Keep rejection on the existing localized parse-warning and numeric-zero path, with no `runtime.xmltocursor` success event, no new alias, and byte/session-state-equivalent existing destinations. Inline/file extra/missing coverage plus valid round trip pass under GCC/Clang and independent review; preserve XML tags, return/event identities, and stack-frugal execution.
- Shipped license JSON Unicode hardening slice: `#3979` decodes `\uXXXX` as UTF-16 code units, requires every high surrogate to be followed immediately by an escaped low surrogate, rejects lone/malformed/truncated surrogates and non-scalars, and emits one canonical four-byte UTF-8 scalar for valid pairs. Preserve ordinary BMP/control and simple escapes, raw valid UTF-8 bytes, escaped/raw canonical equivalence, signed fixture behavior, and invariant payload/signature identities. Focused GCC/Clang, ASan/UBSan, localization/licensing, status, and independent review coverage pass.
- Shipped subscription-date hardening slice: `#3977` requires both signed expiry and classifier current-date inputs to be exact ASCII `YYYY-MM-DD` calendar dates before lexical comparison. Preserve all four-digit years (`0000` through `9999`), Gregorian month/leap rules, same-day active semantics, and malformed classification for alternate separators, timestamps, whitespace, partial/impossible dates, nondigits, or unrelated text. Keep `subscription_expires` unchanged in status for raw troubleshooting, and keep payload/state identities plus human-localization boundaries unchanged. GCC/Clang, ASan/UBSan, signed fixture, localization, and independent review coverage pass.
- Shipped portable iconv-linkage slice: `#3981` resolves non-Windows iconv through CMake's `Iconv::Iconv` target, linked privately by `cf_xbase_runtime`; preserve Linux's built-in-libc result and macOS's external-library propagation without exposing iconv through public runtime headers. Tests that call iconv directly must retain their own direct target link. Local GCC/Clang runtime-host and focused code-page/localization coverage pass; hosted macOS installer/native validation is the authoritative release evidence. Windows continues using `MultiByteToWideChar` / `WideCharToMultiByte` and must not acquire an iconv dependency.
- Shipped installer-document repair slice: `#3982` sources the authoritative repository `LICENSE.md` while preserving the stable package path `share/copperfin/LICENSE`; do not rename the installed artifact to `LICENSE.md` or duplicate both names without a new compatibility decision. CMake configuration validates every required document, and the documentation-only component byte-compares README, license, security policy, and remaining-work outputs. Keep `copperfin_package_document_check` in all three installer jobs even while those lanes configure with tests disabled. Single/multi-config checks plus real local TGZ/DEB packages pass. The separate non-Windows iconv linkage contract is `#3981`.
- Shipped license-integer hardening slice: `#3976` parses signed JSON integers through a checked unsigned magnitude with distinct positive/negative `long long` limits, so exact `LLONG_MIN` is accepted without signed overflow and one-past or arbitrarily long decimals fail closed. Classification validates `seats` and `perpetual_max_major_version` against `int` before publishing either field; any out-of-range value returns malformed with both integer fields left at zero. Preserve valid canonical payload/signature behavior, invariant field/state identities, and the raw non-localized troubleshooting-diagnostic boundary. Focused GCC/Clang, localization/licensing CTest, and fresh ASan/UBSan coverage pass.
- Shipped portable ctype/high-byte hardening slice: `#3973` uses deterministic ASCII-only case, identifier, drive-letter, and whitespace helpers across query translation, project-workspace classification, and Studio document companion recovery. Preserve bytes `>= 0x80` unchanged and treat them as identifier bytes so keywords never match inside UTF-8 tokens; do not describe this as Unicode case folding. Companion filename recovery is exact-first and returns actual directory-entry leaf spelling, with UTF-8 case-variant coverage across PJX/PJT, SCX/SCT, VCX/VCT, FRX/FRT, LBX/LBT, and MNX/MNT. GCC/Clang localization/query/workspace/Studio/startup-pipeline coverage passes without visible-text or machine-contract changes.
- Shipped visual-asset undo journal hardening slice: `#3971` bounds every serialized read by actual remaining bytes plus platform/container limits before allocation, validates scalar and grouped headers/counts/indexes/flags, accepts the versionless legacy scalar form only at exact EOF, rejects partial grouped counts and trailing bytes, and preflights every target before grouped mutation. Corrupt journals remain in place, return the existing localized journal-read failure, and leave the target asset byte-for-byte unchanged. Full GCC/Clang visual-editor coverage and independent re-review pass; serialized journal fields plus Studio command/JSON identities are unchanged.
- Shipped immutable-audit append hardening slice: `#3970` distinguishes missing/zero-byte/blank-only streams from malformed existing tails. Before append opens an existing log, require readable newline-terminated content plus a structurally valid five-field last event and valid `GENESIS`/SHA-256 hash shapes; malformed, truncated, or unreadable tails fail with localized human errors and preserve exact bytes. Keep full historical cryptographic verification in `verify_immutable_audit_chain(...)`: append performs tail admission, not an implicit whole-log verification pass. Focused GCC security/localization and independent Clang security coverage pass; invariant audit fields and machine identities are unchanged.
- Shipped portable FORCEPATH hardening slice: `#3965` normalizes the complete directory argument before joining its source filename. Drive-letter and UNC directories use backslashes, single-root POSIX directories use `/`, mixed relative directories follow their first separator, and separator-free relatives retain the existing slash join. Preserve path components and dot segments byte-for-byte apart from separator replacement; clean styles, trailing separators, and empty inputs remain covered. Focused GCC PRG/path/runtime-surface and independent Clang path coverage pass without visible text, machine-contract, or stack changes.
- Shipped portable JUSTDRIVE hardening slice: `#3964` recognizes complete `\\?\\UNC\\server\\share` roots before ordinary UNC parsing, including root-only inputs and case-insensitive `UNC` markers, while preserving the input spelling. Explicit extended/device `X:` namespace drives retain their roots; incomplete extended UNC, `GLOBALROOT`, and pipe namespaces return no drive instead of fabricated prefixes. Keep drive-letter, ordinary UNC, POSIX, relative, and empty behavior unchanged; do not route this string helper through host `std::filesystem` root rules. Focused GCC path/function/runtime-surface and independent Clang path coverage pass without visible text, machine-contract, or stack changes.
- Shipped POSIX startup-package hardening slice: `#3953` resolves the selected PJX startup primary exact-first and then through a unique case-fold match for every path component, preserves actual on-disk spelling in staged/runtime/debug identities, and fails materialization for ambiguous or missing PRG/SCX/VCX/FRX/LBX/MNX primaries plus missing/ambiguous/failed selected xAsset sidecars. Optional project assets retain warning behavior. Preserve uniquely resolved parent-relative tail and `entry.name` startup fallbacks, but never restore arbitrary ambiguous tail selection. Preserve transactional rollback on every required-startup failure, keep the localized ambiguity/source-file diagnostics human-only, and do not change invariant manifest keys, status fields, command tokens, or exit codes. GCC/Clang runtime-pipeline and combined localization/build-host/package coverage pass across the six-family mixed-separator matrix, exact precedence, ambiguity, missing primaries, missing sidecars, legacy fallbacks, and rematerialization rollback. Full POSIX validation still reproduces only `#3874`'s 44 stale E3/report-layout failures after correcting and rerunning the invalid build-host startup fixture exposed by the new fail-closed contract.
- Shipped portable FULLPATH hardening slice: `#3962` normalizes Windows drive, ordinary UNC, extended UNC, and extended drive dot segments with host-independent component reduction and locked roots. Preserve standard Windows separators and never pop above a drive/share root. Keep `\\.\\pipe`, `\\?\\GLOBALROOT`, and unsupported device namespaces byte-for-byte unchanged; do not reinterpret them as UNC filesystems. Focused path coverage also guards existing relative/default-directory and POSIX behavior.
- Shipped POSIX locale code-page slice: `#3961` structurally extracts the codeset after `.` and before `@`, accepts only explicit UTF/ASCII/decimal/CP/WINDOWS/IBM forms, and resolves `nl_langinfo`, `LC_ALL`, `LC_CTYPE`, and `LANG` in order before fallback 1252. Never restore whole-locale digit scraping: territory values such as `es_419` are not code pages. Keep Windows `GetACP` / `GetOEMCP` and supported VFP conversion validation unchanged. Dedicated pure-parser/fallback tests plus real CPCURRENT coverage pass under local GCC and Clang.
- Shipped `SET EXACT ON` equality slice: `#3963` uses trailing-space-only normalization in ordinary expression equality and shared string-function equality, preserving leading spaces and significant tabs/NULs while disregarding trailing ASCII spaces as mounted VFP9 help requires. Preserve the intentionally asymmetric `SET EXACT OFF` rule: comparison stops at the end of the right operand. Direct `=` and `INLIST()` coverage plus session tests guard both paths; no localization, machine contract, binary flavor, or heap-backed execution behavior changed.
- Shipped string-plus parity slice: `#3887` restricts `+` concatenation to two character operands, preserves numeric pair addition and existing Date/DateTime arithmetic, and routes mixed character/non-character operands through the existing localized Error 107 continuation path. Mounted VFP9 help grounds the character contract. Focused tests cover numeric-looking text, both character/numeric orders, all character/Date/DateTime orders, exact lines, invariant `runtime.error`, pseudo-locale prose, and continued heap-backed execution. Preserve this typed boundary; never restore `value_as_string` coercion for a lone character operand.
- Shipped managed DECLARE hardening slices: `#3943`, `#3945`, and `#3947` use a supported Windows CLR v4 COM-interface bridge, `Assembly.LoadFrom` path semantics, CLR binder-compatible public-static scalar invocation, explicit PE32/PE32+ parsing, native-export precedence for mixed-mode images, managed-fault source retention, and bounded RAII ownership. Preserve absolute/relative/parentless dependency behavior, localized display diagnostics, invariant source/machine identities, direct deferred-EXCEPINFO cleanup coverage, and the distinction between a present empty managed result and an unknown DECLARE fallback. Hosted run `29179815586` at exact SHA `cb78a573` passed managed target 241 on Win32/x64 in 3.02/0.26 seconds and adjacent targets in 1.58/1.46/0.04 and 1.64/11.02/0.03 seconds. Keep the MVP boundary to Windows .NET Framework public static scalar calls; do not infer managed object/event/callback/by-reference or non-Windows CLR-host parity, and keep PRG execution stack-frugal.
- Shipped numeric DECLARE by-reference safety slice: `#3944` follows mounted VFP9's dual-`@` documentation for valid writeback but deliberately rejects declared numeric `@` parameters without a call-site `@` variable instead of reproducing VFP's unsafe pass-by-value pointer hazard. Preserve pre-native Error 11, the four-catalog alias/position diagnostic, shared canonical plus `I`/`L`/`D`/`F`/`I64`/`LONGLONG` classification, and unchanged STRING/managed behavior. Controlled tests prove INTEGER/LONG/INTEGER64/SINGLE/DOUBLE writeback, literal/variable rejection with zero native entries, source alias/path and error events, cleanup, Win32 stdcall `@20`, x64 unified export/ABI, and 512 sequential handler unwinds. Hosted run `29175436178` at exact SHA `ad5993b2` passed targets 3/240/244 on Win32 in 2.80/1.49/0.04 seconds and x64 in 1.46/0.64/0.03 seconds. Never restore scalar marshalling for declared numeric references; keep execution stack-frugal.
- Shipped DECLARE arity slice: `#3946` compares every call-site argument count with the parsed declaration before the managed/native split, so no missing or excess value can enter native marshalling or disagree with the Win32 `@N` export selected from declaration metadata. Mounted VFP9 help grounds Error 1229 / `Too few arguments.` and Error 1230 / `Too many arguments.`; preserve those explicit codes and catalog-routed messages through ordinary statement faults, `ON ERROR`/`AERROR`, and outer run-boundary expression contexts. The independent eight-argument ceiling also faults as Error 1230. Controlled fixture counters cover valid entry, zero/one/eight, cdecl/stdcall, mixed-width/by-reference, over-limit, pseudo-locale, stable source alias/path DECLARE plus error/error-handler events, resumability, and balanced unload on Win32 and x64. Initial run `29174219763` built both architectures and exposed only a wrong event assertion; final run `29174578339` at exact SHA `71053bef` passed targets 3/240/244 on Win32 in 4.16/4.14/0.04 seconds and x64 in 1.41/0.98/0.03 seconds. Keep execution stack-frugal and do not restore excess-argument INTEGER fallback.
- Shipped explicit DECLARE lookup slice: `#3942` retries `FunctionNameA` only after every exact `FunctionName` probe fails for ordinary native DLL/FLL declarations, matching installed VFP9 help and Microsoft-shipped unsuffixed `GetSystemDirectory IN kernel32.dll` / `ShellExecute IN shell32.dll` usage. Preserve exact precedence, cdecl `_NameA`, stdcall `_NameA@N` / `NameA@N`, source function and `AS` alias identities, invariant events, and locale-independent UTF-8 actual-module identity. Controlled tests distinguish parentless executable-directory and quoted-relative copied modules through A-only self-reported paths, exercise exact/A siblings plus real non-UI `GetSystemDirectory`, and prove cleanup. Hosted run `29173050565` at exact SHA `517eff1a` passed targets 3/240/244 on Win32 in 1.83/0.99/0.04 seconds and x64 in 1.63/0.45/0.03 seconds. Keep managed lookup, non-Windows guards, localization, and stack-frugal execution unchanged.
- Shipped WIN32API DECLARE slice: `#3939` recognizes bare `WIN32API` before expression/path evaluation and searches the documented Kernel32/Gdi32/User32/Mpr/Advapi32 set in order from the Windows system directory. Exact export probes precede the VFP `A` fallback on this special path; actual loaded-module/resolved-export identities remain internal while diagnostics and `runtime.declare_dll` events preserve the source designator/name. Keep every successful `LoadLibraryW` reference balanced across alias replacement, failed redeclaration, `QUIT`, and ordinary session destruction. Hosted run `29171981652` at exact SHA `570811ed` passed targets 3/240/244 on Win32 in 1.93/1.59/0.04 seconds and x64 in 1.62/0.90/0.03 seconds. `#3942` separately ships the documented general explicit-DLL `A` fallback. Preserve non-Windows guards, localization, invariant events, and stack-frugal execution.
- Shipped DECLARE SHORT slice: `#3938` treats documented SHORT native returns as signed 16-bit through `VT_I2` dispatch; existing managed `VT_I2` conversion remains unchanged. Do not add SHORT parameters: installed VFP9 help explicitly omits SHORT from the permitted parameter list, and declarations using one fail through `Runtime.Prg.Dispatch.Error.DeclareUnsupportedParameterType` before DLL loading. Controlled coverage preserves `-12345`, the mirrored Microsoft-shipped WinInet declaration shape `SHORT function(INTEGER @ flags, INTEGER reserved)`, and a real WinInet declaration-shape smoke check on both Win32 and x64. Hosted matrix run `29170925611` at exact SHA `e608117b` configured and built both architectures; Win32 passed targets 3/240/244 in 1.98/1.58/0.04 seconds and x64 passed in 1.16/0.47/0.02 seconds. Preserve INTEGER/LONG/SINGLE/DOUBLE, decorated lookup, localization, invariant events, and stack-frugal execution.
- Shipped Win32 DECLARE lookup slice: `#3941` adds a shared stdcall `@N` classifier and probes `_Name@N` then `Name@N` only after exact `Name` and cdecl `_Name` fail on 32-bit Windows. A matched `_Name` sets the native cdecl flag and invokes through `CC_CDECL`; exact and stdcall-decorated matches remain `CC_STDCALL`. Preserve the width contract: DOUBLE/D/F and explicit LONGLONG/INTEGER64/I64 consume 8 bytes by value; every by-reference parameter and all other types consume 4. All general x86 stdcall aliases are removed, making the existing SINGLE and mixed-width fixtures decorated-only; the mixed call uses `AS X86MixedAlias` while lookup retains the source export name, and a dedicated MSVC fixture export exposes only `Name@4` for the final alternate branch. Hosted x86 run `29169777958` at exact SHA `e70d5ac2` configured and built Win32 successfully and passed target 240 in 0.52 seconds. Keep x64 and non-Windows branches unchanged; `#3939` owns shipped WIN32API search and `#3942` now owns shipped explicit-DLL `A` fallback.
- Shipped Win32 DECLARE ABI slice: `#3940` removes the old all-`__int32` x86 stdcall table and routes every 32-bit native signature through the bounded type-aware `DispCallFunc` lane, while Win64 retains its fixed typed dispatcher except for SINGLE. Controlled x86 coverage combines INTEGER, LONG, DOUBLE, and above-32-bit INTEGER64 arguments, signed LONG/INTEGER64 returns and by-reference writeback, DOUBLE by-reference writeback, pointer-shaped STRING return, zero-argument calls, and eight stack positions; the same target also reruns `#3933`'s Windows-wide SINGLE matrix and the existing `lstrcpyA` STRING-pointer/writeback test. The fixture aliases used to isolate this slice are removed by `#3941`; do not restore them after decorated lookup passes. Hosted x86 run `29169198092` at exact SHA `0abbabb4` configured and built Win32 successfully and passed target 240 in 0.55 seconds. Preserve localized errors, invariant events, the eight-argument limit, non-Windows behavior, and stack-frugal execution.
- Shipped DECLARE ABI slice: `#3933` recognizes documented VFP9 `SINGLE` as 32-bit floating point in native by-value, by-reference/writeback, return, and .NET `VT_R4` conversion paths. Any native signature containing SINGLE uses bounded `DispCallFunc` marshalling while non-SINGLE Win64 calls retain the proven fixed typed dispatcher; do not expand the latter into a three-way per-slot template matrix. Controlled Windows coverage spans zero-argument return, mixed INTEGER/SINGLE/DOUBLE register positions, all eight positions including stack slots, DOUBLE return through a SINGLE signature, and SINGLE by-reference writeback. Keep the fixture's stdcall declarations plus undecorated MSVC/MinGW x86 aliases so this marshalling test remains active for both Windows pointer widths; those aliases deliberately isolate `#3933` and do not satisfy decorated-export lookup. Hosted run `29167401711` at exact implementation SHA `dbb95fca` built successfully and passed target 240 in 0.57 seconds plus parser/localization/runtime-pipeline and adjacent PRG targets. `#3940` owns the pre-existing non-SINGLE x86 typed-dispatch defect and `#3941` owns standard `_Name@N`/`Name@N` lookup. Preserve existing localized errors, invariant events, eight-argument guard, non-Windows behavior, and stack-frugal execution.
- Shipped DECLARE width slice: `#3932` removes only VFP `LONG` from the 64-bit classifier shared by .NET VARIANT conversion, native parameter/by-reference storage, and Win64 return selection. Preserve LONG as signed 32-bit beside INTEGER, while intentionally named LONGLONG/INTEGER64/I64 extensions remain 64-bit pending exact-value hardening in `#3934`. Controlled Win64 coverage combines DOUBLE plus LONG arguments, a negative near-boundary LONG return, negative LONG writeback, and the existing signed INTEGER64 distinction. Hosted run `29165702579` at exact SHA `19dfdada` keeps target 240 fully green in 0.56 seconds under MSVC. Keep parser metadata, localization, machine events, x86/non-Windows behavior, and stack-frugal execution unchanged.
- Current Date/DateTime ordering slice: `#3922` gives flavored D/T values a presentation-independent second-based temporal scalar and routes `<`, `>`, `<=`, and `>=` through one chronological comparator. Date normalizes to midnight, empty D/T sorts before valid values, mixed Date/DateTime uses one timeline, and incompatible types retain localized Error 107 continuation. Keep constructors, DBF reads, arithmetic, and date/time conversions propagating the scalar; never reconstruct temporal identity from lossy `SET CENTURY OFF` text or let `SET DATE`/`SET MARK`/`SET HOURS`/`SET SECONDS` alter ordering. Historical/future 1940/2050 and derived-value coverage passes with adjacent control-flow/data-I/O/function/string-math suites. `#3930` and `#3931` separately own MIN/MAX and SQL ORDER BY flavor-aware ordering.
- Current GATHER data-integrity slice: `#3926` gives shared variable lookup a presence signal while preserving existing local-before-global value lookup and empty fallback. `GATHER MEMVAR` alone consumes that signal so fields without same-named variables remain untouched, while declared variables holding genuine empty values still write normally. Keep this aligned with the installed VFP9 GATHER contract, on the existing iterative runtime path, and free of new visible text or machine-contract changes. Focused and adjacent work-area/control-flow/parser/data-I/O/function targets pass.
- Current REPLACE parity slice: `#3927` implements mounted-VFP9 default-current plus explicit ALL/NEXT/RECORD/REST scopes and per-assignment ADDITIVE. Keep physical scope bounds shared with aggregate collection, keep FOR/WHILE/IN/filter behavior composed with them, and preserve the documented SET DELETED exception for explicit single-record scope without bypassing SET FILTER. Keep scope represented through existing IR identifier/operand fields. ADDITIVE belongs to each `Field WITH expression`, applies only to M fields, appends to raw memo bytes, fails closed on unreadable source blocks, and must remain fully reversible across exact DBF/FPT command undo; non-memo ADDITIVE remains an ordinary overwrite. Focused DBF/runtime and adjacent runtime-pipeline/work-area/control-flow/parser/SQL suites pass without repository-root runtime-temp residue.
- Current critical runtime slice: `#3928` keeps WITH leading-dot rewriting away from the case-insensitive reserved `.T.`, `.F.`, `.NULL.`, `.NOT.`, `.AND.`, and `.OR.` token family. Preserve parser ownership of those literals/operators in every expression position while continuing to bind ordinary leading-dot members, including names that merely start with `T` or `Not`; keep the guard non-recursive and on the existing heap-backed iterative PRG path. Focused control-flow and adjacent expression suites pass.
- Current DBF integrity slice: `#3891` gives `ADD COLUMN`, `DROP COLUMN`, `ALTER COLUMN`, and `PACK MEMO` one raw rewrite collector. Keep retained M/G/P cells sourced from `MemoReader::read_block_raw()` and fail before writes when a referenced block is unreadable; keep unaffected fixed cells, opaque VFP null-flag bytes/types, deleted markers, and order copied exactly rather than round-tripped through `display_value`. New and intentionally altered fields must still pass normal storage validation, so opaque types are allowed only when explicitly preserved from the source schema. Focused DBF and adjacent PRG schema/mutation targets pass.
- Shipped Win64 DECLARE slice: `#3895` replaces the raw-export `DispCallFunc` path with a separate typed dispatcher that generates the exact integer/pointer-versus-DOUBLE ABI signature for each zero-through-eight argument shape and exact signed 32-/64-bit, string-pointer, or DOUBLE return shape. Keep this helper isolated from the stack-frugal PRG frame machine and preserve x86/non-Windows behavior and by-reference storage. The parser retains VFP9's documented `IN <library> [AS alias] <typed named parameters>` form as well as the existing parenthesized compatibility form; never let expression parsing discard trailing parameter metadata. Controlled coverage includes quoted paths, aliases, bare library metadata, named/by-reference declarations, zero-argument fractional return, every argument count, mixed register/stack classes, exact return classes, writeback, invariant events, and actual-value diagnostics. Hosted run `29164542227` at exact SHA `9cf08f41` makes target 240 fully green in 0.50 seconds under MSVC; `#3939` separately owns executable WIN32API multi-library search semantics.
- Last shipped visual-asset durability slice: `#3890` gives memo-backed visual edits a same-directory table/companion transaction with staged and flushed new files, original backups, a validated commit marker, exact rollback, and interrupted-operation recovery. Deterministic SCX/SCT, VCX/VCT, FRX/FRT, LBX/LBT, and MNX/MNT coverage exercises sidecar/primary staging, first/second promotion, rollback reporting, stale cleanup, exact-byte preservation, artifact cleanup, and failed-edit undo removal. Existing localized storage/rollback prose and machine contracts are unchanged.
- Last shipped package-hardening slice: `#3881` routes AST, IR, FXP token/embedded FXP, and generated C# source through the same `should_stage_asset(...)` admission rule as runtime content staging. Excluded non-runtime PRGs no longer disclose paths/statements/source in compiler-debug artifacts, excluded SCX/VCX assets no longer generate C# types, and excluded startup assets remain admitted when runtime-required. Excluded inventory metadata, schemas, keys, and localization remain unchanged; the full runtime-pipeline target passes.
- Last shipped report-fidelity slice: `#3919` expands grouped FRX/LBX band `VPOS` edits through the existing section-aware delta model before one atomic batch write. Positive/negative multi-band moves now shift contained objects while explicit caller edits win, duplicate band properties use the final requested value, repeat requests remain byte no-ops, and grouped undo restores bands plus generated object moves in one command. FRX/FRT and LBX/LBT coverage preserves unsupported fields, deleted rows, ordering, and memo bytes; host JSON and localization contracts are unchanged.
- Last shipped Windows DECLARE-loader slice: `#3921` preserves parentless DLL names for the Windows loader policy while continuing to resolve explicit relative directory paths from the VFP current/default directory. Run `29156635629` compiled and passed the parentless system-DLL/by-reference, repository-built explicit-child DLL, missing-library, event, and localized missing-symbol checks; only the separate `#3895` double-ABI script remains red in that target. Absolute paths, localized errors, loaded-module cleanup, .NET detection, and machine event names remain unchanged.
- Last shipped report-fidelity slice: `#3920` adds `BOTMARGIN` to the shared memo-backed FRX/LBX settings allowlist. EXPR-only bottom margins now survive the query/clear/reopen/re-set workflow through the common VSIX/standalone behavior model while comments, unsupported and sibling settings, direct-field precedence, and no-op primary/sidecar bytes remain preserved. Host JSON names and localized display labels are unchanged.
- Last shipped runtime slice: `#3918` implements runtime-session-wide `SET UDFPARMS TO VALUE/REFERENCE`, defaults new runtime sessions to VALUE, and exposes the mode through `SET('UDFPARMS')` / `SET('UDFP')`. UDF-only call classification keeps bare scalars and bare-array element 1 as values under VALUE, aliases eligible bare identifiers under REFERENCE, and preserves explicit `@`, forced-value parentheses, literals/expressions, and reference-default `DO ... WITH` / `CALL ... WITH`. Scalar references are live caller aliases across duplicate and nested formals; arrays retain `#3899`'s canonical caller-owned storage, including `m.`-qualified forms. Scalar identity and cache propagation use an iterative frame-index walk, with a 768-frame forwarding regression guarding against recursive assignment propagation. Caught faults and data-session changes preserve the runtime-session mode, while independent runtime sessions remain isolated. `#4068` owns the pre-existing native-stack recursion in synchronous nested expression evaluation; do not hide it with a larger stack reserve. `#3871` still owns array declaration scope/lifetime.
- Last shipped validation follow-up: Windows run `29154657749` proves `#3917`'s native audit-path expectation passes, then reaches the separator-deployed startup check, where the product reports the correct canonical long path but the fixture still expected its pre-canonical `RUNNER~1` spelling. Commit `3e82f91f` canonicalizes that startup expectation before native conversion; the exact binding target passes on Linux and renewed Windows proof is pending.
- Last shipped localization slice: `#3915` removes POSIX codeset/modifier suffixes before shared locale normalization, so explicit and environment forms such as `pt-BR.UTF-8`, `pt_BR@latin`, and combined variants select `pt-BR` while suffix-only malformed values fall back to `en-US`. Existing catalog/fallback and machine contracts remain unchanged; direct, subprocess CLI/environment, and invariant license-status coverage pass.
- Last shipped slice: `#3901` implements the installed-VFP9 Date/DateTime `+`/`-` matrix through flavored arithmetic rather than string concatenation: Date uses day offsets/differences, DateTime uses second offsets/differences, numeric-first addition is supported, results retain D/T flavor and active SET presentation, and unsupported/mixed forms keep localized Error 107 continuation. Mounted VFP9 probes ground commutativity, fractional Date flooring, empty behavior, and mismatch cases; `#3916` tracks the separate invalid-constructor paths that currently discard empty D/T flavor.
- Last shipped slice: `#3912` contains shared PRG test runtime-temp state under an absolute startup-fixture parent when `working_directory` is empty, falling back to a named Copperfin test area under the absolute OS temp directory for relative/parentless startup paths. Explicit working-directory behavior and production runtime selection remain unchanged; core, control-flow, data-I/O, and command-UNDO/table-mutation tests leave no repository-root `runtime-temp/`.
- Last shipped slice: `#3914` keeps runtime-host breakpoint command/state tests aligned to the canonical admitted startup source identity rather than a pre-canonical Windows temp spelling. Manual manifests are escaped and all add/list/clear/re-add/remove/survivor ordering and pause assertions remain active; run `29152176200` proves those failures are gone without debugger product changes.
- Earlier `#3669` regression correction serialized the manual release/debug manifest fixture's source and working-directory paths through the real escaping contract; raw Windows `\\release_main.prg` had been decoded as `\r`, and run `29152176200` proved that correction. The later reopened failure was also fixture drift, this time a rootless absolute release source compared through canonical running-image aliases, not evidence that ordinary launch selected `app.cfdebug`.
- Last shipped slice: `#3913` makes the runtime-host binding smoke canonicalize the existing rebound content root before converting it to host-native spelling. This matches the product's physical-containment identity, including Windows 8.3 expansion; the full binding target passes on Linux and awaits Windows proof.
- Last shipped slice follow-up: Windows run `29150675806` proves the CRLF parser correction makes `test_build_host_output` fully green in 21.18s, including DLL/FLL provenance, APP/FXP extension payloads, and runtime-host digest checks. Together with run `29149489242`, `test_runtime_pipeline` and `test_prg_engine_control_flow` remain green; the Windows failed-target inventory has fallen from 61 to 58.
- Last shipped slice: `#3911` lets a pending SLEEP statement consume an already-requested cancellation through its existing statement-specific handler before duration evaluation. This preserves localized sleep-cancel prose, exact fault metadata, rollback/events/journal cleanup, and stack-frugal execution; the outer generic cancel path remains authoritative for non-SLEEP statements. The regression is breakpoint-synchronized and has no timing thread race.
- Last shipped slice: `#3910` corrects the APP/FXP build-host extension-payload expectations to compare manifest-escaped paths. This removes the two Windows-only false failures without changing package output or machine contracts; independent APP/FXP content, kind, digest, and existence checks remain active.
- Last shipped slice: `#3909` gives generated DLL/FLL CMake projects explicit per-configuration library/runtime/archive output directories, so Visual Studio and other multi-config generators materialize the exact requested package-root output rather than a hidden `Debug`/`Release` child. Generated-source/API/debug/build-host path expectations now honor manifest/C++ escaping and decode manifest-derived paths before file access; durable Ninja Multi-Config coverage builds both Debug and Release DLL/FLL artifacts in addition to ordinary single-config targets.
- Last shipped slice: `#3907` routes the two debug-manifest `source_roots` expectations through the shared test-side manifest serializer. Windows backslashes are now compared in their escaped wire representation while separate in-memory assertions continue to prove normalized deduplication and source-first/content-second ordering; production `app.cfdebug` contracts are unchanged.
- Last shipped slice: `#3905` makes package companion casefold lookup return the actual full directory-entry filename rather than the inferred lowercase candidate that Windows reports as existing. Exact matches win, one casefold match is accepted, and multiple case-distinct matches fail closed through a localized package diagnostic. Staged xAsset, startup/writable DBF, and immutable DBC companions therefore retain source stem/extension casing in package paths and manifest/debug digest records; full-casing FRX/FRT and LBX/LBT plus all-five startup DBF digest coverage now joins SCX/VCX/MNX/DBF/DBC fixtures.
- Last shipped slice: `#3906` makes the shared `#702` VFP path fixture host-valid: Windows uses a real nested path whose native string carries backslashes, while POSIX retains the legal filename containing synthetic `E:\Forms\...` text. Production display-name/sidecar behavior and assertions are unchanged; this is the path counterpart required for `test_studio_host` to become green after `#3904` removed its Windows stack crash.
- Last shipped slice: `#3904` applies the existing 16 MiB Windows stack-reserve contract to both `copperfin_studio_host` and the large direct shared-model `test_studio_host` executable through one CMake helper. This addresses the deterministic Windows CTest segfault without weakening the test or changing non-Windows links; structural decomposition remains under `#2564`.
- Last shipped slice: `#3903` gives the aggregate Studio host JSON execution tests repository-built success/failure child executables instead of treating every non-macOS host as if `/bin/true` and `/bin/false` existed. CMake injects platform-correct target paths, the fixtures ignore arbitrary structured dispatch arguments, and expected executed-command text follows the existing Windows/POSIX quoting contract; production direct-process launching and JSON machine contracts are unchanged.
- Last shipped slice: `#3902` raises Windows deep validation from a 45-minute to a 90-minute job ceiling while preserving every native, VSIX, standalone Studio, designer-smoke, input, and artifact step. Repaired shell launches make the full Windows test workload real; observed all-target MSVC build time plus the serial full-suite baseline cannot fit the old ceiling.
- Last shipped slice: `#3897` recognizes leading-slash `FULLPATH()` inputs as POSIX absolute independently of host `std::filesystem` rules, normalizes VFP backslashes to forward slashes, and collapses lexical dot segments. Windows therefore no longer prefixes its current/default directory onto portable `/...` source paths, while drive-rooted, UNC, and relative path behavior remains unchanged; focused ordinary/mixed/dot-segment coverage passes on Linux and awaits the combined Windows deep gate.
- Last shipped slice: `#3896` routes all 165 native runtime/package/build/localization/Studio-host shell-backed test process launch call sites across 159 source files through the shared command adapter, including runtime-pipeline CMake configure/build subprocesses and captured localization CLI probes. Windows wraps the complete already-quoted command before `std::system` or `_popen` delegates to `cmd.exe /c`, preventing filename/directory/volume-label syntax errors and empty captures; POSIX command strings remain unchanged. The pure adapter contract, representative long-running launch suites, exact shared Studio-host launch, and the complete native build pass on Linux; full POSIX CTest reproduces only `#3874`'s 44 documented stale E3 failures. Rerun Windows deep validation for the platform-authoritative result.
- Last shipped slice: `#3892` restores MSVC compilation after the runtime module split by explicitly narrowing `std::filesystem::path::preferred_separator` before using it with `std::string` path buffers and defining `NOMINMAX` at the remaining leaking `windows.h` boundary. Linux rebuilt the shared runtime and passed the directly affected runtime-surface, file-I/O, and path-function suites without changing path, locale, or machine contracts; Windows deep validation remains the authoritative platform gate.
- Last shipped slice: `#3888` routes managed project Build/Run/Debug process creation through the shared wrapper-aware start-info path, so configured Windows `.cmd` / `.bat` hosts use `COMSPEC` while direct executables remain direct. Captured process-start exceptions now become localized failure results with retained OS detail; machine arguments, parsed output keys, exit codes, and locale environment names remain invariant.
- Last shipped slice: `#3876` restored default TEXTMERGE delimiter resets for both documented bare `SET TEXTMERGE DELIMITERS` and shipped-VFP `SET TEXTMERGE DELIMITERS TO` syntax. The delimiter branch now uses the shared token-aware `SET ... TO` stripper, preserving custom/single-delimiter/session behavior while restoring immediate `<<` / `>>` merge evaluation.
- Last shipped slice: `#3875` restored the documented VFP string-minus type boundary after `#3844`: character concatenation requires two character operands, while mixed character/numeric operands produce localized Error 107 prose through an invariant `runtime.error` pause at the faulting line. Repeated pause/continue transitions stay on the heap-backed iterative frame machine, and valid trailing-space relocation remains unchanged.
- Last shipped slice: `#3880` implemented stack-frugal `OPEN DATABASE`, `SET DATABASE TO`, `CLOSE DATABASE(S) [ALL]`, `DBC()`, and `DBUSED(cDatabaseName)` parity over heap-backed per-data-session state. Security-enabled package execution hands verified exact DBC/DCT/DCX bytes into normal and bridge sessions; metadata remains immutable and stored procedures, triggers, validation rules, and database events remain inert. Human failures are catalog-routed while command tokens, debug field names, manifest keys, status values, and exit codes remain invariant.
- Previous shipped slice: `#3879` versioned `app.cfmanifest` and `app.cfdebug` at schema `3` and separated copied non-startup DBF primaries plus FPT/CDX/IDX/NDX/MDX companions into an explicit `package_writable` data surface. Security startup still requires direct physically contained files and complete manifest membership, but ordinary DBF-family byte changes survive relaunch without bypassing immutable PRG, xAsset, or DBC/DCT/DCX digest checks. Legacy version 1/2 contracts remain accepted, new human diagnostics are catalog-routed, and machine manifest/status/exit/summary/audit fields remain invariant.
- Previous shipped slice: `#3861` shipped on the build/runtime `#110` lane by rejecting package-internal POSIX symlinks, mount crossings, and Windows reparse-point/junction components while permitting a deployment-root link, then hashing and executing direct-file byte snapshots instead of reopening accepted startup, include, bridge, report, label, or companion paths. Security startup now requires its digest to come from a copied `asset` record; verified source loads are strict and case-insensitive; generated bridge/xAsset bootstrap bytes are handed directly to the PRG session; staged companions receive manifest digests; and localized failures preserve invariant status/exit contracts. The intentional external `app.cfdebug` trust model remains separate.
- Last shipped slice: `#3847` shipped on the common-runtime lane by making `SCATTER FIELDS <list>` and `GATHER FROM <array> FIELDS <list>` honor explicit field-list order instead of silently reverting to physical schema order. The SCATTER and GATHER paths now route selected fields through `filter_field_descriptors(..., /*preserve_explicit_field_order=*/true)` before array materialization or array-backed writeback, and focused `test_prg_engine_data_io` coverage now proves a reordered `FIELDS AGE, NAME` clause keeps AGE in slot 1 and NAME in slot 2 across both directions.
- Last shipped slice: `#3846` shipped on the common-runtime/report-output lane by teaching `REPORT FORM` / `LABEL FORM ... TO FILE` to preserve inline `FOR` / `WHILE` clauses and emit stable active-work-area row data instead of only writing a report-metadata stub. The parser now captures `FOR` into `statement.quaternary_expression` and `WHILE` into `statement.names.front()` for report/label commands, while `open_report_surface(...)` now writes shared layout metadata plus filtered row text from the selected cursor, keeping preview behavior and localized fault contracts unchanged.
- Previous shipped slice: `#3845` shipped on the common-runtime lane by preserving explicit `FIELDS` order across the remaining array and DBF transfer siblings that were still using natural schema order. `COPY TO <dest>`, `COPY TO ARRAY`, and `APPEND FROM ARRAY` now route their descriptor selection through `filter_field_descriptors(..., /*preserve_explicit_field_order=*/true)`, and focused data-IO coverage now proves a reordered `FIELDS NOTE, NAME` list stays in that order in exported DBF schemas, runtime arrays, and array-backed append mapping instead of silently reverting to declaration order. Focused validation used `git diff --check`, `cmake --build build --target test_prg_engine_data_io -j4`, direct execution of `./build/tests/test_prg_engine_data_io`, and `ctest --test-dir build --output-on-failure -R '^test_prg_engine_data_io$'`.
- Last shipped localization child: `#3823`, the managed VSIX workflow parser now mirrors `BuildHost.Prefix.Warning` in its shared localization catalog and consumes localized build-host warning prefixes while keeping `qps-ploc` aligned to the invariant native `warning: ` prefix and preserving machine-readable contracts unchanged.
- Current lane: with `#3839`, `#3838`, `#3835`, `#3834`, `#3830`, `#3829`, `#3822`, `#3821`, `#3820`, `#3819`, `#3813`, `#3812`, `#3808`, `#3805`, `#3803`, `#3802`, `#3801`, `#3800`, `#3799`, `#3798`, `#3795`, and `#3794` now shipped on the native runtime `#3217` lane, `#3837`, `#3836`, `#3832`, `#3824`, `#3816`, and `#3807` shipped on the `#110` hardening lane, `#3823`, `#3810`, and `#3809` shipped on the `#2348` localization lane, `#3840`, `#3833`, `#3831`, `#3828`, `#3827`, `#3826`, `#3825`, `#3818`, `#3817`, `#3815`, `#3814`, `#3811`, `#3806`, `#3797`, and `#3796` shipped on the E3 `#24` lane, and `#194`, `#195`, and `#199` already shipped across the portability `#35` / `#37` lanes, choose the next concrete child from live GitHub state across `#3217`, build/debug pipeline `#110`, remaining portability children, E3 `#24` report/label lane, localization `#2348`, and any remaining non-stale hardening parents, while keeping localization active as both a delivery lane and a standing constraint for new user-facing text. The runtime reflection baseline now includes built-in `ReadExpression()`, built-in `WriteExpression()`, built-in `ReadMethod()`, first-pass built-in `WriteMethod()` replacement for existing same-session PRG methods, and first-pass built-in `WriteMethod(..., .T.)` creation on the starting class for missing same-session PRG methods; the list-control `Requery()` baseline now includes representative single-cursor and joined SQL/query-file row sources through bare source designators plus SQL-style source aliases (`FROM employee e`, `FROM employee AS e`, representative joined alias forms), representative `SELECT DISTINCT ... ORDER BY <ordinal>` support on the existing single-cursor SQL/query-file row-source path, bare `JOIN`, explicit `INNER JOIN`, `LEFT JOIN`, and `LEFT OUTER JOIN`; the native `PageFrame` baseline now keeps `Pages(1..n)`, `FOR EACH` enumeration, `ActivePage`, and `PageCount` shrink behavior aligned to live add/display order instead of lexical member names; the native `Objects` / `Controls` baseline now keeps default-item access, subscript access, enumeration, and post-`RemoveObject()` remaining order aligned to live child add/display order instead of lexical member names; the native hidden-collection selector baseline now also keeps dotted/bracketed member chains on the native item path for `Objects`, `Controls`, `Columns`, `Pages`, and `_SCREEN.Forms` / `_VFP.Forms` instead of falling through to OLE lookup; the native list-control `ControlSource` baseline now also keeps same-session bound memvars/cursor fields synchronized after `Clear()`, `RemoveItem()`, and `RemoveListItem()` mutate the effective selection/value state, matches the existing property-write and `Requery()` sync seam for sort-driven reordering plus `AddItem()` / `AddListItem()` mutations, the native list-control reflection baseline now also includes built-in `IndexToItemID()` and `ItemIDToIndex()` conversion parity across live display order, stable item IDs, sorted reordering, removals, and derived `Init`-time population, and the native list-control item-addressed property baseline now also keeps dotted `ListItem(...)` / `ListItem[...]`, dotted `List(...)`, and selector-style `ListItem(...)` / `ListItem[...]` assignments on the native item-ID path instead of the generic dotted-member fallback, including the added regression check that `oPlain.List(3, 2)` stays aligned immediately after `oPlain.ListItem[m.nTargetId, 2] = 'Zulu Prime'` and before later `SETPEM()` mutation. The report/label preview baseline now also keeps deleted section preview bounds expanded by surviving live objects that remain contained inside deleted sections, which fixes the mounted real-sample `cust.lbx` deleted `Detail` geometry while preserving the zero-width deleted `invoice.frx` case that contains no retained objects. The VSIX builder-summary baseline now localizes current-target artifact kinds through the same shared artifact-kind catalog path already used by object/data summaries, the VSIX project-workflow baseline now parses localized build-host warning prefixes from its shared managed catalog with English fallback plus invariant `qps-ploc` compatibility, the PATH-launched CLI localization baseline now resolves basename-only executable hints through the real executable on PATH instead of the caller's working directory, the inspect CLI license-status baseline now emits the same invariant `status: ok` then `state: ...` contract as the sibling build/runtime hosts and stays unlocalized under `es-419` and `qps-ploc`; library-output package materialization now stages the runtime host beside the generated wrapper with the platform-correct `copperfin_runtime_host[.exe]` filename before DLL/FLL/OCX wrapper build finalization, the generated .NET launcher now uses that same package-planned filename instead of assuming a Windows suffix, and packaged runtime-host self-verification now hashes that same platform-correct bundled sibling host name instead of assuming `copperfin_runtime_host.exe` off Windows; the report/label details-summary baseline now surfaces shared printer/grid settings through the existing localized property-label path, falls back to deleted root settings when live root settings are absent, and now also includes the remaining supported root print settings `DEFAULTSOURCE`, `PRINTQUALITY`, `YRESOLUTION`, `TTOPTION`, `ASCII`, and `COLLATE`; the report/label shared summary baseline now also falls back to deleted root settings for the existing page-setup, printer, column, and sort summary fields when live root settings are absent while still preferring live root settings whenever both sets exist, and now also surfaces additive summary/value pairs for `DEFAULTSOURCE`, `PRINTQUALITY`, `YRESOLUTION`, `TTOPTION`, `ASCII`, and `COLLATE` through the same shared snapshot plus stable host JSON contract; the label-layout native action-validation baseline now directly gates the shared LBX `EXPR` update/clear and edited-deleted restore-as-unplaced flows through `test_studio_host_label_layout_actions`; the managed smoke harness baseline now supports `--list-tests`, `--filter`, and `--exact` while keeping the legacy full-run ordering and exposing the installed-real-asset chain through a stable `SmokeResolvedRealAssetCoverageCluster` name for targeted Linux/Mono validation; the managed report-layout shell-summary smoke baseline now matches the current shared summary counts plus the live Spanish/Portuguese `TrueType` catalog wording, so the targeted offscreen Mono extraction for that assertion cluster no longer fails after the harness packaging fix; the managed report-settings smoke baseline now also materializes and gates shared `BOTMARGIN` selection/update/localization coverage alongside the earlier top/left/right margin checks; the mounted real-sample native object-action baseline now also includes supported `resize-object` command/reopen/undo coverage on copied `invoice.frx` / `cust.lbx` assets, and now also includes real `STYLE3V.FRX` horizontal/vertical `distribute-object` coverage through the public undo API while proving only the primary `FRX` bytes change and the `FRT` sidecar stays untouched; the debug pipeline now treats generated `app.cfdebug` files as first-class contracts through `debug_manifest_version` on the implicit `--debug` startup path while ordinary runtime-manifest validation stays unchanged; and the Linux offscreen `Copperfin.DesignerSmokeTests` packaging baseline now carries the resolved managed dependency set instead of aborting later on missing `System.Memory` / `System.Text.Json`-class loader faults. The runtime path now treats `FULLPATH()` absoluteness portably, runtime-surface host probes and portable-core file helpers resolve relative VFP backslash paths portably, and the native visual-control surface now carries first-pass built-in `TabIndex` and `TabStop` parity for the documented same-session control set. Continue those lanes only where they remove real duplication, stale evidence drift, or host-specific contract leaks rather than shuffling names pointlessly. The `#113`, `#3217`, `#110`, `#23`, and E3 evidence baselines remain as described below; keep the native runtime path focused on prompt-sized common-in-the-wild VFP gaps, and continue closing stale shipped bug-hunt children when the current tree and focused coverage already prove them.
- Next action: `#4090` implementation and local cross-compiler/sanitizer validation are complete; collect exact-commit hosted Linux/macOS plus focused Windows MSVC evidence, then close it and select the next prompt-sized child from live MVP issue state. Mounted VFP9 media at `/run/media/rich/VFPPROD1`, extracted VFP source at `/tmp/vfpsource_3217/VFPSource`, and `~/Downloads/FFC-master.zip` remain available for primary-source evidence.
- Durable CI note: keep required-check identity in the thin platform workflow wrappers, not in a reusable-workflow call chain that changes displayed job names. The wrappers own event triggers, platform runner, timeout, least-privilege permissions, immutable checkout, and concurrency keyed by platform plus event name plus PR/ref so a manual dispatch cannot cancel a push or pull-request run. The local composite action owns only shared setup/configure/build/full-CTest commands. Keep configure, bounded `--parallel 2` compilation, and CTest on the same runner; never upload, download, or reuse a CMake build tree as a release artifact. Native release readiness must require Linux, macOS, and Windows, while focused product workflows retain their own identities and intended product/test artifacts.
- Durable package note: resolve a relative PJX `HOMEDIR` from the project/document directory before testing whether it is a usable directory. Never let process CWD select a same-named relative home or enter generated working-directory/source-root metadata. Accept slash and backslash spellings, fall back to the PJX directory when the resolved home is missing or not a directory, and preserve existing absolute, current-drive-rooted, Windows drive-designator, and file-valued behavior.
- Durable DBF note: raw descriptor collision identity is the trimmed byte string Copperfin actually serializes, truncated to the current 11-byte descriptor span and folded only for ASCII case. Keep validation and serialization on the same helper and reject collisions before any staged write. This is corruption prevention, not VFP9 long-field-name support: `#4034` owns the documented 10-byte free-table limit and DBC logical-to-physical mapping.
- Durable package note: `output_root/<sanitized-project>` is generator-owned and must be replaced as one logical package; caller-owned siblings under `output_root` are not. Repeated materialization uses same-parent `<package>.copperfin-previous` as last-known-good recovery state, `<package>.copperfin-previous.owner` as its ownership proof, and `<package>.copperfin-materializing` as the interruption marker published before a fresh package tree is created. Owner/marker metadata is written to a unique same-directory temp file and renamed into place; only the authoritative names are read during recovery. Marker and owner must be direct regular files containing the exact versioned package identity, and the backup must be a direct directory, before reserved sibling paths are treated as Copperfin-owned; unrecognized, partial-temp, or symlink collisions are not followed or deleted. Normal failures remove the partial tree and marker, restore the backup first, then remove owner metadata; restart recovery trusts an owned backup over an owned-marker partial tree, while a complete unmarked package wins over an owned stale cleanup backup.
- Durable package note: resolve a selected PJX startup primary exact-first, then accept case-insensitive recovery only when every path component has one unique match. Preserve the matched directory-entry spelling in the package-relative staged path and both runtime/debug startup identities. Ambiguous or missing selected primaries and missing, ambiguous, or failed selected xAsset sidecars are package failures, never optional-asset warnings; route human failures through localization catalogs while keeping machine-readable package contracts invariant.
- Durable package note: for security-enabled package materialization, validate trust properties of the caller-supplied runtime-host source path before canonicalization. In particular, require `is_absolute()` on the original path before `weakly_canonical(...)`; otherwise an existing relative path becomes CWD-dependent trusted input. Canonicalization may normalize an already absolute path, but it must not manufacture the property being validated.
- Durable package note: packaged `startup_source` and executable payload paths must preserve their recorded package-relative identity. Prefer the current manifest-root rebind over any still-existing recorded build-root path, never restore basename-only or process-CWD fallback for executable content, reject strict relative escapes plus runtime-manifest absolute startup paths and startup-item roots outside the package, and normalize serialized Windows separators for rebinding and diagnostic basenames. Keep the separate `app.cfdebug` source-provenance contract able to execute its existing absolute source and working-directory paths, keep legacy runtime-manifest `startup_item` fallback package-relative and non-escaping, and preserve localized human diagnostics without localizing manifest keys, `status: error`, or exit code `4`.
- Durable package note: strict `app.cfmanifest` paths must reject every package-internal symlink, mount crossing, or Windows reparse component while allowing the package root itself to be reached through a deployment symlink/junction. Security verification and execution must consume the same direct-file byte snapshot for startup PRGs, verified includes, bridge source, xAsset primary/companion files, and generated bootstrap source; a matching `extension_payload` digest must never substitute for the copied-`asset` digest required by security startup. Every copied SCX/VCX/FRX/LBX/MNX asset must have its inferred memo sidecar admitted through the verified extension-payload set before `runtime.start`, with missing/undigested/redirected companions rejected through invariant security exit/audit contracts. Preserve logical package paths in debugger output, keep machine fields invariant, and do not apply these package restrictions to intentional external `app.cfdebug` source paths.
- Durable package note: public startup/debug source identity is the admitted logical spelling, not the canonical physical spelling used to verify and read bytes. Add deployment-logical or startup-namespace lookup keys only after strict containment, identity, and digest verification has produced the immutable snapshot they reference; never read or trust an alias independently. Preserve a rootless absolute manifest spelling only after strict admission succeeds, while generated/package-root paths continue to rebind into the deployment namespace. A package root itself may be reached through a deployment symlink/junction for startup and missing audit-output admission, but redirects beneath that trust root remain forbidden.
- Durable package note: schema-3 manifests use exactly one invariant `data_policy=package_writable`. Copied non-startup DBF assets are writable data, and their FPT/CDX/IDX/NDX/MDX companions must be completely declared through `data_payload=<path>|package_writable|<seed-sha256>` records. Seed digests are provenance, not immutable launch hashes; direct physical containment and complete membership remain mandatory on every security launch. Keep PRG/xAsset and DBC/DCT/DCX inputs immutable. Security-enabled `OPEN DATABASE` must consume the verified exact DBC/DCT/DCX byte snapshots already admitted by the manifest and must not execute stored procedures, triggers, validation rules, or database events without a separate trust-design slice.
- Durable package note: case-insensitive companion lookup must preserve the actual full directory-entry filename spelling in the staged destination and every manifest/debug path. Prefer an exact filename match, accept only one casefold fallback, and fail closed through `Runtime.Package.Error.AmbiguousCompanionPath` when multiple case-distinct entries match. Never let Windows' case-insensitive `exists()` response turn inferred lowercase SCT/VCT/MNT/FRT/LBT, DBF companion, or DCT/DCX names into the package identity.
- Durable runtime note: database lifecycle state is heap-backed and scoped by VFP data session. Preserve first-open `EXCLUSIVE` / `SHARED` and `NOUPDATE` settings when a DBC is reopened, keep `DBC()` limited to the current full path, keep `DBUSED(cDatabaseName)` limited to the documented current-data-session lookup, and do not add an undocumented `DATABASE()` alias.
- Durable runtime note: VFP string-plus and string-minus concatenation require character operands on both sides. Preserve ordinary `+` concatenation and `-` trailing-space relocation only for character pairs; mixed character/non-character operands must raise catalog-routed Error 107 prose, emit invariant `runtime.error` metadata, and remain continuable without recursive host-stack growth. Numeric `+` and supported Date/DateTime arithmetic retain their typed paths.
- Durable runtime note: VFP `SET EXACT ON` disregards trailing spaces only; leading spaces, tabs, NUL bytes, and non-space length differences remain significant. Apply the same rule to ordinary equality and shared function consumers such as `INLIST()`. `SET EXACT OFF` intentionally compares only until the end of the right operand and is therefore operand-order-sensitive; do not "symmetrize" it.
- Durable portability note: POSIX host-code-page fallback must parse locale values structurally, never by collecting arbitrary digits. Use the codeset component after `.` and before `@`; preserve `nl_langinfo` then `LC_ALL` / `LC_CTYPE` / `LANG` precedence, deterministic fallback 1252, Windows ACP/OEMCP behavior, and the separate VFP conversion-code-page allowlist.
- Durable portability note: Windows drive and UNC FULLPATH normalization must remain host-independent and root-locked, including extended UNC/drive forms. Preserve unsupported Windows device namespaces byte-for-byte; `\\.\\pipe` and `\\?\\GLOBALROOT` are not ordinary UNC shares and must not undergo hierarchical dot-segment reduction.
- Durable portability note: portable-core SQL/workspace/document token matching is byte-oriented and ASCII-only. Fold only ASCII letters, trim only ASCII whitespace, recognize Windows drive letters only in `A-Z` / `a-z`, and preserve every byte `>= 0x80` unchanged. Treat high bytes as identifier bytes for SQL boundaries so ASCII keywords cannot match inside UTF-8 tokens; this is deterministic byte preservation, not Unicode-aware case folding. Studio sidecar recovery owns the companion leaf name: prefer exact spelling, then return the actual case-folded directory entry rather than a synthesized lowercase extension.
- Durable runtime note: array parameter references are frame-local aliases to canonical caller storage, not copied arrays and not global entries keyed by callee parameter names; scalar references are live aliases rather than return-time-only copies. Preserve VFP9's call-mode distinction: `DO ... WITH` procedure identifiers remain reference-default, while UDF calls follow runtime-session-wide `SET UDFPARMS` with VALUE as the default, explicit `@` forcing reference, and parentheses forcing value. A bare array in VALUE mode contributes only element 1; nested scalar/array forwarding must retain ultimate caller storage identity, including `m.`-qualified forms. Keep this state independent between runtime sessions and shared across VFP data-session changes. `#4074`/`#4090` remove native-stack recursion from all UDF calls reached within `RETURN`; `#4068` owns the remaining non-`RETURN` expression contexts, and `#3871` owns array declaration scope/lifetime.
- Durable runtime note: when a pending statement has a statement-specific cancellation contract, the outer iterative dispatcher must defer the cancellation flag to that handler. In particular, SLEEP must check cancellation before evaluating its duration and during the wait loop so localized prose, source metadata, transaction rollback, and machine events remain deterministic; non-SLEEP statements without a specialized path keep the generic async-cancel behavior.
- Durable portability note: runtime paths stored in narrow `std::string` buffers must use an explicitly narrow separator; do not pass `std::filesystem::path::preferred_separator` directly because its type is `wchar_t` under Windows. Every runtime translation unit that includes `windows.h` and uses the C++ standard-library `min` / `max` APIs must define `NOMINMAX` before that include so SDK macros cannot leak into qualified calls.
- Durable Windows stack note: keep the shared 16 MiB linker reserve on both the production Studio host and the large direct shared-model test executable until their oversized dispatch/test bodies are structurally decomposed under `#2564`. Apply the reserve through `copperfin_apply_windows_stack_reserve(...)` so MSVC and MinGW use one contract; do not treat the larger process reserve as permission to reintroduce recursive PRG execution or unbounded host-stack growth.
- Durable path-test note: a VFP logical backslash path is not always a legal synthetic host filename. On Windows, exercise backslash behavior through a real nested native path; on POSIX, retain explicit backslash-bearing filename fixtures where the contract requires proving VFP-aware parsing. Do not make a cross-platform test attempt to create `E:\...` as one filename on Windows or reinterpret a real drive/UNC path as POSIX text.
- Durable portability note: `FULLPATH()` must classify Windows drive roots, UNC roots, and POSIX leading-slash roots independently of the current host filesystem. Preserve drive/UNC spellings, normalize VFP backslashes plus lexical dot segments in POSIX-rooted paths to portable forward-slash form, and use host `std::filesystem` relative resolution only after those cross-host absolute forms have been excluded.
- Durable runtime note: the trim-family baseline is intentionally space-only. `ALLTRIM()` now trims only `CHR(32)` in the ordinary runtime path, and the shared index-expression evaluator now applies the same space-only rule for `ALLTRIM` / `LTRIM` / `RTRIM` instead of using `std::isspace`; tabs, CR, LF, and other control whitespace remain significant unless a caller explicitly removes them by some other means.
- Durable runtime note: keep the native list-control single-cursor `Requery()` SQL/query-file path treating positive numeric `ORDER BY` tokens as projected-column ordinals when they fit the materialized row shape, and keep `SELECT DISTINCT` collapsing duplicate projected rows before refreshed list state is published. Preserve the existing row-order, `ListCount`, `DisplayValue`, selection, cursor-position, event, and stack-frugal frame-machine contracts while extending that mini-parser.
- Durable runtime note: `ADDPROPERTY()` and `REMOVEPROPERTY()` must treat class-defined native default properties the same way they already treat built-in identity/runtime-surface members for helper-based mutation. Keep `RuntimeOleObjectState::default_properties` as the snapshot of helper-protected class defaults, continue excluding the built-in child `parent` reference from that snapshot, and preserve ordinary direct assignment as the writable path for live property changes.
- Durable runtime note: host-driven/session-driven cancellation should now flow through the same transactional cleanup contract regardless of whether the cancel token is observed between statements, inside `SLEEP`, or during lock-retry waits. Preserve the shared cleanup behavior: roll back the active transaction journal, reset `TXNLEVEL()` to `0`, clear command-undo journals, emit the corresponding rollback/cancel events, and keep the public `PrgRuntimeSession::request_cancel()` hook narrow rather than turning it into a broader mutable execution-control surface without a specific issue driving that expansion.
- Durable runtime note: native event/member re-entrancy guards must stay exception-safe. Any path that inserts an `active_native_event_keys` entry for `object_handle:event_name` before invoking delegates, method bodies, or property accessors must release that key via RAII on every exit path, including propagated handler faults, so later explicit `RAISEEVENT()`, direct method dispatch, and property-read/property-write delegate dispatch are never silently disabled by a leaked active key.
- Durable runtime note: keep `INSERT INTO ... SELECT` on the shared query-materialization path rather than growing a second SQL evaluator. Materialize all source rows before mutating the target so self-insert commands remain finite, preserve selected/source cursor positions when those cursors are not also the mutation target, route target conversion through the existing `INSERT ... VALUES` field-write contract, and restore both persisted and in-memory target state when any row in the command fails.
- Durable runtime note: matched destination-field failures in `APPEND FROM ARRAY` must abort the command and flow through the command-undo rollback path; never treat a real DBF writer failure as a skippable column. After rollback, restore the cursor's record count, position, BOF/EOF/found flags, preserve the localized underlying writer diagnostic, and emit neither a success event nor an undo entry for the failed command.
- Durable runtime note: native DBF `APPEND FROM` may silently skip a source field only after descriptor matching proves that field is absent from the destination. A failed write to a matched field is a command failure and must preserve the localized writer diagnostic. Keep the shared file-based APPEND undo guard restoring cursor count, position, found, BOF/EOF, and active-order state whenever it rolls persisted files back.
- Durable runtime note: `IIF()` and `ICASE()` must remain lazy special forms, not ordinary eagerly evaluated functions. `IIF()` must evaluate the condition first and skip the untaken branch entirely; `ICASE()` must evaluate condition/result pairs in order, skip untaken result branches, treat `.NULL.` conditions as false, and return `.NULL.` when no condition matches and no otherwise result is supplied. Preserve those guarantees for guard idioms that would otherwise fault or trigger side effects on skipped branches.
- Durable runtime note: `EVL()` is now first-class and should continue using the runtime's `EMPTY()`-style emptiness rules, not `NVL()`'s null-only rule. Empty strings, zero numerics, false logicals, null/empty values, and typed empty values already representable in the runtime should all route to the fallback argument; non-empty values should preserve the original first argument.
- Durable runtime note: keep the first-pass `SET TEXTMERGE` contract session-scoped. Both bare `SET TEXTMERGE DELIMITERS` and terminal-`TO` `SET TEXTMERGE DELIMITERS TO` must reset to `<<` / `>>`; the latter is used by Microsoft-shipped `GENMENU.PRG` even though the help emphasizes the bare form. Single-argument delimiter updates should continue using the same token for both sides, `TEXTMERGE()` without explicit delimiter arguments should continue honoring the current session delimiters, plain `TEXT ... ENDTEXT` should continue honoring `SET TEXTMERGE ON`, and the explicit `TEXTMERGE` keyword should continue forcing merge evaluation even when `SET TEXTMERGE` itself is OFF.
- Durable runtime note: bare `SET FILTER TO` is the documented VFP filter-clear form and must remain equivalent to `SET FILTER OFF` for the selected or `IN`-targeted work area. Parse terminal `TO` through the shared SET-value helper; never require a trailing space after `TO`, store literal `TO` as the filter expression, localize invariant event details, or change machine-readable `runtime.filter: OFF` behavior.
- Durable runtime-host note: keep the native host tolerant of the Windows-style slash aliases that the generated launcher already accepts for direct invocation. `/debug` and `/locale <value>` should continue behaving exactly like `--debug` and `--locale <value>` during parsing and locale preselection, but the localized usage/help text should continue documenting the canonical `--` tokens only.
- Durable generated-launcher note: pass every runtime-host argument through `ProcessStartInfo.ArgumentList`; do not restore handwritten quoting or a flattened `Arguments` string. Insert invariant `--manifest` plus the selected manifest first, normalize only supported slash aliases, select `app.cfdebug` only for an actual debug request when the file exists, and otherwise retain `app.cfmanifest`. Keep the package root as the host working directory, contain `Process.Start` exceptions through the localized start-failure diagnostic and exit `5`, and emit console diagnostics as UTF-8. The generated project must compile `Program.cs` explicitly because its parent package directory is also the publish output; default SDK globs can exclude the source in that topology.
- Durable runtime-host note: packaged manifest verification must hash the staged sibling runtime host using the platform-correct bundled file name (`copperfin_runtime_host.exe` on Windows, `copperfin_runtime_host` elsewhere). Keep runtime-host tests deploying the executable under its actual built file name so non-Windows verification paths remain covered instead of masked by copied `.exe` aliases.
- Durable host note: default `license.cflicense` discovery in runtime, build, and inspect hosts must use the platform-resolved running executable path, never raw basename `argv[0]` or caller CWD. Keep explicit license overrides authoritative over `COPPERFIN_LICENSE_PATH`, and keep that environment path authoritative over the resolved executable sibling without changing invariant status fields, package metadata, or signature validation.
- Durable build-host note: default runtime-host sibling discovery must use the platform-resolved running build-host image, never raw basename `argv[0]` made absolute against caller CWD. Preserve `--runtime-host` over `COPPERFIN_RUNTIME_HOST_PATH` over deployed sibling fallback, and keep process regressions proving the exact staged bytes for every branch.
- Durable package-contract note: every rewrite of the final package-root `app.cfmanifest` / `app.cfdebug` pair, including late warning or finalization updates, must use `write_runtime_manifest_pair_atomically(...)`; never restore independent `write_text_file(...)` calls for those destinations. Keep both names as distinct direct package-root leaves, retain the pinned-directory/no-reparse/no-replace admission and versioned hash-journal recovery protocol, and preserve the rule that recovery either proves the complete new pair or restores the exact previous pair before cleaning owned artifacts. Fault-injection hooks belong only in `cf_runtime_pipeline_test`; production `cf_runtime_pipeline` must remain hook-free even when tests are enabled.
- Durable package-contract note: a Windows `X:tail` project asset uses that drive's current-directory semantics on Windows and remains unavailable on non-Windows hosts. Freeze an existing source to its absolute identity during planning so later parsing, companion discovery, and materialization cannot reinterpret mutable per-drive process state; never rebind a missing drive-relative source to its PJX-local `entry.name`. Remove the drive root from package-relative identity, require the content root to be exactly the direct `package_root/content` directory, and structurally and physically re-admit every primary and companion destination before writing. Preserve relative plan spelling, reject redirects, device crossings, multiply linked leaves, ADS and Windows device aliases, and keep unsafe optional source identities out of runtime/debug/AST/IR contracts. `#4066` owns handle-pinned inner writes and `#4063` owns the outer package-root transaction race.
- Durable package-contract note: keep `app.cfmanifest` focused on staged runtime execution data and keep source-oriented build/debug provenance in `app.cfdebug`. Runtime-host execution should continue depending on package-root/content-root/working-directory/startup fields plus the stable machine-readable execution contract, without reintroducing local source/build-plan paths into the runtime manifest.
- Durable package-contract note: `app.cfmanifest` audit paths are exact package-relative output identities, not permissive provenance paths. Validate physical containment before every append, reject redirects and multiply linked leaves, and publish an immutable audit-chain update through containment-preserving handles and same-directory atomic replacement. An invalid explicit path must fail closed without modifying any external identity or silently selecting the default leaf. Keep separately allowed external source/debug provenance confined to `app.cfdebug` behavior.
- Durable package-contract note: library-output materialization must continue staging the runtime host beside generated `DLL` / `FLL` / `OCX` wrappers under the package-planned platform-correct `copperfin_runtime_host[.exe]` filename before wrapper build/finalization. Generated launchers and generated wrappers should consume that same package-planned filename instead of re-hardcoding a Windows `.exe` suffix off-platform.
- Durable package-contract note: keep build-time artifact-path provenance out of `app.cfmanifest` too. `module_definition_path`, `library_api_manifest_path`, `fll_api_manifest_path`, `fxp_token_manifest_path`, and `app_archive_manifest_path` belong in `app.cfdebug` and the dedicated sidecar artifacts, not the runtime execution manifest.
- Durable package-contract note: keep `primary_output_path` out of `app.cfmanifest` too. The requested output path belongs in `app.cfdebug` and other inspection-oriented package surfaces; runtime-host execution should not depend on it being mirrored into the runtime execution manifest.
- Durable package-contract note: keep `primary_output_materialized` out of `app.cfmanifest` too. That flag is package-build/finalization state for inspection and should stay in `app.cfdebug`, not the runtime execution manifest.
- Durable package-contract note: for library outputs, keep export-source provenance (`library_function_source`) out of `app.cfmanifest` as well. That location data belongs in `app.cfdebug` and the dedicated DLL/FLL API manifests; the runtime execution manifest should stay limited to staged execution and non-source-sensitive library contract fields.
- Durable package-contract note: keep license inspection metadata out of `app.cfmanifest` too. `license_state`, `license_type`, `license_id`, `license_licensee`, `license_seats`, `license_subscription_expires`, and `license_perpetual_max_major_version` belong in `app.cfdebug` and the dedicated `--license-status` path, not the runtime execution manifest.
- Durable package-contract note: keep feature-flag inventory out of `app.cfmanifest` too. `feature_flag=...` lines are inspection-oriented rollout/build/debug/security metadata and should stay in `app.cfdebug`, not the runtime execution manifest.
- Durable package-contract note: keep compiler-contract digest inventory out of `app.cfmanifest` too. `compiler_contract=path|sha256` lines are build-artifact provenance for AST/IR/manifests/wrappers and should stay in `app.cfdebug`, not the runtime execution manifest.
- Durable package-contract note: keep export-symbol inventory out of `app.cfmanifest` too. `export_symbol=...` lines are callable-surface inspection data and should stay in `app.cfdebug`, not the runtime execution manifest.
- Durable package-contract note: keep `library_function_*` inventory out of `app.cfmanifest` too. Per-routine arity, kind, source, parameter, and call-surface lines are callable-surface inspection data and should stay in `app.cfdebug` plus the dedicated DLL/FLL `.api` artifacts, not the runtime execution manifest.
- Durable package-contract note: keep the `security_roles=` count summary out of `app.cfmanifest` too. That role-count summary is inspection-oriented security profile metadata and should stay in `app.cfdebug` and host preview/reporting surfaces, not the runtime execution manifest.
- Durable package-contract note: keep `launcher_mode=` and `launcher_fallback=` out of `app.cfmanifest` too. That launcher-selection summary is inspection-oriented host-selection metadata and should stay in `app.cfdebug`; runtime-host binding must continue working without those fields in the runtime execution manifest.
- Durable package-contract note: keep the `.NET` policy and gateway-decision mirror out of `app.cfmanifest` too. `dotnet_enabled`, allow/denylist summaries and items, parity summaries and items, and `dotnet_gateway_*` decision diagnostics are inspection-oriented extensibility policy metadata and should stay in `app.cfdebug`, while `dotnet_story` remains the only `.NET` runtime-manifest field that the runtime host still consumes for host-facing output.
- Durable package-contract note: keep the language/AI/extensibility mirror out of `app.cfmanifest` too. `language_integration*`, `ai_feature*`, `extensibility_guardrail*`, `language_integrations`, and `ai_features` are inspection-oriented extensibility inventory and should stay in `app.cfdebug`; `dotnet_story` remains the only host-facing extensibility field that the runtime manifest still carries.
- Durable test-contract note: when a focused test executes product binaries via `$<TARGET_FILE:...>` instead of linking their code directly, add explicit target dependencies for those binaries so narrow target rebuilds cannot run stale fixtures after shared-library or manifest-contract changes. Native Studio-host process-test executables inherit this through the end-of-file directory-target rule in `tests/CMakeLists.txt`; keep new process-host tests within that naming contract or add an equivalent explicit edge, and keep genuinely self-contained targets in the explicit exclusion list.
- Durable test-helper note: shared test suites should use `tests/test_environment_support.h` directly for environment reads and scoped set/restore behavior instead of introducing fresh pass-through wrappers in suite-local support layers. Add a local alias only when a suite genuinely needs a different contract, not just a renamed forwarder.
- Durable test-helper note: `ScopedDefaultLocaleCatalogEnvironment` forces `en-US` but must preserve a caller-provided catalog root when that root contains an openable regular `en-US/strings.json`; only missing or unusable roots should fall back to ancestor discovery. Keep override-preservation fixtures outside the fallback ancestor tree and restore both locale variables exactly after every scope.
- Durable test-helper note: tests intentionally exercising a shell-command contract must pass already argument-quoted command text through `copperfin::test_support::prepare_shell_command_for_system(...)`; `run_shell_command(...)` and captured `_popen`/`popen` paths retain that centralized Windows `cmd.exe /c` adaptation. Ordinary Studio-host fixture execution must instead use `run_process_capture(...)` with direct argv, native-width Windows paths, explicit working directory, and exact raw capture. Normalize line endings only through the opt-in Studio observation helper; never make raw process capture lossy.
- Durable release-validation note: the Windows deep workflow must retain enough job time for the complete all-target MSVC build, serial native CTest, VSIX build, standalone Studio build, designer build/smoke, and artifact upload. Keep the ceiling at 90 minutes unless measured workload changes justify another explicit issue; do not skip tests or weaken gates merely to fit the retired 45-minute limit.
- Durable runtime note: logical operators are now real parser-level semantics, not post-hoc text fragments. Keep `AND` / `.AND.` binding tighter than `OR` / `.OR.`, keep both operators short-circuiting so untaken right-hand operands are parsed but not executed, and keep the full logical-expression entrypoint wired through parenthesized expressions, function arguments, array selectors, and control-flow predicates instead of dropping those contexts back to comparison-only parsing.
- Durable runtime note: keyword unary negation is parser-level too. Keep `NOT` and `.NOT.` routed through the same unary-negation semantics as symbolic `!`, and preserve the ordering where unary keyword negation is recognized before any leading-dot member-access logic so `.NOT. (...)` cannot regress into an implicit OLE/member invocation fault.
- Durable runtime note: the VFP `$` containment operator is part of the comparison layer. Keep `cSubstring $ cWholeString` evaluating as substring-in-haystack boolean containment, not as a raw-left-operand passthrough, and preserve that behavior in ordinary assignments, parenthesized expressions, and control-flow predicates.
- Durable runtime note: the common VFP alternative inequality spellings `#` and `!=` must remain aliases for `<>` in the comparison parser. They should yield boolean inequality results, not degrade into raw-left-operand passthroughs, and they must keep working in ordinary assignments and control-flow predicates.
- Durable runtime note: the VFP exponentiation spellings `^` and `**` now live in a dedicated power tier under unary operators. Preserve representative precedence over multiplication and preserve unary-negation behavior by keeping exponentiation below the unary parser entrypoint and above the multiplicative layer.
- Durable runtime-host note: `RuntimeHost.Launch.Note.CompatibilityLauncher` is a user-facing fallback explanation for failed non-PRG xAsset bootstrap materialization, not a blanket statement that `SCX/VCX/FRX/MNX/LBX` startup families are unsupported. Keep machine-readable output keys stable, keep the detailed bootstrap error surfaced as a separate note, and localize any future wording changes through the shared catalogs.
- Durable runtime note: date-helper parsing must stay aligned with session date settings. Any helper that consumes ordinary runtime date/datetime text values should route through the `SET DATE` / `SET MARK` aware parse helpers rather than the legacy MDY-only parser, or DMY/YMD sessions will silently turn valid date values into `0` / empty results in adjacent helpers.
- Durable runtime note: `JUSTDRIVE()` must parse Windows path text independently of the host filesystem. Recognize a complete extended UNC root as namespace marker plus server plus share (`\\?\\UNC\\server\\share`), preserve the input spelling, and do not let the ordinary two-component UNC parser truncate it to `\\?\\UNC`. Accept only complete namespace UNC roots or explicit namespace `X:` drives; incomplete UNC, `GLOBALROOT`, and pipe namespace inputs have no drive. Keep ordinary drive/UNC, POSIX, relative, and empty contracts unchanged.
- Durable runtime note: `FORCEPATH()` must normalize the whole supplied directory before joining the source filename. Detect drive-letter and UNC directories as Windows style, single-leading-slash absolute directories as POSIX style, and mixed relative directories by their first separator; retain `/` for relative directories with no separator. Normalize separators only: do not collapse repeated separators, reduce dot segments, resolve roots, or consult the host filesystem.
- Durable security note: audit append must inspect an existing stream before opening it for append. Missing, zero-byte, and LF-empty-line-only streams may start at `GENESIS`; every nonempty record must be readable, newline-terminated, structurally five-field, and carry valid previous/observed hash shapes. Reject whitespace-mutated, malformed, or truncated tails without changing bytes. Keep full-chain cryptographic verification explicit in `verify_immutable_audit_chain(...)` rather than silently restarting or duplicating the verifier inside every append.
- Durable test-infrastructure note: `test_studio_host_json` now has built-in selection flags. Use `--list-tests` to enumerate registered names, `--filter <substring>` for a focused subset, and `--exact <name>` for one named test when validating narrow Studio-host JSON slices; keep the no-flag default running the full executable so existing CTest semantics stay intact. Porting the same pattern to other large native executables remains separate work under `#2562`.
- Durable test-infrastructure note: direct-process execution tests must use repository-built child fixtures when the launched process needs to accept arbitrary structured arguments. Do not use `/bin/true` / `/bin/false`, shell built-ins, or other host-specific commands as cross-platform `_spawnvp` / `execvp` fixtures; pass `$<TARGET_FILE:...>` through CMake and keep displayed-command assertions aligned with the product's platform quoting contract.
- Durable test-infrastructure note: assertions against serialized manifest/debug paths must pass host path strings through `quote_manifest_value(...)`. Windows serialization escapes each backslash, so concatenating raw `C:\...` text into `extension_payload=`, `data_asset=`, or `data_payload=` markers creates false platform failures even when the stored path identity is correct.
- Durable runtime note: shared `PrgValue` instances now carry explicit null state plus lightweight date/datetime string flavor metadata. Keep genuine `.NULL.` values distinguishable from uninitialized empties, keep string-backed date/time-producing functions and DBF `D`/`T` field paths tagged through ordinary copies/scatter/gather flows, and preserve `VARTYPE()` / `TYPE()` / `ISNULL()` semantics (`X` vs `U`, `D`, `T`) without widening this into a broad machine-contract change.
- Durable runtime note: `SET PROCEDURE TO <file> [ADDITIVE]` now has first-pass real helper-program semantics rather than dead SET-state text. Registered helper `.prg` files resolve through the active default directory with caller-file fallback, their routines are available to ordinary unqualified expression-level function/procedure calls plus unqualified `DO` / `CALL` / `SPAWN` routine dispatch after the caller program's own routines, `ADDITIVE` preserves first-opened-wins duplicate routine precedence, and non-additive `SET PROCEDURE TO` replaces the active helper list. Keep this on the existing iterative frame machine; do not reintroduce recursive helper dispatch.
- Durable runtime note: positional `APPEND FROM` source formats must preserve the typed order of an explicit `FIELDS a, b, c` list. For `TYPE SDF` and `TYPE DELIMITED` / `TYPE TAB` imports, including the selected SQL/result cursor DELIMITED path, treat source column N as the Nth explicitly listed field rather than the Nth field in destination-table declaration order; keep `FIELDS LIKE` / `FIELDS EXCEPT` on ordinary table-order filtering because they are pattern filters, not explicit reorderings.
- Durable runtime note: `ACOPY()` must not resize an already-existing destination array. Preserve the existing auto-create behavior for a genuinely missing target array, but when the target already exists and the requested copy would overflow it, clamp the copied element count to the target's current capacity and leave the target's shape unchanged.
- Durable runtime note: native `PageFrame.ActivePage` is now a first-pass bounded selector on top of the shipped `Pages` / `PageCount` lane. Keep it writable at runtime, keep non-empty pageframes clamped to the 1-based live page range, keep empty pageframes at `0`, and keep `GETPEM()` / `SETPEM()` / `PEMSTATUS()` / `AMEMBERS()` coherent without allowing `ADDPROPERTY()` / `REMOVEPROPERTY()` to shadow or erase the built-in surface.
- Durable runtime note: native `PageFrame.PageCount` is now a first-pass writable built-in surface on top of the shipped `Pages` collection lane. Keep it bounded to `0..99`, keep growth materializing default trailing `PageN` children in-order for empty/base and derived pageframes, keep shrink removing truncated page subtrees from exported runtime state instead of only detaching them, keep the `Pages` collection plus bounded `ActivePage` selector synchronized after every mutation, and keep direct assignment plus `GETPEM()` / `SETPem()` / `PEMSTATUS()` / `AMEMBERS()` coherent without allowing `ADDPROPERTY()` / `REMOVEPROPERTY()` to shadow or erase the built-in surface.
- Durable runtime note: numeric `EMPTY()` semantics are exact, not fuzzy. For floating numeric runtime values, only `0.0` is empty; do not reintroduce epsilon-tolerance checks that classify tiny nonzero values as empty.
- Durable runtime note: `DELETE ALL` and `RECALL ALL` are whole-table scopes, not current-record aliases. Preserve explicit `ALL` scope through parse/dispatch and route it through the shared table-scan deletion/recall path for both local and selected SQL/result cursors instead of letting it fall into the bare-current-record mutation branch.
- Durable runtime note: implicit same-area `USE` replacement is a real close, including shared-lock cleanup. When a new cursor reuses an occupied work area, release the outgoing cursor's table/record lock ownership through the same close path before overwriting the slot so later `UNLOCK` calls and later runtime sessions do not inherit leaked shared locks from the replaced table.
- Durable report-layout note: when an object overlaps more than one section, the containing section is the one whose bounds contain the object's top edge, not simply the latest later band it overlaps. Keep both the shared layout snapshot helper and the section-geometry reflow helper using that same top-edge rule so tall objects retain stable `containingSectionId` / `sectionRelativeTop` metadata and move with the correct section during band-top edits.
- Durable runtime note: `CATCH TO` inside an active `ON ERROR` handler must materialize the fault that the inner `TRY/CATCH` just caught, not the outer handler snapshot. Keep Exception-object materialization on the live `last_error_*` metadata/compatibility payload for the current caught fault, while preserving the separate ON ERROR compatibility behavior where later top-level `MESSAGE()` / `AERROR()` inside the handler revert to the original outer fault after the inner catch completes.
- Durable runtime note: `TRIM()` is a trailing-space operation, not a left trim. Keep the shared PRG string-function dispatcher routing `TRIM()` with `RTRIM()` semantics while `LTRIM()` remains the separate leading-space helper, and preserve the space-only trim baseline rather than widening this back into `std::isspace` behavior.
- Durable runtime note: `RENAME <old> TO <new>` must fail closed when the destination path is already occupied. Keep the preflight destination-exists check ahead of the filesystem rename call, surface that path through the localized `Runtime.Prg.Dispatch.Error.RenameFileTargetExists` key, and preserve both source and destination bytes on the failure path instead of inheriting host-platform overwrite semantics.
- Durable runtime note: `STUFF()` now follows VFP's insert-only boundary rule when `nStartReplace <= 0`. In that front-of-string boundary case, ignore `nCharsReplace` completely and insert `cReplacement` at the start; keep the existing replacement-length clamp behavior only for positive start positions.
- Durable DBF note: literal `"null"` text is valid data for direct string-bearing DBF fields. Keep `C`, `V`, and `Q` field writes treating `"null"` / `"NULL"` as ordinary text during create/replace flows; do not reintroduce blanket null-token sniffing on those field types unless the storage contract grows an explicit nullability channel.
- Durable DBF note: memo-backed table parsing and `PACK MEMO` now distinguish successful empty decoded payloads from unresolved memo reads. Empty-decoding `M/G/P` payloads should remain empty in `display_value`, and any memo repack path must preserve raw sidecar bytes rather than rebuilding from localized or filtered display text; if a referenced payload cannot be recovered, fail closed instead of synthesizing replacement content.
- Durable runtime note: the shared `TRANSFORM()` baseline now includes representative `@R` literal-character templates plus comma-only numeric grouping without a decimal point. Treat the `@R` separator space as syntax, not output, and keep grouped integer pictures flowing through the same `SET POINT` / `SET SEPARATOR` / `SET CURRENCY` symbol path as decimal numeric pictures.
- Durable Studio-host note: builder/editor-action/designer/toolbox child launch execution should stay argv-based, not shell-based. Keep `build_shell_command()` as display/diagnostic text only; do not route it back through `std::system()` or reintroduce shell parsing as the execution contract for child launch commands.
- Durable report-layout note: live objects that still belong to deleted sections remain live for preview-bounds purposes. Preserve deleted-section membership and deleted-section object ordering for those objects, but keep their geometry in `preview_bounds_*`; `deleted_preview_bounds_*` is for genuinely deleted bands/objects only.
- Durable visual-asset note: undo journals are versionless native local state. Bound every length/count/index conversion by the bytes that actually remain plus platform/container limits before allocating, accept the pre-grouped scalar form only at exact EOF, and reject partial count fields or trailing bytes. Do not introduce arbitrary content-size ceilings that make previously valid large journals unreadable. Preflight every target in a grouped entry before applying the first change so corrupt later selectors cannot leave partial target mutations.
- Durable report-settings note: mounted installed VFP9 `invoice.frx` / `cust.lbx` now treat missing root `LEFTMARGIN` and `RIGHTMARGIN` as memo-backed settings-record `EXPR` lines, not as direct fields. Keep the shared host and shared WinForms editor aligned on that contract: both properties should materialize through their invariant names with `fieldIndex = 6` / `sourceLineIndex = 3` provenance on those mounted samples rather than being hidden behind snapshot-presence gating.
- Durable runtime note: `CONTINUE` now follows VFP's locate-resume semantics again; it is no longer accepted as a loop-control synonym. Use `LOOP` for `FOR` / `DO WHILE` / `SCAN` continuation, and use `CONTINUE` only to resume the active `LOCATE FOR` search on the current work area.
- Durable runtime note: the shipped native list-control baseline now includes single-column `AddItem()`, representative multicolumn `AddItem(..., nIndex, nColumn)` follow-through, builtin `Clear()` with the mounted VFP9 `RowSourceType = 0` gate, read-only `ListCount`, representative writable `List(nIndex)` / `List(nIndex, 1)` plus representative writable multicolumn `List(nIndex, nColumn)` and `List(NewIndex, nColumn)` row-churn follow-through, representative writable `ListItem(nItemID)` / `ListItem[nItemID]` plus multicolumn item-addressed writes keyed by stable item ID rather than display order, `RemoveItem()`, representative `RemoveListItem(nItemID)`, read-only `NewIndex`, read-only `NewItemId`, representative multicolumn `AddListItem()`, writable `ListItemID` selection coherence, representative `ListBox.Selected(nIndex)` row-selection coherence, representative `ListBox.MultiSelect` selection-mode coherence, representative `SelectedID(nItemID)` item-id selection coherence, representative `Sorted` row-order maintenance, first-pass selected `Value` coherence driven by `BoundColumn`, first-pass builtin `BoundTo` plus builtin `ListBox.BoundColumn` / `ListBox.ColumnCount` / `ListBox.ColumnWidths` reflection, first-pass builtin `ListBox.ControlSource` reflection, first-pass `BoundTo = .F.` source-type selection semantics keyed from the bound `ControlSource` value kind when available, first-pass direct-selection and `Requery()` write-back through bound memory-variable and cursor-field `ControlSource` targets, and first-pass `Requery()` refresh for value-list, array, alias, representative single-cursor fields, and representative single-cursor SQL/query-file row sources over the same-session runtime row state for native `ComboBox`/`ListBox` controls, including representative single-cursor and joined query refresh through bare source designators plus representative SQL-style source aliases, bare `JOIN`, explicit `INNER JOIN`, `LEFT JOIN`, and `LEFT OUTER JOIN`. It is still intentionally first-pass: broader joins/full-SQL query parity, broader list-control binding refresh parity, and non-direct write-back edge cases are not claimed complete, so this remains a focused common-in-the-wild runtime baseline rather than a blanket claim of every VFP list-control edge case.
- Durable runtime note: ordinary native PRG self-calls now late-bind correctly from inherited method bodies. `THIS.Method()` and bare self-calls made on the live current object resolve from the object's most-derived runtime class, including across external-base PRG inheritance, while `DODEFAULT()` remains the explicit base-relative dispatch path that starts above the currently executing method's defining class.
- Durable runtime note: same-session native PRG objects now carry first-pass `ReadExpression()` parity for live property sources. Preserve class-body property expression text, keep direct runtime assignment reads on the stack-frugal frame path when source expression text is available, keep reflective method discovery coherent through `GETPEM()` / `PEMSTATUS()` / `AMEMBERS()`, and treat missing properties deterministically as empty-string reads instead of fabricating method bodies or mutating machine-readable runtime identifiers.
- Durable runtime note: VFP help explicitly documents the `OLERequestPendingTimeout = 5000` and `OLEServerBusyRaiseError = .F.` defaults, but the `OLEServerBusyTimeout` topic documents behavior without an explicit numeric default. Copperfin currently seeds a representative `OLEServerBusyTimeout = 5000` value on native OleControl hosts to keep timeout-policy inspection/programmatic-control paths deterministic until a stricter VFP-default citation is available.
- Validation note: on Linux, the full `mono vsix/Copperfin.DesignerSmokeTests/bin/Release/net472/Copperfin.DesignerSmokeTests.exe` run is still not a clean release gate because the unmanaged Mono/libgdiplus tail remains unstable and the suite still lacks targeted filtering, but the earlier managed-assembly packaging gap is now closed. The `net472` output folder now carries `System.Memory.dll`, `System.Text.Json.dll`, and the rest of the resolved managed dependency set, so offscreen Mono probes can progress into existing behavioral assertions instead of aborting on later loader faults. `#3824` used `git diff --check`, `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -c Release`, populated-output inspection, and an offscreen Mono probe that now reaches the existing report-layout shell-summary failures after the earlier localized/project-workspace coverage. Treat Linux Mono smoke as advisory coverage for these managed slices until the remaining assertion noise and filtering gap are closed. Earlier focused validation remains as described below for the shared designer/runtime workflow families.
- Durable report-settings note: the shared managed deleted-settings seam now explicitly carries `BOTMARGIN` alongside `TOPMARGIN`, left/right margins, and the memo-backed printer/page-setup fields. Keep deleted report/label settings selection, update, localization, and machine-contract ordering coverage aligned on the invariant `BOTMARGIN` property name rather than relying on the separate host-only missing-bottom-margin seam.
- Validation note: `#3828` used `git diff --check` and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -c Release`. The focused managed designer-smoke project now compiles on this box with deleted report-settings `BOTMARGIN` selection/update/localization/order coverage added to `SmokeDeletedReportSettingsExplorerSelection()`, closing the remaining managed/shared gap for the deleted bottom-margin property without changing host JSON names or localization keys.
- Validation note: `#3755` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_paper_dimensions_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_paper_dimensions_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with real `PAPERLENGTH` / `PAPERWIDTH` add-reopen-clear lifecycle checks. The managed designer-smoke project also compiles on this box, while `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` still needs Windows/Visual Studio SDK validation because it fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3756` used `git diff --check`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_sort_settings_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_sort_settings_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with the existing native `TAG` add-reopen-clear lifecycle checks, and the managed designer-smoke project compiles on this box with the new installed-VFP9 host-backed/property-grid `TAG` add/undo expectations. As with `#3755`, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3757` used `git diff --check`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_paper_dimensions_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_paper_dimensions_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The focused mounted-media executable still passes on Linux against the mounted VFP9 sample set with the native `PAPERLENGTH` / `PAPERWIDTH` add-reopen-clear lifecycle checks, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with `#3755` and `#3756`, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3758` used `git diff --check`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_sort_settings_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_sort_settings_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The focused mounted-media executable still passes on Linux against the mounted VFP9 sample set with the native `TAG` add-reopen-clear lifecycle checks, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with `#3755` through `#3757`, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3759` used `git diff --check`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_column_setup_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_column_setup_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The focused mounted-media executable still passes on Linux against the mounted VFP9 sample set with the native `COLS` / `COLWIDTH` / `COLSPACING` add-reopen-clear lifecycle checks, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with `#3755` through `#3758`, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3760` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_page_setup_settings_round_trip -j4`, and `ctest --test-dir build -R '^test_studio_host_real_sample_page_setup_settings_round_trip$' --output-on-failure`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native `ORIENTATION`, `PAPERSIZE`, and `COLOR` update-reopen-command-undo checks on copied `invoice.frx` and `cust.lbx`. As with the other native-only mounted-media slices, no VSIX build was needed for this harness extension.
- Validation note: `#3761` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_direct_margins_round_trip -j4`, and `ctest --test-dir build -R '^test_studio_host_real_sample_direct_margins_round_trip$' --output-on-failure`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native `TOPMARGIN` / `BOTMARGIN` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, while proving the primary `FRX/LBX` bytes change and the `FRT/LBT` sidecars remain unchanged through those direct-field edits. As with the other native-only mounted-media slices, no VSIX build was needed for this harness extension.
- Validation note: `#3762` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_left_margin_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_left_margin_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `LEFTMARGIN` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3763` used `git diff --check`, `cmake --build build --target copperfin_studio_host test_studio_host_real_sample_right_margin_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_right_margin_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `RIGHTMARGIN` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3764` used `git diff --check`, `cmake --build build --target test_studio_host_real_sample_driver_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_driver_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `DRIVER` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3765` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_device_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_device_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `DEVICE` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3766` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_output_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_output_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `OUTPUT` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project compiles on this box with the new installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3767` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_defaultsource_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_defaultsource_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `DEFAULTSOURCE` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project still compiles on this box with the existing installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3768` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_printquality_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_printquality_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `PRINTQUALITY` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project still compiles on this box with the existing installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3769` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_yresolution_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_yresolution_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `YRESOLUTION` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project still compiles on this box with the existing installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3770` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_ttoption_round_trip -j4`, `ctest --test-dir build -R '^test_studio_host_real_sample_ttoption_round_trip$' --output-on-failure`, and `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -p:EnableWindowsTargeting=true`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `TTOPTION` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, and the managed designer-smoke project still compiles on this box with the existing installed-VFP9 property-grid add-then-explicit-clear expectations. As with the other managed mounted-media slices, the VSIX project itself still needs Windows/Visual Studio SDK validation because `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -p:EnableWindowsTargeting=true` fails here with a missing `Microsoft.VisualStudio.Validation` task dependency.
- Validation note: `#3771` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_ascii_round_trip -j4`, and `ctest --test-dir build -R '^test_studio_host_real_sample_ascii_round_trip$' --output-on-failure`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `ASCII` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, preserving the existing mounted page-setup harness ownership for `ORIENTATION`, `PAPERSIZE`, and `COLOR`. As with the other native-only mounted-media slices, no VSIX build was needed for this harness addition.
- Validation note: `#3772` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_collate_round_trip -j4`, and `ctest --test-dir build -R '^test_studio_host_real_sample_collate_round_trip$' --output-on-failure`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `COLLATE` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, preserving the existing mounted page-setup harness ownership for `ORIENTATION`, `PAPERSIZE`, and `COLOR`. As with the other native-only mounted-media slices, no VSIX build was needed for this harness addition.
- Validation note: `#3773` used `git diff --check`, `cmake -S . -B build`, `cmake --build build --target test_studio_host_real_sample_copies_round_trip -j4`, and `ctest --test-dir build -R '^test_studio_host_real_sample_copies_round_trip$' --output-on-failure`. The new focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with native memo-backed `COPIES` add-reopen-clear checks on copied `invoice.frx` and `cust.lbx`, preserving the existing mounted page-setup harness ownership for `ORIENTATION`, `PAPERSIZE`, and `COLOR`. As with the other native-only mounted-media slices, no VSIX build was needed for this harness addition.
- Validation note: `#3720` used `git diff --check`, `cmake --build build --target test_studio_host_real_sample_column_setup_round_trip -j4`, and `ctest --test-dir build -R test_studio_host_real_sample_column_setup_round_trip --output-on-failure`. That focused mounted-media executable now passes on Linux against the mounted VFP9 sample set with the added `COLWIDTH` lifecycle checks.
- Validation note: `#3687` used `git diff --check`, `cmake --build build --target test_dbf_table -j4`, and `ctest --test-dir build -R '^test_dbf_table$' --output-on-failure`. The focused DBF executable now also proves zero-block-size memo sidecars fail closed during parse instead of being interpreted with a byte-sized memo stride.
- Validation note: `test_prg_engine_control_flow`, `test_prg_engine_data_io`, and `test_prg_engine_functions` all pass both by direct executable invocation and by serial `ctest` on 2026-07-09. Earlier apparent `ctest` failures for `test_prg_engine_control_flow` were caused by launching the same large native test binary concurrently through both direct execution and `ctest`, while many cases reuse fixed temp roots under the system temp directory; avoid treating concurrent duplicate invocations of the same native test executable as a meaningful validation signal.
- Validation note: the aggregate `test_studio_host` target is buildable again after `#3507` (`157da636`), so narrow Studio-host slices can use either that aggregate target or the split host executables as appropriate instead of assuming the old duplicate-`main` / unresolved-helper link failure is still present.
- Repository licensing changed: prior versions were GPL-3.0-only; new versions are dual-licensed (free individual/non-commercial use plus a paid Commercial License). See `LICENSE.md`, `MIGRATION_NOTICE.md`, and `LEGAL_FAQ.md`. A license-status mechanism (`cf_licensing`, `--license-status`, `app.cfmanifest`/`app.cfdebug` stamping, Studio JSON `licenseProfile`) was added in commits `d94b31f7`/`21177bd9`; it is display/audit-only and never gates functionality.
- `#3211` tracks two pre-existing bugs discovered while adding the licensing work above (unrelated to licensing itself, both confirmed pre-existing via A/B revert): the Windows `/sdl` test-helper `getenv` failure is now addressed in the touched core tests via a shared Windows-safe environment helper, and Windows builds now reserve a 16 MiB stack for `copperfin_studio_host` through target link settings. Root cause (see issue comments) and the 16 MiB fix are both independently verified: Debug and Release `copperfin_studio_host.exe` now both handle `--json` against a trivial `.scx` and the full `solution.pjx` sample with exit code 0, and `test_licensing_status` (22/22) still passes after Codex's `test_environment_support.h` refactor. The underlying oversized Studio host entrypoint (`apps/copperfin_studio_host/main.cpp`, ~4175 lines, ~150+ inlined property-setter blocks not yet split into `try_handle_*` helpers) still merits a later structural refactor as the real fix; the stack reservation is a verified-effective mitigation, not that refactor.
- PRG-level call-stack frugality (a founding requirement) was investigated in depth per user request: `DO`/`CALL`/`DO FORM` are confirmed genuinely frugal -- an explicit heap-backed frame stack inside an iterative `run()` loop (`src/runtime/prg_engine.cpp`), with a tested, configurable `max_call_depth` guardrail (default 1024, `prg_engine_session.inl:1652`) that raises a catchable error before any native stack risk. `#3216` is now covered through that same stack-frugal frame machine rather than a recursive side path, and `#3217` has been refreshed from a stale missing-feature report into the active parent lane for remaining native-OOP/runtime residuals. Treat that lane as common-in-the-wild VFP runtime-parity work, not a fringe wishlist item.
- Environment-access hardening parent `#3214` is closed. Preserve its shared Windows-safe variable/path helpers and open a new prompt-sized child under the live hardening lane only when fresh evidence identifies a remaining direct-access defect.
- E2/#23 remains open mostly for evidence-audit/closure cleanup. Do not open more E2 wrapper work unless new APIs or fresh regression evidence show a real implementation gap.
- Localization/release-readiness slices under `#2348`/`#113` have shipped through `#2352` and `#1856`; continue them only if live issue state makes them higher priority than E3/#24.
- Shell/UI backlog guidance is now captured in `#3050` (platform UI standards for native hosts), `#3051` (dockable Command and terminal surfaces, including the default bottom-tabbed VSIX Command window behavior), and `#2998` (future curated standalone extension support without loading the Copperfin VSIX). Those three issues already cover the current durable UI direction; reuse them rather than opening duplicate backlog items unless new scope appears beyond the existing host-shell, docking, terminal, extension-compatibility, or localization-ready shell-text boundaries.
- Durable VSIX UI note: `#2996` now has a shipped first-pass host-mode chrome compaction in the shared WinForms editor. Keep the Visual Studio document-host path visually compact by suppressing the standalone title/subtitle/guidance banner and using tighter outer padding there, while preserving the richer localized title chrome for standalone Studio mode where the control is the primary shell surface.
- Durable managed-launch note: project Build/Run/Debug and detached launcher creation must stay on the shared wrapper-aware `ProcessStartInfo` path. On Windows, configured `.cmd` / `.bat` hosts use `COMSPEC /d /c`; `.exe` and extensionless hosts stay direct, and non-Windows paths remain direct. Preserve explicit empty arguments, invariant CLI/output/environment contracts, and catalog-routed start failures with retained host diagnostics; do not let `Process.Start` exceptions escape into VSIX or standalone UI workflows.
- Validation note: `#2996` used `git diff --check`, `dotnet build vsix/Copperfin.DesignerSmokeTests/Copperfin.DesignerSmokeTests.csproj -c Release`, and direct Mono exact-smoke execution of `SmokeLocalizedAssetEditorChrome`, `SmokePseudoLocalizedAssetEditorChrome`, and `SmokeLocalizedHostModeChromeCompaction`. The focused managed smoke coverage now proves standalone-localized title chrome still appears when `EmbeddedStudioShell = true` and that the default Visual Studio host mode hides that standalone banner while keeping localized behavior intact. `dotnet build vsix/Copperfin.VisualStudio/Copperfin.VisualStudio.csproj -c Release` still fails on this Linux box with the pre-existing missing `Microsoft.VisualStudio.Validation` SDK-task dependency, so Windows/Visual Studio remains the required validation lane for full VSIX packaging.

## Required Reading

Read only what is needed for the chosen slice:

- `agents.md` for operating rules and safety traceability.
- `docs/23-phase-a-dependency-breakdown.md` for current lane status and historical dependency evidence.
- `docs/28-repository-ontology.md` when the slice needs current repo-structure grounding instead of aspirational architecture summaries.
- `docs/22-vfp-language-reference-coverage.md` when the slice touches VFP/runtime language coverage.
- `docs/safety/triage-rubric.md` for feedback triage.
- `docs/safety/test-coverage-gap-analysis.md` when adding tests.
- Runtime, designer, or host files directly relevant to the selected child issue.

`remaining-work.md` is intentionally deprecated as an active planning source. `issues.txt` is a dated local snapshot, not planning authority; verify live GitHub state before choosing work.

## Validation And Updates

- Run focused tests for the selected slice plus `git diff --check`.
- For safety-relevant documentation, keep `DQ-*`, `DV-*`, and `HZ-*` traceability linked to `docs/safety/hazard-register.md`.
- Before a release tag, run `scripts/validate-safety-traceability.ps1` or the Safety Traceability Gate workflow against the intended release issue set and archive the report.
- Update `agent-handoff.md` only when the last shipped slice, current lane, or next action changes.
- Update `CHANGELOG.md` for lasting repo changes or material tracked-documentation changes.
- Shipped build-host validation slice `#3886` rejects invalid or empty `--configuration` values before plan creation. Accept only the invariant `debug` and `release` tokens after ASCII case/edge-whitespace normalization; preserve nonzero CLI failure, `status: error`, and catalog-backed prose across locales.
